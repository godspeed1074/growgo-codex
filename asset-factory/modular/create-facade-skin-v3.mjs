import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';
const root=path.resolve(import.meta.dirname,'../..'),src=decodePng(path.join(root,'test-output/visible-facade-sanity/SHOP_REFERENCE_VISIBLE_FACADE.png')),out=path.join(root,'test-output/facade-skin');fs.mkdirSync(out,{recursive:true});const crop={x:42,y:135,w:676,h:430},rgba=Buffer.alloc(crop.w*crop.h*4);let kept=0,holes=0;
for(let y=0;y<crop.h;y++)for(let x=0;x<crop.w;x++){const s=((crop.y+y)*src.w+crop.x+x)*4,d=(y*crop.w+x)*4;const nearWhite=src.rgba[s]>232&&src.rgba[s+1]>232&&src.rgba[s+2]>232;const hero=(y<30)||(y>45&&y<145)||(x>55&&x<260&&y>125)||(x>265&&x<555&&y>135)||(x>565&&y>230)||(y>365);const under=hero||nearWhite;rgba[d]=under?145:src.rgba[s];rgba[d+1]=under?83:src.rgba[s+1];rgba[d+2]=under?53:src.rgba[s+2];rgba[d+3]=255;under?holes++:kept++;}
fs.writeFileSync(path.join(out,'SHOP_REFERENCE_FACADE_SKIN.png'),encodePng(crop.w,crop.h,rgba));const mask=Buffer.alloc(crop.w*crop.h*4);for(let i=0;i<crop.w*crop.h;i++)mask[i*4]=mask[i*4+1]=mask[i*4+2]=mask[i*4+3]=255;fs.writeFileSync(path.join(out,'SHOP_REFERENCE_FACADE_SKIN_MASK.png'),encodePng(crop.w,crop.h,mask));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_FACADE_SKIN_SPEC.json'),JSON.stringify({status:'PASS_WITH_NOTES',source:'SHOP_REFERENCE_VISIBLE_FACADE.png',crop,keptPixels:kept,maskedPixels:holes,excluded:['Door','Transom','Window','Awning','Fascia','Shrub','Foundation','White background'],underlay:'GG_MAT_WALL_WARM_BROWN_001',atlasId:'GG-ATLAS-COMMERCIAL-FACADE-A',atlasVersion:'1.0.0',projection:'camera-locked front wall modules only'},null,2));
