import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';const root=path.resolve(import.meta.dirname,'../..'),src=decodePng(path.join(root,'test-output/visible-facade-sanity/SHOP_REFERENCE_VISIBLE_FACADE.png')),out=path.join(root,'test-output/per-module-wall-art'),crop={x:42,y:135,w:676,h:385},parts={LEFT:[0,197],CENTRE:[197,363],RIGHT:[363,676]},holes=[[0,0,676,48],[28,35,625,150],[72,145,245,410],[260,145,530,375],[548,220,676,430],[0,385,676,430]];const hole=(x,y)=>holes.some(([x0,y0,x1,y1])=>x>=x0&&x<x1&&y>=y0&&y<y1);for(const [name,[x0,x1]] of Object.entries(parts)){const w=x1-x0,rgba=Buffer.alloc(w*crop.h*4);for(let y=0;y<crop.h;y++)for(let x=0;x<w;x++){const s=((crop.y+y)*src.w+crop.x+x0+x)*4,d=(y*w+x)*4,h=hole(x0+x,y);rgba[d]=src.rgba[s];rgba[d+1]=src.rgba[s+1];rgba[d+2]=src.rgba[s+2];rgba[d+3]=h?0:255}fs.writeFileSync(path.join(out,`WALL_${name}_REFERENCE_ART.png`),encodePng(w,crop.h,rgba));}
