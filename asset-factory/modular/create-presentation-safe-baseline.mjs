import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';const root=path.resolve(import.meta.dirname,'../..'),src=decodePng(path.join(root,'test-output/optimized-final/SHOP_CALIBRATED_FULL_FRONT.png')),out=path.join(root,'test-output/presentation-derivative');const rgba=Buffer.from(src.rgba),alpha=Buffer.alloc(src.w*src.h*4);for(let i=0;i<rgba.length;i+=4){const inside=!(rgba[i]>244&&rgba[i+1]>244&&rgba[i+2]>244);rgba[i+3]=inside?255:0;alpha[i]=alpha[i+1]=alpha[i+2]=inside?255:0;alpha[i+3]=inside?255:0}fs.writeFileSync(path.join(out,'SHOP_PRESENTATION_FINAL.png'),encodePng(src.w,src.h,rgba));fs.writeFileSync(path.join(out,'SHOP_PRESENTATION_GAMEPLAY.png'),encodePng(src.w,src.h,rgba));fs.writeFileSync(path.join(out,'SHOP_PRESENTATION_ALPHA.png'),encodePng(src.w,src.h,alpha));
