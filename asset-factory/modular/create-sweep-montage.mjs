import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';
const root=path.resolve(import.meta.dirname,'../..'),W=320,H=220,cols=5,rows=5,o=Buffer.alloc(W*cols*H*rows*4,248);for(let i=1;i<=25;i++){const im=decodePng(path.join(root,'test-output/final-sweep',String(i),'SHOP_CALIBRATED_FULL_FRONT.png')),s=Math.min(W/im.w,H/im.h),nw=Math.floor(im.w*s),nh=Math.floor(im.h*s),ox=(W-nw)>>1,oy=(H-nh)>>1,c=(i-1)%cols*W,r=Math.floor((i-1)/cols)*H;for(let y=0;y<nh;y++)for(let x=0;x<nw;x++){const a=((r+oy+y)*(W*cols)+c+ox+x)*4,b=(Math.floor(y/s)*im.w+Math.floor(x/s))*4;o[a]=im.rgba[b];o[a+1]=im.rgba[b+1];o[a+2]=im.rgba[b+2];o[a+3]=255}}fs.writeFileSync(path.join(root,'test-output/final-sweep/SWEEP_MONTAGE.png'),encodePng(W*cols,H*rows,o));
