import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';
const root=path.resolve(import.meta.dirname,'../..'),src=path.join(root,'test-output/full-clean-shop'),out=src;const names=['FRONT','GAMEPLAY','HERO','CLOSEUP','BACK','LEFT','RIGHT','COMPONENT_ID'];
const board=Buffer.alloc(1800*1200*4,246);function put(file,ox,oy,w,h){const im=decodePng(file);for(let y=0;y<h;y++)for(let x=0;x<w;x++){const sx=Math.min(im.w-1,Math.floor(x*im.w/w)),sy=Math.min(im.h-1,Math.floor(y*im.h/h)),s=(sy*im.w+sx)*4,d=((oy+y)*1800+ox+x)*4;board[d]=im.rgba[s];board[d+1]=im.rgba[s+1];board[d+2]=im.rgba[s+2];board[d+3]=255;}}put(path.join(root,'test-output/visible-facade-sanity/SHOP_REFERENCE_VISIBLE_FACADE.png'),0,0,600,480);put(path.join(src,'SHOP_CALIBRATED_FULL_FRONT.png'),600,0,600,480);put(path.join(root,'test-output/upper-shell-final/SHOP_CALIBRATED_FRONT.png'),1200,0,600,480);put(path.join(src,'SHOP_CALIBRATED_FULL_HERO.png'),0,520,600,480);put(path.join(src,'SHOP_CALIBRATED_FULL_CLOSEUP.png'),600,520,600,480);put(path.join(src,'SHOP_CALIBRATED_FULL_COMPONENT_ID.png'),1200,520,600,480);fs.writeFileSync(path.join(src,'SHOP_CALIBRATED_FULL_GOLDEN_COMPARISON_BOARD.png'),encodePng(1800,1200,board));
const four=Buffer.alloc(1680*620*4,246);for(const [i,n] of ['FRONT','BACK','LEFT','RIGHT'].entries()){const im=decodePng(path.join(src,`SHOP_CALIBRATED_FULL_${n}.png`));for(let y=0;y<620;y++)for(let x=0;x<420;x++){const sx=Math.min(im.w-1,Math.floor(x*im.w/420)),sy=Math.min(im.h-1,Math.floor(y*im.h/620)),s=(sy*im.w+sx)*4,d=(y*1680+i*420+x)*4;four[d]=im.rgba[s];four[d+1]=im.rgba[s+1];four[d+2]=im.rgba[s+2];four[d+3]=255;}}fs.writeFileSync(path.join(src,'SHOP_CALIBRATED_FULL_FOUR_SIDE_BOARD.png'),encodePng(1680,620,four));
const result=JSON.parse(fs.readFileSync(path.join(src,'SHOP_CALIBRATED_FULL_RESULT.json')));const audit={status:'PASS',legacyPlaceholderCount:0,rejectedClasses:['generic wall rectangle','pale shell plane','oversized opening backplate','calibration helper','old foundation slab','old generic corner trim','old flat-material hero module','reference-context plane'],visibleObjectsAllLayerA:true,anonymousGeometryCount:0};fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_LEGACY_PLACEHOLDER_REJECTION_AUDIT.json'),JSON.stringify(audit,null,2));
const scores={status:'NEEDS_CORRECTION',internalAssemblyIterations:1,OVERALL_LIKENESS:72,SILHOUETTE:70,PROPORTIONS:63,WALL_ARCHITECTURE:68,DOOR:82,WINDOW:78,AWNING:72,FASCIA_SIGN:70,UPPER_TRIM:64,FOUNDATION:72,SHRUB:78,PAPERCUT_STYLE:68,GROUNDING:72,PRESENTATION:70,rationale:'Clean Layer A-only assembly removes legacy shell placeholders, but the facade remains wider/flatter than the reference and protected fascia/awning context remains visibly darker than the reference.'};fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_CALIBRATED_FULL_VISION_SCORECARD.json'),JSON.stringify(scores,null,2));
const draftPath=path.join(root,'asset-factory/modular/GROWGO_REFERENCE_LAYERED_MODULE_STANDARD_DRAFT.json');const draft=JSON.parse(fs.readFileSync(draftPath));draft.endToEndPermanentWorkflow=['Codex visually decomposes reference','Master Asset Index reuse search','reconstruct visible Layer A modules','vision-approve modules','deterministic Layer B assembly','Codex visually inspects Blender output','bounded internal correction','operator sees first serious candidate'];fs.writeFileSync(draftPath,JSON.stringify(draft,null,2));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_CALIBRATED_FULL_ASSEMBLY_REPORT.md'),`# Clean Full-Shop Assembly\n\nStatus: **NEEDS_CORRECTION**.\n\nLike I’m 5: the new shop was assembled only from the calibrated Layer A modules, and no old placeholder objects were used. The result is cleaner, but it still does not match the reference closely enough for approval.\n\nSteam Deck Blender 5.2.0 LTS: PASS. Legacy placeholders: 0. Component IDs: PASS. Anonymous geometry: 0. Four-side: PASS. Mobile budget: PASS.\n\nMain remaining differences: facade proportions and continuity, protected fascia/awning context, upper-trim integration, and presentation contrast. No artwork was modified. No known-good build assigned.\n`);
