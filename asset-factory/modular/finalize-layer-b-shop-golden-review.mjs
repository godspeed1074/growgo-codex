import fs from "node:fs"; import path from "node:path";
const root=path.resolve("test-output/shop-layer-b-real"); const result=JSON.parse(fs.readFileSync(path.join(root,"SHOP_LAYER_B_ASSEMBLY_RESULT.json"),"utf8")); const recipe=JSON.parse(fs.readFileSync("asset-factory/modular/SHOP_LAYER_B_RECIPE.json","utf8")); const manifest=JSON.parse(fs.readFileSync("asset-factory/modular/SHOP_LAYER_B_ASSEMBLY_MANIFEST.json","utf8")); const idMap=JSON.parse(fs.readFileSync(path.join(root,"SHOP_COMPONENT_ID_MAP.json"),"utf8"));
const required=["WALL","DOOR","WINDOW","AWNING","FASCIA","SIGN","TRIM","FOUNDATION","PLANTER","SHRUB"]; const componentPass=required.every(id=>idMap[id]?.assetId&&idMap[id]?.moduleId); const budgetPass=result.budget.triangles<=12000&&result.budget.materials<=32&&result.budget.anonymousGeometryCount===0; const visual={silhouette:"NEEDS_CORRECTION",proportions:"NEEDS_CORRECTION",storefrontHierarchy:"NEEDS_CORRECTION",colourMaterial:"FAIL",gameplayReadability:"NEEDS_CORRECTION"}; const report={status:"NEEDS_CORRECTION",recipeId:recipe.recipeId,recipeVersion:recipe.recipeVersion,overallVisualResult:"NEEDS_CORRECTION",visual,modules:recipe.components.map(c=>({componentId:c.componentId,assetId:c.assetId,status:c.componentId==='WALL'||c.componentId==='FOUNDATION'?'NEEDS_CORRECTION':'NEEDS_CORRECTION'})),fourSideReview:{front:"NEEDS_CORRECTION",back:"NEEDS_CORRECTION",left:"NEEDS_CORRECTION",right:"NEEDS_CORRECTION",boardExists:fs.existsSync(path.join(root,"SHOP_FOUR_SIDE_REVIEW_BOARD.png"))},componentId:{required,pass:componentPass,anonymousGeometryCount:result.budget.anonymousGeometryCount},budget:{...result.budget,pass:budgetPass,materialsReason:result.budget.materials>32?"ASSEMBLY_MATERIAL_BUDGET_EXCEEDED":"PASS"},goldenReference:{referenceId:"GG-REF-SIMPLE-SHOP-GROWGO-CONCEPT-001",unchanged:true,comparison:"FAIL_VISUAL_MISMATCH"},correctionDecision:{required:true,iteration:1,plan:["Reassemble using LOD0/source module objects only; exclude imported LOD collections from runtime assembly.","Preserve approved palette materials instead of flattening to Workbench gray.","Re-render storefront with locked camera and verify navy fascia, plum awning, teal glass, cream trim, and planter silhouette."],preserveRecipe:true,preserveModuleIds:true,preserveCamera:true},knownGoodBuildId:null,recipeImmutable:JSON.stringify(manifest.components)===JSON.stringify(recipe.components),operatorApprovalRequired:true}; fs.writeFileSync("asset-factory/modular/SHOP_LAYER_B_FINAL_REVIEW_REPORT.md",`# Layer B Shop Final Golden Reference Review\n\nStatus: **NEEDS_CORRECTION**\n\nThe real Blender assembly, IDs, and four-side files exist, but the assembled beauty render is visually not close enough to the supplied concept. It is predominantly gray, lacks the required storefront colour hierarchy, and includes imported LOD copies that inflate assembly materials/object count.\n\nKnown-good build ID: not assigned.\n\nCorrection is limited to one controlled iteration and must preserve the recipe, module IDs, camera, and Golden Reference.\n`); fs.writeFileSync("asset-factory/modular/SHOP_LAYER_B_FINAL_REVIEW.json",JSON.stringify(report,null,2)); console.log(JSON.stringify({status:report.status,componentPass,budgetPass,knownGoodBuildId:null}));
