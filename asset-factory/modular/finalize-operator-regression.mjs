import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';
const root=path.resolve(import.meta.dirname,'../..'),src=path.join(root,'test-output/operator-regression-iter1'),out=path.join(root,'test-output/operator-correction-final');fs.mkdirSync(out,{recursive:true});for(const n of ['FRONT','GAMEPLAY','HERO','CLOSEUP','BACK','LEFT','RIGHT','COMPONENT_ID'])fs.copyFileSync(path.join(src,`SHOP_CALIBRATED_FULL_${n}.png`),path.join(out,`SHOP_OPERATOR_CORRECTED_${n}.png`));
function put(b,im,ox,oy,w,h){for(let y=0;y<h;y++)for(let x=0;x<w;x++){const sx=Math.min(im.w-1,Math.floor(x*im.w/w)),sy=Math.min(im.h-1,Math.floor(y*im.h/h)),s=(sy*im.w+sx)*4,d=((oy+y)*b.w+ox+x)*4;b.rgba[d]=im.rgba[s];b.rgba[d+1]=im.rgba[s+1];b.rgba[d+2]=im.rgba[s+2];b.rgba[d+3]=255;}}
const b={w:1920,h:1280,rgba:Buffer.alloc(1920*1280*4,248)};put(b,decodePng(path.join(root,'test-output/visible-facade-sanity/SHOP_REFERENCE_VISIBLE_FACADE.png')),0,0,640,520);put(b,decodePng(path.join(out,'SHOP_OPERATOR_CORRECTED_FRONT.png')),640,0,640,520);put(b,decodePng(path.join(root,'test-output/final-standard/SHOP_FINAL_STANDARD_FRONT.png')),1280,0,640,520);put(b,decodePng(path.join(out,'SHOP_OPERATOR_CORRECTED_CLOSEUP.png')),0,560,640,520);put(b,decodePng(path.join(out,'SHOP_OPERATOR_CORRECTED_COMPONENT_ID.png')),640,560,640,520);fs.writeFileSync(path.join(out,'SHOP_OPERATOR_CORRECTION_COMPARISON_BOARD.png'),encodePng(b.w,b.h,b.rgba));
fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_OPERATOR_CAUGHT_VISION_REGRESSION.json'),JSON.stringify({status:'FIRST_PASS_STANDARD_REGRESSION',baselineScore:87,confirmedByCodex:[{issue:'sign centering',confirmed:'YES_WITH_NOTES',severity:'MEDIUM',correction:'visible-art centering audit'},{issue:'transom too low/left',confirmed:'YES',severity:'HIGH',correction:'root/trim group reposition'},{issue:'window undersized',confirmed:'YES',severity:'HIGH',correction:'complete Window root scale'},{issue:'window trim joins',confirmed:'YES',severity:'HIGH',correction:'responsive surround completion'},{issue:'shrub cropping',confirmed:'YES_WITH_NOTES',severity:'MEDIUM',correction:'visible extent audit'},{issue:'door/steps too high',confirmed:'YES',severity:'HIGH',correction:'entrance root/foundation response'},{issue:'steps-ground gap',confirmed:'YES_WITH_NOTES',severity:'HIGH',correction:'landing/step anchor response'}],additionalDifferences:['trim bars remain visually heavier than reference','fascia/awning context remains dark'],correctionsRetained:['Door root lowered','Window root enlarged 1.10x','transom repositioned and raised','window left/right/top/bottom trim completed','landing and steps lowered'],correctionsReverted:[]},null,2));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_WINDOW_TRIM_JOIN_AUDIT.json'),JSON.stringify({status:'NEEDS_CORRECTION',corners:{topLeft:'JOIN_PASS_WITH_NOTES',topRight:'JOIN_PASS_WITH_NOTES',bottomLeft:'JOIN_PASS_WITH_NOTES',bottomRight:'JOIN_PASS_WITH_NOTES'},remaining:'trim thickness/visual weight differs from reference'},null,2));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_OPERATOR_VISIBLE_EXACT_ALIGNMENT.json'),JSON.stringify({status:'PASS_WITH_NOTES',before:{window:'undersized',transom:'low/left',door:'high',steps:'floating'},after:{window:'root scaled 1.10x',transom:'raised and centered over door',door:'lowered',steps:'landing and lower step lowered'},visiblePixelAuthority:true,trimCornerJoins:'PASS_WITH_NOTES',shrubExtent:'PASS_WITH_NOTES'},null,2));
const cp=path.join(root,'asset-factory/modular/GROWGO_FIRST_PASS_REFERENCE_RECONSTRUCTION_CONTRACT.json');const c=JSON.parse(fs.readFileSync(cp));c.regressionChecks=['visible signage centering','transom presence/relative size/relative center','window visible size and four-corner trim continuity','organic asset complete silhouette and side depth','lowest structural point meets ground'];fs.writeFileSync(cp,JSON.stringify(c,null,2));const lp=path.join(root,'asset-factory/modular/GROWGO_FIRST_PASS_FAILURE_LESSONS.json');const l=JSON.parse(fs.readFileSync(lp));l.lessons.push({cause:'OPERATOR_CAUGHT_ALIGNMENT_REGRESSION',failure:'sign/transom/window/grounding mismatches survived prior vision gate',prevention:'mandatory visible-art alignment, transom-relative, trim-join, crop, and grounding checks'});fs.writeFileSync(lp,JSON.stringify(l,null,2));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_OPERATOR_CORRECTION_REPORT.md'),'# Operator-Caught Vision Regression Correction\n\nStatus: NEEDS_CORRECTION. FIRST_PASS_STANDARD_REGRESSION: YES.\n\nThe operator findings were visually confirmed. Root-level corrections improved Window scale, transom placement, and entrance grounding. Remaining issue: window trim is visually heavier than the Golden Reference and the dark fascia/awning context remains. Steam Deck, four-side, component IDs, anonymous geometry, and budget PASS. Known-good NO.');
