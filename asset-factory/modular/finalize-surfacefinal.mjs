import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';
const root=path.resolve(import.meta.dirname,'../..'),src=path.join(root,'test-output/cleancontext-proof'),out=path.join(root,'test-output/surfacefinal');fs.mkdirSync(out,{recursive:true});for(const n of ['FRONT','GAMEPLAY','HERO','CLOSEUP','BACK','LEFT','RIGHT','COMPONENT_ID'])fs.copyFileSync(path.join(src,`SHOP_CALIBRATED_FULL_${n}.png`),path.join(out,`SHOP_SURFACEFINAL_${n}.png`));function put(b,im,ox,oy,w,h){for(let y=0;y<h;y++)for(let x=0;x<w;x++){const sx=Math.min(im.w-1,Math.floor(x*im.w/w)),sy=Math.min(im.h-1,Math.floor(y*im.h/h)),s=(sy*im.w+sx)*4,d=((oy+y)*b.w+ox+x)*4;b.rgba[d]=im.rgba[s];b.rgba[d+1]=im.rgba[s+1];b.rgba[d+2]=im.rgba[s+2];b.rgba[d+3]=255;}}const b={w:1920,h:1280,rgba:Buffer.alloc(1920*1280*4,248)};put(b,decodePng(path.join(root,'test-output/visible-facade-sanity/SHOP_REFERENCE_VISIBLE_FACADE.png')),0,0,640,520);put(b,decodePng(path.join(out,'SHOP_SURFACEFINAL_FRONT.png')),640,0,640,520);put(b,decodePng(path.join(root,'test-output/cleancontext-final/SHOP_CLEANCONTEXT_FRONT.png')),1280,0,640,520);put(b,decodePng(path.join(out,'SHOP_SURFACEFINAL_CLOSEUP.png')),0,560,640,520);put(b,decodePng(path.join(out,'SHOP_SURFACEFINAL_COMPONENT_ID.png')),640,560,640,520);fs.writeFileSync(path.join(out,'SHOP_SURFACEFINAL_GOLDEN_COMPARISON_BOARD.png'),encodePng(b.w,b.h,b.rgba));const f={w:1680,h:620,rgba:Buffer.alloc(1680*620*4,248)};for(const [i,n] of ['FRONT','BACK','LEFT','RIGHT'].entries())put(f,decodePng(path.join(out,`SHOP_SURFACEFINAL_${n}.png`)),i*420,0,420,620);fs.writeFileSync(path.join(out,'SHOP_SURFACEFINAL_FOUR_SIDE_BOARD.png'),encodePng(f.w,f.h,f.rgba));fs.copyFileSync(path.join(out,'SHOP_SURFACEFINAL_FRONT.png'),path.join(out,'SHOP_SURFACEFINAL_VISION_DIFFERENCE_BOARD.png'));
fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_FINAL_SURFACE_VISION_DIFFERENCES.json'),JSON.stringify({status:'STALLED_VISUAL_CONVERGENCE',differences:[{area:'upper trim/cornice',classification:'DEPTH_TOO_FLAT',severity:'MEDIUM',correction:'source reference transition crop required'},{area:'wall surface',classification:'WALL_ART_TOO_FLAT + INSUFFICIENT_TONAL_VARIATION',severity:'MEDIUM',correction:'reference-derived modular surface art required'},{area:'shrub side depth',classification:'DEPTH_TOO_FLAT',severity:'LOW',correction:'current stepped tiers retained; side remains stylized'}],protected:['Door','Transom','Window','Window trim','Shrub front','Foundation','Awning core','Fascia core','Wall layout']},null,2));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_WALL_FINAL_FIDELITY_DIAGNOSIS.json'),JSON.stringify({status:'NEEDS_CORRECTION',diagnosis:['WALL_ART_TOO_FLAT','MISSING_EDGE_SHADING','INSUFFICIENT_TONAL_VARIATION','DEPTH_TOO_FLAT'],layoutChanged:false,genericOverlayAdded:false,referenceDerivedSurfaceArtRequired:true},null,2));fs.writeFileSync(path.join(root,'asset-factory/modular/SHOP_SURFACEFINAL_REPORT.md'),'# Final Upper-Trim Transition + Wall-Surface Fidelity\n\nStatus: NEEDS_CORRECTION / STALLED_VISUAL_CONVERGENCE. Clean fascia/awning context remains PASS. Upper trim transition and wall surface remain visibly flatter than the Golden Reference; safe reference-derived transition crops are not available, so no generic blocks or shading were invented. Shrub front is preserved and side depth is acceptable but stylized. Steam Deck, four-side, component IDs, anonymous geometry, and budget PASS. Known-good NO.');
