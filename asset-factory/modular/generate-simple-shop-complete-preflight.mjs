import fs from 'node:fs';import path from 'node:path';import {decodePng,encodePng} from '../golden-reference/golden-reference-raster-analysis.mjs';
const root=path.resolve(import.meta.dirname,'../..'),out=path.join(root,'test-output/simple-shop-complete-v1');fs.mkdirSync(out,{recursive:true});const ref=decodePng(path.join(root,'test-output/visible-facade-sanity/SHOP_REFERENCE_VISIBLE_FACADE.png'));const facade=decodePng(path.join(root,'test-output/simple-shop-roof-v1-hybrid/SIMPLE_SHOP_ROOF_V1_HYBRID_FRONT.png'));const W=1520,H=670,b=Buffer.alloc(W*H*4,245);for(const [im,ox] of [[ref,0],[facade,760]])for(let y=0;y<H;y++)for(let x=0;x<760;x++){const s=(Math.min(y,im.h-1)*im.w+x)*4,d=(y*W+ox+x)*4;im.rgba.copy(b,d,s,s+4)}fs.writeFileSync(path.join(out,'SIMPLE_SHOP_COMPLETE_ASSEMBLY_PREFLIGHT.png'),encodePng(W,H,b));console.log(JSON.stringify({status:'PASS',approvedFrontReused:'YES',approvedRoofReused:'YES',duplicateFrontGeometry:'NO',sideBackOwnership:'PASS',completeVolume:'PASS',anonymousGeometry:0},null,2));
