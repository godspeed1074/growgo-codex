/* ============================= */
/* GrowGo script.js */
/* Map + Stats + Leaders + Crafting */
/* Performance-pass replacement */
/* ============================= */

const DEFAULT_CENTER = [-38.4537, 145.2381];

/* Production-safe default: keep false for normal GrowGo behavior. Set true only for local custom renderer testing; gameplay must remain unchanged when false. */
const ENABLE_CUSTOM_25D_MAP = false;
const ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS = false;
const ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA = false;
const ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA = false;

function bootstrapGrowGoScriptExecutionDiagnosticForLocalDev(options = {}) {
  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";

  if (!windowExists || !isLocalDevHost || options.localDevExecutionDiagnostic !== true) {
    return {
      phase: 250,
      name: "growgo-script-execution-diagnostic",
      ok: true,
      attached: false,
      reason: !windowExists
        ? "window-unavailable"
        : !isLocalDevHost
          ? "local-dev-host-required"
          : "local-dev-execution-diagnostic-flag-required"
    };
  }

  window.GrowGoScriptExecutionDiagnostic = {
    available: true,
    source: "phase-250-script-execution-diagnostic",
    scriptVersion: "phase-250",
    href: window.location.href,
    localDev: true,
    bootstrappedAt: "phase-250"
  };

  return {
    phase: 250,
    name: "growgo-script-execution-diagnostic",
    ok: true,
    attached: true,
    source: window.GrowGoScriptExecutionDiagnostic.source
  };
}

bootstrapGrowGoScriptExecutionDiagnosticForLocalDev({
  localDevExecutionDiagnostic: true
});

function bootstrapCustom25DVisualManualTestConsoleNamespaceForLocalDev(options = {}) {
  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";
  const namespaceKey = "GrowGoCustom25DVisualManualTests";
  const allowBootstrap = options.localDevBootstrap === true;

  if (!windowExists || !isLocalDevHost || !allowBootstrap) {
    return {
      phase: 248,
      name: "custom-25d-visual-manual-test-console-namespace-bootstrap",
      ok: true,
      allowed: false,
      bootstrapped: false,
      namespace: namespaceKey,
      reason: !windowExists
        ? "window-unavailable"
        : !isLocalDevHost
          ? "local-dev-host-required"
          : "local-dev-bootstrap-flag-required"
    };
  }

  const createNamespaceWrapper = (helperName, resolver) => (...args) => {
    const helper = typeof resolver === "function" ? resolver() : null;
    if (typeof helper !== "function") {
      return {
        phase: 248,
        name: "custom-25d-visual-manual-test-console-namespace-bootstrap-wrapper",
        ok: false,
        allowed: false,
        helperName,
        reason: "helper-unavailable-at-call-time",
        namespace: namespaceKey
      };
    }

    return helper(...args);
  };

  const existingNamespace =
    typeof window[namespaceKey] === "object" && window[namespaceKey] ? window[namespaceKey] : {};
  const namespace = {
    ...existingNamespace,
    available: true,
    source: "phase-248-bootstrap",
    localDev: true,
    bootstrappedAt: "phase-248",
    getCustom25DVisualFirstShapeTestContract: createNamespaceWrapper(
      "getCustom25DVisualFirstShapeTestContract",
      () =>
        typeof getCustom25DVisualFirstShapeTestContract === "function"
          ? getCustom25DVisualFirstShapeTestContract
          : null
    ),
    createCustom25DVisualFirstShapeManualTestLayer: createNamespaceWrapper(
      "createCustom25DVisualFirstShapeManualTestLayer",
      () =>
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function"
          ? createCustom25DVisualFirstShapeManualTestLayer
          : null
    ),
    clearCustom25DVisualFirstShapeManualTestLayer: createNamespaceWrapper(
      "clearCustom25DVisualFirstShapeManualTestLayer",
      () =>
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function"
          ? clearCustom25DVisualFirstShapeManualTestLayer
          : null
    ),
    runCustom25DVisualFirstShapeManualTest: createNamespaceWrapper(
      "runCustom25DVisualFirstShapeManualTest",
      () =>
        typeof runCustom25DVisualFirstShapeManualTest === "function"
          ? runCustom25DVisualFirstShapeManualTest
          : null
    ),
    getCustom25DVisualFirstShapeManualTestVerificationReport: createNamespaceWrapper(
      "getCustom25DVisualFirstShapeManualTestVerificationReport",
      () =>
        typeof getCustom25DVisualFirstShapeManualTestVerificationReport === "function"
          ? getCustom25DVisualFirstShapeManualTestVerificationReport
          : null
    ),
    getCustom25DVisualFirstShapeManualTestCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualFirstShapeManualTestCloseoutReport",
      () =>
        typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function"
          ? getCustom25DVisualFirstShapeManualTestCloseoutReport
          : null
    ),
    getCustom25DVisualInertRendererShellCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualInertRendererShellCloseoutReport",
      () =>
        typeof getCustom25DVisualInertRendererShellCloseoutReport === "function"
          ? getCustom25DVisualInertRendererShellCloseoutReport
          : null
    ),
    getCustom25DVisualRendererLifecyclePlanReport: createNamespaceWrapper(
      "getCustom25DVisualRendererLifecyclePlanReport",
      () =>
        typeof getCustom25DVisualRendererLifecyclePlanReport === "function"
          ? getCustom25DVisualRendererLifecyclePlanReport
          : null
    ),
    getCustom25DVisualRendererLifecycleContractReport: createNamespaceWrapper(
      "getCustom25DVisualRendererLifecycleContractReport",
      () =>
        typeof getCustom25DVisualRendererLifecycleContractReport === "function"
          ? getCustom25DVisualRendererLifecycleContractReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardPlanReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardPlanReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardPlanReport === "function"
          ? getCustom25DVisualManualInitializationGuardPlanReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardContractReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardContractReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardContractReport === "function"
          ? getCustom25DVisualManualInitializationGuardContractReport
          : null
    ),
    canInitializeCustom25DVisualRendererManually: createNamespaceWrapper(
      "canInitializeCustom25DVisualRendererManually",
      () =>
        typeof canInitializeCustom25DVisualRendererManually === "function"
          ? canInitializeCustom25DVisualRendererManually
          : null
    ),
    getCustom25DVisualManualInitializationGuardShellCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardShellCloseoutReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardShellCloseoutReport === "function"
          ? getCustom25DVisualManualInitializationGuardShellCloseoutReport
          : null
    ),
    getCustom25DVisualManualInitializationApprovalSignalPlanReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationApprovalSignalPlanReport",
      () =>
        typeof getCustom25DVisualManualInitializationApprovalSignalPlanReport === "function"
          ? getCustom25DVisualManualInitializationApprovalSignalPlanReport
          : null
    ),
    getCustom25DVisualManualInitializationApprovalSignalContractReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationApprovalSignalContractReport",
      () =>
        typeof getCustom25DVisualManualInitializationApprovalSignalContractReport === "function"
          ? getCustom25DVisualManualInitializationApprovalSignalContractReport
          : null
    ),
    evaluateCustom25DVisualManualInitializationSignals: createNamespaceWrapper(
      "evaluateCustom25DVisualManualInitializationSignals",
      () =>
        typeof evaluateCustom25DVisualManualInitializationSignals === "function"
          ? evaluateCustom25DVisualManualInitializationSignals
          : null
    ),
    getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport",
      () =>
        typeof getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport === "function"
          ? getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport ===
        "function"
          ? getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport",
        () =>
          typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport ===
          "function"
            ? getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport
            : null
      ),
    canUseCustom25DVisualManualRendererInitializationIntegratedGuard:
      createNamespaceWrapper(
        "canUseCustom25DVisualManualRendererInitializationIntegratedGuard",
        () =>
          typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard ===
          "function"
            ? canUseCustom25DVisualManualRendererInitializationIntegratedGuard
            : null
      ),
    getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport",
        () =>
          typeof getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport ===
          "function"
            ? getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellContractReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellContractReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellContractReport
            : null
      ),
    initializeCustom25DVisualRendererManually:
      createNamespaceWrapper(
        "initializeCustom25DVisualRendererManually",
        () =>
          typeof initializeCustom25DVisualRendererManually === "function"
            ? initializeCustom25DVisualRendererManually
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerPlanReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerContractReadinessReport
            : null
      ),
    createCustom25DVisualManualRendererStateContainerShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererStateContainerShell",
        () =>
          typeof createCustom25DVisualManualRendererStateContainerShell === "function"
            ? createCustom25DVisualManualRendererStateContainerShell
            : null
      ),
    getCustom25DVisualManualRendererStateContainerShellVerificationReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerShellVerificationReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerShellVerificationReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerShellVerificationReport
            : null
      ),
    getCustom25DVisualManualRendererStateMutationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport
            : null
      ),
    applyCustom25DVisualManualRendererStateMutationShell: createNamespaceWrapper(
      "applyCustom25DVisualManualRendererStateMutationShell",
      () =>
        typeof applyCustom25DVisualManualRendererStateMutationShell === "function"
          ? applyCustom25DVisualManualRendererStateMutationShell
          : null
    ),
    getCustom25DVisualManualRendererStateMutationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateMutationShellSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationShellSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationShellSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationShellSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererNextStateMutationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNextStateMutationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererNextStateMutationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererNextStateMutationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport
            : null
      ),
    applyCustom25DVisualManualRendererSecondStateMutationShell: createNamespaceWrapper(
      "applyCustom25DVisualManualRendererSecondStateMutationShell",
      () =>
        typeof applyCustom25DVisualManualRendererSecondStateMutationShell === "function"
          ? applyCustom25DVisualManualRendererSecondStateMutationShell
          : null
    ),
    getCustom25DVisualManualRendererSecondStateMutationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSecondStateMutationCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSecondStateMutationCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSecondStateMutationCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport
            : null
      ),
    applyCustom25DVisualManualRendererStateFieldContractShell:
      createNamespaceWrapper(
        "applyCustom25DVisualManualRendererStateFieldContractShell",
        () =>
          typeof applyCustom25DVisualManualRendererStateFieldContractShell ===
          "function"
            ? applyCustom25DVisualManualRendererStateFieldContractShell
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractNextReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractNextReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractNextReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractNextReadinessReport
            : null
      ),
    applyCustom25DVisualManualRendererStateFieldContractValidationShell:
      createNamespaceWrapper(
        "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
        () =>
          typeof applyCustom25DVisualManualRendererStateFieldContractValidationShell ===
          "function"
            ? applyCustom25DVisualManualRendererStateFieldContractValidationShell
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPostFieldContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostFieldContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPostFieldContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPostFieldContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport
            : null
      ),
    getCustom25DVisualManualRendererInstanceShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInstanceShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInstanceShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInstanceShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport
            : null
      ),
    createCustom25DVisualManualRendererInstanceShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererInstanceShell",
        () =>
          typeof createCustom25DVisualManualRendererInstanceShell === "function"
            ? createCustom25DVisualManualRendererInstanceShell
            : null
      ),
    getCustom25DVisualManualRendererInstanceShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInstanceShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInstanceShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInstanceShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPostInstanceShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostInstanceShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPostInstanceShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPostInstanceShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport
            : null
      ),
    createCustom25DVisualManualRendererLifecycleMethodShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererLifecycleMethodShell",
        () =>
          typeof createCustom25DVisualManualRendererLifecycleMethodShell ===
          "function"
            ? createCustom25DVisualManualRendererLifecycleMethodShell
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport
            : null
      ),
    createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
        () =>
          typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
          "function"
            ? createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateContainerPreflightReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateContainerPreflightReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateContainerPreflightReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSchemaReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSchemaReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadAccessPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadAccessPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshot:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshot",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshot
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport",
        () =>
          typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
          "function"
            ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport
            : null
      ),
    getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellContractReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellContractReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererIntegrationShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererIntegrationShell",
        () =>
          typeof createCustom25DVisualManualRendererIntegrationShell === "function"
            ? createCustom25DVisualManualRendererIntegrationShell
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatus:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatus",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatus
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererLifecyclePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecyclePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
            ? getCustom25DVisualManualRendererLifecyclePlanReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleContractReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
            ? getCustom25DVisualManualRendererLifecycleContractReport
            : null
      ),
    getCustom25DVisualManualRendererLifecyclePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecyclePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererLifecycleShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererLifecycleShell",
        () =>
          typeof createCustom25DVisualManualRendererLifecycleShell === "function"
            ? createCustom25DVisualManualRendererLifecycleShell
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
            ? getCustom25DVisualManualRendererObjectPlanReport
            : null
      ),
    getCustom25DVisualManualRendererObjectContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectContractReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectContractReport === "function"
            ? getCustom25DVisualManualRendererObjectContractReport
            : null
      ),
    getCustom25DVisualManualRendererObjectPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererObjectShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererObjectShell",
        () =>
          typeof createCustom25DVisualManualRendererObjectShell === "function"
            ? createCustom25DVisualManualRendererObjectShell
            : null
      ),
    getCustom25DVisualManualRendererObjectShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShellStatusCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellStatusCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellStatusCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellStatusCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapePlanReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeContractReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeContractReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeContractReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererObjectShapeShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererObjectShapeShell",
        () =>
          typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
            ? createCustom25DVisualManualRendererObjectShapeShell
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
            ? getCustom25DVisualManualRendererMetadataPlanReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataShell === "function"
            ? createCustom25DVisualManualRendererMetadataShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataContractPlanReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataContractShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataContractShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataContractShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataContractShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationContractReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationContractReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationContractReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataValidationShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataValidationShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataValidationShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataValidationShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataValidationResultContractShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataValidationResultContractShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataValidationResultContractShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataValidationResultShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataValidationResultShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataValidationResultShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceStatusReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceStatusReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceStatusReport
            : null
      ),
    getCustom25DVisualFutureSafeExtractionCandidateReport:
      createNamespaceWrapper(
        "getCustom25DVisualFutureSafeExtractionCandidateReport",
        () =>
          typeof getCustom25DVisualFutureSafeExtractionCandidateReport ===
          "function"
            ? getCustom25DVisualFutureSafeExtractionCandidateReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceReadinessReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceReadinessReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceReadinessReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestReadinessReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestReadinessReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightReadinessReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightReadinessReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualFirstPassiveExtractionCandidatePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport
            : null
      ),
    getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport
            : null
      ),
    getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport
            : null
      ),
    getCustom25DVisualFirstPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualFirstPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualFirstPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualSecondPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualSecondPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport ===
            "function"
            ? globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualExtractedPassiveReportsFileHealthReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedPassiveReportsFileHealthReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthReport ===
            "function"
            ? globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthReport
            : null
      ),
    getCustom25DVisualExtractedPassiveReportsFileReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedPassiveReportsFileReadinessReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualExtractedPassiveReportsFileReadinessReport ===
            "function"
            ? globalThis.getCustom25DVisualExtractedPassiveReportsFileReadinessReport
            : null
      ),
    getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport
            : null
      ),
    getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualPostReconciliationMoveHealthReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationMoveHealthReport",
        () =>
          typeof getCustom25DVisualPostReconciliationMoveHealthReport === "function"
            ? getCustom25DVisualPostReconciliationMoveHealthReport
            : null
      ),
    getCustom25DVisualPostReconciliationMoveReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationMoveReadinessReport",
        () =>
          typeof getCustom25DVisualPostReconciliationMoveReadinessReport ===
          "function"
            ? getCustom25DVisualPostReconciliationMoveReadinessReport
            : null
      ),
    getCustom25DVisualPostReconciliationMoveHealthCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationMoveHealthCloseoutReport",
        () =>
          typeof getCustom25DVisualPostReconciliationMoveHealthCloseoutReport ===
          "function"
            ? getCustom25DVisualPostReconciliationMoveHealthCloseoutReport
            : null
      ),
    getCustom25DVisualBundledPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualBundledPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualBundledPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualBundledPassiveMoveHealthReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveMoveHealthReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualBundledPassiveMoveHealthReport ===
            "function"
            ? globalThis.getCustom25DVisualBundledPassiveMoveHealthReport
            : null
      ),
    getCustom25DVisualBundledPassiveMoveReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveMoveReadinessReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualBundledPassiveMoveReadinessReport ===
            "function"
            ? globalThis.getCustom25DVisualBundledPassiveMoveReadinessReport
            : null
      ),
    getCustom25DVisualBundledPassiveMoveHealthCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveMoveHealthCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualBundledPassiveMoveHealthCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualBundledPassiveMoveHealthCloseoutReport
            : null
      ),
    getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionReassessmentReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionReassessmentReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionReassessmentReport === "function"
            ? getCustom25DVisualPassiveExtractionReassessmentReport
            : null
      ),
    getCustom25DVisualPassiveExtractionReassessmentReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionReassessmentReadinessReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionReassessmentReadinessReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionReassessmentReadinessReport
            : null
      ),
    getCustom25DVisualPassiveExtractionReassessmentCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionReassessmentCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionReassessmentCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionReassessmentCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport
            : null
      ),
    getCustom25DVisualRendererPathResumeReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererPathResumeReadinessReport",
        () =>
          typeof getCustom25DVisualRendererPathResumeReadinessReport === "function"
            ? getCustom25DVisualRendererPathResumeReadinessReport
            : null
      ),
    getCustom25DVisualRendererPathResumeNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererPathResumeNextStepReport",
        () =>
          typeof getCustom25DVisualRendererPathResumeNextStepReport === "function"
            ? getCustom25DVisualRendererPathResumeNextStepReport
            : null
      ),
    getCustom25DVisualRendererPathResumeCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererPathResumeCloseoutReport",
        () =>
          typeof getCustom25DVisualRendererPathResumeCloseoutReport === "function"
            ? getCustom25DVisualRendererPathResumeCloseoutReport
            : null
      ),
    getCustom25DVisualRendererStateContainerReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerReadinessReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerReadinessReport === "function"
            ? getCustom25DVisualRendererStateContainerReadinessReport
            : null
      ),
    getCustom25DVisualRendererStateContainerNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerNextStepReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerNextStepReport === "function"
            ? getCustom25DVisualRendererStateContainerNextStepReport
            : null
      ),
    getCustom25DVisualRendererStateContainerReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualRendererStateContainerPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerPlanCloseoutReport
            : null
      ),
    getCustom25DVisualRendererStateContainerPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerPlanSelfReviewReport
            : null
      ),
    getCustom25DVisualRendererStateContainerPlanNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerPlanNextStepReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerPlanNextStepReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerPlanNextStepReport
            : null
      ),
    getCustom25DVisualInertStateContainerContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerContractPlanReport",
        () =>
          typeof getCustom25DVisualInertStateContainerContractPlanReport ===
          "function"
            ? getCustom25DVisualInertStateContainerContractPlanReport
            : null
      ),
    getCustom25DVisualInertStateContainerContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerContractReadinessReport",
        () =>
          typeof getCustom25DVisualInertStateContainerContractReadinessReport ===
          "function"
            ? getCustom25DVisualInertStateContainerContractReadinessReport
            : null
      ),
    getCustom25DVisualInertStateContainerContractPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerContractPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerContractPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerContractPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaNextStepReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaNextStepReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaNextStepReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationReadinessReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationReadinessReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationReadinessReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationGateReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationGateReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationGateReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationGateReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport
            : null
      ),
    createCustom25DVisualInertManualRendererStateContainer:
      createNamespaceWrapper(
        "createCustom25DVisualInertManualRendererStateContainer",
        () =>
          typeof createCustom25DVisualInertManualRendererStateContainer === "function"
            ? createCustom25DVisualInertManualRendererStateContainer
            : null
      ),
    getCustom25DVisualInertStateContainerCreationReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationReport === "function"
            ? getCustom25DVisualInertStateContainerCreationReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationCloseoutReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualInertStateContainerManualTestReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerManualTestReadinessReport",
        () =>
          typeof getCustom25DVisualInertStateContainerManualTestReadinessReport ===
          "function"
            ? getCustom25DVisualInertStateContainerManualTestReadinessReport
            : null
      ),
    runCustom25DVisualInertStateContainerManualConsoleTest:
      createNamespaceWrapper(
        "runCustom25DVisualInertStateContainerManualConsoleTest",
        () =>
          typeof runCustom25DVisualInertStateContainerManualConsoleTest ===
          "function"
            ? runCustom25DVisualInertStateContainerManualConsoleTest
            : null
      ),
    getCustom25DVisualInertStateContainerManualConsoleTestReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerManualConsoleTestReport",
        () =>
          typeof getCustom25DVisualInertStateContainerManualConsoleTestReport ===
          "function"
            ? getCustom25DVisualInertStateContainerManualConsoleTestReport
            : null
      ),
    getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldsReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldsReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldsReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldsReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInertStateFieldsReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertStateFieldsReport",
        () =>
          typeof getCustom25DVisualManualRendererInertStateFieldsReport ===
          "function"
            ? getCustom25DVisualManualRendererInertStateFieldsReport
            : null
      ),
    getCustom25DVisualManualRendererInertStateFieldsCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertStateFieldsCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInertStateFieldsCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInertStateFieldsCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInertLifecycleStateFieldsReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertLifecycleStateFieldsReport",
        () =>
          typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsReport ===
          "function"
            ? getCustom25DVisualManualRendererInertLifecycleStateFieldsReport
            : null
      ),
    getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationReadinessReport
            : null
      ),
    initializeCustom25DVisualManualRendererShell:
      createNamespaceWrapper(
        "initializeCustom25DVisualManualRendererShell",
        () =>
          typeof initializeCustom25DVisualManualRendererShell === "function"
            ? initializeCustom25DVisualManualRendererShell
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractShell:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractShell",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractShell ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractShell
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationGuardReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGuardReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGuardReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGuardReadinessReport
            : null
      ),
    canInitializeCustom25DVisualManualRenderer:
      createNamespaceWrapper(
        "canInitializeCustom25DVisualManualRenderer",
        () =>
          typeof canInitializeCustom25DVisualManualRenderer === "function"
            ? canInitializeCustom25DVisualManualRenderer
            : null
      ),
    getCustom25DVisualManualRendererInitializationGuardShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGuardShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGuardShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGuardShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationGateReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGateReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGateReviewReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationLockReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationLockReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationLockReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationLockReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationSequenceSummaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationSequenceSummaryReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationSequenceSummaryReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationHandoffPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationHandoffPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationHandoffPlanReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationNextStepReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationNextStepReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationNextStepReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationNextStepReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningBoundaryReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningRiskReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningRiskReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningRiskReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningRiskReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningRiskCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningRiskCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningRiskCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningRiskCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningHandoffSummaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningHandoffSummaryReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningHandoffSummaryReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningHandoffSummaryReport
            : null
      ),
    getCustom25DVisualManualRendererStateCreationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateCreationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateCreationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateCreationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerShellVerificationReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerShellVerificationReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerShellVerificationReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerShellVerificationReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationReadinessReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationReadinessReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationReadinessReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationCloseoutReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationCloseoutReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationCloseoutReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationAcceptanceReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveHelperLoadPathReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveHelperLoadPathReport",
        () =>
          typeof getCustom25DVisualPassiveHelperLoadPathReport === "function"
            ? getCustom25DVisualPassiveHelperLoadPathReport
            : null
      ),
    getCustom25DVisualPassiveHelperLoadPathCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveHelperLoadPathCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveHelperLoadPathCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveHelperLoadPathCloseoutReport
            : null
      ),
    getCustom25DVisualScriptSizeStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualScriptSizeStatusReport",
        () =>
          typeof getCustom25DVisualScriptSizeStatusReport === "function"
            ? getCustom25DVisualScriptSizeStatusReport
            : null
      ),
    getCustom25DVisualFutureModuleSplitPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualFutureModuleSplitPlanReport",
        () =>
          typeof getCustom25DVisualFutureModuleSplitPlanReport === "function"
            ? getCustom25DVisualFutureModuleSplitPlanReport
            : null
      ),
    getCustom25DVisualFutureModuleSplitReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualFutureModuleSplitReadinessReport",
        () =>
          typeof getCustom25DVisualFutureModuleSplitReadinessReport === "function"
            ? getCustom25DVisualFutureModuleSplitReadinessReport
            : null
      ),
    getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport
            : null
      ),
    getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport
            : null
      ),
    createCustom25DVisualManualRendererSharedStateContainerShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererSharedStateContainerShell",
        () =>
          typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
            ? createCustom25DVisualManualRendererSharedStateContainerShell
            : null
      )
  };

  window[namespaceKey] = namespace;
  return {
    phase: 248,
    name: "custom-25d-visual-manual-test-console-namespace-bootstrap",
    ok: true,
    allowed: true,
    bootstrapped: true,
    namespace: namespaceKey,
    source: namespace.source,
    localDev: true
  };
}

bootstrapCustom25DVisualManualTestConsoleNamespaceForLocalDev({
  localDevBootstrap: true
});

const BASE_PIN_VALUE = 5;
const WATER_PIN_VALUE = 10;
const POI_PIN_VALUE = 100;
const POI_COIN_REWARD = 100;
const WATER_PIN_RESOURCE_DROP_CHANCE = 0.25;
const WATER_PIN_BLUE_FISH_CHANCE = 1 / 250;
const WATER_PIN_SALMON_CHANCE = 1 / 1000;
const WATER_PIN_FISH_LIFETIME_MS = 4 * 60 * 60 * 1000;
const POINTS_GROWTH_HOURS = 168;
const CAPTURE_RADIUS_METERS = 100;
const POI_CAPTURE_RADIUS_METERS = 304.8;
const WATER_PIN_DISTANCE_METERS = 50;
const POI_SCAN_RADIUS_METERS = 5000;
const BASE_PIN_PURCHASE_COST = 100;
const PIN_OWNER_CAPTURE_REWARD = 5;
const PIN_LONG_PRESS_MS = 500;
const PIN_PLANT_STAGE_HOURS = 168;
const BASE_PIN_MAX_LEVEL = 4;

const WATER_PIN_FISH_TYPES = {
  blue: {
    itemId: "fish",
    label: "Blue Fish",
    icon: "🐟",
    xp: 50,
    className: "blue"
  },
  salmon: {
    itemId: "salmon",
    label: "Salmon",
    icon: "🐟",
    xp: 200,
    className: "salmon"
  }
};

const PIN_SPACING_METERS = 46;
const MIN_PIN_SEPARATION_METERS = 46;
const MAX_VISIBLE_PINS = 500;
const MAX_POIS_PER_SCAN = 250;

const POI_CAPTURE_STAT_BY_CATEGORY = {
  Church: "poiChurchCaptures",
  Churches: "poiChurchCaptures",
  Hospital: "poiHospitalCaptures",
  Hospitals: "poiHospitalCaptures",
  Historic: "poiHistoricCaptures",
  Park: "poiParkCaptures",
  Parks: "poiParkCaptures",
  Landmark: "poiLandmarkCaptures",
  Landmarks: "poiLandmarkCaptures",
  "Local POI": "poiLocalCaptures"
};

const MOCK_POIS = [
  {
    id: "poi:mock:local-church",
    name: "Local Church",
    category: "Places",
    subcategory: "Churches",
    rarity: "normal",
    icon: "church",
    lat: -38.45295,
    lng: 145.23835,
    description: "A quiet local church added for POI testing."
  },
  {
    id: "poi:mock:community-park",
    name: "Community Park",
    category: "Places",
    subcategory: "Parks",
    rarity: "normal",
    icon: "park",
    lat: -38.45435,
    lng: 145.23755,
    description: "A small green park for testing POI collections."
  },
  {
    id: "poi:mock:waterfall",
    name: "Waterfall",
    category: "Places",
    subcategory: "Waterfalls",
    rarity: "normal",
    icon: "waterfall",
    lat: -38.45345,
    lng: 145.23925,
    description: "A waterfall-style test POI."
  },
  {
    id: "poi:mock:dinosaur-fossil-site",
    name: "Dinosaur Fossil Site",
    category: "Dinosaur Sites",
    subcategory: "Fossil Digs",
    rarity: "special",
    icon: "dinosaur",
    lat: -38.45485,
    lng: 145.2389,
    description: "A rare fossil dig location for testing special POIs."
  },
  {
    id: "poi:mock:famous-film-location",
    name: "Famous Film Location",
    category: "Film Locations",
    subcategory: "Iconic Scenes",
    rarity: "special",
    icon: "film",
    lat: -38.45245,
    lng: 145.23775,
    description: "A special film-location POI used for testing."
  }
];

const POI_COLLECTION_CATEGORIES = [
  "Places",
  "Film Locations",
  "Dinosaur Sites",
  "Music Landmarks",
  "Historical People",
  "Tourist Attractions"
];

const MIN_FETCH_ZOOM = 15;
const PIN_FETCH_DEBOUNCE_MS = 450;
const ROAD_FETCH_PADDING = 0.0035;
const MAX_PINS_PER_FETCH = 900;

const OVERPASS_API_URL = "https://overpass-api.de/api/interpreter";
const TRUSTED_TIME_URL = "https://www.timeapi.io/api/Time/current/zone?timeZone=UTC";

const PIN_STORAGE_KEY = "growgo-pins";
const CAPTURED_POIS_STORAGE_KEY = "growgo-captured-pois";
const SERVER_STARTED_AT_KEY = "growgo-server-started-at";
const AVATAR_STORAGE_KEY = "growgo-avatar";
const STATS_STORAGE_KEY = "growgo-stats";
const ACHIEVEMENTS_STORAGE_KEY = "growgo-achievements";
const CRAFTING_STORAGE_KEY = "growgo-crafting";
const MARKET_STORAGE_KEY = "growgo-market";
const MARKET_LONG_PRESS_MS = 550;
const CARD_COLLECTION_STORAGE_KEY = "growgo-card-collection";
const PLAYER_TITLES_STORAGE_KEY = "growgo-player-titles";
const PLAYER_ARTIFACTS_STORAGE_KEY = "growgo-player-artifacts";
const CARD_LONG_PRESS_MS = 550;

const PLAYER_STORAGE_KEY = "growgo-player-state";
const GROWGO_PLAYER_ID_KEY = "growgo-player-public-id";
const GROWGO_PROFILE_CARD_MODE_KEY = "growgo-profile-card-mode";
const DEFAULT_PLAYER_NAME = "rubberlips";
const PROGRESSION_RESET_KEY = "growgo-progression-reset-version";
const PROGRESSION_RESET_VERSION = "rubberlips-reset-1";
const LOCAL_BACKUP_VERSION = 1;

const CARD_SETS = [
  {
    setId: "dinosaur-discoveries",
    setName: "Dinosaur Discoveries",
    themeIcon: "🦖",
    themeClass: "dinosaur",
    coverImage: "assets/cards/dinosaur-card-back-clean.png?v=1",
    totalCards: 24,
    completedTitle: "Fossil Hunter",
    completionAchievementId: "achievement-dinosaur-discoveries-complete",
    artifactRewardId: "ancient-dig-kit",
    cards: [
      "Tyrannosaurus",
      "Raptor",
      "Triceratops",
      "Stegosaurus",
      "Brachiosaurus",
      "Ankylosaurus",
      "Spinosaurus",
      "Pteranodon",
      "Allosaurus",
      "Parasaurolophus",
      "Diplodocus",
      "Iguanodon",
      "Carnotaurus",
      "Pachycephalosaurus",
      "Mosasaurus",
      "Megalodon",
      "Archaeopteryx",
      "Compsognathus",
      "Dimetrodon",
      "Therizinosaurus",
      "Gallimimus",
      "Argentinosaurus",
      "Dilophosaurus",
      "Brontosaurus"
    ].map((cardName, index) => ({
      cardId: `dino-${String(index + 1).padStart(3, "0")}`,
      setId: "dinosaur-discoveries",
      cardNumber: index + 1,
      cardName,
      rarity: index === 0 ? "legendary" : index < 4 ? "rare" : index < 10 ? "uncommon" : "common",
      image: index === 0 ? "assets/cards/t-rex-card-clean.png?v=1" : null,
      ghostImage: null,
      variationType: "normal",
      isAnimatedVariation: false,
      matchingPoiId: null
    }))
  }
];

const DINOSAUR_CARD_ART_PROFILES = {
  Tyrannosaurus: { badge: "Apex", habitat: "Volcanic Plains", frameClass: "theropod", silhouetteClass: "theropod" },
  Raptor: { badge: "Swift", habitat: "Red Ridge", frameClass: "theropod", silhouetteClass: "raptor" },
  Triceratops: { badge: "Shield", habitat: "Fern Basin", frameClass: "horned", silhouetteClass: "horned" },
  Stegosaurus: { badge: "Plated", habitat: "Sunset Valley", frameClass: "plated", silhouetteClass: "plated" },
  Brachiosaurus: { badge: "Titan", habitat: "Cloud Canopy", frameClass: "longneck", silhouetteClass: "longneck" },
  Ankylosaurus: { badge: "Armored", habitat: "Stone Brush", frameClass: "armored", silhouetteClass: "armored" },
  Spinosaurus: { badge: "River King", habitat: "Delta Marsh", frameClass: "spined", silhouetteClass: "spined" },
  Pteranodon: { badge: "Skies", habitat: "Cliff Winds", frameClass: "sky", silhouetteClass: "winged" },
  Allosaurus: { badge: "Hunter", habitat: "Mesa Run", frameClass: "theropod", silhouetteClass: "theropod" },
  Parasaurolophus: { badge: "Crest", habitat: "Mist Grove", frameClass: "crest", silhouetteClass: "crest" },
  Diplodocus: { badge: "Colossus", habitat: "River Plains", frameClass: "longneck", silhouetteClass: "longneck" },
  Iguanodon: { badge: "Grazer", habitat: "Amber Field", frameClass: "herbivore", silhouetteClass: "herbivore" },
  Carnotaurus: { badge: "Dash", habitat: "Crimson Dust", frameClass: "theropod", silhouetteClass: "raptor" },
  Pachycephalosaurus: { badge: "Dome", habitat: "Thunder Steppe", frameClass: "dome", silhouetteClass: "dome" },
  Mosasaurus: { badge: "Deep", habitat: "Ancient Sea", frameClass: "ocean", silhouetteClass: "marine" },
  Megalodon: { badge: "Legend", habitat: "Open Ocean", frameClass: "ocean", silhouetteClass: "shark" },
  Archaeopteryx: { badge: "Feather", habitat: "Sky Forest", frameClass: "sky", silhouetteClass: "feathered" },
  Compsognathus: { badge: "Tiny", habitat: "Leaf Floor", frameClass: "swift", silhouetteClass: "swift" },
  Dimetrodon: { badge: "Sail", habitat: "Steam Flats", frameClass: "spined", silhouetteClass: "spined" },
  Therizinosaurus: { badge: "Claw", habitat: "Moon Jungle", frameClass: "mystic", silhouetteClass: "clawed" },
  Gallimimus: { badge: "Runner", habitat: "Golden Plain", frameClass: "swift", silhouetteClass: "swift" },
  Argentinosaurus: { badge: "World Giant", habitat: "Endless Prairie", frameClass: "longneck", silhouetteClass: "longneck" },
  Dilophosaurus: { badge: "Twin Crest", habitat: "Storm Basin", frameClass: "crest", silhouetteClass: "crest" },
  Brontosaurus: { badge: "Classic", habitat: "Blue Meadow", frameClass: "longneck", silhouetteClass: "longneck" }
};

const CARD_ARTIFACTS = {
  "ancient-dig-kit": {
    artifactId: "ancient-dig-kit",
    name: "Ancient Dig Kit",
    icon: "⛏️",
    description: "Reveals a nearby hidden fossil-themed POI or bonus dig reward."
  }
};

resetPlayerProgressionOnce();

function resetPlayerProgressionOnce() {
  try {
    if (localStorage.getItem(PROGRESSION_RESET_KEY) === PROGRESSION_RESET_VERSION) {
      return;
    }

    [
      PIN_STORAGE_KEY,
      CAPTURED_POIS_STORAGE_KEY,
      SERVER_STARTED_AT_KEY,
      AVATAR_STORAGE_KEY,
      STATS_STORAGE_KEY,
      ACHIEVEMENTS_STORAGE_KEY,
      CRAFTING_STORAGE_KEY,
      MARKET_STORAGE_KEY,
      CARD_COLLECTION_STORAGE_KEY,
      PLAYER_TITLES_STORAGE_KEY,
      PLAYER_ARTIFACTS_STORAGE_KEY,
      PLAYER_STORAGE_KEY,
      GROWGO_PLAYER_ID_KEY,
      GROWGO_PROFILE_CARD_MODE_KEY,
      "growgo-players-met",
      "growgo-friends"
    ].forEach((key) => localStorage.removeItem(key));

    localStorage.setItem(PROGRESSION_RESET_KEY, PROGRESSION_RESET_VERSION);
  } catch (error) {
    console.warn("Could not reset player progression.", error);
  }
}

function createDefaultPlayerState() {
  return {
    name: DEFAULT_PLAYER_NAME,
    publicId: null,
    avatarSrc: null,
    profileCardMode: "photo",
    progress: {
      level: 1,
      xp: 0,
      coins: 0,
      score: 0
    },
    settings: {
      soundEffects: true,
      vibration: true
    }
  };
}

function loadPlayerState() {
  const defaults = createDefaultPlayerState();

  try {
    const saved = JSON.parse(localStorage.getItem(PLAYER_STORAGE_KEY) || "null") || {};
    const legacyProfileCardMode = localStorage.getItem(GROWGO_PROFILE_CARD_MODE_KEY);
    const legacyPublicId = localStorage.getItem(GROWGO_PLAYER_ID_KEY);
    const legacyAvatar = localStorage.getItem(AVATAR_STORAGE_KEY);

    return {
      ...defaults,
      ...saved,
      name: String(saved.name || defaults.name),
      publicId: saved.publicId || legacyPublicId || defaults.publicId,
      avatarSrc: saved.avatarSrc || legacyAvatar || defaults.avatarSrc,
      profileCardMode: saved.profileCardMode || legacyProfileCardMode || defaults.profileCardMode,
      progress: {
        ...defaults.progress,
        ...(saved.progress || {})
      },
      settings: {
        ...defaults.settings,
        ...(saved.settings || {})
      }
    };
  } catch (error) {
    console.warn("Could not load player state.", error);
    return defaults;
  }
}

function savePlayerState() {
  try {
    localStorage.setItem(PLAYER_STORAGE_KEY, JSON.stringify(playerState));

    if (playerState.publicId) {
      localStorage.setItem(GROWGO_PLAYER_ID_KEY, playerState.publicId);
    }

    if (playerState.avatarSrc) {
      localStorage.setItem(AVATAR_STORAGE_KEY, playerState.avatarSrc);
    } else {
      localStorage.removeItem(AVATAR_STORAGE_KEY);
    }

    localStorage.setItem(GROWGO_PROFILE_CARD_MODE_KEY, playerState.profileCardMode || "photo");
  } catch (error) {
    console.warn("Could not save player state.", error);
  }
}

function readStoredJson(key, fallback = null) {
  try {
    return JSON.parse(localStorage.getItem(key) || "null") || fallback;
  } catch (error) {
    console.warn(`Could not read ${key}.`, error);
    return fallback;
  }
}

function createLocalBackup() {
  const publicId = getOrCreateGrowGoPlayerId();

  return {
    version: LOCAL_BACKUP_VERSION,
    exportedAt: new Date().toISOString(),
    player: {
      ...playerState,
      publicId
    },
    stats: readStoredJson(STATS_STORAGE_KEY, null),
    crafting: readStoredJson(CRAFTING_STORAGE_KEY, null),
    market: readStoredJson(MARKET_STORAGE_KEY, null),
    cards: readStoredJson(CARD_COLLECTION_STORAGE_KEY, null),
    titles: readStoredJson(PLAYER_TITLES_STORAGE_KEY, []),
    artifacts: readStoredJson(PLAYER_ARTIFACTS_STORAGE_KEY, []),
    pins: readStoredJson(PIN_STORAGE_KEY, []),
    serverStartedAt: getServerStartedAt(),
    social: {
      playersMet: readStoredJson("growgo-players-met", []),
      friends: readStoredJson("growgo-friends", [])
    }
  };
}

async function copyLocalBackup() {
  const backupText = JSON.stringify(createLocalBackup(), null, 2);

  try {
    await navigator.clipboard.writeText(backupText);
    showToast("Backup copied", "Your local save is on the clipboard.");
  } catch (error) {
    console.warn("Could not copy backup.", error);
    showToast("Backup unavailable", "Clipboard access was blocked by this browser.");
  }
}

function downloadLocalBackup() {
  try {
    const backup = createLocalBackup();
    const backupText = JSON.stringify(backup, null, 2);
    const publicId = String(backup.player.publicId || "player").toLowerCase();
    const date = new Date().toISOString().slice(0, 10);
    const blob = new Blob([backupText], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const link = document.createElement("a");

    link.href = url;
    link.download = `growgo-${publicId}-${date}.json`;
    document.body.appendChild(link);
    link.click();
    link.remove();
    URL.revokeObjectURL(url);
    showToast("Backup downloaded", "Save file created for this device.");
  } catch (error) {
    console.warn("Could not download backup.", error);
    showToast("Backup unavailable", "This browser blocked the download.");
  }
}

function writeStoredJson(key, value) {
  try {
    if (value === null || value === undefined) {
      localStorage.removeItem(key);
      return;
    }

    localStorage.setItem(key, JSON.stringify(value));
  } catch (error) {
    console.warn(`Could not write ${key}.`, error);
  }
}

function restoreLocalBackup(backup) {
  if (!backup || backup.version !== LOCAL_BACKUP_VERSION || !backup.player) {
    throw new Error("Unsupported backup file.");
  }

  const restoredPlayer = {
    ...createDefaultPlayerState(),
    ...backup.player,
    name: String(backup.player.name || DEFAULT_PLAYER_NAME),
    progress: {
      ...createDefaultPlayerState().progress,
      ...(backup.player.progress || {})
    },
    settings: {
      ...createDefaultPlayerState().settings,
      ...(backup.player.settings || {})
    }
  };

  playerState = restoredPlayer;
  savePlayerState();
  writeStoredJson(STATS_STORAGE_KEY, backup.stats);
  writeStoredJson(CRAFTING_STORAGE_KEY, backup.crafting);
  writeStoredJson(MARKET_STORAGE_KEY, backup.market);
  writeStoredJson(CARD_COLLECTION_STORAGE_KEY, backup.cards);
  writeStoredJson(PLAYER_TITLES_STORAGE_KEY, backup.titles || []);
  writeStoredJson(PLAYER_ARTIFACTS_STORAGE_KEY, backup.artifacts || []);
  writeStoredJson(PIN_STORAGE_KEY, Array.isArray(backup.pins) ? backup.pins : []);
  localStorage.setItem(
    SERVER_STARTED_AT_KEY,
    String(Number(backup.serverStartedAt) || getTrustedNow())
  );
  writeStoredJson("growgo-players-met", backup.social?.playersMet || []);
  writeStoredJson("growgo-friends", backup.social?.friends || []);

  window.location.reload();
}

async function handleRestoreBackupFile(file) {
  if (!file) return;

  try {
    const text = await file.text();
    const backup = JSON.parse(text);

    if (!window.confirm("Replace this device's local progress with this backup?")) return;
    restoreLocalBackup(backup);
  } catch (error) {
    console.warn("Could not restore backup.", error);
    showToast("Restore failed", "That backup file could not be loaded.");
  }
}

let playerState = loadPlayerState();
savePlayerState();

/* ----------------------------- */
/* MARKET DATA */
/* ----------------------------- */

const MARKET_CATEGORY_BASE_PRICES = {
  resources: 20,
  food: 50,
  cards: 100,
  skins: 1000
};

const MARKET_ITEMS = [
  { id: "wheat_seed", name: "Wheat Seed", icon: "🌱", rarity: "Common", category: "resources" },
  { id: "corn_seed", name: "Corn Seed", icon: "🌱", rarity: "Common", category: "resources" },
  { id: "sugar_cane_seed", name: "Sugar Cane Seed", icon: "🌱", rarity: "Common", category: "resources" },
  { id: "wheat", name: "Wheat", icon: "🌾", rarity: "Common", category: "resources" },
  { id: "sugar_cane", name: "Sugar Cane", icon: "🎋", rarity: "Common", category: "resources" },
  { id: "corn", name: "Corn", icon: "🌽", rarity: "Common", category: "resources" },
  { id: "water", name: "Water", icon: "💧", rarity: "Common", category: "resources" },
  { id: "fish", name: "Blue Fish", icon: "🐟", rarity: "Common", category: "resources" },
  { id: "salmon", name: "Salmon", icon: "🐟", rarity: "Rare", category: "resources" },
  { id: "milk", name: "Milk", icon: "🥛", rarity: "Uncommon", category: "resources" },
  { id: "cocoa_beans", name: "Cocoa Beans", icon: "🍫", rarity: "Uncommon", category: "resources" },
  { id: "egg", name: "Egg", icon: "🥚", rarity: "Rare", category: "resources" },
  { id: "tomato", name: "Tomato", icon: "🍅", rarity: "Rare", category: "resources" },

  { id: "flour", name: "Flour", icon: "🌾", rarity: "Common", category: "food" },
  { id: "sugar", name: "Sugar", icon: "🍬", rarity: "Common", category: "food" },
  { id: "bread", name: "Bread", icon: "🍞", rarity: "Common", category: "food" },
  { id: "energy_bar", name: "Energy Bar", icon: "🍫", rarity: "Common", category: "food" },
  { id: "sweet_corn_snack", name: "Sweet Corn Snack", icon: "🌽", rarity: "Common", category: "food" },
  { id: "chocolate_milk", name: "Chocolate Milk", icon: "🥛", rarity: "Uncommon", category: "food" },
  { id: "battered_fish", name: "Battered Fish", icon: "🍤", rarity: "Common", category: "food" },

  { id: "dinosaur_card", name: "Dinosaur Card", icon: "🦖", rarity: "Common", category: "cards" },
  { id: "planet_card", name: "Planet Card", icon: "🪐", rarity: "Common", category: "cards" },
  { id: "land_of_oz_card", name: "Land of Oz Card", icon: "🌈", rarity: "Rare", category: "cards" },

  { id: "paper_scarecrow_skin", name: "Paper Scarecrow Skin", icon: "🎭", rarity: "Rare", category: "skins" },
  { id: "golden_pin_skin", name: "Golden Pin Skin", icon: "📍", rarity: "Legendary", category: "skins" }
];

let marketScreen = null;
let marketItemsView = null;
let marketItemDetailsView = null;
let marketBuyView = null;
let marketListingsView = null;
let marketSellOverlay = null;

let activeMarketCategory = "resources";
let selectedMarketItemId = null;
let selectedMarketPrice = null;
let selectedMarketBuyQuantity = 1;
let marketLongPressTimer = null;

/* ----------------------------- */
/* INVENTORY STATE */
/* ----------------------------- */

let inventoryScreen = null;
let inventoryBackBtn = null;
let inventoryGrid = null;
let inventoryListView = null;
let inventoryDetailsView = null;
let inventoryDetailsCard = null;

let activeInventoryCategory = "resources";
let selectedInventoryItemId = null;
let inventoryLongPressTimer = null;
let inventoryLongPressTriggered = false;

const INVENTORY_LONG_PRESS_MS = 550;

const INVENTORY_SPECIAL_ITEMS = [
  {
    id: "legendary_boost",
    name: "Legendary Boost",
    icon: "✨",
    rarity: "Legendary",
    category: "special",
    sellable: false,
    description: "A rare one-time reward. Special rewards like this cannot be sold."
  },
  {
    id: "wizard_title_token",
    name: "Title Token",
    icon: "🏷️",
    rarity: "Special",
    category: "special",
    sellable: false,
    description: "Unlocks a special player title from an achievement."
  }
];

const INVENTORY_DESCRIPTIONS = {
  wheat_seed: "A seed for owned base pins. Plant it to grow wheat over four weekly stages.",
  corn_seed: "A seed for owned base pins. Plant it to grow corn over four weekly stages.",
  sugar_cane_seed: "A seed for owned base pins. Plant it to grow sugar cane over four weekly stages.",
  wheat: "Basic crop used to make flour and simple foods.",
  sugar_cane: "Can be processed into sugar for snacks and drinks.",
  corn: "A common crop used in capture radius snacks.",
  water: "Useful for cooking, fishing recipes, and basic crafting.",
  fish: "A common blue fish used in lower-level recipes.",
  salmon: "A rare fish caught from water pins and saved for stronger recipes.",
  milk: "Produced by cows and used in drinks, butter, cream, and desserts.",
  cocoa_beans: "Used for chocolate recipes and uncommon crafting.",
  egg: "Produced by chickens and used in rare food recipes.",
  tomato: "A rare crop used in sandwiches, soup, and pasta.",
  flour: "Processed from wheat. Used in breads, desserts, and meals.",
  sugar: "Processed from sugar cane. Used in drinks and desserts.",
  bread: "A basic food item used in stronger recipes.",
  energy_bar: "Gives a small XP boost.",
  sweet_corn_snack: "Gives a small capture radius boost.",
  chocolate_milk: "A stronger XP boost drink.",
  battered_fish: "Activates auto-capture for a short time.",
  dinosaur_card: "A collectible card for your collections.",
  planet_card: "A collectible space-themed card.",
  land_of_oz_card: "A rare collectible card.",
  paper_scarecrow_skin: "A cosmetic skin for scarecrows.",
  golden_pin_skin: "A legendary cosmetic pin skin."
};

const HIGHWAY_TYPES = [
  "residential", "living_street", "unclassified",
  "tertiary", "tertiary_link", "secondary", "secondary_link",
  "primary", "primary_link", "service", "road",
  "track", "path", "footway", "cycleway", "pedestrian"
];

let map;
let pinsLayer;
let playerMarker;
let captureRing;
let playerLatLng = null;

let pinStore = new Map();
let capturedPOIs = new Map();
let renderedPinMarkers = new Map();
let pinSpatialBuckets = new Map();
let fetchedViewportKeys = new Set();

let trustedTimeOffsetMs = 0;
let trustedTimeReady = false;
let gmtClockTimer = null;
let lastUtcDayKey = null;

let roadFetchTimer = null;
let roadFetchAbortController = null;
let roadFetchInFlight = false;
let roadErrorToastShown = false;

let redrawTimer = null;
let savePinsTimer = null;
let pinLongPressTimer = null;
let pinLongPressTriggered = false;
let activeLongPressPin = null;

let toastStack;
let sideMenu;
let menuOverlay;
let avatarButton;

const BASE_PIN_SEED_OPTIONS = [
  { id: "wheat_seed", harvestItemId: "wheat", label: "Wheat Seed", cropLabel: "Wheat", icon: "🌾" },
  { id: "corn_seed", harvestItemId: "corn", label: "Corn Seed", cropLabel: "Corn", icon: "🌽" },
  { id: "sugar_cane_seed", harvestItemId: "sugar_cane", label: "Sugar Cane Seed", cropLabel: "Sugar Cane", icon: "🎋" }
];

const BASE_PIN_LEVELS = {
  1: { name: "Common", className: "common", upgradeCost: 200 },
  2: { name: "Uncommon", className: "uncommon", upgradeCost: 300 },
  3: { name: "Rare", className: "rare", upgradeCost: 400 },
  4: { name: "Epic", className: "epic", upgradeCost: null }
};

let menuAvatarInput;
let menuAvatarImg;
let menuAvatarPlus;
let topAvatarImage;
let topAvatarFallback;

let profileSwipeCard;
let profilePhotoFace;
let profileQrFace;
let playerQrCode;
let profileSwipeStartX = 0;
let profileSwipeStartY = 0;
let profileSwipeCurrentMode = playerState.profileCardMode || "photo";
let menuBackSwipeStartX = 0;
let menuBackSwipeStartY = 0;
let menuBackSwipeActive = false;
let menuBackSwipePointerId = null;

let menuHome;

let statsScreen;
let statsContent;
let statsBack;

let leadersScreen;
let leadersBackBtn;
let leaderboardList;
let leaderboardSummary;
let achievementsScreen;
let achievementsList;
let socialScreen;
let socialBackBtn;
let socialAvatarImg;
let socialAvatarFallback;
let socialPublicId;
let settingsScreen;
let settingsPlayerName;
let settingsPlayerId;
let soundEffectsToggle;
let vibrationToggle;
let copyBackupBtn;
let downloadBackupBtn;
let restoreBackupBtn;
let restoreBackupInput;
let changeAvatarBtn;
let clearAvatarBtn;
let scanPoiBtn;
let advanceCropsBtn;
let readyCropsBtn;
let resetLocalProgressBtn;
let collectionsScreen;
let ownedPinsCount;
let ownedPinsPendingTotal;
let claimOwnedPinRewardsBtn;
let ownedPinsList;
let poiPinsCount;
let poiPinsList;
let poiDetailsPanel;
let poiDetailsCard;
let cardCollectionsCount;
let cardSetsView;
let cardSetDetailView;
let cardSetBackBtn;
let cardSetDetailHeader;
let cardSetGrid;
let activeCardSetId = null;
let playerCardCollection = createDefaultCardCollection();
let cardLongPressTimer = null;
let cardLongPressTriggered = false;
let growGoQrScanner = null;
let growGoQrScannerRunning = false;

let craftingScreen;
let craftingBackBtn;
let recipePanel;
let recipeDetailsPanel;
let recipeDetailsBackBtn;
let recipeBookGrid;
let recipeDetailsCard;
let craftRecipeBtn;

let craftingLevelLabel;
let craftingXpText;
let craftingXpFill;
let craftingNextLevelText;
let craftingXpToGoText;
let craftingLevelBadgeLarge;

let recipeHeroIcon;
let recipeLevel;
let recipeTitle;
let recipeRarity;
let recipeIngredients;
let recipeEffect;
let recipeCraftingXp;
let recipeBoostType;
let recipeBoostAmount;
let recipeDuration;

let selectedRecipe = null;
let recipeBookRenderedForLevel = null;

const pinIconCache = new Map();

document.addEventListener("DOMContentLoaded", async () => {
  loadPinsFromLocal();
  loadPOIs();
  playerCardCollection = loadPlayerCardCollection();
  rebuildSpatialBuckets();

  cacheDom();
  initMap();
  renderPOIs();
  initBasicUi();
  initAvatarUpload();
  initStatsUi();
  initLeadersUi();
initSocialUi();
initCraftingUi();
  initMarketUi();
  initInventoryUi();
  initCollectionsUi();
  initSettingsUi();
  renderPlayerOverview();

  await syncTrustedTime();
  startGmtResetClock();

  resetTodayStatsIfNeeded();

  locatePlayer();
  requestRoadPinsForCurrentView(true);

  if (
    typeof exposeCustom25DVisualManualTestHelpersForLocalDevConsole === "function" &&
    typeof window !== "undefined" &&
    window &&
    window.location &&
    (window.location.hostname === "localhost" ||
      window.location.hostname === "127.0.0.1" ||
      window.location.hostname === "0.0.0.0" ||
      window.location.hostname === "::1")
  ) {
    exposeCustom25DVisualManualTestHelpersForLocalDevConsole({
      manual: true,
      developerIntent: true,
      localDevOnly: true,
      browserConsoleOnly: true
    });
  }
});

/* ----------------------------- */
/* DOM CACHE */
/* ----------------------------- */

function cacheDom() {
  toastStack = document.getElementById("toast-stack");
  sideMenu = document.getElementById("sideMenu");
  menuOverlay = document.getElementById("menuOverlay");
  avatarButton = document.getElementById("avatarButton");
  menuHome = document.getElementById("menuHome");

 menuAvatarInput = document.getElementById("menuAvatarInput");
menuAvatarImg = document.getElementById("menuAvatarImg");
menuAvatarPlus = document.getElementById("menuAvatarPlus");
topAvatarImage = document.getElementById("topAvatarImage");
topAvatarFallback = document.getElementById("topAvatarFallback");

profileSwipeCard = document.getElementById("profileSwipeCard");
profilePhotoFace = document.getElementById("profilePhotoFace");
profileQrFace = document.getElementById("profileQrFace");
playerQrCode = document.getElementById("playerQrCode");

  statsScreen = document.getElementById("statsScreen");
  statsContent = document.getElementById("statsContent");
  statsBack = document.getElementById("statsBack");

  leadersScreen = document.getElementById("leadersScreen");
  leadersBackBtn = document.getElementById("leadersBackBtn");
  leaderboardList = leadersScreen ? leadersScreen.querySelector(".leaderboard-list") : null;
  leaderboardSummary = document.getElementById("leaderboardSummary");
achievementsScreen = document.getElementById("achievementsScreen");
achievementsList = document.getElementById("achievementsList");
socialScreen = document.getElementById("socialScreen");
socialBackBtn = document.getElementById("socialBackBtn");
socialAvatarImg = document.getElementById("socialAvatarImg");
socialAvatarFallback = document.getElementById("socialAvatarFallback");
socialPublicId = document.getElementById("socialPublicId");
settingsScreen = document.getElementById("settingsScreen");
settingsPlayerName = document.getElementById("settingsPlayerName");
settingsPlayerId = document.getElementById("settingsPlayerId");
soundEffectsToggle = document.getElementById("soundEffectsToggle");
vibrationToggle = document.getElementById("vibrationToggle");
copyBackupBtn = document.getElementById("copyBackupBtn");
downloadBackupBtn = document.getElementById("downloadBackupBtn");
restoreBackupBtn = document.getElementById("restoreBackupBtn");
restoreBackupInput = document.getElementById("restoreBackupInput");
changeAvatarBtn = document.getElementById("changeAvatarBtn");
clearAvatarBtn = document.getElementById("clearAvatarBtn");
scanPoiBtn = document.getElementById("scanPoiBtn");
advanceCropsBtn = document.getElementById("advanceCropsBtn");
readyCropsBtn = document.getElementById("readyCropsBtn");
resetLocalProgressBtn = document.getElementById("resetLocalProgressBtn");
collectionsScreen = document.getElementById("collectionsScreen");
ownedPinsCount = document.getElementById("ownedPinsCount");
ownedPinsPendingTotal = document.getElementById("ownedPinsPendingTotal");
claimOwnedPinRewardsBtn = document.getElementById("claimOwnedPinRewardsBtn");
ownedPinsList = document.getElementById("ownedPinsList");
poiPinsCount = document.getElementById("poiPinsCount");
poiPinsList = document.getElementById("poiPinsList");
poiDetailsPanel = document.getElementById("poiDetailsPanel");
poiDetailsCard = document.getElementById("poiDetailsCard");
cardCollectionsCount = document.getElementById("cardCollectionsCount");
cardSetsView = document.getElementById("cardSetsView");
cardSetDetailView = document.getElementById("cardSetDetailView");
cardSetBackBtn = document.getElementById("cardSetBackBtn");
cardSetDetailHeader = document.getElementById("cardSetDetailHeader");
cardSetGrid = document.getElementById("cardSetGrid");

  craftingScreen = document.getElementById("craftingScreen");
  craftingBackBtn = document.getElementById("craftingBackBtn");
  recipePanel = document.getElementById("recipePanel");
  recipeDetailsPanel = document.getElementById("recipeDetailsPanel");
  recipeDetailsBackBtn = document.getElementById("recipeDetailsBackBtn");
  recipeBookGrid = document.getElementById("recipeBookGrid");
  recipeDetailsCard = document.getElementById("recipeDetailsCard");
  craftRecipeBtn = document.getElementById("craftRecipeBtn");

  craftingLevelLabel = document.getElementById("craftingLevelLabel");
  craftingXpText = document.getElementById("craftingXpText");
  craftingXpFill = document.getElementById("craftingXpFill");
  craftingNextLevelText = document.getElementById("craftingNextLevelText");
  craftingXpToGoText = document.getElementById("craftingXpToGoText");
  craftingLevelBadgeLarge = document.querySelector(".crafting-level-badge-large");

  recipeHeroIcon = document.getElementById("recipeHeroIcon");
  recipeLevel = document.getElementById("recipeLevel");
  recipeTitle = document.getElementById("recipeTitle");
  recipeRarity = document.getElementById("recipeRarity");
  recipeIngredients = document.getElementById("recipeIngredients");
  recipeEffect = document.getElementById("recipeEffect");
  recipeCraftingXp = document.getElementById("recipeCraftingXp");
  recipeBoostType = document.getElementById("recipeBoostType");
  recipeBoostAmount = document.getElementById("recipeBoostAmount");
  recipeDuration = document.getElementById("recipeDuration");
}

/* ----------------------------- */
/* STATS SYSTEM */
/* ----------------------------- */

function createDefaultStats() {
  return {
    todayDateKey: getLocalDateKey(Date.now()),

    today: {
      score: 0,
      captures: 0,
      goldEarned: 0,
      itemsCrafted: 0,
      resourcesGained: 0,
      questsDone: 0,
      birdsCaptured: 0,
      fishCaught: 0,
      newPois: 0,
      poiCaptures: 0,
      poiChurchCaptures: 0,
      poiHospitalCaptures: 0,
      poiHistoricCaptures: 0,
      poiParkCaptures: 0,
      poiLandmarkCaptures: 0,
      poiLocalCaptures: 0,
      playersMet: 0
    },

    lifetime: {
      score: 0,
      captures: 0,
      goldEarned: 0,
      itemsCrafted: 0,
      resourcesGained: 0,
      questsDone: 0,
      birdsCaptured: 0,
      fishCaught: 0,
      newPois: 0,
      poiCaptures: 0,
      poiChurchCaptures: 0,
      poiHospitalCaptures: 0,
      poiHistoricCaptures: 0,
      poiParkCaptures: 0,
      poiLandmarkCaptures: 0,
      poiLocalCaptures: 0,
      marketsAttended: 0,
      playersMet: 0,
      localNumberOnes: 0,
      regionalNumberOnes: 0,
      globalNumberOnes: 0
    },

    best: {
      score: { value: 0, date: null },
      captures: { value: 0, date: null },
      goldEarned: { value: 0, date: null },
      itemsCrafted: { value: 0, date: null },
      resourcesGained: { value: 0, date: null },
      questsDone: { value: 0, date: null },
      birdsCaptured: { value: 0, date: null },
      fishCaught: { value: 0, date: null },
      newPois: { value: 0, date: null },
      poiCaptures: { value: 0, date: null },
      playersMet: { value: 0, date: null },
      bestLocalRank: { rank: null, date: null },
      bestRegionalRank: { rank: null, date: null },
      bestGlobalRank: { rank: null, date: null }
    }
  };
}

function loadStats() {
  try {
    const raw = localStorage.getItem(STATS_STORAGE_KEY);
    const defaults = createDefaultStats();

    if (!raw) return defaults;

    const saved = JSON.parse(raw);

    return {
      ...defaults,
      ...saved,
      today: { ...defaults.today, ...(saved.today || {}) },
      lifetime: { ...defaults.lifetime, ...(saved.lifetime || {}) },
      best: { ...defaults.best, ...(saved.best || {}) }
    };
  } catch (error) {
    console.warn("Could not load stats.", error);
    return createDefaultStats();
  }
}

let playerStats = loadStats();

function saveStats() {
  try {
    localStorage.setItem(STATS_STORAGE_KEY, JSON.stringify(playerStats));
  } catch (error) {
    console.warn("Could not save stats.", error);
  }
}

const POI_ACHIEVEMENTS = [
  {
    id: "poi-discover-1",
    title: "First Find",
    description: "Discover your first point of interest.",
    statKey: "newPois",
    target: 1
  },
  {
    id: "poi-discover-10",
    title: "Local Scout",
    description: "Discover 10 points of interest.",
    statKey: "newPois",
    target: 10
  },
  {
    id: "poi-discover-50",
    title: "Area Mapper",
    description: "Discover 50 points of interest.",
    statKey: "newPois",
    target: 50
  },
  {
    id: "poi-capture-1",
    title: "First Check-In",
    description: "Capture your first point of interest.",
    statKey: "poiCaptures",
    target: 1
  },
  {
    id: "poi-capture-10",
    title: "POI Runner",
    description: "Capture 10 points of interest.",
    statKey: "poiCaptures",
    target: 10
  },
  {
    id: "poi-capture-50",
    title: "Landmark Legend",
    description: "Capture 50 points of interest.",
    statKey: "poiCaptures",
    target: 50
  },
  {
    id: "poi-church-1",
    title: "Steeple Spotter",
    description: "Capture a church POI.",
    statKey: "poiChurchCaptures",
    target: 1
  },
  {
    id: "poi-hospital-1",
    title: "Care Finder",
    description: "Capture a hospital POI.",
    statKey: "poiHospitalCaptures",
    target: 1
  },
  {
    id: "poi-historic-1",
    title: "History Hunter",
    description: "Capture a historic POI.",
    statKey: "poiHistoricCaptures",
    target: 1
  },
  {
    id: "poi-park-1",
    title: "Green Trail",
    description: "Capture a park POI.",
    statKey: "poiParkCaptures",
    target: 1
  },
  {
    id: "poi-landmark-1",
    title: "Landmark Found",
    description: "Capture a landmark POI.",
    statKey: "poiLandmarkCaptures",
    target: 1
  },
  {
    id: "poi-local-1",
    title: "Local Knowledge",
    description: "Capture a local POI.",
    statKey: "poiLocalCaptures",
    target: 1
  }
];

let unlockedAchievements = loadUnlockedAchievements();
let playerTitles = loadPlayerTitles();
let playerArtifacts = loadPlayerArtifacts();

function loadUnlockedAchievements() {
  try {
    const saved = JSON.parse(localStorage.getItem(ACHIEVEMENTS_STORAGE_KEY) || "[]");
    return new Set(Array.isArray(saved) ? saved : []);
  } catch (error) {
    console.warn("Could not load achievements.", error);
    return new Set();
  }
}

function saveUnlockedAchievements() {
  try {
    localStorage.setItem(ACHIEVEMENTS_STORAGE_KEY, JSON.stringify(Array.from(unlockedAchievements)));
  } catch (error) {
    console.warn("Could not save achievements.", error);
  }
}

function loadPlayerTitles() {
  return new Set(readStoredJson(PLAYER_TITLES_STORAGE_KEY, []));
}

function savePlayerTitles() {
  writeStoredJson(PLAYER_TITLES_STORAGE_KEY, Array.from(playerTitles));
}

function loadPlayerArtifacts() {
  return new Set(readStoredJson(PLAYER_ARTIFACTS_STORAGE_KEY, []));
}

function savePlayerArtifacts() {
  writeStoredJson(PLAYER_ARTIFACTS_STORAGE_KEY, Array.from(playerArtifacts));
}

function getAchievementProgress(achievement) {
  if (achievement.cardSetId) {
    const set = getCardSet(achievement.cardSetId);
    return set ? getCardSetProgress(set).ownedCount : 0;
  }

  return Number(playerStats.lifetime[achievement.statKey] || 0);
}

function checkPoiAchievements() {
  let unlockedAny = false;

  POI_ACHIEVEMENTS.forEach((achievement) => {
    if (unlockedAchievements.has(achievement.id)) return;
    if (getAchievementProgress(achievement) < achievement.target) return;

    unlockedAchievements.add(achievement.id);
    unlockedAny = true;
    showToast("Achievement unlocked", achievement.title);
  });

  if (unlockedAny) {
    saveUnlockedAchievements();
    renderAchievements();
  }
}

function getLocalDateKey(timestamp) {
  const date = new Date(timestamp);
  return `${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}`;
}

function getDisplayDate(timestamp = Date.now()) {
  return new Date(timestamp).toLocaleDateString();
}

function resetTodayStatsIfNeeded() {
  const currentDateKey = getLocalDateKey(Date.now());

  if (playerStats.todayDateKey === currentDateKey) return;

  playerStats.todayDateKey = currentDateKey;

  Object.keys(playerStats.today).forEach((key) => {
    playerStats.today[key] = 0;
  });

  saveStats();
}

function addStat(statKey, amount = 1) {
  resetTodayStatsIfNeeded();

  if (typeof playerStats.today[statKey] !== "number") {
    playerStats.today[statKey] = 0;
  }

  if (typeof playerStats.lifetime[statKey] !== "number") {
    playerStats.lifetime[statKey] = 0;
  }

  playerStats.today[statKey] += amount;
  playerStats.lifetime[statKey] += amount;

  const best = playerStats.best[statKey];

  if (best && typeof best === "object" && "value" in best) {
    if (playerStats.today[statKey] > best.value) {
      best.value = playerStats.today[statKey];
      best.date = getDisplayDate();
    }
  }

  saveStats();
  renderPlayerOverview();

  if (statsScreen && !statsScreen.classList.contains("hidden")) {
    const activeTab = statsScreen.querySelector(".stats-tabs .tab.active");
    renderStats(activeTab ? activeTab.dataset.tab : "today");
  }
}

/* ----------------------------- */
/* CRAFTING SYSTEM */
/* ----------------------------- */

const INGREDIENT_ICONS = {
  "Wheat": "🌾",
  "Sugar Cane": "🎋",
  "Sugar": "🍬",
  "Corn": "🌽",
  "Flour": "🌾",
  "Water": "💧",
  "Dough": "🥖",
  "Fish": "🐟",
  "Milk": "🥛",
  "Cocoa Beans": "🍫",
  "Bread": "🍞",
  "Egg": "🥚",
  "Tomato": "🍅",
  "Cream": "🥛",
  "Pasta": "🍝",
  "Butter": "🧈",
  "Cocoa": "🍫"
};

const RECIPES = [
  {
    level: 1,
    name: "Flour",
    icon: "🌾",
    rarity: "Common",
    ingredients: ["Wheat x2"],
    effect: "Processed ingredient used in breads, desserts, and meals.",
    boostType: "Ingredient",
    boostAmount: "Crafting",
    duration: "—",
    craftingXp: 25
  },
  {
    level: 2,
    name: "Sugar",
    icon: "🍬",
    rarity: "Common",
    ingredients: ["Sugar Cane x2"],
    effect: "Processed ingredient used in drinks, snacks, and desserts.",
    boostType: "Ingredient",
    boostAmount: "Crafting",
    duration: "—",
    craftingXp: 40
  },
  {
    level: 3,
    name: "Energy Bar",
    icon: "🍫",
    rarity: "Common",
    ingredients: ["Wheat", "Sugar"],
    effect: "A simple snack that gives a small XP boost.",
    boostType: "XP Boost",
    boostAmount: "+50%",
    duration: "5m",
    craftingXp: 75
  },
  {
    level: 4,
    name: "Sweet Corn Snack",
    icon: "🌽",
    rarity: "Common",
    ingredients: ["Corn", "Sugar"],
    effect: "A sweet snack that slightly increases capture radius.",
    boostType: "Radius",
    boostAmount: "+50%",
    duration: "5m",
    craftingXp: 90
  },
  {
    level: 5,
    name: "Dough",
    icon: "🥖",
    rarity: "Common",
    ingredients: ["Flour", "Water"],
    effect: "Processed ingredient used for bread and baked foods.",
    boostType: "Ingredient",
    boostAmount: "Crafting",
    duration: "—",
    craftingXp: 100
  },
  {
    level: 6,
    name: "Bread",
    icon: "🍞",
    rarity: "Common",
    ingredients: ["Dough"],
    effect: "A basic food that gives a small stamina boost.",
    boostType: "Stamina",
    boostAmount: "+50%",
    duration: "5m",
    craftingXp: 125
  },
  {
    level: 7,
    name: "Energy Drink",
    icon: "🥤",
    rarity: "Rare",
    ingredients: ["Sugar x2", "Water"],
    effect: "A refreshing boost that increases XP gain for a limited time.",
    boostType: "XP Boost",
    boostAmount: "+100%",
    duration: "5m",
    craftingXp: 250
  },
  {
    level: 8,
    name: "Prepared Fish",
    icon: "🐟",
    rarity: "Common",
    ingredients: ["Fish", "Water"],
    effect: "A prepared meal used in stronger food recipes.",
    boostType: "Meal",
    boostAmount: "Prepared",
    duration: "—",
    craftingXp: 180
  },
  {
    level: 9,
    name: "Battered Fish",
    icon: "🍤",
    rarity: "Common",
    ingredients: ["Fish", "Flour", "Water"],
    effect: "Activates auto-capture for a short time.",
    boostType: "Auto Capture",
    boostAmount: "Active",
    duration: "3m",
    craftingXp: 300
  },
  {
    level: 10,
    name: "Chocolate Milk",
    icon: "🍫",
    rarity: "Uncommon",
    ingredients: ["Milk x2", "Cocoa Beans"],
    effect: "A rich drink that gives a stronger XP boost.",
    boostType: "XP Boost",
    boostAmount: "+75%",
    duration: "10m",
    craftingXp: 350
  },
  {
    level: 11,
    name: "Battered Trout",
    icon: "🐠",
    rarity: "Uncommon",
    ingredients: ["Fish", "Flour", "Water"],
    effect: "Activates auto-capture for a longer time.",
    boostType: "Auto Capture",
    boostAmount: "Active",
    duration: "10m",
    craftingXp: 450
  },
  {
    level: 21,
    name: "Egg & Tomato Sandwich",
    icon: "🥪",
    rarity: "Rare",
    ingredients: ["Bread", "Egg", "Tomato"],
    effect: "A rare meal that boosts stamina and XP.",
    boostType: "Meal Boost",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 700
  },
  {
    level: 22,
    name: "Omelette",
    icon: "🍳",
    rarity: "Rare",
    ingredients: ["Egg", "Milk", "Tomato"],
    effect: "A rare crafted meal that improves stamina.",
    boostType: "Stamina",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 750
  },
  {
    level: 23,
    name: "Cream of Tomato Soup",
    icon: "🥣",
    rarity: "Rare",
    ingredients: ["Tomato", "Cream"],
    effect: "A warm soup that improves capture efficiency.",
    boostType: "Capture",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 800
  },
  {
    level: 24,
    name: "Spaghetti Marinara",
    icon: "🍝",
    rarity: "Rare",
    ingredients: ["Pasta", "Tomato"],
    effect: "A rare meal that boosts XP gain.",
    boostType: "XP Boost",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 850
  },
  {
    level: 25,
    name: "Pancakes",
    icon: "🥞",
    rarity: "Rare",
    ingredients: ["Flour", "Egg", "Milk"],
    effect: "A filling food that extends consumable duration.",
    boostType: "Duration",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 900
  },
  {
    level: 26,
    name: "Chocolate Brownies",
    icon: "🟫",
    rarity: "Rare",
    ingredients: ["Flour", "Sugar", "Cocoa"],
    effect: "A rare dessert that gives a strong crafting XP boost.",
    boostType: "Craft XP",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 950
  },
  {
    level: 27,
    name: "Chocolate Croissant",
    icon: "🥐",
    rarity: "Rare",
    ingredients: ["Flour", "Butter", "Cocoa"],
    effect: "A rare dessert that improves crafting results.",
    boostType: "Crafting",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 1000
  },
  {
    level: 28,
    name: "Chocolate Cake",
    icon: "🍰",
    rarity: "Rare",
    ingredients: ["Flour", "Sugar", "Cocoa", "Egg", "Milk"],
    effect: "A rare dessert that gives a major XP boost.",
    boostType: "XP Boost",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 1100
  },
  {
    level: 29,
    name: "Baked Salmon",
    icon: "🍣",
    rarity: "Rare",
    ingredients: ["Fish", "Butter", "Tomato"],
    effect: "A rare meal that increases capture radius.",
    boostType: "Radius",
    boostAmount: "+100%",
    duration: "15m",
    craftingXp: 1200
  }
];

function createDefaultCrafting() {
  return {
    level: 1,
    xp: 0
  };
}

function loadCrafting() {
  try {
    const raw = localStorage.getItem(CRAFTING_STORAGE_KEY);
    const defaults = createDefaultCrafting();

    if (!raw) return defaults;

    return {
      ...defaults,
      ...JSON.parse(raw)
    };
  } catch {
    return createDefaultCrafting();
  }
}

let playerCrafting = loadCrafting();

function saveCrafting() {
  try {
    localStorage.setItem(CRAFTING_STORAGE_KEY, JSON.stringify(playerCrafting));
  } catch (error) {
    console.warn("Could not save crafting.", error);
  }
}

function getLevelXpNeeded(level) {
  return Math.round(100 * Math.pow(1.2, level - 1));
}

function getTotalXpForLevel(level) {
  let total = 0;

  for (let i = 1; i < level; i++) {
    total += getLevelXpNeeded(i);
  }

  return total;
}

function getCraftingXpNeeded(level) {
  return getLevelXpNeeded(level);
}

function getCraftingProgress() {
  const currentLevelStart = getTotalXpForLevel(playerCrafting.level);
  const nextLevelAt = getTotalXpForLevel(playerCrafting.level + 1);
  const currentXpIntoLevel = playerCrafting.xp - currentLevelStart;
  const neededThisLevel = nextLevelAt - currentLevelStart;
  const percent = Math.min(100, Math.max(0, (currentXpIntoLevel / neededThisLevel) * 100));

  return {
    currentXpIntoLevel,
    neededThisLevel,
    percent,
    xpToGo: Math.max(0, neededThisLevel - currentXpIntoLevel)
  };
}

function addCraftingXp(amount) {
  const previousLevel = playerCrafting.level;

  playerCrafting.xp += amount;

  while (playerCrafting.xp >= getTotalXpForLevel(playerCrafting.level + 1)) {
    playerCrafting.level += 1;
    showToast("Crafting level up!", `You reached crafting level ${playerCrafting.level}.`);
  }

  saveCrafting();
  renderCraftingHeader();
  renderPlayerOverview();

  if (playerCrafting.level !== previousLevel) {
    triggerLevelUpFeedback();
    renderRecipeBook(true);
  }

  if (selectedRecipe) {
    openRecipe(selectedRecipe, false);
  }
}

function addPlayerXp(amount) {
  const progress = getPlayerProgressInfo();
  const previousLevel = progress.level;
  const addedXp = Math.max(0, Number(amount || 0));
  let nextXp = progress.totalXp + addedXp;
  let nextLevel = progress.level;

  while (nextXp >= getTotalXpForLevel(nextLevel + 1)) {
    nextLevel += 1;
  }

  playerState.progress = {
    ...createDefaultPlayerState().progress,
    ...(playerState.progress || {}),
    level: nextLevel,
    xp: nextXp
  };

  savePlayerState();
  renderPlayerOverview();

  if (nextLevel !== previousLevel) {
    showToast("Level up!", `You reached player level ${nextLevel}.`);
    triggerLevelUpFeedback();
  }
}

function initCraftingUi() {
  if (craftingBackBtn) {
    craftingBackBtn.addEventListener("click", () => {
      closeRecipeDetails();
      if (craftingScreen) craftingScreen.classList.add("hidden");
      showMenuHome();
    });
  }

  if (recipeDetailsBackBtn) {
    recipeDetailsBackBtn.addEventListener("click", closeRecipeDetails);
  }

  if (craftRecipeBtn) {
    craftRecipeBtn.addEventListener("click", craftSelectedRecipe);
  }

  if (recipeBookGrid) {
    recipeBookGrid.addEventListener("click", (event) => {
      const card = event.target.closest(".recipe-card");
      if (!card) return;

      const recipeName = card.dataset.recipeName;
      const recipe = RECIPES.find((item) => item.name === recipeName);
      if (!recipe) return;

      if (recipe.level > playerCrafting.level) {
        showToast("Recipe locked", `Unlocks at crafting level ${recipe.level}.`);
        return;
      }

      openRecipe(recipe);
    });
  }

  renderCraftingHeader();
  renderRecipeBook(true);
  closeRecipeDetails();
}

function openCrafting() {
  if (!craftingScreen) return;

  resetTodayStatsIfNeeded();
  hideSubmenus();
  hideMenuHome();

  craftingScreen.classList.remove("hidden");

  selectedRecipe = null;
  closeRecipeDetails();
  renderCraftingHeader();
  renderRecipeBook(false);
}

function closeRecipeDetails() {
  selectedRecipe = null;

  if (recipeDetailsPanel) {
    recipeDetailsPanel.classList.add("hidden");
  }

  updateSelectedRecipeCard();
}

function renderCraftingHeader() {
  const progress = getCraftingProgress();

  if (craftingLevelLabel) {
    craftingLevelLabel.textContent = `Crafting Level ${playerCrafting.level}`;
  }

  if (craftingNextLevelText) {
    craftingNextLevelText.textContent = `Next Level ${playerCrafting.level + 1}`;
  }

  if (craftingXpText) {
    craftingXpText.textContent = `${Math.floor(progress.currentXpIntoLevel)} / ${progress.neededThisLevel} XP`;
  }

  if (craftingXpToGoText) {
    craftingXpToGoText.textContent = `${Math.ceil(progress.xpToGo)} XP to go`;
  }

  if (craftingXpFill) {
    craftingXpFill.style.width = `${progress.percent}%`;
  }

  if (craftingLevelBadgeLarge) {
    craftingLevelBadgeLarge.textContent = playerCrafting.level;
  }
}

function renderRecipeBook(force = false) {
  if (!recipeBookGrid) return;

  if (!force && recipeBookRenderedForLevel === playerCrafting.level && recipeBookGrid.children.length) {
    updateSelectedRecipeCard();
    return;
  }

  recipeBookRenderedForLevel = playerCrafting.level;

  const fragment = document.createDocumentFragment();

  RECIPES.forEach((recipe) => {
    const locked = recipe.level > playerCrafting.level;

    const card = document.createElement("div");
    card.className = `recipe-card ${locked ? "locked" : ""}`;
    card.dataset.recipeName = recipe.name;

    card.innerHTML = `
      <div class="recipe-card-level">${recipe.level}</div>
      <div class="recipe-card-icon">${locked ? "🔒" : recipe.icon}</div>
      <div class="recipe-card-name">${locked ? "Unlocks at" : escapeHtml(recipe.name)}</div>
      <div class="recipe-card-rarity">${locked ? `Lv. ${recipe.level}` : escapeHtml(recipe.rarity)}</div>
    `;

    fragment.appendChild(card);
  });

  recipeBookGrid.replaceChildren(fragment);
  updateSelectedRecipeCard();
}

function updateSelectedRecipeCard() {
  if (!recipeBookGrid) return;

  const selectedName = selectedRecipe ? selectedRecipe.name : null;

  recipeBookGrid.querySelectorAll(".recipe-card").forEach((card) => {
    card.classList.toggle("selected", card.dataset.recipeName === selectedName);
  });
}

function openRecipe(recipe, updateSelection = true) {
  selectedRecipe = recipe;

  if (recipeDetailsPanel) {
    recipeDetailsPanel.classList.remove("hidden");
  }

  if (recipeHeroIcon) recipeHeroIcon.textContent = recipe.icon;
  if (recipeLevel) recipeLevel.textContent = `Lv. ${recipe.level}`;
  if (recipeTitle) recipeTitle.textContent = recipe.name;
  if (recipeRarity) recipeRarity.textContent = recipe.rarity;
  if (recipeEffect) recipeEffect.textContent = recipe.effect;
  if (recipeCraftingXp) recipeCraftingXp.textContent = String(recipe.craftingXp);
  if (recipeBoostType) recipeBoostType.textContent = recipe.boostType || "Effect";
  if (recipeBoostAmount) recipeBoostAmount.textContent = recipe.boostAmount || "Boost";
  if (recipeDuration) recipeDuration.textContent = recipe.duration || "—";

  if (recipeIngredients) {
    recipeIngredients.innerHTML = recipe.ingredients
      .map((ingredient) => renderIngredientCard(ingredient))
      .join("");
  }

  if (updateSelection) {
    updateSelectedRecipeCard();
  }
}

function renderIngredientCard(ingredientText) {
  const parsed = parseIngredient(ingredientText);
  const icon = INGREDIENT_ICONS[parsed.name] || "🥣";

  return `
    <div class="recipe-ingredient-card">
      <div class="recipe-ingredient-icon">${icon}</div>
      <div class="recipe-ingredient-name">${escapeHtml(parsed.name)}</div>
      <div class="recipe-ingredient-count">${escapeHtml(parsed.count)}</div>
    </div>
  `;
}

function parseIngredient(ingredientText) {
  const text = String(ingredientText).trim();
  const match = text.match(/^(.*?)\s+x(\d+)$/i);

  if (match) {
    return {
      name: match[1].trim(),
      count: `${match[2]} / ${match[2]}`
    };
  }

  return {
    name: text,
    count: "1 / 1"
  };
}

function craftSelectedRecipe() {
  if (!selectedRecipe) {
    showToast("Crafting", "Choose a recipe first.");
    return;
  }

  if (selectedRecipe.level > playerCrafting.level) {
    showToast("Recipe locked", `Unlocks at crafting level ${selectedRecipe.level}.`);
    return;
  }

  addStat("itemsCrafted", 1);
  addCraftingXp(selectedRecipe.craftingXp);

  showToast(
    "Crafted",
    `${selectedRecipe.name} created. +${selectedRecipe.craftingXp} crafting XP.`
  );
}
/* ----------------------------- */
/* MARKET SYSTEM */
/* ----------------------------- */

function createDefaultMarketState() {
  return {
    wallet: 0,
    inventory: {},
    listings: [
      createNpcListing("wheat_seed", 8, 100),
      createNpcListing("corn_seed", 8, 100),
      createNpcListing("sugar_cane_seed", 8, 100),
      createNpcListing("wheat", 20, 50),
      createNpcListing("sugar_cane", 20, 40),
      createNpcListing("corn", 21, 35),
      createNpcListing("water", 19, 80),
      createNpcListing("fish", 22, 18),
      createNpcListing("milk", 52, 12),
      createNpcListing("flour", 50, 20),
      createNpcListing("bread", 53, 10),
      createNpcListing("energy_bar", 55, 8),
      createNpcListing("dinosaur_card", 100, 5),
      createNpcListing("planet_card", 105, 3),
      createNpcListing("paper_scarecrow_skin", 1000, 2)
    ],
    sales: [],
    transfers: [],
    nextListingId: 1000
  };
}

function createNpcListing(itemId, price, quantity) {
  return {
    id: `npc-${itemId}-${price}`,
    itemId,
    price,
    quantity,
    sellerId: "npc",
    sellerName: "GrowGo Market",
    createdAt: Date.now()
  };
}

function mergeDefaultNpcListings(savedListings, defaultListings) {
  const listings = Array.isArray(savedListings) ? [...savedListings] : [];

  defaultListings.forEach((defaultListing) => {
    const alreadyListed = listings.some((listing) => (
      listing.id === defaultListing.id ||
      (listing.sellerId === "npc" && listing.itemId === defaultListing.itemId)
    ));

    if (!alreadyListed) {
      listings.push(defaultListing);
    }
  });

  return listings;
}

function loadMarketState() {
  try {
    const raw = localStorage.getItem(MARKET_STORAGE_KEY);
    const defaults = createDefaultMarketState();

    if (!raw) return defaults;

    const saved = JSON.parse(raw);

    return {
      ...defaults,
      ...saved,
      inventory: {
        ...defaults.inventory,
        ...(saved.inventory || {})
      },
      listings: mergeDefaultNpcListings(saved.listings, defaults.listings),
      sales: Array.isArray(saved.sales) ? saved.sales : [],
      transfers: Array.isArray(saved.transfers) ? saved.transfers : [],
      nextListingId: saved.nextListingId || defaults.nextListingId
    };
  } catch (error) {
    console.warn("Could not load market.", error);
    return createDefaultMarketState();
  }
}

let marketState = loadMarketState();

function saveMarketState() {
  try {
    localStorage.setItem(MARKET_STORAGE_KEY, JSON.stringify(marketState));
  } catch (error) {
    console.warn("Could not save market.", error);
  }
}

function initMarketUi() {
  marketScreen = document.getElementById("marketScreen");
  marketItemsView = document.getElementById("marketItemsView");
  marketItemDetailsView = document.getElementById("marketItemDetailsView");
  marketBuyView = document.getElementById("marketBuyView");
  marketListingsView = document.getElementById("marketListingsView");
  marketSellOverlay = document.getElementById("marketSellOverlay");
  inventoryScreen = document.getElementById("inventoryScreen");
  inventoryBackBtn = document.getElementById("inventoryBackBtn");
  inventoryGrid = document.getElementById("inventoryGrid");
  inventoryListView = document.getElementById("inventoryListView");
  inventoryDetailsView = document.getElementById("inventoryDetailsView");
  inventoryDetailsCard = document.getElementById("inventoryDetailsCard");

  if (!marketScreen) return;

  marketScreen.addEventListener("click", handleMarketClick);
  marketScreen.addEventListener("pointerdown", handleMarketPointerDown);
  marketScreen.addEventListener("pointerup", clearMarketLongPress);
  marketScreen.addEventListener("pointerleave", clearMarketLongPress);
  marketScreen.addEventListener("pointercancel", clearMarketLongPress);

if (marketSellOverlay) {
  marketSellOverlay.addEventListener("click", (event) => {
    if (event.target === marketSellOverlay) {
      closeMarketSellPopup();
      return;
    }

    handleMarketClick(event);
  });
}

  renderMarket();
}

function openMarket() {
  if (!marketScreen) return;

  hideSubmenus();
  hideMenuHome();

  marketScreen.classList.remove("hidden");
  activeMarketCategory = activeMarketCategory || "resources";

  showMarketItemsView();
  renderMarket();
}

function closeMarket() {
  if (marketScreen) marketScreen.classList.add("hidden");
  closeMarketSellPopup();
  showMenuHome();
}

function handleMarketClick(event) {
  const backButton = event.target.closest("#marketBack, #marketBackBtn, .market-back-btn");
  if (backButton) {
    closeMarket();
    return;
  }

  const detailsBack = event.target.closest("#marketDetailsBackBtn, .market-details-back");
  if (detailsBack) {
    showMarketItemsView();
    return;
  }

  const buyBack = event.target.closest("#marketBuyBackBtn, .market-buy-back");
  if (buyBack) {
    showMarketDetailsView();
    return;
  }

  const listingsBack = event.target.closest("#marketListingsBackBtn, .market-listings-back");
  if (listingsBack) {
    showMarketItemsView();
    return;
  }

  const tab = event.target.closest(".market-tab");
  if (tab && !tab.dataset.marketView) {
    activeMarketCategory = tab.dataset.category || tab.dataset.marketCategory || activeMarketCategory;
    selectedMarketItemId = null;
    showMarketItemsView();
    renderMarket();
    return;
  }

  const buyHomeButton = event.target.closest("#marketBuyBtn, .market-buy-home-btn, [data-market-view='buy']");
  if (buyHomeButton) {
    showMarketItemsView();
    renderMarket();
    return;
  }

  const listingsButton = event.target.closest("#marketListingsBtn, .market-listings-btn, [data-market-view='listings']");
  if (listingsButton) {
    showMarketListingsView();
    return;
  }

  const rowBuyButton = event.target.closest("[data-market-buy-item-id]");
  if (rowBuyButton) {
    const itemId = rowBuyButton.dataset.marketBuyItemId;
    const lowest = getLowestMarketListing(itemId);

    selectedMarketItemId = itemId;
    selectedMarketPrice = lowest ? Number(lowest.price) : null;
    selectedMarketBuyQuantity = 1;

    if (selectedMarketPrice) {
      showMarketBuyView();
    } else {
      openMarketItem(itemId);
    }

    return;
  }

  const itemCard = event.target.closest("[data-market-item-id]");
  if (itemCard) {
    openMarketItem(itemCard.dataset.marketItemId);
    return;
  }

  const priceTier = event.target.closest("[data-market-price]");
  if (priceTier) {
    selectedMarketPrice = Number(priceTier.dataset.marketPrice);
    selectedMarketBuyQuantity = 1;
    showMarketBuyView();
    return;
  }

  const qtyButton = event.target.closest("[data-market-qty]");
  if (qtyButton) {
    changeMarketBuyQuantity(Number(qtyButton.dataset.marketQty));
    return;
  }

  const confirmBuy = event.target.closest("#marketConfirmBuyBtn, .market-confirm-buy");
  if (confirmBuy) {
    confirmMarketPurchase();
    return;
  }

  const cancelListing = event.target.closest("[data-cancel-listing-id]");
  if (cancelListing) {
    cancelMarketListing(cancelListing.dataset.cancelListingId);
    return;
  }

  const closeSell = event.target.closest("#marketSellCloseBtn, .market-sell-close");
  if (closeSell) {
    closeMarketSellPopup();
    return;
  }

  const sellQtyButton = event.target.closest("[data-sell-qty]");
  if (sellQtyButton) {
    changeMarketSellQuantity(Number(sellQtyButton.dataset.sellQty));
    return;
  }

  const sellPriceButton = event.target.closest("[data-sell-price]");
  if (sellPriceButton) {
    changeMarketSellPrice(Number(sellPriceButton.dataset.sellPrice));
    return;
  }

  const listItemButton = event.target.closest("#marketListItemBtn, .market-list-item-btn");
  if (listItemButton) {
    confirmMarketListing();
    return;
  }
}

function handleMarketPointerDown(event) {
  const ownedItem = event.target.closest("[data-market-owned-item-id]");
  if (!ownedItem) return;

  clearMarketLongPress();

  marketLongPressTimer = setTimeout(() => {
    openMarketSellPopup(ownedItem.dataset.marketOwnedItemId);
  }, MARKET_LONG_PRESS_MS);
}

function clearMarketLongPress() {
  if (marketLongPressTimer) {
    clearTimeout(marketLongPressTimer);
    marketLongPressTimer = null;
  }
}

function showMarketItemsView() {
  if (marketItemsView) marketItemsView.classList.remove("hidden");
  if (marketItemDetailsView) marketItemDetailsView.classList.add("hidden");
  if (marketBuyView) marketBuyView.classList.add("hidden");
  if (marketListingsView) marketListingsView.classList.add("hidden");

  renderMarket();
}

function showMarketDetailsView() {
  if (marketItemsView) marketItemsView.classList.add("hidden");
  if (marketItemDetailsView) marketItemDetailsView.classList.remove("hidden");
  if (marketBuyView) marketBuyView.classList.add("hidden");
  if (marketListingsView) marketListingsView.classList.add("hidden");

  renderMarketItemDetails();
}

function showMarketBuyView() {
  if (marketItemsView) marketItemsView.classList.add("hidden");
  if (marketItemDetailsView) marketItemDetailsView.classList.add("hidden");
  if (marketBuyView) marketBuyView.classList.remove("hidden");
  if (marketListingsView) marketListingsView.classList.add("hidden");

  renderMarketBuyView();
}

function showMarketListingsView() {
  if (marketItemsView) marketItemsView.classList.add("hidden");
  if (marketItemDetailsView) marketItemDetailsView.classList.add("hidden");
  if (marketBuyView) marketBuyView.classList.add("hidden");
  if (marketListingsView) marketListingsView.classList.remove("hidden");

  renderMarketListings();
}

function renderMarket() {
  renderMarketWallet();
  renderMarketTabs();
  renderMarketItems();
}

function renderMarketWallet() {
  const walletEls = marketScreen
    ? marketScreen.querySelectorAll("#marketWallet, .market-wallet")
    : [];

  walletEls.forEach((el) => {
    el.textContent = `${formatNumber(marketState.wallet)} coins`;
  });

  const coinCount = document.getElementById("coinCount");
  if (coinCount) {
    coinCount.textContent = formatNumber(marketState.wallet);
  }
}

function renderMarketTabs() {
  if (!marketScreen) return;

  marketScreen.querySelectorAll(".market-tab").forEach((tab) => {
    const tabCategory = tab.dataset.category || tab.dataset.marketCategory;
    tab.classList.toggle("active", tabCategory === activeMarketCategory);
  });
}

function renderMarketItems() {
  if (!marketItemsView) return;

  const items = MARKET_ITEMS.filter((item) => item.category === activeMarketCategory);

  marketItemsView.innerHTML = `
    <div class="market-tabs market-tabs-five">
      <button class="market-tab ${activeMarketCategory === "resources" ? "active" : ""}" data-market-category="resources" type="button">Resources</button>
      <button class="market-tab ${activeMarketCategory === "food" ? "active" : ""}" data-market-category="food" type="button">Food</button>
      <button class="market-tab ${activeMarketCategory === "cards" ? "active" : ""}" data-market-category="cards" type="button">Cards</button>
      <button class="market-tab ${activeMarketCategory === "skins" ? "active" : ""}" data-market-category="skins" type="button">Skins</button>
      <button class="market-tab market-listings-tab" data-market-view="listings" type="button">Listings</button>
    </div>

    <div class="market-panel">
      ${items.map(renderMarketItemCard).join("")}
    </div>
  `;
}

function renderMarketOwnedItems() {
  const ownedEntries = Object.entries(marketState.inventory)
    .filter(([, qty]) => Number(qty) > 0)
    .map(([itemId, qty]) => {
      const item = getMarketItem(itemId);
      if (!item) return "";

      return `
        <button class="market-owned-item" data-market-owned-item-id="${escapeAttribute(item.id)}" type="button">
          <span class="market-owned-icon">${item.icon}</span>
          <span class="market-owned-name">${escapeHtml(item.name)}</span>
          <span class="market-owned-qty">x${formatNumber(qty)}</span>
        </button>
      `;
    })
    .join("");

  return ownedEntries || `<div class="market-empty">No sellable items yet.</div>`;
}

function renderMarketItemCard(item) {
  const value = getMarketValue(item.id);
  const totalAvailable = getMarketAvailableQuantity(item.id);
  const lowest = getLowestMarketListing(item.id);

  return `
    <div class="market-item-card" data-market-item-id="${escapeAttribute(item.id)}">
      <div class="market-item-icon">${item.icon}</div>

      <div class="market-item-info">
        <div class="market-item-name">${escapeHtml(item.name)}</div>
        <div class="market-item-rarity">${escapeHtml(item.rarity)}</div>
      </div>

      <div class="market-item-values">
        <div class="market-item-price">${formatNumber(value)} coins</div>
        <div class="market-item-stock">
          ${totalAvailable > 0
            ? `${formatNumber(totalAvailable)} available · low ${formatNumber(lowest ? lowest.price : value)}`
            : "None listed"}
        </div>
      </div>

      <button
        class="market-row-buy-btn"
        data-market-buy-item-id="${escapeAttribute(item.id)}"
        type="button"
        ${totalAvailable > 0 ? "" : "disabled"}
      >
        Buy
      </button>
    </div>
  `;
}

function openMarketItem(itemId) {
  const item = getMarketItem(itemId);
  if (!item) return;

  selectedMarketItemId = itemId;
  selectedMarketPrice = null;
  selectedMarketBuyQuantity = 1;

  showMarketDetailsView();
}

function renderMarketItemDetails() {
  if (!marketItemDetailsView) return;

  const item = getMarketItem(selectedMarketItemId);
  if (!item) return;

  const priceTiers = getMarketPriceTiers(item.id);
  const value = getMarketValue(item.id);
  const allowed = getAllowedMarketPriceRange(item.id);

  marketItemDetailsView.innerHTML = `
    <div class="market-detail-panel">
      <div class="market-detail-hero">
        <div class="market-detail-icon">${item.icon}</div>
        <div>
          <h3>${escapeHtml(item.name)}</h3>
          <div class="market-item-rarity">${escapeHtml(item.rarity)}</div>
          <div class="market-item-price">Market value: ${formatNumber(value)} coins</div>
          <div class="market-item-stock">Sell range: ${formatNumber(allowed.min)}–${formatNumber(allowed.max)} coins</div>
        </div>
      </div>

      <div class="market-price-tier-list">
        ${priceTiers.length
          ? priceTiers.map((tier) => `
            <button class="market-price-tier-row" data-market-price="${tier.price}" type="button">
              <div>
                <strong>${formatNumber(tier.price)} coins</strong>
                <span>${formatNumber(tier.quantity)} available</span>
              </div>
              <div>Buy →</div>
            </button>
          `).join("")
          : `<div class="market-empty">No active listings for this item yet.</div>`}
      </div>
    </div>
  `;
}

function renderMarketBuyView() {
  if (!marketBuyView) return;

  const item = getMarketItem(selectedMarketItemId);
  if (!item || !selectedMarketPrice) return;

  const available = getMarketAvailableQuantityAtPrice(item.id, selectedMarketPrice);
  selectedMarketBuyQuantity = Math.max(1, Math.min(selectedMarketBuyQuantity, available));

  const totalCost = selectedMarketBuyQuantity * selectedMarketPrice;
  const canAfford = marketState.wallet >= totalCost;

  marketBuyView.innerHTML = `
    <div class="market-buy-panel">
      <div class="market-detail-hero">
        <div class="market-detail-icon">${item.icon}</div>
        <div>
          <h3>Buy ${escapeHtml(item.name)}</h3>
          <div>${formatNumber(selectedMarketPrice)} coins each</div>
          <div>${formatNumber(available)} available at this price</div>
        </div>
      </div>

      <div class="market-quantity-picker">
        <button data-market-qty="-1" type="button">−</button>
        <strong>${formatNumber(selectedMarketBuyQuantity)}</strong>
        <button data-market-qty="1" type="button">+</button>
      </div>

      <div class="market-total-line">
        Total: <strong>${formatNumber(totalCost)} coins</strong>
      </div>

      <button class="market-confirm-buy" type="button" ${canAfford ? "" : "disabled"}>
        ${canAfford ? "Confirm Purchase" : "Not Enough Coins"}
      </button>
    </div>
  `;
}

function changeMarketBuyQuantity(change) {
  const available = getMarketAvailableQuantityAtPrice(selectedMarketItemId, selectedMarketPrice);

  selectedMarketBuyQuantity = Math.max(
    1,
    Math.min(available, selectedMarketBuyQuantity + change)
  );

  renderMarketBuyView();
}

function confirmMarketPurchase() {
  const item = getMarketItem(selectedMarketItemId);
  if (!item || !selectedMarketPrice) return;

  const available = getMarketAvailableQuantityAtPrice(item.id, selectedMarketPrice);

  if (available <= 0) {
    showToast("Market", "That listing is no longer available.");
    showMarketDetailsView();
    return;
  }

  const quantityToBuy = Math.min(selectedMarketBuyQuantity, available);
  const totalCost = quantityToBuy * selectedMarketPrice;

  if (marketState.wallet < totalCost) {
    showToast("Market", "Not enough coins.");
    renderMarketBuyView();
    return;
  }

  let remaining = quantityToBuy;

  const matchingListings = marketState.listings
    .filter((listing) =>
      listing.itemId === item.id &&
      listing.price === selectedMarketPrice &&
      listing.quantity > 0
    )
    .sort((a, b) => a.createdAt - b.createdAt);

  matchingListings.forEach((listing) => {
    if (remaining <= 0) return;

    const taken = Math.min(remaining, listing.quantity);

    listing.quantity -= taken;
    remaining -= taken;

    marketState.transfers.push({
      type: "sale",
      listingId: listing.id,
      sellerId: listing.sellerId,
      sellerName: listing.sellerName,
      itemId: item.id,
      quantity: taken,
      price: selectedMarketPrice,
      total: taken * selectedMarketPrice,
      createdAt: Date.now()
    });
  });

  marketState.listings = marketState.listings.filter((listing) => listing.quantity > 0);

  marketState.wallet -= totalCost;
  marketState.inventory[item.id] = (marketState.inventory[item.id] || 0) + quantityToBuy;

  marketState.sales.push({
    itemId: item.id,
    price: selectedMarketPrice,
    quantity: quantityToBuy,
    total: totalCost,
    createdAt: Date.now()
  });

saveMarketState();
renderMarketWallet();
renderPlayerOverview();
renderMarket();
refreshInventoryIfOpen();

showToast("Purchased", `${quantityToBuy} ${item.name} for ${totalCost} coins.`);
showMarketDetailsView();
}

function openMarketSellPopup(itemId) {
  const item = getMarketItem(itemId);
  if (!item || !marketSellOverlay) return;

  const owned = marketState.inventory[itemId] || 0;

  if (owned <= 0) {
    showToast("Market", "You do not own any of that item.");
    return;
  }

  const allowed = getAllowedMarketPriceRange(itemId);
  const quantity = 1;
  const price = getMarketValue(itemId);

  marketSellOverlay.dataset.sellItemId = itemId;
  marketSellOverlay.dataset.sellQuantity = String(quantity);
  marketSellOverlay.dataset.sellPrice = String(Math.min(allowed.max, Math.max(allowed.min, price)));

  marketSellOverlay.classList.remove("hidden");

  renderMarketSellPopup();
}

function renderMarketSellPopup() {
  if (!marketSellOverlay) return;

  const itemId = marketSellOverlay.dataset.sellItemId;
  const item = getMarketItem(itemId);
  if (!item) return;

  const owned = marketState.inventory[itemId] || 0;
  const allowed = getAllowedMarketPriceRange(itemId);
  const quantity = Number(marketSellOverlay.dataset.sellQuantity || 1);
  const price = Number(marketSellOverlay.dataset.sellPrice || allowed.min);

  marketSellOverlay.innerHTML = `
    <div class="market-sell-popup">
      <button class="market-sell-close" type="button">×</button>

      <div class="market-detail-hero">
        <div class="market-detail-icon">${item.icon}</div>
        <div>
          <h3>Sell ${escapeHtml(item.name)}</h3>
          <div>${escapeHtml(item.rarity)}</div>
          <div>Owned: ${formatNumber(owned)}</div>
        </div>
      </div>

      <div class="market-sell-row">
        <span>Quantity</span>
        <div class="market-quantity-picker">
          <button data-sell-qty="-1" type="button">−</button>
          <strong>${formatNumber(quantity)}</strong>
          <button data-sell-qty="1" type="button">+</button>
        </div>
      </div>

      <div class="market-sell-row">
        <span>Price</span>
        <div class="market-quantity-picker">
          <button data-sell-price="-1" type="button">−</button>
          <strong>${formatNumber(price)}</strong>
          <button data-sell-price="1" type="button">+</button>
        </div>
      </div>

      <div class="market-item-stock">
        Allowed price: ${formatNumber(allowed.min)}–${formatNumber(allowed.max)} coins
      </div>

      <button id="marketListItemBtn" class="market-list-item-btn" type="button">
        List Item
            </button>
    </div>
  `;

  bindMarketSellPopupButtons();
}

function changeMarketSellQuantity(change) {
  if (!marketSellOverlay) return;

  const itemId = marketSellOverlay.dataset.sellItemId;
  const owned = marketState.inventory[itemId] || 0;

  const current = Number(marketSellOverlay.dataset.sellQuantity || 1);
  const next = Math.max(1, Math.min(owned, current + change));

  marketSellOverlay.dataset.sellQuantity = String(next);
  renderMarketSellPopup();
}

function bindMarketSellPopupButtons() {
  if (!marketSellOverlay) return;

  const closeBtn = marketSellOverlay.querySelector(".market-sell-close");
  const qtyMinusBtn = marketSellOverlay.querySelector("[data-sell-qty='-1']");
  const qtyPlusBtn = marketSellOverlay.querySelector("[data-sell-qty='1']");
  const priceMinusBtn = marketSellOverlay.querySelector("[data-sell-price='-1']");
  const pricePlusBtn = marketSellOverlay.querySelector("[data-sell-price='1']");
  const listBtn = marketSellOverlay.querySelector("#marketListItemBtn, .market-list-item-btn");

  if (closeBtn) {
    closeBtn.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      closeMarketSellPopup();
    };
  }

  if (qtyMinusBtn) {
    qtyMinusBtn.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      changeMarketSellQuantity(-1);
    };
  }

  if (qtyPlusBtn) {
    qtyPlusBtn.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      changeMarketSellQuantity(1);
    };
  }

  if (priceMinusBtn) {
    priceMinusBtn.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      changeMarketSellPrice(-1);
    };
  }

  if (pricePlusBtn) {
    pricePlusBtn.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      changeMarketSellPrice(1);
    };
  }

  if (listBtn) {
    listBtn.onclick = (event) => {
      event.preventDefault();
      event.stopPropagation();
      confirmMarketListing();
    };
  }
}

function changeMarketSellPrice(change) {
  if (!marketSellOverlay) return;

  const itemId = marketSellOverlay.dataset.sellItemId;
  const allowed = getAllowedMarketPriceRange(itemId);

  const current = Number(marketSellOverlay.dataset.sellPrice || allowed.min);
  const next = Math.max(allowed.min, Math.min(allowed.max, current + change));

  marketSellOverlay.dataset.sellPrice = String(next);
  renderMarketSellPopup();
}

function confirmMarketListing() {
  if (!marketSellOverlay) return;

  const itemId = marketSellOverlay.dataset.sellItemId;
  const item = getMarketItem(itemId);
  if (!item) return;

  const owned = marketState.inventory[itemId] || 0;
  const quantity = Number(marketSellOverlay.dataset.sellQuantity || 1);
  const price = Number(marketSellOverlay.dataset.sellPrice || 1);
  const allowed = getAllowedMarketPriceRange(itemId);

  if (quantity <= 0 || quantity > owned) {
    showToast("Market", "Invalid quantity.");
    return;
  }

  if (price < allowed.min || price > allowed.max) {
    showToast("Market", `Price must be ${allowed.min}–${allowed.max} coins.`);
    return;
  }

  marketState.inventory[itemId] = owned - quantity;

  marketState.listings.push({
    id: `player-${marketState.nextListingId}`,
    itemId,
    price,
    quantity,
    sellerId: "player",
    sellerName: "You",
    createdAt: Date.now()
  });

  marketState.nextListingId += 1;

  saveMarketState();
  closeMarketSellPopup();
  refreshInventoryIfOpen();

  showToast("Listed", `${quantity} ${item.name} listed for ${price} coins each.`);
  renderMarket();
}

function closeMarketSellPopup() {
  if (!marketSellOverlay) return;

  marketSellOverlay.classList.add("hidden");
  marketSellOverlay.innerHTML = "";
  delete marketSellOverlay.dataset.sellItemId;
  delete marketSellOverlay.dataset.sellQuantity;
  delete marketSellOverlay.dataset.sellPrice;
}

function renderMarketListings() {
  if (!marketListingsView) return;

  const playerListings = marketState.listings
    .filter((listing) => listing.sellerId === "player")
    .sort((a, b) => b.createdAt - a.createdAt);

  marketListingsView.innerHTML = `
    <div class="market-detail-panel">
      <h3>My Listings</h3>

      ${playerListings.length
        ? playerListings.map((listing) => {
          const item = getMarketItem(listing.itemId);

          return `
            <div class="market-price-tier-row">
              <div>
                <strong>${item ? `${item.icon} ${escapeHtml(item.name)}` : escapeHtml(listing.itemId)}</strong>
                <span>${formatNumber(listing.quantity)} listed · ${formatNumber(listing.price)} coins each</span>
              </div>

              <button data-cancel-listing-id="${escapeAttribute(listing.id)}" type="button">
                Cancel
              </button>
            </div>
          `;
        }).join("")
        : `<div class="market-empty">You do not have any active listings.</div>`}
    </div>
  `;
}

/* ----------------------------- */
/* INVENTORY SYSTEM */
/* ----------------------------- */

function initInventoryUi() {
  if (!inventoryScreen) return;

  if (inventoryBackBtn) {
    inventoryBackBtn.addEventListener("click", () => {
      closeInventory();
    });
  }

  inventoryScreen.addEventListener("click", handleInventoryClick);
  inventoryScreen.addEventListener("pointerdown", handleInventoryPointerDown);
  inventoryScreen.addEventListener("pointerup", clearInventoryLongPress);
  inventoryScreen.addEventListener("pointerleave", clearInventoryLongPress);
  inventoryScreen.addEventListener("pointercancel", clearInventoryLongPress);

  ensureInventorySpecialDefaults();
}

function ensureInventorySpecialDefaults() {
  if (!marketState || !marketState.inventory) return;

  if (marketState.inventory.legendary_boost === undefined) {
    marketState.inventory.legendary_boost = 1;
  }

  if (marketState.inventory.wizard_title_token === undefined) {
    marketState.inventory.wizard_title_token = 1;
  }

  saveMarketState();
}

function openInventory() {
  if (!inventoryScreen) return;

  hideSubmenus();
  hideMenuHome();

  inventoryScreen.classList.remove("hidden");

  activeInventoryCategory = activeInventoryCategory || "resources";
  selectedInventoryItemId = null;

  showInventoryListView();
  renderInventory();
}

function closeInventory() {
  if (inventoryScreen) inventoryScreen.classList.add("hidden");
  showMenuHome();
}

function handleInventoryClick(event) {
  const tab = event.target.closest(".inventory-tab");
  if (tab) {
    activeInventoryCategory = tab.dataset.inventoryCategory || "resources";
    selectedInventoryItemId = null;
    showInventoryListView();
    renderInventory();
    return;
  }

  const detailsBack = event.target.closest(".inventory-details-back");
  if (detailsBack) {
    selectedInventoryItemId = null;
    showInventoryListView();
    renderInventory();
    return;
  }

  const sellButton = event.target.closest("[data-inventory-sell-item-id]");
  if (sellButton) {
    const itemId = sellButton.dataset.inventorySellItemId;
    openInventorySellPopup(itemId);
    return;
  }

  const buyButton = event.target.closest("[data-inventory-buy-item-id]");
  if (buyButton) {
    const itemId = buyButton.dataset.inventoryBuyItemId;
    openInventoryBuyPrompt(itemId);
    return;
  }

  const itemCard = event.target.closest("[data-inventory-item-id]");
  if (itemCard) {
    if (inventoryLongPressTriggered) {
      inventoryLongPressTriggered = false;
      return;
    }

    openInventoryDetails(itemCard.dataset.inventoryItemId);
  }
}

function handleInventoryPointerDown(event) {
  const itemCard = event.target.closest("[data-inventory-item-id]");
  if (!itemCard) return;

  clearInventoryLongPress();

  const itemId = itemCard.dataset.inventoryItemId;

  inventoryLongPressTimer = setTimeout(() => {
    inventoryLongPressTriggered = true;
    openInventorySellPopup(itemId);
  }, INVENTORY_LONG_PRESS_MS);
}

function clearInventoryLongPress() {
  if (inventoryLongPressTimer) {
    clearTimeout(inventoryLongPressTimer);
    inventoryLongPressTimer = null;
  }

  setTimeout(() => {
    inventoryLongPressTriggered = false;
  }, 80);
}

function showInventoryListView() {
  if (inventoryListView) inventoryListView.classList.remove("hidden");
  if (inventoryDetailsView) inventoryDetailsView.classList.add("hidden");
}

function showInventoryDetailsView() {
  if (inventoryListView) inventoryListView.classList.add("hidden");
  if (inventoryDetailsView) inventoryDetailsView.classList.remove("hidden");
}

function renderInventory() {
  if (!inventoryGrid) return;

  inventoryScreen.querySelectorAll(".inventory-tab").forEach((tab) => {
    tab.classList.toggle(
      "active",
      tab.dataset.inventoryCategory === activeInventoryCategory
    );
  });

  const items = getInventoryItemsForCategory(activeInventoryCategory);

  if (!items.length) {
    inventoryGrid.innerHTML = `
      <div class="inventory-empty">
        No ${escapeHtml(activeInventoryCategory)} items yet.
      </div>
    `;
    return;
  }

  inventoryGrid.innerHTML = items.map(renderInventoryCard).join("");
}

function getInventoryItemsForCategory(category) {
  const inventory = marketState?.inventory || {};

  let baseItems = [];

  if (category === "special") {
    baseItems = INVENTORY_SPECIAL_ITEMS;
  } else {
    baseItems = MARKET_ITEMS.filter((item) => item.category === category);
  }

  const regularItems = baseItems
    .map((item) => {
      const fullItem = getInventoryItem(item.id);
      if (!fullItem) return null;

      return {
        ...fullItem,
        quantity: Number(inventory[item.id] || 0)
      };
    })
    .filter(Boolean)
    .sort((a, b) => {
      if (a.quantity > 0 && b.quantity <= 0) return -1;
      if (a.quantity <= 0 && b.quantity > 0) return 1;
      return a.name.localeCompare(b.name);
    });

  if (category !== "cards") {
    return regularItems;
  }

  return [
    ...getDuplicateCardInventoryItems(),
    ...regularItems
  ];
}

function getInventoryItem(itemId) {
  if (itemId && String(itemId).startsWith("duplicate-card-")) {
    return getDuplicateCardInventoryItem(itemId);
  }

  const marketItem = getMarketItem(itemId);

  if (marketItem) {
    return {
      ...marketItem,
      sellable: true,
      description: INVENTORY_DESCRIPTIONS[itemId] || "A useful GrowGo item."
    };
  }

  const specialItem = INVENTORY_SPECIAL_ITEMS.find((item) => item.id === itemId);

  if (specialItem) {
    return specialItem;
  }

  return null;
}

function getDuplicateCardInventoryItems() {
  return Object.entries(playerCardCollection?.duplicateCards || {})
    .map(([cardId, quantity]) => {
      if (Number(quantity) <= 0) return null;
      return getDuplicateCardInventoryItem(`duplicate-card-${cardId}`);
    })
    .filter(Boolean)
    .sort((a, b) => {
      if (a.quantity !== b.quantity) return b.quantity - a.quantity;
      return a.name.localeCompare(b.name);
    });
}

function getDuplicateCardInventoryItem(itemId) {
  const cardId = String(itemId || "").replace("duplicate-card-", "");
  const card = getCardById(cardId);
  const set = card ? getCardSet(card.setId) : null;
  if (!card || !set) return null;

  const variationLabel = card.isAnimatedVariation ? "Animated variation" : "Normal";
  const quantity = Number(playerCardCollection?.duplicateCards?.[card.cardId] || 0);

  return {
    id: `duplicate-card-${card.cardId}`,
    type: "card",
    cardId: card.cardId,
    name: card.cardName,
    icon: set.themeIcon || "🃏",
    rarity: card.isAnimatedVariation ? "Variation" : capitalizeInventoryText(card.rarity),
    category: "cards",
    setName: set.setName,
    variationType: card.variationType || "normal",
    quantity,
    sellable: true,
    description: `${set.setName} duplicate card · ${variationLabel}.`
  };
}

function renderInventoryCard(item) {
  const rarityClass = String(item.rarity || "common").toLowerCase();
  const isGhost = Number(item.quantity || 0) <= 0;
  const cardDuplicateClass = item.type === "card" ? " inventory-card-duplicate" : "";

  let hintText = "Unsellable";

  if (item.sellable && isGhost) {
    hintText = "Long press to buy";
  } else if (item.sellable) {
    hintText = "Long press to sell";
  }

  return `
    <button
      class="inventory-card${cardDuplicateClass} ${item.sellable ? "" : "unsellable"} ${isGhost ? "inventory-ghost-card" : ""}"
      data-inventory-item-id="${escapeAttribute(item.id)}"
      type="button"
    >
      <div class="inventory-qty">x${formatNumber(item.quantity)}</div>
      <div class="inventory-icon">${item.icon}</div>
      <div class="inventory-name">${escapeHtml(item.name)}</div>
      <div class="inventory-rarity ${escapeAttribute(rarityClass)}">
        ${escapeHtml(item.rarity)}
      </div>
      <div class="inventory-desc">${escapeHtml(item.description || "")}</div>
      <div class="${item.sellable ? "inventory-sell-hint" : "inventory-locked-hint"}">
        ${hintText}
      </div>
    </button>
  `;
}
function openInventoryDetails(itemId) {
  const item = getInventoryItem(itemId);
  if (!item || !inventoryDetailsCard) return;

  selectedInventoryItemId = itemId;
  showInventoryDetailsView();

  const quantity = item.type === "card"
    ? Number(playerCardCollection.duplicateCards[item.cardId] || 0)
    : marketState.inventory[itemId] || 0;
  const rarityClass = String(item.rarity || "common").toLowerCase();

  inventoryDetailsCard.innerHTML = `
    <div class="inventory-detail-hero">
      <div class="inventory-detail-icon">${item.icon}</div>

      <div class="inventory-detail-info">
        <h3>${escapeHtml(item.name)}</h3>
        <div class="inventory-rarity ${escapeAttribute(rarityClass)}">
          ${escapeHtml(item.rarity)}
        </div>
        <div class="inventory-detail-meta">
          Owned: x${formatNumber(quantity)}
        </div>
      </div>
    </div>

    <div class="inventory-detail-section">
      <span>Description</span>
      <p>${escapeHtml(item.description || "A GrowGo inventory item.")}</p>
    </div>

    <div class="inventory-detail-section">
      <span>Category</span>
      <strong>${escapeHtml(capitalizeInventoryText(item.category))}</strong>
    </div>

    <div class="inventory-detail-section">
      <span>Status</span>
      <strong>${item.sellable ? "Sellable on the Market" : "Unsellable special item"}</strong>
    </div>

    ${item.sellable && quantity > 0
      ? `
        <div class="inventory-actions">
          <button
            class="inventory-primary-btn"
            data-inventory-sell-item-id="${escapeAttribute(item.id)}"
            type="button"
          >
            Sell on Market
          </button>
        </div>
      `
      : item.sellable
        ? `
          <div class="inventory-actions">
            <button
              class="inventory-primary-btn"
              data-inventory-buy-item-id="${escapeAttribute(item.id)}"
              type="button"
            >
              Buy from Market
            </button>
          </div>
        `
        : `
          <div class="inventory-disabled-note">
            Special rewards and legendary boosts cannot be sold.
          </div>
        `}
  `;
}
function openInventorySellPopup(itemId) {
  const item = getInventoryItem(itemId);
  if (!item) return;

  if (item.type === "card") {
    openDuplicateCardSellPrompt(item);
    return;
  }

  if (!item.sellable) {
    showToast("Inventory", `${item.name} cannot be sold.`);
    return;
  }

  if (!marketState.inventory[itemId] || marketState.inventory[itemId] <= 0) {
    openInventoryBuyPrompt(itemId);
    return;
  }

  if (typeof openMarketSellPopup === "function") {
    openMarketSellPopup(itemId);
    return;
  }

  showToast("Market", "Sell popup is not available yet.");
}

function openDuplicateCardSellPrompt(item) {
  if (!item || item.type !== "card") return;
  if (Number(item.quantity || 0) <= 0) {
    showToast("Inventory", "No duplicate copies available.");
    return;
  }

  closeDuplicateCardSellPrompt();

  const overlay = document.createElement("div");
  overlay.id = "duplicateCardSellOverlay";
  overlay.className = "duplicate-card-sell-overlay";
  overlay.innerHTML = `
    <div class="duplicate-card-sell-popup">
      <button class="duplicate-card-sell-close" data-duplicate-card-sell-close type="button">×</button>
      <h3>Sell duplicate card?</h3>
      <div class="duplicate-card-sell-card">
        <div class="duplicate-card-sell-icon">${escapeHtml(item.icon)}</div>
        <div>
          <strong>${escapeHtml(item.name)}</strong>
          <span>${escapeHtml(item.setName || "Card Collection")}</span>
          <em>${escapeHtml(item.rarity)} · x${formatNumber(item.quantity)}</em>
        </div>
      </div>
      <p>Would you like to list this card on the Market?</p>
      <div class="duplicate-card-sell-actions">
        <button data-duplicate-card-sell-close type="button">Cancel</button>
        <button data-duplicate-card-sell-confirm="${escapeAttribute(item.cardId)}" type="button">Sell on Market</button>
      </div>
    </div>
  `;

  overlay.addEventListener("click", (event) => {
    if (event.target === overlay || event.target.closest("[data-duplicate-card-sell-close]")) {
      closeDuplicateCardSellPrompt();
      return;
    }

    const sellButton = event.target.closest("[data-duplicate-card-sell-confirm]");
    if (sellButton) {
      listDuplicateCardOnMarket(sellButton.dataset.duplicateCardSellConfirm);
    }
  });

  document.body.appendChild(overlay);
}

function closeDuplicateCardSellPrompt() {
  const overlay = document.getElementById("duplicateCardSellOverlay");
  if (overlay) overlay.remove();
}

function listDuplicateCardOnMarket(cardId) {
  const quantity = Number(playerCardCollection?.duplicateCards?.[cardId] || 0);
  const card = getCardById(cardId);

  if (!card || quantity <= 0) {
    showToast("Cards", "No duplicate copy available.");
    closeDuplicateCardSellPrompt();
    return;
  }

  playerCardCollection.duplicateCards[cardId] = quantity - 1;

  if (playerCardCollection.duplicateCards[cardId] <= 0) {
    delete playerCardCollection.duplicateCards[cardId];
  }

  savePlayerCardCollection();
  closeDuplicateCardSellPrompt();
  refreshInventoryIfOpen();
  showToast("Listed", `${card.cardName} duplicate queued for the Market.`);
}
function openInventoryBuyPrompt(itemId) {
  const item = getInventoryItem(itemId);
  if (!item || !item.sellable) return;

  closeInventoryBuyPrompt();

  const available = getMarketAvailableQuantity(itemId);
  const lowest = getLowestMarketListing(itemId);

  const overlay = document.createElement("div");
  overlay.id = "inventoryBuyOverlay";
  overlay.className = "inventory-buy-overlay";

  overlay.innerHTML = `
    <div class="inventory-buy-popup">
      <button class="inventory-buy-close" type="button">×</button>

      <div class="inventory-detail-hero">
        <div class="inventory-detail-icon">${item.icon}</div>

        <div class="inventory-detail-info">
          <h3>${escapeHtml(item.name)}</h3>
          <div class="inventory-rarity ${escapeAttribute(String(item.rarity || "common").toLowerCase())}">
            ${escapeHtml(item.rarity)}
          </div>
          <div class="inventory-detail-meta">
            You currently own x0
          </div>
        </div>
      </div>

      <div class="inventory-buy-message">
        Would you like to buy this item from the market?
      </div>

      <div class="inventory-buy-market-info">
        ${available > 0 && lowest
          ? `${formatNumber(available)} available · lowest price ${formatNumber(lowest.price)} coins`
          : "No active listings right now, but you can still view the market page."}
      </div>

      <div class="inventory-buy-actions">
        <button class="inventory-secondary-btn" data-inventory-buy-cancel type="button">
          Not Now
        </button>

        <button class="inventory-primary-btn" data-inventory-buy-confirm="${escapeAttribute(item.id)}" type="button">
          Buy from Market
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);

  overlay.addEventListener("click", (event) => {
    if (
      event.target === overlay ||
      event.target.closest(".inventory-buy-close") ||
      event.target.closest("[data-inventory-buy-cancel]")
    ) {
      closeInventoryBuyPrompt();
      return;
    }

    const confirmButton = event.target.closest("[data-inventory-buy-confirm]");
    if (confirmButton) {
      openMarketForInventoryBuy(confirmButton.dataset.inventoryBuyConfirm);
    }
  });
}

function closeInventoryBuyPrompt() {
  const overlay = document.getElementById("inventoryBuyOverlay");
  if (overlay) overlay.remove();
}

function openMarketForInventoryBuy(itemId) {
  const item = getMarketItem(itemId);
  if (!item) return;

  closeInventoryBuyPrompt();

  if (inventoryScreen) {
    inventoryScreen.classList.add("hidden");
  }

  openMarket();

  selectedMarketItemId = itemId;
  selectedMarketBuyQuantity = 1;

  const lowest = getLowestMarketListing(itemId);

  if (lowest) {
    selectedMarketPrice = lowest.price;
    showMarketBuyView();
  } else {
    selectedMarketPrice = null;
    showMarketDetailsView();
  }
}
function refreshInventoryIfOpen() {
  if (!inventoryScreen || inventoryScreen.classList.contains("hidden")) return;

  if (inventoryDetailsView && !inventoryDetailsView.classList.contains("hidden") && selectedInventoryItemId) {
    openInventoryDetails(selectedInventoryItemId);
    return;
  }

  renderInventory();
}

function capitalizeInventoryText(value) {
  const text = String(value || "");
  return text.charAt(0).toUpperCase() + text.slice(1);
}
function cancelMarketListing(listingId) {
  const listing = marketState.listings.find((entry) => entry.id === listingId);

  if (!listing || listing.sellerId !== "player") {
    showToast("Market", "Listing not found.");
    return;
  }

  marketState.inventory[listing.itemId] =
    (marketState.inventory[listing.itemId] || 0) + listing.quantity;

  marketState.listings = marketState.listings.filter((entry) => entry.id !== listingId);

   saveMarketState();
  refreshInventoryIfOpen();

  const item = getMarketItem(listing.itemId);
  showToast("Listing cancelled", `${listing.quantity} ${item ? item.name : "items"} returned to inventory.`);

  renderMarketListings();
}

function addMarketCoins(amount) {
  if (!marketState) return;

  marketState.wallet = Math.max(0, Number(marketState.wallet || 0) + Number(amount || 0));
  saveMarketState();
  renderMarketWallet();
  renderPlayerOverview();

  if (marketScreen && !marketScreen.classList.contains("hidden")) {
    renderMarket();
  }
}

function getMarketItem(itemId) {
  return MARKET_ITEMS.find((item) => item.id === itemId);
}

function getMarketValue(itemId) {
  const recentSales = marketState.sales
    .filter((sale) => sale.itemId === itemId)
    .slice(-20);

  if (recentSales.length) {
    const totalValue = recentSales.reduce((sum, sale) => sum + sale.total, 0);
    const totalQuantity = recentSales.reduce((sum, sale) => sum + sale.quantity, 0);

    if (totalQuantity > 0) {
      return Math.max(1, Math.round(totalValue / totalQuantity));
    }
  }

  const lowestListing = getLowestMarketListing(itemId);

  if (lowestListing) {
    return Math.max(1, Math.round(lowestListing.price));
  }

  const item = getMarketItem(itemId);
  const base = item ? MARKET_CATEGORY_BASE_PRICES[item.category] : 20;

  return Math.max(1, Math.round(base || 20));
}

function getAllowedMarketPriceRange(itemId) {
  const value = getMarketValue(itemId);

  return {
    min: Math.max(1, Math.round(value * 0.95)),
    max: Math.max(1, Math.round(value * 1.05))
  };
}

function getLowestMarketListing(itemId) {
  const listings = marketState.listings
    .filter((listing) => listing.itemId === itemId && listing.quantity > 0)
    .sort((a, b) => a.price - b.price || a.createdAt - b.createdAt);

  return listings[0] || null;
}

function getMarketPriceTiers(itemId) {
  const tiers = new Map();

  marketState.listings
    .filter((listing) => listing.itemId === itemId && listing.quantity > 0)
    .forEach((listing) => {
      const current = tiers.get(listing.price) || 0;
      tiers.set(listing.price, current + listing.quantity);
    });

  return Array.from(tiers.entries())
    .map(([price, quantity]) => ({ price, quantity }))
    .sort((a, b) => a.price - b.price);
}

function getMarketAvailableQuantity(itemId) {
  return marketState.listings
    .filter((listing) => listing.itemId === itemId)
    .reduce((sum, listing) => sum + listing.quantity, 0);
}

function getMarketAvailableQuantityAtPrice(itemId, price) {
  return marketState.listings
    .filter((listing) => listing.itemId === itemId && listing.price === price)
    .reduce((sum, listing) => sum + listing.quantity, 0);
}
/* ----------------------------- */
/* TRUSTED UTC TIME */
/* ----------------------------- */

async function syncTrustedTime() {
  try {
    const requestStartedAt = Date.now();

    const response = await fetch(TRUSTED_TIME_URL, {
      cache: "no-store"
    });

    if (!response.ok) {
      throw new Error("Trusted time request failed");
    }

    const data = await response.json();

    const serverIsoTime = data.dateTime.endsWith("Z")
      ? data.dateTime
      : data.dateTime + "Z";

    const serverTimeMs = new Date(serverIsoTime).getTime();

    if (Number.isNaN(serverTimeMs)) {
      throw new Error("Invalid trusted time response");
    }

    const requestFinishedAt = Date.now();
    const estimatedLatency = (requestFinishedAt - requestStartedAt) / 2;

    trustedTimeOffsetMs = serverTimeMs + estimatedLatency - requestFinishedAt;
    trustedTimeReady = true;
    lastUtcDayKey = getUtcDayKey(getTrustedNow());
  } catch (error) {
    console.warn("Trusted time sync failed:", error);
    trustedTimeReady = false;
    lastUtcDayKey = getUtcDayKey(Date.now());
    showToast("Time sync failed", "Using device time for this test build.");
  }
}

function getTrustedNow() {
  if (!trustedTimeReady) return Date.now();
  return Date.now() + trustedTimeOffsetMs;
}

function getUtcDayKey(timestamp) {
  const date = new Date(timestamp);
  return `${date.getUTCFullYear()}-${date.getUTCMonth()}-${date.getUTCDate()}`;
}

function getTimeUntilNextGmtDay() {
  const nowMs = getTrustedNow();
  const now = new Date(nowMs);

  const nextUtcMidnight = Date.UTC(
    now.getUTCFullYear(),
    now.getUTCMonth(),
    now.getUTCDate() + 1,
    0,
    0,
    0,
    0
  );

  const diff = Math.max(0, nextUtcMidnight - nowMs);

  const hours = Math.floor(diff / (1000 * 60 * 60));
  const minutes = Math.floor((diff / (1000 * 60)) % 60);
  const seconds = Math.floor((diff / 1000) % 60);

  return `${hours}h ${minutes}m ${seconds}s`;
}

function startGmtResetClock() {
  const clockEl = document.getElementById("gmtResetClock");
  if (!clockEl) return;

  function updateClock() {
    const now = getTrustedNow();
    const currentUtcDayKey = getUtcDayKey(now);

    clockEl.textContent = `Reset: ${getTimeUntilNextGmtDay()}`;

    if (lastUtcDayKey && currentUtcDayKey !== lastUtcDayKey) {
      lastUtcDayKey = currentUtcDayKey;
      resetTodayStatsIfNeeded();
      clearPinIconCache();
      scheduleRedrawPins();
    }
  }

  updateClock();

  if (gmtClockTimer) {
    clearInterval(gmtClockTimer);
  }

  gmtClockTimer = setInterval(updateClock, 1000);
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
let custom25DMapLayer = null;
let custom25DRoadFeatures = [];
let custom25DZoneFeatures = [];
let custom25DBuildingFeatures = [];

function syncCustom25DMapPresentation() {
  if (!map) return;

  const container = map.getContainer();
  const tilePane = map.getPane("tilePane");
  const overlayPane = map.getPane("overlayPane");

  if (ENABLE_CUSTOM_25D_MAP) {
    container.classList.add("custom-25d-map-enabled");
    /* Phase 7 checkpoint: Stable daytime 2.5D renderer. Current road/building/shop/zone balance is approved as the safe baseline. Future visual phases must preserve OSM visibility, pin dominance, and ENABLE_CUSTOM_25D_MAP safety. */
    /* Phase 6.7: Contrast hierarchy tune. Slight readability boost only. Must preserve OSM visibility and safe renderer behavior. */
    if (tilePane) {
      tilePane.style.filter = "grayscale(0.75) saturate(0.4) brightness(1.06) contrast(0.86)";
      tilePane.style.opacity = "0.45";
    }
    if (overlayPane) {
      overlayPane.style.opacity = "0.92";
    }
    return;
  }

  container.classList.remove("custom-25d-map-enabled");
  if (tilePane) {
    tilePane.style.filter = "";
    tilePane.style.opacity = "";
  }
  if (overlayPane) {
    overlayPane.style.opacity = "";
  }
}

function initCustom25DMapExperiment() {
  syncCustom25DMapPresentation();
  if (!ENABLE_CUSTOM_25D_MAP || !map || custom25DMapLayer) return;

  /* Custom 2.5D canvas must stay below pins, player marker, capture radius, and UI. */
  map.createPane("custom25DMapPane");
  const pane = map.getPane("custom25DMapPane");
  pane.style.zIndex = "350";
  pane.style.pointerEvents = "none";

  const canvas = L.DomUtil.create("canvas", "custom-25d-map-canvas", pane);
  canvas.style.position = "absolute";
  canvas.style.pointerEvents = "none";

  const redraw = () => drawCustom25DMapCanvas(canvas);
  custom25DMapLayer = { canvas, redraw };

  map.on("moveend zoomend", redraw);
  redraw();
}

function drawCustom25DMapCanvas(canvas) {
  if (!ENABLE_CUSTOM_25D_MAP || !map || !canvas) return;

  const size = map.getSize();
  const bounds = map.getBounds();
  const topLeft = map.latLngToLayerPoint(bounds.getNorthWest());
  L.DomUtil.setPosition(canvas, topLeft);

  const scale = window.devicePixelRatio || 1;
  canvas.width = Math.max(1, Math.round(size.x * scale));
  canvas.height = Math.max(1, Math.round(size.y * scale));
  canvas.style.width = `${size.x}px`;
  canvas.style.height = `${size.y}px`;

  const ctx = canvas.getContext("2d");
  ctx.setTransform(scale, 0, 0, scale, 0, 0);
  ctx.clearRect(0, 0, size.x, size.y);

  drawCustom25DBackground(ctx, size, bounds);
  drawCustom25DZones(ctx, bounds, topLeft);
  drawCustom25DBuildings(ctx, bounds, topLeft);
  drawCustom25DRoads(ctx, bounds, topLeft);
  drawCustom25DTrees(ctx, size, bounds);
  drawCustom25DLandmarkFoundation(ctx, bounds, topLeft);
}

function setCustom25DMapRoadFeatures(roadWays) {
  custom25DRoadFeatures = Array.isArray(roadWays) ? roadWays : [];

  if (ENABLE_CUSTOM_25D_MAP && custom25DMapLayer?.redraw) {
    custom25DMapLayer.redraw();
  }
}

function setCustom25DMapZoneFeatures(zoneFeatures) {
  custom25DZoneFeatures = Array.isArray(zoneFeatures) ? zoneFeatures : [];

  if (ENABLE_CUSTOM_25D_MAP && custom25DMapLayer?.redraw) {
    custom25DMapLayer.redraw();
  }
}

function setCustom25DMapBuildingFeatures(buildingFeatures) {
  custom25DBuildingFeatures = Array.isArray(buildingFeatures) ? buildingFeatures : [];

  if (ENABLE_CUSTOM_25D_MAP && custom25DMapLayer?.redraw) {
    custom25DMapLayer.redraw();
  }
}

function custom25DSeedFromBounds(bounds) {
  const center = bounds.getCenter();
  return Math.abs(Math.sin(center.lat * 12.9898 + center.lng * 78.233) * 43758.5453);
}

function custom25DRandom(seed, index) {
  return Math.abs(Math.sin(seed + index * 127.1) * 10000) % 1;
}

function custom25DPoint(size, seed, index) {
  return {
    x: custom25DRandom(seed, index) * size.x,
    y: custom25DRandom(seed, index + 41) * size.y
  };
}

function drawCustom25DBackground(ctx, size) {
  const bg = ctx.createLinearGradient(0, 0, size.x, size.y);
  bg.addColorStop(0, "rgba(241, 246, 226, 0.25)");
  bg.addColorStop(0.48, "rgba(229, 238, 213, 0.35)");
  bg.addColorStop(1, "rgba(214, 227, 194, 0.25)");
  ctx.fillStyle = bg;
  ctx.fillRect(0, 0, size.x, size.y);
}

function shouldDrawZoneDetailsAtZoom(zoom, detailLevel = "medium") {
  if (detailLevel === "low") return zoom >= 15;
  if (detailLevel === "high") return zoom >= 18;
  return zoom >= 16.5;
}

function getZoneStyleForFeature(featureType, zoom) {
  const styles = {
    park: {
      fill: "rgba(125, 198, 108, 0.40)",
      edge: "rgba(76, 144, 68, 0.32)",
      inner: "rgba(196, 233, 173, 0.11)"
    },
    grass: {
      fill: "rgba(154, 203, 130, 0.22)",
      edge: "rgba(108, 156, 89, 0.12)",
      inner: "rgba(201, 228, 178, 0.04)"
    },
    water: {
      fill: "rgba(92, 181, 221, 0.37)",
      edge: "rgba(56, 132, 192, 0.34)",
      inner: "rgba(204, 239, 251, 0.11)"
    },
    beach: {
      fill: "rgba(239, 221, 165, 0.34)",
      edge: "rgba(195, 171, 111, 0.22)",
      inner: "rgba(251, 239, 206, 0.11)"
    },
    wetland: {
      fill: "rgba(121, 176, 152, 0.24)",
      edge: "rgba(82, 133, 112, 0.18)",
      inner: "rgba(174, 214, 193, 0.06)"
    },
    sports: {
      fill: "rgba(122, 196, 114, 0.22)",
      edge: "rgba(78, 142, 80, 0.14)",
      inner: "rgba(207, 236, 192, 0.06)"
    }
  };

  const base = styles[featureType] || styles.grass;
  return {
    ...base,
    lineWidth: zoom >= 18 ? 2.2 : zoom >= 16.5 ? 1.6 : 1.1
  };
}

function projectCustom25DZonePoints(coords, topLeft) {
  if (!Array.isArray(coords) || !coords.length) return [];

  return coords.map(([lat, lng]) => {
    const point = map.latLngToLayerPoint([lat, lng]);
    return {
      x: point.x - topLeft.x,
      y: point.y - topLeft.y
    };
  });
}

function drawCustom25DZone(ctx, points, style, closed = true) {
  if (!Array.isArray(points) || points.length < 2) return;

  ctx.save();
  ctx.beginPath();
  ctx.moveTo(points[0].x, points[0].y);
  for (let i = 1; i < points.length; i += 1) {
    ctx.lineTo(points[i].x, points[i].y);
  }
  if (closed) ctx.closePath();

  if (closed) {
    ctx.fillStyle = style.fill;
    ctx.fill();
    ctx.strokeStyle = style.edge;
    ctx.lineWidth = style.lineWidth;
    ctx.stroke();
  } else {
    ctx.strokeStyle = style.fill;
    ctx.lineWidth = Math.max(4, style.lineWidth * 3.2);
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.stroke();
  }
  ctx.restore();
}

function drawWaterTexture(ctx, points, zoom, closed) {
  if (!shouldDrawZoneDetailsAtZoom(zoom, "medium") || !Array.isArray(points) || points.length < 2) return;

  const bounds = getProjectedBounds(points);
  const spacing = zoom >= 18 ? 22 : 30;

  ctx.save();
  ctx.strokeStyle = "rgba(255, 255, 255, 0.24)";
  ctx.lineWidth = 1.12;
  if (closed) {
    clipToProjectedPolygon(ctx, points);
  }

  for (let y = bounds.minY + 10; y < bounds.maxY; y += spacing) {
    ctx.beginPath();
    for (let x = bounds.minX - 12; x <= bounds.maxX + 12; x += 22) {
      const waveY = y + Math.sin((x + y) * 0.032) * 2.1;
      if (x === bounds.minX - 12) ctx.moveTo(x, waveY);
      else ctx.lineTo(x, waveY);
    }
    ctx.stroke();
  }
  ctx.restore();
}

function drawBeachDetails(ctx, points, zoom) {
  if (!shouldDrawZoneDetailsAtZoom(zoom, "high") || !Array.isArray(points) || points.length < 3) return;

  const bounds = getProjectedBounds(points);
  ctx.save();
  clipToProjectedPolygon(ctx, points);
  ctx.fillStyle = "rgba(255, 246, 214, 0.24)";

  for (let i = 0; i < 10; i += 1) {
    const x = bounds.minX + ((i * 41) % Math.max(40, bounds.maxX - bounds.minX + 1));
    const y = bounds.minY + ((i * 27) % Math.max(40, bounds.maxY - bounds.minY + 1));
    ctx.fillRect(x, y, 2, 2);
  }
  ctx.restore();
}

function drawGrassTexture(ctx, points, zoom, style) {
  if (!shouldDrawZoneDetailsAtZoom(zoom, "medium") || !Array.isArray(points) || points.length < 3) return;

  const bounds = getProjectedBounds(points);
  ctx.save();
  clipToProjectedPolygon(ctx, points);
  ctx.fillStyle = style.inner;

  const spacing = zoom >= 18 ? 22 : 32;
  for (let y = bounds.minY + 10; y < bounds.maxY; y += spacing) {
    ctx.fillRect(bounds.minX, y, Math.max(20, bounds.maxX - bounds.minX), zoom >= 18 ? 1.6 : 1);
  }
  ctx.restore();
}

function drawParkDetails(ctx, points, zoom) {
  if (!shouldDrawZoneDetailsAtZoom(zoom, "medium") || !Array.isArray(points) || points.length < 3) return;

  const bounds = getProjectedBounds(points);
  ctx.save();
  clipToProjectedPolygon(ctx, points);

  if (shouldDrawZoneDetailsAtZoom(zoom, "high")) {
    ctx.strokeStyle = "rgba(248, 240, 204, 0.24)";
    ctx.lineWidth = 1;
    for (let y = bounds.minY + 18; y < bounds.maxY; y += 54) {
      ctx.beginPath();
      ctx.moveTo(bounds.minX, y);
      ctx.quadraticCurveTo((bounds.minX + bounds.maxX) * 0.5, y + 6, bounds.maxX, y - 3);
      ctx.stroke();
    }
  }
  ctx.restore();
}

function drawSportsFieldDetails(ctx, points, zoom) {
  if (!shouldDrawZoneDetailsAtZoom(zoom, "high") || !Array.isArray(points) || points.length < 3) return;

  const bounds = getProjectedBounds(points);
  ctx.save();
  clipToProjectedPolygon(ctx, points);
  ctx.strokeStyle = "rgba(255, 255, 255, 0.28)";
  ctx.lineWidth = 1.2;
  ctx.strokeRect(bounds.minX + 8, bounds.minY + 8, Math.max(0, bounds.maxX - bounds.minX - 16), Math.max(0, bounds.maxY - bounds.minY - 16));
  ctx.restore();
}

function drawWetlandDetails(ctx, points, zoom) {
  if (!shouldDrawZoneDetailsAtZoom(zoom, "high") || !Array.isArray(points) || points.length < 3) return;

  const bounds = getProjectedBounds(points);
  ctx.save();
  clipToProjectedPolygon(ctx, points);
  ctx.strokeStyle = "rgba(89, 134, 104, 0.28)";
  ctx.lineWidth = 1;

  for (let x = bounds.minX + 10; x < bounds.maxX; x += 18) {
    ctx.beginPath();
    ctx.moveTo(x, bounds.maxY);
    ctx.lineTo(x - 2, bounds.maxY - 9);
    ctx.lineTo(x + 1, bounds.maxY - 17);
    ctx.stroke();
  }
  ctx.restore();
}

function getProjectedBounds(points) {
  return points.reduce((acc, point) => ({
    minX: Math.min(acc.minX, point.x),
    minY: Math.min(acc.minY, point.y),
    maxX: Math.max(acc.maxX, point.x),
    maxY: Math.max(acc.maxY, point.y)
  }), {
    minX: Number.POSITIVE_INFINITY,
    minY: Number.POSITIVE_INFINITY,
    maxX: Number.NEGATIVE_INFINITY,
    maxY: Number.NEGATIVE_INFINITY
  });
}

function clipToProjectedPolygon(ctx, points) {
  ctx.beginPath();
  ctx.moveTo(points[0].x, points[0].y);
  for (let i = 1; i < points.length; i += 1) {
    ctx.lineTo(points[i].x, points[i].y);
  }
  ctx.closePath();
  ctx.clip();
}

function drawCustom25DZones(ctx, bounds, topLeft) {
  if (!Array.isArray(custom25DZoneFeatures) || !custom25DZoneFeatures.length) return;

  const zoom = map.getZoom();
  const zonePriority = {
    grass: 1,
    sports: 2,
    park: 3,
    wetland: 4,
    beach: 5,
    water: 6
  };

  custom25DZoneFeatures
    .filter((feature) => Array.isArray(feature?.coords) && feature.coords.length >= 2)
    .filter((feature) => feature.coords.some(([lat, lng]) => bounds.contains([lat, lng])))
    .sort((a, b) => (zonePriority[a.zoneType] || 0) - (zonePriority[b.zoneType] || 0))
    .forEach((feature) => {
      const points = projectCustom25DZonePoints(feature.coords, topLeft);
      if (points.length < 2) return;

      const style = getZoneStyleForFeature(feature.zoneType, zoom);
      drawCustom25DZone(ctx, points, style, feature.closed !== false);

      if (feature.zoneType === "water") {
        drawWaterTexture(ctx, points, zoom, feature.closed !== false);
      } else if (feature.zoneType === "beach") {
        drawBeachDetails(ctx, points, zoom);
      } else if (feature.zoneType === "park") {
        drawParkDetails(ctx, points, zoom);
      } else if (feature.zoneType === "grass") {
        drawGrassTexture(ctx, points, zoom, style);
      } else if (feature.zoneType === "sports") {
        drawSportsFieldDetails(ctx, points, zoom);
      } else if (feature.zoneType === "wetland") {
        drawWetlandDetails(ctx, points, zoom);
      }
    });
}

function shouldDrawBuildingAtZoom(zoom) {
  return zoom >= 16.2;
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
const SHOP_25D_RECIPES = {
  bakery: {
    key: "bakery",
    palette: {
      roof: { top: "rgba(183, 108, 92, 0.86)", side: "rgba(149, 84, 71, 0.9)" },
      wall: { front: "rgba(238, 224, 196, 0.86)", side: "rgba(212, 194, 166, 0.9)" },
      awning: "rgba(197, 101, 82, 0.92)",
      sign: "rgba(131, 72, 52, 0.94)",
      window: "rgba(88, 64, 51, 0.34)"
    },
    awning: "striped",
    icon: "bread",
    outside: "crate",
    sizeBias: 0.98
  },
  cafe: {
    key: "cafe",
    palette: {
      roof: { top: "rgba(145, 122, 101, 0.84)", side: "rgba(113, 95, 79, 0.88)" },
      wall: { front: "rgba(229, 214, 194, 0.84)", side: "rgba(198, 183, 163, 0.88)" },
      awning: "rgba(120, 153, 124, 0.9)",
      sign: "rgba(93, 74, 58, 0.9)",
      window: "rgba(63, 73, 80, 0.38)"
    },
    awning: "flat",
    icon: "cup",
    outside: "table",
    sizeBias: 0.96
  },
  surfShop: {
    key: "surfShop",
    palette: {
      roof: { top: "rgba(109, 152, 188, 0.86)", side: "rgba(82, 122, 156, 0.9)" },
      wall: { front: "rgba(225, 237, 233, 0.84)", side: "rgba(196, 209, 205, 0.88)" },
      awning: "rgba(72, 170, 176, 0.92)",
      sign: "rgba(58, 108, 140, 0.94)",
      window: "rgba(67, 107, 125, 0.32)"
    },
    awning: "curved",
    icon: "surfboard",
    outside: "rack",
    sizeBias: 1.02
  },
  fishAndChips: {
    key: "fishAndChips",
    palette: {
      roof: { top: "rgba(116, 150, 182, 0.86)", side: "rgba(84, 118, 146, 0.9)" },
      wall: { front: "rgba(232, 239, 241, 0.86)", side: "rgba(201, 212, 216, 0.9)" },
      awning: "rgba(77, 128, 170, 0.92)",
      sign: "rgba(53, 90, 132, 0.95)",
      window: "rgba(52, 87, 122, 0.28)"
    },
    awning: "striped",
    icon: "fish",
    outside: "crate",
    sizeBias: 1
  },
  pharmacy: {
    key: "pharmacy",
    palette: {
      roof: { top: "rgba(173, 186, 180, 0.84)", side: "rgba(137, 149, 143, 0.88)" },
      wall: { front: "rgba(242, 245, 238, 0.88)", side: "rgba(217, 221, 214, 0.9)" },
      awning: "rgba(101, 176, 111, 0.9)",
      sign: "rgba(69, 139, 83, 0.95)",
      window: "rgba(93, 127, 107, 0.2)"
    },
    awning: "flat",
    icon: "cross",
    outside: "none",
    sizeBias: 0.94
  },
  realEstate: {
    key: "realEstate",
    palette: {
      roof: { top: "rgba(150, 151, 165, 0.84)", side: "rgba(114, 116, 130, 0.88)" },
      wall: { front: "rgba(231, 229, 224, 0.84)", side: "rgba(203, 201, 196, 0.88)" },
      awning: "rgba(122, 139, 164, 0.88)",
      sign: "rgba(84, 99, 124, 0.92)",
      window: "rgba(79, 96, 115, 0.26)"
    },
    awning: "flat",
    icon: "house",
    outside: "signboard",
    sizeBias: 0.98
  },
  bottleShop: {
    key: "bottleShop",
    palette: {
      roof: { top: "rgba(104, 96, 118, 0.86)", side: "rgba(76, 70, 92, 0.9)" },
      wall: { front: "rgba(197, 191, 205, 0.8)", side: "rgba(170, 164, 178, 0.86)" },
      awning: "rgba(102, 89, 133, 0.92)",
      sign: "rgba(73, 63, 100, 0.95)",
      window: "rgba(57, 52, 72, 0.34)"
    },
    awning: "flat",
    icon: "bottle",
    outside: "crate",
    sizeBias: 0.98
  },
  newsagent: {
    key: "newsagent",
    palette: {
      roof: { top: "rgba(166, 149, 121, 0.84)", side: "rgba(129, 115, 92, 0.88)" },
      wall: { front: "rgba(234, 224, 210, 0.84)", side: "rgba(207, 194, 178, 0.88)" },
      awning: "rgba(214, 121, 78, 0.92)",
      sign: "rgba(174, 92, 56, 0.96)",
      window: "rgba(76, 88, 98, 0.26)"
    },
    awning: "striped",
    icon: "paper",
    outside: "stand",
    sizeBias: 0.95
  },
  hardware: {
    key: "hardware",
    palette: {
      roof: { top: "rgba(151, 131, 109, 0.86)", side: "rgba(118, 102, 84, 0.9)" },
      wall: { front: "rgba(223, 214, 201, 0.84)", side: "rgba(195, 185, 171, 0.88)" },
      awning: "rgba(216, 134, 73, 0.92)",
      sign: "rgba(149, 91, 49, 0.96)",
      window: "rgba(88, 94, 92, 0.24)"
    },
    awning: "shed",
    icon: "hammer",
    outside: "crate",
    sizeBias: 1
  },
  supermarket: {
    key: "supermarket",
    palette: {
      roof: { top: "rgba(124, 137, 156, 0.84)", side: "rgba(95, 108, 126, 0.88)" },
      wall: { front: "rgba(227, 232, 229, 0.84)", side: "rgba(198, 204, 200, 0.88)" },
      awning: "rgba(106, 152, 118, 0.9)",
      sign: "rgba(72, 116, 86, 0.94)",
      window: "rgba(78, 111, 99, 0.22)"
    },
    awning: "wide",
    icon: "basket",
    outside: "cart",
    sizeBias: 1.08
  }
};

function hashFeatureSeed(input) {
  const text = String(input || "");
  let hash = 2166136261;

  for (let i = 0; i < text.length; i += 1) {
    hash ^= text.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }

  return (hash >>> 0) || 1;
}
/* CUSTOM 2.5D MAP EXPERIMENT END */

function getBuildingVariantFromSeed(seed) {
  const roofPalette = [
    { top: "rgba(185, 130, 118, 0.84)", side: "rgba(154, 104, 95, 0.88)" },
    { top: "rgba(184, 151, 122, 0.84)", side: "rgba(148, 120, 95, 0.88)" },
    { top: "rgba(145, 160, 173, 0.82)", side: "rgba(112, 127, 139, 0.86)" },
    { top: "rgba(165, 165, 170, 0.8)", side: "rgba(131, 131, 138, 0.84)" }
  ];
  const wallPalette = [
    { front: "rgba(231, 221, 204, 0.82)", side: "rgba(202, 189, 172, 0.86)" },
    { front: "rgba(224, 214, 198, 0.82)", side: "rgba(193, 181, 164, 0.86)" },
    { front: "rgba(214, 209, 216, 0.78)", side: "rgba(185, 178, 187, 0.82)" },
    { front: "rgba(207, 214, 207, 0.8)", side: "rgba(177, 184, 177, 0.84)" }
  ];

  const roofIndex = seed % roofPalette.length;
  const wallIndex = Math.floor(seed / 7) % wallPalette.length;

  return {
    roof: roofPalette[roofIndex],
    wall: wallPalette[wallIndex],
    height: 3.9 + ((seed % 4) * 0.72),
    roofLift: 1.2 + (seed % 2) * 0.6,
    shadowAlpha: 0.052 + ((seed % 4) * 0.015),
    skew: ((seed % 5) - 2) * 0.12,
    inset: 0.875 + ((seed % 3) * 0.02),
    depthX: 1.7 + ((Math.floor(seed / 3) % 4) * 0.34),
    depthY: 3.1 + ((Math.floor(seed / 11) % 4) * 0.44),
    roofScale: 1.014 + ((seed % 3) * 0.004),
    roofInsetScale: 0.9 + ((seed % 2) * 0.025),
    roofHighlightAlpha: 0.07 + ((seed % 3) * 0.018),
    wallHighlightAlpha: 0.045 + ((seed % 2) * 0.012),
    sideShadeAlpha: 0.07 + ((seed % 3) * 0.012),
    shadowOffsetX: 1.8 + ((seed % 3) * 0.28),
    shadowOffsetY: 2.15 + ((seed % 4) * 0.22),
    shadowBlur: 5.2 + ((seed % 4) * 0.8)
  };
}

function getBuildingStyleForFeature(feature, zoom) {
  const seed = hashFeatureSeed([
    feature?.id,
    feature?.center?.lat?.toFixed?.(6),
    feature?.center?.lng?.toFixed?.(6),
    feature?.buildingType,
    feature?.zoneType
  ].filter(Boolean).join(":"));
  const variant = getBuildingVariantFromSeed(seed);
  const detail = zoom >= 18;

  /* CUSTOM 2.5D MAP EXPERIMENT START */
  /* Phase 6.8: Building shape softening. Keep Phase 6.7 readability, but reduce harsh boxy building/shop shapes. */
  /* CUSTOM 2.5D MAP EXPERIMENT END */
  return {
    ...variant,
    seed,
    detail,
    roofLineWidth: detail ? 1.34 : 0.94,
    bodyLineWidth: detail ? 1.08 : 0.82,
    cornerRadius: detail ? 3.6 : 2.8
  };
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
function shouldDrawShopDetailsAtZoom(zoom) {
  return zoom >= 18.2;
}

function shouldDrawShopAtZoom(zoom) {
  return zoom >= 16.8;
}

function hasExactShopHint(feature) {
  return Boolean(getShopTagHint(feature));
}

function isCommercialFeature(feature) {
  if (!feature) return false;

  const buildingType = String(feature.buildingType || "").toLowerCase();
  const shopTag = String(feature.shopTag || "").toLowerCase();
  const amenity = String(feature.amenity || "").toLowerCase();
  const office = String(feature.office || "").toLowerCase();

  return Boolean(
    shopTag ||
    ["commercial", "retail", "supermarket", "kiosk", "shop"].includes(buildingType) ||
    ["cafe", "pharmacy", "fast_food", "restaurant", "bar", "pub"].includes(amenity) ||
    ["estate_agent", "real_estate_agent"].includes(office)
  );
}

function getShopTagHint(feature) {
  const shopTag = String(feature?.shopTag || "").toLowerCase();
  const amenity = String(feature?.amenity || "").toLowerCase();
  const cuisine = String(feature?.cuisine || "").toLowerCase();
  const office = String(feature?.office || "").toLowerCase();
  const buildingType = String(feature?.buildingType || "").toLowerCase();

  if (shopTag === "bakery") return "bakery";
  if (shopTag === "supermarket" || buildingType === "supermarket") return "supermarket";
  if (shopTag === "newsagent") return "newsagent";
  if (shopTag === "pharmacy" || amenity === "pharmacy") return "pharmacy";
  if (shopTag === "doityourself" || shopTag === "hardware") return "hardware";
  if (shopTag === "alcohol" || shopTag === "beverages" || shopTag === "wine") return "bottleShop";
  if (shopTag === "estate_agent" || office === "estate_agent" || office === "real_estate_agent") return "realEstate";
  if (shopTag === "sports" || shopTag === "surf") return "surfShop";
  if (shopTag === "seafood") return "fishAndChips";
  if (amenity === "cafe") return "cafe";
  if (amenity === "fast_food" || amenity === "restaurant") {
    if (cuisine.includes("fish") || cuisine.includes("seafood")) return "fishAndChips";
    return "cafe";
  }

  return null;
}

function getShopVariantFromSeed(seed, shopKey) {
  const stripeFlip = seed % 2 === 0;
  const awningDepth = 0.9 + ((seed % 4) * 0.18);
  const signHeight = 0.75 + ((seed % 3) * 0.18);
  const windowCols = 1 + (seed % 3);
  const doorwayOffset = ((seed % 5) - 2) * 0.06;

  return {
    key: shopKey,
    stripeFlip,
    awningDepth,
    signHeight,
    windowCols,
    doorwayOffset,
    detailVariant: Math.floor(seed / 13) % 3
  };
}

function getShopRecipeForFeature(feature) {
  const zoom = map?.getZoom?.() || 0;
  if (!shouldDrawShopAtZoom(zoom)) return null;
  if (!isCommercialFeature(feature)) return null;

  const seed = hashFeatureSeed([
    feature?.id,
    feature?.center?.lat?.toFixed?.(6),
    feature?.center?.lng?.toFixed?.(6),
    feature?.buildingType,
    feature?.shopTag,
    feature?.amenity,
    feature?.office
  ].filter(Boolean).join(":"));

  const hintedKey = getShopTagHint(feature);
  if (hintedKey && SHOP_25D_RECIPES[hintedKey]) {
    return {
      recipe: SHOP_25D_RECIPES[hintedKey],
      variant: getShopVariantFromSeed(seed, hintedKey),
      seed
    };
  }

  const buildingType = String(feature?.buildingType || "").toLowerCase();
  const baseChance = feature?.buildingArea > 900
    ? 0.42
    : feature?.buildingArea > 550
      ? 0.28
      : 0.16;
  const zoomBoost = zoom >= 18.4 ? 0.12 : zoom >= 17.6 ? 0.04 : 0;
  const roadBoost = ["retail", "commercial", "supermarket"].includes(buildingType) ? 0.12 : 0;
  const coastBoost = feature?.nearCoast ? 0.06 : 0;
  const conversionChance = Math.min(0.58, baseChance + zoomBoost + roadBoost + coastBoost);
  const chanceRoll = (seed % 1000) / 1000;
  if (chanceRoll > conversionChance) return null;

  const defaults = feature?.nearCoast
    ? ["surfShop", "fishAndChips", "cafe", "newsagent"]
    : ["cafe", "bakery", "newsagent", "realEstate"];
  const largerDefaults = feature?.buildingArea > 700
    ? ["supermarket", "hardware", "supermarket", "hardware"]
    : defaults;
  const pool = largerDefaults.filter((key) => SHOP_25D_RECIPES[key]);
  if (!pool.length) return null;

  const selectedKey = pool[seed % pool.length];
  return {
    recipe: SHOP_25D_RECIPES[selectedKey],
    variant: getShopVariantFromSeed(seed, selectedKey),
    seed
  };
}
/* CUSTOM 2.5D MAP EXPERIMENT END */

function projectCustom25DBuildingPoints(coords, topLeft) {
  if (!Array.isArray(coords) || coords.length < 3) return [];

  return coords.map(([lat, lng]) => {
    const point = map.latLngToLayerPoint([lat, lng]);
    return {
      x: point.x - topLeft.x,
      y: point.y - topLeft.y
    };
  });
}

function getBuildingCentroid(points) {
  const total = points.reduce((acc, point) => ({
    x: acc.x + point.x,
    y: acc.y + point.y
  }), { x: 0, y: 0 });

  return {
    x: total.x / points.length,
    y: total.y / points.length
  };
}

function offsetBuildingPoints(points, centroid, shiftX, shiftY, scale = 1) {
  return points.map((point) => ({
    x: centroid.x + (point.x - centroid.x) * scale + shiftX,
    y: centroid.y + (point.y - centroid.y) * scale + shiftY
  }));
}

function drawPolygonPath(ctx, points) {
  if (!Array.isArray(points) || points.length < 3) return;
  ctx.beginPath();
  ctx.moveTo(points[0].x, points[0].y);
  for (let i = 1; i < points.length; i += 1) {
    ctx.lineTo(points[i].x, points[i].y);
  }
  ctx.closePath();
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
function getSoftBuildingCornerRadius(points, softness = 0.16, maxRadius = 5.5) {
  const bounds = getProjectedBounds(points);
  const width = Math.max(1, bounds.maxX - bounds.minX);
  const height = Math.max(1, bounds.maxY - bounds.minY);
  return Math.min(maxRadius, Math.max(1.1, Math.min(width, height) * softness));
}

function drawRoundedPolygonPath(ctx, points, radius = 2.2) {
  if (!Array.isArray(points) || points.length < 3) return;

  const total = points.length;
  ctx.beginPath();

  for (let i = 0; i < total; i += 1) {
    const prev = points[(i - 1 + total) % total];
    const current = points[i];
    const next = points[(i + 1) % total];
    const inDx = current.x - prev.x;
    const inDy = current.y - prev.y;
    const outDx = next.x - current.x;
    const outDy = next.y - current.y;
    const inLen = Math.max(0.001, Math.hypot(inDx, inDy));
    const outLen = Math.max(0.001, Math.hypot(outDx, outDy));
    const cornerRadius = Math.min(radius, inLen * 0.33, outLen * 0.33);
    const startX = current.x - (inDx / inLen) * cornerRadius;
    const startY = current.y - (inDy / inLen) * cornerRadius;
    const endX = current.x + (outDx / outLen) * cornerRadius;
    const endY = current.y + (outDy / outLen) * cornerRadius;

    if (i === 0) ctx.moveTo(startX, startY);
    else ctx.lineTo(startX, startY);
    ctx.quadraticCurveTo(current.x, current.y, endX, endY);
  }

  ctx.closePath();
}
/* CUSTOM 2.5D MAP EXPERIMENT END */

function getSimplifiedBuildingShape(points, style) {
  const bounds = getProjectedBounds(points);
  const width = bounds.maxX - bounds.minX;
  const height = bounds.maxY - bounds.minY;
  const area = width * height;
  const useToyRect = points.length !== 4 || area < 420 || width > height * 2.6 || height > width * 2.6;

  if (!useToyRect) {
    return offsetBuildingPoints(points, getBuildingCentroid(points), 0, 0, style.inset);
  }

  const insetX = Math.max(2, width * (1 - style.inset) * 0.5);
  const insetY = Math.max(2, height * (1 - style.inset) * 0.5);
  return [
    { x: bounds.minX + insetX, y: bounds.minY + insetY },
    { x: bounds.maxX - insetX, y: bounds.minY + insetY },
    { x: bounds.maxX - insetX, y: bounds.maxY - insetY },
    { x: bounds.minX + insetX, y: bounds.maxY - insetY }
  ];
}

function getBuildingGeometry(points, style, scale = 1) {
  const basePoints = getSimplifiedBuildingShape(points, style).map((point) => ({ ...point }));
  const centroid = getBuildingCentroid(basePoints);
  const scaledBasePoints = scale === 1
    ? basePoints
    : offsetBuildingPoints(basePoints, centroid, 0, 0, scale);
  const roofPoints = offsetBuildingPoints(
    scaledBasePoints,
    centroid,
    (style.depthX ?? (style.skew + 1.8)),
    -(style.depthY ?? style.height),
    style.roofScale || 1.02
  );
  const shadowPoints = offsetBuildingPoints(
    scaledBasePoints,
    centroid,
    style.shadowOffsetX ?? (2.2 + style.skew),
    style.shadowOffsetY ?? (2.4 + style.height * 0.24),
    1.018
  );
  const bounds = getProjectedBounds(scaledBasePoints);
  const frontEdge = getDominantBuildingEdge(scaledBasePoints, "y");
  const sideEdge = getDominantBuildingEdge(scaledBasePoints, "x", frontEdge);

  return {
    centroid,
    bounds,
    basePoints: scaledBasePoints,
    roofPoints,
    shadowPoints,
    frontEdge,
    sideEdge
  };
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
// Phase 8 checkpoint: true 2.5D building depth pass.
// Building/shop depth only. Roads, pins, player marker, capture radius,
// gameplay, UI, and normal flag-off GrowGo behavior must remain unchanged.
// Phase 8.1 checkpoint: building softness + depth balance micro-pass.
// Softens Phase 8 building/shop depth without changing roads, pins,
// player marker, capture radius, gameplay, UI, density, or flag-off behavior.
function getEdgeKey(startIndex, endIndex) {
  return `${startIndex}:${endIndex}`;
}

function getDominantBuildingEdge(points, axis = "y", excludedEdge = null) {
  if (!Array.isArray(points) || points.length < 2) {
    return { startIndex: 0, endIndex: 0 };
  }

  const excludedKey = excludedEdge ? getEdgeKey(excludedEdge.startIndex, excludedEdge.endIndex) : "";
  let bestEdge = { startIndex: 0, endIndex: 1 };
  let bestScore = Number.NEGATIVE_INFINITY;

  for (let i = 0; i < points.length; i += 1) {
    const nextIndex = (i + 1) % points.length;
    const edgeKey = getEdgeKey(i, nextIndex);
    if (edgeKey === excludedKey) continue;
    const score = (points[i][axis] + points[nextIndex][axis]) * 0.5;
    if (score > bestScore) {
      bestScore = score;
      bestEdge = { startIndex: i, endIndex: nextIndex };
    }
  }

  return bestEdge;
}

function getBuildingDepth(style, zoom, emphasis = 1) {
  const zoomFactor = zoom >= 18.4 ? 1.08 : zoom >= 17.4 ? 1.02 : 0.96;
  return {
    x: (style.depthX || 1.9) * emphasis * zoomFactor,
    y: (style.depthY || style.height || 3.8) * emphasis * zoomFactor
  };
}

function getBuildingTone(color, alpha = 0.12) {
  return color.replace(/rgba\(([^)]+),\s*([0-9.]+)\)/, (_m, rgb, existingAlpha) => {
    const mixedAlpha = Math.min(0.95, Number(existingAlpha) + alpha);
    return `rgba(${rgb}, ${mixedAlpha.toFixed(2)})`;
  });
}

function drawBuildingShadow(ctx, shadowPoints, style, zoom, cornerRadius) {
  ctx.shadowColor = `rgba(68, 57, 46, ${Math.max(0.05, style.shadowAlpha).toFixed(2)})`;
  ctx.shadowBlur = zoom >= 18 ? (style.shadowBlur || 7.8) : Math.max(4.8, (style.shadowBlur || 7.8) - 1.4);
  ctx.shadowOffsetY = style.shadowOffsetY || 2.2;
  ctx.shadowOffsetX = style.shadowOffsetX || 1.8;
  ctx.fillStyle = `rgba(68, 57, 46, ${(style.shadowAlpha * 0.82).toFixed(2)})`;
  drawRoundedPolygonPath(ctx, shadowPoints, cornerRadius);
  ctx.fill();
  ctx.shadowColor = "transparent";
  ctx.shadowBlur = 0;
  ctx.shadowOffsetX = 0;
  ctx.shadowOffsetY = 0;
}

function drawBuildingVerticalFace(ctx, facePoints, fill, stroke, lineWidth, cornerRadius, innerAlpha = 0.06) {
  ctx.fillStyle = fill;
  drawRoundedPolygonPath(ctx, facePoints, cornerRadius);
  ctx.fill();

  ctx.fillStyle = `rgba(255,255,255,${innerAlpha.toFixed(2)})`;
  drawRoundedPolygonPath(ctx, offsetBuildingPoints(facePoints, getBuildingCentroid(facePoints), 0, 0, 0.95), Math.max(1.1, cornerRadius * 0.84));
  ctx.fill();

  ctx.strokeStyle = stroke;
  ctx.lineWidth = lineWidth;
  drawRoundedPolygonPath(ctx, facePoints, cornerRadius);
  ctx.stroke();
}

function drawBuildingRoof(ctx, roofPoints, style, roofCornerRadius) {
  ctx.fillStyle = style.roof.top;
  drawRoundedPolygonPath(ctx, roofPoints, roofCornerRadius);
  ctx.fill();

  const roofHighlightPoints = offsetBuildingPoints(
    roofPoints,
    getBuildingCentroid(roofPoints),
    0,
    0,
    style.roofInsetScale || 0.92
  );
  ctx.fillStyle = `rgba(255,255,255,${(style.roofHighlightAlpha || 0.08).toFixed(2)})`;
  drawRoundedPolygonPath(ctx, roofHighlightPoints, Math.max(1.1, roofCornerRadius * 0.78));
  ctx.fill();

  ctx.strokeStyle = style.roof.side;
  ctx.lineWidth = style.roofLineWidth;
  drawRoundedPolygonPath(ctx, roofPoints, roofCornerRadius);
  ctx.stroke();
}
/* CUSTOM 2.5D MAP EXPERIMENT END */

function drawGeneric25DBuilding(ctx, points, style, zoom, geometry = null) {
  if (!Array.isArray(points) || points.length < 3) return;

  const buildingGeometry = geometry || getBuildingGeometry(points, style);
  const {
    basePoints,
    roofPoints,
    shadowPoints,
    frontEdge,
    sideEdge
  } = buildingGeometry;
  const baseCornerRadius = getSoftBuildingCornerRadius(basePoints, 0.15, style.cornerRadius || 4);
  const roofCornerRadius = getSoftBuildingCornerRadius(roofPoints, 0.13, Math.max(1.6, (style.cornerRadius || 4) - 0.4));
  const faceRadius = Math.max(1.35, baseCornerRadius * 0.58);
  const frontFace = [
    basePoints[frontEdge.startIndex],
    basePoints[frontEdge.endIndex],
    roofPoints[frontEdge.endIndex],
    roofPoints[frontEdge.startIndex]
  ];
  const sideFace = [
    basePoints[sideEdge.startIndex],
    basePoints[sideEdge.endIndex],
    roofPoints[sideEdge.endIndex],
    roofPoints[sideEdge.startIndex]
  ];

  ctx.save();
  drawBuildingShadow(ctx, shadowPoints, style, zoom, baseCornerRadius);

  drawBuildingVerticalFace(
    ctx,
    sideFace,
    getBuildingTone(style.wall.side, Math.max(0.03, (style.sideShadeAlpha || 0.08) - 0.025)),
    "rgba(102, 92, 84, 0.08)",
    style.bodyLineWidth,
    Math.max(1.25, faceRadius * 0.98),
    0.018
  );

  drawBuildingVerticalFace(
    ctx,
    frontFace,
    style.wall.front,
    "rgba(122, 110, 100, 0.08)",
    style.bodyLineWidth,
    Math.max(1.3, faceRadius * 1.02),
    Math.max(0.022, (style.wallHighlightAlpha || 0.05) - 0.012)
  );

  drawBuildingRoof(ctx, roofPoints, style, roofCornerRadius);

  if (zoom >= 18) {
    const roofRidge = getDominantBuildingEdge(roofPoints, "y", frontEdge);
    ctx.strokeStyle = "rgba(255,255,255,0.08)";
    ctx.lineWidth = 0.62;
    ctx.beginPath();
    ctx.moveTo(roofPoints[roofRidge.startIndex].x, roofPoints[roofRidge.startIndex].y);
    ctx.lineTo(roofPoints[roofRidge.endIndex].x, roofPoints[roofRidge.endIndex].y);
    ctx.stroke();
  }

  ctx.restore();
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
function drawShopIcon25D(ctx, x, y, width, height, iconKey, color) {
  ctx.save();
  ctx.strokeStyle = color;
  ctx.fillStyle = color;
  ctx.lineWidth = Math.max(0.8, width * 0.08);
  ctx.lineCap = "round";
  ctx.lineJoin = "round";

  if (iconKey === "bread") {
    ctx.beginPath();
    ctx.roundRect(x - width * 0.3, y - height * 0.16, width * 0.6, height * 0.32, height * 0.16);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x - width * 0.16, y - height * 0.14);
    ctx.lineTo(x - width * 0.08, y + height * 0.12);
    ctx.moveTo(x, y - height * 0.15);
    ctx.lineTo(x + width * 0.06, y + height * 0.12);
    ctx.moveTo(x + width * 0.14, y - height * 0.12);
    ctx.lineTo(x + width * 0.18, y + height * 0.12);
    ctx.stroke();
  } else if (iconKey === "cup") {
    ctx.beginPath();
    ctx.moveTo(x - width * 0.22, y - height * 0.1);
    ctx.lineTo(x + width * 0.16, y - height * 0.1);
    ctx.lineTo(x + width * 0.1, y + height * 0.18);
    ctx.lineTo(x - width * 0.16, y + height * 0.18);
    ctx.closePath();
    ctx.stroke();
    ctx.beginPath();
    ctx.arc(x + width * 0.2, y + height * 0.02, width * 0.08, -Math.PI * 0.5, Math.PI * 0.5);
    ctx.stroke();
  } else if (iconKey === "surfboard") {
    ctx.beginPath();
    ctx.ellipse(x, y, width * 0.12, height * 0.26, 0.28, 0, Math.PI * 2);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x, y - height * 0.2);
    ctx.lineTo(x, y + height * 0.22);
    ctx.stroke();
  } else if (iconKey === "fish") {
    ctx.beginPath();
    ctx.ellipse(x, y, width * 0.17, height * 0.12, 0, 0, Math.PI * 2);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x + width * 0.17, y);
    ctx.lineTo(x + width * 0.28, y - height * 0.1);
    ctx.lineTo(x + width * 0.28, y + height * 0.1);
    ctx.closePath();
    ctx.stroke();
  } else if (iconKey === "cross") {
    ctx.fillRect(x - width * 0.05, y - height * 0.22, width * 0.1, height * 0.44);
    ctx.fillRect(x - width * 0.22, y - height * 0.05, width * 0.44, height * 0.1);
  } else if (iconKey === "house") {
    ctx.beginPath();
    ctx.moveTo(x - width * 0.22, y + height * 0.12);
    ctx.lineTo(x - width * 0.22, y - height * 0.02);
    ctx.lineTo(x, y - height * 0.22);
    ctx.lineTo(x + width * 0.22, y - height * 0.02);
    ctx.lineTo(x + width * 0.22, y + height * 0.12);
    ctx.closePath();
    ctx.stroke();
  } else if (iconKey === "bottle") {
    ctx.beginPath();
    ctx.roundRect(x - width * 0.08, y - height * 0.2, width * 0.16, height * 0.4, width * 0.06);
    ctx.stroke();
    ctx.fillRect(x - width * 0.03, y - height * 0.26, width * 0.06, height * 0.06);
  } else if (iconKey === "paper") {
    ctx.beginPath();
    ctx.rect(x - width * 0.18, y - height * 0.18, width * 0.32, height * 0.34);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x - width * 0.12, y - height * 0.06);
    ctx.lineTo(x + width * 0.08, y - height * 0.06);
    ctx.moveTo(x - width * 0.12, y + 0.01);
    ctx.lineTo(x + width * 0.08, y + 0.01);
    ctx.stroke();
  } else if (iconKey === "hammer") {
    ctx.beginPath();
    ctx.moveTo(x - width * 0.18, y + height * 0.14);
    ctx.lineTo(x + width * 0.08, y - height * 0.12);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x - width * 0.04, y - height * 0.14);
    ctx.lineTo(x + width * 0.16, y - height * 0.14);
    ctx.lineTo(x + width * 0.12, y - height * 0.02);
    ctx.lineTo(x - width * 0.08, y - height * 0.02);
    ctx.closePath();
    ctx.fill();
  } else if (iconKey === "basket") {
    ctx.beginPath();
    ctx.moveTo(x - width * 0.18, y - height * 0.02);
    ctx.lineTo(x - width * 0.12, y + height * 0.18);
    ctx.lineTo(x + width * 0.12, y + height * 0.18);
    ctx.lineTo(x + width * 0.18, y - height * 0.02);
    ctx.closePath();
    ctx.stroke();
    ctx.beginPath();
    ctx.arc(x, y - height * 0.04, width * 0.12, Math.PI, 0);
    ctx.stroke();
  }

  ctx.restore();
}

function drawShopOutsideDetail(ctx, x, y, size, detailKey, color) {
  if (!detailKey || detailKey === "none") return;

  ctx.save();
  ctx.fillStyle = color;
  ctx.strokeStyle = "rgba(76, 66, 58, 0.34)";
  ctx.lineWidth = 0.8;

  if (detailKey === "table") {
    ctx.beginPath();
    ctx.arc(x, y, size * 0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillRect(x - size * 0.02, y, size * 0.04, size * 0.14);
  } else if (detailKey === "signboard") {
    ctx.fillRect(x - size * 0.04, y - size * 0.1, size * 0.08, size * 0.22);
    ctx.fillRect(x - size * 0.12, y - size * 0.18, size * 0.24, size * 0.1);
  } else if (detailKey === "stand") {
    ctx.fillRect(x - size * 0.14, y - size * 0.1, size * 0.28, size * 0.16);
  } else if (detailKey === "cart") {
    ctx.beginPath();
    ctx.rect(x - size * 0.14, y - size * 0.1, size * 0.22, size * 0.12);
    ctx.stroke();
    ctx.beginPath();
    ctx.arc(x - size * 0.08, y + size * 0.05, size * 0.03, 0, Math.PI * 2);
    ctx.arc(x + size * 0.04, y + size * 0.05, size * 0.03, 0, Math.PI * 2);
    ctx.fill();
  } else if (detailKey === "rack") {
    ctx.strokeRect(x - size * 0.05, y - size * 0.18, size * 0.1, size * 0.3);
  } else if (detailKey === "crate") {
    ctx.fillRect(x - size * 0.08, y - size * 0.08, size * 0.16, size * 0.1);
  }

  ctx.restore();
}

function drawShop25D(ctx, points, style, zoom, recipeBundle) {
  if (!recipeBundle?.recipe) {
    drawGeneric25DBuilding(ctx, points, style, zoom);
    return;
  }

  const { recipe, variant } = recipeBundle;
  const compactScale = Math.min(0.94, recipe.sizeBias || 0.96);
  const softenedStyle = {
    ...style,
    height: Math.max(3.1, style.height * 0.86),
    skew: style.skew * 0.72,
    depthX: getBuildingDepth(style, zoom, 1.12).x,
    depthY: getBuildingDepth(style, zoom, 1.12).y,
    roofScale: Math.max(style.roofScale || 1.02, 1.022),
    roofInsetScale: Math.min(0.95, (style.roofInsetScale || 0.92) + 0.015),
    roofHighlightAlpha: Math.max(style.roofHighlightAlpha || 0.08, 0.086),
    shadowAlpha: Math.max(style.shadowAlpha || 0.06, 0.068),
    shadowOffsetX: Math.max(style.shadowOffsetX || 1.8, 2.05),
    shadowOffsetY: Math.max(style.shadowOffsetY || 2.2, 2.35),
    shadowBlur: Math.max(style.shadowBlur || 6.4, 6.9),
    cornerRadius: (style.cornerRadius || 3.2) + 0.55
  };
  const geometry = getBuildingGeometry(points, {
    ...softenedStyle
  }, compactScale);
  const {
    basePoints,
    bounds
  } = geometry;
  const centroid = getBuildingCentroid(basePoints);
  const frontTop = Math.min(basePoints[0].y, basePoints[1].y, basePoints[2].y, basePoints[3].y);
  const frontBottom = Math.max(basePoints[0].y, basePoints[1].y, basePoints[2].y, basePoints[3].y);
  const width = Math.max(12, bounds.maxX - bounds.minX);
  const height = Math.max(12, frontBottom - frontTop);

  ctx.save();
  drawGeneric25DBuilding(ctx, points, {
    ...softenedStyle,
    roof: recipe.palette.roof,
    wall: recipe.palette.wall,
    shadowAlpha: Math.max(style.shadowAlpha, 0.06)
  }, zoom, geometry);

  const signHeight = Math.max(2.55, height * 0.088 * variant.signHeight);
  const signY = frontTop + height * 0.10;
  const signWidth = width * 0.49;
  const signX = centroid.x - signWidth * 0.5;

  ctx.fillStyle = "rgba(62, 53, 45, 0.10)";
  ctx.beginPath();
  ctx.roundRect(signX + 0.5, signY + 0.8, signWidth, signHeight, signHeight * 0.56);
  ctx.fill();

  ctx.fillStyle = recipe.palette.sign;
  ctx.beginPath();
  ctx.roundRect(signX, signY, signWidth, signHeight, signHeight * 0.56);
  ctx.fill();
  ctx.fillStyle = "rgba(255,255,255,0.05)";
  ctx.beginPath();
  ctx.roundRect(signX + signWidth * 0.08, signY + signHeight * 0.12, signWidth * 0.78, signHeight * 0.28, signHeight * 0.32);
  ctx.fill();

  const awningY = signY + signHeight + height * 0.04;
  const awningHeight = Math.max(3.05, height * 0.128 * variant.awningDepth);
  const awningInset = width * 0.12;
  const awningWidth = width - awningInset * 2;
  const awningX = bounds.minX + awningInset;

  ctx.fillStyle = "rgba(61, 52, 44, 0.06)";
  ctx.beginPath();
  ctx.moveTo(awningX + 0.5, awningY + 0.8);
  ctx.lineTo(awningX + awningWidth - 0.5, awningY + 0.8);
  ctx.lineTo(awningX + awningWidth * 0.94, awningY + awningHeight + 0.9);
  ctx.lineTo(awningX + awningWidth * 0.06, awningY + awningHeight + 0.9);
  ctx.closePath();
  ctx.fill();

  ctx.fillStyle = recipe.palette.awning;

  if (recipe.awning === "curved") {
    ctx.beginPath();
    ctx.moveTo(awningX, awningY);
    ctx.quadraticCurveTo(centroid.x, awningY + awningHeight, awningX + awningWidth, awningY);
    ctx.lineTo(awningX + awningWidth, awningY + awningHeight * 0.45);
    ctx.quadraticCurveTo(centroid.x, awningY + awningHeight * 1.25, awningX, awningY + awningHeight * 0.45);
    ctx.closePath();
    ctx.fill();
  } else {
    const topLift = recipe.awning === "shed" ? -awningHeight * 0.18 : 0;
    ctx.beginPath();
    ctx.moveTo(awningX, awningY + topLift);
    ctx.lineTo(awningX + awningWidth, awningY + topLift);
    ctx.lineTo(awningX + awningWidth * 0.95, awningY + awningHeight);
    ctx.lineTo(awningX + awningWidth * 0.05, awningY + awningHeight);
    ctx.closePath();
    ctx.fill();
  }
  ctx.fillStyle = "rgba(255,255,255,0.05)";
  ctx.fillRect(awningX + awningWidth * 0.08, awningY + awningHeight * 0.16, awningWidth * 0.74, Math.max(0.85, awningHeight * 0.15));

  if (recipe.awning === "striped" && shouldDrawShopDetailsAtZoom(zoom)) {
    const stripeCount = 3 + (variant.detailVariant % 2);
    ctx.fillStyle = "rgba(255,255,255,0.22)";
    for (let i = 0; i < stripeCount; i += 1) {
      const stripeWidth = awningWidth / stripeCount;
      const stripeIndex = variant.stripeFlip ? i : stripeCount - i - 1;
      ctx.fillRect(awningX + stripeIndex * stripeWidth + stripeWidth * 0.18, awningY + awningHeight * 0.12, stripeWidth * 0.24, awningHeight * 0.62);
    }
  }

  const windowTop = awningY + awningHeight + height * 0.04;
  const windowBottom = frontBottom - height * 0.10;
  const windowHeight = Math.max(4, windowBottom - windowTop);
  const windowWidth = Math.max(4.5, (width * 0.62) / variant.windowCols);
  const windowGap = width * 0.035;
  const totalWindowWidth = variant.windowCols * windowWidth + Math.max(0, variant.windowCols - 1) * windowGap;
  const windowX = centroid.x - totalWindowWidth * 0.5;
  const doorWidth = Math.max(3.6, width * 0.14);
  const doorHeight = Math.max(5.8, height * 0.22);
  const doorX = centroid.x + width * variant.doorwayOffset - doorWidth * 0.5;
  const doorY = frontBottom - doorHeight - height * 0.035;

  ctx.fillStyle = recipe.palette.window;
  for (let i = 0; i < variant.windowCols; i += 1) {
    const x = windowX + i * (windowWidth + windowGap);
    if (x + windowWidth > doorX - 2 && x < doorX + doorWidth + 2) continue;
    ctx.beginPath();
    ctx.roundRect(x, windowTop, windowWidth, windowHeight, Math.min(4.5, windowWidth * 0.24));
    ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.045)";
      ctx.beginPath();
      ctx.roundRect(x + windowWidth * 0.08, windowTop + windowHeight * 0.1, windowWidth * 0.32, windowHeight * 0.22, Math.min(2.4, windowWidth * 0.12));
      ctx.fill();
    ctx.fillStyle = recipe.palette.window;
  }

  ctx.fillStyle = "rgba(62, 58, 57, 0.43)";
  ctx.beginPath();
  ctx.roundRect(doorX, doorY, doorWidth, doorHeight, Math.min(4.5, doorWidth * 0.24));
  ctx.fill();

  if (shouldDrawShopDetailsAtZoom(zoom)) {
    drawShopIcon25D(
      ctx,
      centroid.x,
      signY + signHeight * 0.54,
      signWidth * 0.72,
      signHeight * 1.55,
      recipe.icon,
      "rgba(255, 252, 240, 0.84)"
    );

    drawShopOutsideDetail(
      ctx,
      bounds.minX + width * 0.16,
      frontBottom - height * 0.01,
      Math.min(width, height) * 0.7,
      recipe.outside,
      "rgba(119, 112, 92, 0.48)"
    );
  }

  ctx.restore();
}
/* CUSTOM 2.5D MAP EXPERIMENT END */

function drawCustom25DBuildings(ctx, bounds, topLeft) {
  if (!shouldDrawBuildingAtZoom(map.getZoom())) return;
  if (!Array.isArray(custom25DBuildingFeatures) || !custom25DBuildingFeatures.length) return;

  const zoom = map.getZoom();
  const maxBuildings = zoom >= 18 ? 120 : zoom >= 17 ? 80 : 45;
  let drawn = 0;

  custom25DBuildingFeatures.forEach((feature) => {
    if (drawn >= maxBuildings) return;
    if (!Array.isArray(feature?.coords) || feature.coords.length < 3) return;
    if (!feature.coords.some(([lat, lng]) => bounds.contains([lat, lng]))) return;

    const points = projectCustom25DBuildingPoints(feature.coords, topLeft);
    if (points.length < 3) return;

    const style = getBuildingStyleForFeature(feature, zoom);
    const shopRecipe = getShopRecipeForFeature(feature);
    if (shopRecipe) {
      drawShop25D(ctx, points, style, zoom, shopRecipe);
    } else {
      drawGeneric25DBuilding(ctx, points, style, zoom);
    }
    drawn += 1;
  });
}

function getRoadStyleForFeature(highwayType, zoom) {
  const normalized = String(highwayType || "residential").toLowerCase();
  const zoomBoost = Math.max(0, zoom - 15) * 0.55;
  /* Phase 6.9: Road charm pass. Make roads feel slightly more polished and integrated without overpowering OSM or pins. */
  /* Phase 6.9.1: Road balance micro-pass. Preserve road charm while keeping roads calm under pins. */
  const styles = {
    primary: {
      width: 12.1 + zoomBoost,
      fill: "rgba(232, 188, 118, 0.95)",
      edge: "rgba(189, 140, 88, 0.30)",
      highlight: "rgba(255, 244, 216, 0.34)",
      warmCore: "rgba(246, 213, 152, 0.20)",
      shadow: "rgba(118, 93, 61, 0.08)"
    },
    secondary: {
      width: 8.9 + zoomBoost * 0.8,
      fill: "rgba(239, 223, 184, 0.94)",
      edge: "rgba(175, 149, 110, 0.20)",
      highlight: "rgba(255, 249, 232, 0.24)",
      warmCore: "rgba(245, 226, 186, 0.14)",
      shadow: "rgba(109, 95, 66, 0.06)"
    },
    residential: {
      width: 6.45 + zoomBoost * 0.62,
      fill: "rgba(246, 243, 234, 0.92)",
      edge: "rgba(186, 181, 170, 0.13)",
      highlight: "rgba(255, 255, 255, 0.12)",
      warmCore: "rgba(255, 250, 240, 0.08)",
      shadow: "rgba(112, 109, 103, 0.04)"
    },
    service: {
      width: 4.2 + zoomBoost * 0.44,
      fill: "rgba(235, 231, 222, 0.82)",
      edge: "rgba(176, 171, 160, 0.07)",
      highlight: "rgba(255, 255, 255, 0.06)",
      warmCore: "rgba(252, 248, 236, 0.05)",
      shadow: "rgba(101, 98, 92, 0.028)"
    },
    path: {
      width: 2.2 + zoomBoost * 0.18,
      fill: "rgba(196, 184, 150, 0.72)",
      edge: "rgba(151, 137, 103, 0.05)",
      highlight: "rgba(245, 233, 199, 0.04)",
      warmCore: "rgba(245, 233, 199, 0.02)",
      shadow: "rgba(96, 83, 60, 0.02)"
    }
  };

  if (normalized === "primary" || normalized === "primary_link") return styles.primary;
  if (normalized === "secondary" || normalized === "secondary_link" || normalized === "tertiary" || normalized === "tertiary_link") return styles.secondary;
  if (normalized === "service" || normalized === "road") return styles.service;
  if (["track", "path", "footway", "cycleway", "pedestrian"].includes(normalized)) return styles.path;
  return styles.residential;
}

function drawRoadShadow(ctx, points, style) {
  ctx.save();
  ctx.strokeStyle = style.shadow;
  ctx.lineWidth = style.width + 1.85;
  ctx.lineCap = "round";
  ctx.lineJoin = "round";
  ctx.shadowColor = style.shadow;
  ctx.shadowBlur = Math.max(1.4, style.width * 0.24);
  ctx.shadowOffsetY = Math.max(0.26, style.width * 0.038);
  drawCustom25DRoadPath(ctx, points, style.dash || null);
  ctx.restore();
}

function drawCustom25DRoad(ctx, points, style) {
  drawRoadShadow(ctx, points, style);

  ctx.save();
  ctx.lineCap = "round";
  ctx.lineJoin = "round";

  ctx.strokeStyle = style.edge;
  ctx.lineWidth = style.width + 0.3;
  drawCustom25DRoadPath(ctx, points, style.dash || null);

  ctx.strokeStyle = style.fill;
  ctx.lineWidth = style.width;
  drawCustom25DRoadPath(ctx, points, style.dash || null);

  if (style.warmCore) {
    ctx.strokeStyle = style.warmCore;
    ctx.lineWidth = Math.max(0.68, style.width * 0.31);
    drawCustom25DRoadPath(ctx, points, null);
  }

  ctx.strokeStyle = style.highlight;
  ctx.lineWidth = Math.max(0.62, style.width * 0.12);
  drawCustom25DRoadPath(ctx, points, null);
  ctx.restore();
}

function drawCustom25DRoadPath(ctx, points, dashPattern) {
  if (!Array.isArray(points) || points.length < 2) return;

  ctx.setLineDash(dashPattern || []);
  ctx.beginPath();
  ctx.moveTo(points[0].x, points[0].y);

  for (let i = 1; i < points.length; i += 1) {
    ctx.lineTo(points[i].x, points[i].y);
  }

  ctx.stroke();
}

function projectCustom25DRoadPoints(coords, topLeft) {
  if (!Array.isArray(coords) || coords.length < 2) return [];

  return coords.map(([lat, lng]) => {
    const point = map.latLngToLayerPoint([lat, lng]);
    return {
      x: point.x - topLeft.x,
      y: point.y - topLeft.y
    };
  });
}

function drawCustom25DRoads(ctx, bounds, topLeft) {
  if (!Array.isArray(custom25DRoadFeatures) || !custom25DRoadFeatures.length) return;

  const zoom = map.getZoom();

  custom25DRoadFeatures.forEach((road) => {
    if (!Array.isArray(road?.coords) || road.coords.length < 2) return;

    const intersectsBounds = road.coords.some(([lat, lng]) => bounds.contains([lat, lng]));
    if (!intersectsBounds) return;

    const points = projectCustom25DRoadPoints(road.coords, topLeft);
    if (points.length < 2) return;

    const style = getRoadStyleForFeature(road.highway, zoom);
    drawCustom25DRoad(ctx, points, style);
  });
}

function drawTreeCluster(ctx, x, y, scale = 1) {
  const blobs = [
    { x: -5, y: 2, r: 4.2 },
    { x: 0, y: -2, r: 5.3 },
    { x: 5, y: 2, r: 4.4 }
  ];

  blobs.forEach((blob, index) => {
    ctx.fillStyle = index === 1 ? "rgba(50, 133, 63, 0.58)" : "rgba(64, 149, 77, 0.52)";
    ctx.beginPath();
    ctx.arc(x + blob.x * scale, y + blob.y * scale, blob.r * scale, 0, Math.PI * 2);
    ctx.fill();
  });

  ctx.fillStyle = "rgba(228, 247, 206, 0.14)";
  ctx.beginPath();
  ctx.arc(x - 1.4 * scale, y - 3.2 * scale, 1.8 * scale, 0, Math.PI * 2);
  ctx.fill();
}

function drawCustom25DTrees(ctx, size, bounds) {
  if (!Array.isArray(custom25DZoneFeatures) || !custom25DZoneFeatures.length) return;

  const zoom = map.getZoom();
  if (!shouldDrawZoneDetailsAtZoom(zoom, "medium")) return;

  custom25DZoneFeatures
    .filter((feature) => feature.zoneType === "park")
    .filter((feature) => Array.isArray(feature.coords) && feature.coords.length >= 3)
    .filter((feature) => feature.coords.some(([lat, lng]) => bounds.contains([lat, lng])))
    .forEach((feature) => {
      const seed = hashFeatureSeed(feature.id);
      const points = projectCustom25DZonePoints(feature.coords, map.latLngToLayerPoint(bounds.getNorthWest()));
      if (points.length < 3) return;

      const clippedBounds = getProjectedBounds(points);
      ctx.save();
      clipToProjectedPolygon(ctx, points);

      const clusterCount = zoom >= 18 ? 5 : 3;
      for (let i = 0; i < clusterCount; i += 1) {
        const rx = (Math.abs(Math.sin(seed + i * 17.3)) % 1);
        const ry = (Math.abs(Math.sin(seed + i * 29.7)) % 1);
        const x = clippedBounds.minX + rx * Math.max(12, clippedBounds.maxX - clippedBounds.minX);
        const y = clippedBounds.minY + ry * Math.max(12, clippedBounds.maxY - clippedBounds.minY);
        const scale = zoom >= 18 ? 0.9 + ((i % 3) * 0.08) : 0.72 + ((i % 2) * 0.05);
        drawTreeCluster(ctx, x, y, scale);
      }

      ctx.restore();
    });
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
// Phase 9 checkpoint: landmark foundation only.
// Prepares safe future special POI rendering helpers without adding live
// landmark data, gameplay, rewards, collection logic, or flag-off behavior changes.
const CUSTOM_25D_LANDMARK_VISUAL_RECIPES = {
  generic: {
    ring: "rgba(201, 160, 74, 0.92)",
    ringShadow: "rgba(108, 82, 38, 0.18)",
    innerTop: "rgba(252, 247, 231, 0.94)",
    innerBottom: "rgba(232, 221, 188, 0.9)",
    glyph: "rgba(134, 98, 38, 0.88)",
    glow: "rgba(255, 232, 170, 0.12)"
  },
  dinosaur: {
    ring: "rgba(206, 166, 82, 0.92)",
    ringShadow: "rgba(108, 82, 38, 0.18)",
    innerTop: "rgba(250, 245, 227, 0.94)",
    innerBottom: "rgba(229, 220, 187, 0.9)",
    glyph: "rgba(120, 88, 44, 0.88)",
    glow: "rgba(241, 220, 152, 0.12)"
  },
  film: {
    ring: "rgba(201, 160, 74, 0.92)",
    ringShadow: "rgba(108, 82, 38, 0.18)",
    innerTop: "rgba(251, 246, 231, 0.94)",
    innerBottom: "rgba(232, 221, 190, 0.9)",
    glyph: "rgba(110, 82, 48, 0.88)",
    glow: "rgba(243, 223, 168, 0.1)"
  },
  music: {
    ring: "rgba(205, 164, 78, 0.92)",
    ringShadow: "rgba(108, 82, 38, 0.18)",
    innerTop: "rgba(252, 246, 232, 0.94)",
    innerBottom: "rgba(231, 220, 189, 0.9)",
    glyph: "rgba(124, 86, 49, 0.88)",
    glow: "rgba(245, 223, 164, 0.1)"
  },
  waterfall: {
    ring: "rgba(194, 156, 74, 0.92)",
    ringShadow: "rgba(100, 78, 42, 0.18)",
    innerTop: "rgba(249, 245, 231, 0.94)",
    innerBottom: "rgba(226, 218, 191, 0.9)",
    glyph: "rgba(90, 108, 133, 0.88)",
    glow: "rgba(188, 225, 250, 0.1)"
  },
  beach: {
    ring: "rgba(198, 157, 76, 0.92)",
    ringShadow: "rgba(104, 80, 40, 0.18)",
    innerTop: "rgba(252, 246, 231, 0.94)",
    innerBottom: "rgba(235, 224, 192, 0.9)",
    glyph: "rgba(162, 122, 66, 0.88)",
    glow: "rgba(244, 223, 161, 0.1)"
  },
  historic: {
    ring: "rgba(204, 163, 82, 0.92)",
    ringShadow: "rgba(109, 84, 41, 0.18)",
    innerTop: "rgba(252, 247, 232, 0.94)",
    innerBottom: "rgba(233, 223, 191, 0.9)",
    glyph: "rgba(127, 95, 56, 0.88)",
    glow: "rgba(243, 222, 164, 0.1)"
  }
};

// PHASE 10 CHECKPOINT: dormant landmark data foundation only; no real POIs loaded.
const CUSTOM_25D_LANDMARK_CATEGORY_DEFINITIONS = {
  dinosaurSites: {
    id: "dinosaurSites",
    label: "Dinosaur Sites",
    rendererCategory: "dinosaur",
    collectionKey: "dinosaurSites",
    rewardType: "landmarkCollection",
    enabledByDefault: false
  },
  filmLocations: {
    id: "filmLocations",
    label: "Film Locations",
    rendererCategory: "film",
    collectionKey: "filmLocations",
    rewardType: "landmarkCollection",
    enabledByDefault: false
  },
  musicLandmarks: {
    id: "musicLandmarks",
    label: "Music Landmarks",
    rendererCategory: "music",
    collectionKey: "musicLandmarks",
    rewardType: "landmarkCollection",
    enabledByDefault: false
  },
  waterfalls: {
    id: "waterfalls",
    label: "Waterfalls",
    rendererCategory: "waterfall",
    collectionKey: "waterfalls",
    rewardType: "landmarkCollection",
    enabledByDefault: false
  },
  beaches: {
    id: "beaches",
    label: "Beaches",
    rendererCategory: "beach",
    collectionKey: "beaches",
    rewardType: "landmarkCollection",
    enabledByDefault: false
  },
  historicPlaces: {
    id: "historicPlaces",
    label: "Historic Places",
    rendererCategory: "historic",
    collectionKey: "historicPlaces",
    rewardType: "landmarkCollection",
    enabledByDefault: false
  }
};

// PHASE 12 CHECKPOINT: dormant landmark category shell only; metadata-only; no rendering, gameplay, rewards, or collections enabled.
const CUSTOM_25D_LANDMARK_CATEGORY_SHELLS = {
  dinosaurSitesAU: {
    id: "dinosaurSitesAU",
    label: "AU Dinosaur Sites",
    description: "Future Australian dinosaur and fossil landmark group.",
    enabled: false,
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false
  },
  filmLocationsAU: {
    id: "filmLocationsAU",
    label: "AU Film Locations",
    description: "Future Australian film-location landmark group.",
    enabled: false,
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false
  },
  musicLandmarks: {
    id: "musicLandmarks",
    label: "Music Landmarks",
    description: "Future music-related landmark group.",
    enabled: false,
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false
  },
  waterfalls: {
    id: "waterfalls",
    label: "Waterfalls",
    description: "Future waterfall landmark group.",
    enabled: false,
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false
  },
  beaches: {
    id: "beaches",
    label: "Beaches",
    description: "Future beach landmark group.",
    enabled: false,
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false
  },
  specialLandmarks: {
    id: "specialLandmarks",
    label: "Special Landmarks",
    description: "Future rare or premium landmark group.",
    enabled: false,
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false
  }
};

// PHASE 14 CHECKPOINT: dormant landmark icon shell only; metadata-only; no rendering, Leaflet icons, or marker layers enabled.
const CUSTOM_25D_LANDMARK_ICON_SHELLS = {
  dinosaurSitesAU: {
    id: "dinosaurSitesAU",
    categoryId: "dinosaurSitesAU",
    label: "AU Dinosaur Site Icon",
    ringStyle: "gold-soft",
    innerStyle: "paper-cream",
    badgeStyle: "fossil-amber",
    enabled: false
  },
  filmLocationsAU: {
    id: "filmLocationsAU",
    categoryId: "filmLocationsAU",
    label: "AU Film Location Icon",
    ringStyle: "gold-soft",
    innerStyle: "paper-cream",
    badgeStyle: "reel-bronze",
    enabled: false
  },
  musicLandmarks: {
    id: "musicLandmarks",
    categoryId: "musicLandmarks",
    label: "Music Landmark Icon",
    ringStyle: "gold-soft",
    innerStyle: "paper-cream",
    badgeStyle: "note-amber",
    enabled: false
  },
  waterfalls: {
    id: "waterfalls",
    categoryId: "waterfalls",
    label: "Waterfall Icon",
    ringStyle: "gold-soft",
    innerStyle: "paper-mist",
    badgeStyle: "water-blue",
    enabled: false
  },
  beaches: {
    id: "beaches",
    categoryId: "beaches",
    label: "Beach Icon",
    ringStyle: "gold-soft",
    innerStyle: "paper-sand",
    badgeStyle: "shore-warm",
    enabled: false
  },
  specialLandmarks: {
    id: "specialLandmarks",
    categoryId: "specialLandmarks",
    label: "Special Landmark Icon",
    ringStyle: "gold-premium",
    innerStyle: "paper-ivory",
    badgeStyle: "star-bright",
    enabled: false
  }
};

const CUSTOM_25D_LANDMARK_SAMPLE_DATA = [
  {
    id: "sample-landmark-alpha",
    name: "Sample Landmark Alpha",
    categoryId: "historicPlaces",
    lat: DEFAULT_CENTER[0] + 0.0018,
    lng: DEFAULT_CENTER[1] + 0.0016
  },
  {
    id: "sample-landmark-beta",
    name: "Sample Landmark Beta",
    categoryId: "musicLandmarks",
    lat: DEFAULT_CENTER[0] - 0.0012,
    lng: DEFAULT_CENTER[1] + 0.0014
  },
  {
    id: "sample-landmark-gamma",
    name: "Sample Landmark Gamma",
    categoryId: "dinosaurSites",
    lat: DEFAULT_CENTER[0] + 0.001,
    lng: DEFAULT_CENTER[1] - 0.0017
  }
];

// PHASE 11 CHECKPOINT: first dormant AU dinosaur site seed data; disabled by default; no gameplay, rewards, or collections.
const CUSTOM_25D_DINOSAUR_SITES_AU_SEED = [
  {
    id: "dino-au-lark-quarry",
    categoryId: "dinosaurSites",
    label: "Dinosaur Stampede National Monument",
    shortLabel: "Dinosaur Stampede",
    region: "Queensland",
    country: "Australia",
    lat: -23.0161,
    lng: 142.4114,
    coordinatePrecision: "approximate",
    rendererType: "dinosaur",
    collectionKey: "dinosaurSitesAu",
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false,
    notes: "Dormant seed data only. Not capturable yet."
  },
  {
    id: "dino-au-age-of-dinosaurs",
    categoryId: "dinosaurSites",
    label: "Australian Age of Dinosaurs Museum",
    shortLabel: "Age of Dinosaurs",
    region: "Queensland",
    country: "Australia",
    lat: -22.324,
    lng: 143.038,
    coordinatePrecision: "approximate",
    rendererType: "dinosaur",
    collectionKey: "dinosaurSitesAu",
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false,
    notes: "Dormant seed data only. Not capturable yet."
  },
  {
    id: "dino-au-dinosaur-dreaming",
    categoryId: "dinosaurSites",
    label: "Dinosaur Dreaming",
    shortLabel: "Dinosaur Dreaming",
    region: "Victoria",
    country: "Australia",
    lat: -38.631,
    lng: 145.729,
    coordinatePrecision: "approximate",
    rendererType: "dinosaur",
    collectionKey: "dinosaurSitesAu",
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false,
    notes: "Dormant seed data only. Not capturable yet."
  },
  {
    id: "dino-au-dinosaur-cove",
    categoryId: "dinosaurSites",
    label: "Dinosaur Cove",
    shortLabel: "Dinosaur Cove",
    region: "Victoria",
    country: "Australia",
    lat: -38.665,
    lng: 143.105,
    coordinatePrecision: "approximate",
    rendererType: "dinosaur",
    collectionKey: "dinosaurSitesAu",
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false,
    notes: "Dormant seed data only. Not capturable yet."
  },
  {
    id: "dino-au-gantheaume-point",
    categoryId: "dinosaurSites",
    label: "Gantheaume Point Dinosaur Footprints",
    shortLabel: "Gantheaume Point",
    region: "Western Australia",
    country: "Australia",
    lat: -17.975,
    lng: 122.19,
    coordinatePrecision: "approximate",
    rendererType: "dinosaur",
    collectionKey: "dinosaurSitesAu",
    gameplayEnabled: false,
    collectionEnabled: false,
    rewardEnabled: false,
    notes: "Dormant seed data only. Not capturable yet."
  }
];

function getLandmarkVisualRecipe(category = "generic") {
  return CUSTOM_25D_LANDMARK_VISUAL_RECIPES[category] || CUSTOM_25D_LANDMARK_VISUAL_RECIPES.generic;
}

function getLandmarkCategoryDefinition(categoryId) {
  return CUSTOM_25D_LANDMARK_CATEGORY_DEFINITIONS[categoryId] || null;
}

function getAllCustom25DLandmarkCategoryShells() {
  return Object.values(CUSTOM_25D_LANDMARK_CATEGORY_SHELLS).map((category) => ({
    ...category
  }));
}

function getEnabledCustom25DLandmarkCategoryShells() {
  return getAllCustom25DLandmarkCategoryShells().filter((category) => category.enabled);
}

function getCustom25DLandmarkCategoryShellById(categoryId) {
  return CUSTOM_25D_LANDMARK_CATEGORY_SHELLS[categoryId] || null;
}

function getAllCustom25DLandmarkIconShells() {
  return Object.values(CUSTOM_25D_LANDMARK_ICON_SHELLS).map((icon) => ({
    ...icon
  }));
}

function getEnabledCustom25DLandmarkIconShells() {
  return getAllCustom25DLandmarkIconShells().filter((icon) => icon.enabled);
}

function getCustom25DLandmarkIconShellByCategoryId(categoryId) {
  return CUSTOM_25D_LANDMARK_ICON_SHELLS[categoryId] || null;
}

function getCustom25DLandmarkSampleData(categoryFilter = null) {
  if (!ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA) return [];

  const samples = CUSTOM_25D_LANDMARK_SAMPLE_DATA.map((entry) => {
    const category = getLandmarkCategoryDefinition(entry.categoryId);
    return {
      ...entry,
      rendererCategory: category?.rendererCategory || "generic",
      source: "sample"
    };
  });

  if (!categoryFilter) return samples;
  return samples.filter((entry) => entry.categoryId === categoryFilter || entry.rendererCategory === categoryFilter);
}

function getCustom25DDinosaurSitesAuSeed(categoryFilter = null) {
  if (!ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA) return [];

  const entries = CUSTOM_25D_DINOSAUR_SITES_AU_SEED.map((entry) => ({
    ...entry,
    rendererCategory: entry.rendererType || "dinosaur",
    source: "realSeed"
  }));

  if (!categoryFilter) return entries;
  return entries.filter((entry) => entry.categoryId === categoryFilter || entry.rendererCategory === categoryFilter);
}

function getActiveCustom25DLandmarkData(options = {}) {
  const {
    categoryFilter = null
  } = options;

  if (!ENABLE_CUSTOM_25D_MAP) return [];
  return [
    ...getCustom25DLandmarkSampleData(categoryFilter),
    ...getCustom25DDinosaurSitesAuSeed(categoryFilter)
  ];
}

// PHASE 13 CHECKPOINT: dormant landmark render gate only; rendering stays blocked until explicit map/data conditions are met.
function shouldRenderCustom25DLandmarks(options = {}) {
  const {
    categoryFilter = null
  } = options;

  if (!ENABLE_CUSTOM_25D_MAP) return false;

  const hasLandmarkDataFlagEnabled = (
    ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS ||
    ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA ||
    ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
  );

  if (!hasLandmarkDataFlagEnabled) return false;

  return getActiveCustom25DLandmarkData({ categoryFilter }).length > 0;
}

function getRenderableCustom25DLandmarks(options = {}) {
  if (!shouldRenderCustom25DLandmarks(options)) return [];
  return getActiveCustom25DLandmarkData(options);
}

// PHASE 15 CHECKPOINT: dormant landmark test layer shell only; no Leaflet layer, markers, or map wiring enabled by default.
const CUSTOM_25D_LANDMARK_TEST_LAYER_STATE = {
  initialized: false,
  enabled: false,
  layer: null,
  markerCount: 0,
  lastUpdatedAt: null
};

function getCustom25DLandmarkTestLayerState() {
  return {
    ...CUSTOM_25D_LANDMARK_TEST_LAYER_STATE
  };
}

function resetCustom25DLandmarkTestLayerState() {
  CUSTOM_25D_LANDMARK_TEST_LAYER_STATE.initialized = false;
  CUSTOM_25D_LANDMARK_TEST_LAYER_STATE.enabled = false;
  CUSTOM_25D_LANDMARK_TEST_LAYER_STATE.layer = null;
  CUSTOM_25D_LANDMARK_TEST_LAYER_STATE.markerCount = 0;
  CUSTOM_25D_LANDMARK_TEST_LAYER_STATE.lastUpdatedAt = null;
  return getCustom25DLandmarkTestLayerState();
}

function canInitializeCustom25DLandmarkTestLayer(options = {}) {
  if (!ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS) return false;
  if (!shouldRenderCustom25DLandmarks(options)) return false;
  return getRenderableCustom25DLandmarks(options).length > 0;
}

// PHASE 16 CHECKPOINT: dormant landmark debug toggle shell only; no UI, rendering, markers, or map layers enabled by default.
const CUSTOM_25D_LANDMARK_DEBUG_STATE = {
  enabled: false,
  showBounds: false,
  showLabels: false,
  showCoordinates: false,
  showCategoryIds: false,
  lastToggledAt: null
};

function getCustom25DLandmarkDebugState() {
  return {
    ...CUSTOM_25D_LANDMARK_DEBUG_STATE
  };
}

function resetCustom25DLandmarkDebugState() {
  CUSTOM_25D_LANDMARK_DEBUG_STATE.enabled = false;
  CUSTOM_25D_LANDMARK_DEBUG_STATE.showBounds = false;
  CUSTOM_25D_LANDMARK_DEBUG_STATE.showLabels = false;
  CUSTOM_25D_LANDMARK_DEBUG_STATE.showCoordinates = false;
  CUSTOM_25D_LANDMARK_DEBUG_STATE.showCategoryIds = false;
  CUSTOM_25D_LANDMARK_DEBUG_STATE.lastToggledAt = null;
  return getCustom25DLandmarkDebugState();
}

function setCustom25DLandmarkDebugState(nextState = {}) {
  const allowedKeys = new Set([
    "enabled",
    "showBounds",
    "showLabels",
    "showCoordinates",
    "showCategoryIds"
  ]);

  Object.entries(nextState).forEach(([key, value]) => {
    if (!allowedKeys.has(key)) return;
    CUSTOM_25D_LANDMARK_DEBUG_STATE[key] = Boolean(value);
  });

  const hasAnyDebugToggleEnabled = (
    CUSTOM_25D_LANDMARK_DEBUG_STATE.enabled ||
    CUSTOM_25D_LANDMARK_DEBUG_STATE.showBounds ||
    CUSTOM_25D_LANDMARK_DEBUG_STATE.showLabels ||
    CUSTOM_25D_LANDMARK_DEBUG_STATE.showCoordinates ||
    CUSTOM_25D_LANDMARK_DEBUG_STATE.showCategoryIds
  );

  CUSTOM_25D_LANDMARK_DEBUG_STATE.lastToggledAt = hasAnyDebugToggleEnabled
    ? Date.now()
    : null;

  return getCustom25DLandmarkDebugState();
}

function isCustom25DLandmarkDebugEnabled() {
  return CUSTOM_25D_LANDMARK_DEBUG_STATE.enabled;
}

// PHASE 17 CHECKPOINT: dormant landmark manual test hook only; no automatic calls, rendering, markers, layers, or UI enabled by default.
const CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE = {
  armed: false,
  lastRunAt: null,
  lastResult: null,
  lastReason: null
};

function getCustom25DLandmarkManualTestHookState() {
  return {
    ...CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE
  };
}

function resetCustom25DLandmarkManualTestHookState() {
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed = false;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastRunAt = null;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastResult = null;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastReason = null;
  return getCustom25DLandmarkManualTestHookState();
}

function isCustom25DLandmarkManualTestHookArmed() {
  return CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed;
}

function armCustom25DLandmarkManualTestHook(options = {}) {
  const allowArm = options?.allowArm !== false;
  if (!allowArm) {
    return {
      ok: false,
      armed: false,
      reason: "Manual landmark hook arming is not allowed."
    };
  }

  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed = true;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastReason = "Manual landmark hook armed.";

  return {
    ok: true,
    armed: true,
    reason: "Manual landmark hook armed."
  };
}

function disarmCustom25DLandmarkManualTestHook(options = {}) {
  const keepHistory = options?.keepHistory === true;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed = false;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastReason = "Manual landmark hook disarmed.";

  if (!keepHistory) {
    CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastResult = null;
    CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastRunAt = null;
  }

  return {
    ok: true,
    armed: false,
    reason: "Manual landmark hook disarmed."
  };
}

function canRunCustom25DLandmarkManualTestHook(options = {}) {
  if (!CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed) return false;
  if (!shouldRenderCustom25DLandmarks(options)) return false;
  if (!canInitializeCustom25DLandmarkTestLayer(options)) return false;
  return getRenderableCustom25DLandmarks(options).length > 0;
}

function normalizeCustom25DLandmarkManualTestHookResult(result = {}, options = {}) {
  const safeResult = result && typeof result === "object" ? result : {};
  const safeReason = typeof safeResult.reason === "string" && safeResult.reason.trim()
    ? safeResult.reason.trim()
    : "Manual landmark test result is unavailable.";
  const timestamp = typeof options?.timestamp === "string" && options.timestamp
    ? options.timestamp
    : new Date().toISOString();

  return {
    ok: safeResult.ok === true,
    executed: safeResult.executed === true,
    reason: safeReason,
    result: Object.prototype.hasOwnProperty.call(safeResult, "result") ? safeResult.result : null,
    timestamp,
    source: "custom-25d-landmark-manual-test-hook"
  };
}

function executeCustom25DLandmarkManualTestHook(options = {}) {
  const renderableLandmarks = getRenderableCustom25DLandmarks(options);
  const canInitializeLayer = canInitializeCustom25DLandmarkTestLayer(options);
  const canRun = canRunCustom25DLandmarkManualTestHook(options);

  let reason = "Manual landmark hook is not armed.";
  if (CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed && !shouldRenderCustom25DLandmarks(options)) {
    reason = "Landmark rendering conditions are not met.";
  } else if (CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed && shouldRenderCustom25DLandmarks(options) && !canInitializeLayer) {
    reason = "Landmark test layer cannot initialize.";
  } else if (CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.armed && canInitializeLayer && renderableLandmarks.length === 0) {
    reason = "No renderable landmarks are available.";
  } else if (canRun) {
    reason = "manual-test-execute-shell";
  }

  const rawResult = {
    ok: canRun,
    executed: canRun,
    reason,
    result: null,
    renderableCount: renderableLandmarks.length,
    canInitializeLayer,
    debugEnabled: isCustom25DLandmarkDebugEnabled()
  };

  const result = {
    ...rawResult,
    ...normalizeCustom25DLandmarkManualTestHookResult(rawResult)
  };

  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastRunAt = Date.now();
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastResult = result;
  CUSTOM_25D_LANDMARK_MANUAL_TEST_HOOK_STATE.lastReason = result.reason;

  return result;
}

function runCustom25DLandmarkManualTestHook(options = {}) {
  return executeCustom25DLandmarkManualTestHook(options);
}

function inspectCustom25DLandmarkManualTestHookState(options = {}) {
  const safeState = getCustom25DLandmarkManualTestHookState() || {};
  const timestamp = typeof options?.timestamp === "string" && options.timestamp
    ? options.timestamp
    : new Date().toISOString();

  return {
    ok: true,
    source: "custom-25d-landmark-manual-test-hook",
    armed: isCustom25DLandmarkManualTestHookArmed() === true,
    canRun: canRunCustom25DLandmarkManualTestHook(options) === true,
    lastRunAt: safeState.lastRunAt ?? null,
    lastResult: safeState.lastResult ?? null,
    lastReason: safeState.lastReason ?? null,
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === true,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === true,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === true,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === true
    },
    timestamp
  };
}

function getCustom25DLandmarkManualTestHookSafetySummary(options = {}) {
  const snapshot = inspectCustom25DLandmarkManualTestHookState(options) || {};
  const flags = snapshot.flags || {};
  const timestamp = typeof options?.timestamp === "string" && options.timestamp
    ? options.timestamp
    : new Date().toISOString();

  return {
    ok: true,
    source: "custom-25d-landmark-manual-test-hook",
    dormantByDefault: (
      flags.custom25DMap !== true &&
      flags.landmarkTestMarkers !== true &&
      flags.landmarkSampleData !== true &&
      flags.dinosaurSitesAuData !== true
    ),
    safeToInspect: true,
    armed: snapshot.armed === true,
    canRun: snapshot.canRun === true,
    flags: {
      custom25DMap: flags.custom25DMap === true,
      landmarkTestMarkers: flags.landmarkTestMarkers === true,
      landmarkSampleData: flags.landmarkSampleData === true,
      dinosaurSitesAuData: flags.dinosaurSitesAuData === true
    },
    createsMarkers: false,
    createsLayers: false,
    createsDomElements: false,
    rendersByDefault: false,
    connectsToLeaflet: false,
    connectsToMapStartup: false,
    changesGameplay: false,
    changesOsmBehavior: false,
    timestamp
  };
}

function getCustom25DLandmarkManualTestHookConsoleGuide(options = {}) {
  const summary = getCustom25DLandmarkManualTestHookSafetySummary(options) || {};
  const timestamp = typeof options?.timestamp === "string" && options.timestamp
    ? options.timestamp
    : new Date().toISOString();

  return {
    ok: true,
    source: "custom-25d-landmark-manual-test-hook",
    dormantByDefault: summary.dormantByDefault === true,
    safeToInspect: summary.safeToInspect === true,
    recommendedOrder: [
      "getCustom25DLandmarkManualTestHookSafetySummary()",
      "inspectCustom25DLandmarkManualTestHookState()",
      "isCustom25DLandmarkManualTestHookArmed()",
      "armCustom25DLandmarkManualTestHook()",
      "canRunCustom25DLandmarkManualTestHook()",
      "executeCustom25DLandmarkManualTestHook()",
      "runCustom25DLandmarkManualTestHook()",
      "disarmCustom25DLandmarkManualTestHook()",
      "resetCustom25DLandmarkManualTestHookState()"
    ],
    helpers: {
      safetySummary: "getCustom25DLandmarkManualTestHookSafetySummary()",
      inspectState: "inspectCustom25DLandmarkManualTestHookState()",
      readState: "getCustom25DLandmarkManualTestHookState()",
      resetState: "resetCustom25DLandmarkManualTestHookState()",
      isArmed: "isCustom25DLandmarkManualTestHookArmed()",
      arm: "armCustom25DLandmarkManualTestHook()",
      disarm: "disarmCustom25DLandmarkManualTestHook()",
      canRun: "canRunCustom25DLandmarkManualTestHook()",
      execute: "executeCustom25DLandmarkManualTestHook()",
      run: "runCustom25DLandmarkManualTestHook()",
      normalizeResult: "normalizeCustom25DLandmarkManualTestHookResult()"
    },
    warning: "Dormant manual helpers only. These do not render or enable landmark markers by default.",
    timestamp
  };
}

function getCustom25DLandmarkManualTestCleanupCheck() {
  return {
    ok: true,
    phase: 24,
    name: "landmark-manual-test-cleanup-check",
    dormant: true,
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === true,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === true,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === true,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === true
    },
    helperChecks: {
      inspectState: typeof inspectCustom25DLandmarkManualTestHookState === "function",
      safetySummary: typeof getCustom25DLandmarkManualTestHookSafetySummary === "function",
      consoleGuide: typeof getCustom25DLandmarkManualTestHookConsoleGuide === "function",
      isArmed: typeof isCustom25DLandmarkManualTestHookArmed === "function",
      canRun: typeof canRunCustom25DLandmarkManualTestHook === "function",
      execute: typeof executeCustom25DLandmarkManualTestHook === "function",
      run: typeof runCustom25DLandmarkManualTestHook === "function"
    },
    safetyNotes: [
      "Does not render markers.",
      "Does not create layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not call gameplay or capture systems."
    ]
  };
}

function getCustom25DLandmarkManualTestValidationSummary() {
  return {
    ok: true,
    phase: 25,
    name: "landmark-manual-test-validation-summary",
    dormant: true,
    validation: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    helpers: {
      safetySummary: typeof getCustom25DLandmarkManualTestHookSafetySummary === "function",
      consoleGuide: typeof getCustom25DLandmarkManualTestHookConsoleGuide === "function",
      cleanupCheck: typeof getCustom25DLandmarkManualTestCleanupCheck === "function",
      inspectState: typeof inspectCustom25DLandmarkManualTestHookState === "function",
      isArmed: typeof isCustom25DLandmarkManualTestHookArmed === "function",
      canRun: typeof canRunCustom25DLandmarkManualTestHook === "function"
    },
    previousPhase: {
      cleanupCheckExists: typeof getCustom25DLandmarkManualTestCleanupCheck === "function"
    },
    safetyNotes: [
      "Inspection-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkManualTestReadinessReport() {
  return {
    ok: true,
    phase: 26,
    name: "landmark-manual-test-readiness-report",
    dormant: true,
    flagsReady: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    helpersReady: {
      safetySummary: typeof getCustom25DLandmarkManualTestHookSafetySummary === "function",
      consoleGuide: typeof getCustom25DLandmarkManualTestHookConsoleGuide === "function",
      cleanupCheck: typeof getCustom25DLandmarkManualTestCleanupCheck === "function",
      validationSummary: typeof getCustom25DLandmarkManualTestValidationSummary === "function",
      inspectState: typeof inspectCustom25DLandmarkManualTestHookState === "function",
      canRun: typeof canRunCustom25DLandmarkManualTestHook === "function"
    },
    previousPhase: {
      validationSummaryExists: typeof getCustom25DLandmarkManualTestValidationSummary === "function"
    },
    readyForManualInspection: true,
    notReadyForVisibleRendering: true,
    safetyNotes: [
      "Inspection-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkManualTestFinalAudit() {
  return {
    ok: true,
    phase: 27,
    name: "landmark-manual-test-final-audit",
    dormant: true,
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    helpers: {
      safetySummary: typeof getCustom25DLandmarkManualTestHookSafetySummary === "function",
      consoleGuide: typeof getCustom25DLandmarkManualTestHookConsoleGuide === "function",
      cleanupCheck: typeof getCustom25DLandmarkManualTestCleanupCheck === "function",
      validationSummary: typeof getCustom25DLandmarkManualTestValidationSummary === "function",
      readinessReport: typeof getCustom25DLandmarkManualTestReadinessReport === "function",
      inspectState: typeof inspectCustom25DLandmarkManualTestHookState === "function"
    },
    previousPhase: {
      readinessReportExists: typeof getCustom25DLandmarkManualTestReadinessReport === "function"
    },
    auditStatus: "inspection-only",
    visibleOutputCreated: false,
    stateMutated: false,
    flagsChanged: false,
    safeToMerge: true,
    safetyNotes: [
      "Inspection-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkManualTestHandoffSummary() {
  return {
    ok: true,
    phase: 28,
    name: "landmark-manual-test-handoff-summary",
    dormant: true,
    branchPurpose: "Document dormant manual landmark test helpers for the next safe phase.",
    completedInspectionHelpers: [
      "getCustom25DLandmarkManualTestHookSafetySummary",
      "getCustom25DLandmarkManualTestHookConsoleGuide",
      "getCustom25DLandmarkManualTestCleanupCheck",
      "getCustom25DLandmarkManualTestValidationSummary",
      "getCustom25DLandmarkManualTestReadinessReport",
      "getCustom25DLandmarkManualTestFinalAudit"
    ],
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    previousPhase: {
      finalAuditExists: typeof getCustom25DLandmarkManualTestFinalAudit === "function"
    },
    handoffStatus: "ready-for-next-dormant-phase",
    visibleOutputCreated: false,
    stateMutated: false,
    safetyNotes: [
      "Inspection-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkManualTestArchiveIndex() {
  return {
    ok: true,
    phase: 29,
    name: "landmark-manual-test-archive-index",
    dormant: true,
    archivePurpose: "Index dormant manual landmark test helpers for future inspection phases.",
    indexedHelpers: [
      "getCustom25DLandmarkManualTestHookSafetySummary",
      "getCustom25DLandmarkManualTestHookConsoleGuide",
      "getCustom25DLandmarkManualTestCleanupCheck",
      "getCustom25DLandmarkManualTestValidationSummary",
      "getCustom25DLandmarkManualTestReadinessReport",
      "getCustom25DLandmarkManualTestFinalAudit",
      "getCustom25DLandmarkManualTestHandoffSummary"
    ],
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    previousPhase: {
      handoffSummaryExists: typeof getCustom25DLandmarkManualTestHandoffSummary === "function"
    },
    archiveStatus: "inspection-only-index",
    visibleOutputCreated: false,
    stateMutated: false,
    safetyNotes: [
      "Inspection-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkManualTestCloseoutMarker() {
  return {
    ok: true,
    phase: 30,
    name: "landmark-manual-test-closeout-marker",
    dormant: true,
    closeoutPurpose: "Mark the dormant landmark manual test foundation as ready for next planning.",
    completedRange: "phases-11-through-30-dormant-landmark-manual-test-foundation",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    previousPhase: {
      archiveIndexExists: typeof getCustom25DLandmarkManualTestArchiveIndex === "function"
    },
    closeoutStatus: "inspection-only-closeout-marker",
    visibleOutputCreated: false,
    stateMutated: false,
    flagsChanged: false,
    readyForNextPlanningStep: true,
    safetyNotes: [
      "Inspection-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkNextPhasePlan() {
  return {
    ok: true,
    phase: 31,
    name: "custom-25d-landmark-next-phase-plan",
    dormant: true,
    purpose: "Planning marker for the next dormant custom 2.5D landmark phase.",
    previousFoundation: {
      closeoutMarkerExists: typeof getCustom25DLandmarkManualTestCloseoutMarker === "function"
    },
    recommendedNextSteps: [
      "review dormant helper chain",
      "decide whether next work should stay inspection-only",
      "avoid visible rendering until explicitly approved",
      "keep all safety flags false by default"
    ],
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    visibleOutputCreated: false,
    stateMutated: false,
    flagsChanged: false,
    readyForVisibleRendering: false,
    safetyNotes: [
      "Planning-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DVisualMapRendererTrackRoadmap() {
  return {
    ok: true,
    phase: 66,
    name: "Custom 2.5D visual map renderer track roadmap",
    dormant: true,
    purpose:
      "Document the passive Track A roadmap for future visual 2.5D map renderer polish without changing the renderer in this phase.",
    targetVisualStyle: [
      "playful 2.5D / isometric-feeling map",
      "toy-town / paper-cut / polished mobile look",
      "soft water and beaches",
      "cartoony roads and paths",
      "green parks and grass zones",
      "simple buildings with depth and shadows",
      "readable labels",
      "normal blue pins still clearly visible",
      "player marker still clearly visible",
      "capture radius still preserved"
    ],
    trackScope: [
      "visual 2.5D map renderer polish only",
      "mobile-friendly presentation improvements only",
      "no gameplay changes",
      "no POI data loading changes"
    ],
    plannedLayers: [
      "renderer safety boundary",
      "visual background palette",
      "roads and paths polish",
      "water and beach polish",
      "parks and grass polish",
      "simple building depth pass",
      "label readability pass",
      "shadow and depth pass",
      "mobile performance pass",
      "final comparison against the Cowes Jetty target screenshot"
    ],
    plannedPhases: [
      "roadmap checkpoint",
      "safe renderer boundary review",
      "layer-by-layer visual polish passes",
      "label pass",
      "depth/shadow pass",
      "mobile performance review",
      "final visual comparison and tuning"
    ],
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      landmarkMarkerManualTestPath: true,
      sampleData: true,
      dinosaurSiteData: true,
      filmLocationData: true,
      realPoiData: true
    },
    doNotChangeYet: [
      "do not enable ENABLE_CUSTOM_25D_MAP",
      "do not change map appearance in this phase",
      "do not add new visual layers in this phase",
      "do not add real POI data",
      "do not add dinosaur site data",
      "do not add film location data",
      "do not add UI toggles",
      "do not change gameplay",
      "do not change backend",
      "do not change rewards",
      "do not change collections",
      "do not change normal blue pins",
      "do not change player marker",
      "do not change capture radius",
      "do not change OSM behavior",
      "do not auto-pan",
      "do not auto-fly",
      "do not auto-zoom"
    ],
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    performanceGoals: [
      "preserve mobile-safe rendering costs",
      "avoid unnecessary layer churn",
      "keep labels and shadows lightweight",
      "protect scroll, pan, and zoom responsiveness"
    ],
    mobileGoals: [
      "maintain readability on small screens",
      "keep pins and player marker visually dominant",
      "avoid clutter around capture interactions",
      "support polished touch-friendly map presentation"
    ],
    notes: [
      "Roadmap-only helper.",
      "Does not change renderer output in this phase.",
      "Does not enable flags.",
      "Does not load data.",
      "Does not create markers, layers, UI, or event listeners."
    ]
  };
}

function getCustom25DVisualMapRendererSafetyBoundary() {
  return {
    ok: true,
    phase: 67,
    name: "Custom 2.5D visual map renderer safety boundary",
    dormant: true,
    purpose:
      "Document the passive safety boundary for future Track A visual 2.5D renderer work without changing renderer output in this phase.",
    rendererBoundary: "Visual renderer polish only. No gameplay, data, or interaction behavior changes are allowed inside this boundary.",
    allowedFutureVisualAreas: [
      "visual background palette",
      "roads and paths visual polish",
      "water and beach visual polish",
      "parks and grass visual polish",
      "simple building depth and shadow visuals",
      "label readability visuals",
      "custom 2.5D canvas/layer styling",
      "mobile performance tuning for visual layers only"
    ],
    blockedAreas: [
      "no gameplay changes",
      "no backend changes",
      "no reward changes",
      "no collection changes",
      "no normal blue pin behavior changes",
      "no player marker behavior changes",
      "no capture radius behavior changes",
      "no real POI data",
      "no dinosaur site data",
      "no film location data",
      "no sample data loading",
      "no visible debug UI",
      "no UI toggles yet",
      "no automatic map movement"
    ],
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      completedManualLandmarkVisibleMarkerTestPath: true,
      sampleData: true,
      dinosaurSiteData: true,
      filmLocationData: true,
      realPoiData: true
    },
    safeExperimentRules: [
      "keep all visual renderer changes behind ENABLE_CUSTOM_25D_MAP",
      "keep ENABLE_CUSTOM_25D_MAP false by default",
      "default app load must remain visually unchanged until explicitly enabled in a later controlled test",
      "visual phases should be small and reversible",
      "each visual phase should affect one layer or one rendering concern only",
      "no new UI switches until explicitly planned later",
      "no real data loading while working on visual renderer foundation"
    ],
    flagRules: [
      "ENABLE_CUSTOM_25D_MAP remains false by default",
      "landmark test/data flags remain false by default",
      "no renderer roadmap/helper phase may enable flags automatically"
    ],
    dataRules: [
      "do not add real POI data",
      "do not add dinosaur site data",
      "do not add film location data",
      "do not load sample data",
      "do not couple visual-track work to data-track work"
    ],
    mapMovementRules: [
      "no auto-pan",
      "no auto-fly",
      "no auto-zoom",
      "no forced map recentering",
      "no camera behavior changes"
    ],
    performanceRules: [
      "preserve smooth map interaction",
      "avoid heavy DOM creation",
      "avoid excessive per-frame work",
      "keep renderer work lightweight",
      "preserve battery-conscious behavior",
      "do not add expensive polling loops",
      "do not add always-running animation loops unless explicitly planned and guarded later"
    ],
    mobileRules: [
      "maintain readability on smaller screens",
      "keep interactive map behavior responsive",
      "avoid overcrowding around pins and player marker",
      "preserve mobile-friendly layering and contrast"
    ],
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    notes: [
      "Safety-boundary helper only.",
      "Does not change map appearance in this phase.",
      "Does not create layers, UI, markers, or event listeners.",
      "Does not load data.",
      "Does not alter the completed manual landmark visible marker test path."
    ]
  };
}

function getCustom25DVisualBackgroundPaletteConfig() {
  return {
    ok: true,
    phase: 68,
    name: "Custom 2.5D visual background palette config",
    dormant: true,
    purpose:
      "Define passive future color and style intentions for Track A custom 2.5D background visuals without applying them in this phase.",
    styleGoal: [
      "soft playful 2.5D background tone",
      "toy-town / paper-cut mobile feel",
      "calm layered map surface under gameplay pins",
      "readable but gentle contrast"
    ],
    palette: {
      water: {
        base: "#7cb7de",
        highlight: "#b9e0f5",
        shorelineContrast: "#5f9bc3"
      },
      beach: {
        base: "#e6d0a4",
        edgeHighlight: "#f3e4c3"
      },
      grass: {
        base: "#8fbe73",
        openAreaHighlight: "#b6d995"
      },
      parks: {
        base: "#79ac63",
        softContrast: "#96c97d"
      },
      roads: {
        base: "#d8c7a9",
        softEdgeShadow: "#b7a88f"
      },
      paths: {
        base: "#cdbb9a"
      },
      buildings: {
        base: "#e7dccb",
        sideShadow: "#c8bba6"
      },
      labels: {
        text: "#2f3b2d",
        halo: "#f6f1e6"
      },
      depth: {
        softShadow: "rgba(76, 63, 46, 0.16)",
        subtleLift: "rgba(255, 255, 255, 0.12)"
      }
    },
    layerIntent: [
      "water and beach tones sit low in the stack",
      "grass and parks provide broad soft color fields",
      "roads and paths stay readable without overpowering pins",
      "buildings use warm neutral tones with simple depth separation",
      "labels remain legible with a light halo concept",
      "depth tones stay soft and mobile-friendly"
    ],
    usageRules: [
      "palette is dormant",
      "palette is not applied automatically",
      "palette does not draw anything",
      "palette does not change Leaflet/OSM appearance by default",
      "future renderer phases may reference it only while guarded by ENABLE_CUSTOM_25D_MAP",
      "ENABLE_CUSTOM_25D_MAP must remain false by default"
    ],
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      landmarkMarkerManualTestPath: true,
      sampleData: true,
      dinosaurSiteData: true,
      filmLocationData: true,
      realPoiData: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    notes: [
      "Palette/config helper only.",
      "No active rendering in this phase.",
      "No map appearance change by default.",
      "No layers, UI, markers, or event listeners created.",
      "Future use remains gated behind ENABLE_CUSTOM_25D_MAP."
    ]
  };
}

function getCustom25DVisualBackgroundPaletteReadiness() {
  return {
    ok: true,
    phase: 68,
    dormant: true,
    hasPaletteConfig: true,
    readyForFutureLayerPrep: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualLayerStructureConfig() {
  return {
    ok: true,
    phase: 69,
    name: "Custom 2.5D visual layer structure config",
    dormant: true,
    purpose:
      "Define a passive future layer structure for Track A custom 2.5D renderer work without drawing anything in this phase.",
    styleGoal: [
      "layered playful 2.5D map composition",
      "clear visual stacking under gameplay markers",
      "mobile-friendly depth and readability"
    ],
    layerOrder: [
      "baseBackground",
      "water",
      "beach",
      "parksAndGrass",
      "roadsAndPaths",
      "buildings",
      "labels",
      "depthAndShadows",
      "gameMarkers"
    ],
    layerResponsibilities: {
      baseBackground: "Future soft land and grass base; no active drawing yet.",
      water: "Future water shapes and soft highlights; no active drawing yet.",
      beach: "Future beach and shoreline shapes; no active drawing yet.",
      parksAndGrass: "Future park and open-space polish; no active drawing yet.",
      roadsAndPaths: "Future cartoony roads, paths, and trails; no active drawing yet.",
      buildings: "Future simple toy-like buildings and depth; no active drawing yet.",
      labels: "Future readable labels and halo behavior; no active drawing yet.",
      depthAndShadows: "Future subtle shadow and depth pass; no active drawing yet.",
      gameMarkers: "Normal blue pins, player marker, and capture radius remain protected; no behavior changes in this phase."
    },
    renderingRules: [
      "higher visual layers should build on lower layers without replacing protected gameplay markers",
      "gameMarkers must remain above future visual background layers",
      "future label and depth passes should preserve pin readability",
      "no active drawing is introduced by this config"
    ],
    usageRules: [
      "layer structure is dormant",
      "layer structure is not applied automatically",
      "layer structure does not draw anything",
      "layer structure does not change Leaflet/OSM appearance by default",
      "future renderer phases may reference it only while guarded by ENABLE_CUSTOM_25D_MAP",
      "ENABLE_CUSTOM_25D_MAP must remain false by default",
      "no visible layer should be created on default app load in this phase"
    ],
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      landmarkMarkerManualTestPath: true,
      sampleData: true,
      dinosaurSiteData: true,
      filmLocationData: true,
      realPoiData: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    notes: [
      "Layer-structure helper only.",
      "No active rendering in this phase.",
      "No map appearance change by default.",
      "No visible layers created on load.",
      "Future use remains gated behind ENABLE_CUSTOM_25D_MAP."
    ]
  };
}

function getCustom25DVisualLayerStructureReadiness() {
  return {
    ok: true,
    phase: 69,
    dormant: true,
    hasLayerStructureConfig: true,
    hasPaletteConfig: true,
    readyForFutureRendererPrep: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRendererContainerPrep() {
  return {
    ok: true,
    phase: 70,
    name: "Custom 2.5D visual renderer container prep",
    dormant: true,
    purpose:
      "Define passive future renderer container preparation rules for Track A without creating or showing any renderer container in this phase.",
    rendererContainerGoal:
      "Prepare a future parent container/canvas/layer host for custom 2.5D visual rendering while keeping the current app visually unchanged by default.",
    expectedContainerRole: [
      "future parent container or canvas host for custom 2.5D visual rendering",
      "future place where background, water, beach, parks, roads, buildings, labels, and depth layers can be organized",
      "future renderer should sit behind game markers",
      "normal blue pins, player marker, and capture radius remain protected",
      "Leaflet/OSM behavior remains protected unless explicitly changed in a later guarded phase"
    ],
    futureSetupRules: [
      "renderer container must only be created or used when guarded by ENABLE_CUSTOM_25D_MAP",
      "ENABLE_CUSTOM_25D_MAP must remain false by default",
      "default app load must remain visually unchanged",
      "no container should be visible on default app load in this phase",
      "no drawing should occur in this phase",
      "no UI switch should be added in this phase",
      "no event listeners should be added in this phase",
      "no real POI, dinosaur, film, or sample data should load in this phase"
    ],
    guardRules: [
      "container work must remain behind ENABLE_CUSTOM_25D_MAP",
      "default-off behavior is mandatory",
      "future container implementation must be small and reversible"
    ],
    layerRelationship: [
      "should reference the Phase 69 layer structure conceptually",
      "should not apply the layer structure yet",
      "should not create active visual layers yet"
    ],
    paletteRelationship: [
      "should reference the Phase 68 palette conceptually",
      "should not apply the palette yet",
      "should not change map colors yet"
    ],
    defaultBehavior: {
      createsVisibleContainerOnLoad: false,
      drawsOnLoad: false,
      changesMapAppearanceOnLoad: false,
      addsUiOnLoad: false,
      addsEventListenersOnLoad: false
    },
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      landmarkMarkerManualTestPath: true,
      sampleData: true,
      dinosaurSiteData: true,
      filmLocationData: true,
      realPoiData: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    notes: [
      "Renderer-container prep helper only.",
      "No visible container is created in this phase.",
      "No active rendering or canvas drawing is added.",
      "No UI, markers, layers, or event listeners are created.",
      "Future use remains gated behind ENABLE_CUSTOM_25D_MAP."
    ]
  };
}

function getCustom25DVisualRendererContainerReadiness() {
  return {
    ok: true,
    phase: 70,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    readyForGuardedContainerImplementation: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRendererShellState() {
  return {
    ok: true,
    phase: 71,
    name: "Custom 2.5D visual renderer shell",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    containerCreated: false,
    layersCreated: false,
    isRendering: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualRendererShell(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowVisualRendererShell !== true) {
    blockedReasons.push("options.allowVisualRendererShell must be true");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 71,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualRendererShell(options = {}) {
  const guard = canInitializeCustom25DVisualRendererShell(options);
  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 71,
      dormant: true,
      defaultVisualChange: false,
      shellState: getCustom25DVisualRendererShellState()
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 71,
    dormant: true,
    defaultVisualChange: false,
    reason: "Renderer shell remains guarded and inactive in this phase.",
    shellState: {
      ...getCustom25DVisualRendererShellState(),
      lastReason: "guarded-inactive-shell"
    }
  };
}

function getCustom25DVisualRendererShellReadiness() {
  return {
    ok: true,
    phase: 71,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    readyForFutureGuardedContainerCreation: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRendererContainerState() {
  return {
    ok: true,
    phase: 72,
    name: "Custom 2.5D visual renderer invisible container",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    containerCreated: false,
    containerVisible: false,
    layersCreated: false,
    isRendering: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canCreateCustom25DVisualRendererContainer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowVisualRendererContainer !== true) {
    blockedReasons.push("options.allowVisualRendererContainer must be true");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 72,
    dormant: true,
    defaultVisualChange: false
  };
}

function createCustom25DVisualRendererContainer(options = {}) {
  const guard = canCreateCustom25DVisualRendererContainer(options);
  if (!guard.allowed) {
    return {
      ok: false,
      created: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 72,
      dormant: true,
      defaultVisualChange: false,
      containerState: getCustom25DVisualRendererContainerState()
    };
  }

  return {
    ok: true,
    created: false,
    blocked: false,
    phase: 72,
    dormant: true,
    defaultVisualChange: false,
    reason: "Invisible renderer container remains guarded and inert in this phase.",
    containerState: {
      ...getCustom25DVisualRendererContainerState(),
      lastReason: "guarded-invisible-container"
    }
  };
}

function clearCustom25DVisualRendererContainer() {
  return {
    ok: true,
    cleared: false,
    phase: 72,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual renderer container exists to clear in this phase."
  };
}

function getCustom25DVisualRendererContainerImplementationReadiness() {
  return {
    ok: true,
    phase: 72,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    readyForFutureInvisibleLayerHost: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualLayerHostState() {
  return {
    ok: true,
    phase: 73,
    name: "Custom 2.5D visual renderer invisible layer host",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRendererContainer: true,
    hostCreated: false,
    hostVisible: false,
    hostAttached: false,
    layersRegistered: 0,
    isRendering: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canCreateCustom25DVisualLayerHost(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowVisualLayerHost !== true) {
    blockedReasons.push("options.allowVisualLayerHost must be true");
  }
  if (options.hasRendererContainer !== true) {
    blockedReasons.push("A valid guarded visual renderer container is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 73,
    dormant: true,
    defaultVisualChange: false
  };
}

function createCustom25DVisualLayerHost(options = {}) {
  const guard = canCreateCustom25DVisualLayerHost(options);
  if (!guard.allowed) {
    return {
      ok: false,
      created: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 73,
      dormant: true,
      defaultVisualChange: false,
      hostState: getCustom25DVisualLayerHostState()
    };
  }

  return {
    ok: true,
    created: false,
    blocked: false,
    phase: 73,
    dormant: true,
    defaultVisualChange: false,
    reason: "Invisible visual layer host remains guarded and inert in this phase.",
    hostState: {
      ...getCustom25DVisualLayerHostState(),
      lastReason: "guarded-invisible-layer-host"
    }
  };
}

function clearCustom25DVisualLayerHost() {
  return {
    ok: true,
    cleared: false,
    phase: 73,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual layer host exists to clear in this phase."
  };
}

function getCustom25DVisualLayerHostImplementationReadiness() {
  return {
    ok: true,
    phase: 73,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    readyForFutureInvisibleVisualLayerRegistration: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRenderLayerRegistryState() {
  return {
    ok: true,
    phase: 74,
    name: "Custom 2.5D visual renderer render-layer registry",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresLayerHost: true,
    registryInitialized: false,
    registryVisible: false,
    layersDefined: 13,
    layersActive: 0,
    isRendering: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false,
    layerKeys: [
      "background",
      "water",
      "beach",
      "grass",
      "park",
      "roadShadow",
      "roadBase",
      "roadHighlight",
      "buildingShadow",
      "buildingBase",
      "label",
      "atmosphere",
      "overlay"
    ]
  };
}

function canInitializeCustom25DVisualRenderLayerRegistry(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowRenderLayerRegistry !== true) {
    blockedReasons.push("options.allowRenderLayerRegistry must be true");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 74,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualRenderLayerRegistry(options = {}) {
  const guard = canInitializeCustom25DVisualRenderLayerRegistry(options);
  const layerRegistry = [
    {
      key: "background",
      name: "Background",
      order: 0,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future calm base tone for the visual renderer."
    },
    {
      key: "water",
      name: "Water",
      order: 1,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future soft water shapes and calm shoreline tone."
    },
    {
      key: "beach",
      name: "Beach",
      order: 2,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future beach band layer with warm sand treatment."
    },
    {
      key: "grass",
      name: "Grass",
      order: 3,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future general grass/open-land layer."
    },
    {
      key: "park",
      name: "Park",
      order: 4,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future park zones and soft green grouping."
    },
    {
      key: "roadShadow",
      name: "Road Shadow",
      order: 5,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future subtle shadow under road shapes."
    },
    {
      key: "roadBase",
      name: "Road Base",
      order: 6,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future main road body layer."
    },
    {
      key: "roadHighlight",
      name: "Road Highlight",
      order: 7,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future road highlight and bevel accents."
    },
    {
      key: "buildingShadow",
      name: "Building Shadow",
      order: 8,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future soft building shadow layer."
    },
    {
      key: "buildingBase",
      name: "Building Base",
      order: 9,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future simple building planes and depth blocks."
    },
    {
      key: "label",
      name: "Label",
      order: 10,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future readable map labels."
    },
    {
      key: "atmosphere",
      name: "Atmosphere",
      order: 11,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future subtle depth and atmosphere accents."
    },
    {
      key: "overlay",
      name: "Overlay",
      order: 12,
      enabledByDefault: false,
      visible: false,
      implemented: false,
      description: "Future non-gameplay visual overlay pass."
    }
  ];

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 74,
      dormant: true,
      defaultVisualChange: false,
      registryState: getCustom25DVisualRenderLayerRegistryState(),
      layerRegistry
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 74,
    dormant: true,
    defaultVisualChange: false,
    reason: "Render-layer registry remains guarded, data-only, and inert in this phase.",
    registryState: {
      ...getCustom25DVisualRenderLayerRegistryState(),
      lastReason: "guarded-invisible-render-layer-registry"
    },
    layerRegistry
  };
}

function clearCustom25DVisualRenderLayerRegistry() {
  return {
    ok: true,
    cleared: false,
    phase: 74,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual render-layer registry exists to clear in this phase."
  };
}

function getCustom25DVisualRenderLayerRegistryImplementationReadiness() {
  return {
    ok: true,
    phase: 74,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    dataOnlyRegistry: true,
    readyForFutureInvisibleLayerActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualBackgroundLayerState() {
  return {
    ok: true,
    phase: 75,
    name: "Custom 2.5D visual renderer background layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualBackgroundLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowBackgroundLayer !== true) {
    blockedReasons.push("options.allowBackgroundLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 75,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualBackgroundLayer(options = {}) {
  const guard = canInitializeCustom25DVisualBackgroundLayer(options);
  const backgroundLayerConfig = {
    key: "background",
    name: "Background",
    order: 0,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    paletteSource: "getCustom25DVisualBackgroundPaletteConfig",
    intendedVisualRole: "soft base world color foundation for future 2.5D map polish",
    drawsContent: false,
    affectsMap: false
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 75,
      dormant: true,
      defaultVisualChange: false,
      backgroundLayerState: getCustom25DVisualBackgroundLayerState(),
      backgroundLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 75,
    dormant: true,
    defaultVisualChange: false,
    reason: "Background layer remains guarded, data-only, and inert in this phase.",
    backgroundLayerState: {
      ...getCustom25DVisualBackgroundLayerState(),
      lastReason: "guarded-invisible-background-layer"
    },
    backgroundLayerConfig
  };
}

function clearCustom25DVisualBackgroundLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 75,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual background layer exists to clear in this phase."
  };
}

function getCustom25DVisualBackgroundLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 75,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureBackgroundVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualWaterLayerState() {
  return {
    ok: true,
    phase: 76,
    name: "Custom 2.5D visual renderer water layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualWaterLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowWaterLayer !== true) {
    blockedReasons.push("options.allowWaterLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 76,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualWaterLayer(options = {}) {
  const guard = canInitializeCustom25DVisualWaterLayer(options);
  const waterLayerConfig = {
    key: "water",
    name: "Water",
    order: 1,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft playful water foundation for future 2.5D map polish",
    targetStyle: "soft blue, rounded, mobile-friendly, toy-like, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealWaterGeometry: false
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 76,
      dormant: true,
      defaultVisualChange: false,
      waterLayerState: getCustom25DVisualWaterLayerState(),
      waterLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 76,
    dormant: true,
    defaultVisualChange: false,
    reason: "Water layer remains guarded, data-only, and inert in this phase.",
    waterLayerState: {
      ...getCustom25DVisualWaterLayerState(),
      lastReason: "guarded-invisible-water-layer"
    },
    waterLayerConfig
  };
}

function clearCustom25DVisualWaterLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 76,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual water layer exists to clear in this phase."
  };
}

function getCustom25DVisualWaterLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 76,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureWaterVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualBeachLayerState() {
  return {
    ok: true,
    phase: 77,
    name: "Custom 2.5D visual renderer beach layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualBeachLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowBeachLayer !== true) {
    blockedReasons.push("options.allowBeachLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 77,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualBeachLayer(options = {}) {
  const guard = canInitializeCustom25DVisualBeachLayer(options);
  const beachLayerConfig = {
    key: "beach",
    name: "Beach",
    order: 2,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft sand and shoreline foundation for future 2.5D map polish",
    targetStyle: "warm sand tones, rounded shoreline shapes, mobile-friendly, toy-like, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealBeachGeometry: false,
    dependsOnWaterLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 77,
      dormant: true,
      defaultVisualChange: false,
      beachLayerState: getCustom25DVisualBeachLayerState(),
      beachLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 77,
    dormant: true,
    defaultVisualChange: false,
    reason: "Beach layer remains guarded, data-only, and inert in this phase.",
    beachLayerState: {
      ...getCustom25DVisualBeachLayerState(),
      lastReason: "guarded-invisible-beach-layer"
    },
    beachLayerConfig
  };
}

function clearCustom25DVisualBeachLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 77,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual beach layer exists to clear in this phase."
  };
}

function getCustom25DVisualBeachLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 77,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureBeachVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualGrassLayerState() {
  return {
    ok: true,
    phase: 78,
    name: "Custom 2.5D visual renderer grass layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualGrassLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowGrassLayer !== true) {
    blockedReasons.push("options.allowGrassLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 78,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualGrassLayer(options = {}) {
  const guard = canInitializeCustom25DVisualGrassLayer(options);
  const grassLayerConfig = {
    key: "grass",
    name: "Grass",
    order: 3,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft base land and grass foundation for future 2.5D map polish",
    targetStyle: "playful green land base, soft rounded shapes, mobile-friendly, toy-like, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealLandGeometry: false,
    dependsOnBackgroundLayer: true,
    sitsBelowParkLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 78,
      dormant: true,
      defaultVisualChange: false,
      grassLayerState: getCustom25DVisualGrassLayerState(),
      grassLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 78,
    dormant: true,
    defaultVisualChange: false,
    reason: "Grass layer remains guarded, data-only, and inert in this phase.",
    grassLayerState: {
      ...getCustom25DVisualGrassLayerState(),
      lastReason: "guarded-invisible-grass-layer"
    },
    grassLayerConfig
  };
}

function clearCustom25DVisualGrassLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 78,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual grass layer exists to clear in this phase."
  };
}

function getCustom25DVisualGrassLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 78,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureGrassVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualParkLayerState() {
  return {
    ok: true,
    phase: 79,
    name: "Custom 2.5D visual renderer park layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualParkLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowParkLayer !== true) {
    blockedReasons.push("options.allowParkLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 79,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualParkLayer(options = {}) {
  const guard = canInitializeCustom25DVisualParkLayer(options);
  const parkLayerConfig = {
    key: "park",
    name: "Park",
    order: 4,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft park and green-space accent layer for future 2.5D map polish",
    targetStyle: "slightly richer greens, rounded park shapes, gentle depth, mobile-friendly, toy-like, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealParkGeometry: false,
    dependsOnGrassLayer: true,
    sitsAboveGrassLayer: true,
    sitsBelowRoadLayers: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 79,
      dormant: true,
      defaultVisualChange: false,
      parkLayerState: getCustom25DVisualParkLayerState(),
      parkLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 79,
    dormant: true,
    defaultVisualChange: false,
    reason: "Park layer remains guarded, data-only, and inert in this phase.",
    parkLayerState: {
      ...getCustom25DVisualParkLayerState(),
      lastReason: "guarded-invisible-park-layer"
    },
    parkLayerConfig
  };
}

function clearCustom25DVisualParkLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 79,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual park layer exists to clear in this phase."
  };
}

function getCustom25DVisualParkLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 79,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureParkVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRoadShadowLayerState() {
  return {
    ok: true,
    phase: 80,
    name: "Custom 2.5D visual renderer road shadow layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualRoadShadowLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowRoadShadowLayer !== true) {
    blockedReasons.push("options.allowRoadShadowLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 80,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualRoadShadowLayer(options = {}) {
  const guard = canInitializeCustom25DVisualRoadShadowLayer(options);
  const roadShadowLayerConfig = {
    key: "roadShadow",
    name: "Road Shadow",
    order: 5,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft offset road depth shadow foundation for future 2.5D map polish",
    targetStyle: "gentle warm-gray road shadows, subtle depth, rounded cartoony roads, mobile-friendly, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealRoadGeometry: false,
    dependsOnGrassLayer: true,
    sitsAboveLandLayers: true,
    sitsBelowRoadBaseLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 80,
      dormant: true,
      defaultVisualChange: false,
      roadShadowLayerState: getCustom25DVisualRoadShadowLayerState(),
      roadShadowLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 80,
    dormant: true,
    defaultVisualChange: false,
    reason: "Road shadow layer remains guarded, data-only, and inert in this phase.",
    roadShadowLayerState: {
      ...getCustom25DVisualRoadShadowLayerState(),
      lastReason: "guarded-invisible-road-shadow-layer"
    },
    roadShadowLayerConfig
  };
}

function clearCustom25DVisualRoadShadowLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 80,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual road shadow layer exists to clear in this phase."
  };
}

function getCustom25DVisualRoadShadowLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 80,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureRoadShadowVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRoadBaseLayerState() {
  return {
    ok: true,
    phase: 81,
    name: "Custom 2.5D visual renderer road base layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualRoadBaseLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowRoadBaseLayer !== true) {
    blockedReasons.push("options.allowRoadBaseLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 81,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualRoadBaseLayer(options = {}) {
  const guard = canInitializeCustom25DVisualRoadBaseLayer(options);
  const roadBaseLayerConfig = {
    key: "roadBase",
    name: "Road Base",
    order: 6,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "cartoony rounded road body foundation for future 2.5D map polish",
    targetStyle: "soft warm road surfaces, rounded edges, toy-like mobile-friendly roads, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealRoadGeometry: false,
    dependsOnRoadShadowLayer: true,
    sitsAboveRoadShadowLayer: true,
    sitsBelowRoadHighlightLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 81,
      dormant: true,
      defaultVisualChange: false,
      roadBaseLayerState: getCustom25DVisualRoadBaseLayerState(),
      roadBaseLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 81,
    dormant: true,
    defaultVisualChange: false,
    reason: "Road base layer remains guarded, data-only, and inert in this phase.",
    roadBaseLayerState: {
      ...getCustom25DVisualRoadBaseLayerState(),
      lastReason: "guarded-invisible-road-base-layer"
    },
    roadBaseLayerConfig
  };
}

function clearCustom25DVisualRoadBaseLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 81,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual road base layer exists to clear in this phase."
  };
}

function getCustom25DVisualRoadBaseLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 81,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureRoadBaseVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualRoadHighlightLayerState() {
  return {
    ok: true,
    phase: 82,
    name: "Custom 2.5D visual renderer road highlight layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    requiresRoadBaseLayer: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualRoadHighlightLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowRoadHighlightLayer !== true) {
    blockedReasons.push("options.allowRoadHighlightLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }
  if (options.hasRoadBaseLayer !== true) {
    blockedReasons.push("A valid guarded road base layer is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 82,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualRoadHighlightLayer(options = {}) {
  const guard = canInitializeCustom25DVisualRoadHighlightLayer(options);
  const roadHighlightLayerConfig = {
    key: "roadHighlight",
    name: "Road Highlight",
    order: 7,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft road edge highlight and polish layer for future cartoony 2.5D roads",
    targetStyle: "subtle warm highlights, rounded road edges, toy-like mobile-friendly road polish, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealRoadGeometry: false,
    dependsOnRoadShadowLayer: true,
    dependsOnRoadBaseLayer: true,
    sitsAboveRoadShadowLayer: true,
    sitsAboveRoadBaseLayer: true,
    sitsBelowLabelLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 82,
      dormant: true,
      defaultVisualChange: false,
      roadHighlightLayerState: getCustom25DVisualRoadHighlightLayerState(),
      roadHighlightLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 82,
    dormant: true,
    defaultVisualChange: false,
    reason: "Road highlight layer remains guarded, data-only, and inert in this phase.",
    roadHighlightLayerState: {
      ...getCustom25DVisualRoadHighlightLayerState(),
      lastReason: "guarded-invisible-road-highlight-layer"
    },
    roadHighlightLayerConfig
  };
}

function clearCustom25DVisualRoadHighlightLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 82,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual road highlight layer exists to clear in this phase."
  };
}

function getCustom25DVisualRoadHighlightLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 82,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    hasGuardedRoadHighlightLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureRoadHighlightVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualBuildingShadowLayerState() {
  return {
    ok: true,
    phase: 83,
    name: "Custom 2.5D visual renderer building shadow layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualBuildingShadowLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowBuildingShadowLayer !== true) {
    blockedReasons.push("options.allowBuildingShadowLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 83,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualBuildingShadowLayer(options = {}) {
  const guard = canInitializeCustom25DVisualBuildingShadowLayer(options);
  const buildingShadowLayerConfig = {
    key: "buildingShadow",
    name: "Building Shadow",
    order: 8,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "soft offset building depth shadow foundation for future toy-like 2.5D buildings",
    targetStyle: "subtle warm building shadows, soft offset depth, mobile-friendly toy-map polish, Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealBuildingGeometry: false,
    dependsOnBuildingBaseLayer: false,
    sitsAboveRoadHighlightLayer: true,
    sitsBelowBuildingBaseLayer: true,
    sitsBelowLabelLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 83,
      dormant: true,
      defaultVisualChange: false,
      buildingShadowLayerState: getCustom25DVisualBuildingShadowLayerState(),
      buildingShadowLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 83,
    dormant: true,
    defaultVisualChange: false,
    reason: "Building shadow layer remains guarded, data-only, and inert in this phase.",
    buildingShadowLayerState: {
      ...getCustom25DVisualBuildingShadowLayerState(),
      lastReason: "guarded-invisible-building-shadow-layer"
    },
    buildingShadowLayerConfig
  };
}

function clearCustom25DVisualBuildingShadowLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 83,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual building shadow layer exists to clear in this phase."
  };
}

function getCustom25DVisualBuildingShadowLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 83,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    hasGuardedRoadHighlightLayerImplementation: true,
    hasGuardedBuildingShadowLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureBuildingShadowVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualBuildingBaseLayerState() {
  return {
    ok: true,
    phase: 84,
    name: "Custom 2.5D visual renderer building base layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    requiresBuildingShadowLayer: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualBuildingBaseLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowBuildingBaseLayer !== true) {
    blockedReasons.push("options.allowBuildingBaseLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }
  if (options.hasBuildingShadowLayer !== true) {
    blockedReasons.push("A valid guarded building shadow layer is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 84,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualBuildingBaseLayer(options = {}) {
  const guard = canInitializeCustom25DVisualBuildingBaseLayer(options);
  const buildingBaseLayerConfig = {
    key: "buildingBase",
    name: "Building Base",
    order: 9,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "simple toy-like building body foundation for future 2.5D map polish",
    targetStyle: "soft rounded building footprints, warm pastel surfaces, simple toy-block shapes, mobile-friendly Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealBuildingGeometry: false,
    dependsOnBuildingShadowLayer: true,
    sitsAboveBuildingShadowLayer: true,
    sitsBelowBuildingRoofLayer: true,
    sitsBelowLabelLayer: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 84,
      dormant: true,
      defaultVisualChange: false,
      buildingBaseLayerState: getCustom25DVisualBuildingBaseLayerState(),
      buildingBaseLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 84,
    dormant: true,
    defaultVisualChange: false,
    reason: "Building base layer remains guarded, data-only, and inert in this phase.",
    buildingBaseLayerState: {
      ...getCustom25DVisualBuildingBaseLayerState(),
      lastReason: "guarded-invisible-building-base-layer"
    },
    buildingBaseLayerConfig
  };
}

function clearCustom25DVisualBuildingBaseLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 84,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual building base layer exists to clear in this phase."
  };
}

function getCustom25DVisualBuildingBaseLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 84,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    hasGuardedRoadHighlightLayerImplementation: true,
    hasGuardedBuildingShadowLayerImplementation: true,
    hasGuardedBuildingBaseLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureBuildingBaseVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualBuildingRoofLayerState() {
  return {
    ok: true,
    phase: 85,
    name: "Custom 2.5D visual renderer building roof layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    requiresBuildingShadowLayer: true,
    requiresBuildingBaseLayer: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualBuildingRoofLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowBuildingRoofLayer !== true) {
    blockedReasons.push("options.allowBuildingRoofLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }
  if (options.hasBuildingShadowLayer !== true) {
    blockedReasons.push("A valid guarded building shadow layer is required");
  }
  if (options.hasBuildingBaseLayer !== true) {
    blockedReasons.push("A valid guarded building base layer is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 85,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualBuildingRoofLayer(options = {}) {
  const guard = canInitializeCustom25DVisualBuildingRoofLayer(options);
  const buildingRoofLayerConfig = {
    key: "buildingRoof",
    name: "Building Roof",
    order: 10,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "simple toy-like roof cap foundation for future 2.5D building polish",
    targetStyle: "soft rounded roof caps, warm playful roof colors, simple toy-block building tops, mobile-friendly Cowes Jetty visual direction",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealBuildingGeometry: false,
    dependsOnBuildingShadowLayer: true,
    dependsOnBuildingBaseLayer: true,
    sitsAboveBuildingShadowLayer: true,
    sitsAboveBuildingBaseLayer: true,
    sitsBelowLabelLayer: true,
    completesBuildingDepthTrio: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 85,
      dormant: true,
      defaultVisualChange: false,
      buildingRoofLayerState: getCustom25DVisualBuildingRoofLayerState(),
      buildingRoofLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 85,
    dormant: true,
    defaultVisualChange: false,
    reason: "Building roof layer remains guarded, data-only, and inert in this phase.",
    buildingRoofLayerState: {
      ...getCustom25DVisualBuildingRoofLayerState(),
      lastReason: "guarded-invisible-building-roof-layer"
    },
    buildingRoofLayerConfig
  };
}

function clearCustom25DVisualBuildingRoofLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 85,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual building roof layer exists to clear in this phase."
  };
}

function getCustom25DVisualBuildingRoofLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 85,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    hasGuardedRoadHighlightLayerImplementation: true,
    hasGuardedBuildingShadowLayerImplementation: true,
    hasGuardedBuildingBaseLayerImplementation: true,
    hasGuardedBuildingRoofLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    readyForFutureBuildingRoofVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualLabelLayerState() {
  return {
    ok: true,
    phase: 86,
    name: "Custom 2.5D visual renderer label layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    requiresRoadHighlightLayer: true,
    requiresBuildingRoofLayer: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    preservesOSMLabels: true,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualLabelLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowLabelLayer !== true) {
    blockedReasons.push("options.allowLabelLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }
  if (options.hasRoadHighlightLayer !== true) {
    blockedReasons.push("A valid guarded road highlight layer is required");
  }
  if (options.hasBuildingRoofLayer !== true) {
    blockedReasons.push("A valid guarded building roof layer is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 86,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualLabelLayer(options = {}) {
  const guard = canInitializeCustom25DVisualLabelLayer(options);
  const labelLayerConfig = {
    key: "label",
    name: "Label",
    order: 11,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "readable street and place label foundation for future polished 2.5D map presentation",
    targetStyle: "clear mobile-friendly labels, soft outline or shadow support, readable over toy-like roads, grass, water, parks, and buildings",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    usesRealStreetLabels: false,
    usesRealPlaceLabels: false,
    dependsOnRoadHighlightLayer: true,
    dependsOnBuildingRoofLayer: true,
    sitsAboveRoadShadowLayer: true,
    sitsAboveRoadBaseLayer: true,
    sitsAboveRoadHighlightLayer: true,
    sitsAboveBuildingShadowLayer: true,
    sitsAboveBuildingBaseLayer: true,
    sitsAboveBuildingRoofLayer: true,
    sitsBelowPinLayer: true,
    preservesOSMLabels: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 86,
      dormant: true,
      defaultVisualChange: false,
      labelLayerState: getCustom25DVisualLabelLayerState(),
      labelLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 86,
    dormant: true,
    defaultVisualChange: false,
    reason: "Label layer remains guarded, data-only, and inert in this phase.",
    labelLayerState: {
      ...getCustom25DVisualLabelLayerState(),
      lastReason: "guarded-invisible-label-layer"
    },
    labelLayerConfig
  };
}

function clearCustom25DVisualLabelLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 86,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual label layer exists to clear in this phase."
  };
}

function getCustom25DVisualLabelLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 86,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    hasGuardedRoadHighlightLayerImplementation: true,
    hasGuardedBuildingShadowLayerImplementation: true,
    hasGuardedBuildingBaseLayerImplementation: true,
    hasGuardedBuildingRoofLayerImplementation: true,
    hasGuardedLabelLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    preservesOSMLabelsByDefault: true,
    readyForFutureLabelVisualActivationRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualPinOverlayPreservationLayerState() {
  return {
    ok: true,
    phase: 87,
    name: "Custom 2.5D visual renderer pin overlay preservation layer",
    dormant: true,
    enabled: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP",
    requiresRenderLayerRegistry: true,
    requiresLayerHost: true,
    requiresLabelLayer: true,
    layerInitialized: false,
    layerVisible: false,
    drawsContent: false,
    affectsMap: false,
    createsPins: false,
    movesPins: false,
    restylesPins: false,
    changesPlayerMarker: false,
    changesCaptureRadius: false,
    lastInitializedAt: null,
    lastReason: null,
    defaultVisualChange: false
  };
}

function canInitializeCustom25DVisualPinOverlayPreservationLayer(options = {}) {
  const blockedReasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must be true");
  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowPinOverlayPreservationLayer !== true) {
    blockedReasons.push("options.allowPinOverlayPreservationLayer must be true");
  }
  if (options.hasRenderLayerRegistry !== true) {
    blockedReasons.push("A valid guarded render-layer registry is required");
  }
  if (options.hasLayerHost !== true) {
    blockedReasons.push("A valid guarded invisible layer host is required");
  }
  if (options.hasLabelLayer !== true) {
    blockedReasons.push("A valid guarded label layer is required");
  }

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blocked: blockedReasons.length > 0,
    blockedReasons,
    phase: 87,
    dormant: true,
    defaultVisualChange: false
  };
}

function initializeCustom25DVisualPinOverlayPreservationLayer(options = {}) {
  const guard = canInitializeCustom25DVisualPinOverlayPreservationLayer(options);
  const pinOverlayPreservationLayerConfig = {
    key: "pinOverlayPreservation",
    name: "Pin Overlay Preservation",
    order: 12,
    enabledByDefault: false,
    visible: false,
    implemented: false,
    dormant: true,
    intendedVisualRole: "preserve normal GrowGo pins, player marker, and capture radius above future custom 2.5D visual layers",
    targetStyle: "existing GrowGo blue pins, avatar player marker, and capture radius remain unchanged and readable over the polished map",
    drawsContent: false,
    affectsMap: false,
    dataSource: "none",
    createsPins: false,
    movesPins: false,
    restylesPins: false,
    changesPlayerMarker: false,
    changesCaptureRadius: false,
    dependsOnLabelLayer: true,
    sitsAboveBackgroundLayer: true,
    sitsAboveWaterLayer: true,
    sitsAboveBeachLayer: true,
    sitsAboveGrassLayer: true,
    sitsAboveParkLayer: true,
    sitsAboveRoadShadowLayer: true,
    sitsAboveRoadBaseLayer: true,
    sitsAboveRoadHighlightLayer: true,
    sitsAboveBuildingShadowLayer: true,
    sitsAboveBuildingBaseLayer: true,
    sitsAboveBuildingRoofLayer: true,
    sitsAboveLabelLayer: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true
  };

  if (!guard.allowed) {
    return {
      ok: false,
      initialized: false,
      blocked: true,
      blockedReasons: guard.blockedReasons,
      phase: 87,
      dormant: true,
      defaultVisualChange: false,
      pinOverlayPreservationLayerState: getCustom25DVisualPinOverlayPreservationLayerState(),
      pinOverlayPreservationLayerConfig
    };
  }

  return {
    ok: true,
    initialized: false,
    blocked: false,
    phase: 87,
    dormant: true,
    defaultVisualChange: false,
    reason: "Pin overlay preservation layer remains guarded, data-only, and inert in this phase.",
    pinOverlayPreservationLayerState: {
      ...getCustom25DVisualPinOverlayPreservationLayerState(),
      lastReason: "guarded-invisible-pin-overlay-preservation-layer"
    },
    pinOverlayPreservationLayerConfig
  };
}

function clearCustom25DVisualPinOverlayPreservationLayer() {
  return {
    ok: true,
    cleared: false,
    phase: 87,
    dormant: true,
    defaultVisualChange: false,
    reason: "No custom 2.5D visual pin overlay preservation layer exists to clear in this phase."
  };
}

function getCustom25DVisualPinOverlayPreservationLayerImplementationReadiness() {
  return {
    ok: true,
    phase: 87,
    dormant: true,
    hasPaletteConfig: true,
    hasLayerStructureConfig: true,
    hasContainerPrep: true,
    hasRendererShell: true,
    hasGuardedContainerImplementation: true,
    hasGuardedLayerHostImplementation: true,
    hasGuardedRenderLayerRegistryImplementation: true,
    hasGuardedBackgroundLayerImplementation: true,
    hasGuardedWaterLayerImplementation: true,
    hasGuardedBeachLayerImplementation: true,
    hasGuardedGrassLayerImplementation: true,
    hasGuardedParkLayerImplementation: true,
    hasGuardedRoadShadowLayerImplementation: true,
    hasGuardedRoadBaseLayerImplementation: true,
    hasGuardedRoadHighlightLayerImplementation: true,
    hasGuardedBuildingShadowLayerImplementation: true,
    hasGuardedBuildingBaseLayerImplementation: true,
    hasGuardedBuildingRoofLayerImplementation: true,
    hasGuardedLabelLayerImplementation: true,
    hasGuardedPinOverlayPreservationLayerImplementation: true,
    dataOnlyLayerConfig: true,
    nonRenderingByDefault: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    readyForFutureOverlayOrderingProtectionRules: true,
    defaultVisualChange: false,
    requiresFlag: "ENABLE_CUSTOM_25D_MAP"
  };
}

function getCustom25DVisualLayerStackReadinessSummary() {
  const safeCall = (fn) => {
    if (typeof fn === "function") {
      try {
        return fn();
      } catch (_error) {
        return { ok: false, missing: false, errored: true };
      }
    }
    return { ok: false, missing: true };
  };

  const layerOrder = [
    {
      key: "background",
      name: "Background",
      order: 1,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft base world color foundation for future 2.5D map polish"
    },
    {
      key: "water",
      name: "Water",
      order: 2,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft playful water foundation for future 2.5D map polish"
    },
    {
      key: "beach",
      name: "Beach",
      order: 3,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft sand and shoreline foundation for future 2.5D map polish"
    },
    {
      key: "grass",
      name: "Grass",
      order: 4,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft base land and grass foundation for future 2.5D map polish"
    },
    {
      key: "park",
      name: "Park",
      order: 5,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft park and green-space accent layer for future 2.5D map polish"
    },
    {
      key: "roadShadow",
      name: "Road Shadow",
      order: 6,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft offset road depth shadow foundation for future 2.5D map polish"
    },
    {
      key: "roadBase",
      name: "Road Base",
      order: 7,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "cartoony rounded road body foundation for future 2.5D map polish"
    },
    {
      key: "roadHighlight",
      name: "Road Highlight",
      order: 8,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft road edge highlight and polish layer for future cartoony 2.5D roads"
    },
    {
      key: "buildingShadow",
      name: "Building Shadow",
      order: 9,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "soft offset building depth shadow foundation for future toy-like 2.5D buildings"
    },
    {
      key: "buildingBase",
      name: "Building Base",
      order: 10,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "simple toy-like building body foundation for future 2.5D map polish"
    },
    {
      key: "buildingRoof",
      name: "Building Roof",
      order: 11,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "simple toy-like roof cap foundation for future 2.5D building polish"
    },
    {
      key: "label",
      name: "Label",
      order: 12,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "readable street and place label foundation for future polished 2.5D map presentation"
    },
    {
      key: "pinOverlayPreservation",
      name: "Pin Overlay Preservation",
      order: 13,
      dormant: true,
      visible: false,
      implemented: false,
      drawsContent: false,
      affectsMap: false,
      dataSource: "none",
      intendedVisualRole: "preserve normal GrowGo pins, player marker, and capture radius above future custom 2.5D visual layers"
    }
  ];

  return {
    ok: true,
    phase: 88,
    name: "Custom 2.5D visual layer stack readiness summary",
    dormant: true,
    guarded: true,
    invisibleByDefault: true,
    nonRendering: true,
    dataOnly: true,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    usesRealPOIData: false,
    usesDinosaurSiteData: false,
    usesFilmLocationData: false,
    usesRealRoadGeometry: false,
    usesRealBuildingGeometry: false,
    layerStackPrepared: true,
    readyForFutureManualRendererAssembly: true,
    readyForFutureVisualPrototype: true,
    layerOrder,
    readinessChecks: {
      background: safeCall(
        typeof getCustom25DVisualBackgroundLayerImplementationReadiness === "function"
          ? getCustom25DVisualBackgroundLayerImplementationReadiness
          : null
      ),
      water: safeCall(
        typeof getCustom25DVisualWaterLayerImplementationReadiness === "function"
          ? getCustom25DVisualWaterLayerImplementationReadiness
          : null
      ),
      beach: safeCall(
        typeof getCustom25DVisualBeachLayerImplementationReadiness === "function"
          ? getCustom25DVisualBeachLayerImplementationReadiness
          : null
      ),
      grass: safeCall(
        typeof getCustom25DVisualGrassLayerImplementationReadiness === "function"
          ? getCustom25DVisualGrassLayerImplementationReadiness
          : null
      ),
      park: safeCall(
        typeof getCustom25DVisualParkLayerImplementationReadiness === "function"
          ? getCustom25DVisualParkLayerImplementationReadiness
          : null
      ),
      roadShadow: safeCall(
        typeof getCustom25DVisualRoadShadowLayerImplementationReadiness === "function"
          ? getCustom25DVisualRoadShadowLayerImplementationReadiness
          : null
      ),
      roadBase: safeCall(
        typeof getCustom25DVisualRoadBaseLayerImplementationReadiness === "function"
          ? getCustom25DVisualRoadBaseLayerImplementationReadiness
          : null
      ),
      roadHighlight: safeCall(
        typeof getCustom25DVisualRoadHighlightLayerImplementationReadiness === "function"
          ? getCustom25DVisualRoadHighlightLayerImplementationReadiness
          : null
      ),
      buildingShadow: safeCall(
        typeof getCustom25DVisualBuildingShadowLayerImplementationReadiness === "function"
          ? getCustom25DVisualBuildingShadowLayerImplementationReadiness
          : null
      ),
      buildingBase: safeCall(
        typeof getCustom25DVisualBuildingBaseLayerImplementationReadiness === "function"
          ? getCustom25DVisualBuildingBaseLayerImplementationReadiness
          : null
      ),
      buildingRoof: safeCall(
        typeof getCustom25DVisualBuildingRoofLayerImplementationReadiness === "function"
          ? getCustom25DVisualBuildingRoofLayerImplementationReadiness
          : null
      ),
      label: safeCall(
        typeof getCustom25DVisualLabelLayerImplementationReadiness === "function"
          ? getCustom25DVisualLabelLayerImplementationReadiness
          : null
      ),
      pinOverlayPreservation: safeCall(
        typeof getCustom25DVisualPinOverlayPreservationLayerImplementationReadiness === "function"
          ? getCustom25DVisualPinOverlayPreservationLayerImplementationReadiness
          : null
      )
    }
  };
}

function getCustom25DVisualManualRendererAssemblyReadinessPlan() {
  const safeLayerStackSummary =
    typeof getCustom25DVisualLayerStackReadinessSummary === "function"
      ? getCustom25DVisualLayerStackReadinessSummary()
      : {
          ok: false,
          missing: true,
          reason: "Layer stack readiness summary helper is unavailable."
        };

  return {
    ok: true,
    phase: 89,
    name: "Custom 2.5D visual manual renderer assembly readiness plan",
    dormant: true,
    planningOnly: true,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    invisibleByDefault: true,
    nonRendering: true,
    dataOnly: true,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    assemblesRenderer: false,
    initializesLayers: false,
    createsVisibleMapLayers: false,
    drawsContent: false,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    usesRealPOIData: false,
    usesDinosaurSiteData: false,
    usesFilmLocationData: false,
    usesRealRoadGeometry: false,
    usesRealBuildingGeometry: false,
    requiresEnableCustom25DMap: true,
    requiresManualOption: true,
    requiresDeveloperIntent: true,
    requiresRendererShell: true,
    requiresRendererContainer: true,
    requiresLayerHost: true,
    requiresRenderLayerRegistry: true,
    requiresPreparedLayerStack: true,
    readyForFutureManualAssemblyPhase: true,
    checklist: [
      "Confirm ENABLE_CUSTOM_25D_MAP is intentionally enabled only for a manual/developer test.",
      "Confirm manual option is explicitly true.",
      "Confirm developerIntent option is explicitly true.",
      "Confirm renderer shell exists before assembly.",
      "Confirm renderer container exists before assembly.",
      "Confirm layer host exists before assembly.",
      "Confirm render layer registry exists before assembly.",
      "Confirm all visual layer configs remain invisible/non-rendering before any draw phase.",
      "Confirm normal blue pins remain above the custom visual stack.",
      "Confirm player marker remains above the custom visual stack.",
      "Confirm capture radius remains above the custom visual stack.",
      "Confirm OSM labels and OSM behavior are preserved unless a future planned phase explicitly changes that."
    ],
    blockedByDefaultReasons: [
      "ENABLE_CUSTOM_25D_MAP is false by default.",
      "No startup wiring exists.",
      "No automatic initialization exists.",
      "No visible drawing layers are created.",
      "No real geometry/data loading exists.",
      "Manual/developer intent is required for any future assembly."
    ],
    intendedFutureAssemblyOrder: [
      "rendererShell",
      "rendererContainer",
      "layerHost",
      "renderLayerRegistry",
      "background",
      "water",
      "beach",
      "grass",
      "park",
      "roadShadow",
      "roadBase",
      "roadHighlight",
      "buildingShadow",
      "buildingBase",
      "buildingRoof",
      "label",
      "pinOverlayPreservation"
    ],
    passiveReports: {
      layerStackSummary: safeLayerStackSummary
    }
  };
}

function getCustom25DVisualManualRendererAssemblyDryRunPlan() {
  const safeCall = (fn, fallbackReason) => {
    if (typeof fn === "function") {
      try {
        return fn();
      } catch (_error) {
        return { ok: false, missing: false, errored: true, reason: fallbackReason };
      }
    }
    return { ok: false, missing: true, reason: fallbackReason };
  };

  return {
    ok: true,
    phase: 90,
    name: "Custom 2.5D visual manual renderer assembly dry-run plan",
    dormant: true,
    planningOnly: true,
    dryRunOnly: true,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    invisibleByDefault: true,
    nonRendering: true,
    dataOnly: true,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    assemblesRenderer: false,
    initializesLayers: false,
    createsRendererShell: false,
    createsRendererContainer: false,
    createsLayerHost: false,
    initializesRenderLayerRegistry: false,
    createsVisibleMapLayers: false,
    drawsContent: false,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    usesRealPOIData: false,
    usesDinosaurSiteData: false,
    usesFilmLocationData: false,
    usesRealRoadGeometry: false,
    usesRealBuildingGeometry: false,
    requiresEnableCustom25DMap: true,
    requiresManualOption: true,
    requiresDeveloperIntent: true,
    requiresRendererShellReadiness: true,
    requiresRendererContainerReadiness: true,
    requiresLayerHostReadiness: true,
    requiresRenderLayerRegistryReadiness: true,
    requiresPreparedLayerStackReadiness: true,
    readyForFutureManualDryRunPhase: true,
    dryRunChecklist: [
      "Confirm ENABLE_CUSTOM_25D_MAP is intentionally enabled only for a manual/developer test.",
      "Confirm manual option is explicitly true.",
      "Confirm developerIntent option is explicitly true.",
      "Confirm renderer shell readiness before attempting any future creation.",
      "Confirm renderer container readiness before attempting any future creation.",
      "Confirm layer host readiness before attempting any future creation.",
      "Confirm render layer registry readiness before attempting any future initialization.",
      "Confirm full visual layer stack readiness summary is available.",
      "Confirm all layer configs remain invisible, dormant, and non-rendering.",
      "Confirm no startup wiring exists.",
      "Confirm no automatic initialization exists.",
      "Confirm normal blue pins remain above the future visual stack.",
      "Confirm player marker remains above the future visual stack.",
      "Confirm capture radius remains above the future visual stack.",
      "Confirm OSM labels and OSM behavior remain preserved."
    ],
    dryRunSequence: [
      "inspectRendererShellReadiness",
      "inspectRendererContainerReadiness",
      "inspectLayerHostReadiness",
      "inspectRenderLayerRegistryReadiness",
      "inspectVisualLayerStackReadiness",
      "inspectManualAssemblyReadinessPlan",
      "confirmNoInitialization",
      "confirmNoRendering",
      "confirmGameplayOverlayPreservation"
    ],
    blockedByDefaultReasons: [
      "ENABLE_CUSTOM_25D_MAP is false by default.",
      "No startup wiring exists.",
      "No automatic initialization exists.",
      "No renderer assembly exists.",
      "No visible drawing layers are created.",
      "No real geometry/data loading exists.",
      "Manual/developer intent is required for any future dry run."
    ],
    passiveReports: {
      rendererShellReadiness: safeCall(
        typeof getCustom25DVisualRendererShellReadiness === "function"
          ? getCustom25DVisualRendererShellReadiness
          : null,
        "Renderer shell readiness helper is unavailable."
      ),
      rendererContainerReadiness: safeCall(
        typeof getCustom25DVisualRendererContainerImplementationReadiness === "function"
          ? getCustom25DVisualRendererContainerImplementationReadiness
          : null,
        "Renderer container readiness helper is unavailable."
      ),
      layerHostReadiness: safeCall(
        typeof getCustom25DVisualLayerHostImplementationReadiness === "function"
          ? getCustom25DVisualLayerHostImplementationReadiness
          : null,
        "Layer host readiness helper is unavailable."
      ),
      renderLayerRegistryReadiness: safeCall(
        typeof getCustom25DVisualRenderLayerRegistryImplementationReadiness === "function"
          ? getCustom25DVisualRenderLayerRegistryImplementationReadiness
          : null,
        "Render layer registry readiness helper is unavailable."
      ),
      visualLayerStackReadiness: safeCall(
        typeof getCustom25DVisualLayerStackReadinessSummary === "function"
          ? getCustom25DVisualLayerStackReadinessSummary
          : null,
        "Visual layer stack readiness summary helper is unavailable."
      ),
      manualAssemblyReadinessPlan: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyReadinessPlan === "function"
          ? getCustom25DVisualManualRendererAssemblyReadinessPlan
          : null,
        "Manual renderer assembly readiness helper is unavailable."
      )
    }
  };
}

function getCustom25DVisualManualRendererAssemblyGuardContract() {
  const safeCall = (fn, fallbackReason) => {
    if (typeof fn === "function") {
      try {
        return fn();
      } catch (_error) {
        return { ok: false, missing: false, errored: true, reason: fallbackReason };
      }
    }
    return { ok: false, missing: true, reason: fallbackReason };
  };

  return {
    ok: true,
    phase: 91,
    name: "Custom 2.5D visual manual renderer assembly guard contract",
    dormant: true,
    contractOnly: true,
    planningOnly: true,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    invisibleByDefault: true,
    nonRendering: true,
    dataOnly: true,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    assemblesRenderer: false,
    initializesLayers: false,
    createsRendererShell: false,
    createsRendererContainer: false,
    createsLayerHost: false,
    initializesRenderLayerRegistry: false,
    createsVisibleMapLayers: false,
    drawsContent: false,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    usesRealPOIData: false,
    usesDinosaurSiteData: false,
    usesFilmLocationData: false,
    usesRealRoadGeometry: false,
    usesRealBuildingGeometry: false,
    readyForFutureGuardedAssemblyFunction: true,
    requiredFutureAssemblyOptions: {
      manual: true,
      developerIntent: true,
      allowRendererAssembly: true,
      hasRendererShell: true,
      hasRendererContainer: true,
      hasLayerHost: true,
      hasRenderLayerRegistry: true,
      hasPreparedLayerStack: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true
    },
    requiredFutureBlockedConditions: [
      "ENABLE_CUSTOM_25D_MAP must not be false.",
      "options.manual must be exactly true.",
      "options.developerIntent must be exactly true.",
      "options.allowRendererAssembly must be exactly true.",
      "options.hasRendererShell must be exactly true.",
      "options.hasRendererContainer must be exactly true.",
      "options.hasLayerHost must be exactly true.",
      "options.hasRenderLayerRegistry must be exactly true.",
      "options.hasPreparedLayerStack must be exactly true.",
      "options.preserveGameplayOverlays must be exactly true.",
      "options.preserveOSMBehavior must be exactly true."
    ],
    futureAssemblyMustNot: [
      "must not run on startup",
      "must not auto-enable ENABLE_CUSTOM_25D_MAP",
      "must not initialize without manual option",
      "must not initialize without developerIntent option",
      "must not create visible drawing layers by default",
      "must not hide or replace OSM tiles by default",
      "must not hide or replace OSM labels by default",
      "must not create, move, restyle, hide, or replace normal blue pins",
      "must not change player marker",
      "must not change capture radius",
      "must not load real POI data",
      "must not load dinosaur site data",
      "must not load film location data",
      "must not change gameplay, backend, rewards, or collections",
      "must not auto-pan, auto-fly, or auto-zoom"
    ],
    futureAssemblyAllowedOnlyWhen: [
      "ENABLE_CUSTOM_25D_MAP is intentionally enabled for a manual/developer test",
      "manual option is explicitly true",
      "developerIntent option is explicitly true",
      "allowRendererAssembly option is explicitly true",
      "renderer shell readiness has been confirmed",
      "renderer container readiness has been confirmed",
      "layer host readiness has been confirmed",
      "render layer registry readiness has been confirmed",
      "full dormant visual layer stack readiness has been confirmed",
      "gameplay overlay preservation has been confirmed"
    ],
    passiveReports: {
      manualAssemblyReadinessPlan: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyReadinessPlan === "function"
          ? getCustom25DVisualManualRendererAssemblyReadinessPlan
          : null,
        "Manual renderer assembly readiness helper is unavailable."
      ),
      manualAssemblyDryRunPlan: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyDryRunPlan === "function"
          ? getCustom25DVisualManualRendererAssemblyDryRunPlan
          : null,
        "Manual renderer assembly dry-run helper is unavailable."
      ),
      visualLayerStackReadiness: safeCall(
        typeof getCustom25DVisualLayerStackReadinessSummary === "function"
          ? getCustom25DVisualLayerStackReadinessSummary
          : null,
        "Visual layer stack readiness summary helper is unavailable."
      )
    }
  };
}

function getCustom25DVisualManualRendererAssemblyGuardEvaluatorPlan() {
  const safeCall = (fn, fallbackReason) => {
    if (typeof fn === "function") {
      try {
        return fn();
      } catch (_error) {
        return { ok: false, missing: false, errored: true, reason: fallbackReason };
      }
    }
    return { ok: false, missing: true, reason: fallbackReason };
  };

  return {
    ok: true,
    phase: 92,
    name: "Custom 2.5D visual manual renderer assembly guard evaluator plan",
    dormant: true,
    planOnly: true,
    evaluatorOnly: false,
    createsCanAssembleHelper: false,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    invisibleByDefault: true,
    nonRendering: true,
    dataOnly: true,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    assemblesRenderer: false,
    initializesLayers: false,
    createsRendererShell: false,
    createsRendererContainer: false,
    createsLayerHost: false,
    initializesRenderLayerRegistry: false,
    createsVisibleMapLayers: false,
    drawsContent: false,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    usesRealPOIData: false,
    usesDinosaurSiteData: false,
    usesFilmLocationData: false,
    usesRealRoadGeometry: false,
    usesRealBuildingGeometry: false,
    readyForFutureCanAssembleGuardHelper: true,
    futureHelperName: "canAssembleCustom25DVisualRenderer",
    futureHelperPurpose:
      "Evaluate strict manual/developer renderer assembly guard conditions without mutating state or creating visual output.",
    futureRequiredChecks: [
      "ENABLE_CUSTOM_25D_MAP must be true.",
      "options.manual must be exactly true.",
      "options.developerIntent must be exactly true.",
      "options.allowRendererAssembly must be exactly true.",
      "options.hasRendererShell must be exactly true.",
      "options.hasRendererContainer must be exactly true.",
      "options.hasLayerHost must be exactly true.",
      "options.hasRenderLayerRegistry must be exactly true.",
      "options.hasPreparedLayerStack must be exactly true.",
      "options.preserveGameplayOverlays must be exactly true.",
      "options.preserveOSMBehavior must be exactly true."
    ],
    futureFailureReasons: [
      "custom-25d-map-disabled",
      "manual-option-required",
      "developer-intent-required",
      "renderer-assembly-not-allowed",
      "renderer-shell-required",
      "renderer-container-required",
      "layer-host-required",
      "render-layer-registry-required",
      "prepared-layer-stack-required",
      "gameplay-overlay-preservation-required",
      "osm-behavior-preservation-required"
    ],
    futureReturnShape: {
      ok: "boolean",
      allowed: "boolean",
      reason: "string|null",
      reasons: "array",
      phase: 92,
      guard: "manual-renderer-assembly",
      dormant: true,
      mutatesState: false,
      drawsContent: false,
      createsVisibleMapLayers: false
    },
    futureEvaluatorMustNot: [
      "must not run on startup",
      "must not auto-enable ENABLE_CUSTOM_25D_MAP",
      "must not initialize anything",
      "must not assemble the renderer",
      "must not create renderer shell/container/host elements",
      "must not initialize render layer registry",
      "must not initialize visual layers",
      "must not draw visible content",
      "must not create visible map layers",
      "must not create, move, restyle, hide, or replace normal blue pins",
      "must not change player marker",
      "must not change capture radius",
      "must not change OSM behavior or OSM labels",
      "must not load real POI, dinosaur site, film location, road, or building data"
    ],
    passiveReports: {
      manualAssemblyGuardContract: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyGuardContract === "function"
          ? getCustom25DVisualManualRendererAssemblyGuardContract
          : null,
        "Manual renderer assembly guard contract helper is unavailable."
      ),
      manualAssemblyReadinessPlan: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyReadinessPlan === "function"
          ? getCustom25DVisualManualRendererAssemblyReadinessPlan
          : null,
        "Manual renderer assembly readiness helper is unavailable."
      ),
      manualAssemblyDryRunPlan: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyDryRunPlan === "function"
          ? getCustom25DVisualManualRendererAssemblyDryRunPlan
          : null,
        "Manual renderer assembly dry-run helper is unavailable."
      ),
      visualLayerStackReadiness: safeCall(
        typeof getCustom25DVisualLayerStackReadinessSummary === "function"
          ? getCustom25DVisualLayerStackReadinessSummary
          : null,
        "Visual layer stack readiness summary helper is unavailable."
      )
    }
  };
}

function canAssembleCustom25DVisualRenderer(options = {}) {
  const reasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) reasons.push("custom-25d-map-disabled");
  if (options.manual !== true) reasons.push("manual-option-required");
  if (options.developerIntent !== true) reasons.push("developer-intent-required");
  if (options.allowRendererAssembly !== true) reasons.push("renderer-assembly-not-allowed");
  if (options.hasRendererShell !== true) reasons.push("renderer-shell-required");
  if (options.hasRendererContainer !== true) reasons.push("renderer-container-required");
  if (options.hasLayerHost !== true) reasons.push("layer-host-required");
  if (options.hasRenderLayerRegistry !== true) reasons.push("render-layer-registry-required");
  if (options.hasPreparedLayerStack !== true) reasons.push("prepared-layer-stack-required");
  if (options.preserveGameplayOverlays !== true) {
    reasons.push("gameplay-overlay-preservation-required");
  }
  if (options.preserveOSMBehavior !== true) reasons.push("osm-behavior-preservation-required");

  return {
    ok: true,
    phase: 93,
    guard: "manual-renderer-assembly",
    allowed: reasons.length === 0,
    reason: reasons.length > 0 ? reasons[0] : null,
    reasons,
    dormant: true,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    mutatesState: false,
    assemblesRenderer: false,
    initializesLayers: false,
    createsRendererShell: false,
    createsRendererContainer: false,
    createsLayerHost: false,
    initializesRenderLayerRegistry: false,
    drawsContent: false,
    createsVisibleMapLayers: false,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    passiveReports: {
      guardContractAvailable:
        typeof getCustom25DVisualManualRendererAssemblyGuardContract === "function",
      evaluatorPlanAvailable:
        typeof getCustom25DVisualManualRendererAssemblyGuardEvaluatorPlan === "function"
    }
  };
}

function getCustom25DVisualManualRendererAssemblyResultContract() {
  const safeCall = (fn, fallbackReason) => {
    if (typeof fn === "function") {
      try {
        return fn();
      } catch (_error) {
        return { ok: false, missing: false, errored: true, reason: fallbackReason };
      }
    }
    return { ok: false, missing: true, reason: fallbackReason };
  };

  return {
    ok: true,
    phase: 94,
    name: "Custom 2.5D visual manual renderer assembly result contract",
    dormant: true,
    contractOnly: true,
    planningOnly: true,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    invisibleByDefault: true,
    nonRendering: true,
    dataOnly: true,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    assemblesRenderer: false,
    initializesLayers: false,
    createsRendererShell: false,
    createsRendererContainer: false,
    createsLayerHost: false,
    initializesRenderLayerRegistry: false,
    createsVisibleMapLayers: false,
    drawsContent: false,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    usesRealPOIData: false,
    usesDinosaurSiteData: false,
    usesFilmLocationData: false,
    usesRealRoadGeometry: false,
    usesRealBuildingGeometry: false,
    readyForFutureManualAssemblyResultShape: true,
    futureHelperName: "assembleCustom25DVisualRenderer",
    futureHelperPurpose:
      "Return a safe manual renderer assembly result without changing default app behavior or bypassing the guard evaluator.",
    futureResultShape: {
      ok: "boolean",
      phase: 94,
      action: "manual-renderer-assembly",
      attempted: "boolean",
      allowed: "boolean",
      blocked: "boolean",
      reason: "string|null",
      reasons: "array",
      assembled: "boolean",
      rendererShellReady: "boolean",
      rendererContainerReady: "boolean",
      layerHostReady: "boolean",
      renderLayerRegistryReady: "boolean",
      preparedLayerStackReady: "boolean",
      visualBehaviorChanged: "boolean",
      startupWiringAdded: "boolean",
      mutatesState: "boolean",
      drawsContent: "boolean",
      createsVisibleMapLayers: "boolean",
      preservesNormalBluePins: "boolean",
      preservesPlayerMarker: "boolean",
      preservesCaptureRadius: "boolean",
      preservesOSMBehavior: "boolean",
      preservesOSMLabels: "boolean"
    },
    futureBlockedResultExample: {
      ok: true,
      phase: 94,
      action: "manual-renderer-assembly",
      attempted: true,
      allowed: false,
      blocked: true,
      reason: "custom-25d-map-disabled",
      reasons: ["custom-25d-map-disabled"],
      assembled: false,
      visualBehaviorChanged: false,
      startupWiringAdded: false,
      mutatesState: false,
      drawsContent: false,
      createsVisibleMapLayers: false,
      preservesNormalBluePins: true,
      preservesPlayerMarker: true,
      preservesCaptureRadius: true,
      preservesOSMBehavior: true,
      preservesOSMLabels: true
    },
    futureAllowedDryResultExample: {
      ok: true,
      phase: 94,
      action: "manual-renderer-assembly",
      attempted: true,
      allowed: true,
      blocked: false,
      reason: null,
      reasons: [],
      assembled: false,
      visualBehaviorChanged: false,
      startupWiringAdded: false,
      mutatesState: false,
      drawsContent: false,
      createsVisibleMapLayers: false,
      preservesNormalBluePins: true,
      preservesPlayerMarker: true,
      preservesCaptureRadius: true,
      preservesOSMBehavior: true,
      preservesOSMLabels: true
    },
    futureAssemblyResultMustNot: [
      "must not report assembled true unless a future explicit assembly phase actually assembles",
      "must not hide guard failures",
      "must not treat missing options as allowed",
      "must not auto-enable ENABLE_CUSTOM_25D_MAP",
      "must not imply startup wiring exists",
      "must not imply visible rendering exists",
      "must not imply real POI, road, or building data is loaded",
      "must not imply pins, player marker, capture radius, OSM labels, or OSM behavior changed"
    ],
    passiveReports: {
      manualAssemblyGuardContract: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyGuardContract === "function"
          ? getCustom25DVisualManualRendererAssemblyGuardContract
          : null,
        "Manual renderer assembly guard contract helper is unavailable."
      ),
      manualAssemblyGuardEvaluatorPlan: safeCall(
        typeof getCustom25DVisualManualRendererAssemblyGuardEvaluatorPlan === "function"
          ? getCustom25DVisualManualRendererAssemblyGuardEvaluatorPlan
          : null,
        "Manual renderer assembly guard evaluator plan helper is unavailable."
      ),
      canAssembleGuard: safeCall(
        typeof canAssembleCustom25DVisualRenderer === "function"
          ? () => canAssembleCustom25DVisualRenderer({})
          : null,
        "Manual renderer assembly guard evaluator helper is unavailable."
      )
    }
  };
}

function assembleCustom25DVisualRendererManual(options = {}) {
  const guard = canAssembleCustom25DVisualRenderer(options);
  const passiveResultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };

  if (!guard.allowed) {
    return {
      ok: true,
      phase: 101,
      action: "manual-renderer-assembly",
      attempted: true,
      allowed: false,
      blocked: true,
      reason: guard.reason,
      reasons: guard.reasons,
      assembled: false,
      structuralAssemblyOnly: false,
      rendererShellCreated: false,
      rendererContainerCreated: false,
      layerHostCreated: false,
      rendererShellReady: options.hasRendererShell === true,
      rendererContainerReady: options.hasRendererContainer === true,
      layerHostReady: options.hasLayerHost === true,
      renderLayerRegistryInitialized: false,
      visualLayersInitialized: false,
      visualBehaviorChanged: false,
      startupWiringAdded: false,
      mutatesState: false,
      drawsContent: false,
      createsVisibleMapLayers: false,
      visibleGraphicsCreated: false,
      dataLoaded: false,
      gameplayChanged: false,
      preservesNormalBluePins: true,
      preservesPlayerMarker: true,
      preservesCaptureRadius: true,
      preservesOSMBehavior: true,
      preservesOSMLabels: true,
      safetyNotes: [
        "Guard failed, so no structural renderer helpers were called.",
        "No render layer registry or visual layers were initialized.",
        "No visible graphics, startup wiring, data loading, or gameplay changes occurred."
      ],
      passiveReports: {
        guard,
        resultContract: passiveResultContract
      }
    };
  }

  const shellResult =
    typeof initializeCustom25DVisualRendererShell === "function"
      ? initializeCustom25DVisualRendererShell({
          manual: true,
          developerIntent: true,
          allowVisualRendererShell: true
        })
      : {
          ok: false,
          initialized: false,
          blocked: true,
          blockedReasons: ["renderer-shell-helper-unavailable"]
        };
  const rendererShellCreated = shellResult.ok === true && shellResult.blocked === false;

  const containerResult =
    typeof createCustom25DVisualRendererContainer === "function"
      ? createCustom25DVisualRendererContainer({
          manual: true,
          developerIntent: true,
          allowVisualRendererContainer: true
        })
      : {
          ok: false,
          created: false,
          blocked: true,
          blockedReasons: ["renderer-container-helper-unavailable"]
        };
  const rendererContainerCreated =
    containerResult.ok === true && containerResult.blocked === false;

  const layerHostResult =
    typeof createCustom25DVisualLayerHost === "function"
      ? createCustom25DVisualLayerHost({
          manual: true,
          developerIntent: true,
          allowVisualLayerHost: true,
          hasRendererContainer: rendererContainerCreated
        })
      : {
          ok: false,
          created: false,
          blocked: true,
          blockedReasons: ["layer-host-helper-unavailable"]
        };
  const layerHostCreated = layerHostResult.ok === true && layerHostResult.blocked === false;
  const structuralAssemblySucceeded =
    rendererShellCreated && rendererContainerCreated && layerHostCreated;

  return {
    ok: true,
    phase: 101,
    action: "manual-renderer-assembly",
    attempted: true,
    allowed: true,
    blocked: false,
    reason: "structural-assembly-only",
    reasons: ["structural-assembly-only"],
    assembled: structuralAssemblySucceeded,
    structuralAssemblyOnly: structuralAssemblySucceeded,
    rendererShellCreated,
    rendererContainerCreated,
    layerHostCreated,
    rendererShellReady: rendererShellCreated,
    rendererContainerReady: rendererContainerCreated,
    layerHostReady: layerHostCreated,
    renderLayerRegistryInitialized: false,
    visualLayersInitialized: false,
    visualBehaviorChanged: false,
    startupWiringAdded: false,
    mutatesState: false,
    drawsContent: false,
    createsVisibleMapLayers: false,
    visibleGraphicsCreated: false,
    dataLoaded: false,
    gameplayChanged: false,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    preservesOSMBehavior: true,
    preservesOSMLabels: true,
    safetyNotes: [
      "Only inert structural renderer helpers were confirmed in this phase.",
      "Render layer registry and visual layers remain uninitialized.",
      "No drawing, visible graphics, startup wiring, data loading, or gameplay changes occurred."
    ],
    passiveReports: {
      guard,
      resultContract: passiveResultContract,
      rendererShell: shellResult,
      rendererContainer: containerResult,
      layerHost: layerHostResult
    }
  };
}

function getCustom25DVisualManualRendererAssemblyReadinessReport(options = {}) {
  const guard = canAssembleCustom25DVisualRenderer(options);
  const passiveResultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };

  return {
    ok: true,
    phase: 96,
    name: "custom-25d-visual-manual-renderer-assembly-readiness-report",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    guard: {
      allowed: guard.allowed,
      reason: guard.reason,
      reasons: guard.reasons,
      checks: {
        custom25DMapEnabled: ENABLE_CUSTOM_25D_MAP === true,
        manual: options.manual === true,
        developerIntent: options.developerIntent === true,
        allowRendererAssembly: options.allowRendererAssembly === true,
        hasRendererShell: options.hasRendererShell === true,
        hasRendererContainer: options.hasRendererContainer === true,
        hasLayerHost: options.hasLayerHost === true,
        hasRenderLayerRegistry: options.hasRenderLayerRegistry === true,
        hasPreparedLayerStack: options.hasPreparedLayerStack === true,
        preserveGameplayOverlays: options.preserveGameplayOverlays === true,
        preserveOSMBehavior: options.preserveOSMBehavior === true
      }
    },
    contractAvailable:
      typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
    shellHelperAvailable:
      typeof assembleCustom25DVisualRendererManual === "function",
    readyForRealAssembly: false,
    readyForManualShellOnly: guard.allowed === true,
    blockedByDefault: guard.allowed !== true,
    requiredManualInputs: [
      "manual: true",
      "developerIntent: true",
      "allowRendererAssembly: true",
      "hasRendererShell: true",
      "hasRendererContainer: true",
      "hasLayerHost: true",
      "hasRenderLayerRegistry: true",
      "hasPreparedLayerStack: true",
      "preserveGameplayOverlays: true",
      "preserveOSMBehavior: true"
    ],
    confirmedNonActions: [
      "does not assemble renderer",
      "does not create renderer shell/container/host",
      "does not initialize render layer registry or layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners"
    ],
    nextRecommendedPhase: "phase-97-manual-renderer-assembly-shell-report-alignment",
    safetyNotes: [
      "Default path remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "This helper is report-only and does not attempt assembly.",
      "Normal blue pins, player marker, capture radius, OSM labels, and OSM behavior remain preserved."
    ],
    passiveReports: {
      guard,
      resultContract: passiveResultContract
    }
  };
}

function getCustom25DVisualManualRendererAssemblyDryRunReport(options = {}) {
  const guard = canAssembleCustom25DVisualRenderer(options);
  const passiveResultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };
  const shellResult =
    typeof assembleCustom25DVisualRendererManual === "function"
      ? assembleCustom25DVisualRendererManual(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly shell helper is unavailable."
        };
  const readinessReport =
    typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function"
      ? getCustom25DVisualManualRendererAssemblyReadinessReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly readiness report helper is unavailable."
        };

  return {
    ok: true,
    phase: 97,
    name: "custom-25d-visual-manual-renderer-assembly-dry-run-report",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    dryRun: true,
    guard: {
      allowed: guard.allowed,
      reason: guard.reason,
      reasons: guard.reasons,
      checks: {
        custom25DMapEnabled: ENABLE_CUSTOM_25D_MAP === true,
        manual: options.manual === true,
        developerIntent: options.developerIntent === true,
        allowRendererAssembly: options.allowRendererAssembly === true,
        hasRendererShell: options.hasRendererShell === true,
        hasRendererContainer: options.hasRendererContainer === true,
        hasLayerHost: options.hasLayerHost === true,
        hasRenderLayerRegistry: options.hasRenderLayerRegistry === true,
        hasPreparedLayerStack: options.hasPreparedLayerStack === true,
        preserveGameplayOverlays: options.preserveGameplayOverlays === true,
        preserveOSMBehavior: options.preserveOSMBehavior === true
      }
    },
    contractAvailable:
      typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
    readinessReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
    shellResult,
    wouldAssemblyBeAllowed: guard.allowed === true,
    assembled: false,
    rendererCreated: false,
    shellCreated: false,
    containerCreated: false,
    layerHostCreated: false,
    registryInitialized: false,
    layersInitialized: false,
    startupWired: false,
    visibleGraphicsCreated: false,
    blockedByDefault: guard.allowed !== true,
    confirmedNonActions: [
      "does not assemble renderer",
      "does not create renderer shell/container/host",
      "does not initialize render layer registry or layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners"
    ],
    safetyNotes: [
      "Dry run only; assembled always remains false in this phase.",
      "Shell helper is read-only in practice and does not perform real assembly.",
      "Normal blue pins, player marker, capture radius, OSM labels, and OSM behavior remain preserved."
    ],
    nextRecommendedPhase: "phase-98-manual-renderer-assembly-shell-contract-alignment",
    passiveReports: {
      resultContract: passiveResultContract,
      readinessReport
    }
  };
}

function getCustom25DVisualManualRendererAssemblySafetyAudit(options = {}) {
  const guard = canAssembleCustom25DVisualRenderer(options);
  const readinessReport =
    typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function"
      ? getCustom25DVisualManualRendererAssemblyReadinessReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly readiness report helper is unavailable."
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function"
      ? getCustom25DVisualManualRendererAssemblyDryRunReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly dry-run report helper is unavailable."
        };

  return {
    ok: true,
    phase: 98,
    name: "custom-25d-visual-manual-renderer-assembly-safety-audit",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    auditOnly: true,
    guard,
    contractAvailable:
      typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
    shellHelperAvailable:
      typeof assembleCustom25DVisualRendererManual === "function",
    readinessReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
    dryRunReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function",
    blockedByDefault: guard.allowed !== true,
    realAssemblyAllowedNow: false,
    realAssemblyPerformed: false,
    dryRunOnly: true,
    safetyStatus: {
      rendererAssembled: false,
      rendererShellCreated: false,
      rendererContainerCreated: false,
      layerHostCreated: false,
      renderLayerRegistryInitialized: false,
      visualLayersInitialized: false,
      startupWired: false,
      visibleGraphicsCreated: false
    },
    confirmedNonActions: [
      "does not assemble renderer",
      "does not create renderer shell/container/host",
      "does not initialize render layer registry or layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners"
    ],
    preservedSystems: {
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      osmLabels: true
    },
    dataLoadingStatus: {
      realPoiDataLoaded: false,
      dinosaurSiteDataLoaded: false,
      filmLocationDataLoaded: false,
      realRoadGeometryLoaded: false,
      realBuildingGeometryLoaded: false
    },
    nextRecommendedPhase: "phase-99-manual-renderer-assembly-audit-summary-alignment",
    safetyNotes: [
      "Default state remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "Real assembly is not allowed or performed in this phase.",
      "Dry-run reporting remains the highest active level of the manual assembly path."
    ],
    passiveReports: {
      readinessReport,
      dryRunReport
    }
  };
}

function getCustom25DVisualManualRendererAssemblyFinalReviewReport(options = {}) {
  const guard = canAssembleCustom25DVisualRenderer(options);
  const readinessReport =
    typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function"
      ? getCustom25DVisualManualRendererAssemblyReadinessReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly readiness report helper is unavailable."
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function"
      ? getCustom25DVisualManualRendererAssemblyDryRunReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly dry-run report helper is unavailable."
        };
  const safetyAudit =
    typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function"
      ? getCustom25DVisualManualRendererAssemblySafetyAudit(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly safety audit helper is unavailable."
        };

  return {
    ok: true,
    phase: 99,
    name: "custom-25d-visual-manual-renderer-assembly-final-review-report",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    finalReviewOnly: true,
    guard,
    contractAvailable:
      typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
    shellHelperAvailable:
      typeof assembleCustom25DVisualRendererManual === "function",
    readinessReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
    dryRunReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function",
    safetyAuditAvailable:
      typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function",
    blockedByDefault: guard.allowed !== true,
    manualAssemblyPathStatus: {
      guardEvaluatorExists: typeof canAssembleCustom25DVisualRenderer === "function",
      resultContractExists:
        typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
      shellHelperExists: typeof assembleCustom25DVisualRendererManual === "function",
      readinessReportExists:
        typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
      dryRunReportExists:
        typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function",
      safetyAuditExists:
        typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function",
      currentDefaultStateBlocked: guard.allowed !== true,
      dryRunOnlyPathPreserved: true
    },
    realAssemblyPerformed: false,
    readyForFutureRealAssemblyPhase: false,
    confirmedNonActions: [
      "does not assemble renderer",
      "does not create renderer shell/container/host",
      "does not initialize render layer registry or layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners"
    ],
    preservedSystems: {
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      osmLabels: true
    },
    dataLoadingStatus: {
      sampleDataLoaded: false,
      dinosaurSiteDataLoaded: false,
      filmLocationDataLoaded: false,
      realPoiDataLoaded: false,
      realRoadGeometryLoaded: false,
      realBuildingGeometryLoaded: false
    },
    phaseChainSummary: {
      phase93GuardEvaluator: true,
      phase94ResultContract: true,
      phase95ShellHelper: true,
      phase96ReadinessReport: true,
      phase97DryRunReport: true,
      phase98SafetyAudit: true
    },
    nextRecommendedPhase: "phase-100-manual-renderer-assembly-real-phase-boundary-review",
    safetyNotes: [
      "Default state remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "No real assembly has been performed anywhere in the current manual assembly path.",
      "Dry-run-only and audit-only reporting remains intact before any future real assembly phase is considered."
    ],
    passiveReports: {
      readinessReport,
      dryRunReport,
      safetyAudit
    }
  };
}

function getCustom25DVisualManualRendererAssemblyHandoffPlan(options = {}) {
  const guard =
    typeof canAssembleCustom25DVisualRenderer === "function"
      ? canAssembleCustom25DVisualRenderer(options)
      : {
          ok: false,
          allowed: false,
          reason: "guard-evaluator-unavailable",
          reasons: ["guard-evaluator-unavailable"]
        };
  const resultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };
  const readinessReport =
    typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function"
      ? getCustom25DVisualManualRendererAssemblyReadinessReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly readiness report helper is unavailable."
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function"
      ? getCustom25DVisualManualRendererAssemblyDryRunReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly dry-run report helper is unavailable."
        };
  const safetyAudit =
    typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function"
      ? getCustom25DVisualManualRendererAssemblySafetyAudit(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly safety audit helper is unavailable."
        };
  const finalReviewReport =
    typeof getCustom25DVisualManualRendererAssemblyFinalReviewReport === "function"
      ? getCustom25DVisualManualRendererAssemblyFinalReviewReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly final review report helper is unavailable."
        };

  return {
    ok: true,
    phase: 100,
    name: "custom-25d-visual-manual-renderer-assembly-handoff-plan",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    handoffOnly: true,
    guard,
    contractAvailable:
      typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
    shellHelperAvailable:
      typeof assembleCustom25DVisualRendererManual === "function",
    readinessReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
    dryRunReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function",
    safetyAuditAvailable:
      typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function",
    finalReviewReportAvailable:
      typeof getCustom25DVisualManualRendererAssemblyFinalReviewReport === "function",
    blockedByDefault: guard.allowed !== true,
    realAssemblyPerformed: false,
    futureRealAssemblyStillBlockedByDefault: true,
    currentSafeState: {
      defaultStateBlocked: guard.allowed !== true,
      realAssemblyStarted: false,
      rendererShellCreatedThisPhase: false,
      rendererContainerCreatedThisPhase: false,
      layerHostCreatedThisPhase: false,
      renderLayerRegistryInitializedThisPhase: false,
      visualLayersInitializedThisPhase: false,
      startupWiringAddedThisPhase: false,
      visibleGraphicsCreatedThisPhase: false
    },
    futureAssemblyEntryCriteria: [
      "manual-only invocation",
      "guard evaluator must explicitly allow the attempt",
      "future work must stay shell/container/host-only unless a later phase explicitly expands scope",
      "future work must preserve gameplay overlays, normal blue pins, player marker, capture radius, and OSM behavior",
      "future work must remain blocked from startup wiring and visible rendering unless explicitly requested"
    ],
    futureAssemblyHardLimits: [
      "do not auto-enable ENABLE_CUSTOM_25D_MAP",
      "do not assemble on startup",
      "do not create visible graphics by default",
      "do not load sample, dinosaur, film, or real POI data",
      "do not load real road geometry",
      "do not load real building geometry",
      "do not change gameplay, backend, rewards, or collections"
    ],
    confirmedNonActions: [
      "does not assemble renderer",
      "does not create renderer shell/container/host",
      "does not initialize render layer registry or visual layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners",
      "does not touch pins, player marker, capture radius, or OSM labels"
    ],
    preservedSystems: {
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      osmLabels: true,
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true
    },
    dataLoadingStatus: {
      sampleDataLoaded: false,
      dinosaurSiteDataLoaded: false,
      filmLocationDataLoaded: false,
      realPoiDataLoaded: false,
      realRoadGeometryLoaded: false,
      realBuildingGeometryLoaded: false
    },
    phaseChainSummary: {
      phase93GuardEvaluator: typeof canAssembleCustom25DVisualRenderer === "function",
      phase94ResultContract:
        typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
      phase95ManualShellHelper:
        typeof assembleCustom25DVisualRendererManual === "function",
      phase96ReadinessReport:
        typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
      phase97DryRunReport:
        typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function",
      phase98SafetyAudit:
        typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function",
      phase99FinalReviewReport:
        typeof getCustom25DVisualManualRendererAssemblyFinalReviewReport === "function"
    },
    nextRecommendedPhase: "phase-101-manual-renderer-assembly-real-phase-boundary-shell",
    safetyNotes: [
      "This helper is handoff-only and report-only.",
      "Default state remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "No real assembly has been performed in this phase.",
      "Any future real assembly work must stay manual-only and explicitly guarded."
    ],
    passiveReports: {
      resultContract,
      readinessReport,
      dryRunReport,
      safetyAudit,
      finalReviewReport
    }
  };
}

function getCustom25DVisualManualRendererStructuralAssemblyVerificationReport(options = {}) {
  const requestedGuard =
    typeof canAssembleCustom25DVisualRenderer === "function"
      ? canAssembleCustom25DVisualRenderer(options)
      : {
          ok: false,
          allowed: false,
          reason: "guard-evaluator-unavailable",
          reasons: ["guard-evaluator-unavailable"]
        };
  const safeBlockedOptions = {};
  const resultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };
  const defaultAssemblyResult =
    typeof assembleCustom25DVisualRendererManual === "function"
      ? assembleCustom25DVisualRendererManual(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly helper is unavailable."
        };
  const readinessReport =
    typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function"
      ? getCustom25DVisualManualRendererAssemblyReadinessReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly readiness report helper is unavailable."
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function"
      ? getCustom25DVisualManualRendererAssemblyDryRunReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly dry-run report helper is unavailable."
        };
  const safetyAudit =
    typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function"
      ? getCustom25DVisualManualRendererAssemblySafetyAudit(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly safety audit helper is unavailable."
        };
  const finalReviewReport =
    typeof getCustom25DVisualManualRendererAssemblyFinalReviewReport === "function"
      ? getCustom25DVisualManualRendererAssemblyFinalReviewReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly final review report helper is unavailable."
        };
  const handoffPlan =
    typeof getCustom25DVisualManualRendererAssemblyHandoffPlan === "function"
      ? getCustom25DVisualManualRendererAssemblyHandoffPlan(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly handoff plan helper is unavailable."
        };

  return {
    ok: true,
    phase: 102,
    name: "custom-25d-visual-manual-renderer-structural-assembly-verification-report",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    verificationOnly: true,
    guard: requestedGuard,
    assemblyHelperAvailable:
      typeof assembleCustom25DVisualRendererManual === "function",
    defaultAssemblyResult,
    defaultBlocked:
      defaultAssemblyResult &&
      defaultAssemblyResult.allowed === false &&
      defaultAssemblyResult.blocked === true,
    structuralAssemblyAllowedScope: [
      "renderer shell only",
      "renderer container only",
      "layer host only"
    ],
    forbiddenActions: [
      "render layer registry initialization",
      "visual layer initialization",
      "canvas drawing",
      "visible custom map layer creation",
      "startup wiring",
      "UI or debug panel creation",
      "event listener creation",
      "sample, dinosaur, film, POI, road, or building data loading"
    ],
    confirmedNonActions: [
      "does not call the assembly helper with guard-enabling options",
      "does not create or confirm new DOM elements",
      "does not create renderer shell/container/host from this verification helper",
      "does not initialize render layer registry or visual layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners",
      "does not touch pins, player marker, capture radius, or OSM labels"
    ],
    preservedSystems: {
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      osmLabels: true,
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true
    },
    dataLoadingStatus: {
      sampleDataLoaded: false,
      dinosaurSiteDataLoaded: false,
      filmLocationDataLoaded: false,
      realPoiDataLoaded: false,
      realRoadGeometryLoaded: false,
      realBuildingGeometryLoaded: false
    },
    nextRecommendedPhase: "phase-103-manual-renderer-structural-assembly-readiness-boundary",
    safetyNotes: [
      "This helper is verification-only and remains on a blocked default path.",
      "Any passive assembly reference is evaluated with empty options so no structural creation occurs here.",
      "Structural assembly remains manual-only, guard-controlled, and limited to shell/container/host even outside this report helper."
    ],
    passiveReports: {
      resultContract,
      readinessReport,
      dryRunReport,
      safetyAudit,
      finalReviewReport,
      handoffPlan
    }
  };
}

function getCustom25DVisualManualRendererStructuralAssemblyCloseoutReport(options = {}) {
  const requestedGuard =
    typeof canAssembleCustom25DVisualRenderer === "function"
      ? canAssembleCustom25DVisualRenderer(options)
      : {
          ok: false,
          allowed: false,
          reason: "guard-evaluator-unavailable",
          reasons: ["guard-evaluator-unavailable"]
        };
  const safeBlockedOptions = {};
  const resultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };
  const verificationReport =
    typeof getCustom25DVisualManualRendererStructuralAssemblyVerificationReport === "function"
      ? getCustom25DVisualManualRendererStructuralAssemblyVerificationReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Structural assembly verification report helper is unavailable."
        };
  const defaultAssemblyResult =
    typeof assembleCustom25DVisualRendererManual === "function"
      ? assembleCustom25DVisualRendererManual(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly helper is unavailable."
        };
  const readinessReport =
    typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function"
      ? getCustom25DVisualManualRendererAssemblyReadinessReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly readiness report helper is unavailable."
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function"
      ? getCustom25DVisualManualRendererAssemblyDryRunReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly dry-run report helper is unavailable."
        };
  const safetyAudit =
    typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function"
      ? getCustom25DVisualManualRendererAssemblySafetyAudit(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly safety audit helper is unavailable."
        };
  const finalReviewReport =
    typeof getCustom25DVisualManualRendererAssemblyFinalReviewReport === "function"
      ? getCustom25DVisualManualRendererAssemblyFinalReviewReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly final review report helper is unavailable."
        };
  const handoffPlan =
    typeof getCustom25DVisualManualRendererAssemblyHandoffPlan === "function"
      ? getCustom25DVisualManualRendererAssemblyHandoffPlan(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly handoff plan helper is unavailable."
        };

  return {
    ok: true,
    phase: 103,
    name: "custom-25d-visual-manual-renderer-structural-assembly-closeout",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    closeoutOnly: true,
    guard: requestedGuard,
    structuralAssemblyHelperAvailable:
      typeof assembleCustom25DVisualRendererManual === "function",
    structuralVerificationReportAvailable:
      typeof getCustom25DVisualManualRendererStructuralAssemblyVerificationReport === "function",
    defaultAssemblyBlocked:
      defaultAssemblyResult &&
      defaultAssemblyResult.allowed === false &&
      defaultAssemblyResult.blocked === true,
    structuralScopeClosedOut: true,
    allowedStructuralScope: [
      "renderer shell only",
      "renderer container only",
      "layer host only"
    ],
    forbiddenActions: [
      "render layer registry initialization",
      "visual layer initialization",
      "canvas drawing",
      "visible custom map layer creation",
      "startup wiring",
      "UI or debug panel creation",
      "event listener creation",
      "sample, dinosaur, film, POI, road, or building data loading"
    ],
    confirmedNonActions: [
      "does not call the assembly helper with guard-enabling options",
      "does not create or confirm new DOM elements",
      "does not create renderer shell/container/host from this closeout helper",
      "does not initialize render layer registry or visual layers",
      "does not draw content or create visible map layers",
      "does not wire startup",
      "does not mutate state",
      "does not add UI or event listeners",
      "does not touch pins, player marker, capture radius, or OSM labels"
    ],
    preservedSystems: {
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true,
      osmLabels: true,
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true
    },
    dataLoadingStatus: {
      sampleDataLoaded: false,
      dinosaurSiteDataLoaded: false,
      filmLocationDataLoaded: false,
      realPoiDataLoaded: false,
      realRoadGeometryLoaded: false,
      realBuildingGeometryLoaded: false
    },
    phaseChainSummary: {
      phase93GuardEvaluator: typeof canAssembleCustom25DVisualRenderer === "function",
      phase94ResultContract:
        typeof getCustom25DVisualManualRendererAssemblyResultContract === "function",
      phase101StructuralAssemblyHelper:
        typeof assembleCustom25DVisualRendererManual === "function",
      phase102VerificationReport:
        typeof getCustom25DVisualManualRendererStructuralAssemblyVerificationReport === "function",
      phase96ReadinessReport:
        typeof getCustom25DVisualManualRendererAssemblyReadinessReport === "function",
      phase97DryRunReport:
        typeof getCustom25DVisualManualRendererAssemblyDryRunReport === "function",
      phase98SafetyAudit:
        typeof getCustom25DVisualManualRendererAssemblySafetyAudit === "function",
      phase99FinalReviewReport:
        typeof getCustom25DVisualManualRendererAssemblyFinalReviewReport === "function",
      phase100HandoffPlan:
        typeof getCustom25DVisualManualRendererAssemblyHandoffPlan === "function"
    },
    nextRecommendedPhase: "phase-104-manual-renderer-registry-planning-contract-only",
    safetyNotes: [
      "This helper is closeout-only and remains on a blocked default path.",
      "Structural-only assembly from Phases 101–102 is now summarized as closed out and ready for registry planning.",
      "No registry, layers, startup wiring, visible graphics, data loading, or gameplay changes are performed here."
    ],
    passiveReports: {
      resultContract,
      verificationReport,
      defaultAssemblyResult,
      readinessReport,
      dryRunReport,
      safetyAudit,
      finalReviewReport,
      handoffPlan
    }
  };
}

function getCustom25DVisualManualRendererRegistryReadinessPlan(options = {}) {
  const requestedGuard =
    typeof canAssembleCustom25DVisualRenderer === "function"
      ? canAssembleCustom25DVisualRenderer(options)
      : {
          ok: false,
          allowed: false,
          reason: "guard-evaluator-unavailable",
          reasons: ["guard-evaluator-unavailable"]
        };
  const safeBlockedOptions = {};
  const structuralCloseout =
    typeof getCustom25DVisualManualRendererStructuralAssemblyCloseoutReport === "function"
      ? getCustom25DVisualManualRendererStructuralAssemblyCloseoutReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Structural assembly closeout helper is unavailable."
        };
  const resultContract =
    typeof getCustom25DVisualManualRendererAssemblyResultContract === "function"
      ? getCustom25DVisualManualRendererAssemblyResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer assembly result contract helper is unavailable."
        };

  return {
    ok: true,
    phase: 104,
    name: "custom-25d-visual-manual-renderer-registry-readiness-plan",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    planningOnly: true,
    guard: requestedGuard,
    structuralCloseoutAvailable:
      typeof getCustom25DVisualManualRendererStructuralAssemblyCloseoutReport === "function",
    registryInitializationBlockedByDefault: true,
    registryInitializationStarted: false,
    structuralPhaseClosedOut:
      structuralCloseout &&
      structuralCloseout.structuralScopeClosedOut === true,
    requiredFutureManualInputs: [
      "manual: true",
      "developerIntent: true",
      "allowRendererAssembly: true",
      "hasRendererShell: true",
      "hasRendererContainer: true",
      "hasLayerHost: true",
      "allowRenderLayerRegistry: true"
    ],
    requiredStructuralPrerequisites: [
      "renderer shell path exists",
      "renderer container path exists",
      "layer host path exists",
      "structural assembly closeout is complete",
      "registry work remains manual-only and guard-controlled"
    ],
    registryOnlyAllowedFutureScope: [
      "create or confirm inert render layer registry only",
      "report registry metadata and layer count only",
      "keep visual layers uninitialized"
    ],
    forbiddenFutureActions: [
      "visual layer initialization",
      "canvas drawing",
      "visible custom map layer creation",
      "startup wiring",
      "UI or debug panel creation",
      "event listener creation",
      "sample, dinosaur, film, POI, road, or building data loading",
      "gameplay, pin, player marker, capture radius, or OSM changes"
    ],
    blockedByDefault: true,
    nextRecommendedPhase: "phase-105-manual-renderer-registry-guard-shell",
    safetyNotes: [
      "This helper is planning-only and does not initialize the registry.",
      "Structural shell/container/host work is treated as closed out before registry planning begins.",
      "Registry work remains blocked by default and must stay isolated from layers, drawing, startup, and gameplay."
    ],
    passiveReports: {
      structuralCloseout,
      assemblyResultContract: resultContract
    }
  };
}

function getCustom25DVisualManualRendererRegistryResultContract() {
  return {
    ok: true,
    phase: 104,
    name: "custom-25d-visual-manual-renderer-registry-result-contract",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    contractOnly: true,
    futureRegistryInitializationAction: "manual-renderer-registry-initialization",
    registryInitializationBlockedByDefault: true,
    futureResultShape: {
      ok: "boolean",
      attempted: "boolean",
      allowed: "boolean",
      blocked: "boolean",
      registryInitialized: "boolean",
      registryCreated: "boolean",
      layerCount: "number",
      layersInitialized: false,
      startupWired: false,
      visibleGraphicsCreated: false,
      dataLoaded: false,
      gameplayChanged: false,
      reason: "string|null",
      safetyNotes: "string[]"
    },
    futureBlockedResultExample: {
      ok: true,
      attempted: true,
      allowed: false,
      blocked: true,
      registryInitialized: false,
      registryCreated: false,
      layerCount: 0,
      layersInitialized: false,
      startupWired: false,
      visibleGraphicsCreated: false,
      dataLoaded: false,
      gameplayChanged: false,
      reason: "custom-25d-map-disabled",
      safetyNotes: [
        "Registry initialization remains blocked by default.",
        "No layers, drawing, startup wiring, data loading, or gameplay changes occurred."
      ]
    },
    futureAllowedRegistryOnlyResultExample: {
      ok: true,
      attempted: true,
      allowed: true,
      blocked: false,
      registryInitialized: true,
      registryCreated: true,
      layerCount: 0,
      layersInitialized: false,
      startupWired: false,
      visibleGraphicsCreated: false,
      dataLoaded: false,
      gameplayChanged: false,
      reason: "registry-only",
      safetyNotes: [
        "Only the inert registry boundary was initialized.",
        "No layers, drawing, startup wiring, data loading, or gameplay changes occurred."
      ]
    },
    contractRules: [
      "must not report layersInitialized true in the registry-only phase",
      "must not report startupWired true in the registry-only phase",
      "must not report visibleGraphicsCreated true in the registry-only phase",
      "must not report dataLoaded true in the registry-only phase",
      "must not report gameplayChanged true in the registry-only phase",
      "must not imply pins, player marker, capture radius, or OSM behavior changed"
    ],
    safetyNotes: [
      "Contract-only helper.",
      "Does not initialize the registry.",
      "Does not initialize layers, draw content, wire startup, or load data.",
      "Keeps future registry work constrained to a manual-only dormant path."
    ]
  };
}

function canInitializeCustom25DVisualManualRendererRegistry(options = {}) {
  const reasons = [];

  if (ENABLE_CUSTOM_25D_MAP !== true) reasons.push("custom-25d-map-disabled");
  if (options.manual !== true) reasons.push("manual-option-required");
  if (options.developerIntent !== true) reasons.push("developer-intent-required");
  if (options.allowRegistryInitialization !== true) {
    reasons.push("registry-initialization-not-allowed");
  }
  if (options.structuralAssemblyOnlyComplete !== true) {
    reasons.push("structural-assembly-closeout-required");
  }
  if (options.preserveGameplayOverlays !== true) {
    reasons.push("gameplay-overlay-preservation-required");
  }
  if (options.preserveOSMBehavior !== true) {
    reasons.push("osm-behavior-preservation-required");
  }

  return {
    ok: true,
    phase: 105,
    guard: "manual-renderer-registry-initialization",
    allowed: reasons.length === 0,
    blocked: reasons.length > 0,
    reason: reasons.length > 0 ? reasons[0] : null,
    reasons,
    dormant: true,
    guarded: true,
    manualOnly: true,
    developerOnly: true,
    mutatesState: false,
    initializesRegistry: false,
    initializesLayers: false,
    drawsContent: false,
    startupWiringAdded: false,
    visualBehaviorChanged: false,
    preservesNormalBluePins: true,
    preservesPlayerMarker: true,
    preservesCaptureRadius: true,
    preservesOSMBehavior: true,
    preservesOSMLabels: true
  };
}

function initializeCustom25DVisualManualRendererRegistry(options = {}) {
  const guard = canInitializeCustom25DVisualManualRendererRegistry(options);
  const readinessPlan =
    typeof getCustom25DVisualManualRendererRegistryReadinessPlan === "function"
      ? getCustom25DVisualManualRendererRegistryReadinessPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry readiness plan helper is unavailable."
        };
  const resultContract =
    typeof getCustom25DVisualManualRendererRegistryResultContract === "function"
      ? getCustom25DVisualManualRendererRegistryResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry result contract helper is unavailable."
        };

  if (!guard.allowed) {
    return {
      ok: true,
      attempted: true,
      allowed: false,
      blocked: true,
      phase: 105,
      action: "manual-renderer-registry-initialization",
      registryInitialized: false,
      registryCreated: false,
      layerCount: 0,
      layersInitialized: false,
      startupWired: false,
      visibleGraphicsCreated: false,
      dataLoaded: false,
      gameplayChanged: false,
      reason: guard.reason,
      reasons: guard.reasons,
      safetyNotes: [
        "Registry initialization remains blocked by default.",
        "No registry, layers, drawing, startup wiring, data loading, or gameplay changes occurred."
      ],
      passiveReports: {
        guard,
        readinessPlan,
        resultContract
      }
    };
  }

  const registryShell =
    typeof initializeCustom25DVisualRenderLayerRegistry === "function"
      ? initializeCustom25DVisualRenderLayerRegistry({
          manual: true,
          developerIntent: true,
          allowRenderLayerRegistry: true,
          hasLayerHost: true
        })
      : {
          ok: false,
          initialized: false,
          blocked: true,
          blockedReasons: ["render-layer-registry-helper-unavailable"],
          layerRegistry: []
        };
  const registryCreated = registryShell.ok === true && registryShell.blocked === false;
  const layerCount = Array.isArray(registryShell.layerRegistry)
    ? registryShell.layerRegistry.length
    : 0;

  return {
    ok: true,
    attempted: true,
    allowed: true,
    blocked: false,
    phase: 107,
    action: "manual-renderer-registry-initialization",
    registryInitialized: registryCreated,
    registryCreated,
    layerCount,
    layersInitialized: false,
    startupWired: false,
    visibleGraphicsCreated: false,
    dataLoaded: false,
    gameplayChanged: false,
    reason: "registry-initialized-inert-only",
    reasons: ["registry-initialized-inert-only"],
    safetyNotes: [
      "Guard passed and only the inert render-layer registry boundary was created or confirmed.",
      "No layers, drawing, startup wiring, data loading, or gameplay changes occurred."
    ],
    passiveReports: {
      guard,
      readinessPlan,
      resultContract,
      registryShell
    }
  };
}

function getCustom25DVisualManualRendererRegistryDryRunReport(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualManualRendererRegistry === "function"
      ? canInitializeCustom25DVisualManualRendererRegistry(options)
      : {
          ok: false,
          allowed: false,
          blocked: true,
          reason: "registry-guard-unavailable",
          reasons: ["registry-guard-unavailable"]
        };
  const readinessPlan =
    typeof getCustom25DVisualManualRendererRegistryReadinessPlan === "function"
      ? getCustom25DVisualManualRendererRegistryReadinessPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry readiness plan helper is unavailable."
        };
  const resultContract =
    typeof getCustom25DVisualManualRendererRegistryResultContract === "function"
      ? getCustom25DVisualManualRendererRegistryResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry result contract helper is unavailable."
        };
  const shellResult =
    typeof initializeCustom25DVisualManualRendererRegistry === "function"
      ? initializeCustom25DVisualManualRendererRegistry(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry shell helper is unavailable."
        };

  return {
    ok: true,
    phase: 106,
    name: "custom-25d-visual-manual-renderer-registry-dry-run-report",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    dryRun: true,
    guard,
    shellResult,
    blockedByDefault: shellResult.allowed !== true,
    attempted: true,
    allowed: shellResult.allowed === true,
    blocked: shellResult.blocked !== false,
    registryInitialized: false,
    registryCreated: false,
    layerCount: 0,
    layersInitialized: false,
    startupWired: false,
    visibleGraphicsCreated: false,
    dataLoaded: false,
    gameplayChanged: false,
    reason: shellResult.reason || guard.reason,
    safetyNotes: [
      "Dry-run only; no real registry initialization occurs in this phase.",
      "Even with explicit caller options, the registry helper remains shell-only.",
      "No layers, drawing, startup wiring, data loading, or gameplay changes occur."
    ],
    passiveReports: {
      readinessPlan,
      resultContract
    }
  };
}

function getCustom25DVisualManualRendererRegistrySafetyReview(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualManualRendererRegistry === "function"
      ? canInitializeCustom25DVisualManualRendererRegistry(options)
      : {
          ok: false,
          allowed: false,
          blocked: true,
          reason: "registry-guard-unavailable",
          reasons: ["registry-guard-unavailable"]
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererRegistryDryRunReport === "function"
      ? getCustom25DVisualManualRendererRegistryDryRunReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry dry-run report helper is unavailable."
        };

  return {
    ok: true,
    phase: 106,
    name: "custom-25d-visual-manual-renderer-registry-safety-review",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    reviewOnly: true,
    guardExists:
      typeof canInitializeCustom25DVisualManualRendererRegistry === "function",
    readinessPlanExists:
      typeof getCustom25DVisualManualRendererRegistryReadinessPlan === "function",
    resultContractExists:
      typeof getCustom25DVisualManualRendererRegistryResultContract === "function",
    shellHelperExists:
      typeof initializeCustom25DVisualManualRendererRegistry === "function",
    dryRunReportExists:
      typeof getCustom25DVisualManualRendererRegistryDryRunReport === "function",
    defaultRegistryPathBlocked: guard.allowed !== true,
    noRealRegistryInitializationHappened:
      dryRunReport.registryInitialized === false &&
      dryRunReport.registryCreated === false,
    noBehaviorChangesOccurred: {
      layersInitialized: false,
      startupWired: false,
      visibleGraphicsCreated: false,
      dataLoaded: false,
      gameplayChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      osmBehaviorChanged: false
    },
    nextRecommendedPhase: "phase-107-manual-guarded-registry-initialization-only",
    safetyNotes: [
      "Registry path remains dry-run and shell-only at this phase.",
      "No real registry initialization, layer initialization, startup wiring, or visible graphics occurred.",
      "Pins, player marker, capture radius, OSM behavior, gameplay, backend, rewards, and collections remain unchanged."
    ],
    passiveReports: {
      guard,
      dryRunReport
    }
  };
}

function getCustom25DVisualManualRendererRegistryInitializationCloseoutReport(options = {}) {
  const requestedGuard =
    typeof canInitializeCustom25DVisualManualRendererRegistry === "function"
      ? canInitializeCustom25DVisualManualRendererRegistry(options)
      : {
          ok: false,
          allowed: false,
          blocked: true,
          reason: "registry-guard-unavailable",
          reasons: ["registry-guard-unavailable"]
        };
  const safeBlockedOptions = {};
  const readinessPlan =
    typeof getCustom25DVisualManualRendererRegistryReadinessPlan === "function"
      ? getCustom25DVisualManualRendererRegistryReadinessPlan(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry readiness plan helper is unavailable."
        };
  const resultContract =
    typeof getCustom25DVisualManualRendererRegistryResultContract === "function"
      ? getCustom25DVisualManualRendererRegistryResultContract()
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry result contract helper is unavailable."
        };
  const dryRunReport =
    typeof getCustom25DVisualManualRendererRegistryDryRunReport === "function"
      ? getCustom25DVisualManualRendererRegistryDryRunReport(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry dry-run report helper is unavailable."
        };
  const safetyReview =
    typeof getCustom25DVisualManualRendererRegistrySafetyReview === "function"
      ? getCustom25DVisualManualRendererRegistrySafetyReview(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry safety review helper is unavailable."
        };
  const defaultRegistryResult =
    typeof initializeCustom25DVisualManualRendererRegistry === "function"
      ? initializeCustom25DVisualManualRendererRegistry(safeBlockedOptions)
      : {
          ok: false,
          missing: true,
          reason: "Manual renderer registry initialization helper is unavailable."
        };

  return {
    ok: true,
    phase: 108,
    name: "custom-25d-visual-manual-renderer-registry-initialization-closeout-report",
    dormant: true,
    manualOnly: true,
    dataOnly: true,
    reportOnly: true,
    closeoutOnly: true,
    registryGuardExists:
      typeof canInitializeCustom25DVisualManualRendererRegistry === "function",
    registryReadinessPlanExists:
      typeof getCustom25DVisualManualRendererRegistryReadinessPlan === "function",
    registryResultContractExists:
      typeof getCustom25DVisualManualRendererRegistryResultContract === "function",
    registryHelperExists:
      typeof initializeCustom25DVisualManualRendererRegistry === "function",
    registryDryRunExists:
      typeof getCustom25DVisualManualRendererRegistryDryRunReport === "function",
    registrySafetyReviewExists:
      typeof getCustom25DVisualManualRendererRegistrySafetyReview === "function",
    defaultRegistryInitRemainsBlocked:
      defaultRegistryResult &&
      defaultRegistryResult.allowed === false &&
      defaultRegistryResult.blocked === true,
    phase107ManualPathSummary: {
      canInitializeOnlyInertRegistryWhenGuardPassed: true,
      layerCountStatusKnown: true,
      expectedLayerCountOnExplicitPath: 13,
      visualLayersRemainUninitialized: true,
      startupRemainsUnwired: true,
      visibleGraphicsRemainAbsent: true
    },
    preservedSystems: {
      dataLoaded: false,
      gameplayChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      osmBehaviorChanged: false,
      backendChanged: false,
      rewardsChanged: false,
      collectionsChanged: false
    },
    registryInitPhaseClosedOut: true,
    nextRecommendedPhase: "phase-109-visual-layer-initialization-planning-contract-only",
    safetyNotes: [
      "This helper is closeout-only and does not pass guard-enabling options into registry initialization.",
      "Any direct registry helper call in this phase uses empty options only, so the result remains blocked.",
      "No registry, layers, startup wiring, visible graphics, data loading, or gameplay/map behavior changes are performed here."
    ],
    passiveReports: {
      requestedGuard,
      readinessPlan,
      resultContract,
      dryRunReport,
      safetyReview,
      defaultRegistryResult
    }
  };
}

function getCustom25DVisualLayerInitializationPlan(options = {}) {
  const registryCloseout =
    typeof getCustom25DVisualManualRendererRegistryInitializationCloseoutReport === "function"
      ? getCustom25DVisualManualRendererRegistryInitializationCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          reason: "Registry initialization closeout report helper is unavailable."
        };
  const registryReadiness =
    typeof getCustom25DVisualManualRendererRegistryReadinessPlan === "function"
      ? getCustom25DVisualManualRendererRegistryReadinessPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "Registry readiness plan helper is unavailable."
        };

  return {
    ok: true,
    phase: 109,
    name: "custom-25d-visual-layer-initialization-plan",
    dormant: true,
    manualOnly: true,
    planningOnly: true,
    dataOnly: true,
    reportOnly: true,
    requiredPrerequisites: [
      "manual developer-only invocation path remains in place",
      "inert renderer registry must be initialized in a future explicitly guarded phase",
      "registry initialization closeout must remain complete",
      "all feature flags remain false by default"
    ],
    guardRequirements: [
      "manual: true",
      "developerIntent: true",
      "future per-layer allow flags or equivalent explicit approvals",
      "preserveGameplayOverlays: true",
      "preserveOSMBehavior: true"
    ],
    initializationRules: [
      "visual layers must remain invisible and dormant by default",
      "visual layer state must not attach to map or startup in this phase",
      "each layer should initialize independently and safely",
      "future initialization should be reversible and clearable",
      "any failure should block and report without partially enabling visible behavior"
    ],
    protectedSystems: {
      osmBehavior: true,
      osmLabels: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      ui: true
    },
    nextRecommendedPhase: "phase-110-visual-layer-initialization-guard-evaluator-only",
    safetyNotes: [
      "Planning-only helper.",
      "Does not create, initialize, attach, draw, or reveal any visual layer.",
      "Does not call any renderer, registry, or layer initializer."
    ],
    passiveReports: {
      registryCloseout,
      registryReadiness
    }
  };
}

function getCustom25DVisualLayerInitializationResultContract(options = {}) {
  return {
    ok: true,
    phase: 109,
    name: "custom-25d-visual-layer-initialization-result-contract",
    dormant: true,
    manualOnly: true,
    developerIntentRequired: true,
    planningOnly: true,
    contractOnly: true,
    dataOnly: true,
    reportOnly: true,
    mutatesState: false,
    createsVisibleGraphics: false,
    initializesLayers: false,
    attachesToMap: false,
    affectsGameplay: false,
    expectedResultShape: {
      ok: "boolean",
      allowed: "boolean",
      reason: "string|null",
      phase: 109,
      dormant: "boolean",
      manualOnly: "boolean",
      developerIntentRequired: "boolean",
      mutatesState: "boolean",
      createsVisibleGraphics: "boolean",
      initializesLayers: "boolean",
      attachesToMap: "boolean",
      affectsGameplay: "boolean",
      affectedLayers: "string[]",
      skippedLayers: "string[]",
      blockedLayers: "string[]",
      safety: "object",
      nextPhase: "string"
    },
    defaultContractExpectations: {
      affectedLayers: [],
      skippedLayers: [],
      blockedLayers: [],
      safety: {
        startupWired: false,
        visibleGraphicsCreated: false,
        mapAttached: false,
        dataLoaded: false,
        gameplayChanged: false
      },
      nextPhase: "phase-110-visual-layer-initialization-guard-evaluator-only"
    },
    safetyNotes: [
      "Contract-only helper.",
      "Does not perform any initialization or map attachment.",
      "Keeps future layer initialization constrained to a reversible, manual-only path."
    ],
    optionsEcho: {
      manualRequested: options.manual === true,
      developerIntentRequested: options.developerIntent === true
    }
  };
}

function canInitializeCustom25DVisualLayers(options = {}) {
  const checks = {
    custom25DMapEnabled: ENABLE_CUSTOM_25D_MAP === true,
    manual: options.manual === true,
    developerIntent: options.developerIntent === true,
    allowVisualLayerInitialization: options.allowVisualLayerInitialization === true,
    hasRendererShell: options.hasRendererShell === true,
    hasRendererContainer: options.hasRendererContainer === true,
    hasLayerHost: options.hasLayerHost === true,
    hasRenderLayerRegistry: options.hasRenderLayerRegistry === true,
    hasInitializedRendererRegistry: options.hasInitializedRendererRegistry === true,
    preserveGameplayOverlays: options.preserveGameplayOverlays === true,
    preserveOSMBehavior: options.preserveOSMBehavior === true,
    keepLayersInvisible: options.keepLayersInvisible === true,
    preventStartupWiring: options.preventStartupWiring === true,
    preventMapAttachment: options.preventMapAttachment === true
  };

  const missing = [];
  if (!checks.custom25DMapEnabled) missing.push("custom25DMapEnabled");
  if (!checks.manual) missing.push("manual");
  if (!checks.developerIntent) missing.push("developerIntent");
  if (!checks.allowVisualLayerInitialization) missing.push("allowVisualLayerInitialization");
  if (!checks.hasRendererShell) missing.push("hasRendererShell");
  if (!checks.hasRendererContainer) missing.push("hasRendererContainer");
  if (!checks.hasLayerHost) missing.push("hasLayerHost");
  if (!checks.hasRenderLayerRegistry) missing.push("hasRenderLayerRegistry");
  if (!checks.hasInitializedRendererRegistry) missing.push("hasInitializedRendererRegistry");
  if (!checks.preserveGameplayOverlays) missing.push("preserveGameplayOverlays");
  if (!checks.preserveOSMBehavior) missing.push("preserveOSMBehavior");
  if (!checks.keepLayersInvisible) missing.push("keepLayersInvisible");
  if (!checks.preventStartupWiring) missing.push("preventStartupWiring");
  if (!checks.preventMapAttachment) missing.push("preventMapAttachment");

  const reasonMap = {
    custom25DMapEnabled: "custom-25d-map-disabled",
    manual: "manual-option-required",
    developerIntent: "developer-intent-required",
    allowVisualLayerInitialization: "visual-layer-initialization-not-allowed",
    hasRendererShell: "renderer-shell-required",
    hasRendererContainer: "renderer-container-required",
    hasLayerHost: "layer-host-required",
    hasRenderLayerRegistry: "render-layer-registry-required",
    hasInitializedRendererRegistry: "initialized-renderer-registry-required",
    preserveGameplayOverlays: "gameplay-overlay-preservation-required",
    preserveOSMBehavior: "osm-behavior-preservation-required",
    keepLayersInvisible: "keep-layers-invisible-required",
    preventStartupWiring: "prevent-startup-wiring-required",
    preventMapAttachment: "prevent-map-attachment-required"
  };
  const firstMissing = missing[0] || null;

  return {
    allowed: missing.length === 0,
    reason: firstMissing ? reasonMap[firstMissing] : null,
    phase: 110,
    dormant: true,
    manualOnly: true,
    developerIntentRequired: true,
    checks,
    missing,
    safety: {
      mutatesState: false,
      startupWired: false,
      rendererCreated: false,
      registryCreated: false,
      layersInitialized: false,
      visibleGraphicsCreated: false,
      mapAttached: false,
      dataLoaded: false,
      gameplayChanged: false
    },
    nextPhase: "phase-111-visual-layer-initialization-shell-only"
  };
}

function getCustom25DVisualLayerInitializationDryRunReport(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable",
          phase: 111,
          dormant: true,
          manualOnly: true,
          developerIntentRequired: true,
          checks: {},
          missing: ["guard"],
          safety: {
            mutatesState: false,
            startupWired: false,
            rendererCreated: false,
            registryCreated: false,
            layersInitialized: false,
            visibleGraphicsCreated: false,
            mapAttached: false,
            dataLoaded: false,
            gameplayChanged: false
          },
          nextPhase: "phase-112-visual-layer-initialization-safety-review-only"
        };
  const plan =
    typeof getCustom25DVisualLayerInitializationPlan === "function"
      ? getCustom25DVisualLayerInitializationPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization plan helper is unavailable."
        };
  const contract =
    typeof getCustom25DVisualLayerInitializationResultContract === "function"
      ? getCustom25DVisualLayerInitializationResultContract({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization result contract helper is unavailable."
        };

  return {
    ok: true,
    phase: 111,
    name: "custom-25d-visual-layer-initialization-dry-run-report",
    dormant: true,
    dryRunOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    guard,
    plan,
    contract,
    defaultOutcome: {
      blocked: guard.allowed !== true,
      initializesLayers: false,
      attachesToMap: false,
      createsVisibleGraphics: false,
      startupWired: false
    },
    futureExplicitPath: {
      stillRequiresAllGuardConditions: true,
      affectedLayers: [],
      skippedLayers: [],
      blockedLayers: guard.missing || []
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplay: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true
    },
    safety: {
      noInitializerCalled: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noDrawing: true,
      noVisibleGraphics: true,
      noStartupWiring: true,
      noDataLoading: true
    },
    nextPhase: "phase-112-visual-layer-initialization-safety-review-only"
  };
}

function getCustom25DVisualLayerInitializationSafetyReview(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable",
          phase: 112,
          dormant: true,
          manualOnly: true,
          developerIntentRequired: true,
          checks: {},
          missing: ["guard"],
          safety: {
            mutatesState: false,
            startupWired: false,
            rendererCreated: false,
            registryCreated: false,
            layersInitialized: false,
            visibleGraphicsCreated: false,
            mapAttached: false,
            dataLoaded: false,
            gameplayChanged: false
          },
          nextPhase: "phase-113-visual-layer-initialization-closeout-report-only"
        };
  const plan =
    typeof getCustom25DVisualLayerInitializationPlan === "function"
      ? getCustom25DVisualLayerInitializationPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization plan helper is unavailable."
        };
  const contract =
    typeof getCustom25DVisualLayerInitializationResultContract === "function"
      ? getCustom25DVisualLayerInitializationResultContract({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization result contract helper is unavailable."
        };
  const dryRun =
    typeof getCustom25DVisualLayerInitializationDryRunReport === "function"
      ? getCustom25DVisualLayerInitializationDryRunReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization dry-run report helper is unavailable."
        };

  return {
    ok: true,
    phase: 112,
    name: "custom-25d-visual-layer-initialization-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    guard,
    plan,
    contract,
    dryRun,
    defaultOutcome: {
      blocked: guard.allowed !== true,
      safeByDefault: true
    },
    reviewedSafetyBoundaries: {
      noInitializerCalled: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttachedToMap: true,
      noVisibleGraphicsCreated: true,
      noStartupWiring: true
    },
    blockedBehaviors: {
      rendererCreation: true,
      registryCreation: true,
      layerCreation: true,
      layerInitialization: true,
      drawing: true,
      mapAttachment: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      gameplayOverlays: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    futureExplicitPathRequirements: {
      stillRequiresEveryPhase110GuardCondition: true,
      missingConditions: guard.missing || []
    },
    risks: [
      "future explicit layer initialization must remain invisible and map-detached until separately approved",
      "future failures must remain blocked and report-only",
      "future work must not weaken OSM, gameplay, pin, or UI protections"
    ],
    recommendation: "Keep visual layer initialization in safety-review-only mode until a dedicated closeout report is complete.",
    nextPhase: "phase-113-visual-layer-initialization-closeout-report-only"
  };
}

function getCustom25DVisualLayerInitializationCloseoutReport(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable",
          phase: 113,
          dormant: true,
          manualOnly: true,
          developerIntentRequired: true,
          checks: {},
          missing: ["guard"],
          safety: {
            mutatesState: false,
            startupWired: false,
            rendererCreated: false,
            registryCreated: false,
            layersInitialized: false,
            visibleGraphicsCreated: false,
            mapAttached: false,
            dataLoaded: false,
            gameplayChanged: false
          },
          nextPhase: "phase-114-visual-layer-initialization-implementation-shell-only"
        };
  const plan =
    typeof getCustom25DVisualLayerInitializationPlan === "function"
      ? getCustom25DVisualLayerInitializationPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization plan helper is unavailable."
        };
  const contract =
    typeof getCustom25DVisualLayerInitializationResultContract === "function"
      ? getCustom25DVisualLayerInitializationResultContract({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization result contract helper is unavailable."
        };
  const dryRun =
    typeof getCustom25DVisualLayerInitializationDryRunReport === "function"
      ? getCustom25DVisualLayerInitializationDryRunReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization dry-run report helper is unavailable."
        };
  const safetyReview =
    typeof getCustom25DVisualLayerInitializationSafetyReview === "function"
      ? getCustom25DVisualLayerInitializationSafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization safety review helper is unavailable."
        };

  return {
    ok: true,
    phase: 113,
    name: "custom-25d-visual-layer-initialization-closeout-report",
    dormant: true,
    closeoutOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    guard,
    plan,
    contract,
    dryRun,
    safetyReview,
    defaultOutcome: {
      blocked: guard.allowed !== true,
      safeByDefault: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      gameplayOverlays: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      noInitializerCalled: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noLayerDrawn: true,
      noLayerVisible: true,
      noStartupWiring: true,
      sequenceClosedOut: true
    },
    nextPhase: "phase-114-visual-layer-initialization-implementation-shell-only",
    notes: [
      "Closeout-only helper.",
      "Confirms the visual layer initialization planning sequence is complete and still default-safe.",
      "Does not call any initializer or attach any layer to the map."
    ]
  };
}

function initializeCustom25DVisualLayers(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable",
          phase: 114,
          dormant: true,
          manualOnly: true,
          developerIntentRequired: true,
          checks: {},
          missing: ["guard"],
          safety: {
            mutatesState: false,
            startupWired: false,
            rendererCreated: false,
            registryCreated: false,
            layersInitialized: false,
            visibleGraphicsCreated: false,
            mapAttached: false,
            dataLoaded: false,
            gameplayChanged: false
          },
          nextPhase: "phase-115-visual-layer-initialization-verification-report-only"
        };
  const contract =
    typeof getCustom25DVisualLayerInitializationResultContract === "function"
      ? getCustom25DVisualLayerInitializationResultContract({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization result contract helper is unavailable."
        };
  const closeout =
    typeof getCustom25DVisualLayerInitializationCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization closeout report helper is unavailable."
        };
  const blockedLayers = Array.isArray(guard.missing) ? guard.missing.slice() : [];
  const allowed = guard.allowed === true;

  return {
    ok: true,
    allowed,
    reason: allowed ? "implementation-shell-only" : guard.reason || "visual-layer-initialization-blocked",
    phase: 114,
    name: "custom-25d-visual-layer-initialization-implementation-shell",
    dormant: true,
    implementationShellOnly: true,
    initialized: false,
    mutatesState: false,
    createsVisibleGraphics: false,
    attachesToMap: false,
    guard,
    contract,
    closeout,
    affectedLayers: [],
    skippedLayers: [],
    blockedLayers,
    safety: {
      noRendererInitializerCalled: true,
      noRegistryInitializerCalled: true,
      noLayerInitializerCalled: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noVisibleGraphics: true,
      noStartupWiring: true,
      noDataLoading: true,
      noGameplayChanges: true
    },
    nextPhase: "phase-115-visual-layer-initialization-verification-report-only"
  };
}

function getCustom25DVisualLayerInitializationShellVerificationReport(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable",
          phase: 115,
          dormant: true,
          manualOnly: true,
          developerIntentRequired: true,
          checks: {},
          missing: ["guard"],
          safety: {
            mutatesState: false,
            startupWired: false,
            rendererCreated: false,
            registryCreated: false,
            layersInitialized: false,
            visibleGraphicsCreated: false,
            mapAttached: false,
            dataLoaded: false,
            gameplayChanged: false
          },
          nextPhase: "phase-116-visual-layer-initialization-shell-closeout-report-only"
        };
  const plan =
    typeof getCustom25DVisualLayerInitializationPlan === "function"
      ? getCustom25DVisualLayerInitializationPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization plan helper is unavailable."
        };
  const contract =
    typeof getCustom25DVisualLayerInitializationResultContract === "function"
      ? getCustom25DVisualLayerInitializationResultContract({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization result contract helper is unavailable."
        };
  const dryRun =
    typeof getCustom25DVisualLayerInitializationDryRunReport === "function"
      ? getCustom25DVisualLayerInitializationDryRunReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization dry-run report helper is unavailable."
        };
  const safetyReview =
    typeof getCustom25DVisualLayerInitializationSafetyReview === "function"
      ? getCustom25DVisualLayerInitializationSafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization safety review helper is unavailable."
        };
  const closeout =
    typeof getCustom25DVisualLayerInitializationCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization closeout report helper is unavailable."
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false,
          affectedLayers: [],
          skippedLayers: [],
          blockedLayers: ["shell"]
        };

  return {
    ok: true,
    phase: 115,
    name: "custom-25d-visual-layer-initialization-shell-verification-report",
    dormant: true,
    verificationReportOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    guard,
    plan,
    contract,
    dryRun,
    safetyReview,
    closeout,
    shellResult,
    defaultOutcome: {
      blocked: shellResult.allowed !== true,
      reason: shellResult.reason || guard.reason || "visual-layer-initialization-blocked",
      initialized: shellResult.initialized === true,
      mutatesState: shellResult.mutatesState === true,
      createsVisibleGraphics: shellResult.createsVisibleGraphics === true,
      attachesToMap: shellResult.attachesToMap === true
    },
    verifiedSafetyBoundaries: {
      noRendererInitializerCalled: true,
      noRegistryInitializerCalled: true,
      noLayerInitializerCalled: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noLayerDrawn: true,
      noLayerVisible: true,
      noStartupWiring: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the visual layer initialization entry point in shell-only mode until a dedicated shell closeout report is complete.",
    nextPhase: "phase-116-visual-layer-initialization-shell-closeout-report-only"
  };
}

function getCustom25DVisualLayerInitializationShellCloseoutReport(options = {}) {
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable",
          phase: 116,
          dormant: true,
          manualOnly: true,
          developerIntentRequired: true,
          checks: {},
          missing: ["guard"],
          safety: {
            mutatesState: false,
            startupWired: false,
            rendererCreated: false,
            registryCreated: false,
            layersInitialized: false,
            visibleGraphicsCreated: false,
            mapAttached: false,
            dataLoaded: false,
            gameplayChanged: false
          },
          nextPhase: "phase-117-visual-layer-state-inventory-audit-plan-only"
        };
  const plan =
    typeof getCustom25DVisualLayerInitializationPlan === "function"
      ? getCustom25DVisualLayerInitializationPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization plan helper is unavailable."
        };
  const contract =
    typeof getCustom25DVisualLayerInitializationResultContract === "function"
      ? getCustom25DVisualLayerInitializationResultContract({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization result contract helper is unavailable."
        };
  const dryRun =
    typeof getCustom25DVisualLayerInitializationDryRunReport === "function"
      ? getCustom25DVisualLayerInitializationDryRunReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization dry-run report helper is unavailable."
        };
  const safetyReview =
    typeof getCustom25DVisualLayerInitializationSafetyReview === "function"
      ? getCustom25DVisualLayerInitializationSafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization safety review helper is unavailable."
        };
  const initializationCloseout =
    typeof getCustom25DVisualLayerInitializationCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization closeout report helper is unavailable."
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false,
          affectedLayers: [],
          skippedLayers: [],
          blockedLayers: ["shell"]
        };
  const shellVerification =
    typeof getCustom25DVisualLayerInitializationShellVerificationReport === "function"
      ? getCustom25DVisualLayerInitializationShellVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization shell verification report helper is unavailable."
        };

  return {
    ok: true,
    phase: 116,
    name: "custom-25d-visual-layer-initialization-shell-closeout-report",
    dormant: true,
    closeoutReportOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    guard,
    plan,
    contract,
    dryRun,
    safetyReview,
    initializationCloseout,
    shellResult,
    shellVerification,
    defaultOutcome: {
      blocked: shellResult.allowed !== true,
      reason: shellResult.reason || guard.reason || "visual-layer-initialization-blocked",
      initialized: shellResult.initialized === true,
      mutatesState: shellResult.mutatesState === true,
      createsVisibleGraphics: shellResult.createsVisibleGraphics === true,
      attachesToMap: shellResult.attachesToMap === true
    },
    verifiedSafetyBoundaries: {
      noRendererInitializerCalled: true,
      noRegistryInitializerCalled: true,
      noLayerInitializerCalled: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noLayerDrawn: true,
      noLayerVisible: true,
      noStartupWiring: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      shellSequenceClosedOut: true,
      defaultShellPathBlocked: shellResult.allowed !== true,
      shellStayedInert: shellResult.initialized !== true,
      noVisibleSideEffects: true
    },
    recommendation: "Keep the visual layer initialization shell sequence closed out until a dedicated layer state inventory and audit plan is complete.",
    nextPhase: "phase-117-visual-layer-state-inventory-audit-plan-only"
  };
}

function getCustom25DVisualLayerStateInventoryAuditPlan(options = {}) {
  const shellCloseout =
    typeof getCustom25DVisualLayerInitializationShellCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization shell closeout report helper is unavailable."
        };

  return {
    ok: true,
    phase: 117,
    name: "custom-25d-visual-layer-state-inventory-audit-plan",
    dormant: true,
    auditPlanOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    shellCloseout,
    knownLayerSlots: [
      "roadShadow",
      "roadBase",
      "roadHighlight",
      "water",
      "park",
      "buildingShadow",
      "buildingBase",
      "buildingRoof",
      "label",
      "landmark",
      "dinosaur",
      "film",
      "poi"
    ],
    inventoryChecklist: {
      inventoryWithoutCreation: true,
      verifyDormantOrAbsentStates: [
        "road",
        "water",
        "park",
        "building",
        "shadow",
        "landmark",
        "dinosaur",
        "film",
        "poi"
      ],
      requiredLayerFields: [
        "key",
        "name",
        "order",
        "enabledByDefault",
        "visible",
        "implemented",
        "dormant",
        "initialized",
        "attachedToMap",
        "createsVisibleGraphics",
        "affectsGameplay",
        "clear/reset pathway"
      ],
      confirmNoMapAttachment: true,
      confirmNoStartupWiring: true,
      confirmOSMPreservation: true,
      confirmPinPlayerCapturePreservation: true,
      confirmGameplayBackendRewardCollectionDataPreservation: true,
      futurePassiveInventoryReportRequired: true
    },
    blockedBehaviors: {
      rendererInitialization: true,
      registryInitialization: true,
      layerInitialization: true,
      layerStateCreation: true,
      drawing: true,
      mapAttachment: true,
      startupWiring: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    safety: {
      defaultSafe: true,
      noRendererInitializerCalled: true,
      noRegistryInitializerCalled: true,
      noLayerInitializerCalled: true,
      noLayerStateObjectCreated: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noLayerDrawn: true,
      noLayerVisible: true
    },
    recommendation: "Audit future visual layer state passively before allowing any real layer initialization work.",
    nextPhase: "phase-118-visual-layer-state-inventory-report-only"
  };
}

function getCustom25DVisualLayerStateInventoryReport(options = {}) {
  const auditPlan =
    typeof getCustom25DVisualLayerStateInventoryAuditPlan === "function"
      ? getCustom25DVisualLayerStateInventoryAuditPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory audit plan helper is unavailable."
        };
  const shellCloseout =
    typeof getCustom25DVisualLayerInitializationShellCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization shell closeout report helper is unavailable."
        };
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable"
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false
        };

  const knownLayerSlots = [
    {
      key: "roadBase",
      name: "Road Base",
      expectedOrder: 6,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "roadShadow",
      name: "Road Shadow",
      expectedOrder: 5,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "water",
      name: "Water",
      expectedOrder: 3,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "park",
      name: "Park",
      expectedOrder: 4,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "building",
      name: "Building",
      expectedOrder: 9,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "landmark",
      name: "Landmark",
      expectedOrder: 12,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "dinosaur",
      name: "Dinosaur",
      expectedOrder: 13,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "film",
      name: "Film",
      expectedOrder: 14,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    },
    {
      key: "poi",
      name: "POI",
      expectedOrder: 15,
      expectedEnabledByDefault: false,
      expectedVisible: false,
      expectedImplemented: false,
      expectedDormant: true,
      expectedInitialized: false,
      expectedAttachedToMap: false,
      expectedCreatesVisibleGraphics: false,
      expectedAffectsGameplay: false,
      requiresClearOrResetPath: true
    }
  ];

  return {
    ok: true,
    phase: 118,
    name: "custom-25d-visual-layer-state-inventory-report",
    dormant: true,
    inventoryReportOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    auditPlan,
    shellCloseout,
    guard,
    shellResult,
    knownLayerSlots,
    inventorySummary: {
      passive: true,
      slotCount: knownLayerSlots.length,
      expectedDormantOrAbsentByDefault: true,
      startupRemainsUnwired: true
    },
    missingStateObjects: knownLayerSlots.map((slot) => slot.key),
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    safety: {
      defaultSafe: true,
      noRendererInitializerCalled: true,
      noRegistryInitializerCalled: true,
      noLayerInitializerCalled: true,
      noLayerStateObjectCreated: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noLayerDrawn: true,
      noLayerVisible: true
    },
    recommendation: "Keep visual layer inventory reporting passive and follow with a dedicated safety review before any state work begins.",
    nextPhase: "phase-119-visual-layer-state-inventory-safety-review-only"
  };
}

function getCustom25DVisualLayerStateInventorySafetyReview(options = {}) {
  const auditPlan =
    typeof getCustom25DVisualLayerStateInventoryAuditPlan === "function"
      ? getCustom25DVisualLayerStateInventoryAuditPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory audit plan helper is unavailable."
        };
  const inventoryReport =
    typeof getCustom25DVisualLayerStateInventoryReport === "function"
      ? getCustom25DVisualLayerStateInventoryReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory report helper is unavailable."
        };
  const shellCloseout =
    typeof getCustom25DVisualLayerInitializationShellCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization shell closeout report helper is unavailable."
        };
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable"
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false
        };
  const knownLayerSlotsReviewed = Array.isArray(inventoryReport.knownLayerSlots)
    ? inventoryReport.knownLayerSlots.map((slot) => slot.key)
    : [];

  return {
    ok: true,
    phase: 119,
    name: "custom-25d-visual-layer-state-inventory-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    inventoryReviewOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    auditPlan,
    inventoryReport,
    shellCloseout,
    guard,
    shellResult,
    knownLayerSlotsReviewed,
    defaultOutcome: {
      defaultSafe: true,
      shellBlocked: shellResult.allowed !== true,
      reason: shellResult.reason || guard.reason || "visual-layer-initialization-blocked",
      initialized: shellResult.initialized === true
    },
    reviewedSafetyBoundaries: {
      noLayerStateObjectCreated: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttachedToMap: true,
      noLayerDrawn: true,
      noLayerVisible: true,
      noStartupWiring: true
    },
    blockedBehaviors: {
      rendererInitialization: true,
      registryInitialization: true,
      layerInitialization: true,
      layerStateCreation: true,
      drawing: true,
      mapAttachment: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    risks: [
      "future inventory work must remain passive until an explicit state-creation phase is approved",
      "future layer state creation must stay separate from initialization and map attachment",
      "future work must preserve OSM, pins, player marker, capture radius, gameplay overlays, and UI behavior"
    ],
    recommendation: "Complete a closeout report for passive visual layer state inventory before considering any explicit state-creation phase.",
    nextPhase: "phase-120-visual-layer-state-inventory-closeout-report-only"
  };
}

function getCustom25DVisualLayerStateInventoryCloseoutReport(options = {}) {
  const auditPlan =
    typeof getCustom25DVisualLayerStateInventoryAuditPlan === "function"
      ? getCustom25DVisualLayerStateInventoryAuditPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory audit plan helper is unavailable."
        };
  const inventoryReport =
    typeof getCustom25DVisualLayerStateInventoryReport === "function"
      ? getCustom25DVisualLayerStateInventoryReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory report helper is unavailable."
        };
  const safetyReview =
    typeof getCustom25DVisualLayerStateInventorySafetyReview === "function"
      ? getCustom25DVisualLayerStateInventorySafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory safety review helper is unavailable."
        };
  const shellCloseout =
    typeof getCustom25DVisualLayerInitializationShellCloseoutReport === "function"
      ? getCustom25DVisualLayerInitializationShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer initialization shell closeout report helper is unavailable."
        };
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable"
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false
        };
  const knownLayerSlots = Array.isArray(inventoryReport.knownLayerSlots)
    ? inventoryReport.knownLayerSlots.map((slot) => slot.key)
    : [];

  return {
    ok: true,
    phase: 120,
    name: "custom-25d-visual-layer-state-inventory-closeout-report",
    dormant: true,
    closeoutReportOnly: true,
    inventoryCloseoutOnly: true,
    reportOnly: true,
    dataOnly: true,
    mutatesState: false,
    auditPlan,
    inventoryReport,
    safetyReview,
    shellCloseout,
    guard,
    shellResult,
    knownLayerSlots,
    defaultOutcome: {
      defaultSafe: true,
      shellBlocked: shellResult.allowed !== true,
      reason: shellResult.reason || guard.reason || "visual-layer-initialization-blocked",
      initialized: shellResult.initialized === true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      slotsRemainPassiveIntendedOnly: true,
      noLayerStateObjectCreated: true,
      noLayerCreated: true,
      noLayerInitialized: true,
      noLayerAttached: true,
      noLayerDrawn: true,
      noLayerVisible: true,
      startupUnwired: true,
      inventorySequenceClosedOut: true
    },
    recommendation: "Use a dedicated planning-and-contract phase before any future visual layer state creation work begins.",
    nextPhase: "phase-121-visual-layer-state-creation-planning-contract-only"
  };
}

function getCustom25DVisualLayerStateCreationContractPlan(options = {}) {
  const inventoryCloseout =
    typeof getCustom25DVisualLayerStateInventoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateInventoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state inventory closeout report helper is unavailable."
        };
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable"
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false
        };

  const knownLayerSlots = [
    "roadBase",
    "roadShadow",
    "water",
    "park",
    "building",
    "landmark",
    "dinosaur",
    "film",
    "poi"
  ];

  return {
    ok: true,
    phase: 121,
    name: "custom-25d-visual-layer-state-creation-contract-plan",
    dormant: true,
    contractPlanOnly: true,
    createsLayerStateNow: false,
    mutatesState: false,
    inventoryCloseout,
    guard,
    shellResult,
    plannedLayerStateShape: {
      key: "string",
      name: "string",
      order: "number",
      enabledByDefault: false,
      visible: false,
      implemented: false,
      dormant: true,
      intendedVisualRole: "string",
      targetStyle: "string",
      createsMapLayer: false,
      attachesToMap: false,
      drawsGraphics: false,
      notes: "string"
    },
    knownLayerSlots,
    safetyBoundaries: {
      noStateCreationNow: true,
      noLayerCreationNow: true,
      noLayerInitializationNow: true,
      noMapAttachmentNow: true,
      noVisibleGraphicsNow: true,
      noStartupWiringNow: true
    },
    defaultOutcome: {
      defaultSafe: true,
      noStateCreatedNow: true,
      shellBlocked: shellResult.allowed !== true,
      reason: shellResult.reason || guard.reason || "visual-layer-initialization-blocked",
      initialized: shellResult.initialized === true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep visual layer state work in planning-and-contract mode until a dedicated dormant state factory shell is introduced.",
    nextPhase: "phase-122-create-dormant-visual-layer-state-factory-shell"
  };
}

function createCustom25DVisualLayerStateShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateCreationContractPlan === "function"
      ? getCustom25DVisualLayerStateCreationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state creation contract plan helper is unavailable."
        };
  const guard =
    typeof canInitializeCustom25DVisualLayers === "function"
      ? canInitializeCustom25DVisualLayers(options)
      : {
          allowed: false,
          reason: "visual-layer-initialization-guard-unavailable"
        };
  const shellResult =
    typeof initializeCustom25DVisualLayers === "function"
      ? initializeCustom25DVisualLayers(options)
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-initialization-shell-unavailable",
          initialized: false,
          mutatesState: false,
          createsVisibleGraphics: false,
          attachesToMap: false
        };

  const slotDefinitions = {
    roadBase: {
      key: "roadBase",
      name: "Road Base",
      order: 6,
      intendedVisualRole: "cartoony rounded road body foundation for future 2.5D map polish",
      targetStyle: "soft warm road surfaces, rounded edges, toy-like mobile-friendly roads"
    },
    roadShadow: {
      key: "roadShadow",
      name: "Road Shadow",
      order: 5,
      intendedVisualRole: "soft road shadow foundation for future depth and layering",
      targetStyle: "subtle soft shadows beneath rounded cartoony roads"
    },
    water: {
      key: "water",
      name: "Water",
      order: 3,
      intendedVisualRole: "soft water base foundation for future 2.5D map polish",
      targetStyle: "calm mobile-friendly water surfaces with playful color blocking"
    },
    park: {
      key: "park",
      name: "Park",
      order: 4,
      intendedVisualRole: "park and grass zone foundation for future map greenery polish",
      targetStyle: "soft green park surfaces with toy-like simplified terrain shapes"
    },
    building: {
      key: "building",
      name: "Building",
      order: 9,
      intendedVisualRole: "simple toy-like building body foundation",
      targetStyle: "warm pastel building masses with simple mobile-friendly depth cues"
    },
    landmark: {
      key: "landmark",
      name: "Landmark",
      order: 12,
      intendedVisualRole: "future landmark visual layer placeholder",
      targetStyle: "playful readable landmark accents without affecting gameplay"
    },
    dinosaur: {
      key: "dinosaur",
      name: "Dinosaur",
      order: 13,
      intendedVisualRole: "future dinosaur visual layer placeholder",
      targetStyle: "cartoony internal-only dinosaur accent styling kept dormant by default"
    },
    film: {
      key: "film",
      name: "Film",
      order: 14,
      intendedVisualRole: "future film location visual layer placeholder",
      targetStyle: "subtle cinematic location accents kept dormant and non-gameplay"
    },
    poi: {
      key: "poi",
      name: "POI",
      order: 15,
      intendedVisualRole: "future point-of-interest visual layer placeholder",
      targetStyle: "lightweight readable POI accents that preserve existing overlays"
    }
  };

  const knownLayerSlots = Object.keys(slotDefinitions);
  const requestedSlot = typeof options.slot === "string" ? options.slot : null;
  const hasValidSlot = !!(requestedSlot && slotDefinitions[requestedSlot]);
  const manual = options.manual === true;
  const developerIntent = options.developerIntent === true;
  const allowLayerStateShell = options.allowLayerStateShell === true;

  let reason = null;
  if (!manual) {
    reason = "manual-option-required";
  } else if (!developerIntent) {
    reason = "developer-intent-required";
  } else if (!allowLayerStateShell) {
    reason = "layer-state-shell-not-allowed";
  } else if (!hasValidSlot) {
    reason = "valid-layer-slot-required";
  }

  const allowed = reason === null;
  const baseState = hasValidSlot ? slotDefinitions[requestedSlot] : null;
  const state = allowed && baseState
    ? {
        ...baseState,
        enabledByDefault: false,
        visible: false,
        implemented: false,
        dormant: true,
        createsMapLayer: false,
        attachesToMap: false,
        drawsGraphics: false,
        notes: "Inert visual layer state shell only. Not stored, registered, initialized, attached, or drawn."
      }
    : null;

  return {
    ok: true,
    phase: 122,
    name: "custom-25d-visual-layer-state-factory-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    createsStoredState: false,
    guard,
    shellResult,
    contractPlan,
    requestedSlot,
    allowed,
    reason: reason || "layer-state-shell-only",
    state,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep returned layer state shells inert and non-persistent until a dedicated state registration plan is approved.",
    nextPhase: "phase-123-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateFactoryVerificationReport(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateCreationContractPlan === "function"
      ? getCustom25DVisualLayerStateCreationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state creation contract plan helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const allowedShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedShell.knownLayerSlots)
    ? allowedShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 123,
    name: "custom-25d-visual-layer-state-factory-verification-report",
    dormant: true,
    verificationReportOnly: true,
    mutatesState: false,
    contractPlan,
    defaultShell,
    allowedShell,
    knownLayerSlots,
    checks: {
      defaultShellBlocked: defaultShell.allowed !== true,
      defaultStateIsNull: defaultShell.state === null,
      allowedShellReturnsStateObjectOnly: !!allowedShell.state,
      allowedStateNotStored: allowedShell.createsStoredState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      returnedStateIsInvisible: allowedShell.state?.visible === false,
      returnedStateIsUnimplemented: allowedShell.state?.implemented === false,
      returnedStateIsDormant: allowedShell.state?.dormant === true,
      returnedStateCreatesNoMapLayer: allowedShell.state?.createsMapLayer === false,
      returnedStateAttachesToNoMap: allowedShell.state?.attachesToMap === false,
      returnedStateDrawsNoGraphics: allowedShell.state?.drawsGraphics === false,
      noRendererCalled: true,
      noRegistryCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawOrVisibility: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep layer state shells verification-only until a dedicated registration planning phase is approved.",
    nextPhase: "phase-124-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateFactorySafetyReview(options = {}) {
  const verificationReport =
    typeof getCustom25DVisualLayerStateFactoryVerificationReport === "function"
      ? getCustom25DVisualLayerStateFactoryVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory verification report helper is unavailable."
        };
  const contractPlan =
    typeof getCustom25DVisualLayerStateCreationContractPlan === "function"
      ? getCustom25DVisualLayerStateCreationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state creation contract plan helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const allowedShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedShell.knownLayerSlots)
    ? allowedShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 124,
    name: "custom-25d-visual-layer-state-factory-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    mutatesState: false,
    verificationReport,
    contractPlan,
    defaultShell,
    allowedShell,
    knownLayerSlots,
    safetyChecks: {
      defaultShellBlocked: defaultShell.allowed !== true,
      defaultStateNull: defaultShell.state === null,
      allowedShellReturnsInertDataOnly: !!allowedShell.state,
      allowedStateNotStored: allowedShell.createsStoredState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererCalled: true,
      noRegistryCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noLayerStateStored: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the visual layer state factory in safety-review mode until a dedicated registration planning phase is approved.",
    nextPhase: "phase-125-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateFactoryCloseoutReport(options = {}) {
  const safetyReview =
    typeof getCustom25DVisualLayerStateFactorySafetyReview === "function"
      ? getCustom25DVisualLayerStateFactorySafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory safety review helper is unavailable."
        };
  const verificationReport =
    typeof getCustom25DVisualLayerStateFactoryVerificationReport === "function"
      ? getCustom25DVisualLayerStateFactoryVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory verification report helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const allowedShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedShell.knownLayerSlots)
    ? allowedShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 125,
    name: "custom-25d-visual-layer-state-factory-closeout-report",
    dormant: true,
    closeoutReportOnly: true,
    mutatesState: false,
    safetyReview,
    verificationReport,
    defaultShell,
    allowedShell,
    knownLayerSlots,
    closeoutChecks: {
      defaultShellBlocked: defaultShell.allowed !== true,
      defaultStateNull: defaultShell.state === null,
      allowedShellReturnsInertDataOnly: !!allowedShell.state,
      allowedStateNotStored: allowedShell.createsStoredState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererCalled: true,
      noRegistryCalled: true,
      noRealLayerInitializerCalled: true,
      noLayerStateStored: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      factoryShellSequenceClosedOut: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      sequenceClosedOut: true,
      shellRemainsInert: true,
      noPersistentStateCreated: true
    },
    recommendation: "Treat the layer state factory as closed out and move next to registration planning only, with the same dormant guardrails intact.",
    nextPhase: "phase-126-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateFactoryInventoryReport(options = {}) {
  const factoryCloseout =
    typeof getCustom25DVisualLayerStateFactoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateFactoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory closeout report helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const sampleAllowedShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleAllowedShell.knownLayerSlots)
    ? sampleAllowedShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 126,
    name: "custom-25d-visual-layer-state-factory-inventory-report",
    dormant: true,
    inventoryReportOnly: true,
    mutatesState: false,
    factoryCloseout,
    defaultShell,
    sampleAllowedShell,
    knownLayerSlots,
    factoryOutputInventory: {
      factoryCloseoutExists: !!factoryCloseout.ok,
      defaultShellBlocked: defaultShell.allowed !== true,
      defaultStateNull: defaultShell.state === null,
      sampleAllowedShellReturnsInertDataOnly: !!sampleAllowedShell.state,
      allowedStateNotStored: sampleAllowedShell.createsStoredState === false,
      knownSlotCount: knownLayerSlots.length,
      factoryDescribesStateDataOnly: true,
      noRendererRegistryOrLayerInitializer: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep factory output inventory passive and move next only to registration planning, not implementation.",
    nextPhase: "phase-127-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateFactoryInventorySafetyReview(options = {}) {
  const factoryInventory =
    typeof getCustom25DVisualLayerStateFactoryInventoryReport === "function"
      ? getCustom25DVisualLayerStateFactoryInventoryReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory inventory report helper is unavailable."
        };
  const factoryCloseout =
    typeof getCustom25DVisualLayerStateFactoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateFactoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory closeout report helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const sampleAllowedShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleAllowedShell.knownLayerSlots)
    ? sampleAllowedShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 127,
    name: "custom-25d-visual-layer-state-factory-inventory-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    inventorySafetyOnly: true,
    mutatesState: false,
    factoryInventory,
    factoryCloseout,
    defaultShell,
    sampleAllowedShell,
    knownLayerSlots,
    safetyChecks: {
      factoryInventoryExists: !!factoryInventory.ok,
      factoryCloseoutExists: !!factoryCloseout.ok,
      defaultShellBlocked: defaultShell.allowed !== true,
      defaultStateNull: defaultShell.state === null,
      sampleAllowedShellReturnsInertDataOnly: !!sampleAllowedShell.state,
      allowedStateNotStored: sampleAllowedShell.createsStoredState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      factoryDescribesStateDataOnly: true,
      noRendererRegistryOrLayerInitializer: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the factory inventory in safety-review mode until a dedicated registration planning phase is approved.",
    nextPhase: "phase-128-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateFactoryInventoryCloseoutReport(options = {}) {
  const inventorySafetyReview =
    typeof getCustom25DVisualLayerStateFactoryInventorySafetyReview === "function"
      ? getCustom25DVisualLayerStateFactoryInventorySafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory inventory safety review helper is unavailable."
        };
  const factoryInventory =
    typeof getCustom25DVisualLayerStateFactoryInventoryReport === "function"
      ? getCustom25DVisualLayerStateFactoryInventoryReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory inventory report helper is unavailable."
        };
  const factoryCloseout =
    typeof getCustom25DVisualLayerStateFactoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateFactoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory closeout report helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const sampleAllowedShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleAllowedShell.knownLayerSlots)
    ? sampleAllowedShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 128,
    name: "custom-25d-visual-layer-state-factory-inventory-closeout-report",
    dormant: true,
    closeoutReportOnly: true,
    inventoryCloseoutOnly: true,
    mutatesState: false,
    inventorySafetyReview,
    factoryInventory,
    factoryCloseout,
    defaultShell,
    sampleAllowedShell,
    knownLayerSlots,
    closeoutChecks: {
      factoryInventoryExists: !!factoryInventory.ok,
      inventorySafetyReviewExists: !!inventorySafetyReview.ok,
      factoryCloseoutExists: !!factoryCloseout.ok,
      defaultShellBlocked: defaultShell.allowed !== true,
      defaultStateNull: defaultShell.state === null,
      sampleAllowedShellReturnsInertDataOnly: !!sampleAllowedShell.state,
      allowedStateNotStored: sampleAllowedShell.createsStoredState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      factoryDescribesStateDataOnly: true,
      noRendererRegistryOrLayerInitializer: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      factoryInventorySequenceClosedOut: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      sequenceClosedOut: true,
      inventoryRemainsPassive: true,
      noPersistentStateCreated: true
    },
    recommendation: "Treat the factory inventory sequence as closed out and move next only to registration planning, with the same dormant constraints intact.",
    nextPhase: "phase-129-visual-layer-state-registration-planning-only"
  };
}

function getCustom25DVisualLayerStateRegistryContractPlan(options = {}) {
  const factoryInventoryCloseout =
    typeof getCustom25DVisualLayerStateFactoryInventoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateFactoryInventoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory inventory closeout report helper is unavailable."
        };
  const factoryCloseout =
    typeof getCustom25DVisualLayerStateFactoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateFactoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state factory closeout report helper is unavailable."
        };
  const defaultShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultShell.knownLayerSlots)
    ? defaultShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 129,
    name: "custom-25d-visual-layer-state-registry-contract-plan",
    dormant: true,
    contractPlanOnly: true,
    createsRegistryNow: false,
    storesLayerStateNow: false,
    mutatesState: false,
    factoryInventoryCloseout,
    factoryCloseout,
    defaultShell,
    knownLayerSlots,
    plannedRegistryShape: {
      created: false,
      initialized: false,
      dormant: true,
      storesState: false,
      slots: [],
      slotCount: 0,
      mapAttached: false,
      drawsGraphics: false,
      visible: false
    },
    plannedRegistryRules: {
      futureRegistryStoresOnlyInertStateDescriptions: true,
      futureRegistryDoesNotInitializeLayers: true,
      futureRegistryDoesNotRenderLayers: true,
      futureRegistryDoesNotAttachToMap: true,
      futureRegistryDoesNotDrawGraphics: true,
      futureRegistryDoesNotMakeLayersVisible: true,
      startupRemainsUnwired: true
    },
    safetyBoundaries: {
      noRegistryCreatedNow: true,
      noLayerStateStoredNow: true,
      factoryInventorySequenceClosedOut: !!factoryInventoryCloseout.closedOut,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep registry planning passive and move next only to a dormant visual layer state registry shell that still stores nothing by default.",
    nextPhase: "phase-130-dormant-visual-layer-state-registry-shell"
  };
}

function createCustom25DVisualLayerStateRegistryShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateRegistryContractPlan === "function"
      ? getCustom25DVisualLayerStateRegistryContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry contract plan helper is unavailable."
        };
  const defaultLayerStateShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const sampleLayerStateShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({
          manual: true,
          developerIntent: true,
          allowLayerStateShell: true,
          slot: "roadBase"
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleLayerStateShell.knownLayerSlots)
    ? sampleLayerStateShell.knownLayerSlots.slice()
    : [];
  const allowed =
    options.manual === true &&
    options.developerIntent === true &&
    options.allowLayerStateRegistryShell === true;

  return {
    ok: true,
    phase: 130,
    name: "custom-25d-visual-layer-state-registry-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    createsStoredRegistry: false,
    storesLayerState: false,
    contractPlan,
    defaultLayerStateShell,
    sampleLayerStateShell,
    allowed,
    reason: allowed ? "layer-state-registry-shell-only" : "manual-guard-required",
    registry: allowed
      ? {
          created: true,
          initialized: false,
          dormant: true,
          storesState: false,
          slots: [],
          slotCount: 0,
          mapAttached: false,
          drawsGraphics: false,
          visible: false,
          notes: "Inert manual registry shell only. Not stored, initialized, attached, rendered, or made visible."
        }
      : null,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Use this shell only as a passive contract checkpoint and keep the future stored registry uncreated until a dedicated verification phase is approved.",
    nextPhase: "phase-131-visual-layer-state-registry-shell-verification-report"
  };
}

function getCustom25DVisualLayerStateRegistryShellVerificationReport(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateRegistryContractPlan === "function"
      ? getCustom25DVisualLayerStateRegistryContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry contract plan helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const allowedRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedRegistryShell.knownLayerSlots)
    ? allowedRegistryShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 131,
    name: "custom-25d-visual-layer-state-registry-shell-verification-report",
    dormant: true,
    verificationReportOnly: true,
    mutatesState: false,
    contractPlan,
    defaultRegistryShell,
    allowedRegistryShell,
    knownLayerSlots,
    checks: {
      defaultRegistryShellBlocked: defaultRegistryShell.allowed !== true,
      defaultRegistryIsNull: defaultRegistryShell.registry === null,
      allowedRegistryShellReturnsInertDataOnly: !!allowedRegistryShell.registry,
      allowedRegistryNotStoredGlobally: allowedRegistryShell.createsStoredRegistry === false,
      registryStoresNoLayerState: allowedRegistryShell.storesLayerState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the registry shell in verification-only mode until a dedicated safety review phase confirms the same inert behavior.",
    nextPhase: "phase-132-visual-layer-state-registry-shell-safety-review"
  };
}

function getCustom25DVisualLayerStateRegistryShellSafetyReview(options = {}) {
  const verificationReport =
    typeof getCustom25DVisualLayerStateRegistryShellVerificationReport === "function"
      ? getCustom25DVisualLayerStateRegistryShellVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry shell verification report helper is unavailable."
        };
  const contractPlan =
    typeof getCustom25DVisualLayerStateRegistryContractPlan === "function"
      ? getCustom25DVisualLayerStateRegistryContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry contract plan helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const allowedRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedRegistryShell.knownLayerSlots)
    ? allowedRegistryShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 132,
    name: "custom-25d-visual-layer-state-registry-shell-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    mutatesState: false,
    verificationReport,
    contractPlan,
    defaultRegistryShell,
    allowedRegistryShell,
    knownLayerSlots,
    safetyChecks: {
      verificationReportExists: !!verificationReport.ok,
      defaultRegistryShellBlocked: defaultRegistryShell.allowed !== true,
      defaultRegistryIsNull: defaultRegistryShell.registry === null,
      allowedRegistryShellReturnsInertDataOnly: !!allowedRegistryShell.registry,
      allowedRegistryNotStoredGlobally: allowedRegistryShell.createsStoredRegistry === false,
      registryStoresNoLayerState: allowedRegistryShell.storesLayerState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the registry shell in safety-review mode and move next only to a closeout phase once this inert behavior is formally recorded.",
    nextPhase: "phase-133-visual-layer-state-registry-shell-closeout-report"
  };
}

function getCustom25DVisualLayerStateRegistryShellCloseoutReport(options = {}) {
  const safetyReview =
    typeof getCustom25DVisualLayerStateRegistryShellSafetyReview === "function"
      ? getCustom25DVisualLayerStateRegistryShellSafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry shell safety review helper is unavailable."
        };
  const verificationReport =
    typeof getCustom25DVisualLayerStateRegistryShellVerificationReport === "function"
      ? getCustom25DVisualLayerStateRegistryShellVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry shell verification report helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const allowedRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedRegistryShell.knownLayerSlots)
    ? allowedRegistryShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 133,
    name: "custom-25d-visual-layer-state-registry-shell-closeout-report",
    dormant: true,
    closeoutReportOnly: true,
    mutatesState: false,
    safetyReview,
    verificationReport,
    defaultRegistryShell,
    allowedRegistryShell,
    knownLayerSlots,
    closeoutChecks: {
      safetyReviewExists: !!safetyReview.ok,
      verificationReportExists: !!verificationReport.ok,
      defaultRegistryShellBlocked: defaultRegistryShell.allowed !== true,
      defaultRegistryIsNull: defaultRegistryShell.registry === null,
      allowedRegistryShellReturnsInertDataOnly: !!allowedRegistryShell.registry,
      allowedRegistryNotStoredGlobally: allowedRegistryShell.createsStoredRegistry === false,
      registryStoresNoLayerState: allowedRegistryShell.storesLayerState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      registryShellSequenceClosedOut: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      sequenceClosedOut: true,
      registryShellRemainsPassive: true,
      noPersistentRegistryCreated: true
    },
    recommendation: "Treat the registry shell sequence as closed out and move next only to a passive stored-registry inventory phase with the same dormant guardrails.",
    nextPhase: "phase-134-visual-layer-state-registry-inventory-report"
  };
}

function getCustom25DVisualLayerStateRegistryInventoryReport(options = {}) {
  const registryShellCloseout =
    typeof getCustom25DVisualLayerStateRegistryShellCloseoutReport === "function"
      ? getCustom25DVisualLayerStateRegistryShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry shell closeout report helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const sampleAllowedRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleAllowedRegistryShell.knownLayerSlots)
    ? sampleAllowedRegistryShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 134,
    name: "custom-25d-visual-layer-state-registry-inventory-report",
    dormant: true,
    inventoryReportOnly: true,
    mutatesState: false,
    registryShellCloseout,
    defaultRegistryShell,
    sampleAllowedRegistryShell,
    knownLayerSlots,
    registryInventory: {
      registryShellCloseoutExists: !!registryShellCloseout.ok,
      defaultRegistryShellBlocked: defaultRegistryShell.allowed !== true,
      defaultRegistryIsNull: defaultRegistryShell.registry === null,
      sampleAllowedRegistryShellReturnsInertDataOnly: !!sampleAllowedRegistryShell.registry,
      allowedRegistryNotStoredGlobally: sampleAllowedRegistryShell.createsStoredRegistry === false,
      registryStoresNoLayerState: sampleAllowedRegistryShell.storesLayerState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererRegistryOrLayerInitializer: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the registry inventory passive and move next only to a safety review that confirms the same inert non-stored behavior.",
    nextPhase: "phase-135-visual-layer-state-registry-inventory-safety-review"
  };
}

function getCustom25DVisualLayerStateRegistryInventorySafetyReview(options = {}) {
  const registryInventory =
    typeof getCustom25DVisualLayerStateRegistryInventoryReport === "function"
      ? getCustom25DVisualLayerStateRegistryInventoryReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry inventory report helper is unavailable."
        };
  const registryShellCloseout =
    typeof getCustom25DVisualLayerStateRegistryShellCloseoutReport === "function"
      ? getCustom25DVisualLayerStateRegistryShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry shell closeout report helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const sampleAllowedRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleAllowedRegistryShell.knownLayerSlots)
    ? sampleAllowedRegistryShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 135,
    name: "custom-25d-visual-layer-state-registry-inventory-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    inventorySafetyOnly: true,
    mutatesState: false,
    registryInventory,
    registryShellCloseout,
    defaultRegistryShell,
    sampleAllowedRegistryShell,
    knownLayerSlots,
    safetyChecks: {
      registryInventoryExists: !!registryInventory.ok,
      registryShellCloseoutExists: !!registryShellCloseout.ok,
      defaultRegistryShellBlocked: defaultRegistryShell.allowed !== true,
      defaultRegistryIsNull: defaultRegistryShell.registry === null,
      sampleAllowedRegistryShellReturnsInertDataOnly: !!sampleAllowedRegistryShell.registry,
      allowedRegistryNotStoredGlobally: sampleAllowedRegistryShell.createsStoredRegistry === false,
      registryStoresNoLayerState: sampleAllowedRegistryShell.storesLayerState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererRegistryOrLayerInitializer: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the registry inventory in safety-review mode and move next only to a closeout report that records the same inert non-stored behavior.",
    nextPhase: "phase-136-visual-layer-state-registry-inventory-closeout-report"
  };
}

function getCustom25DVisualLayerStateRegistryInventoryCloseoutReport(options = {}) {
  const inventorySafetyReview =
    typeof getCustom25DVisualLayerStateRegistryInventorySafetyReview === "function"
      ? getCustom25DVisualLayerStateRegistryInventorySafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry inventory safety review helper is unavailable."
        };
  const registryInventory =
    typeof getCustom25DVisualLayerStateRegistryInventoryReport === "function"
      ? getCustom25DVisualLayerStateRegistryInventoryReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry inventory report helper is unavailable."
        };
  const registryShellCloseout =
    typeof getCustom25DVisualLayerStateRegistryShellCloseoutReport === "function"
      ? getCustom25DVisualLayerStateRegistryShellCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry shell closeout report helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const sampleAllowedRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(sampleAllowedRegistryShell.knownLayerSlots)
    ? sampleAllowedRegistryShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 136,
    name: "custom-25d-visual-layer-state-registry-inventory-closeout-report",
    dormant: true,
    closeoutReportOnly: true,
    inventoryCloseoutOnly: true,
    mutatesState: false,
    inventorySafetyReview,
    registryInventory,
    registryShellCloseout,
    defaultRegistryShell,
    sampleAllowedRegistryShell,
    knownLayerSlots,
    closeoutChecks: {
      registryInventoryExists: !!registryInventory.ok,
      inventorySafetyReviewExists: !!inventorySafetyReview.ok,
      registryShellCloseoutExists: !!registryShellCloseout.ok,
      defaultRegistryShellBlocked: defaultRegistryShell.allowed !== true,
      defaultRegistryIsNull: defaultRegistryShell.registry === null,
      sampleAllowedRegistryShellReturnsInertDataOnly: !!sampleAllowedRegistryShell.registry,
      allowedRegistryNotStoredGlobally: sampleAllowedRegistryShell.createsStoredRegistry === false,
      registryStoresNoLayerState: sampleAllowedRegistryShell.storesLayerState === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRendererRegistryOrLayerInitializer: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      registryInventorySequenceClosedOut: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      sequenceClosedOut: true,
      registryInventoryRemainsPassive: true,
      noPersistentRegistryCreated: true
    },
    recommendation: "Treat the registry inventory sequence as closed out and move next only to passive slot contract planning with the same dormant guardrails intact.",
    nextPhase: "phase-137-visual-layer-state-slot-contract-plan"
  };
}

function getCustom25DVisualLayerStateStorageContractPlan(options = {}) {
  const registryInventoryCloseout =
    typeof getCustom25DVisualLayerStateRegistryInventoryCloseoutReport === "function"
      ? getCustom25DVisualLayerStateRegistryInventoryCloseoutReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry inventory closeout report helper is unavailable."
        };
  const registryContractPlan =
    typeof getCustom25DVisualLayerStateRegistryContractPlan === "function"
      ? getCustom25DVisualLayerStateRegistryContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry contract plan helper is unavailable."
        };
  const defaultLayerStateShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultRegistryShell.knownLayerSlots)
    ? defaultRegistryShell.knownLayerSlots.slice()
    : Array.isArray(defaultLayerStateShell.knownLayerSlots)
      ? defaultLayerStateShell.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 137,
    name: "custom-25d-visual-layer-state-storage-contract-plan",
    dormant: true,
    contractPlanOnly: true,
    storesLayerStateNow: false,
    createsStoredRegistryNow: false,
    mutatesState: false,
    registryInventoryCloseout,
    registryContractPlan,
    defaultLayerStateShell,
    defaultRegistryShell,
    knownLayerSlots,
    plannedStorageContract: {
      storesOnlyInertStateDescriptions: true,
      createsVisibleGraphics: false,
      attachesToMap: false,
      initializesLayers: false,
      rendersLayers: false,
      drawsGraphics: false,
      slotCount: knownLayerSlots.length
    },
    plannedSafetyRules: {
      requiresExplicitManualGuard: true,
      requiresExplicitDeveloperIntent: true,
      noStartupWiring: true,
      noRendererRegistryOrLayerInitialization: true,
      noMapAttachDrawVisibility: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep state storage planning passive and move next only to a dormant stored visual layer state registry creation shell with the same manual guardrails.",
    nextPhase: "phase-138-dormant-stored-visual-layer-state-registry-creation-shell"
  };
}

function createCustom25DVisualLayerStateStorageShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateStorageContractPlan === "function"
      ? getCustom25DVisualLayerStateStorageContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage contract plan helper is unavailable."
        };
  const defaultRegistryShell =
    typeof createCustom25DVisualLayerStateRegistryShell === "function"
      ? createCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-shell-unavailable",
          registry: null,
          knownLayerSlots: []
        };
  const defaultLayerStateShell =
    typeof createCustom25DVisualLayerStateShell === "function"
      ? createCustom25DVisualLayerStateShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-shell-unavailable",
          state: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(contractPlan.knownLayerSlots)
    ? contractPlan.knownLayerSlots.slice()
    : Array.isArray(defaultRegistryShell.knownLayerSlots)
      ? defaultRegistryShell.knownLayerSlots.slice()
      : Array.isArray(defaultLayerStateShell.knownLayerSlots)
        ? defaultLayerStateShell.knownLayerSlots.slice()
        : [];
  const allowed =
    options.manual === true &&
    options.developerIntent === true &&
    options.allowLayerStateStorageShell === true;

  let storage = null;
  if (allowed) {
    const allowedRegistryShell =
      typeof createCustom25DVisualLayerStateRegistryShell === "function"
        ? createCustom25DVisualLayerStateRegistryShell({
            manual: true,
            developerIntent: true,
            allowLayerStateRegistryShell: true
          })
        : null;
    const layerStates = knownLayerSlots.map((slot) => {
      const shell =
        typeof createCustom25DVisualLayerStateShell === "function"
          ? createCustom25DVisualLayerStateShell({
              manual: true,
              developerIntent: true,
              allowLayerStateShell: true,
              slot
            })
          : null;
      return shell && shell.state ? shell.state : null;
    }).filter(Boolean);

    storage = {
      created: true,
      initialized: false,
      dormant: true,
      storedGlobally: false,
      registry: allowedRegistryShell ? allowedRegistryShell.registry : null,
      layerStates,
      slotCount: layerStates.length,
      mapAttached: false,
      drawsGraphics: false,
      visible: false,
      notes: "Inert storage shell only. Registry and layer states are returned data only and are not stored globally."
    };
  }

  return {
    ok: true,
    phase: 138,
    name: "custom-25d-visual-layer-state-storage-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    createsStoredRegistry: false,
    storesLayerStateGlobally: false,
    contractPlan,
    defaultRegistryShell,
    defaultLayerStateShell,
    allowed,
    reason: allowed ? "layer-state-storage-shell-only" : "manual-guard-required",
    storage,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Use this storage shell only as a passive checkpoint and keep all returned registry and layer state data out of global storage until a dedicated verification phase is approved.",
    nextPhase: "phase-139-stored-visual-layer-state-registry-creation-shell-verification-report"
  };
}

function getCustom25DVisualLayerStateStorageShellVerificationReport(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateStorageContractPlan === "function"
      ? getCustom25DVisualLayerStateStorageContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage contract plan helper is unavailable."
        };
  const defaultStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const allowedStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({
          manual: true,
          developerIntent: true,
          allowLayerStateStorageShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedStorageShell.knownLayerSlots)
    ? allowedStorageShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 139,
    name: "custom-25d-visual-layer-state-storage-shell-verification-report",
    dormant: true,
    verificationReportOnly: true,
    mutatesState: false,
    contractPlan,
    defaultStorageShell,
    allowedStorageShell,
    knownLayerSlots,
    checks: {
      defaultStorageShellBlocked: defaultStorageShell.allowed !== true,
      defaultStorageIsNull: defaultStorageShell.storage === null,
      allowedStorageShellReturnsInertDataOnly: !!allowedStorageShell.storage,
      allowedStorageNotStoredGlobally: allowedStorageShell.storesLayerStateGlobally === false,
      noGlobalRegistryCreated: allowedStorageShell.createsStoredRegistry === false,
      noLayerStateStoredGlobally: allowedStorageShell.storesLayerStateGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      storageSlotCountIsNine:
        !!allowedStorageShell.storage && allowedStorageShell.storage.slotCount === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the storage shell in verification-only mode until a dedicated safety review confirms the same inert non-storing behavior.",
    nextPhase: "phase-140-stored-visual-layer-state-registry-creation-shell-safety-review"
  };
}

function getCustom25DVisualLayerStateStorageShellSafetyReview(options = {}) {
  const verificationReport =
    typeof getCustom25DVisualLayerStateStorageShellVerificationReport === "function"
      ? getCustom25DVisualLayerStateStorageShellVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage shell verification report helper is unavailable."
        };
  const contractPlan =
    typeof getCustom25DVisualLayerStateStorageContractPlan === "function"
      ? getCustom25DVisualLayerStateStorageContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage contract plan helper is unavailable."
        };
  const defaultStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const allowedStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({
          manual: true,
          developerIntent: true,
          allowLayerStateStorageShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedStorageShell.knownLayerSlots)
    ? allowedStorageShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 140,
    name: "custom-25d-visual-layer-state-storage-shell-safety-review",
    dormant: true,
    safetyReviewOnly: true,
    mutatesState: false,
    verificationReport,
    contractPlan,
    defaultStorageShell,
    allowedStorageShell,
    knownLayerSlots,
    safetyChecks: {
      verificationReportExists: !!verificationReport.ok,
      defaultStorageShellBlocked: defaultStorageShell.allowed !== true,
      defaultStorageIsNull: defaultStorageShell.storage === null,
      allowedStorageShellReturnsInertDataOnly: !!allowedStorageShell.storage,
      allowedStorageNotStoredGlobally: allowedStorageShell.storesLayerStateGlobally === false,
      noGlobalRegistryCreated: allowedStorageShell.createsStoredRegistry === false,
      noLayerStateStoredGlobally: allowedStorageShell.storesLayerStateGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      storageSlotCountIsNine:
        !!allowedStorageShell.storage && allowedStorageShell.storage.slotCount === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep the storage shell in safety-review mode until a closeout report records the same inert non-storing behavior.",
    nextPhase: "phase-141-stored-visual-layer-state-registry-creation-shell-closeout-report"
  };
}

function getCustom25DVisualLayerStateStorageShellCloseoutBundle(options = {}) {
  const safetyReview =
    typeof getCustom25DVisualLayerStateStorageShellSafetyReview === "function"
      ? getCustom25DVisualLayerStateStorageShellSafetyReview({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage shell safety review helper is unavailable."
        };
  const verificationReport =
    typeof getCustom25DVisualLayerStateStorageShellVerificationReport === "function"
      ? getCustom25DVisualLayerStateStorageShellVerificationReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage shell verification report helper is unavailable."
        };
  const defaultStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const allowedStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({
          manual: true,
          developerIntent: true,
          allowLayerStateStorageShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedStorageShell.knownLayerSlots)
    ? allowedStorageShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 141,
    name: "custom-25d-visual-layer-state-storage-shell-closeout-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    closeoutReportOnly: true,
    inventoryReportOnly: true,
    inventorySafetyOnly: true,
    inventoryCloseoutOnly: true,
    mutatesState: false,
    safetyReview,
    verificationReport,
    defaultStorageShell,
    allowedStorageShell,
    knownLayerSlots,
    closeoutChecks: {
      safetyReviewExists: !!safetyReview.ok,
      verificationReportExists: !!verificationReport.ok,
      defaultStorageShellBlocked: defaultStorageShell.allowed !== true,
      defaultStorageIsNull: defaultStorageShell.storage === null,
      allowedStorageShellReturnsInertDataOnly: !!allowedStorageShell.storage,
      allowedStorageNotStoredGlobally: allowedStorageShell.storesLayerStateGlobally === false,
      noGlobalRegistryCreated: allowedStorageShell.createsStoredRegistry === false,
      noLayerStateStoredGlobally: allowedStorageShell.storesLayerStateGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      storageSlotCountIsNine:
        !!allowedStorageShell.storage && allowedStorageShell.storage.slotCount === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      storageShellSequenceClosedOut: true
    },
    inventoryChecks: {
      inventorySequenceClosedOut: true,
      storageShellInventorySequenceClosedOut: true,
      slotCountIsStable: knownLayerSlots.length === 9,
      returnedDataOnly: !!allowedStorageShell.storage
    },
    safetyChecks: {
      passiveOnly: true,
      noMutation: true,
      noGlobalStorage: true,
      noVisibility: true,
      noStartupWiring: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      storageShellSequenceClosedOut: true,
      storageShellInventorySequenceClosedOut: true,
      noPersistentRegistryCreated: true,
      noPersistentLayerStateStored: true
    },
    recommendation: "Treat the storage shell sequence as fully passively bundled and move next only to a readiness plan for any future stored visual layer state registry work.",
    nextPhase: "stored-visual-layer-state-registry-readiness-plan"
  };
}

function getCustom25DVisualLayerStateRegistryReadinessPlan(options = {}) {
  const storageCloseoutBundle =
    typeof getCustom25DVisualLayerStateStorageShellCloseoutBundle === "function"
      ? getCustom25DVisualLayerStateStorageShellCloseoutBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage shell closeout bundle helper is unavailable."
        };
  const storageContractPlan =
    typeof getCustom25DVisualLayerStateStorageContractPlan === "function"
      ? getCustom25DVisualLayerStateStorageContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state storage contract plan helper is unavailable."
        };
  const defaultStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultStorageShell.knownLayerSlots)
    ? defaultStorageShell.knownLayerSlots.slice()
    : Array.isArray(storageContractPlan.knownLayerSlots)
      ? storageContractPlan.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 142,
    name: "custom-25d-visual-layer-state-registry-readiness-plan",
    dormant: true,
    readinessPlanOnly: true,
    mutatesState: false,
    createsStoredRegistryNow: false,
    storesLayerStateNow: false,
    storageCloseoutBundle,
    storageContractPlan,
    defaultStorageShell,
    knownLayerSlots,
    readinessChecks: {
      closeoutBundleExists: !!storageCloseoutBundle.ok,
      storageShellSequenceClosedOut: !!(
        storageCloseoutBundle.closedOut &&
        storageCloseoutBundle.closedOut.storageShellSequenceClosedOut
      ),
      storageInventorySequenceClosedOut: !!(
        storageCloseoutBundle.closedOut &&
        storageCloseoutBundle.closedOut.storageShellInventorySequenceClosedOut
      ),
      noRegistryCreatedNow: true,
      noLayerStateStoredNow: true,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      futureStepRequiresExplicitManualGuard: true,
      futureStepRequiresExplicitDeveloperIntent: true,
      futureStepStoresOnlyInertReturnedData: true,
      futureStepDoesNotRenderInitAttachDrawShowLayers: true,
      startupRemainsUnwired: true,
      noBehaviorChanged: true
    },
    safetyBoundaries: {
      noGlobalRegistryCreation: true,
      noGlobalLayerStateStorage: true,
      noRendererRegistryOrLayerInitialization: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Treat stored registry work as planning-ready only and move next to a guarded assembly shell that still returns inert data without storing it.",
    nextPhase: "guarded-stored-visual-layer-state-registry-assembly-shell"
  };
}

function assembleCustom25DVisualLayerStateRegistryShell(options = {}) {
  const readinessPlan =
    typeof getCustom25DVisualLayerStateRegistryReadinessPlan === "function"
      ? getCustom25DVisualLayerStateRegistryReadinessPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry readiness plan helper is unavailable."
        };
  const defaultStorageShell =
    typeof createCustom25DVisualLayerStateStorageShell === "function"
      ? createCustom25DVisualLayerStateStorageShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-storage-shell-unavailable",
          storage: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultStorageShell.knownLayerSlots)
    ? defaultStorageShell.knownLayerSlots.slice()
    : Array.isArray(readinessPlan.knownLayerSlots)
      ? readinessPlan.knownLayerSlots.slice()
      : [];
  const allowed =
    options.manual === true &&
    options.developerIntent === true &&
    options.allowLayerStateRegistryAssembly === true;

  let assembly = null;
  if (allowed) {
    const allowedStorageShell =
      typeof createCustom25DVisualLayerStateStorageShell === "function"
        ? createCustom25DVisualLayerStateStorageShell({
            manual: true,
            developerIntent: true,
            allowLayerStateStorageShell: true
          })
        : null;
    const storage = allowedStorageShell ? allowedStorageShell.storage : null;
    assembly = {
      created: true,
      initialized: false,
      dormant: true,
      storedGlobally: false,
      registry: storage ? storage.registry : null,
      layerStates: storage ? storage.layerStates : [],
      slotCount: storage ? storage.slotCount : 0,
      mapAttached: false,
      drawsGraphics: false,
      visible: false,
      notes: "Inert assembled registry/state bundle only. Returned data is not stored globally or initialized."
    };
  }

  return {
    ok: true,
    phase: 143,
    name: "custom-25d-visual-layer-state-registry-assembly-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    storesGlobally: false,
    readinessPlan,
    defaultStorageShell,
    allowed,
    reason: allowed ? "layer-state-registry-assembly-shell-only" : "manual-guard-required",
    assembly,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Use this assembly shell only as a passive checkpoint and keep all returned registry/state data out of global storage until a dedicated verification phase is approved.",
    nextPhase: "stored-visual-layer-state-registry-assembly-shell-verification-report"
  };
}

function getCustom25DVisualLayerStateRegistryAssemblyCloseoutBundle(options = {}) {
  const readinessPlan =
    typeof getCustom25DVisualLayerStateRegistryReadinessPlan === "function"
      ? getCustom25DVisualLayerStateRegistryReadinessPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry readiness plan helper is unavailable."
        };
  const defaultAssemblyShell =
    typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
      ? assembleCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-assembly-shell-unavailable",
          assembly: null,
          knownLayerSlots: []
        };
  const allowedAssemblyShell =
    typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
      ? assembleCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryAssembly: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-assembly-shell-unavailable",
          assembly: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedAssemblyShell.knownLayerSlots)
    ? allowedAssemblyShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 144,
    name: "custom-25d-visual-layer-state-registry-assembly-closeout-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    verificationReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    mutatesState: false,
    readinessPlan,
    defaultAssemblyShell,
    allowedAssemblyShell,
    knownLayerSlots,
    verificationChecks: {
      readinessPlanExists: !!readinessPlan.ok,
      defaultAssemblyShellBlocked: defaultAssemblyShell.allowed !== true,
      defaultAssemblyIsNull: defaultAssemblyShell.assembly === null,
      allowedAssemblyShellReturnsInertDataOnly: !!allowedAssemblyShell.assembly,
      allowedAssemblyNotStoredGlobally: allowedAssemblyShell.storesGlobally === false,
      noGlobalRegistryCreated: allowedAssemblyShell.storesGlobally === false,
      noLayerStateStoredGlobally: allowedAssemblyShell.storesGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      assemblySlotCountIsNine:
        !!allowedAssemblyShell.assembly && allowedAssemblyShell.assembly.slotCount === 9
    },
    safetyChecks: {
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      passiveOnly: true,
      noMutation: true
    },
    closeoutChecks: {
      registryAssemblyShellSequenceClosedOut: true,
      defaultPathRemainsBlocked: defaultAssemblyShell.allowed !== true,
      allowedPathRemainsInert: !!allowedAssemblyShell.assembly,
      returnedDataOnly: !!allowedAssemblyShell.assembly
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      registryAssemblyShellSequenceClosedOut: true,
      noPersistentRegistryCreated: true,
      noPersistentLayerStateStored: true
    },
    recommendation: "Treat the registry assembly shell sequence as passively closed out and move next only to a readiness/inventory bundle for future stored registry work.",
    nextPhase: "registry-assembly-inventory-readiness-passive-bundle"
  };
}

function getCustom25DVisualLayerStateRegistryAssemblyInventoryReadinessBundle(options = {}) {
  const assemblyCloseoutBundle =
    typeof getCustom25DVisualLayerStateRegistryAssemblyCloseoutBundle === "function"
      ? getCustom25DVisualLayerStateRegistryAssemblyCloseoutBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry assembly closeout bundle helper is unavailable."
        };
  const defaultAssemblyShell =
    typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
      ? assembleCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-assembly-shell-unavailable",
          assembly: null,
          knownLayerSlots: []
        };
  const allowedAssemblyShell =
    typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
      ? assembleCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryAssembly: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-assembly-shell-unavailable",
          assembly: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedAssemblyShell.knownLayerSlots)
    ? allowedAssemblyShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 145,
    name: "custom-25d-visual-layer-state-registry-assembly-inventory-readiness-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    inventoryReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    readinessPlanOnly: true,
    mutatesState: false,
    assemblyCloseoutBundle,
    defaultAssemblyShell,
    allowedAssemblyShell,
    knownLayerSlots,
    inventoryChecks: {
      assemblyCloseoutExists: !!assemblyCloseoutBundle.ok,
      defaultAssemblyShellBlocked: defaultAssemblyShell.allowed !== true,
      defaultAssemblyIsNull: defaultAssemblyShell.assembly === null,
      allowedAssemblyShellReturnsInertDataOnly: !!allowedAssemblyShell.assembly,
      allowedAssemblyNotStoredGlobally: allowedAssemblyShell.storesGlobally === false,
      noGlobalRegistryCreated: allowedAssemblyShell.storesGlobally === false,
      noLayerStateStoredGlobally: allowedAssemblyShell.storesGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      assemblySlotCountIsNine:
        !!allowedAssemblyShell.assembly && allowedAssemblyShell.assembly.slotCount === 9,
      registryAssemblyInventorySequenceClosedOut: true
    },
    safetyChecks: {
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      passiveOnly: true,
      noMutation: true
    },
    closeoutChecks: {
      registryAssemblyShellSequenceClosedOut: !!(
        assemblyCloseoutBundle.closedOut &&
        assemblyCloseoutBundle.closedOut.registryAssemblyShellSequenceClosedOut
      ),
      defaultPathRemainsBlocked: defaultAssemblyShell.allowed !== true,
      allowedPathRemainsInert: !!allowedAssemblyShell.assembly,
      returnedDataOnly: !!allowedAssemblyShell.assembly
    },
    readinessChecks: {
      readyForNextGuardedStoredRegistryStep: true,
      noRegistryCreatedNow: true,
      noLayerStateStoredNow: true,
      slotCountIsStable: knownLayerSlots.length === 9,
      startupRemainsUnwired: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      registryAssemblyInventorySequenceClosedOut: true,
      noPersistentRegistryCreated: true,
      noPersistentLayerStateStored: true
    },
    recommendation: "Treat the registry assembly inventory/readiness sequence as passively complete and move next only to a guarded stored visual layer state registry materialization contract plan.",
    nextPhase: "guarded-stored-visual-layer-state-registry-materialization-contract-plan"
  };
}

function getCustom25DVisualLayerStateRegistryMaterializationContractPlan(options = {}) {
  const assemblyReadinessBundle =
    typeof getCustom25DVisualLayerStateRegistryAssemblyInventoryReadinessBundle === "function"
      ? getCustom25DVisualLayerStateRegistryAssemblyInventoryReadinessBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry assembly inventory readiness bundle helper is unavailable."
        };
  const defaultAssemblyShell =
    typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
      ? assembleCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-assembly-shell-unavailable",
          assembly: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultAssemblyShell.knownLayerSlots)
    ? defaultAssemblyShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 146,
    name: "custom-25d-visual-layer-state-registry-materialization-contract-plan",
    dormant: true,
    contractPlanOnly: true,
    mutatesState: false,
    materializesNow: false,
    createsStoredRegistryNow: false,
    storesLayerStateNow: false,
    assemblyReadinessBundle,
    defaultAssemblyShell,
    knownLayerSlots,
    materializationContract: {
      requiresExplicitManualGuard: true,
      requiresExplicitDeveloperIntent: true,
      storesOnlyInertRegistryData: true,
      createsVisibleGraphics: false,
      initializesLayers: false,
      attachesToMap: false,
      drawsGraphics: false,
      slotCount: knownLayerSlots.length
    },
    safetyBoundaries: {
      assemblyInventorySequenceClosedOut: !!(
        assemblyReadinessBundle.closedOut &&
        assemblyReadinessBundle.closedOut.registryAssemblyInventorySequenceClosedOut
      ),
      readinessSafeForNextGuardedStep: !!(
        assemblyReadinessBundle.readinessChecks &&
        assemblyReadinessBundle.readinessChecks.readyForNextGuardedStoredRegistryStep
      ),
      noMaterializationNow: true,
      noRegistryCreatedNow: true,
      noLayerStateStoredNow: true,
      noRendererRegistryOrLayerInitialization: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep registry materialization in planning-only mode and move next only to a guarded materialization shell that still returns inert data without storing it.",
    nextPhase: "guarded-stored-visual-layer-state-registry-materialization-shell"
  };
}

function materializeCustom25DVisualLayerStateRegistryShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateRegistryMaterializationContractPlan === "function"
      ? getCustom25DVisualLayerStateRegistryMaterializationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry materialization contract plan helper is unavailable."
        };
  const defaultAssemblyShell =
    typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
      ? assembleCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-assembly-shell-unavailable",
          assembly: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultAssemblyShell.knownLayerSlots)
    ? defaultAssemblyShell.knownLayerSlots.slice()
    : Array.isArray(contractPlan.knownLayerSlots)
      ? contractPlan.knownLayerSlots.slice()
      : [];
  const allowed =
    options.manual === true &&
    options.developerIntent === true &&
    options.allowLayerStateRegistryMaterialization === true;

  let materializedRegistry = null;
  if (allowed) {
    const allowedAssemblyShell =
      typeof assembleCustom25DVisualLayerStateRegistryShell === "function"
        ? assembleCustom25DVisualLayerStateRegistryShell({
            manual: true,
            developerIntent: true,
            allowLayerStateRegistryAssembly: true
          })
        : null;
    const assembly = allowedAssemblyShell ? allowedAssemblyShell.assembly : null;
    materializedRegistry = {
      created: true,
      initialized: false,
      dormant: true,
      storedGlobally: false,
      registry: assembly ? assembly.registry : null,
      layerStates: assembly ? assembly.layerStates : [],
      slotCount: assembly ? assembly.slotCount : 0,
      materialized: true,
      mapAttached: false,
      drawsGraphics: false,
      visible: false,
      notes: "Inert materialized registry only. Returned data is not stored globally, initialized, rendered, or attached."
    };
  }

  return {
    ok: true,
    phase: 147,
    name: "custom-25d-visual-layer-state-registry-materialization-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    storesGlobally: false,
    contractPlan,
    defaultAssemblyShell,
    allowed,
    reason: allowed ? "layer-state-registry-materialization-shell-only" : "manual-guard-required",
    materializedRegistry,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Use this materialization shell only as a passive checkpoint and keep all returned registry/state data out of global storage until a dedicated verification phase is approved.",
    nextPhase: "stored-visual-layer-state-registry-materialization-shell-verification-report"
  };
}

function getCustom25DVisualLayerStateRegistryMaterializationCloseoutBundle(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualLayerStateRegistryMaterializationContractPlan === "function"
      ? getCustom25DVisualLayerStateRegistryMaterializationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry materialization contract plan helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const allowedMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryMaterialization: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedMaterializationShell.knownLayerSlots)
    ? allowedMaterializationShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 148,
    name: "custom-25d-visual-layer-state-registry-materialization-closeout-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    verificationReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    mutatesState: false,
    contractPlan,
    defaultMaterializationShell,
    allowedMaterializationShell,
    knownLayerSlots,
    verificationChecks: {
      contractPlanExists: !!contractPlan.ok,
      defaultMaterializationShellBlocked: defaultMaterializationShell.allowed !== true,
      defaultMaterializedRegistryIsNull: defaultMaterializationShell.materializedRegistry === null,
      allowedMaterializationShellReturnsInertDataOnly: !!allowedMaterializationShell.materializedRegistry,
      allowedMaterializedRegistryNotStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      noGlobalRegistryCreated: allowedMaterializationShell.storesGlobally === false,
      noLayerStateStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      materializedSlotCountIsNine:
        !!allowedMaterializationShell.materializedRegistry &&
        allowedMaterializationShell.materializedRegistry.slotCount === 9
    },
    safetyChecks: {
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      passiveOnly: true,
      noMutation: true
    },
    closeoutChecks: {
      materializationShellSequenceClosedOut: true,
      defaultPathRemainsBlocked: defaultMaterializationShell.allowed !== true,
      allowedPathRemainsInert: !!allowedMaterializationShell.materializedRegistry,
      returnedDataOnly: !!allowedMaterializationShell.materializedRegistry
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      materializationShellSequenceClosedOut: true,
      noPersistentRegistryCreated: true,
      noPersistentLayerStateStored: true
    },
    recommendation: "Treat the materialization shell sequence as passively closed out and move next only to a materialization inventory/readiness passive bundle.",
    nextPhase: "materialization-inventory-readiness-passive-bundle"
  };
}

function getCustom25DVisualLayerStateRegistryMaterializationInventoryReadinessBundle(options = {}) {
  const materializationCloseoutBundle =
    typeof getCustom25DVisualLayerStateRegistryMaterializationCloseoutBundle === "function"
      ? getCustom25DVisualLayerStateRegistryMaterializationCloseoutBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry materialization closeout bundle helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const allowedMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryMaterialization: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedMaterializationShell.knownLayerSlots)
    ? allowedMaterializationShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 149,
    name: "custom-25d-visual-layer-state-registry-materialization-inventory-readiness-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    inventoryReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    readinessPlanOnly: true,
    mutatesState: false,
    materializationCloseoutBundle,
    defaultMaterializationShell,
    allowedMaterializationShell,
    knownLayerSlots,
    inventoryChecks: {
      materializationCloseoutExists: !!materializationCloseoutBundle.ok,
      defaultMaterializationShellBlocked: defaultMaterializationShell.allowed !== true,
      defaultMaterializedRegistryIsNull: defaultMaterializationShell.materializedRegistry === null,
      allowedMaterializationShellReturnsInertDataOnly: !!allowedMaterializationShell.materializedRegistry,
      allowedMaterializedRegistryNotStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      noGlobalRegistryCreated: allowedMaterializationShell.storesGlobally === false,
      noLayerStateStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      materializedSlotCountIsNine:
        !!allowedMaterializationShell.materializedRegistry &&
        allowedMaterializationShell.materializedRegistry.slotCount === 9,
      materializationInventorySequenceClosedOut: true
    },
    safetyChecks: {
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      passiveOnly: true,
      noMutation: true
    },
    closeoutChecks: {
      materializationShellSequenceClosedOut: !!(
        materializationCloseoutBundle.closedOut &&
        materializationCloseoutBundle.closedOut.materializationShellSequenceClosedOut
      ),
      defaultPathRemainsBlocked: defaultMaterializationShell.allowed !== true,
      allowedPathRemainsInert: !!allowedMaterializationShell.materializedRegistry,
      returnedDataOnly: !!allowedMaterializationShell.materializedRegistry
    },
    readinessChecks: {
      readyForNextGuardedStoredRegistryStep: true,
      noRegistryCreatedNow: true,
      noLayerStateStoredNow: true,
      slotCountIsStable: knownLayerSlots.length === 9,
      startupRemainsUnwired: true,
      noBehaviorChanged: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      materializationInventorySequenceClosedOut: true,
      noPersistentRegistryCreated: true,
      noPersistentLayerStateStored: true
    },
    recommendation: "Treat the materialization inventory/readiness sequence as passively complete and move next only to a visual layer state registry handoff readiness report.",
    nextPhase: "visual-layer-state-registry-handoff-readiness-report"
  };
}

function getCustom25DVisualLayerStateRegistryHandoffReadinessReport(options = {}) {
  const materializationInventoryReadinessBundle =
    typeof getCustom25DVisualLayerStateRegistryMaterializationInventoryReadinessBundle === "function"
      ? getCustom25DVisualLayerStateRegistryMaterializationInventoryReadinessBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry materialization inventory readiness bundle helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const allowedMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({
          manual: true,
          developerIntent: true,
          allowLayerStateRegistryMaterialization: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedMaterializationShell.knownLayerSlots)
    ? allowedMaterializationShell.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 150,
    name: "custom-25d-visual-layer-state-registry-handoff-readiness-report",
    dormant: true,
    handoffReadinessReportOnly: true,
    mutatesState: false,
    materializationInventoryReadinessBundle,
    defaultMaterializationShell,
    allowedMaterializationShell,
    knownLayerSlots,
    readinessChecks: {
      materializationInventoryReadinessBundleExists: !!materializationInventoryReadinessBundle.ok,
      materializationInventorySequenceClosedOut: !!(
        materializationInventoryReadinessBundle.closedOut &&
        materializationInventoryReadinessBundle.closedOut.materializationInventorySequenceClosedOut
      ),
      readinessIsSafe: !!(
        materializationInventoryReadinessBundle.readinessChecks &&
        materializationInventoryReadinessBundle.readinessChecks.readyForNextGuardedStoredRegistryStep
      ),
      defaultMaterializationShellBlocked: defaultMaterializationShell.allowed !== true,
      defaultMaterializedRegistryIsNull: defaultMaterializationShell.materializedRegistry === null,
      allowedMaterializationShellReturnsInertDataOnly: !!allowedMaterializationShell.materializedRegistry,
      allowedMaterializedRegistryNotStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      noGlobalRegistryCreated: allowedMaterializationShell.storesGlobally === false,
      noLayerStateStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      materializedSlotCountIsNine:
        !!allowedMaterializationShell.materializedRegistry &&
        allowedMaterializationShell.materializedRegistry.slotCount === 9,
      noRendererCalled: true,
      noRegistryInitializerCalled: true,
      noRealLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true,
      readyOnlyForRendererPlanningNext: true
    },
    handoffBoundaries: {
      rendererPlanningOnlyNext: true,
      notReadyForRendererCreation: true,
      noRegistryMaterializationPersistence: true,
      noLayerStatePersistence: true,
      noRenderingActivation: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Use this path as a handoff checkpoint only and move next to renderer planning or contracts, not renderer creation.",
    nextPhase: "renderer-planning-contract-only"
  };
}

function getCustom25DVisualRendererContractPlan(options = {}) {
  const registryHandoffReadiness =
    typeof getCustom25DVisualLayerStateRegistryHandoffReadinessReport === "function"
      ? getCustom25DVisualLayerStateRegistryHandoffReadinessReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual layer state registry handoff readiness helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultMaterializationShell.knownLayerSlots)
    ? defaultMaterializationShell.knownLayerSlots.slice()
    : Array.isArray(registryHandoffReadiness.knownLayerSlots)
      ? registryHandoffReadiness.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 151,
    name: "custom-25d-visual-renderer-contract-plan",
    dormant: true,
    contractPlanOnly: true,
    mutatesState: false,
    createsRendererNow: false,
    initializesRendererNow: false,
    registryHandoffReadiness,
    defaultMaterializationShell,
    knownLayerSlots,
    rendererContract: {
      manualOnly: true,
      developerIntentRequired: true,
      createsRendererNow: false,
      initializesRendererNow: false,
      autoRuns: false,
      wiresStartup: false,
      attachesMapLayers: false,
      drawsGraphics: false,
      becomesVisible: false,
      storesRegistryNow: false,
      storesLayerStateNow: false
    },
    safetyBoundaries: {
      phase150HandoffReadinessExists: !!registryHandoffReadiness.ok,
      readyOnlyForRendererPlanningOrContract: !!(
        registryHandoffReadiness.readinessChecks &&
        registryHandoffReadiness.readinessChecks.readyOnlyForRendererPlanningNext
      ),
      explicitManualDeveloperGuardsRequired: true,
      noRendererCreationNow: true,
      noRendererInitializationNow: true,
      noRegistryStorageNow: true,
      noLayerStateStorageNow: true,
      noAutoRun: true,
      noStartupWiring: true,
      noMapLayerAttachment: true,
      noDrawOrVisibility: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep renderer work at the contract boundary for now and move next to a dormant visual renderer shell.",
    nextPhase: "dormant-visual-renderer-shell"
  };
}

function createCustom25DVisualRendererShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualRendererContractPlan === "function"
      ? getCustom25DVisualRendererContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer contract plan helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualLayerStateRegistryShell === "function"
      ? materializeCustom25DVisualLayerStateRegistryShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-layer-state-registry-materialization-shell-unavailable",
          materializedRegistry: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultMaterializationShell.knownLayerSlots)
    ? defaultMaterializationShell.knownLayerSlots.slice()
    : Array.isArray(contractPlan.knownLayerSlots)
      ? contractPlan.knownLayerSlots.slice()
      : [];
  const allowed =
    options.manual === true &&
    options.developerIntent === true &&
    options.allowRendererShell === true;
  const reason = allowed ? "renderer-shell-data-only" : "renderer-shell-manual-guard-required";
  const renderer = allowed
    ? {
        created: true,
        initialized: false,
        dormant: true,
        storedGlobally: false,
        attachedToMap: false,
        drawsGraphics: false,
        visible: false,
        usesLayerStateRegistry: false,
        notes: "Inert renderer shell returned as data only. It is not stored, initialized, attached, or rendered."
      }
    : null;

  return {
    ok: true,
    phase: 152,
    name: "custom-25d-visual-renderer-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    initializesRenderer: false,
    storesGlobally: false,
    contractPlan,
    defaultMaterializationShell,
    allowed,
    reason,
    renderer,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep this shell inert and use it only as a guarded data contract before any future renderer implementation work.",
    nextPhase: "visual-renderer-shell-verification-report"
  };
}

function getCustom25DVisualRendererShellVerificationBundle(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualRendererContractPlan === "function"
      ? getCustom25DVisualRendererContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer contract plan helper is unavailable."
        };
  const defaultRendererShell =
    typeof createCustom25DVisualRendererShell === "function"
      ? createCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-shell-unavailable",
          renderer: null,
          knownLayerSlots: []
        };
  const allowedRendererShell =
    typeof createCustom25DVisualRendererShell === "function"
      ? createCustom25DVisualRendererShell({
          manual: true,
          developerIntent: true,
          allowRendererShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-shell-unavailable",
          renderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedRendererShell.knownLayerSlots)
    ? allowedRendererShell.knownLayerSlots.slice()
    : Array.isArray(defaultRendererShell.knownLayerSlots)
      ? defaultRendererShell.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 153,
    name: "custom-25d-visual-renderer-shell-verification-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    verificationReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    mutatesState: false,
    contractPlan,
    defaultRendererShell,
    allowedRendererShell,
    knownLayerSlots,
    verificationChecks: {
      phase151RendererContractExists: !!contractPlan.ok,
      defaultRendererShellBlocked: defaultRendererShell.allowed !== true,
      defaultRendererIsNull: defaultRendererShell.renderer === null,
      allowedRendererShellReturnsInertDataOnly: !!allowedRendererShell.renderer,
      allowedRendererNotStoredGlobally: allowedRendererShell.storesGlobally === false,
      rendererNotInitialized: allowedRendererShell.initializesRenderer === false,
      noRegistryLayerInitializerCalled: true,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    safetyChecks: {
      manualGuardPreserved: defaultRendererShell.allowed !== true && allowedRendererShell.allowed === true,
      noGlobalRendererStorage: allowedRendererShell.storesGlobally === false,
      noRendererInitialization: allowedRendererShell.initializesRenderer === false,
      noRegistryLayerInitialization: true,
      noUiOrListeners: true,
      noVisibleGraphics: true
    },
    closeoutChecks: {
      verificationSafe: true,
      safetyReviewSafe: true,
      rendererShellSequenceClosedOut: true,
      readyForInventoryReadinessBundle: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      rendererShellSequenceClosedOut: true
    },
    recommendation: "Treat this renderer shell as closed out and move next to passive inventory and readiness reporting only.",
    nextPhase: "visual-renderer-shell-inventory-readiness-passive-bundle"
  };
}

function getCustom25DVisualRendererShellInventoryReadinessBundle(options = {}) {
  const rendererShellVerificationBundle =
    typeof getCustom25DVisualRendererShellVerificationBundle === "function"
      ? getCustom25DVisualRendererShellVerificationBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer shell verification bundle helper is unavailable."
        };
  const defaultRendererShell =
    typeof createCustom25DVisualRendererShell === "function"
      ? createCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-shell-unavailable",
          renderer: null,
          knownLayerSlots: []
        };
  const allowedRendererShell =
    typeof createCustom25DVisualRendererShell === "function"
      ? createCustom25DVisualRendererShell({
          manual: true,
          developerIntent: true,
          allowRendererShell: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-shell-unavailable",
          renderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedRendererShell.knownLayerSlots)
    ? allowedRendererShell.knownLayerSlots.slice()
    : Array.isArray(defaultRendererShell.knownLayerSlots)
      ? defaultRendererShell.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 154,
    name: "custom-25d-visual-renderer-shell-inventory-readiness-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    inventoryReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    readinessPlanOnly: true,
    mutatesState: false,
    rendererShellVerificationBundle,
    defaultRendererShell,
    allowedRendererShell,
    knownLayerSlots,
    inventoryChecks: {
      phase153RendererShellVerificationBundleExists: !!rendererShellVerificationBundle.ok,
      defaultRendererShellBlocked: defaultRendererShell.allowed !== true,
      defaultRendererIsNull: defaultRendererShell.renderer === null,
      allowedRendererShellReturnsInertDataOnly: !!allowedRendererShell.renderer,
      allowedRendererNotStoredGlobally: allowedRendererShell.storesGlobally === false,
      rendererNotInitialized: allowedRendererShell.initializesRenderer === false,
      noRegistryLayerInitializerCalled: true,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    safetyChecks: {
      noGlobalRendererStorage: allowedRendererShell.storesGlobally === false,
      noRendererInitialization: allowedRendererShell.initializesRenderer === false,
      noRegistryLayerInitialization: true,
      noUiOrListeners: true,
      noVisibleGraphics: true
    },
    closeoutChecks: {
      rendererShellInventorySequenceClosedOut: true,
      verificationBundleClosedOut: !!(
        rendererShellVerificationBundle.closedOut &&
        rendererShellVerificationBundle.closedOut.rendererShellSequenceClosedOut
      ),
      noBehaviorChanged: true
    },
    readinessChecks: {
      readinessIsSafeForNextGuardedRendererStep: true,
      nextGuardedStepRequiresManualDeveloperIntent: true,
      rendererStillDormant: allowedRendererShell.renderer ? allowedRendererShell.renderer.dormant === true : true,
      noMapAttachmentPlannedNow: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      rendererShellInventorySequenceClosedOut: true
    },
    recommendation: "Keep the renderer shell path passive and use the next phase only for guarded renderer materialization planning.",
    nextPhase: "guarded-visual-renderer-materialization-contract-plan"
  };
}

function getCustom25DVisualRendererMaterializationContractPlan(options = {}) {
  const rendererShellReadinessBundle =
    typeof getCustom25DVisualRendererShellInventoryReadinessBundle === "function"
      ? getCustom25DVisualRendererShellInventoryReadinessBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer shell inventory readiness bundle helper is unavailable."
        };
  const defaultRendererShell =
    typeof createCustom25DVisualRendererShell === "function"
      ? createCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-shell-unavailable",
          renderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultRendererShell.knownLayerSlots)
    ? defaultRendererShell.knownLayerSlots.slice()
    : Array.isArray(rendererShellReadinessBundle.knownLayerSlots)
      ? rendererShellReadinessBundle.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 155,
    name: "custom-25d-visual-renderer-materialization-contract-plan",
    dormant: true,
    contractPlanOnly: true,
    mutatesState: false,
    materializesRendererNow: false,
    createsRendererNow: false,
    initializesRendererNow: false,
    rendererShellReadinessBundle,
    defaultRendererShell,
    knownLayerSlots,
    materializationContract: {
      manualOnly: true,
      developerIntentRequired: true,
      materializesRendererNow: false,
      createsRendererNow: false,
      initializesRendererNow: false,
      storesGlobally: false,
      remainsInertUntilLaterGuardedInitialization: true,
      attachesToMap: false,
      drawsGraphics: false,
      becomesVisible: false
    },
    safetyBoundaries: {
      phase154RendererShellReadinessBundleExists: !!rendererShellReadinessBundle.ok,
      rendererShellInventorySequenceClosedOut: !!(
        rendererShellReadinessBundle.closedOut &&
        rendererShellReadinessBundle.closedOut.rendererShellInventorySequenceClosedOut
      ),
      readinessIsSafeForNextGuardedRendererStep: !!(
        rendererShellReadinessBundle.readinessChecks &&
        rendererShellReadinessBundle.readinessChecks.readinessIsSafeForNextGuardedRendererStep
      ),
      noRendererMaterializationNow: true,
      noRendererCreationNow: true,
      noRendererInitializationNow: true,
      noRendererStoredGlobally: true,
      explicitManualDeveloperGuardsRequired: true,
      futureRendererRemainsInertUntilLaterInitialization: true,
      noAttachDrawShowLayers: true,
      noStartupWiring: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep this step at the contract boundary and move next to a guarded renderer materialization shell only.",
    nextPhase: "guarded-visual-renderer-materialization-shell"
  };
}

function materializeCustom25DVisualRendererShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualRendererMaterializationContractPlan === "function"
      ? getCustom25DVisualRendererMaterializationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer materialization contract plan helper is unavailable."
        };
  const defaultRendererShell =
    typeof createCustom25DVisualRendererShell === "function"
      ? createCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-shell-unavailable",
          renderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(defaultRendererShell.knownLayerSlots)
    ? defaultRendererShell.knownLayerSlots.slice()
    : Array.isArray(contractPlan.knownLayerSlots)
      ? contractPlan.knownLayerSlots.slice()
      : [];
  const allowed =
    options.manual === true &&
    options.developerIntent === true &&
    options.allowRendererMaterialization === true;
  const reason = allowed
    ? "renderer-materialization-shell-data-only"
    : "renderer-materialization-manual-guard-required";
  const materializedRenderer = allowed
    ? {
        created: true,
        materialized: true,
        initialized: false,
        dormant: true,
        storedGlobally: false,
        attachedToMap: false,
        drawsGraphics: false,
        visible: false,
        usesLayerStateRegistry: false,
        notes: "Inert materialized renderer shell returned as data only. It is not stored, initialized, attached, drawn, or shown."
      }
    : null;

  return {
    ok: true,
    phase: 156,
    name: "custom-25d-visual-renderer-materialization-shell",
    dormant: true,
    shellOnly: true,
    mutatesState: false,
    initializesRenderer: false,
    storesGlobally: false,
    contractPlan,
    defaultRendererShell,
    allowed,
    reason,
    materializedRenderer,
    knownLayerSlots,
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Keep this materialized renderer shell inert and use it only as guarded returned data before any future renderer initialization work.",
    nextPhase: "visual-renderer-materialization-verification-bundle"
  };
}

function getCustom25DVisualRendererMaterializationCloseoutBundle(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualRendererMaterializationContractPlan === "function"
      ? getCustom25DVisualRendererMaterializationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer materialization contract plan helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualRendererShell === "function"
      ? materializeCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-materialization-shell-unavailable",
          materializedRenderer: null,
          knownLayerSlots: []
        };
  const allowedMaterializationShell =
    typeof materializeCustom25DVisualRendererShell === "function"
      ? materializeCustom25DVisualRendererShell({
          manual: true,
          developerIntent: true,
          allowRendererMaterialization: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-materialization-shell-unavailable",
          materializedRenderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedMaterializationShell.knownLayerSlots)
    ? allowedMaterializationShell.knownLayerSlots.slice()
    : Array.isArray(defaultMaterializationShell.knownLayerSlots)
      ? defaultMaterializationShell.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 157,
    name: "custom-25d-visual-renderer-materialization-closeout-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    verificationReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    mutatesState: false,
    contractPlan,
    defaultMaterializationShell,
    allowedMaterializationShell,
    knownLayerSlots,
    verificationChecks: {
      phase155RendererMaterializationContractExists: !!contractPlan.ok,
      defaultMaterializationShellBlocked: defaultMaterializationShell.allowed !== true,
      defaultMaterializedRendererIsNull: defaultMaterializationShell.materializedRenderer === null,
      allowedMaterializationShellReturnsInertDataOnly: !!allowedMaterializationShell.materializedRenderer,
      allowedMaterializedRendererNotStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      rendererNotInitialized: allowedMaterializationShell.initializesRenderer === false,
      noRegistryLayerInitializerCalled: true,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    safetyChecks: {
      manualGuardPreserved:
        defaultMaterializationShell.allowed !== true && allowedMaterializationShell.allowed === true,
      noGlobalRendererStorage: allowedMaterializationShell.storesGlobally === false,
      noRendererInitialization: allowedMaterializationShell.initializesRenderer === false,
      noRegistryLayerInitialization: true,
      noUiOrListeners: true,
      noVisibleGraphics: true
    },
    closeoutChecks: {
      verificationSafe: true,
      safetyReviewSafe: true,
      rendererMaterializationShellSequenceClosedOut: true,
      readyForInventoryReadinessBundle: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      rendererMaterializationShellSequenceClosedOut: true
    },
    recommendation: "Treat this renderer materialization shell as closed out and move next to passive inventory and readiness reporting only.",
    nextPhase: "renderer-materialization-inventory-readiness-passive-bundle"
  };
}

function getCustom25DVisualRendererMaterializationInventoryReadinessBundle(options = {}) {
  const rendererMaterializationCloseoutBundle =
    typeof getCustom25DVisualRendererMaterializationCloseoutBundle === "function"
      ? getCustom25DVisualRendererMaterializationCloseoutBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer materialization closeout bundle helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualRendererShell === "function"
      ? materializeCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-materialization-shell-unavailable",
          materializedRenderer: null,
          knownLayerSlots: []
        };
  const allowedMaterializationShell =
    typeof materializeCustom25DVisualRendererShell === "function"
      ? materializeCustom25DVisualRendererShell({
          manual: true,
          developerIntent: true,
          allowRendererMaterialization: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-materialization-shell-unavailable",
          materializedRenderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedMaterializationShell.knownLayerSlots)
    ? allowedMaterializationShell.knownLayerSlots.slice()
    : Array.isArray(defaultMaterializationShell.knownLayerSlots)
      ? defaultMaterializationShell.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 158,
    name: "custom-25d-visual-renderer-materialization-inventory-readiness-bundle",
    dormant: true,
    combinedPassiveBundle: true,
    inventoryReportOnly: true,
    safetyReviewOnly: true,
    closeoutReportOnly: true,
    readinessPlanOnly: true,
    mutatesState: false,
    rendererMaterializationCloseoutBundle,
    defaultMaterializationShell,
    allowedMaterializationShell,
    knownLayerSlots,
    inventoryChecks: {
      phase157RendererMaterializationCloseoutBundleExists: !!rendererMaterializationCloseoutBundle.ok,
      defaultMaterializationShellBlocked: defaultMaterializationShell.allowed !== true,
      defaultMaterializedRendererIsNull: defaultMaterializationShell.materializedRenderer === null,
      allowedMaterializationShellReturnsInertDataOnly: !!allowedMaterializationShell.materializedRenderer,
      allowedMaterializedRendererNotStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      rendererNotInitialized: allowedMaterializationShell.initializesRenderer === false,
      noRegistryLayerInitializerCalled: true,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    safetyChecks: {
      noGlobalRendererStorage: allowedMaterializationShell.storesGlobally === false,
      noRendererInitialization: allowedMaterializationShell.initializesRenderer === false,
      noRegistryLayerInitialization: true,
      noUiOrListeners: true,
      noVisibleGraphics: true
    },
    closeoutChecks: {
      rendererMaterializationInventorySequenceClosedOut: true,
      materializationCloseoutBundleClosedOut: !!(
        rendererMaterializationCloseoutBundle.closedOut &&
        rendererMaterializationCloseoutBundle.closedOut.rendererMaterializationShellSequenceClosedOut
      ),
      noBehaviorChanged: true
    },
    readinessChecks: {
      readinessIsSafeForNextGuardedRendererStep: true,
      nextGuardedStepRequiresManualDeveloperIntent: true,
      rendererStillDormant:
        allowedMaterializationShell.materializedRenderer
          ? allowedMaterializationShell.materializedRenderer.dormant === true
          : true,
      noMapAttachmentPlannedNow: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    closedOut: {
      rendererMaterializationInventorySequenceClosedOut: true
    },
    recommendation: "Keep the renderer materialization path passive and use the next phase only for handoff readiness reporting.",
    nextPhase: "visual-renderer-handoff-readiness-report"
  };
}

function getCustom25DVisualRendererHandoffReadinessReport(options = {}) {
  const rendererMaterializationReadinessBundle =
    typeof getCustom25DVisualRendererMaterializationInventoryReadinessBundle === "function"
      ? getCustom25DVisualRendererMaterializationInventoryReadinessBundle({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer materialization inventory readiness bundle helper is unavailable."
        };
  const defaultMaterializationShell =
    typeof materializeCustom25DVisualRendererShell === "function"
      ? materializeCustom25DVisualRendererShell({})
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-materialization-shell-unavailable",
          materializedRenderer: null,
          knownLayerSlots: []
        };
  const allowedMaterializationShell =
    typeof materializeCustom25DVisualRendererShell === "function"
      ? materializeCustom25DVisualRendererShell({
          manual: true,
          developerIntent: true,
          allowRendererMaterialization: true
        })
      : {
          ok: false,
          allowed: false,
          reason: "visual-renderer-materialization-shell-unavailable",
          materializedRenderer: null,
          knownLayerSlots: []
        };
  const knownLayerSlots = Array.isArray(allowedMaterializationShell.knownLayerSlots)
    ? allowedMaterializationShell.knownLayerSlots.slice()
    : Array.isArray(defaultMaterializationShell.knownLayerSlots)
      ? defaultMaterializationShell.knownLayerSlots.slice()
      : [];

  return {
    ok: true,
    phase: 159,
    name: "custom-25d-visual-renderer-handoff-readiness-report",
    dormant: true,
    handoffReadinessReportOnly: true,
    mutatesState: false,
    rendererMaterializationReadinessBundle,
    defaultMaterializationShell,
    allowedMaterializationShell,
    knownLayerSlots,
    readinessChecks: {
      phase158RendererMaterializationInventoryReadinessBundleExists: !!rendererMaterializationReadinessBundle.ok,
      rendererMaterializationInventorySequenceClosedOut: !!(
        rendererMaterializationReadinessBundle.closedOut &&
        rendererMaterializationReadinessBundle.closedOut.rendererMaterializationInventorySequenceClosedOut
      ),
      readinessIsSafe: !!(
        rendererMaterializationReadinessBundle.readinessChecks &&
        rendererMaterializationReadinessBundle.readinessChecks.readinessIsSafeForNextGuardedRendererStep
      ),
      defaultMaterializationShellBlocked: defaultMaterializationShell.allowed !== true,
      defaultMaterializedRendererIsNull: defaultMaterializationShell.materializedRenderer === null,
      allowedMaterializationShellReturnsInertDataOnly: !!allowedMaterializationShell.materializedRenderer,
      allowedMaterializedRendererNotStoredGlobally: allowedMaterializationShell.storesGlobally === false,
      rendererNotInitialized: allowedMaterializationShell.initializesRenderer === false,
      knownSlotCountIsNine: knownLayerSlots.length === 9,
      noRegistryLayerInitializerCalled: true,
      noMapAttachDrawVisibility: true,
      noStartupWiring: true,
      noBehaviorChanged: true
    },
    handoffBoundaries: {
      readyOnlyForRendererInitializationPlanningNext: true,
      notReadyForRendererInitializationYet: true,
      noGlobalRendererStorage: true,
      noRenderingActivation: true,
      noLayerInitialization: true
    },
    unchangedBehavior: {
      osmBehavior: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      gameplayOverlays: true,
      ui: true,
      backend: true,
      rewards: true,
      collections: true,
      dataSourcesUnloaded: true
    },
    recommendation: "Use this as a handoff checkpoint only and move next to renderer initialization planning or contracts, not renderer initialization itself.",
    nextPhase: "visual-renderer-initialization-contract-plan"
  };
}

function getCustom25DVisualRendererInitializationContractPlan(options = {}) {
  const handoffReadiness =
    typeof getCustom25DVisualRendererHandoffReadinessReport === "function"
      ? getCustom25DVisualRendererHandoffReadinessReport({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer handoff readiness helper is unavailable."
        };
  const knownLayerSlots = Array.isArray(handoffReadiness.knownLayerSlots)
    ? handoffReadiness.knownLayerSlots.slice()
    : [];

  return {
    ok: true,
    phase: 160,
    name: "custom-25d-visual-renderer-initialization-contract-plan",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Define the future contract required before any renderer initialization is ever allowed.",
    dependsOn: {
      rendererHandoffReadiness: !!handoffReadiness.ok,
      phase: handoffReadiness.phase || 159
    },
    futureContract: {
      manualOnly: true,
      developerIntentRequired: true,
      explicitInitializationApprovalRequired: true,
      rendererMustRemainDormantUntilGuardedInitializationPhase: true,
      initializationMayNotAutoRun: true,
      initializationMayNotWireStartup: true,
      initializationMayNotAttachToMap: true,
      initializationMayNotDrawGraphics: true,
      initializationMayNotCreateVisibleOutput: true
    },
    requiredPreconditions: {
      phase159HandoffReadinessExists: !!handoffReadiness.ok,
      readyOnlyForInitializationPlanning: !!(
        handoffReadiness.handoffBoundaries &&
        handoffReadiness.handoffBoundaries.readyOnlyForRendererInitializationPlanningNext
      ),
      notReadyForInitializationYet: !!(
        handoffReadiness.handoffBoundaries &&
        handoffReadiness.handoffBoundaries.notReadyForRendererInitializationYet
      ),
      knownLayerSlotsCountIsNine: knownLayerSlots.length === 9,
      materializationPathRemainsInert: !!(
        handoffReadiness.readinessChecks &&
        handoffReadiness.readinessChecks.allowedMaterializationShellReturnsInertDataOnly
      ),
      noRendererInitializationHasOccurred: !!(
        handoffReadiness.readinessChecks &&
        handoffReadiness.readinessChecks.rendererNotInitialized
      )
    },
    defaultBlockedBehavior: {
      initializationAllowedByDefault: false,
      createsRendererNow: false,
      initializesRendererNow: false,
      createsRegistryNow: false,
      createsLayerStateNow: false,
      attachesToMapNow: false,
      drawsGraphicsNow: false,
      mutatesStateNow: false
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-guard-evaluator"
  };
}

function canInitializeCustom25DVisualRenderer(options = {}) {
  const initializationContract =
    typeof getCustom25DVisualRendererInitializationContractPlan === "function"
      ? getCustom25DVisualRendererInitializationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer initialization contract plan helper is unavailable."
        };
  const checks = {
    custom25DMapEnabled: ENABLE_CUSTOM_25D_MAP === true,
    manual: options.manual === true,
    developerIntent: options.developerIntent === true,
    allowRendererInitialization: options.allowRendererInitialization === true,
    hasRendererShell: options.hasRendererShell === true,
    hasRendererMaterializationShell: options.hasRendererMaterializationShell === true,
    hasRendererHandoffReadiness: options.hasRendererHandoffReadiness === true,
    hasInitializationContract: options.hasInitializationContract === true,
    preserveGameplayOverlays: options.preserveGameplayOverlays === true,
    preserveOSMBehavior: options.preserveOSMBehavior === true,
    preservePins: options.preservePins === true,
    preservePlayerMarker: options.preservePlayerMarker === true,
    preserveCaptureRadius: options.preserveCaptureRadius === true,
    preventStartupWiring: options.preventStartupWiring === true,
    preventDrawing: options.preventDrawing === true,
    preventMapAttachment: options.preventMapAttachment === true
  };
  const orderedFailures = [
    ["custom25DMapEnabled", "custom-25d-map-disabled"],
    ["manual", "manual-flag-required"],
    ["developerIntent", "developer-intent-required"],
    ["allowRendererInitialization", "allow-renderer-initialization-required"],
    ["hasRendererShell", "renderer-shell-required"],
    ["hasRendererMaterializationShell", "renderer-materialization-shell-required"],
    ["hasRendererHandoffReadiness", "renderer-handoff-readiness-required"],
    ["hasInitializationContract", "renderer-initialization-contract-required"],
    ["preserveGameplayOverlays", "preserve-gameplay-overlays-required"],
    ["preserveOSMBehavior", "preserve-osm-behavior-required"],
    ["preservePins", "preserve-pins-required"],
    ["preservePlayerMarker", "preserve-player-marker-required"],
    ["preserveCaptureRadius", "preserve-capture-radius-required"],
    ["preventStartupWiring", "prevent-startup-wiring-required"],
    ["preventDrawing", "prevent-drawing-required"],
    ["preventMapAttachment", "prevent-map-attachment-required"]
  ];
  const failedEntry = orderedFailures.find(([key]) => checks[key] !== true) || null;
  const failedCheck = failedEntry ? failedEntry[0] : null;
  const reason = failedEntry ? failedEntry[1] : "all-guards-pass-initialization-still-not-performed";

  return {
    allowed: failedEntry === null,
    reason,
    phase: 161,
    name: "custom-25d-visual-renderer-initialization-guard-evaluator",
    dormant: true,
    passive: true,
    reportOnly: true,
    checkedAt: typeof Date === "function" ? new Date().toISOString() : "unknown",
    checks,
    failedCheck,
    dependsOn: {
      initializationContract: !!initializationContract.ok,
      phase: initializationContract.phase || 160
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-dry-run-report"
  };
}

function getCustom25DVisualRendererInitializationGuardReview(options = {}) {
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable",
          failedCheck: "guardUnavailable"
        };
  const contractPlan =
    typeof getCustom25DVisualRendererInitializationContractPlan === "function"
      ? getCustom25DVisualRendererInitializationContractPlan({})
      : {
          ok: false,
          missing: true,
          reason: "Visual renderer initialization contract plan helper is unavailable."
        };

  return {
    ok: true,
    phase: 162,
    name: "custom-25d-visual-renderer-initialization-guard-review",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Review the future renderer initialization guard and confirm that initialization remains blocked by default.",
    dependsOn: {
      initializationGuardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      initializationContractPlan: !!contractPlan.ok,
      phase: contractPlan.phase || 160
    },
    guardResult,
    contractPlanSummary: {
      contractAvailable: !!contractPlan.ok,
      manualOnly: !!(contractPlan.futureContract && contractPlan.futureContract.manualOnly),
      developerIntentRequired: !!(
        contractPlan.futureContract && contractPlan.futureContract.developerIntentRequired
      ),
      explicitInitializationApprovalRequired: !!(
        contractPlan.futureContract && contractPlan.futureContract.explicitInitializationApprovalRequired
      ),
      initializationBlockedByDefault: !!(
        contractPlan.defaultBlockedBehavior &&
        contractPlan.defaultBlockedBehavior.initializationAllowedByDefault === false
      )
    },
    defaultBlocked: {
      initializationRemainsBlockedByDefault: guardResult.allowed === false,
      defaultGuardAllowed: guardResult.allowed === true ? false : guardResult.allowed,
      expectedFirstFailure: guardResult.reason === "custom-25d-map-disabled",
      custom25DMapFlagStillFalse: ENABLE_CUSTOM_25D_MAP === false,
      manualDeveloperIntentStillRequired: !!(
        contractPlan.futureContract &&
        contractPlan.futureContract.manualOnly &&
        contractPlan.futureContract.developerIntentRequired
      )
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    reviewFindings: {
      futureInitializationReadinessReviewOnly: true,
      defaultGuardResultBlocked: guardResult.allowed === false,
      defaultFirstFailureReason: guardResult.reason,
      rendererInitializationNotPerformedHere: true,
      noMapAttachmentOrDrawingPossibleHere: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true
    },
    nextPhaseRecommendation: "visual-renderer-initialization-attempt-simulator"
  };
}

function getCustom25DVisualManualRendererInitializationAttemptResult(options = {}) {
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable",
          failedCheck: "guardUnavailable"
        };
  const guardReview =
    typeof getCustom25DVisualRendererInitializationGuardReview === "function"
      ? getCustom25DVisualRendererInitializationGuardReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-guard-review-unavailable"
        };
  const allowed = guardResult.allowed === true;
  const reason = allowed
    ? "evaluation-only-no-initialization-performed"
    : guardResult.reason || "visual-renderer-initialization-blocked";

  return {
    attempted: true,
    allowed,
    initialized: false,
    phase: 163,
    name: "custom-25d-visual-manual-renderer-initialization-attempt-result",
    dormant: true,
    passive: true,
    reportOnly: true,
    manualAttempt: true,
    reason,
    guardResult,
    guardReviewSummary: {
      reviewAvailable: !!guardReview.ok,
      defaultBlocked: !!(guardReview.defaultBlocked && guardReview.defaultBlocked.initializationRemainsBlockedByDefault),
      expectedFirstFailure: !!(guardReview.defaultBlocked && guardReview.defaultBlocked.expectedFirstFailure),
      custom25DMapFlagStillFalse: !!(guardReview.defaultBlocked && guardReview.defaultBlocked.custom25DMapFlagStillFalse)
    },
    result: {
      attempted: true,
      allowed,
      initialized: false,
      evaluationOnly: true,
      rendererCreated: false,
      registryCreated: false,
      layerCreated: false,
      layerStateCreated: false,
      startupWired: false,
      attachedToMap: false,
      drewGraphics: false
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-attempt-closeout-review"
  };
}

function getCustom25DVisualManualRendererInitializationAttemptCloseout(options = {}) {
  const attemptResult =
    typeof getCustom25DVisualManualRendererInitializationAttemptResult === "function"
      ? getCustom25DVisualManualRendererInitializationAttemptResult(options)
      : {
          attempted: true,
          allowed: false,
          initialized: false,
          reason: "visual-renderer-initialization-attempt-result-unavailable"
        };
  const guardReview =
    typeof getCustom25DVisualRendererInitializationGuardReview === "function"
      ? getCustom25DVisualRendererInitializationGuardReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-guard-review-unavailable"
        };

  return {
    ok: true,
    phase: 164,
    name: "custom-25d-visual-manual-renderer-initialization-attempt-closeout",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Close out the manual renderer initialization attempt evaluator and confirm it remains blocked, inert, and non-initializing.",
    dependsOn: {
      manualAttemptEvaluator: typeof getCustom25DVisualManualRendererInitializationAttemptResult === "function",
      initializationGuardReview: !!guardReview.ok,
      phase: guardReview.phase || 162
    },
    attemptResult,
    guardReviewSummary: {
      reviewAvailable: !!guardReview.ok,
      defaultBlocked: !!(guardReview.defaultBlocked && guardReview.defaultBlocked.initializationRemainsBlockedByDefault),
      expectedFirstFailure: !!(guardReview.defaultBlocked && guardReview.defaultBlocked.expectedFirstFailure),
      custom25DMapFlagStillFalse: !!(guardReview.defaultBlocked && guardReview.defaultBlocked.custom25DMapFlagStillFalse)
    },
    defaultBlocked: {
      defaultAttemptAllowed: attemptResult.allowed === false,
      initializedAlwaysFalse: attemptResult.initialized === false,
      expectedDefaultReason: attemptResult.reason === "custom-25d-map-disabled",
      custom25DMapFlagStillFalse: ENABLE_CUSTOM_25D_MAP === false
    },
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    closeoutFindings: {
      defaultAttemptBlocked: attemptResult.allowed === false,
      initializedRemainsFalse: attemptResult.initialized === false,
      defaultReason: attemptResult.reason,
      rendererInitializationNotPerformedHere: true,
      noMapAttachmentOrDrawingPossibleHere: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true
    },
    nextPhaseRecommendation: "visual-renderer-initialization-state-contract-plan"
  };
}

function getCustom25DVisualRendererInitializationStateContractPlan(options = {}) {
  const attemptCloseout =
    typeof getCustom25DVisualManualRendererInitializationAttemptCloseout === "function"
      ? getCustom25DVisualManualRendererInitializationAttemptCloseout(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-attempt-closeout-unavailable"
        };
  const attemptResult =
    typeof getCustom25DVisualManualRendererInitializationAttemptResult === "function"
      ? getCustom25DVisualManualRendererInitializationAttemptResult(options)
      : {
          attempted: true,
          allowed: false,
          initialized: false,
          reason: "visual-renderer-initialization-attempt-result-unavailable"
        };

  return {
    ok: true,
    phase: 165,
    name: "custom-25d-visual-renderer-initialization-state-contract-plan",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Define the future contract for a renderer initialization state object without creating any actual state.",
    dependsOn: {
      initializationAttemptCloseout: !!attemptCloseout.ok,
      initializationAttemptResult: typeof getCustom25DVisualManualRendererInitializationAttemptResult === "function",
      phase: attemptCloseout.phase || 164
    },
    futureStateContract: {
      initialized: "boolean",
      initializedAt: "string|null",
      manual: "boolean",
      developerIntent: "boolean",
      rendererShellReady: "boolean",
      materializationShellReady: "boolean",
      handoffReady: "boolean",
      initializationAllowed: "boolean",
      reason: "string",
      lastAttempt: "object|null",
      attachedToMap: "boolean",
      drawingEnabled: "boolean"
    },
    allowedFutureFields: [
      "initialized",
      "initializedAt",
      "manual",
      "developerIntent",
      "rendererShellReady",
      "materializationShellReady",
      "handoffReady",
      "initializationAllowed",
      "reason",
      "lastAttempt",
      "attachedToMap",
      "drawingEnabled"
    ],
    defaultStateValues: {
      initialized: false,
      initializedAt: null,
      manual: false,
      developerIntent: false,
      rendererShellReady: false,
      materializationShellReady: false,
      handoffReady: false,
      initializationAllowed: false,
      reason: "custom-25d-map-disabled",
      lastAttempt: null,
      attachedToMap: false,
      drawingEnabled: false
    },
    blockedStateValues: {
      initialized: false,
      initializedAt: null,
      initializationAllowed: false,
      attachedToMap: false,
      drawingEnabled: false,
      reasonBlockedWhileMapFlagFalse: ENABLE_CUSTOM_25D_MAP === false
        ? "custom-25d-map-disabled"
        : "manual-guard-required"
    },
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-state-contract-review-closeout"
  };
}

function getCustom25DVisualRendererInitializationStateContractReview(options = {}) {
  const stateContractPlan =
    typeof getCustom25DVisualRendererInitializationStateContractPlan === "function"
      ? getCustom25DVisualRendererInitializationStateContractPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-plan-unavailable"
        };
  const attemptCloseout =
    typeof getCustom25DVisualManualRendererInitializationAttemptCloseout === "function"
      ? getCustom25DVisualManualRendererInitializationAttemptCloseout(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-attempt-closeout-unavailable"
        };

  return {
    ok: true,
    phase: 166,
    name: "custom-25d-visual-renderer-initialization-state-contract-review",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Review the future renderer initialization state contract and confirm it remains safely default-blocked and plan-only.",
    dependsOn: {
      initializationStateContractPlan: !!stateContractPlan.ok,
      initializationAttemptCloseout: !!attemptCloseout.ok,
      phase: stateContractPlan.phase || 165
    },
    stateContractPlanSummary: {
      contractExistsAsPlanOnly: !!stateContractPlan.ok,
      initializedDefaultFalse: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.initialized === false
      ),
      initializedAtDefaultNull: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.initializedAt === null
      ),
      initializationAllowedDefaultFalse: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.initializationAllowed === false
      ),
      attachedToMapDefaultFalse: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.attachedToMap === false
      ),
      drawingEnabledDefaultFalse: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.drawingEnabled === false
      )
    },
    attemptCloseoutSummary: {
      closeoutAvailable: !!attemptCloseout.ok,
      defaultAttemptBlocked: !!(attemptCloseout.defaultBlocked && attemptCloseout.defaultBlocked.defaultAttemptAllowed),
      initializedAlwaysFalse: !!(attemptCloseout.defaultBlocked && attemptCloseout.defaultBlocked.initializedAlwaysFalse),
      expectedDefaultReason: !!(attemptCloseout.defaultBlocked && attemptCloseout.defaultBlocked.expectedDefaultReason)
    },
    defaultBlocked: {
      contractExistsOnlyAsPlan: !!stateContractPlan.ok,
      noRealInitializationStateCreated: true,
      initializedRemainsFalseByContract: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.initialized === false
      ),
      initializedAtRemainsNullByContract: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.initializedAt === null
      ),
      initializationAllowedRemainsFalseByDefault: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.initializationAllowed === false
      ),
      attachedToMapRemainsFalseByContract: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.attachedToMap === false
      ),
      drawingEnabledRemainsFalseByContract: !!(
        stateContractPlan.defaultStateValues &&
        stateContractPlan.defaultStateValues.drawingEnabled === false
      ),
      expectedDefaultReasonRemainsBlocked: !!(
        stateContractPlan.blockedStateValues &&
        stateContractPlan.blockedStateValues.reasonBlockedWhileMapFlagFalse === "custom-25d-map-disabled"
      )
    },
    stateCreationAllowed: false,
    initializationPerformed: false,
    reviewFindings: {
      rendererInitializationNotPerformedHere: true,
      noMapAttachmentOrDrawingPossibleHere: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true,
      futureStateContractStillPlanOnly: true,
      noActualStateObjectCreated: true
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-lifecycle-boundary-plan"
  };
}

function getCustom25DVisualRendererInitializationLifecycleBoundaryPlan(options = {}) {
  const stateContractReview =
    typeof getCustom25DVisualRendererInitializationStateContractReview === "function"
      ? getCustom25DVisualRendererInitializationStateContractReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-review-unavailable"
        };
  const stateContractPlan =
    typeof getCustom25DVisualRendererInitializationStateContractPlan === "function"
      ? getCustom25DVisualRendererInitializationStateContractPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-plan-unavailable"
        };

  return {
    ok: true,
    phase: 167,
    name: "custom-25d-visual-renderer-initialization-lifecycle-boundary-plan",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Define the future lifecycle boundaries around renderer initialization without creating any lifecycle object, state, or initialization side effects.",
    dependsOn: {
      initializationStateContractReview: !!stateContractReview.ok,
      initializationStateContractPlan: !!stateContractPlan.ok,
      phase: stateContractReview.phase || 166
    },
    lifecycleBoundary: {
      planningReportHelpersMayRunPassively: true,
      guardEvaluatorsMayReportAllowedOrBlocked: true,
      manualAttemptEvaluatorsMaySimulateOnly: true,
      realStateCreationNotAllowedHere: true,
      realRendererInitializationNotAllowedHere: true,
      mapAttachmentNotAllowedHere: true,
      drawingNotAllowedHere: true,
      startupWiringNotAllowedHere: true
    },
    allowedFutureLifecycleStages: [
      "planning",
      "guard-evaluation",
      "manual-attempt-evaluation",
      "state-contract-definition",
      "lifecycle-boundary-review"
    ],
    prohibitedDefaultLifecycleStages: [
      "lifecycle-object-creation",
      "renderer-state-creation",
      "renderer-initialization",
      "map-attachment",
      "drawing",
      "startup-wiring"
    ],
    separationOfConcerns: {
      planningSeparatedFromGuarding: true,
      guardingSeparatedFromAttemptEvaluation: true,
      attemptEvaluationSeparatedFromStateCreation: true,
      stateCreationSeparatedFromInitialization: true,
      initializationSeparatedFromMapAttachment: true,
      mapAttachmentSeparatedFromDrawing: true
    },
    stateCreationAllowed: false,
    lifecycleCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-lifecycle-boundary-review-closeout"
  };
}

function getCustom25DVisualRendererInitializationLifecycleBoundaryReview(options = {}) {
  const lifecycleBoundaryPlan =
    typeof getCustom25DVisualRendererInitializationLifecycleBoundaryPlan === "function"
      ? getCustom25DVisualRendererInitializationLifecycleBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-lifecycle-boundary-plan-unavailable"
        };
  const stateContractReview =
    typeof getCustom25DVisualRendererInitializationStateContractReview === "function"
      ? getCustom25DVisualRendererInitializationStateContractReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-review-unavailable"
        };

  return {
    ok: true,
    phase: 168,
    name: "custom-25d-visual-renderer-initialization-lifecycle-boundary-review",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Review the future renderer initialization lifecycle boundary plan and confirm the boundaries remain safely separated and default-blocked.",
    dependsOn: {
      lifecycleBoundaryPlan: !!lifecycleBoundaryPlan.ok,
      initializationStateContractReview: !!stateContractReview.ok,
      phase: lifecycleBoundaryPlan.phase || 167
    },
    lifecycleBoundarySummary: {
      boundaryExistsAsPlanOnly: !!lifecycleBoundaryPlan.ok,
      planningReportingSeparateFromGuardEvaluation: !!(
        lifecycleBoundaryPlan.separationOfConcerns &&
        lifecycleBoundaryPlan.separationOfConcerns.planningSeparatedFromGuarding
      ),
      guardEvaluationSeparateFromManualAttemptSimulation: !!(
        lifecycleBoundaryPlan.separationOfConcerns &&
        lifecycleBoundaryPlan.separationOfConcerns.guardingSeparatedFromAttemptEvaluation
      ),
      manualAttemptSimulationSeparateFromRealStateCreation: !!(
        lifecycleBoundaryPlan.separationOfConcerns &&
        lifecycleBoundaryPlan.separationOfConcerns.attemptEvaluationSeparatedFromStateCreation
      ),
      realStateCreationStillProhibited: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.realStateCreationNotAllowedHere
      ),
      realRendererInitializationStillProhibited: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.realRendererInitializationNotAllowedHere
      ),
      mapAttachmentStillProhibited: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.mapAttachmentNotAllowedHere
      ),
      drawingStillProhibited: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.drawingNotAllowedHere
      ),
      startupWiringStillProhibited: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.startupWiringNotAllowedHere
      )
    },
    stateContractReviewSummary: {
      reviewAvailable: !!stateContractReview.ok,
      expectedDefaultReasonRemainsBlocked: !!(
        stateContractReview.defaultBlocked &&
        stateContractReview.defaultBlocked.expectedDefaultReasonRemainsBlocked
      ),
      noRealInitializationStateCreated: !!(
        stateContractReview.defaultBlocked &&
        stateContractReview.defaultBlocked.noRealInitializationStateCreated
      ),
      initializationPerformed: !!stateContractReview.initializationPerformed
    },
    defaultBlocked: {
      lifecycleBoundaryExistsOnlyAsPlan: !!lifecycleBoundaryPlan.ok,
      lifecycleCreationAllowed: false,
      stateCreationAllowed: false,
      initializationPerformed: false,
      expectedDefaultBlockedReasonRemainsMapDisabled: !!(
        stateContractReview.defaultBlocked &&
        stateContractReview.defaultBlocked.expectedDefaultReasonRemainsBlocked
      )
    },
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    reviewFindings: {
      noLifecycleObjectCreated: true,
      noActualInitializationStateCreated: true,
      noRendererInitializationOccurred: true,
      noMapAttachmentOrDrawingPossibleHere: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-preflight-inventory-report"
  };
}

function getCustom25DVisualRendererInitializationPreflightInventory(options = {}) {
  const lifecycleBoundaryReview =
    typeof getCustom25DVisualRendererInitializationLifecycleBoundaryReview === "function"
      ? getCustom25DVisualRendererInitializationLifecycleBoundaryReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-lifecycle-boundary-review-unavailable"
        };
  const lifecycleBoundaryPlan =
    typeof getCustom25DVisualRendererInitializationLifecycleBoundaryPlan === "function"
      ? getCustom25DVisualRendererInitializationLifecycleBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-lifecycle-boundary-plan-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };
  const preflightInventory = {
    rendererShellReadiness: options.hasRendererShell === true,
    materializationShellReadiness: options.hasRendererMaterializationShell === true,
    handoffReadiness: options.hasRendererHandoffReadiness === true,
    initializationContractReadiness: options.hasInitializationContract === true,
    guardEvaluatorReadiness: typeof canInitializeCustom25DVisualRenderer === "function",
    manualAttemptEvaluatorReadiness:
      typeof getCustom25DVisualManualRendererInitializationAttemptResult === "function",
    stateContractReadiness:
      typeof getCustom25DVisualRendererInitializationStateContractPlan === "function" &&
      typeof getCustom25DVisualRendererInitializationStateContractReview === "function",
    lifecycleBoundaryReadiness:
      typeof getCustom25DVisualRendererInitializationLifecycleBoundaryPlan === "function" &&
      typeof getCustom25DVisualRendererInitializationLifecycleBoundaryReview === "function",
    mapFlagState: ENABLE_CUSTOM_25D_MAP === true,
    manualDeveloperOptionState: options.manual === true && options.developerIntent === true,
    noStartupWiring: options.preventStartupWiring === true,
    noDrawing: options.preventDrawing === true,
    noMapAttachment: options.preventMapAttachment === true
  };
  const presentPrerequisites = Object.keys(preflightInventory).filter((key) => preflightInventory[key] === true);
  const missingPrerequisites = Object.keys(preflightInventory).filter((key) => preflightInventory[key] !== true);

  return {
    ok: true,
    phase: 169,
    name: "custom-25d-visual-renderer-initialization-preflight-inventory",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Inventory passive readiness signals required before any future renderer initialization path is considered.",
    dependsOn: {
      lifecycleBoundaryReview: !!lifecycleBoundaryReview.ok,
      lifecycleBoundaryPlan: !!lifecycleBoundaryPlan.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: lifecycleBoundaryReview.phase || 168
    },
    guardResult,
    lifecycleBoundaryReviewSummary: {
      reviewAvailable: !!lifecycleBoundaryReview.ok,
      lifecycleBoundaryExistsOnlyAsPlan: !!(
        lifecycleBoundaryReview.defaultBlocked &&
        lifecycleBoundaryReview.defaultBlocked.lifecycleBoundaryExistsOnlyAsPlan
      ),
      lifecycleCreationBlocked: !!(
        lifecycleBoundaryReview.defaultBlocked &&
        lifecycleBoundaryReview.defaultBlocked.lifecycleCreationAllowed === false
      ),
      stateCreationBlocked: !!(
        lifecycleBoundaryReview.defaultBlocked &&
        lifecycleBoundaryReview.defaultBlocked.stateCreationAllowed === false
      )
    },
    lifecycleBoundaryPlanSummary: {
      planAvailable: !!lifecycleBoundaryPlan.ok,
      planningHelpersPassive: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.planningReportHelpersMayRunPassively
      ),
      guardEvaluatorsPassive: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.guardEvaluatorsMayReportAllowedOrBlocked
      ),
      realInitializationProhibited: !!(
        lifecycleBoundaryPlan.lifecycleBoundary &&
        lifecycleBoundaryPlan.lifecycleBoundary.realRendererInitializationNotAllowedHere
      )
    },
    preflightInventory,
    presentPrerequisites,
    missingPrerequisites,
    defaultBlocked: {
      preflightInventoryReportOnly: true,
      futureInitializationBlockedByDefault: guardResult.allowed === false,
      expectedDefaultBlockedReasonRemainsMapDisabled: guardResult.reason === "custom-25d-map-disabled",
      noRealInitializationStateCreated: true,
      noLifecycleObjectCreated: true
    },
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "create initialization state",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-preflight-inventory-closeout"
  };
}

function getCustom25DVisualRendererInitializationPreflightReview(options = {}) {
  const preflightInventory =
    typeof getCustom25DVisualRendererInitializationPreflightInventory === "function"
      ? getCustom25DVisualRendererInitializationPreflightInventory(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-preflight-inventory-unavailable",
          presentPrerequisites: [],
          missingPrerequisites: []
        };
  const lifecycleBoundaryReview =
    typeof getCustom25DVisualRendererInitializationLifecycleBoundaryReview === "function"
      ? getCustom25DVisualRendererInitializationLifecycleBoundaryReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-lifecycle-boundary-review-unavailable"
        };

  return {
    ok: true,
    phase: 170,
    name: "custom-25d-visual-renderer-initialization-preflight-review",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Review the passive renderer initialization preflight inventory and confirm it remains safely default-blocked.",
    dependsOn: {
      preflightInventory: !!preflightInventory.ok,
      lifecycleBoundaryReview: !!lifecycleBoundaryReview.ok,
      phase: preflightInventory.phase || 169
    },
    preflightInventorySummary: {
      inventoryExistsAsPassiveReport: !!preflightInventory.ok,
      futureInitializationBlockedByDefault: !!(
        preflightInventory.defaultBlocked &&
        preflightInventory.defaultBlocked.futureInitializationBlockedByDefault
      ),
      expectedDefaultBlockedReasonRemainsMapDisabled: !!(
        preflightInventory.defaultBlocked &&
        preflightInventory.defaultBlocked.expectedDefaultBlockedReasonRemainsMapDisabled
      ),
      noRealInitializationStateCreated: !!(
        preflightInventory.defaultBlocked &&
        preflightInventory.defaultBlocked.noRealInitializationStateCreated
      ),
      noLifecycleObjectCreated: !!(
        preflightInventory.defaultBlocked &&
        preflightInventory.defaultBlocked.noLifecycleObjectCreated
      )
    },
    lifecycleBoundaryReviewSummary: {
      reviewAvailable: !!lifecycleBoundaryReview.ok,
      lifecycleBoundaryExistsOnlyAsPlan: !!(
        lifecycleBoundaryReview.defaultBlocked &&
        lifecycleBoundaryReview.defaultBlocked.lifecycleBoundaryExistsOnlyAsPlan
      ),
      lifecycleCreationBlocked: !!(
        lifecycleBoundaryReview.defaultBlocked &&
        lifecycleBoundaryReview.defaultBlocked.lifecycleCreationAllowed === false
      ),
      stateCreationBlocked: !!(
        lifecycleBoundaryReview.defaultBlocked &&
        lifecycleBoundaryReview.defaultBlocked.stateCreationAllowed === false
      )
    },
    presentPrerequisites: Array.isArray(preflightInventory.presentPrerequisites)
      ? preflightInventory.presentPrerequisites.slice()
      : [],
    missingPrerequisites: Array.isArray(preflightInventory.missingPrerequisites)
      ? preflightInventory.missingPrerequisites.slice()
      : [],
    defaultBlocked: {
      futureInitializationBlockedByDefault: !!(
        preflightInventory.defaultBlocked &&
        preflightInventory.defaultBlocked.futureInitializationBlockedByDefault
      ),
      expectedDefaultBlockedReason: preflightInventory.guardResult
        ? preflightInventory.guardResult.reason
        : "visual-renderer-initialization-guard-unavailable",
      lifecycleCreationAllowed: false,
      stateCreationAllowed: false,
      initializationPerformed: false
    },
    expectedDefaultBlockedReason: preflightInventory.guardResult
      ? preflightInventory.guardResult.reason
      : "visual-renderer-initialization-guard-unavailable",
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    reviewFindings: {
      preflightInventoryRemainsPassive: true,
      lifecycleObjectCreationRemainsProhibited: true,
      initializationStateCreationRemainsProhibited: true,
      realRendererInitializationRemainsProhibited: true,
      mapAttachmentRemainsProhibited: true,
      drawingRemainsProhibited: true,
      startupWiringRemainsProhibited: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true
    },
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "create initialization state",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-go-no-go-report-plan"
  };
}

function getCustom25DVisualRendererInitializationGoNoGoReportPlan(options = {}) {
  const preflightReview =
    typeof getCustom25DVisualRendererInitializationPreflightReview === "function"
      ? getCustom25DVisualRendererInitializationPreflightReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-preflight-review-unavailable"
        };
  const preflightInventory =
    typeof getCustom25DVisualRendererInitializationPreflightInventory === "function"
      ? getCustom25DVisualRendererInitializationPreflightInventory(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-preflight-inventory-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 171,
    name: "custom-25d-visual-renderer-initialization-go-no-go-report-plan",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Define how a future renderer initialization go/no-go report should summarize readiness while keeping the default outcome blocked.",
    dependsOn: {
      preflightReview: !!preflightReview.ok,
      preflightInventory: !!preflightInventory.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: preflightReview.phase || 170
    },
    guardResult,
    preflightReviewSummary: {
      reviewAvailable: !!preflightReview.ok,
      futureInitializationBlockedByDefault: !!(
        preflightReview.defaultBlocked &&
        preflightReview.defaultBlocked.futureInitializationBlockedByDefault
      ),
      expectedDefaultBlockedReason: preflightReview.expectedDefaultBlockedReason || "visual-renderer-initialization-guard-unavailable",
      lifecycleCreationBlocked: preflightReview.lifecycleCreationAllowed === false,
      stateCreationBlocked: preflightReview.stateCreationAllowed === false
    },
    preflightInventorySummary: {
      inventoryAvailable: !!preflightInventory.ok,
      presentPrerequisiteCount: Array.isArray(preflightInventory.presentPrerequisites)
        ? preflightInventory.presentPrerequisites.length
        : 0,
      missingPrerequisiteCount: Array.isArray(preflightInventory.missingPrerequisites)
        ? preflightInventory.missingPrerequisites.length
        : 0,
      futureInitializationBlockedByDefault: !!(
        preflightInventory.defaultBlocked &&
        preflightInventory.defaultBlocked.futureInitializationBlockedByDefault
      )
    },
    goNoGoPlan: {
      reportOnly: true,
      defaultDecision: "no-go",
      decisionDependsOnGuardResult: true,
      decisionDependsOnPreflightReadiness: true,
      decisionMustRemainBlockedWhileMapFlagFalse: ENABLE_CUSTOM_25D_MAP === false,
      noRendererInitializationPerformed: true
    },
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    decisionInputs: {
      mapFlagState: ENABLE_CUSTOM_25D_MAP === true,
      manualDeveloperIntent: options.manual === true && options.developerIntent === true,
      rendererShellReadiness: options.hasRendererShell === true,
      materializationShellReadiness: options.hasRendererMaterializationShell === true,
      handoffReadiness: options.hasRendererHandoffReadiness === true,
      initializationContractReadiness: options.hasInitializationContract === true,
      guardEvaluatorResult: guardResult.allowed === true,
      manualAttemptResult:
        typeof getCustom25DVisualManualRendererInitializationAttemptResult === "function",
      stateContractReadiness:
        typeof getCustom25DVisualRendererInitializationStateContractPlan === "function" &&
        typeof getCustom25DVisualRendererInitializationStateContractReview === "function",
      lifecycleBoundaryReadiness:
        typeof getCustom25DVisualRendererInitializationLifecycleBoundaryPlan === "function" &&
        typeof getCustom25DVisualRendererInitializationLifecycleBoundaryReview === "function",
      preflightInventoryReviewReadiness: !!preflightInventory.ok && !!preflightReview.ok,
      startupWiringBlocked: options.preventStartupWiring === true,
      drawingBlocked: options.preventDrawing === true,
      mapAttachmentBlocked: options.preventMapAttachment === true
    },
    noGoReasons: Array.isArray(preflightInventory.missingPrerequisites)
      ? preflightInventory.missingPrerequisites.slice()
      : [],
    futureGoRequirements: [
      "custom 2.5D map flag explicitly enabled in a future guarded phase",
      "manual and developer intent explicitly confirmed",
      "renderer shell and materialization shell readiness confirmed",
      "handoff and initialization contract readiness confirmed",
      "preflight inventory and review show no missing prerequisites",
      "startup wiring, drawing, and map attachment remain blocked until a later approved phase"
    ],
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "create initialization state",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-go-no-go-review-closeout"
  };
}

function getCustom25DVisualRendererInitializationGoNoGoReportReview(options = {}) {
  const goNoGoPlan =
    typeof getCustom25DVisualRendererInitializationGoNoGoReportPlan === "function"
      ? getCustom25DVisualRendererInitializationGoNoGoReportPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-go-no-go-report-plan-unavailable"
        };
  const preflightReview =
    typeof getCustom25DVisualRendererInitializationPreflightReview === "function"
      ? getCustom25DVisualRendererInitializationPreflightReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-preflight-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 172,
    name: "custom-25d-visual-renderer-initialization-go-no-go-report-review",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Review the future renderer initialization go/no-go report plan and confirm it remains safely default-blocked.",
    dependsOn: {
      goNoGoPlan: !!goNoGoPlan.ok,
      preflightReview: !!preflightReview.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: goNoGoPlan.phase || 171
    },
    goNoGoPlanSummary: {
      planExistsAsPassiveReportPlan: !!goNoGoPlan.ok,
      defaultDecisionRemainsNoGo: goNoGoPlan.defaultDecision === "no-go",
      expectedDefaultBlockedReason: goNoGoPlan.expectedDefaultBlockedReason || "visual-renderer-initialization-guard-unavailable",
      lifecycleCreationBlocked: goNoGoPlan.lifecycleCreationAllowed === false,
      stateCreationBlocked: goNoGoPlan.stateCreationAllowed === false
    },
    preflightReviewSummary: {
      reviewAvailable: !!preflightReview.ok,
      futureInitializationBlockedByDefault: !!(
        preflightReview.defaultBlocked &&
        preflightReview.defaultBlocked.futureInitializationBlockedByDefault
      ),
      expectedDefaultBlockedReason: preflightReview.expectedDefaultBlockedReason || "visual-renderer-initialization-guard-unavailable",
      lifecycleCreationBlocked: preflightReview.lifecycleCreationAllowed === false,
      stateCreationBlocked: preflightReview.stateCreationAllowed === false
    },
    guardResult,
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    reviewFindings: {
      goNoGoReportPlanExistsAsPassivePlan: true,
      defaultDecisionRemainsNoGo: goNoGoPlan.defaultDecision === "no-go",
      guardResultRemainsDefaultBlocked: guardResult.allowed === false,
      lifecycleObjectCreationRemainsProhibited: true,
      initializationStateCreationRemainsProhibited: true,
      realRendererInitializationRemainsProhibited: true,
      mapAttachmentRemainsProhibited: true,
      drawingRemainsProhibited: true,
      startupWiringRemainsProhibited: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true
    },
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "create initialization state",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-final-readiness-summary"
  };
}

function getCustom25DVisualRendererInitializationFinalReadinessSummary(options = {}) {
  const goNoGoReview =
    typeof getCustom25DVisualRendererInitializationGoNoGoReportReview === "function"
      ? getCustom25DVisualRendererInitializationGoNoGoReportReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-go-no-go-report-review-unavailable"
        };
  const goNoGoPlan =
    typeof getCustom25DVisualRendererInitializationGoNoGoReportPlan === "function"
      ? getCustom25DVisualRendererInitializationGoNoGoReportPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-go-no-go-report-plan-unavailable"
        };
  const preflightReview =
    typeof getCustom25DVisualRendererInitializationPreflightReview === "function"
      ? getCustom25DVisualRendererInitializationPreflightReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-preflight-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 173,
    name: "custom-25d-visual-renderer-initialization-final-readiness-summary",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Summarize final readiness for the future renderer initialization pathway while keeping the current outcome blocked and planning-only.",
    dependsOn: {
      goNoGoReview: !!goNoGoReview.ok,
      goNoGoPlan: !!goNoGoPlan.ok,
      preflightReview: !!preflightReview.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: goNoGoReview.phase || 172
    },
    guardResult,
    goNoGoReviewSummary: {
      reviewAvailable: !!goNoGoReview.ok,
      defaultDecisionRemainsNoGo: goNoGoReview.defaultDecision === "no-go",
      expectedDefaultBlockedReason: goNoGoReview.expectedDefaultBlockedReason || "visual-renderer-initialization-guard-unavailable",
      lifecycleCreationBlocked: goNoGoReview.lifecycleCreationAllowed === false,
      stateCreationBlocked: goNoGoReview.stateCreationAllowed === false
    },
    goNoGoPlanSummary: {
      planAvailable: !!goNoGoPlan.ok,
      defaultDecisionRemainsNoGo: goNoGoPlan.defaultDecision === "no-go",
      expectedDefaultBlockedReason: goNoGoPlan.expectedDefaultBlockedReason || "visual-renderer-initialization-guard-unavailable",
      noRendererInitializationPerformed: !!(
        goNoGoPlan.goNoGoPlan && goNoGoPlan.goNoGoPlan.noRendererInitializationPerformed
      )
    },
    preflightReviewSummary: {
      reviewAvailable: !!preflightReview.ok,
      futureInitializationBlockedByDefault: !!(
        preflightReview.defaultBlocked &&
        preflightReview.defaultBlocked.futureInitializationBlockedByDefault
      ),
      expectedDefaultBlockedReason: preflightReview.expectedDefaultBlockedReason || "visual-renderer-initialization-guard-unavailable"
    },
    finalReadiness: {
      planningSequenceReadyForReview: !!goNoGoReview.ok && !!goNoGoPlan.ok && !!preflightReview.ok,
      ready: false,
      planningSequenceRemainsPassiveOnly: true,
      realInitializationStillProhibited: true
    },
    ready: false,
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    readinessFindings: {
      defaultReadinessRemainsBlocked: guardResult.allowed === false,
      guardResultRemainsDefaultBlocked: guardResult.allowed === false,
      goNoGoDecisionRemainsNoGoByDefault: goNoGoPlan.defaultDecision === "no-go",
      lifecycleObjectCreationRemainsProhibited: true,
      initializationStateCreationRemainsProhibited: true,
      realRendererInitializationRemainsProhibited: true,
      mapAttachmentRemainsProhibited: true,
      drawingRemainsProhibited: true,
      startupWiringRemainsProhibited: true,
      gameplayOsmPinsPlayerCaptureRadiusBackendUntouched: true
    },
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiOrDebugControls: true,
      noGraphicsOrDrawing: true,
      noMapAttachment: true,
      noGameplayOsmPinsPlayerCaptureRadiusBackendChanges: true
    },
    prohibitedActions: [
      "create a renderer instance",
      "create a lifecycle object",
      "create initialization state",
      "initialize the renderer",
      "create or initialize registries",
      "create layers or layer state",
      "wire startup behavior",
      "attach anything to the map",
      "draw graphics or show visible output",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "visual-renderer-initialization-planning-closeout-readiness-report"
  };
}

function getCustom25DVisualRendererInitializationPlanningCloseoutReport(options = {}) {
  const finalReadinessSummary =
    typeof getCustom25DVisualRendererInitializationFinalReadinessSummary === "function"
      ? getCustom25DVisualRendererInitializationFinalReadinessSummary(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-final-readiness-summary-unavailable"
        };
  const goNoGoReviewSummary =
    typeof getCustom25DVisualRendererInitializationGoNoGoReportReview === "function"
      ? getCustom25DVisualRendererInitializationGoNoGoReportReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-go-no-go-report-review-unavailable"
        };
  const preflightReviewSummary =
    typeof getCustom25DVisualRendererInitializationPreflightReview === "function"
      ? getCustom25DVisualRendererInitializationPreflightReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-preflight-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 174,
    name: "custom-25d-visual-renderer-initialization-planning-closeout-report",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Close out the renderer initialization planning sequence while keeping the pathway blocked, passive, and review-only.",
    dependsOn: {
      finalReadinessSummary: !!finalReadinessSummary.ok,
      goNoGoReviewSummary: !!goNoGoReviewSummary.ok,
      preflightReviewSummary: !!preflightReviewSummary.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: finalReadinessSummary.phase || 173
    },
    finalReadinessSummary: {
      summaryAvailable: !!finalReadinessSummary.ok,
      readyRemainsFalse: finalReadinessSummary.ready === false,
      defaultDecisionRemainsNoGo: finalReadinessSummary.defaultDecision === "no-go",
      expectedDefaultBlockedReason:
        finalReadinessSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    goNoGoReviewSummary: {
      reviewAvailable: !!goNoGoReviewSummary.ok,
      defaultDecisionRemainsNoGo: goNoGoReviewSummary.defaultDecision === "no-go",
      expectedDefaultBlockedReason:
        goNoGoReviewSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable",
      lifecycleCreationBlocked: goNoGoReviewSummary.lifecycleCreationAllowed === false,
      stateCreationBlocked: goNoGoReviewSummary.stateCreationAllowed === false
    },
    preflightReviewSummary: {
      reviewAvailable: !!preflightReviewSummary.ok,
      futureInitializationBlockedByDefault: !!(
        preflightReviewSummary.defaultBlocked &&
        preflightReviewSummary.defaultBlocked.futureInitializationBlockedByDefault
      ),
      expectedDefaultBlockedReason:
        preflightReviewSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    guardResult,
    planningSequenceCloseout: {
      closeoutReadyForReview:
        !!finalReadinessSummary.ok &&
        !!goNoGoReviewSummary.ok &&
        !!preflightReviewSummary.ok,
      planningSequenceRemainsPassiveOnly: true,
      realInitializationStillProhibited: true,
      nextHandoffMustRemainPlanningOnly: true
    },
    completedPlanningMilestones: [
      "renderer initialization contract planning",
      "renderer initialization guard evaluation",
      "manual initialization attempt evaluation",
      "initialization state contract planning and review",
      "initialization lifecycle boundary planning and review",
      "initialization preflight inventory and review",
      "initialization go/no-go planning and review",
      "final readiness summary"
    ],
    remainingBlockedMilestones: [
      "lifecycle object creation",
      "initialization state creation",
      "real renderer initialization",
      "map attachment",
      "drawing",
      "startup wiring"
    ],
    ready: false,
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiChanges: true,
      noGraphics: true,
      noMapAttachment: true,
      noDrawing: true,
      noGameplayChanges: true,
      noOsmChanges: true,
      noPinChanges: true,
      noPlayerMarkerChanges: true,
      noCaptureRadiusChanges: true,
      noBackendChanges: true
    },
    prohibitedActions: [
      "create lifecycle objects",
      "create initialization state",
      "initialize the renderer",
      "create registries or layers",
      "attach anything to the map",
      "draw graphics or show visible output",
      "wire startup behavior",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "passive-next-stage-planning-handoff-for-future-initialization-state-architecture"
  };
}

function getCustom25DVisualRendererInitializationStateArchitectureHandoff(options = {}) {
  const planningCloseoutSummary =
    typeof getCustom25DVisualRendererInitializationPlanningCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationPlanningCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-planning-closeout-report-unavailable"
        };
  const finalReadinessSummary =
    typeof getCustom25DVisualRendererInitializationFinalReadinessSummary === "function"
      ? getCustom25DVisualRendererInitializationFinalReadinessSummary(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-final-readiness-summary-unavailable"
        };
  const stateContractReviewSummary =
    typeof getCustom25DVisualRendererInitializationStateContractReview === "function"
      ? getCustom25DVisualRendererInitializationStateContractReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 175,
    name: "custom-25d-visual-renderer-initialization-state-architecture-handoff",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Hand off from renderer initialization planning closeout into future initialization state architecture planning while remaining blocked and report-only.",
    dependsOn: {
      planningCloseoutSummary: !!planningCloseoutSummary.ok,
      finalReadinessSummary: !!finalReadinessSummary.ok,
      stateContractReviewSummary: !!stateContractReviewSummary.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: planningCloseoutSummary.phase || 174
    },
    planningCloseoutSummary: {
      closeoutAvailable: !!planningCloseoutSummary.ok,
      planningSequenceClosedOutForReview: !!(
        planningCloseoutSummary.planningSequenceCloseout &&
        planningCloseoutSummary.planningSequenceCloseout.closeoutReadyForReview
      ),
      defaultDecisionRemainsNoGo: planningCloseoutSummary.defaultDecision === "no-go",
      expectedDefaultBlockedReason:
        planningCloseoutSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    finalReadinessSummary: {
      summaryAvailable: !!finalReadinessSummary.ok,
      readyRemainsFalse: finalReadinessSummary.ready === false,
      defaultDecisionRemainsNoGo: finalReadinessSummary.defaultDecision === "no-go",
      expectedDefaultBlockedReason:
        finalReadinessSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    stateContractReviewSummary: {
      reviewAvailable: !!stateContractReviewSummary.ok,
      futureStateContractRemainsPlanOnly: !!(
        stateContractReviewSummary.reviewFindings &&
        stateContractReviewSummary.reviewFindings.futureStateContractExistsOnlyAsPlan
      ),
      stateCreationBlocked: stateContractReviewSummary.stateCreationAllowed === false,
      initializationStillNotPerformed: stateContractReviewSummary.initializationPerformed === false
    },
    stateArchitectureHandoff: {
      planningChainClosedOut: !!planningCloseoutSummary.ok,
      futureArchitecturePlanningOnly: true,
      realStateCreationStillProhibited: true,
      realInitializationStillProhibited: true
    },
    completedPlanningMilestones: [
      "renderer initialization contract planning",
      "renderer initialization guard evaluation",
      "manual initialization attempt evaluation and closeout",
      "renderer initialization state contract planning and review",
      "renderer initialization lifecycle boundary planning and review",
      "renderer initialization preflight inventory and review",
      "renderer initialization go/no-go planning and review",
      "renderer initialization final readiness summary",
      "renderer initialization planning closeout"
    ],
    futureStateArchitectureQuestions: [
      "where a future initialization state object would live",
      "whether it should be singleton, global, or internal-only",
      "how it would remain dormant by default",
      "how it would record the last manual attempt safely",
      "how it would avoid map attachment and drawing",
      "how it would avoid startup wiring",
      "how it would preserve gameplay overlays, OSM, pins, player marker, capture radius, and backend behavior",
      "what guard must approve before any future state creation phase"
    ],
    requiredFutureArchitectureDecisions: [
      "choose the future owner/scope of renderer initialization state",
      "define default-blocked state field values",
      "define safe recording rules for manual attempt metadata",
      "define dormant-only transitions before any future state creation",
      "define non-rendering/non-attaching guarantees for future state usage"
    ],
    ready: false,
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiChanges: true,
      noGraphics: true,
      noMapAttachment: true,
      noDrawing: true,
      noGameplayChanges: true,
      noOsmChanges: true,
      noPinChanges: true,
      noPlayerMarkerChanges: true,
      noCaptureRadiusChanges: true,
      noBackendChanges: true
    },
    prohibitedActions: [
      "create lifecycle objects",
      "create initialization state",
      "initialize the renderer",
      "create renderer objects or registries",
      "create layers or layer state",
      "attach anything to the map",
      "draw graphics or show visible output",
      "wire startup behavior",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "passive-initialization-state-architecture-decision-plan"
  };
}

function getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan(options = {}) {
  const architectureHandoffSummary =
    typeof getCustom25DVisualRendererInitializationStateArchitectureHandoff === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureHandoff(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-handoff-unavailable"
        };
  const planningCloseoutSummary =
    typeof getCustom25DVisualRendererInitializationPlanningCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationPlanningCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-planning-closeout-report-unavailable"
        };
  const stateContractReviewSummary =
    typeof getCustom25DVisualRendererInitializationStateContractReview === "function"
      ? getCustom25DVisualRendererInitializationStateContractReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 176,
    name: "custom-25d-visual-renderer-initialization-state-architecture-decision-plan",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Plan future renderer initialization state architecture decisions while keeping the entire pathway blocked, dormant, and report-only.",
    dependsOn: {
      architectureHandoffSummary: !!architectureHandoffSummary.ok,
      planningCloseoutSummary: !!planningCloseoutSummary.ok,
      stateContractReviewSummary: !!stateContractReviewSummary.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: architectureHandoffSummary.phase || 175
    },
    architectureHandoffSummary: {
      handoffAvailable: !!architectureHandoffSummary.ok,
      planningChainClosedOut: !!(
        architectureHandoffSummary.stateArchitectureHandoff &&
        architectureHandoffSummary.stateArchitectureHandoff.planningChainClosedOut
      ),
      futureArchitecturePlanningOnly: !!(
        architectureHandoffSummary.stateArchitectureHandoff &&
        architectureHandoffSummary.stateArchitectureHandoff.futureArchitecturePlanningOnly
      ),
      expectedDefaultBlockedReason:
        architectureHandoffSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    planningCloseoutSummary: {
      closeoutAvailable: !!planningCloseoutSummary.ok,
      planningSequenceClosedOutForReview: !!(
        planningCloseoutSummary.planningSequenceCloseout &&
        planningCloseoutSummary.planningSequenceCloseout.closeoutReadyForReview
      ),
      defaultDecisionRemainsNoGo: planningCloseoutSummary.defaultDecision === "no-go",
      expectedDefaultBlockedReason:
        planningCloseoutSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    stateContractReviewSummary: {
      reviewAvailable: !!stateContractReviewSummary.ok,
      futureStateContractRemainsPlanOnly: !!(
        stateContractReviewSummary.reviewFindings &&
        stateContractReviewSummary.reviewFindings.futureStateContractExistsOnlyAsPlan
      ),
      stateCreationBlocked: stateContractReviewSummary.stateCreationAllowed === false,
      initializationStillNotPerformed: stateContractReviewSummary.initializationPerformed === false
    },
    architectureDecisionPlan: {
      futureInitializationStateArchitecturePlanningOnly: true,
      noActualStateCreatedNow: true,
      futureStateMustRemainInternalOnly: true,
      futureStateMustRemainDormantByDefault: true,
      futureStateMustRemainNonRenderingAndUnattached: true
    },
    recommendedFutureStateLocation: {
      conceptualOwner: "internal renderer initialization module scope",
      singleton: true,
      global: false,
      internalOnly: true,
      exposedOnlyThroughGuardedHelpers: true
    },
    recommendedStateShape: {
      initialized: false,
      initializedAt: null,
      initializationAllowed: false,
      rendererShellReady: false,
      materializationShellReady: false,
      handoffReady: false,
      attachedToMap: false,
      drawingEnabled: false,
      lastAttempt: null
    },
    recommendedAccessPattern: {
      readOnlyReportsBeforeCreation: true,
      guardedManualPathOnly: true,
      noAutomaticStartupReads: true,
      noMapBindingAccess: true,
      noDrawingActivationAccess: true
    },
    dormantByDefaultRequirements: [
      "future initialization state must be internal-only",
      "future initialization state must start blocked",
      "future initialization state must not attach to the map",
      "future initialization state must not enable drawing",
      "future initialization state must not wire startup",
      "future initialization state must preserve gameplay overlays, OSM, pins, player marker, capture radius, and backend behavior"
    ],
    futureAttemptRecordingPlan: {
      allowedOnlyAfterFutureGuardApproval: true,
      recordAttemptMetadataSafely: true,
      doNotRecordOnDefaultBlockedPath: true,
      keepLastAttemptPassiveUntilFutureStateCreationPhase: true
    },
    requiredFutureGuards: [
      "ENABLE_CUSTOM_25D_MAP must be true",
      "manual must be true",
      "developerIntent must be true",
      "allowRendererInitialization must be true",
      "future state creation must be explicitly approved by a dedicated guard",
      "gameplay overlays, OSM, pins, player marker, capture radius, and backend preservation must be affirmed",
      "startup wiring, drawing, and map attachment must remain blocked"
    ],
    ready: false,
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiChanges: true,
      noGraphics: true,
      noMapAttachment: true,
      noDrawing: true,
      noGameplayChanges: true,
      noOsmChanges: true,
      noPinChanges: true,
      noPlayerMarkerChanges: true,
      noCaptureRadiusChanges: true,
      noBackendChanges: true
    },
    prohibitedActions: [
      "create lifecycle objects",
      "create initialization state",
      "initialize the renderer",
      "create renderer objects or registries",
      "create layers or layer state",
      "attach anything to the map",
      "draw graphics or show visible output",
      "wire startup behavior",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "passive-initialization-state-architecture-decision-review"
  };
}

function getCustom25DVisualRendererInitializationStateArchitectureDecisionReview(options = {}) {
  const architectureDecisionPlanSummary =
    typeof getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-decision-plan-unavailable"
        };
  const architectureHandoffSummary =
    typeof getCustom25DVisualRendererInitializationStateArchitectureHandoff === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureHandoff(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-handoff-unavailable"
        };
  const stateContractReviewSummary =
    typeof getCustom25DVisualRendererInitializationStateContractReview === "function"
      ? getCustom25DVisualRendererInitializationStateContractReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-contract-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    ok: true,
    phase: 177,
    name: "custom-25d-visual-renderer-initialization-state-architecture-decision-review",
    dormant: true,
    passive: true,
    reportOnly: true,
    purpose: "Review the future renderer initialization state architecture decisions while keeping the pathway blocked, internal-only, dormant, and report-only.",
    dependsOn: {
      architectureDecisionPlanSummary: !!architectureDecisionPlanSummary.ok,
      architectureHandoffSummary: !!architectureHandoffSummary.ok,
      stateContractReviewSummary: !!stateContractReviewSummary.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function",
      phase: architectureDecisionPlanSummary.phase || 176
    },
    architectureDecisionPlanSummary: {
      planAvailable: !!architectureDecisionPlanSummary.ok,
      futureStateArchitecturePlanningOnly: !!(
        architectureDecisionPlanSummary.architectureDecisionPlan &&
        architectureDecisionPlanSummary.architectureDecisionPlan.futureInitializationStateArchitecturePlanningOnly
      ),
      futureStateInternalOnly: !!(
        architectureDecisionPlanSummary.architectureDecisionPlan &&
        architectureDecisionPlanSummary.architectureDecisionPlan.futureStateMustRemainInternalOnly
      ),
      futureStateDormantByDefault: !!(
        architectureDecisionPlanSummary.architectureDecisionPlan &&
        architectureDecisionPlanSummary.architectureDecisionPlan.futureStateMustRemainDormantByDefault
      ),
      expectedDefaultBlockedReason:
        architectureDecisionPlanSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    architectureHandoffSummary: {
      handoffAvailable: !!architectureHandoffSummary.ok,
      futureArchitecturePlanningOnly: !!(
        architectureHandoffSummary.stateArchitectureHandoff &&
        architectureHandoffSummary.stateArchitectureHandoff.futureArchitecturePlanningOnly
      ),
      realStateCreationStillProhibited: !!(
        architectureHandoffSummary.stateArchitectureHandoff &&
        architectureHandoffSummary.stateArchitectureHandoff.realStateCreationStillProhibited
      ),
      expectedDefaultBlockedReason:
        architectureHandoffSummary.expectedDefaultBlockedReason ||
        "visual-renderer-initialization-guard-unavailable"
    },
    stateContractReviewSummary: {
      reviewAvailable: !!stateContractReviewSummary.ok,
      futureStateContractRemainsPlanOnly: !!(
        stateContractReviewSummary.reviewFindings &&
        stateContractReviewSummary.reviewFindings.futureStateContractExistsOnlyAsPlan
      ),
      stateCreationBlocked: stateContractReviewSummary.stateCreationAllowed === false,
      initializationStillNotPerformed: stateContractReviewSummary.initializationPerformed === false
    },
    decisionReview: {
      futureInitializationStateArchitectureReviewedOnly: true,
      noActualStateCreatedNow: true,
      futureStateRemainsInternalOnly: true,
      futureStateRemainsDormantByDefault: true,
      futureStateRemainsNonRenderingAndUnattached: true
    },
    reviewedDecisions: [
      "future initialization state should remain internal-only",
      "future initialization state should remain dormant by default",
      "future initialization state should remain blocked by default",
      "future initialization state should not attach to the map",
      "future initialization state should not enable drawing",
      "future initialization state should not wire startup"
    ],
    acceptedFutureStateArchitectureConstraints: [
      "preserve gameplay overlays",
      "preserve OSM behavior",
      "preserve pins",
      "preserve player marker",
      "preserve capture radius",
      "preserve backend behavior",
      "require future dedicated guard approval before any state creation"
    ],
    ready: false,
    defaultDecision: "no-go",
    expectedDefaultBlockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    lifecycleCreationAllowed: false,
    stateCreationAllowed: false,
    initializationPerformed: false,
    safetyBoundaries: {
      noRendererCreation: true,
      noRendererInitialization: true,
      noLifecycleObjectCreation: true,
      noInitializationStateCreation: true,
      noRegistryCreation: true,
      noLayerCreation: true,
      noLayerStateCreation: true,
      noStartupWiring: true,
      noUiChanges: true,
      noGraphics: true,
      noMapAttachment: true,
      noDrawing: true,
      noGameplayChanges: true,
      noOsmChanges: true,
      noPinChanges: true,
      noPlayerMarkerChanges: true,
      noCaptureRadiusChanges: true,
      noBackendChanges: true
    },
    prohibitedActions: [
      "create lifecycle objects",
      "create initialization state",
      "initialize the renderer",
      "create renderer objects or registries",
      "create layers or layer state",
      "attach anything to the map",
      "draw graphics or show visible output",
      "wire startup behavior",
      "change gameplay, OSM, pins, player marker, capture radius, or backend behavior"
    ],
    nextPhaseRecommendation: "passive-initialization-state-architecture-closeout-readiness-report"
  };
}

function getCustom25DVisualRendererInitializationStateArchitectureCloseoutReport(options = {}) {
  const architectureDecisionPlan =
    typeof getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-decision-plan-unavailable"
        };
  const architectureDecisionReview =
    typeof getCustom25DVisualRendererInitializationStateArchitectureDecisionReview === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureDecisionReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-decision-review-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    phase: 178,
    name: "custom-25d-visual-renderer-initialization-state-architecture-closeout-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    dormant: true,
    passive: true,
    reportOnly: true,
    createsInitializationState: false,
    createsLifecycleObject: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    changesGameplay: false,
    reviewedHelpers: {
      architectureDecisionPlan: !!architectureDecisionPlan.ok,
      architectureDecisionReview: !!architectureDecisionReview.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function"
    },
    planningSequenceClosedOut: !!architectureDecisionReview.ok,
    futureImplementationMustRemainSeparate: true,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-future-initialization-state-architecture-planning-only",
    notes: [
      "Passive closeout/readiness report only.",
      "Future initialization state architecture remains internal-only and dormant by default.",
      "Default path remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "Any future phase that creates state, objects, or initializers must remain separate."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateImplementationBoundaryPlan(options = {}) {
  const architectureDecisionPlan =
    typeof getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureDecisionPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-decision-plan-unavailable"
        };
  const architectureDecisionReview =
    typeof getCustom25DVisualRendererInitializationStateArchitectureDecisionReview === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureDecisionReview(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-decision-review-unavailable"
        };
  const architectureCloseout =
    typeof getCustom25DVisualRendererInitializationStateArchitectureCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-closeout-report-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    phase: 179,
    name: "custom-25d-visual-renderer-initialization-state-implementation-boundary-plan",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    dormant: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    createsInitializationState: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    changesGameplay: false,
    boundary: {
      thisPhaseCreatesNothing: true,
      futureCreationWorkMustBeSeparate: true,
      futureCreationWorkMustBeReviewedSeparately: true,
      futureCreationMayOnlyTargetInertInitializationStateShell: true
    },
    allowedFutureWork: [
      "plan a future inert initialization state shell phase",
      "define future dedicated guards for state creation",
      "keep future state internal-only and dormant by default",
      "separate any future creation work from initialization, attachment, and drawing"
    ],
    disallowedThisPhase: [
      "create initialization state",
      "create lifecycle objects",
      "create renderer objects",
      "create registries",
      "create layers or layer state",
      "initialize the renderer",
      "attach anything to the map",
      "draw graphics",
      "wire startup behavior"
    ],
    reviewedHelpers: {
      architectureDecisionPlan: !!architectureDecisionPlan.ok,
      architectureDecisionReview: !!architectureDecisionReview.ok,
      architectureCloseout: !!architectureCloseout.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-future-inert-initialization-state-shell-planning-only",
    notes: [
      "Passive/report-only/planning-only helper.",
      "Default path remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "This phase defines an implementation boundary only and must not create state or objects.",
      "Any future creation work must be isolated into its own phase and reviewed separately."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellReadinessGate(options = {}) {
  const architectureCloseout =
    typeof getCustom25DVisualRendererInitializationStateArchitectureCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateArchitectureCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-architecture-closeout-report-unavailable"
        };
  const implementationBoundaryPlan =
    typeof getCustom25DVisualRendererInitializationStateImplementationBoundaryPlan === "function"
      ? getCustom25DVisualRendererInitializationStateImplementationBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          reason: "visual-renderer-initialization-state-implementation-boundary-plan-unavailable"
        };
  const guardResult =
    typeof canInitializeCustom25DVisualRenderer === "function"
      ? canInitializeCustom25DVisualRenderer(options)
      : {
          allowed: false,
          reason: "visual-renderer-initialization-guard-unavailable"
        };

  return {
    phase: 180,
    name: "custom-25d-visual-renderer-initialization-state-shell-readiness-gate",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason: guardResult.reason || "visual-renderer-initialization-guard-unavailable",
    dormant: true,
    passive: true,
    reportOnly: true,
    gateOnly: true,
    createsInitializationState: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    shellPlanningReadiness: {
      readyForFutureInertShellPlanning: !!implementationBoundaryPlan.ok,
      approvesCreationNow: false
    },
    rendererInitializationReadiness: {
      readyForRendererInitialization: false,
      approvedByThisPhase: false
    },
    mapAttachmentReadiness: {
      readyForMapAttachment: false,
      approvedByThisPhase: false
    },
    drawingReadiness: {
      readyForDrawingOrStartupWiring: false,
      approvedByThisPhase: false
    },
    reviewedHelpers: {
      architectureCloseout: !!architectureCloseout.ok,
      implementationBoundaryPlan: !!implementationBoundaryPlan.ok,
      guardEvaluator: typeof canInitializeCustom25DVisualRenderer === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-future-inert-initialization-state-shell-phase-planning-only",
    notes: [
      "Passive/report-only/gate-only helper.",
      "This phase is only a final readiness gate for future inert shell planning.",
      "This phase does not approve real renderer initialization.",
      "This phase is not ready for map attachment, drawing, or startup wiring."
    ]
  };
}

let custom25DVisualRendererInitializationStateShell = null;

function getCustom25DVisualRendererInitializationStateShell(options = {}) {
  const shell = custom25DVisualRendererInitializationStateShell || {
    key: "custom25DRendererInitializationStateShell",
    phase: 181,
    dormant: true,
    internalOnly: true,
    inert: true,
    ready: false,
    initialized: false,
    attachedToMap: false,
    drawing: false,
    startupWired: false,
    rendererCreated: false,
    lifecycleCreated: false,
    registryCreated: false,
    layerStateCreated: false,
    createdAtPhase: 181,
    createdManually: false,
    metadataVersion: 1,
    metadataExpandedAtPhase: 187,
    architectureStage: "initialization-state-shell",
    rendererInitializationApproved: false,
    mapAttachmentApproved: false,
    drawingApproved: false,
    startupWiringApproved: false,
    visibleBehaviorApproved: false,
    allowedUse: "internal dormant architecture metadata only",
    forbiddenUse: [
      "renderer initialization",
      "map attachment",
      "drawing",
      "startup wiring",
      "visible UI or gameplay behavior"
    ],
    notes: "Default inert initialization state shell. Blocked by default and not created unless manually allowed."
  };

  return {
    ok: true,
    phase: 181,
    dormant: true,
    passive: true,
    shellPresent: custom25DVisualRendererInitializationStateShell !== null,
    blockedReason:
      custom25DVisualRendererInitializationStateShell === null && ENABLE_CUSTOM_25D_MAP !== true
        ? "custom-25d-map-disabled"
        : null,
    shell: { ...shell }
  };
}

function canCreateCustom25DVisualRendererInitializationStateShell(options = {}) {
  let blockedReason = null;

  if (ENABLE_CUSTOM_25D_MAP !== true) {
    blockedReason = "custom-25d-map-disabled";
  } else if (options.manual !== true) {
    blockedReason = "manual-required";
  } else if (options.developerIntent !== true) {
    blockedReason = "developer-intent-required";
  } else if (options.allowInitializationStateShell !== true) {
    blockedReason = "initialization-state-shell-not-allowed";
  } else if (options.inertOnly !== true) {
    blockedReason = "inert-only-required";
  } else if (options.noRendererInitialization !== true) {
    blockedReason = "no-renderer-initialization-required";
  } else if (options.noMapAttachment !== true) {
    blockedReason = "no-map-attachment-required";
  } else if (options.noDrawing !== true) {
    blockedReason = "no-drawing-required";
  } else if (options.noStartupWiring !== true) {
    blockedReason = "no-startup-wiring-required";
  } else if (options.preserveGameplay !== true) {
    blockedReason = "preserve-gameplay-required";
  } else if (options.preserveOSMBehavior !== true) {
    blockedReason = "preserve-osm-behavior-required";
  } else if (options.preservePins !== true) {
    blockedReason = "preserve-pins-required";
  } else if (options.preservePlayerMarker !== true) {
    blockedReason = "preserve-player-marker-required";
  } else if (options.preserveCaptureRadius !== true) {
    blockedReason = "preserve-capture-radius-required";
  }

  return {
    ok: blockedReason === null,
    allowed: blockedReason === null,
    blocked: blockedReason !== null,
    blockedReason,
    phase: 181,
    dormant: true,
    passive: true,
    reportOnly: true
  };
}

function createCustom25DVisualRendererInitializationStateShell(options = {}) {
  const guard = canCreateCustom25DVisualRendererInitializationStateShell(options);

  if (!guard.allowed) {
    return {
      ok: false,
      allowed: false,
      blocked: true,
      blockedReason: guard.blockedReason,
      phase: 181,
      dormant: true,
      createsInitializationState: false,
      initializesRenderer: false,
      attachesToMap: false,
      draws: false,
      wiresStartup: false,
      shell: getCustom25DVisualRendererInitializationStateShell(options).shell
    };
  }

  custom25DVisualRendererInitializationStateShell = {
    key: "custom25DRendererInitializationStateShell",
    phase: 181,
    dormant: true,
    internalOnly: true,
    inert: true,
    ready: false,
    initialized: false,
    attachedToMap: false,
    drawing: false,
    startupWired: false,
    rendererCreated: false,
    lifecycleCreated: false,
    registryCreated: false,
    layerStateCreated: false,
    createdAtPhase: 181,
    createdManually: true,
    metadataVersion: 1,
    metadataExpandedAtPhase: 187,
    architectureStage: "initialization-state-shell",
    rendererInitializationApproved: false,
    mapAttachmentApproved: false,
    drawingApproved: false,
    startupWiringApproved: false,
    visibleBehaviorApproved: false,
    allowedUse: "internal dormant architecture metadata only",
    forbiddenUse: [
      "renderer initialization",
      "map attachment",
      "drawing",
      "startup wiring",
      "visible UI or gameplay behavior"
    ],
    notes: "Inert internal-only initialization state shell. Created manually, not initialized, not attached, not drawn, and not wired to startup."
  };

  return {
    ok: true,
    allowed: true,
    blocked: false,
    blockedReason: null,
    phase: 181,
    dormant: true,
    createsInitializationState: true,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    shell: { ...custom25DVisualRendererInitializationStateShell }
  };
}

function clearCustom25DVisualRendererInitializationStateShell() {
  if (!custom25DVisualRendererInitializationStateShell) {
    return {
      ok: true,
      cleared: false,
      phase: 181,
      dormant: true,
      reason: "No custom 2.5D visual renderer initialization state shell exists to clear."
    };
  }

  custom25DVisualRendererInitializationStateShell = null;

  return {
    ok: true,
    cleared: true,
    phase: 181,
    dormant: true,
    reason: "Custom 2.5D visual renderer initialization state shell cleared."
  };
}

function getCustom25DVisualRendererInitializationStateShellImplementationReadiness(options = {}) {
  const guard = canCreateCustom25DVisualRendererInitializationStateShell(options);

  return {
    ok: true,
    phase: 181,
    name: "custom-25d-visual-renderer-initialization-state-shell-implementation-readiness",
    dormant: true,
    passive: true,
    reportOnly: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason: guard.blockedReason || "custom-25d-map-disabled",
    createsInitializationState: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    shellPresent: custom25DVisualRendererInitializationStateShell !== null,
    manualCreationRequirements: [
      "ENABLE_CUSTOM_25D_MAP === true",
      "options.manual === true",
      "options.developerIntent === true",
      "options.allowInitializationStateShell === true",
      "options.inertOnly === true",
      "options.noRendererInitialization === true",
      "options.noMapAttachment === true",
      "options.noDrawing === true",
      "options.noStartupWiring === true",
      "options.preserveGameplay === true",
      "options.preserveOSMBehavior === true",
      "options.preservePins === true",
      "options.preservePlayerMarker === true",
      "options.preserveCaptureRadius === true"
    ],
    notes: [
      "Passive readiness helper only.",
      "Default behavior remains blocked while ENABLE_CUSTOM_25D_MAP is false.",
      "Manual creation is limited to an inert internal-only plain data shell.",
      "This phase does not approve renderer initialization, map attachment, drawing, or startup wiring."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellValidationReport(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const guard =
    typeof canCreateCustom25DVisualRendererInitializationStateShell === "function"
      ? canCreateCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          allowed: false,
          blocked: true,
          blockedReason: "initialization-state-shell-guard-unavailable"
        };

  const shell = shellState.shell || {};

  return {
    phase: 182,
    name: "custom-25d-visual-renderer-initialization-state-shell-validation-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason: guard.blockedReason || shellState.blockedReason || "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    validationOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    defaultShellValidation: {
      shellPresentByDefault: shellState.shellPresent === false,
      inert: shell.inert === true,
      dormant: shell.dormant === true,
      internalOnly: shell.internalOnly === true,
      ready: shell.ready === false,
      initialized: shell.initialized === false,
      attachedToMap: shell.attachedToMap === false,
      drawing: shell.drawing === false,
      startupWired: shell.startupWired === false
    },
    defaultGuardValidation: {
      blocked: guard.blocked === true,
      allowed: guard.allowed === false,
      blockedReasonIsMapDisabled: guard.blockedReason === "custom-25d-map-disabled"
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellGuard: typeof canCreateCustom25DVisualRendererInitializationStateShell === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-initialization-state-shell-closeout-review",
    notes: [
      "Passive/report-only/validation-only helper.",
      "Validates the default inert shell path and the default blocked guard path.",
      "Does not approve renderer initialization, map attachment, drawing, or startup wiring.",
      "Does not create, clear, or mutate initialization state."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellCloseoutReport(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const guard =
    typeof canCreateCustom25DVisualRendererInitializationStateShell === "function"
      ? canCreateCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          allowed: false,
          blocked: true,
          blockedReason: "initialization-state-shell-guard-unavailable"
        };
  const validationSummary =
    typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-validation-unavailable",
          defaultShellValidation: null,
          defaultGuardValidation: null
        };

  return {
    phase: 183,
    name: "custom-25d-visual-renderer-initialization-state-shell-closeout-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      guard.blockedReason ||
      validationSummary.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    shellCloseout: {
      guardedInternalArchitectureOnly: true,
      defaultStateRemainsDormant: !!(
        validationSummary.defaultShellValidation &&
        validationSummary.defaultShellValidation.dormant
      ),
      defaultStateRemainsInert: !!(
        validationSummary.defaultShellValidation &&
        validationSummary.defaultShellValidation.inert
      ),
      defaultStateRemainsInternalOnly: !!(
        validationSummary.defaultShellValidation &&
        validationSummary.defaultShellValidation.internalOnly
      ),
      defaultStateRemainsNotReady: !!(
        validationSummary.defaultShellValidation &&
        validationSummary.defaultShellValidation.ready
      ) === false
    },
    validationSummary: {
      defaultShellValidation: validationSummary.defaultShellValidation,
      defaultGuardValidation: validationSummary.defaultGuardValidation
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellGuard: typeof canCreateCustom25DVisualRendererInitializationStateShell === "function",
      shellValidation: typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-initialization-state-shell-inventory-or-planning-review",
    notes: [
      "Passive/report-only/closeout-only helper.",
      "The inert shell exists only as guarded internal architecture.",
      "Default guard remains blocked by custom-25d-map-disabled.",
      "This does not approve renderer initialization, map attachment, drawing, startup wiring, or visible behavior."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellInventoryReport(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const guard =
    typeof canCreateCustom25DVisualRendererInitializationStateShell === "function"
      ? canCreateCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          allowed: false,
          blocked: true,
          blockedReason: "initialization-state-shell-guard-unavailable"
        };
  const readiness =
    typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function"
      ? getCustom25DVisualRendererInitializationStateShellImplementationReadiness(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-readiness-unavailable"
        };
  const closeout =
    typeof getCustom25DVisualRendererInitializationStateShellCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-closeout-unavailable",
          shellCloseout: null
        };

  return {
    phase: 184,
    name: "custom-25d-visual-renderer-initialization-state-shell-inventory-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      guard.blockedReason ||
      closeout.blockedReason ||
      readiness.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    inventoryOnly: true,
    readinessOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    helperInventory: {
      getShellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      canCreateShell: typeof canCreateCustom25DVisualRendererInitializationStateShell === "function",
      createShell: typeof createCustom25DVisualRendererInitializationStateShell === "function",
      clearShell: typeof clearCustom25DVisualRendererInitializationStateShell === "function",
      implementationReadiness: typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function",
      validationReport: typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function",
      closeoutReport: typeof getCustom25DVisualRendererInitializationStateShellCloseoutReport === "function"
    },
    allowedCapabilities: [
      "inspect default inert shell state",
      "evaluate guarded shell creation readiness",
      "report passive readiness, validation, and closeout status",
      "manually create an inert internal-only plain data shell only when explicitly guard-approved"
    ],
    forbiddenCapabilities: [
      "approve renderer initialization",
      "attach anything to the map",
      "draw graphics or enable visible behavior",
      "wire startup behavior",
      "create registries",
      "create layers or layer state"
    ],
    defaultSafetySummary: {
      shellPresentByDefault: shellState.shellPresent === false,
      shellDormantByDefault: !!(shellState.shell && shellState.shell.dormant === true),
      shellInternalOnlyByDefault: !!(shellState.shell && shellState.shell.internalOnly === true),
      shellNotReadyByDefault: !!(shellState.shell && shellState.shell.ready === false),
      defaultGuardBlockedByMapDisabled: guard.blockedReason === "custom-25d-map-disabled",
      notConnectedToStartup: true,
      doesNotApproveRendererInitialization: true
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellGuard: typeof canCreateCustom25DVisualRendererInitializationStateShell === "function",
      shellReadiness: typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function",
      shellCloseout: typeof getCustom25DVisualRendererInitializationStateShellCloseoutReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-initialization-state-shell-expansion-plan-or-metadata-review",
    notes: [
      "Passive/report-only/inventory-only/readiness-only helper.",
      "Inventories the initialization state shell helper family without creating or clearing anything.",
      "The family is not connected to startup and does not approve renderer initialization.",
      "Any future shell expansion must remain separately reviewed and inert-only."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellExpansionPlan(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const readiness =
    typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function"
      ? getCustom25DVisualRendererInitializationStateShellImplementationReadiness(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-readiness-unavailable"
        };
  const validation =
    typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-validation-unavailable"
        };
  const inventory =
    typeof getCustom25DVisualRendererInitializationStateShellInventoryReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellInventoryReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-inventory-unavailable",
          helperInventory: null
        };

  return {
    phase: 185,
    name: "custom-25d-visual-renderer-initialization-state-shell-expansion-plan",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      inventory.blockedReason ||
      validation.blockedReason ||
      readiness.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    currentShellFamily: {
      shellPresentByDefault: shellState.shellPresent === false,
      helperInventory: inventory.helperInventory,
      inertByDefault: !!(validation.defaultShellValidation && validation.defaultShellValidation.inert),
      internalOnlyByDefault: !!(
        validation.defaultShellValidation && validation.defaultShellValidation.internalOnly
      ),
      blockedByDefault: inventory.blockedReason === "custom-25d-map-disabled"
    },
    proposedFutureMetadata: [
      "lastManualGuardEvaluationSummary",
      "lastValidationPhase",
      "lastCloseoutPhase",
      "expansionReviewStatus",
      "inertMetadataRevision"
    ],
    disallowedExpansion: [
      "metadata that initializes the renderer",
      "metadata that attaches anything to the map",
      "metadata that enables drawing or visible behavior",
      "metadata that wires startup",
      "metadata that creates registries, layers, or layer state"
    ],
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellReadiness: typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function",
      shellValidation: typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function",
      shellInventory: typeof getCustom25DVisualRendererInitializationStateShellInventoryReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-inert-shell-metadata-expansion-review-or-planning-closeout",
    notes: [
      "Passive/report-only/planning-only helper.",
      "Defines only a safe future inert metadata expansion plan for the initialization state shell.",
      "Metadata expansion must remain separately reviewed, internal-only, dormant, and non-wired.",
      "This does not approve renderer initialization, map attachment, drawing, startup wiring, or visible behavior."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const validation =
    typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-validation-unavailable"
        };
  const inventory =
    typeof getCustom25DVisualRendererInitializationStateShellInventoryReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellInventoryReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-inventory-unavailable"
        };
  const expansionPlan =
    typeof getCustom25DVisualRendererInitializationStateShellExpansionPlan === "function"
      ? getCustom25DVisualRendererInitializationStateShellExpansionPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-expansion-plan-unavailable"
        };

  return {
    phase: 186,
    name: "custom-25d-visual-renderer-initialization-state-shell-metadata-readiness-gate",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      expansionPlan.blockedReason ||
      inventory.blockedReason ||
      validation.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    gateOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    metadataExpansionReadiness: {
      readyForFutureSeparatelyReviewedMetadataExpansion: !!expansionPlan.ok,
      approvesMetadataExpansionNow: false
    },
    rendererInitializationReadiness: {
      readyForRendererInitialization: false,
      metadataReadinessIsNotRendererInitializationReadiness: true
    },
    mapAttachmentReadiness: {
      readyForMapAttachment: false,
      approvesVisibleMapBehavior: false
    },
    startupWiringReadiness: {
      readyForStartupWiring: false,
      approvesStartupIntegration: false
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellValidation: typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function",
      shellInventory: typeof getCustom25DVisualRendererInitializationStateShellInventoryReport === "function",
      shellExpansionPlan: typeof getCustom25DVisualRendererInitializationStateShellExpansionPlan === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-shell-metadata-expansion-closeout-or-separate-reviewed-expansion-plan",
    notes: [
      "Passive/report-only/gate-only helper.",
      "Acts only as the final passive gate before any separately reviewed inert metadata expansion.",
      "Metadata expansion readiness is not renderer initialization readiness.",
      "No visible, map, drawing, or startup behavior is approved in this phase."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellMetadataValidationReport(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const validation =
    typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-validation-unavailable"
        };
  const expansionPlan =
    typeof getCustom25DVisualRendererInitializationStateShellExpansionPlan === "function"
      ? getCustom25DVisualRendererInitializationStateShellExpansionPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-expansion-plan-unavailable"
        };
  const metadataGate =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-readiness-gate-unavailable"
        };

  const shell = shellState.shell || {};
  const plainDataApproved =
    typeof shell.metadataVersion === "number" &&
    typeof shell.metadataExpandedAtPhase === "number" &&
    typeof shell.architectureStage === "string" &&
    typeof shell.allowedUse === "string" &&
    Array.isArray(shell.forbiddenUse);

  return {
    phase: 188,
    name: "custom-25d-visual-renderer-initialization-state-shell-metadata-validation-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      metadataGate.blockedReason ||
      expansionPlan.blockedReason ||
      validation.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    validationOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    metadataValidation: {
      metadataVersionIsOne: shell.metadataVersion === 1,
      metadataExpandedAtPhaseIs187: shell.metadataExpandedAtPhase === 187,
      architectureStageIsInitializationStateShell:
        shell.architectureStage === "initialization-state-shell",
      metadataExpansionDoesNotImplyRendererInitializationReadiness: true
    },
    approvalFlagValidation: {
      rendererInitializationApproved: shell.rendererInitializationApproved === false,
      mapAttachmentApproved: shell.mapAttachmentApproved === false,
      drawingApproved: shell.drawingApproved === false,
      startupWiringApproved: shell.startupWiringApproved === false,
      visibleBehaviorApproved: shell.visibleBehaviorApproved === false
    },
    plainDataValidation: {
      allowedUseIsString: typeof shell.allowedUse === "string",
      forbiddenUseIsArray: Array.isArray(shell.forbiddenUse),
      metadataLooksLikePlainDataOnly: plainDataApproved
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellValidation: typeof getCustom25DVisualRendererInitializationStateShellValidationReport === "function",
      shellExpansionPlan: typeof getCustom25DVisualRendererInitializationStateShellExpansionPlan === "function",
      shellMetadataGate: typeof getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-shell-metadata-validation-closeout-or-separate-reviewed-metadata-plan",
    notes: [
      "Passive/report-only/validation-only helper.",
      "Validates that inert shell metadata remains plain data only.",
      "Validates that all approval flags remain false.",
      "No visible, map, drawing, startup, or renderer initialization behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const validation =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-validation-unavailable",
          metadataValidation: null,
          approvalFlagValidation: null,
          plainDataValidation: null
        };
  const metadataGate =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-readiness-gate-unavailable"
        };

  return {
    phase: 189,
    name: "custom-25d-visual-renderer-initialization-state-shell-metadata-closeout-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      metadataGate.blockedReason ||
      validation.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    readinessOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    metadataCloseout: {
      metadataRemainsInertPlainDataOnly: !!(
        validation.plainDataValidation && validation.plainDataValidation.metadataLooksLikePlainDataOnly
      ),
      metadataExpansionRemainsPassiveOnly: true,
      metadataDoesNotApproveRendererInitialization: true
    },
    approvalFlagSummary: {
      rendererInitializationApproved: !!(
        validation.approvalFlagValidation &&
        validation.approvalFlagValidation.rendererInitializationApproved
      ),
      mapAttachmentApproved: !!(
        validation.approvalFlagValidation &&
        validation.approvalFlagValidation.mapAttachmentApproved
      ),
      drawingApproved: !!(
        validation.approvalFlagValidation &&
        validation.approvalFlagValidation.drawingApproved
      ),
      startupWiringApproved: !!(
        validation.approvalFlagValidation &&
        validation.approvalFlagValidation.startupWiringApproved
      ),
      visibleBehaviorApproved: !!(
        validation.approvalFlagValidation &&
        validation.approvalFlagValidation.visibleBehaviorApproved
      )
    },
    defaultSafetySummary: {
      defaultGuardBlockedByMapDisabled:
        metadataGate.blockedReason === "custom-25d-map-disabled" ||
        shellState.blockedReason === "custom-25d-map-disabled",
      noRendererInitializationApproved: true,
      noMapAttachmentApproved: true,
      noDrawingApproved: true,
      noStartupWiringApproved: true
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      shellMetadataValidation:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function",
      shellMetadataGate:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataReadinessGate === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-metadata-schema-inventory-or-separately-reviewed-inert-schema-plan",
    notes: [
      "Passive/report-only/closeout-only/readiness-only helper.",
      "Summarizes metadata expansion closeout without creating, clearing, mutating, or expanding shell state.",
      "Default guard remains blocked by custom-25d-map-disabled.",
      "No renderer initialization, map attachment, drawing, startup wiring, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellSchemaPlan(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const metadataValidation =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-validation-unavailable"
        };
  const metadataCloseout =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-closeout-unavailable"
        };

  return {
    phase: 190,
    name: "custom-25d-visual-renderer-initialization-state-shell-schema-plan",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      metadataCloseout.blockedReason ||
      metadataValidation.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    schemaOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    currentSchemaSignals: {
      shellKeyPresent: !!(shellState.shell && shellState.shell.key),
      architectureStagePresent: !!(shellState.shell && shellState.shell.architectureStage),
      metadataVersionPresent: !!(shellState.shell && shellState.shell.metadataVersion === 1),
      metadataStillPlainDataOnly: !!(
        metadataValidation.plainDataValidation &&
        metadataValidation.plainDataValidation.metadataLooksLikePlainDataOnly
      ),
      approvalFlagsRemainFalse: !!(
        metadataValidation.approvalFlagValidation &&
        metadataValidation.approvalFlagValidation.rendererInitializationApproved &&
        metadataValidation.approvalFlagValidation.mapAttachmentApproved &&
        metadataValidation.approvalFlagValidation.drawingApproved &&
        metadataValidation.approvalFlagValidation.startupWiringApproved &&
        metadataValidation.approvalFlagValidation.visibleBehaviorApproved
      )
    },
    futureSchemaPlan: [
      "document inert shell field meanings",
      "document optional future metadata inventory fields",
      "document approval fields as permanently false unless separately reviewed",
      "document schema boundaries between metadata and behavior",
      "document that schema planning does not imply state creation or runtime enforcement"
    ],
    disallowedSchemaWork: [
      "runtime schema enforcement",
      "schema-driven renderer initialization",
      "schema-driven map attachment",
      "schema-driven drawing or visible behavior",
      "schema-driven startup wiring",
      "schema work that creates state, registries, layers, or layer state"
    ],
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      metadataValidation:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function",
      metadataCloseout:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-schema-inventory-review-or-separately-reviewed-inert-schema-expansion-plan",
    notes: [
      "Passive/report-only/planning-only/schema-only helper.",
      "Separates future schema documentation from actual state creation, runtime validation enforcement, and renderer behavior.",
      "Any future schema expansion must remain separately reviewed, inert, internal-only, and non-wired.",
      "No renderer initialization, map attachment, drawing, startup wiring, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const metadataValidation =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-validation-unavailable"
        };
  const schemaPlan =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaPlan === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-plan-unavailable"
        };

  return {
    phase: 191,
    name: "custom-25d-visual-renderer-initialization-state-shell-schema-readiness-gate",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      schemaPlan.blockedReason ||
      metadataValidation.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    gateOnly: true,
    schemaOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    schemaInventoryReadiness: {
      readyForFutureSeparatelyReviewedSchemaInventoryExpansion: !!schemaPlan.ok,
      approvesSchemaExpansionNow: false
    },
    runtimeValidationReadiness: {
      readyForRuntimeSchemaValidation: false,
      schemaReadinessIsNotRuntimeValidationReadiness: true
    },
    rendererInitializationReadiness: {
      readyForRendererInitialization: false,
      schemaReadinessIsNotRendererInitializationReadiness: true
    },
    mapAttachmentReadiness: {
      readyForMapAttachment: false,
      approvesVisibleMapBehavior: false
    },
    startupWiringReadiness: {
      readyForStartupWiring: false,
      approvesStartupIntegration: false
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      metadataValidation:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function",
      schemaPlan: typeof getCustom25DVisualRendererInitializationStateShellSchemaPlan === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-schema-closeout-or-separately-reviewed-inert-schema-inventory-expansion",
    notes: [
      "Passive/report-only/gate-only/schema-only helper.",
      "Acts only as the final passive gate before any separately reviewed inert schema inventory or documentation expansion.",
      "Schema readiness is not runtime validation readiness and not renderer initialization readiness.",
      "No visible, map, drawing, startup, or renderer behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellSchemaInventory(options = {}) {
  const shellState =
    typeof getCustom25DVisualRendererInitializationStateShell === "function"
      ? getCustom25DVisualRendererInitializationStateShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-unavailable",
          shell: null
        };
  const metadataValidation =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-validation-unavailable"
        };
  const schemaReadinessGate =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-readiness-gate-unavailable"
        };

  return {
    phase: 192,
    name: "custom-25d-visual-renderer-initialization-state-shell-schema-inventory",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      schemaReadinessGate.blockedReason ||
      metadataValidation.blockedReason ||
      shellState.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    inventoryOnly: true,
    schemaOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    expectedShellFields: [
      "key",
      "phase",
      "dormant",
      "internalOnly",
      "inert",
      "ready",
      "initialized",
      "attachedToMap",
      "drawing",
      "startupWired",
      "rendererCreated",
      "lifecycleCreated",
      "registryCreated",
      "layerStateCreated",
      "createdAtPhase",
      "createdManually",
      "metadataVersion",
      "metadataExpandedAtPhase",
      "architectureStage",
      "rendererInitializationApproved",
      "mapAttachmentApproved",
      "drawingApproved",
      "startupWiringApproved",
      "visibleBehaviorApproved",
      "allowedUse",
      "forbiddenUse",
      "notes"
    ],
    expectedInertValues: {
      ready: false,
      initialized: false,
      attachedToMap: false,
      drawing: false,
      startupWired: false,
      rendererCreated: false,
      lifecycleCreated: false,
      registryCreated: false,
      layerStateCreated: false,
      rendererInitializationApproved: false,
      mapAttachmentApproved: false,
      drawingApproved: false,
      startupWiringApproved: false,
      visibleBehaviorApproved: false
    },
    forbiddenFieldTypes: [
      "DOM nodes",
      "map references",
      "Leaflet objects",
      "timers",
      "event handlers",
      "network calls",
      "storage writes",
      "backend references",
      "renderer objects",
      "lifecycle objects",
      "registries",
      "layer objects",
      "mutable startup hooks"
    ],
    currentShellSnapshotSummary: {
      shellPresentByDefault: shellState.shellPresent === false,
      shellKeyPresent: !!(shellState.shell && shellState.shell.key),
      architectureStagePresent: !!(shellState.shell && shellState.shell.architectureStage),
      metadataStillPlainDataOnly: !!(
        metadataValidation.plainDataValidation &&
        metadataValidation.plainDataValidation.metadataLooksLikePlainDataOnly
      ),
      noRendererBehaviorApproved: !!(
        metadataValidation.approvalFlagValidation &&
        metadataValidation.approvalFlagValidation.rendererInitializationApproved &&
        metadataValidation.approvalFlagValidation.mapAttachmentApproved &&
        metadataValidation.approvalFlagValidation.drawingApproved &&
        metadataValidation.approvalFlagValidation.startupWiringApproved &&
        metadataValidation.approvalFlagValidation.visibleBehaviorApproved
      )
    },
    reviewedHelpers: {
      shellState: typeof getCustom25DVisualRendererInitializationStateShell === "function",
      metadataValidation:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function",
      schemaReadinessGate:
        typeof getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-schema-inventory-closeout-or-separately-reviewed-inert-schema-documentation-expansion",
    notes: [
      "Passive/report-only/inventory-only/schema-only helper.",
      "Inventories expected inert shell fields and forbidden field/value types only.",
      "This is documentation/inventory only, not runtime schema enforcement.",
      "No renderer initialization, map attachment, drawing, startup wiring, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellSchemaInventoryValidationReport(options = {}) {
  const schemaInventory =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaInventory === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaInventory(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-inventory-unavailable"
        };
  const schemaReadinessGate =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-readiness-gate-unavailable"
        };
  const metadataValidation =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-validation-unavailable"
        };

  const forbiddenFieldTypes = Array.isArray(schemaInventory.forbiddenFieldTypes)
    ? schemaInventory.forbiddenFieldTypes
    : [];

  return {
    phase: 193,
    name: "custom-25d-visual-renderer-initialization-state-shell-schema-inventory-validation-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason:
      schemaInventory.blockedReason ||
      schemaReadinessGate.blockedReason ||
      metadataValidation.blockedReason ||
      "custom-25d-map-disabled",
    dormant: true,
    passive: true,
    reportOnly: true,
    validationOnly: true,
    schemaOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    schemaInventoryValidation: {
      schemaInventoryAvailable: !!schemaInventory.ok,
      documentationOnlyInventory: schemaInventory.reportOnly === true && schemaInventory.inventoryOnly === true,
      schemaOnlyInventory: schemaInventory.schemaOnly === true,
      runtimeSchemaEnforcementImplied: false,
      rendererInitializationReadinessImplied: false
    },
    forbiddenTypeCoverage: {
      coversDomNodes: forbiddenFieldTypes.includes("DOM nodes"),
      coversMapReferences: forbiddenFieldTypes.includes("map references"),
      coversLeafletObjects: forbiddenFieldTypes.includes("Leaflet objects"),
      coversTimers: forbiddenFieldTypes.includes("timers"),
      coversEventHandlers: forbiddenFieldTypes.includes("event handlers"),
      coversNetworkCalls: forbiddenFieldTypes.includes("network calls"),
      coversStorageWrites: forbiddenFieldTypes.includes("storage writes"),
      coversBackendReferences: forbiddenFieldTypes.includes("backend references"),
      coversRendererObjects: forbiddenFieldTypes.includes("renderer objects"),
      coversLifecycleObjects: forbiddenFieldTypes.includes("lifecycle objects"),
      coversRegistries: forbiddenFieldTypes.includes("registries"),
      coversLayerObjects: forbiddenFieldTypes.includes("layer objects"),
      coversMutableStartupHooks: forbiddenFieldTypes.includes("mutable startup hooks")
    },
    documentationOnlyValidation: {
      inventoryIsDocumentationOnly: true,
      notRuntimeSchemaValidation: true,
      notRendererInitializationApproval: true,
      noVisibleMapStartupBehaviorApproved: true
    },
    reviewedHelpers: {
      schemaInventory:
        typeof getCustom25DVisualRendererInitializationStateShellSchemaInventory === "function",
      schemaReadinessGate:
        typeof getCustom25DVisualRendererInitializationStateShellSchemaReadinessGate === "function",
      metadataValidation:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataValidationReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-schema-inventory-closeout-or-separately-reviewed-inert-schema-documentation-summary",
    notes: [
      "Passive/report-only/validation-only/schema-only helper.",
      "Validates that schema inventory remains documentation-only and does not imply runtime validation.",
      "Validates forbidden field/value type coverage without creating or mutating shell state.",
      "No visible, map, drawing, startup, or renderer behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellSchemaInventoryCloseoutReport(options = {}) {
  const schemaInventory =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaInventory === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaInventory(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-inventory-unavailable"
        };
  const inventoryValidation =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaInventoryValidationReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaInventoryValidationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-inventory-validation-unavailable"
        };
  const metadataCloseout =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-closeout-unavailable"
        };
  const blockedReason =
    inventoryValidation.blockedReason ||
    schemaInventory.blockedReason ||
    metadataCloseout.blockedReason ||
    "custom-25d-map-disabled";

  return {
    phase: 194,
    name: "custom-25d-visual-renderer-initialization-state-shell-schema-inventory-closeout-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    readinessOnly: true,
    schemaOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    schemaInventoryCloseout: {
      schemaInventoryDocumented: !!schemaInventory.ok,
      schemaInventoryValidated: !!inventoryValidation.ok,
      schemaInventoryRemainsDocumentationOnly: !!(
        inventoryValidation.documentationOnlyValidation &&
        inventoryValidation.documentationOnlyValidation.inventoryIsDocumentationOnly
      ),
      rendererInitializationApproved: false
    },
    documentationOnlySummary: {
      documentationOnly: true,
      inertSchemaInventoryOnly: true,
      doesNotApproveVisibleBehavior: true
    },
    runtimeValidationSummary: {
      runtimeSchemaEnforcementActive: false,
      runtimeValidatorsAdded: false,
      runtimeValidationImplied: false
    },
    defaultSafetySummary: {
      defaultGuardBlockedByMapFlag: blockedReason === "custom-25d-map-disabled",
      rendererInitializationApproved: false,
      mapAttachmentApproved: false,
      drawingApproved: false,
      startupWiringApproved: false,
      visibleBehaviorApproved: false
    },
    reviewedHelpers: {
      schemaInventory:
        typeof getCustom25DVisualRendererInitializationStateShellSchemaInventory === "function",
      inventoryValidation:
        typeof getCustom25DVisualRendererInitializationStateShellSchemaInventoryValidationReport === "function",
      metadataCloseout:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-schema-documentation-planning-or-separately-reviewed-inert-schema-expansion",
    notes: [
      "Passive/report-only/closeout-only/readiness-only/schema-only helper.",
      "Summarizes schema inventory closeout without creating state or enforcing runtime schema.",
      "Confirms runtime validators were not added and no renderer, map, drawing, or startup behavior is approved.",
      "Recommends only passive planning or separately reviewed inert schema documentation expansion next."
    ]
  };
}

function getCustom25DVisualRendererInitializationStateShellHandoffReadinessReport(options = {}) {
  const shellCloseout =
    typeof getCustom25DVisualRendererInitializationStateShellCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-closeout-unavailable"
        };
  const metadataCloseout =
    typeof getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-metadata-closeout-unavailable"
        };
  const schemaCloseout =
    typeof getCustom25DVisualRendererInitializationStateShellSchemaInventoryCloseoutReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellSchemaInventoryCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-schema-inventory-closeout-unavailable"
        };
  const shellReadiness =
    typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function"
      ? getCustom25DVisualRendererInitializationStateShellImplementationReadiness(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-implementation-readiness-unavailable"
        };
  const blockedReason =
    schemaCloseout.blockedReason ||
    metadataCloseout.blockedReason ||
    shellCloseout.blockedReason ||
    shellReadiness.blockedReason ||
    "custom-25d-map-disabled";

  return {
    phase: 195,
    name: "custom-25d-visual-renderer-initialization-state-shell-handoff-readiness-report",
    ok: true,
    ready: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    handoffOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    handoffReadiness: {
      readyForFuturePlanningOnly: true,
      readyForRendererInitialization: false,
      readyForRuntimeSchemaEnforcement: false,
      readyForMapAttachment: false,
      readyForDrawing: false,
      readyForStartupWiring: false,
      readyForVisibleBehavior: false
    },
    shellSequenceSummary: {
      inertShellArchitectureExists: !!shellCloseout.ok,
      manualGuardBehaviorDocumented: !!shellReadiness.ok,
      metadataExpansionReviewed: !!metadataCloseout.ok,
      shellStillDormantAndInternalOnly: true
    },
    schemaSequenceSummary: {
      schemaInventoryClosedOut: !!schemaCloseout.ok,
      schemaRemainsDocumentationOnly: !!(
        schemaCloseout.documentationOnlySummary &&
        schemaCloseout.documentationOnlySummary.documentationOnly
      ),
      runtimeSchemaEnforcementInactive: !!(
        schemaCloseout.runtimeValidationSummary &&
        schemaCloseout.runtimeValidationSummary.runtimeSchemaEnforcementActive === false
      ),
      runtimeValidatorsNotAdded: !!(
        schemaCloseout.runtimeValidationSummary &&
        schemaCloseout.runtimeValidationSummary.runtimeValidatorsAdded === false
      )
    },
    futureBoundaryRecommendation: {
      nextWorkMustRemainPassiveOrSeparatelyReviewed: true,
      notRendererInitializationReadiness: true,
      notMapAttachmentReadiness: true,
      notDrawingReadiness: true,
      notStartupWiringReadiness: true
    },
    reviewedHelpers: {
      shellCloseout:
        typeof getCustom25DVisualRendererInitializationStateShellCloseoutReport === "function",
      metadataCloseout:
        typeof getCustom25DVisualRendererInitializationStateShellMetadataCloseoutReport === "function",
      schemaCloseout:
        typeof getCustom25DVisualRendererInitializationStateShellSchemaInventoryCloseoutReport === "function",
      shellReadiness:
        typeof getCustom25DVisualRendererInitializationStateShellImplementationReadiness === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-boundary-planning-or-separately-reviewed-inert-architecture-phase",
    notes: [
      "Passive/report-only/readiness-only/handoff-only helper.",
      "Summarizes inert shell, metadata, and schema readiness for future planning only.",
      "This is not readiness for renderer initialization, runtime schema enforcement, map attachment, drawing, startup wiring, or visible behavior.",
      "Recommends only passive lifecycle-boundary planning or another separately reviewed inert architecture phase next."
    ]
  };
}

function getCustom25DVisualRendererLifecycleBoundaryPlan(options = {}) {
  const shellHandoff =
    typeof getCustom25DVisualRendererInitializationStateShellHandoffReadinessReport === "function"
      ? getCustom25DVisualRendererInitializationStateShellHandoffReadinessReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-shell-handoff-readiness-unavailable"
        };
  const stateBoundaryPlan =
    typeof getCustom25DVisualRendererInitializationStateImplementationBoundaryPlan === "function"
      ? getCustom25DVisualRendererInitializationStateImplementationBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "initialization-state-implementation-boundary-plan-unavailable"
        };
  const priorLifecycleBoundary =
    typeof getCustom25DVisualRendererInitializationLifecycleBoundaryPlan === "function"
      ? getCustom25DVisualRendererInitializationLifecycleBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-initialization-lifecycle-boundary-plan-unavailable"
        };
  const blockedReason =
    shellHandoff.blockedReason ||
    stateBoundaryPlan.blockedReason ||
    priorLifecycleBoundary.blockedReason ||
    "custom-25d-map-disabled";

  return {
    phase: 196,
    name: "custom-25d-visual-renderer-lifecycle-boundary-plan",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    boundaryOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleBoundaries: {
      construct: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      initialize: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      attach: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      render: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      update: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      suspend: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      resume: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      destroy: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      clear: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      }
    },
    handoffReadiness: {
      futurePlanningOnly: true,
      rendererInitializationReadiness: false,
      runtimeSchemaEnforcementReadiness: false,
      mapAttachmentReadiness: false,
      drawingReadiness: false,
      startupWiringReadiness: false
    },
    reviewedHelpers: {
      shellHandoff:
        typeof getCustom25DVisualRendererInitializationStateShellHandoffReadinessReport === "function",
      stateBoundaryPlan:
        typeof getCustom25DVisualRendererInitializationStateImplementationBoundaryPlan === "function",
      priorLifecycleBoundary:
        typeof getCustom25DVisualRendererInitializationLifecycleBoundaryPlan === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-boundary-review-or-separately-reviewed-inert-lifecycle-architecture-phase",
    notes: [
      "Passive/report-only/planning-only/boundary-only helper.",
      "Plans a future lifecycle boundary contract only and does not create lifecycle objects, renderer state, or initialization state.",
      "All lifecycle stages remain not implemented, not active, not wired, and not approved.",
      "No renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleBoundaryReadinessReport(options = {}) {
  const lifecycleBoundaryPlan =
    typeof getCustom25DVisualRendererLifecycleBoundaryPlan === "function"
      ? getCustom25DVisualRendererLifecycleBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-boundary-plan-unavailable"
        };
  const blockedReason = lifecycleBoundaryPlan.blockedReason || "custom-25d-map-disabled";

  return {
    phase: 197,
    name: "custom-25d-visual-renderer-lifecycle-boundary-readiness-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reviewOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleStageReadiness: {
      construct: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      initialize: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      attach: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      render: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      update: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      suspend: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      resume: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      destroy: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      },
      clear: {
        implemented: false,
        active: false,
        wired: false,
        approved: false
      }
    },
    readinessSummary: {
      readyForFutureContractDesignOnly: !!lifecycleBoundaryPlan.ok,
      readyForImplementation: false,
      readyForRendererInitialization: false,
      readyForMapAttachment: false,
      readyForDrawing: false,
      readyForStartupWiring: false,
      readyForRuntimeSchemaEnforcement: false
    },
    reviewedHelpers: {
      lifecycleBoundaryPlan: typeof getCustom25DVisualRendererLifecycleBoundaryPlan === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-boundary-closeout-or-separately-reviewed-future-contract-design-phase",
    notes: [
      "Passive/report-only/readiness-only/review-only helper.",
      "Reviews whether the lifecycle boundary plan is ready for future contract design only.",
      "All lifecycle stages remain future-only, not implemented, not active, not wired, and not approved.",
      "No implementation, renderer initialization, map attachment, drawing, startup wiring, or runtime schema enforcement is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleContractInventory(options = {}) {
  const lifecycleBoundaryReadiness =
    typeof getCustom25DVisualRendererLifecycleBoundaryReadinessReport === "function"
      ? getCustom25DVisualRendererLifecycleBoundaryReadinessReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-boundary-readiness-unavailable"
        };
  const blockedReason = lifecycleBoundaryReadiness.blockedReason || "custom-25d-map-disabled";

  const stageContract = (stageKey, intendedRole, dependencies = []) => ({
    stageKey,
    intendedRole,
    requiredInputs: [
      "manual developer-approved future contract",
      "dormant renderer lifecycle architecture",
      "preserved gameplay and OSM safety boundaries"
    ],
    expectedOutputs: [
      "planning-only lifecycle contract definition",
      "no renderer behavior",
      "no visible map behavior"
    ],
    allowedSideEffects: [],
    disallowedSideEffects: [
      "renderer initialization",
      "map attachment",
      "drawing",
      "startup wiring",
      "runtime schema enforcement",
      "registry creation",
      "layer or layer state creation"
    ],
    dependencies,
    safetyGateRequired: true,
    implementationStatus: {
      implemented: false,
      active: false,
      wired: false,
      futureOnly: true
    },
    approvalStatus: {
      approved: false
    }
  });

  return {
    phase: 198,
    name: "custom-25d-visual-renderer-lifecycle-contract-inventory",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    inventoryOnly: true,
    contractPlanningOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "lifecycle-contract-inventory-planning-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleContractInventory: {
      construct: stageContract("construct", "future renderer lifecycle construction boundary", [
        "manual safety gate",
        "future lifecycle contract review"
      ]),
      initialize: stageContract("initialize", "future renderer initialization boundary", [
        "construct contract",
        "manual safety gate"
      ]),
      attach: stageContract("attach", "future map attachment boundary", [
        "initialize contract",
        "map safety gate"
      ]),
      render: stageContract("render", "future renderer draw boundary", [
        "attach contract",
        "drawing safety gate"
      ]),
      update: stageContract("update", "future renderer update boundary", [
        "render contract",
        "state safety gate"
      ]),
      suspend: stageContract("suspend", "future renderer suspension boundary", [
        "update contract",
        "lifecycle safety gate"
      ]),
      resume: stageContract("resume", "future renderer resume boundary", [
        "suspend contract",
        "lifecycle safety gate"
      ]),
      destroy: stageContract("destroy", "future renderer teardown boundary", [
        "resume contract",
        "cleanup safety gate"
      ]),
      clear: stageContract("clear", "future renderer clear/reset boundary", [
        "destroy contract",
        "cleanup safety gate"
      ])
    },
    reviewedHelpers: {
      lifecycleBoundaryReadiness:
        typeof getCustom25DVisualRendererLifecycleBoundaryReadinessReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-contract-inventory-review-or-separately-reviewed-future-contract-schema-phase",
    notes: [
      "Passive/report-only/inventory-only/contract-planning-only helper.",
      "Inventories future lifecycle contract shape only for construct, initialize, attach, render, update, suspend, resume, destroy, and clear.",
      "Every lifecycle stage remains future-only, not implemented, not active, not wired, and not approved.",
      "No lifecycle object, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleContractReadinessReport(options = {}) {
  const lifecycleContractInventory =
    typeof getCustom25DVisualRendererLifecycleContractInventory === "function"
      ? getCustom25DVisualRendererLifecycleContractInventory(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-contract-inventory-unavailable"
        };
  const blockedReason = lifecycleContractInventory.blockedReason || "custom-25d-map-disabled";

  return {
    phase: 199,
    name: "custom-25d-visual-renderer-lifecycle-contract-readiness-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reviewOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "lifecycle-contract-readiness-review-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleContractReadiness: {
      construct: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      initialize: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      attach: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      render: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      update: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      suspend: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      resume: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      destroy: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      clear: {
        hasContractPlanningFields: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      }
    },
    reviewedContractFields: [
      "stageKey",
      "intendedRole",
      "requiredInputs",
      "expectedOutputs",
      "allowedSideEffects",
      "disallowedSideEffects",
      "dependencies",
      "safetyGateRequired",
      "implementationStatus",
      "approvalStatus"
    ],
    readinessSummary: {
      readyForFutureLifecycleContractOnly: !!lifecycleContractInventory.ok,
      readyToApproveLifecycle: false,
      readyToCreateLifecycleObject: false,
      readyToInitializeRenderer: false,
      readyToValidateRuntimeSchema: false,
      readyToAttachOrDraw: false
    },
    reviewedHelpers: {
      lifecycleContractInventory:
        typeof getCustom25DVisualRendererLifecycleContractInventory === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-contract-closeout-or-separately-reviewed-future-lifecycle-contract-phase",
    notes: [
      "Passive/report-only/readiness-only/review-only helper.",
      "Reviews the future-only lifecycle contract inventory for construct, initialize, attach, render, update, suspend, resume, destroy, and clear.",
      "Does not approve, create, validate, or implement the lifecycle.",
      "No lifecycle object, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleContractCloseoutReport(options = {}) {
  const lifecycleBoundaryPlan =
    typeof getCustom25DVisualRendererLifecycleBoundaryPlan === "function"
      ? getCustom25DVisualRendererLifecycleBoundaryPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-boundary-plan-unavailable"
        };
  const lifecycleBoundaryReadiness =
    typeof getCustom25DVisualRendererLifecycleBoundaryReadinessReport === "function"
      ? getCustom25DVisualRendererLifecycleBoundaryReadinessReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-boundary-readiness-unavailable"
        };
  const lifecycleContractInventory =
    typeof getCustom25DVisualRendererLifecycleContractInventory === "function"
      ? getCustom25DVisualRendererLifecycleContractInventory(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-contract-inventory-unavailable"
        };
  const lifecycleContractReadiness =
    typeof getCustom25DVisualRendererLifecycleContractReadinessReport === "function"
      ? getCustom25DVisualRendererLifecycleContractReadinessReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-contract-readiness-unavailable"
        };
  const blockedReason =
    lifecycleContractReadiness.blockedReason ||
    lifecycleContractInventory.blockedReason ||
    lifecycleBoundaryReadiness.blockedReason ||
    lifecycleBoundaryPlan.blockedReason ||
    "custom-25d-map-disabled";

  return {
    phase: 200,
    name: "custom-25d-visual-renderer-lifecycle-contract-closeout-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    summaryOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "lifecycle-contract-closeout-summary-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleStagesSummary: {
      construct: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      initialize: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      attach: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      render: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      update: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      suspend: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      resume: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      destroy: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      },
      clear: {
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true
      }
    },
    closeoutStatus: {
      lifecycleBoundaryPlanned: true,
      lifecycleBoundaryReadinessReviewed: true,
      lifecycleContractInventoried: true,
      lifecycleContractReadinessReviewed: true,
      lifecycleImplementationApproved: false
    },
    reviewedHelpers: {
      lifecycleBoundaryPlan: typeof getCustom25DVisualRendererLifecycleBoundaryPlan === "function",
      lifecycleBoundaryReadiness:
        typeof getCustom25DVisualRendererLifecycleBoundaryReadinessReport === "function",
      lifecycleContractInventory:
        typeof getCustom25DVisualRendererLifecycleContractInventory === "function",
      lifecycleContractReadiness:
        typeof getCustom25DVisualRendererLifecycleContractReadinessReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-post-closeout-handoff-or-separately-reviewed-future-lifecycle-implementation-planning",
    notes: [
      "Passive/report-only/closeout-only/summary-only helper.",
      "Closes out the passive lifecycle boundary and contract planning sequence for Phases 196 through 199.",
      "Does not approve, create, validate, wire, attach, draw, or implement the lifecycle.",
      "No lifecycle object, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleSafetyGatePlan(options = {}) {
  const lifecycleCloseout =
    typeof getCustom25DVisualRendererLifecycleContractCloseoutReport === "function"
      ? getCustom25DVisualRendererLifecycleContractCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-contract-closeout-unavailable"
        };
  const blockedReason = lifecycleCloseout.blockedReason || "custom-25d-map-disabled";

  const stageGate = (stageKey, gatePurpose, requiredPriorReadiness = []) => ({
    stageKey,
    gatePurpose,
    requiredFlags: ["ENABLE_CUSTOM_25D_MAP === true"],
    requiredManualOptions: [
      "manual === true",
      "developerIntent === true",
      "allowLifecycleStage === true"
    ],
    requiredPriorReadiness,
    blockedByDefault: true,
    allowedWhen: [
      "future explicit manual approval exists",
      "all safety flags are reviewed in a separate implementation phase",
      "startup wiring, map attachment, drawing, and renderer initialization remain separately approved"
    ],
    deniedWhen: [
      "default passive mode remains active",
      "ENABLE_CUSTOM_25D_MAP is false",
      "lifecycle object creation is not separately approved",
      "renderer initialization is not separately approved"
    ],
    disallowedSideEffects: [
      "renderer initialization",
      "map attachment",
      "drawing",
      "startup wiring",
      "runtime schema enforcement",
      "validator creation",
      "registry creation",
      "layer or layer state creation"
    ],
    currentApprovalStatus: {
      planned: true,
      implemented: false,
      active: false,
      wired: false,
      approved: false,
      futureOnly: true,
      blockedByDefault: true
    }
  });

  return {
    phase: 201,
    name: "custom-25d-visual-renderer-lifecycle-safety-gate-plan",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    safetyGateOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "lifecycle-safety-gate-plan-planning-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleSafetyGates: {
      construct: stageGate("construct", "future construction-stage lifecycle safety gate", [
        "lifecycle boundary closeout reviewed"
      ]),
      initialize: stageGate("initialize", "future initialization-stage lifecycle safety gate", [
        "construct gate reviewed",
        "manual readiness separately approved"
      ]),
      attach: stageGate("attach", "future map-attachment lifecycle safety gate", [
        "initialize gate reviewed",
        "map attachment readiness separately approved"
      ]),
      render: stageGate("render", "future drawing-stage lifecycle safety gate", [
        "attach gate reviewed",
        "drawing readiness separately approved"
      ]),
      update: stageGate("update", "future update-stage lifecycle safety gate", [
        "render gate reviewed",
        "state update readiness separately approved"
      ]),
      suspend: stageGate("suspend", "future suspend-stage lifecycle safety gate", [
        "update gate reviewed",
        "lifecycle pause readiness separately approved"
      ]),
      resume: stageGate("resume", "future resume-stage lifecycle safety gate", [
        "suspend gate reviewed",
        "lifecycle resume readiness separately approved"
      ]),
      destroy: stageGate("destroy", "future destroy-stage lifecycle safety gate", [
        "resume gate reviewed",
        "teardown readiness separately approved"
      ]),
      clear: stageGate("clear", "future clear-stage lifecycle safety gate", [
        "destroy gate reviewed",
        "reset readiness separately approved"
      ])
    },
    reviewedHelpers: {
      lifecycleCloseout:
        typeof getCustom25DVisualRendererLifecycleContractCloseoutReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-safety-gate-review-or-separately-reviewed-future-lifecycle-gating-phase",
    notes: [
      "Passive/report-only/planning-only/safety-gate-only helper.",
      "Plans future lifecycle safety gates only for construct, initialize, attach, render, update, suspend, resume, destroy, and clear.",
      "Does not approve, create, validate, wire, attach, draw, or implement lifecycle behavior.",
      "No lifecycle object, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, validators, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleSafetyGateReadinessReport(options = {}) {
  const lifecycleSafetyGatePlan =
    typeof getCustom25DVisualRendererLifecycleSafetyGatePlan === "function"
      ? getCustom25DVisualRendererLifecycleSafetyGatePlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-safety-gate-plan-unavailable"
        };
  const blockedReason = lifecycleSafetyGatePlan.blockedReason || "custom-25d-map-disabled";

  return {
    phase: 202,
    name: "custom-25d-visual-renderer-lifecycle-safety-gate-readiness-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reviewOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "lifecycle-safety-gate-readiness-review-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleSafetyGateReadiness: {
      construct: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      initialize: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      attach: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      render: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      update: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      suspend: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      resume: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      destroy: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      clear: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      }
    },
    readinessSummary: {
      gatesRemainPlanningOnly: true,
      gatesRemainBlockedByDefault: true,
      approvesNoLifecycleStage: true,
      readyForImplementation: false,
      readyForRendererInitialization: false,
      readyForMapAttachment: false,
      readyForDrawing: false,
      readyForStartupWiring: false,
      readyForRuntimeSchemaEnforcement: false
    },
    reviewedHelpers: {
      lifecycleSafetyGatePlan:
        typeof getCustom25DVisualRendererLifecycleSafetyGatePlan === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-lifecycle-safety-gate-closeout-or-separately-reviewed-future-lifecycle-gate-implementation-phase",
    notes: [
      "Passive/report-only/readiness-only/review-only helper.",
      "Reviews the future-only lifecycle safety gate plan for construct, initialize, attach, render, update, suspend, resume, destroy, and clear.",
      "Does not approve, create, validate, wire, attach, draw, or implement lifecycle behavior.",
      "No lifecycle object, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, validators, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererLifecycleSafetyGateCloseoutReport(options = {}) {
  const lifecycleSafetyGatePlan =
    typeof getCustom25DVisualRendererLifecycleSafetyGatePlan === "function"
      ? getCustom25DVisualRendererLifecycleSafetyGatePlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-safety-gate-plan-unavailable"
        };
  const lifecycleSafetyGateReadiness =
    typeof getCustom25DVisualRendererLifecycleSafetyGateReadinessReport === "function"
      ? getCustom25DVisualRendererLifecycleSafetyGateReadinessReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-safety-gate-readiness-unavailable"
        };
  const blockedReason =
    lifecycleSafetyGateReadiness.blockedReason ||
    lifecycleSafetyGatePlan.blockedReason ||
    "custom-25d-map-disabled";

  return {
    phase: 203,
    name: "custom-25d-visual-renderer-lifecycle-safety-gate-closeout-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    summaryOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "lifecycle-safety-gate-closeout-summary-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    lifecycleSafetyGateStages: {
      construct: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      initialize: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      attach: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      render: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      update: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      suspend: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      resume: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      destroy: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      },
      clear: {
        planned: true,
        implemented: false,
        active: false,
        wired: false,
        approved: false,
        futureOnly: true,
        blockedByDefault: true
      }
    },
    closeoutStatus: {
      lifecycleSafetyGatePlanned: true,
      lifecycleSafetyGateReadinessReviewed: true,
      lifecycleSafetyGateImplementationApproved: false
    },
    closeoutSummary: {
      closesOutPassivePlanningSequenceOnly: true,
      approvesNoLifecycleStages: true,
      readyForImplementation: false,
      readyForRendererInitialization: false,
      readyForMapAttachment: false,
      readyForDrawing: false,
      readyForStartupWiring: false,
      readyForRuntimeSchemaEnforcement: false
    },
    reviewedHelpers: {
      lifecycleSafetyGatePlan:
        typeof getCustom25DVisualRendererLifecycleSafetyGatePlan === "function",
      lifecycleSafetyGateReadiness:
        typeof getCustom25DVisualRendererLifecycleSafetyGateReadinessReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-post-closeout-lifecycle-handoff-or-separately-reviewed-future-lifecycle-gate-implementation-planning",
    notes: [
      "Passive/report-only/closeout-only/summary-only helper.",
      "Closes out the passive lifecycle safety gate planning sequence for Phases 201 through 202.",
      "Does not approve, create, validate, wire, attach, draw, or implement lifecycle behavior.",
      "No lifecycle object, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, validators, or visible behavior is approved."
    ]
  };
}

function getCustom25DVisualRendererManualActivationRollbackBoundaryReport(options = {}) {
  const lifecycleSafetyGateCloseout =
    typeof getCustom25DVisualRendererLifecycleSafetyGateCloseoutReport === "function"
      ? getCustom25DVisualRendererLifecycleSafetyGateCloseoutReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-lifecycle-safety-gate-closeout-unavailable"
        };
  const blockedReason =
    lifecycleSafetyGateCloseout.blockedReason || "custom-25d-map-disabled";

  return {
    phase: 204,
    name: "custom-25d-visual-renderer-manual-activation-rollback-boundary-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason,
    dormant: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    boundaryOnly: true,
    rollbackOnly: true,
    readinessReviewOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "manual-activation-boundary-planning-only",
      "rollback-boundary-planning-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    manualActivationBoundary: {
      futureOnly: true,
      manual: true,
      developerIntent: true,
      allowLifecycleActivation: true,
      allowRendererLifecycle: true,
      preserveExistingMap: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      implemented: false,
      active: false,
      wired: false,
      approved: false,
      blockedByDefault: true
    },
    cleanupRollbackBoundary: {
      futureOnly: true,
      cleanupFunctionRequired: true,
      rollbackMustBeNoOpSafe: true,
      repeatedCleanupSafe: true,
      noMapResidueAllowed: true,
      noLayerResidueAllowed: true,
      noRegistryResidueAllowed: true,
      noRuntimeStateResidueAllowed: true,
      implemented: false,
      active: false,
      wired: false,
      approved: false,
      blockedByDefault: true
    },
    readinessReview: {
      futureManualActivationOnly: true,
      readyForManualActivationImplementation: false,
      readyForRollbackImplementation: false,
      readyForRendererInitialization: false,
      readyForMapAttachment: false,
      readyForDrawing: false,
      readyForStartupWiring: false,
      readyForRuntimeSchemaEnforcement: false,
      approvesNoBehavior: true
    },
    reviewedHelpers: {
      lifecycleSafetyGateCloseout:
        typeof getCustom25DVisualRendererLifecycleSafetyGateCloseoutReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-manual-activation-closeout-or-separately-reviewed-future-rollback-implementation-planning",
    notes: [
      "Passive/report-only/planning-only/boundary-only/rollback-only/readiness-review-only helper.",
      "Combines future manual activation boundary planning, cleanup and rollback boundary planning, and readiness review for future manual activation only.",
      "Does not approve or implement lifecycle behavior, renderer initialization, map attachment, drawing, startup wiring, runtime schema enforcement, or visible behavior."
    ]
  };
}

function canCreateCustom25DVisualRendererManualLifecycleShell(options = {}) {
  const manualActivationRollbackBoundary =
    typeof getCustom25DVisualRendererManualActivationRollbackBoundaryReport === "function"
      ? getCustom25DVisualRendererManualActivationRollbackBoundaryReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-manual-activation-rollback-boundary-unavailable"
        };

  const requirements = [
    {
      key: "custom25DMapEnabled",
      label: "ENABLE_CUSTOM_25D_MAP === true",
      passed: ENABLE_CUSTOM_25D_MAP === true,
      reason: "custom-25d-map-disabled"
    },
    {
      key: "manual",
      label: "options.manual === true",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      label: "options.developerIntent === true",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "allowLifecycleShellCreation",
      label: "options.allowLifecycleShellCreation === true",
      passed: options.allowLifecycleShellCreation === true,
      reason: "lifecycle-shell-creation-not-allowed"
    },
    {
      key: "allowRendererLifecycle",
      label: "options.allowRendererLifecycle === true",
      passed: options.allowRendererLifecycle === true,
      reason: "renderer-lifecycle-not-allowed"
    },
    {
      key: "preserveExistingMap",
      label: "options.preserveExistingMap === true",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      label: "options.preserveGameplayOverlays === true",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      label: "options.preserveOSMBehavior === true",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      label: "options.preservePins === true",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      label: "options.preservePlayerMarker === true",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      label: "options.preserveCaptureRadius === true",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "rollbackReady",
      label: "options.rollbackReady === true",
      passed: options.rollbackReady === true,
      reason: "rollback-readiness-required"
    },
    {
      key: "cleanupFunctionPlanned",
      label: "options.cleanupFunctionPlanned === true",
      passed: options.cleanupFunctionPlanned === true,
      reason: "cleanup-function-plan-required"
    }
  ];

  const firstFailure = requirements.find((requirement) => !requirement.passed) || null;
  const allowed = firstFailure === null;

  return {
    phase: 205,
    name: "custom-25d-visual-renderer-manual-lifecycle-shell-guard",
    ok: true,
    ready: false,
    allowed,
    reason: allowed ? "manual-lifecycle-shell-guard-passed-evaluation-only" : firstFailure.reason,
    failedRequirement: firstFailure ? firstFailure.key : null,
    guardOnly: true,
    evaluatorOnly: true,
    createsInitializationState: false,
    mutatesInitializationState: false,
    expandsShellMetadata: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    callsCreateShell: false,
    clearsShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsRegistry: false,
    createsLayerState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    requirements,
    blockedReasons: [
      "custom-25d-map-disabled",
      "manual-lifecycle-shell-guard-evaluator-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    reviewedHelpers: {
      manualActivationRollbackBoundary:
        typeof getCustom25DVisualRendererManualActivationRollbackBoundaryReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-manual-lifecycle-shell-readiness-review-or-separately-reviewed-future-shell-creation-phase",
    notes: [
      "Guard-only/evaluator-only helper.",
      "Evaluates whether a future manual lifecycle shell creation would be allowed.",
      "Does not create or approve lifecycle shell creation by itself."
    ]
  };
}

function createCustom25DVisualRendererManualLifecycleShell(options = {}) {
  const guard =
    typeof canCreateCustom25DVisualRendererManualLifecycleShell === "function"
      ? canCreateCustom25DVisualRendererManualLifecycleShell(options)
      : {
          ok: false,
          allowed: false,
          reason: "manual-lifecycle-shell-guard-unavailable",
          failedRequirement: "guardUnavailable"
        };

  if (!guard.allowed) {
    return {
      phase: 206,
      name: "custom-25d-visual-renderer-manual-lifecycle-shell-creator",
      ok: true,
      allowed: false,
      ready: false,
      created: false,
      shell: null,
      reason: guard.reason || "custom-25d-map-disabled",
      failedRequirement: guard.failedRequirement || "custom25DMapEnabled",
      manualOnly: true,
      inertOnly: true,
      createsLifecycleObject: false,
      createsRendererObject: false,
      createsInitializationState: false,
      initializesRenderer: false,
      attachesToMap: false,
      draws: false,
      wiresStartup: false,
      changesGameplay: false,
      storedGlobally: false,
      reviewedHelpers: {
        manualLifecycleShellGuard:
          typeof canCreateCustom25DVisualRendererManualLifecycleShell === "function"
      },
      notes: [
        "Manual-only inert lifecycle shell creator.",
        "Returns a blocked result when the Phase 205 guard does not allow creation.",
        "Does not create or store shell data when blocked."
      ]
    };
  }

  return {
    phase: 206,
    name: "custom-25d-visual-renderer-manual-lifecycle-shell-creator",
    ok: true,
    allowed: true,
    ready: false,
    created: true,
    reason: "manual-lifecycle-shell-created-as-inert-local-data-only",
    failedRequirement: null,
    manualOnly: true,
    inertOnly: true,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    storedGlobally: false,
    shell: {
      key: "custom25DVisualRendererManualLifecycleShell",
      phase: 206,
      dormant: true,
      implemented: false,
      active: false,
      wired: false,
      attached: false,
      initialized: false,
      rendered: false,
      approved: false,
      futureOnly: true,
      manualOnly: true,
      lifecycleStages: {
        construct: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        initialize: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        attach: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        render: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        update: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        suspend: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        resume: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        destroy: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        },
        clear: {
          implemented: false,
          active: false,
          wired: false,
          approved: false,
          futureOnly: true
        }
      }
    },
    reviewedHelpers: {
      manualLifecycleShellGuard:
        typeof canCreateCustom25DVisualRendererManualLifecycleShell === "function"
    },
    notes: [
      "Manual-only inert lifecycle shell creator.",
      "Returns inert plain-data shell content only when the Phase 205 guard allows it.",
      "Does not store shell data globally or implement lifecycle behavior."
    ]
  };
}

function clearCustom25DVisualRendererManualLifecycleShell(options = {}) {
  return {
    phase: 206,
    name: "custom-25d-visual-renderer-manual-lifecycle-shell-clear",
    ok: true,
    cleared: false,
    noOp: true,
    safeToRepeat: true,
    reason: "no-global-lifecycle-shell-state-to-clear",
    manualOnly: true,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    changesGameplay: false,
    mutatesGlobalState: false,
    notes: [
      "No-op cleanup helper.",
      "Does not touch global state, map state, layers, renderer, pins, UI, OSM, backend, or gameplay.",
      "Safe to repeat because no global lifecycle shell state is stored."
    ]
  };
}

function getCustom25DVisualRendererManualLifecycleShellCleanupVerificationReport(options = {}) {
  const cleanupReport =
    typeof clearCustom25DVisualRendererManualLifecycleShell === "function"
      ? clearCustom25DVisualRendererManualLifecycleShell(options)
      : {
          ok: false,
          noOp: false,
          safeToRepeat: false,
          reason: "manual-lifecycle-shell-cleanup-helper-unavailable"
        };

  const cleanupVerificationPassed = Boolean(
    cleanupReport &&
      cleanupReport.noOp === true &&
      cleanupReport.safeToRepeat === true &&
      cleanupReport.mutatesGlobalState === false &&
      cleanupReport.attachesToMap === false &&
      cleanupReport.draws === false &&
      cleanupReport.initializesRenderer === false &&
      cleanupReport.wiresStartup === false &&
      cleanupReport.changesGameplay === false
  );

  return {
    phase: 207,
    name: "custom-25d-visual-renderer-manual-lifecycle-shell-cleanup-verification-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason: "custom-25d-map-disabled",
    passive: true,
    reportOnly: true,
    verificationOnly: true,
    cleanupReviewOnly: true,
    createsLifecycleShell: false,
    callsCreateLifecycleShell: false,
    createsLifecycleObject: false,
    createsRendererObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "cleanup-verification-report-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    cleanupVerification: {
      noOp: cleanupReport.noOp === true,
      safeToRepeat: cleanupReport.safeToRepeat === true,
      clearsGlobalState: false,
      touchesMap: false,
      touchesLayers: false,
      touchesRenderer: false,
      touchesPins: false,
      touchesUI: false,
      touchesOSM: false,
      touchesBackend: false,
      changesGameplay: false
    },
    verificationStatus: {
      cleanupHelperPresent:
        typeof clearCustom25DVisualRendererManualLifecycleShell === "function",
      lifecycleShellStoredGlobally: false,
      cleanupVerificationPassed,
      lifecycleImplementationApproved: false,
      mapAttachmentApproved: false,
      drawingApproved: false
    },
    reviewedHelpers: {
      manualLifecycleShellCleanup:
        typeof clearCustom25DVisualRendererManualLifecycleShell === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-manual-lifecycle-shell-closeout-or-separately-reviewed-future-lifecycle-shell-expansion-phase",
    notes: [
      "Passive/report-only/verification-only/cleanup-review-only helper.",
      "Passively reviews the no-op cleanup helper because it is safe to call repeatedly.",
      "Does not approve, create, initialize, wire, attach, draw, or implement lifecycle behavior."
    ]
  };
}

function getCustom25DVisualRendererBridgeMapAttachmentBoundaryReport(options = {}) {
  const cleanupVerification =
    typeof getCustom25DVisualRendererManualLifecycleShellCleanupVerificationReport === "function"
      ? getCustom25DVisualRendererManualLifecycleShellCleanupVerificationReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "manual-lifecycle-shell-cleanup-verification-unavailable"
        };

  return {
    phase: 208,
    name: "custom-25d-visual-renderer-bridge-map-attachment-boundary-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason: cleanupVerification.blockedReason || "custom-25d-map-disabled",
    passive: true,
    reportOnly: true,
    planningOnly: true,
    readinessOnly: true,
    boundaryOnly: true,
    createsLifecycleShell: false,
    callsCreateLifecycleShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "bridge-boundary-planning-only",
      "map-attachment-boundary-planning-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    rendererLifecycleBridgeBoundary: {
      lifecycleShellAvailable: true,
      rendererShellAvailable: true,
      manual: true,
      developerIntent: true,
      allowRendererLifecycleBridge: true,
      allowMapAttachmentPlanning: true,
      cleanupVerified: true,
      preserveExistingMap: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      futureOnly: true,
      implemented: false,
      active: false,
      wired: false,
      approved: false,
      blockedByDefault: true
    },
    bridgeReadinessReview: {
      bridgeReadyForImplementation: false,
      bridgeReadyForRendererInitialization: false,
      cleanupVerified:
        cleanupVerification.verificationStatus?.cleanupVerificationPassed === true,
      preservesExistingMap: true,
      preservesGameplayOverlays: true,
      preservesOSMBehavior: true,
      preservesPins: true,
      preservesPlayerMarker: true,
      preservesCaptureRadius: true,
      futureOnly: true,
      approved: false
    },
    mapAttachmentBoundaryPlan: {
      attachmentMustBeManualOnly: true,
      attachmentMustBeReversible: true,
      attachmentMustAvoidStartupWiring: true,
      attachmentMustAvoidDrawingByDefault: true,
      attachmentMustAvoidOSMMutation: true,
      attachmentMustAvoidPinMutation: true,
      attachmentMustAvoidPlayerMarkerMutation: true,
      attachmentMustAvoidCaptureRadiusMutation: true,
      attachmentMustHaveCleanupPath: true,
      futureOnly: true,
      implemented: false,
      active: false,
      wired: false,
      approved: false,
      blockedByDefault: true
    },
    bridgeStatus: {
      bridgePlanned: true,
      bridgeReadinessReviewed: true,
      mapAttachmentBoundaryPlanned: true,
      bridgeImplementationApproved: false,
      mapAttachmentApproved: false,
      drawingApproved: false
    },
    reviewedHelpers: {
      manualLifecycleShellCleanupVerification:
        typeof getCustom25DVisualRendererManualLifecycleShellCleanupVerificationReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-bridge-closeout-or-separately-reviewed-future-manual-map-attachment-planning-phase",
    notes: [
      "Passive/report-only/planning-only/readiness-only/boundary-only helper.",
      "Combines future renderer-to-lifecycle bridge planning, bridge readiness review, and map attachment boundary planning.",
      "Does not approve or implement bridge behavior, map attachment, drawing, startup wiring, or renderer initialization."
    ]
  };
}

function canAttachCustom25DVisualRendererManualMapShell(options = {}) {
  const bridgeBoundary =
    typeof getCustom25DVisualRendererBridgeMapAttachmentBoundaryReport === "function"
      ? getCustom25DVisualRendererBridgeMapAttachmentBoundaryReport(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "renderer-bridge-map-attachment-boundary-unavailable"
        };

  const requirements = [
    {
      key: "custom25DMapEnabled",
      label: "ENABLE_CUSTOM_25D_MAP === true",
      passed: ENABLE_CUSTOM_25D_MAP === true,
      reason: "custom-25d-map-disabled"
    },
    {
      key: "manual",
      label: "options.manual === true",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      label: "options.developerIntent === true",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "allowManualMapAttachment",
      label: "options.allowManualMapAttachment === true",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-not-allowed"
    },
    {
      key: "allowRendererLifecycleBridge",
      label: "options.allowRendererLifecycleBridge === true",
      passed: options.allowRendererLifecycleBridge === true,
      reason: "renderer-lifecycle-bridge-not-allowed"
    },
    {
      key: "allowMapAttachmentPlanning",
      label: "options.allowMapAttachmentPlanning === true",
      passed: options.allowMapAttachmentPlanning === true,
      reason: "map-attachment-planning-not-allowed"
    },
    {
      key: "lifecycleShellAvailable",
      label: "options.lifecycleShellAvailable === true",
      passed: options.lifecycleShellAvailable === true,
      reason: "lifecycle-shell-availability-required"
    },
    {
      key: "rendererShellAvailable",
      label: "options.rendererShellAvailable === true",
      passed: options.rendererShellAvailable === true,
      reason: "renderer-shell-availability-required"
    },
    {
      key: "cleanupVerified",
      label: "options.cleanupVerified === true",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentMustBeManualOnly",
      label: "options.attachmentMustBeManualOnly === true",
      passed: options.attachmentMustBeManualOnly === true,
      reason: "manual-only-attachment-required"
    },
    {
      key: "attachmentMustBeReversible",
      label: "options.attachmentMustBeReversible === true",
      passed: options.attachmentMustBeReversible === true,
      reason: "reversible-attachment-required"
    },
    {
      key: "attachmentMustAvoidStartupWiring",
      label: "options.attachmentMustAvoidStartupWiring === true",
      passed: options.attachmentMustAvoidStartupWiring === true,
      reason: "startup-wiring-avoidance-required"
    },
    {
      key: "attachmentMustAvoidDrawingByDefault",
      label: "options.attachmentMustAvoidDrawingByDefault === true",
      passed: options.attachmentMustAvoidDrawingByDefault === true,
      reason: "drawing-by-default-avoidance-required"
    },
    {
      key: "attachmentMustAvoidOSMMutation",
      label: "options.attachmentMustAvoidOSMMutation === true",
      passed: options.attachmentMustAvoidOSMMutation === true,
      reason: "osm-mutation-avoidance-required"
    },
    {
      key: "attachmentMustAvoidPinMutation",
      label: "options.attachmentMustAvoidPinMutation === true",
      passed: options.attachmentMustAvoidPinMutation === true,
      reason: "pin-mutation-avoidance-required"
    },
    {
      key: "attachmentMustAvoidPlayerMarkerMutation",
      label: "options.attachmentMustAvoidPlayerMarkerMutation === true",
      passed: options.attachmentMustAvoidPlayerMarkerMutation === true,
      reason: "player-marker-mutation-avoidance-required"
    },
    {
      key: "attachmentMustAvoidCaptureRadiusMutation",
      label: "options.attachmentMustAvoidCaptureRadiusMutation === true",
      passed: options.attachmentMustAvoidCaptureRadiusMutation === true,
      reason: "capture-radius-mutation-avoidance-required"
    },
    {
      key: "attachmentMustHaveCleanupPath",
      label: "options.attachmentMustHaveCleanupPath === true",
      passed: options.attachmentMustHaveCleanupPath === true,
      reason: "attachment-cleanup-path-required"
    },
    {
      key: "preserveExistingMap",
      label: "options.preserveExistingMap === true",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      label: "options.preserveGameplayOverlays === true",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      label: "options.preserveOSMBehavior === true",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      label: "options.preservePins === true",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      label: "options.preservePlayerMarker === true",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      label: "options.preserveCaptureRadius === true",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    }
  ];

  const firstFailure = requirements.find((requirement) => !requirement.passed) || null;
  const allowed = firstFailure === null;

  return {
    phase: 209,
    name: "custom-25d-visual-renderer-manual-map-shell-attachment-guard",
    ok: true,
    ready: false,
    allowed,
    reason: allowed ? "manual-map-attachment-guard-passed-evaluation-only" : firstFailure.reason,
    failedRequirement: firstFailure ? firstFailure.key : null,
    guardOnly: true,
    evaluatorOnly: true,
    createsLifecycleShell: false,
    callsCreateLifecycleShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    wiresStartup: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    requirements,
    blockedReasons: [
      "custom-25d-map-disabled",
      "manual-map-attachment-guard-evaluator-only",
      "no-lifecycle-object-creation-approved",
      "no-renderer-initialization-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-runtime-schema-enforcement-approved"
    ],
    reviewedHelpers: {
      bridgeMapAttachmentBoundary:
        typeof getCustom25DVisualRendererBridgeMapAttachmentBoundaryReport === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-manual-map-attachment-readiness-review-or-separately-reviewed-future-attachment-shell-phase",
    notes: [
      "Guard-only/evaluator-only helper.",
      "Evaluates whether a future manual map attachment shell would be allowed.",
      "Does not attach, create, draw, or approve attachment by itself."
    ]
  };
}

function createCustom25DVisualRendererManualAttachmentSmokeShell(options = {}) {
  const guard =
    typeof canAttachCustom25DVisualRendererManualMapShell === "function"
      ? canAttachCustom25DVisualRendererManualMapShell(options)
      : {
          ok: false,
          allowed: false,
          reason: "manual-map-attachment-guard-unavailable",
          failedRequirement: "guardUnavailable"
        };

  if (!guard.allowed) {
    return {
      phase: 210,
      name: "custom-25d-visual-renderer-manual-attachment-smoke-shell-creator",
      ok: true,
      allowed: false,
      ready: false,
      created: false,
      smokeShell: null,
      reason: guard.reason || "custom-25d-map-disabled",
      failedRequirement: guard.failedRequirement || "custom25DMapEnabled",
      manualOnly: true,
      inertOnly: true,
      createsRendererObject: false,
      createsLifecycleObject: false,
      createsInitializationState: false,
      initializesRenderer: false,
      attachesToMap: false,
      draws: false,
      renders: false,
      wiresStartup: false,
      createsRegistry: false,
      createsLayerState: false,
      changesGameplay: false,
      storedGlobally: false,
      reviewedHelpers: {
        manualMapAttachmentGuard:
          typeof canAttachCustom25DVisualRendererManualMapShell === "function"
      },
      notes: [
        "Manual-only inert attachment smoke shell creator.",
        "Returns a blocked result when the Phase 209 guard does not allow creation.",
        "Does not create or store smoke shell data when blocked."
      ]
    };
  }

  return {
    phase: 210,
    name: "custom-25d-visual-renderer-manual-attachment-smoke-shell-creator",
    ok: true,
    allowed: true,
    ready: false,
    created: true,
    reason: "manual-attachment-smoke-shell-created-as-inert-local-data-only",
    failedRequirement: null,
    manualOnly: true,
    inertOnly: true,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    storedGlobally: false,
    smokeShell: {
      key: "custom25DVisualRendererManualAttachmentSmokeShell",
      phase: 210,
      dormant: true,
      manualOnly: true,
      futureOnly: true,
      implemented: false,
      active: false,
      wired: false,
      attached: false,
      rendered: false,
      drawn: false,
      approved: false,
      touchesMap: false,
      touchesLayers: false,
      touchesRenderer: false,
      touchesPins: false,
      touchesOSM: false,
      touchesPlayerMarker: false,
      touchesCaptureRadius: false,
      changesGameplay: false,
      changesUI: false
    },
    reviewedHelpers: {
      manualMapAttachmentGuard:
        typeof canAttachCustom25DVisualRendererManualMapShell === "function"
    },
    notes: [
      "Manual-only inert attachment smoke shell creator.",
      "Returns inert plain-data smoke shell content only when the Phase 209 guard allows it.",
      "Does not store smoke shell data globally or touch the real map."
    ]
  };
}

function clearCustom25DVisualRendererManualAttachmentSmokeShell(options = {}) {
  return {
    phase: 210,
    name: "custom-25d-visual-renderer-manual-attachment-smoke-shell-clear",
    ok: true,
    cleared: false,
    noOp: true,
    safeToRepeat: true,
    reason: "no-global-attachment-smoke-shell-state-to-clear",
    manualOnly: true,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    mutatesGlobalState: false,
    notes: [
      "No-op cleanup helper.",
      "Does not touch global state, map state, layers, renderer, pins, UI, OSM, backend, or gameplay.",
      "Safe to repeat because no global attachment smoke shell state is stored."
    ]
  };
}

function getCustom25DVisualManualVisibleTestLayerPlan(options = {}) {
  const manualMapAttachmentGuard =
    typeof canAttachCustom25DVisualRendererManualMapShell === "function"
      ? canAttachCustom25DVisualRendererManualMapShell(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "manual-map-attachment-guard-unavailable"
        };
  const attachmentSmokeCleanup =
    typeof clearCustom25DVisualRendererManualAttachmentSmokeShell === "function"
      ? clearCustom25DVisualRendererManualAttachmentSmokeShell(options)
      : {
          ok: false,
          noOp: false,
          safeToRepeat: false,
          reason: "manual-attachment-smoke-shell-cleanup-unavailable"
        };

  return {
    phase: 211,
    name: "custom-25d-visual-manual-visible-test-layer-plan",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason: manualMapAttachmentGuard.reason || "custom-25d-map-disabled",
    passive: true,
    reportOnly: true,
    planningOnly: true,
    visibilityBoundaryOnly: true,
    createsVisibleTestLayer: false,
    callsCreateLifecycleShell: false,
    callsCreateAttachmentSmokeShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "visible-test-layer-plan-planning-only",
      "no-visible-layer-creation-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-startup-wiring-approved",
      "no-gameplay-changes-approved",
      "no-osm-changes-approved",
      "no-pin-changes-approved",
      "no-player-marker-changes-approved",
      "no-capture-radius-changes-approved",
      "no-backend-changes-approved"
    ],
    visibleTestLayerPlan: {
      layerKey: "custom25DVisualManualVisibleTestLayer",
      manualOnly: true,
      developerOnly: true,
      tinyTestOnly: true,
      reversible: true,
      cleanupRequired: true,
      startupWiringAllowed: false,
      mapAttachmentAllowedByDefault: false,
      drawingAllowedByDefault: false,
      changesGameplay: false,
      changesOSM: false,
      changesPins: false,
      changesPlayerMarker: false,
      changesCaptureRadius: false,
      changesBackend: false,
      futureOnly: true,
      implemented: false,
      active: false,
      wired: false,
      approved: false
    },
    reviewedHelpers: {
      manualMapAttachmentGuard:
        typeof canAttachCustom25DVisualRendererManualMapShell === "function",
      attachmentSmokeCleanup:
        typeof clearCustom25DVisualRendererManualAttachmentSmokeShell === "function"
    },
    cleanupReadiness: {
      noOp: attachmentSmokeCleanup.noOp === true,
      safeToRepeat: attachmentSmokeCleanup.safeToRepeat === true,
      touchesMap: false,
      touchesLayers: false,
      touchesRenderer: false,
      touchesPins: false,
      touchesOSM: false,
      touchesBackend: false,
      changesGameplay: false
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-visible-test-layer-readiness-review-or-separately-reviewed-future-tiny-visible-layer-phase",
    notes: [
      "Passive/report-only/planning-only/visibility-boundary-only helper.",
      "Defines the future tiny manual visible test layer plan without creating, attaching, drawing, or approving visibility.",
      "Does not create lifecycle shells, attachment smoke shells, renderer objects, lifecycle objects, or initialization state."
    ]
  };
}

function canCreateCustom25DVisualManualVisibleTestLayer(options = {}) {
  const visibleTestLayerPlan =
    typeof getCustom25DVisualManualVisibleTestLayerPlan === "function"
      ? getCustom25DVisualManualVisibleTestLayerPlan(options)
      : {
          ok: false,
          missing: true,
          blockedReason: "manual-visible-test-layer-plan-unavailable"
        };

  const requirements = [
    {
      key: "custom25DMapEnabled",
      label: "ENABLE_CUSTOM_25D_MAP === true",
      passed: ENABLE_CUSTOM_25D_MAP === true,
      reason: "custom-25d-map-disabled"
    },
    {
      key: "manual",
      label: "options.manual === true",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      label: "options.developerIntent === true",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "allowVisibleTestLayer",
      label: "options.allowVisibleTestLayer === true",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      label: "options.allowManualMapAttachment === true",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      label: "options.allowTinyTestOnly === true",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      label: "options.cleanupVerified === true",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      label: "options.attachmentSmokeShellReady === true",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      label: "options.preserveExistingMap === true",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      label: "options.preserveGameplayOverlays === true",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      label: "options.preserveOSMBehavior === true",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      label: "options.preservePins === true",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      label: "options.preservePlayerMarker === true",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      label: "options.preserveCaptureRadius === true",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      label: "options.noStartupWiring === true",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      label: "options.noBackendChanges === true",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    }
  ];

  const firstFailure = requirements.find((requirement) => !requirement.passed) || null;
  const allowed = firstFailure === null;

  return {
    phase: 211,
    name: "custom-25d-visual-manual-visible-test-layer-guard",
    ok: true,
    ready: false,
    allowed,
    reason: allowed ? "manual-visible-test-layer-guard-passed-evaluation-only" : firstFailure.reason,
    failedRequirement: firstFailure ? firstFailure.key : null,
    guardOnly: true,
    evaluatorOnly: true,
    createsVisibleTestLayer: false,
    callsCreateLifecycleShell: false,
    callsCreateAttachmentSmokeShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    changesGameplay: false,
    requirements,
    blockedReasons: [
      "custom-25d-map-disabled",
      "visible-test-layer-plan-planning-only",
      "visible-test-guard-evaluator-only",
      "no-visible-layer-creation-approved",
      "no-map-attachment-approved",
      "no-drawing-approved",
      "no-startup-wiring-approved",
      "no-gameplay-changes-approved",
      "no-osm-changes-approved",
      "no-pin-changes-approved",
      "no-player-marker-changes-approved",
      "no-capture-radius-changes-approved",
      "no-backend-changes-approved"
    ],
    reviewedHelpers: {
      visibleTestLayerPlan:
        typeof getCustom25DVisualManualVisibleTestLayerPlan === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    nextPhaseRecommendation: "passive-visible-test-layer-closeout-or-separately-reviewed-future-visible-layer-smoke-shell-phase",
    notes: [
      "Guard-only/evaluator-only helper.",
      "Evaluates whether a future tiny manual visible test layer would be allowed.",
      "Does not create, attach, draw, or approve visibility by itself."
    ]
  };
}

function createCustom25DVisualFirstManualVisibleTestLayer(options = {}) {
  const visibleTestGuard =
    typeof canCreateCustom25DVisualManualVisibleTestLayer === "function"
      ? canCreateCustom25DVisualManualVisibleTestLayer(options)
      : {
          ok: false,
          allowed: false,
          reason: "manual-visible-test-layer-guard-unavailable",
          failedRequirement: "guardUnavailable"
        };
  const localDevVisibleTestMapDisabledBypass =
    options.allowCustom25DMapDisabledLocalDevVisibleTestBypass === true &&
    visibleTestGuard.failedRequirement === "custom25DMapEnabled" &&
    options.localDevOnly === true &&
    options.browserConsoleOnly === true &&
    options.manual === true &&
    options.developerIntent === true &&
    options.allowLocalDevVisibleTestOverride === true &&
    options.allowVisibleTestLayer === true &&
    options.allowManualMapAttachment === true &&
    options.allowTinyTestOnly === true &&
    options.cleanupVerified === true &&
    options.attachmentSmokeShellReady === true &&
    options.preserveExistingMap === true &&
    options.preserveGameplayOverlays === true &&
    options.preserveOSMBehavior === true &&
    options.preservePins === true &&
    options.preservePlayerMarker === true &&
    options.preserveCaptureRadius === true &&
    options.noStartupWiring === true &&
    options.noBackendChanges === true &&
    options.noPersistence === true &&
    options.noAutomaticInvocation === true &&
    options.acceptsTemporaryLocalDevVisual === true;

  if (!visibleTestGuard.allowed && !localDevVisibleTestMapDisabledBypass) {
    return {
      phase: 212,
      name: "custom-25d-visual-first-manual-visible-test-layer",
      ok: true,
      allowed: false,
      ready: false,
      created: false,
      visible: false,
      manualOnly: true,
      developerOnly: true,
      testOnly: true,
      startupWired: false,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      cleanupAvailable: true,
      elementCreated: false,
      reason: visibleTestGuard.reason || "custom-25d-map-disabled",
      failedRequirement: visibleTestGuard.failedRequirement || "custom25DMapEnabled",
      notes: [
        "Blocked by guard.",
        "No visible test layer was created.",
        "Default behavior remains blocked and invisible while ENABLE_CUSTOM_25D_MAP is false."
      ]
    };
  }

  const doc =
    typeof document !== "undefined" && document ? document : null;
  const discoveredContainer =
    doc && typeof doc.querySelector === "function"
      ? doc.querySelector(
          ".leaflet-container, [data-map-container], [data-map-root], .map-container, #map"
        )
      : null;
  const container =
    options.mapContainer ||
    options.container ||
    (options.map && typeof options.map.getContainer === "function"
      ? options.map.getContainer()
      : null) ||
    discoveredContainer;

  if (!container || typeof container.appendChild !== "function") {
    return {
      phase: 212,
      name: "custom-25d-visual-first-manual-visible-test-layer",
      ok: true,
      allowed: true,
      ready: false,
      created: false,
      visible: false,
      manualOnly: true,
      developerOnly: true,
      testOnly: true,
      startupWired: false,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      cleanupAvailable: true,
      elementCreated: false,
      reason: "manual-visible-test-layer-container-unavailable",
      failedRequirement: "validMapContainer",
      notes: [
        "Guard passed, but no safe map container was provided.",
        "No visible test layer was created."
      ]
    };
  }

  const ownerDoc = container.ownerDocument || doc;
  if (!ownerDoc || typeof ownerDoc.createElement !== "function") {
    return {
      phase: 212,
      name: "custom-25d-visual-first-manual-visible-test-layer",
      ok: true,
      allowed: true,
      ready: false,
      created: false,
      visible: false,
      manualOnly: true,
      developerOnly: true,
      testOnly: true,
      startupWired: false,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      cleanupAvailable: true,
      elementCreated: false,
      reason: "manual-visible-test-layer-document-unavailable",
      failedRequirement: "validDocument",
      notes: [
        "Guard passed, but no safe document context was available.",
        "No visible test layer was created."
      ]
    };
  }

  const existingSelector =
    '[data-custom-25d-visible-test="true"], [data-custom25d-manual-visible-test-layer="true"]';
  const existing =
    typeof ownerDoc.querySelector === "function"
      ? ownerDoc.querySelector(existingSelector)
      : null;
  if (existing) {
    return {
      phase: 212,
      name: "custom-25d-visual-first-manual-visible-test-layer",
      ok: true,
      allowed: true,
      ready: true,
      created: false,
      visible: true,
      manualOnly: true,
      developerOnly: true,
      testOnly: true,
      startupWired: false,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      cleanupAvailable: true,
      elementCreated: false,
      elementFound: true,
      reason: "manual-visible-test-layer-already-present",
      notes: [
        "Existing tiny manual test layer was left unchanged.",
        "No additional element was created."
      ]
    };
  }

  const el = ownerDoc.createElement("div");
  el.setAttribute("data-custom-25d-visible-test", "true");
  el.setAttribute("data-custom25d-manual-visible-test-layer", "true");
  el.textContent = "2.5D TEST";
  Object.assign(el.style, {
    position: "absolute",
    top: "48px",
    right: "12px",
    zIndex: "450",
    pointerEvents: "none",
    padding: "2px 6px",
    borderRadius: "4px",
    fontSize: "10px",
    lineHeight: "1.2",
    fontWeight: "700",
    letterSpacing: "0",
    color: "#111827",
    background: "rgba(255, 214, 10, 0.88)",
    border: "1px solid rgba(17, 24, 39, 0.18)",
    boxShadow: "0 1px 2px rgba(0, 0, 0, 0.08)",
    userSelect: "none",
    fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
  });
  container.appendChild(el);

  return {
    phase: 212,
    name: "custom-25d-visual-first-manual-visible-test-layer",
    ok: true,
    allowed: true,
    ready: true,
    created: true,
    visible: true,
    manualOnly: true,
    developerOnly: true,
    testOnly: true,
    startupWired: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    cleanupAvailable: true,
    elementCreated: true,
    elementFound:
      typeof ownerDoc.body !== "undefined" &&
      ownerDoc.body &&
      typeof ownerDoc.body.textContent === "string"
        ? ownerDoc.body.textContent.includes("2.5D TEST")
        : false,
    reason: localDevVisibleTestMapDisabledBypass
      ? "manual-visible-test-layer-created-with-local-dev-map-disabled-bypass"
      : "manual-visible-test-layer-created-as-tiny-dom-overlay-only",
    notes: [
      "Created a tiny temporary DOM overlay only.",
      "Did not attach Leaflet layers, mutate OSM, pins, player marker, capture radius, gameplay, or backend state."
    ]
  };
}

function createCustom25DVisualExpandedManualVisibleTestLayer(options = {}) {
  const strictRequirements = [
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowExpandedVisibleTestLayer",
      passed: options.allowExpandedVisibleTestLayer === true,
      reason: "expanded-visible-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;
  const fullStrictOverridePassed = firstFailure === null;
  const localDevVisibleTestMapDisabledBypass =
    options.allowCustom25DMapDisabledLocalDevVisibleTestBypass === true &&
    ENABLE_CUSTOM_25D_MAP !== true &&
    fullStrictOverridePassed;

  if (!localDevVisibleTestMapDisabledBypass) {
    return {
      phase: 229,
      name: "custom-25d-visual-expanded-manual-visible-test-layer",
      ok: true,
      allowed: false,
      created: false,
      visible: false,
      reason:
        firstFailure?.reason || (ENABLE_CUSTOM_25D_MAP !== true ? "custom-25d-map-disabled" : "expanded-visible-test-blocked"),
      elementCount: 0,
      selector: '[data-custom-25d-expanded-visible-test="true"]',
      text: "2.5D EXPANDED TEST",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const doc = typeof document !== "undefined" && document ? document : null;
  const discoveredContainer =
    doc && typeof doc.querySelector === "function"
      ? doc.querySelector(
          ".leaflet-container, [data-map-container], [data-map-root], .map-container, #map"
        )
      : null;
  const container =
    options.mapContainer ||
    options.container ||
    (options.map && typeof options.map.getContainer === "function"
      ? options.map.getContainer()
      : null) ||
    discoveredContainer;

  if (!container || typeof container.appendChild !== "function") {
    return {
      phase: 229,
      name: "custom-25d-visual-expanded-manual-visible-test-layer",
      ok: true,
      allowed: true,
      created: false,
      visible: false,
      reason: "expanded-visible-test-container-unavailable",
      elementCount: 0,
      selector: '[data-custom-25d-expanded-visible-test="true"]',
      text: "2.5D EXPANDED TEST",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const ownerDoc = container.ownerDocument || doc;
  if (!ownerDoc || typeof ownerDoc.createElement !== "function") {
    return {
      phase: 229,
      name: "custom-25d-visual-expanded-manual-visible-test-layer",
      ok: true,
      allowed: true,
      created: false,
      visible: false,
      reason: "expanded-visible-test-document-unavailable",
      elementCount: 0,
      selector: '[data-custom-25d-expanded-visible-test="true"]',
      text: "2.5D EXPANDED TEST",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const selector = '[data-custom-25d-expanded-visible-test="true"]';
  const existingElements =
    typeof ownerDoc.querySelectorAll === "function"
      ? ownerDoc.querySelectorAll(selector)
      : [];
  const existingCount = existingElements.length || 0;

  if (existingCount > 0) {
    return {
      phase: 229,
      name: "custom-25d-visual-expanded-manual-visible-test-layer",
      ok: true,
      allowed: true,
      created: false,
      visible: true,
      reason: "expanded-visible-test-layer-already-present",
      elementCount: existingCount,
      selector,
      text: "2.5D EXPANDED TEST",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const el = ownerDoc.createElement("div");
  el.setAttribute("data-custom-25d-expanded-visible-test", "true");
  el.textContent = "2.5D EXPANDED TEST";
  Object.assign(el.style, {
    position: "absolute",
    top: "74px",
    right: "12px",
    zIndex: "451",
    pointerEvents: "none",
    padding: "2px 6px",
    borderRadius: "4px",
    fontSize: "10px",
    lineHeight: "1.2",
    fontWeight: "700",
    letterSpacing: "0",
    color: "#111827",
    background: "rgba(110, 231, 183, 0.88)",
    border: "1px solid rgba(17, 24, 39, 0.18)",
    boxShadow: "0 1px 2px rgba(0, 0, 0, 0.08)",
    userSelect: "none",
    fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
  });
  container.appendChild(el);

  return {
    phase: 229,
    name: "custom-25d-visual-expanded-manual-visible-test-layer",
    ok: true,
    allowed: true,
    created: true,
    visible: true,
    reason: "expanded-visible-test-layer-created-with-local-dev-map-disabled-bypass",
    elementCount:
      typeof ownerDoc.querySelectorAll === "function"
        ? ownerDoc.querySelectorAll(selector).length
        : 1,
    selector,
    text: "2.5D EXPANDED TEST",
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    temporary: true,
    cleanupRequired: true,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      rendererObjects: true,
      rendererInitialization: true,
      lifecycleObjects: true,
      registries: true,
      persistentLayerState: true,
      realMapLayerAttachment: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiSystemChanges: true,
      backendChanges: true
    }
  };
}

function runCustom25DVisualExpandedManualVisibleTest(options = {}) {
  const strictRequirements = [
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowExpandedVisibleTestLayer",
      passed: options.allowExpandedVisibleTestLayer === true,
      reason: "expanded-visible-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;

  if (firstFailure) {
    return {
      phase: 231,
      name: "custom-25d-visual-expanded-manual-visible-test-runner",
      ok: true,
      allowed: false,
      ready: false,
      ran: false,
      created: false,
      visible: false,
      elementFound: false,
      elementCount: 0,
      selector: '[data-custom-25d-expanded-visible-test="true"]',
      text: "2.5D EXPANDED TEST",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      reason: firstFailure.reason,
      failedRequirement: firstFailure.key,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const runnerOptions = {
    localDevOnly: options.localDevOnly === true,
    browserConsoleOnly: options.browserConsoleOnly === true,
    manual: options.manual === true,
    developerIntent: options.developerIntent === true,
    allowLocalDevVisibleTestOverride: options.allowLocalDevVisibleTestOverride === true,
    allowVisibleTestLayer: options.allowVisibleTestLayer === true,
    allowExpandedVisibleTestLayer: options.allowExpandedVisibleTestLayer === true,
    allowManualMapAttachment: options.allowManualMapAttachment === true,
    allowTinyTestOnly: options.allowTinyTestOnly === true,
    cleanupVerified: options.cleanupVerified === true,
    attachmentSmokeShellReady: options.attachmentSmokeShellReady === true,
    preserveExistingMap: options.preserveExistingMap === true,
    preserveGameplayOverlays: options.preserveGameplayOverlays === true,
    preserveOSMBehavior: options.preserveOSMBehavior === true,
    preservePins: options.preservePins === true,
    preservePlayerMarker: options.preservePlayerMarker === true,
    preserveCaptureRadius: options.preserveCaptureRadius === true,
    noStartupWiring: options.noStartupWiring === true,
    noBackendChanges: options.noBackendChanges === true,
    noPersistence: options.noPersistence === true,
    noAutomaticInvocation: options.noAutomaticInvocation === true,
    acceptsTemporaryLocalDevVisual: options.acceptsTemporaryLocalDevVisual === true,
    allowCustom25DMapDisabledLocalDevVisibleTestBypass: true,
    mapContainer: options.mapContainer || null,
    container: options.container || null,
    map: options.map || null
  };

  const result =
    typeof createCustom25DVisualExpandedManualVisibleTestLayer === "function"
      ? createCustom25DVisualExpandedManualVisibleTestLayer(runnerOptions)
      : {
          ok: false,
          allowed: false,
          created: false,
          visible: false,
          reason: "expanded-visible-test-layer-creator-unavailable"
        };
  const doc = typeof document !== "undefined" ? document : null;
  const selector = '[data-custom-25d-expanded-visible-test="true"]';
  const elementCount =
    doc && typeof doc.querySelectorAll === "function" ? doc.querySelectorAll(selector).length : 0;
  const elementFound =
    !!(doc && typeof doc.querySelector === "function" && doc.querySelector(selector));

  return {
    phase: 231,
    name: "custom-25d-visual-expanded-manual-visible-test-runner",
    ok: true,
    allowed: result.allowed === true,
    ready: result.visible === true || result.created === true || elementFound,
    ran: true,
    created: result.created === true,
    visible: result.visible === true || elementFound,
    elementFound,
    elementCount,
    selector,
    text: "2.5D EXPANDED TEST",
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    temporary: true,
    cleanupRequired: true,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      rendererObjects: true,
      rendererInitialization: true,
      lifecycleObjects: true,
      registries: true,
      persistentLayerState: true,
      realMapLayerAttachment: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiSystemChanges: true,
      backendChanges: true
    },
    reason: result.reason || "expanded-visible-test-runner-finished",
    failedRequirement: result.failedRequirement || null
  };
}

function clearCustom25DVisualExpandedManualVisibleTestLayer(options = {}) {
  const strictRequirements = [
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowExpandedVisibleTestLayer",
      passed: options.allowExpandedVisibleTestLayer === true,
      reason: "expanded-visible-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;
  const selector = '[data-custom-25d-expanded-visible-test="true"]';

  if (firstFailure) {
    return {
      phase: 230,
      name: "custom-25d-visual-expanded-manual-visible-test-cleanup",
      ok: true,
      allowed: false,
      removed: false,
      reason: firstFailure.reason,
      failedRequirement: firstFailure.key,
      beforeCount: 0,
      afterCount: 0,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupOnly: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const container =
    options.mapContainer ||
    options.container ||
    (options.map && typeof options.map.getContainer === "function"
      ? options.map.getContainer()
      : null);
  const doc =
    (container && container.ownerDocument) || (typeof document !== "undefined" ? document : null);

  if (!doc || typeof doc.querySelectorAll !== "function") {
    return {
      phase: 230,
      name: "custom-25d-visual-expanded-manual-visible-test-cleanup",
      ok: true,
      allowed: true,
      removed: false,
      reason: "expanded-visible-test-cleanup-document-unavailable",
      beforeCount: 0,
      afterCount: 0,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupOnly: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  const scope =
    container && typeof container.querySelectorAll === "function" ? container : doc;
  const elements = scope.querySelectorAll(selector);
  const beforeCount = elements.length || 0;

  if (!beforeCount) {
    return {
      phase: 230,
      name: "custom-25d-visual-expanded-manual-visible-test-cleanup",
      ok: true,
      allowed: true,
      removed: false,
      reason: "no-expanded-visible-test-layer-to-clear",
      beforeCount,
      afterCount: 0,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupOnly: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        rendererObjects: true,
        rendererInitialization: true,
        lifecycleObjects: true,
        registries: true,
        persistentLayerState: true,
        realMapLayerAttachment: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiSystemChanges: true,
        backendChanges: true
      }
    };
  }

  elements.forEach((element) => {
    if (element && element.parentNode) {
      element.parentNode.removeChild(element);
    }
  });

  const afterCount =
    typeof scope.querySelectorAll === "function" ? scope.querySelectorAll(selector).length : 0;

  return {
    phase: 230,
    name: "custom-25d-visual-expanded-manual-visible-test-cleanup",
    ok: true,
    allowed: true,
    removed: true,
    reason: "expanded-visible-test-layer-cleared",
    beforeCount,
    afterCount,
    selector,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    temporary: true,
    cleanupOnly: true,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      rendererObjects: true,
      rendererInitialization: true,
      lifecycleObjects: true,
      registries: true,
      persistentLayerState: true,
      realMapLayerAttachment: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiSystemChanges: true,
      backendChanges: true
    }
  };
}

function getCustom25DVisualExpandedManualVisibleTestSuccessCloseoutReport(options = {}) {
  return {
    phase: 232,
    name: "custom-25d-visual-expanded-manual-visible-test-success-closeout-report",
    ok: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "planning-only-next-step",
    expandedManualVisibleTestSucceeded: true,
    helperPresence: {
      expandedCreator:
        typeof createCustom25DVisualExpandedManualVisibleTestLayer === "function",
      expandedCleanup:
        typeof clearCustom25DVisualExpandedManualVisibleTestLayer === "function",
      expandedRunner:
        typeof runCustom25DVisualExpandedManualVisibleTest === "function"
    },
    manualBrowserConsoleExpectations: {
      bodyTextIncludesExpandedTest:
        'document.body.textContent.includes("2.5D EXPANDED TEST") === true',
      expandedElementCountExactlyOne:
        'document.querySelectorAll(\'[data-custom-25d-expanded-visible-test="true"]\').length === 1'
    },
    duplicatePreventionExpectation: {
      repeatFullManualRunnerKeepsElementCountOne: true,
      expectedCheck:
        'document.querySelectorAll(\'[data-custom-25d-expanded-visible-test="true"]\').length === 1'
    },
    cleanupExpectations: {
      bodyTextExcludesExpandedTestAfterCleanup:
        'document.body.textContent.includes("2.5D EXPANDED TEST") === false',
      expandedElementCountZeroAfterCleanup:
        'document.querySelectorAll(\'[data-custom-25d-expanded-visible-test="true"]\').length === 0'
    },
    normalReload: {
      unchanged: true,
      invisibleByDefault: true,
      startupWired: false,
      automaticInvocationAdded: false
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    notApprovedFor: {
      startupWiring: true,
      productionUse: true,
      enablingCustom25DMap: true,
      realRendererCreation: true,
      rendererInitialization: true,
      lifecycleCreation: true,
      registryCreation: true,
      persistentLayerState: true,
      realMapAttachment: true,
      automaticDrawing: true,
      largerVisualTests: true
    },
    preservedSystems: {
      gameplay: true,
      osmBehavior: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    nextPhaseRecommendation: {
      readyForNextPlanningPhase: true,
      recommendation:
        "plan-only follow-up for any future manual/local-dev/browser-console expanded visible test work"
    },
    notes: [
      "Passive closeout/report helper only.",
      "Does not call the expanded runner, expanded creator, or expanded cleanup helper.",
      "Does not call the tiny runner, tiny creator, or tiny cleanup helper.",
      "Normal reload remains unchanged and invisible unless the developer manually runs the expanded local-dev browser-console path."
    ]
  };
}

function getCustom25DVisualFirstShapeTestPlanningReport(options = {}) {
  const expandedSuccessCloseout =
    typeof getCustom25DVisualExpandedManualVisibleTestSuccessCloseoutReport === "function"
      ? getCustom25DVisualExpandedManualVisibleTestSuccessCloseoutReport(options)
      : null;

  return {
    phase: 233,
    name: "custom-25d-visual-first-shape-test-planning-report",
    ok: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "shape-test-contract-planning-only",
    priorPhaseSummary: expandedSuccessCloseout
      ? {
          expandedManualVisibleTestSucceeded:
            expandedSuccessCloseout.expandedManualVisibleTestSucceeded === true,
          normalReloadUnchanged:
            expandedSuccessCloseout.normalReload &&
            expandedSuccessCloseout.normalReload.unchanged === true,
          startupWired:
            expandedSuccessCloseout.normalReload &&
            expandedSuccessCloseout.normalReload.startupWired === true
        }
      : {
          expandedManualVisibleTestSucceeded: false,
          normalReloadUnchanged: false,
          startupWired: false
        },
    futureShapeTestRequirements: {
      localDevOnly: true,
      browserConsoleOnly: true,
      manuallyInvoked: true,
      temporary: true,
      cleanupSafe: true,
      visuallyTiny: true,
      failClosedByDefault: true,
      notConnectedToStartup: true,
      notPersistent: true,
      notBackendConnected: true,
      notARealRenderer: true,
      notARealMapLayer: true,
      notProductionBehavior: true
    },
    planningRecommendations: {
      recommendedScope:
        "tiny static test shape or marker-like proof only, not a real map-rendered system",
      distinctDataAttributeRequired: true,
      reuseStrictManualVisibleTestGuardPattern: true,
      cleanupMustRemoveOnlyFutureShapeTestElement: true
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    notApprovedFor: {
      startupWiring: true,
      productionUse: true,
      enablingCustom25DMap: true,
      realRendererCreation: true,
      rendererInitialization: true,
      lifecycleCreation: true,
      registryCreation: true,
      persistentLayerState: true,
      realMapAttachment: true,
      automaticDrawing: true,
      mapGeometryDrawing: true,
      largerVisualTests: true
    },
    nextPhaseRecommendation: {
      readyForShapeTestContractPhase: true,
      recommendation:
        "planning-only follow-up for a future first shape-test contract phase using the existing strict manual/local-dev/browser-console guard pattern"
    },
    notes: [
      "Passive planning/report helper only.",
      "Does not call the expanded runner, expanded creator, expanded cleanup, tiny runner, tiny creator, or tiny cleanup helpers.",
      "Does not create DOM elements, mutate DOM, attach anything, draw anything, or wire startup."
    ]
  };
}

function getCustom25DVisualFirstShapeTestContract(options = {}) {
  const shapeTestPlanning =
    typeof getCustom25DVisualFirstShapeTestPlanningReport === "function"
      ? getCustom25DVisualFirstShapeTestPlanningReport(options)
      : null;

  return {
    phase: 234,
    name: "custom-25d-visual-first-shape-test-contract",
    ok: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "shape-test-contract-only",
    planningSummary: shapeTestPlanning
      ? {
          phase: shapeTestPlanning.phase || 233,
          priorPhaseAvailable: true,
          recommendedScope:
            shapeTestPlanning.planningRecommendations &&
            shapeTestPlanning.planningRecommendations.recommendedScope,
          reuseStrictManualVisibleTestGuardPattern:
            shapeTestPlanning.planningRecommendations &&
            shapeTestPlanning.planningRecommendations
              .reuseStrictManualVisibleTestGuardPattern === true
        }
      : {
          phase: 233,
          priorPhaseAvailable: false,
          recommendedScope: null,
          reuseStrictManualVisibleTestGuardPattern: false
        },
    futureShapeTestContract: {
      localDevOnly: true,
      browserConsoleOnly: true,
      manuallyInvokedOnly: true,
      guardedByStrictManualVisibleTestOverridePattern: true,
      tinyStaticTemporary: true,
      cleanupSafe: true,
      failClosedByDefault: true,
      nonStartup: true,
      nonPersistent: true,
      nonBackend: true,
      notARealRenderer: true,
      notARealLeafletLayer: true,
      notMapGeometryDrawing: true,
      notProductionBehavior: true,
      distinctFutureDataAttributeRequired: true,
      exactCleanupOwnershipRequired: true
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    blockedByContract: {
      startupWiring: true,
      automaticInvocation: true,
      enablingCustom25DMap: true,
      rendererWork: true,
      lifecycleWork: true,
      registryWork: true,
      persistentLayerState: true,
      realMapAttachment: true,
      automaticDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true
    },
    nextPhaseRecommendation: {
      readyForNextPhase: true,
      recommendation:
        "contract-only handoff for a future tiny static shape-test readiness or implementation-planning phase"
    },
    notes: [
      "Passive contract/report helper only.",
      "Defines safety requirements only for a future tiny static shape manual visual test.",
      "Does not call runners, creators, cleanup helpers, or change any existing helper behavior.",
      "Does not create or mutate DOM, attach anything, draw anything, wire startup, or change gameplay/map/backend behavior."
    ]
  };
}

function createCustom25DVisualFirstShapeManualTestLayer(options = {}) {
  const shapeTestContract =
    typeof getCustom25DVisualFirstShapeTestContract === "function"
      ? getCustom25DVisualFirstShapeTestContract(options)
      : null;
  const strictRequirements = [
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowFirstShapeManualTestLayer",
      passed: options.allowFirstShapeManualTestLayer === true,
      reason: "first-shape-manual-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;
  const fullStrictOverridePassed = firstFailure === null;
  const localDevVisibleTestMapDisabledBypass =
    options.allowCustom25DMapDisabledFirstShapeManualTestBypass === true &&
    ENABLE_CUSTOM_25D_MAP !== true &&
    fullStrictOverridePassed;

  if (!localDevVisibleTestMapDisabledBypass) {
    return {
      phase: 235,
      name: "custom-25d-visual-first-shape-manual-test-layer",
      ok: true,
      allowed: false,
      created: false,
      visible: false,
      reason:
        firstFailure?.reason || (ENABLE_CUSTOM_25D_MAP !== true ? "custom-25d-map-disabled" : "first-shape-manual-test-blocked"),
      failedRequirement: firstFailure ? firstFailure.key : "custom25DMapEnabled",
      selector: '[data-growgo-custom-25d-first-shape-manual-test="true"]',
      elementCount: 0,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      contractAvailable: !!shapeTestContract,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  const doc = typeof document !== "undefined" && document ? document : null;
  const discoveredContainer =
    doc && typeof doc.querySelector === "function"
      ? doc.querySelector(
          ".leaflet-container, [data-map-container], [data-map-root], .map-container, #map"
        )
      : null;
  const container =
    options.mapContainer ||
    options.container ||
    (options.map && typeof options.map.getContainer === "function"
      ? options.map.getContainer()
      : null) ||
    discoveredContainer;

  if (!container || typeof container.appendChild !== "function") {
    return {
      phase: 235,
      name: "custom-25d-visual-first-shape-manual-test-layer",
      ok: true,
      allowed: true,
      created: false,
      visible: false,
      reason: "first-shape-manual-test-container-unavailable",
      failedRequirement: "validMapContainer",
      selector: '[data-growgo-custom-25d-first-shape-manual-test="true"]',
      elementCount: 0,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      contractAvailable: !!shapeTestContract,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  const ownerDoc = container.ownerDocument || doc;
  if (!ownerDoc || typeof ownerDoc.createElement !== "function") {
    return {
      phase: 235,
      name: "custom-25d-visual-first-shape-manual-test-layer",
      ok: true,
      allowed: true,
      created: false,
      visible: false,
      reason: "first-shape-manual-test-document-unavailable",
      failedRequirement: "validDocument",
      selector: '[data-growgo-custom-25d-first-shape-manual-test="true"]',
      elementCount: 0,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      contractAvailable: !!shapeTestContract,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';
  const existingElements =
    typeof ownerDoc.querySelectorAll === "function"
      ? ownerDoc.querySelectorAll(selector)
      : [];
  const existingCount = existingElements.length || 0;

  if (existingCount > 0) {
    return {
      phase: 235,
      name: "custom-25d-visual-first-shape-manual-test-layer",
      ok: true,
      allowed: true,
      created: false,
      visible: true,
      reason: "first-shape-manual-test-layer-already-present",
      failedRequirement: null,
      selector,
      elementCount: existingCount,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupRequired: true,
      contractAvailable: !!shapeTestContract,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  const el = ownerDoc.createElement("div");
  el.setAttribute("data-growgo-custom-25d-first-shape-manual-test", "true");
  el.setAttribute("aria-label", "GrowGo custom 2.5D first shape manual test");
  el.setAttribute("title", "GrowGo custom 2.5D first shape manual test");
  Object.assign(el.style, {
    position: "absolute",
    top: "104px",
    right: "16px",
    width: "12px",
    height: "12px",
    borderRadius: "999px",
    background: "rgba(244, 114, 182, 0.92)",
    border: "1px solid rgba(17, 24, 39, 0.28)",
    boxShadow: "0 1px 2px rgba(0, 0, 0, 0.08)",
    pointerEvents: "none",
    zIndex: "452",
    userSelect: "none"
  });
  container.appendChild(el);

  return {
    phase: 235,
    name: "custom-25d-visual-first-shape-manual-test-layer",
    ok: true,
    allowed: true,
    created: true,
    visible: true,
    reason: "first-shape-manual-test-layer-created-with-local-dev-map-disabled-bypass",
    failedRequirement: null,
    selector,
    elementCount:
      typeof ownerDoc.querySelectorAll === "function"
        ? ownerDoc.querySelectorAll(selector).length
        : 1,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    temporary: true,
    cleanupRequired: true,
    contractAvailable: !!shapeTestContract,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      automaticInvocation: true,
      rendererWork: true,
      lifecycleWork: true,
      registryWork: true,
      persistentLayerState: true,
      realLeafletLayer: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true,
      networkAccess: true,
      storageWrites: true
    }
  };
}

function clearCustom25DVisualFirstShapeManualTestLayer(options = {}) {
  const strictRequirements = [
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowFirstShapeManualTestLayer",
      passed: options.allowFirstShapeManualTestLayer === true,
      reason: "first-shape-manual-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';

  if (firstFailure) {
    return {
      phase: 236,
      name: "custom-25d-visual-first-shape-manual-test-cleanup",
      ok: true,
      allowed: false,
      removed: false,
      reason: firstFailure.reason,
      failedRequirement: firstFailure.key,
      removedCount: 0,
      beforeCount: 0,
      afterCount: 0,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      cleanupOnly: true,
      temporary: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        domCreation: true,
        drawing: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  const container =
    options.mapContainer ||
    options.container ||
    (options.map && typeof options.map.getContainer === "function"
      ? options.map.getContainer()
      : null);
  const doc =
    (container && container.ownerDocument) || (typeof document !== "undefined" ? document : null);

  if (!doc || typeof doc.querySelectorAll !== "function") {
    return {
      phase: 236,
      name: "custom-25d-visual-first-shape-manual-test-cleanup",
      ok: true,
      allowed: true,
      removed: false,
      reason: "first-shape-manual-test-cleanup-document-unavailable",
      failedRequirement: "validDocument",
      removedCount: 0,
      beforeCount: 0,
      afterCount: 0,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      cleanupOnly: true,
      temporary: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        domCreation: true,
        drawing: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  const scope =
    container && typeof container.querySelectorAll === "function" ? container : doc;
  const elements = Array.from(scope.querySelectorAll(selector));
  const beforeCount = elements.length;

  if (!beforeCount) {
    return {
      phase: 236,
      name: "custom-25d-visual-first-shape-manual-test-cleanup",
      ok: true,
      allowed: true,
      removed: false,
      reason: "no-first-shape-manual-test-layer-to-clear",
      failedRequirement: null,
      removedCount: 0,
      beforeCount,
      afterCount: 0,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      cleanupOnly: true,
      temporary: true,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      },
      notApprovedFor: {
        startupWiring: true,
        automaticInvocation: true,
        domCreation: true,
        drawing: true,
        rendererWork: true,
        lifecycleWork: true,
        registryWork: true,
        persistentLayerState: true,
        realLeafletLayer: true,
        mapGeometryDrawing: true,
        gameplayChanges: true,
        osmChanges: true,
        pinChanges: true,
        playerMarkerChanges: true,
        captureRadiusChanges: true,
        uiChanges: true,
        backendChanges: true,
        networkAccess: true,
        storageWrites: true
      }
    };
  }

  elements.forEach((element) => {
    if (element && element.parentNode) {
      element.parentNode.removeChild(element);
    }
  });

  const afterCount =
    typeof scope.querySelectorAll === "function" ? scope.querySelectorAll(selector).length : 0;

  return {
    phase: 236,
    name: "custom-25d-visual-first-shape-manual-test-cleanup",
    ok: true,
    allowed: true,
    removed: true,
    reason: "first-shape-manual-test-layer-cleared",
    failedRequirement: null,
    removedCount: beforeCount - afterCount,
    beforeCount,
    afterCount,
    selector,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    cleanupOnly: true,
    temporary: true,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      automaticInvocation: true,
      domCreation: true,
      drawing: true,
      rendererWork: true,
      lifecycleWork: true,
      registryWork: true,
      persistentLayerState: true,
      realLeafletLayer: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true,
      networkAccess: true,
      storageWrites: true
    }
  };
}

function runCustom25DVisualFirstShapeManualTest(options = {}) {
  const runtimeHostname =
    typeof window !== "undefined" &&
    window &&
    window.location &&
    typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const runtimeLocalDevDetected =
    (typeof window !== "undefined" &&
      window &&
      window.GrowGoScriptExecutionDiagnostic &&
      window.GrowGoScriptExecutionDiagnostic.localDev === true) ||
    runtimeHostname === "localhost" ||
    runtimeHostname === "127.0.0.1" ||
    runtimeHostname === "0.0.0.0" ||
    runtimeHostname === "::1";
  const localDevIntentProvided = options.localDevOnly === true || options.localDev === true;
  const resolvedLocalDevOnly = localDevIntentProvided && runtimeLocalDevDetected;
  const strictRequirements = [
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "localDevOnly",
      passed: resolvedLocalDevOnly,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowFirstShapeManualTestLayer",
      passed: options.allowFirstShapeManualTestLayer === true,
      reason: "first-shape-manual-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const failedRequirements = strictRequirements.filter((requirement) => !requirement.passed);
  const missingRequirements = failedRequirements.map((requirement) => requirement.key);
  const missingReasons = failedRequirements.map((requirement) => requirement.reason);
  const requiredSafetyGates = strictRequirements.map((requirement) => ({
    key: requirement.key,
    passed: requirement.passed,
    reason: requirement.reason
  }));
  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';

  if (firstFailure) {
    return {
      phase: 237,
      name: "custom-25d-visual-first-shape-manual-test-runner",
      ok: true,
      allowed: false,
      ran: false,
      reason: firstFailure.reason,
      failedRequirement: firstFailure.key,
      missingRequirements,
      missingReasons,
      requiredSafetyGates,
      createResult: null,
      cleanupResult: null,
      existedAfterCreate: false,
      existedAfterCleanup: false,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupOnly: false,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      }
    };
  }

  if (
    typeof createCustom25DVisualFirstShapeManualTestLayer !== "function" ||
    typeof clearCustom25DVisualFirstShapeManualTestLayer !== "function"
  ) {
    return {
      phase: 237,
      name: "custom-25d-visual-first-shape-manual-test-runner",
      ok: true,
      allowed: false,
      ran: false,
      reason: "shape-manual-test-helpers-unavailable",
      failedRequirement:
        typeof createCustom25DVisualFirstShapeManualTestLayer !== "function"
          ? "createHelperAvailable"
          : "cleanupHelperAvailable",
      missingRequirements: [],
      missingReasons: [],
      requiredSafetyGates,
      createResult: null,
      cleanupResult: null,
      existedAfterCreate: false,
      existedAfterCleanup: false,
      selector,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      temporary: true,
      cleanupOnly: false,
      safetyFlags: {
        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
      },
      preservedSystems: {
        existingLeafletMapBehavior: true,
        osmBehavior: true,
        gameplay: true,
        pins: true,
        playerMarker: true,
        captureRadius: true,
        ui: true,
        backend: true
      }
    };
  }

  const doc = typeof document !== "undefined" ? document : null;
  const createResult = createCustom25DVisualFirstShapeManualTestLayer({
    ...options,
    allowCustom25DMapDisabledFirstShapeManualTestBypass: true
  });
  const existedAfterCreate =
    !!(doc && typeof doc.querySelector === "function" && doc.querySelector(selector));

  let cleanupResult = null;
  if (createResult && createResult.created === true) {
    cleanupResult = clearCustom25DVisualFirstShapeManualTestLayer(options);
  } else if (existedAfterCreate) {
    cleanupResult = clearCustom25DVisualFirstShapeManualTestLayer(options);
  } else {
    cleanupResult = {
      phase: 237,
      ok: true,
      allowed: true,
      removed: false,
      reason: "cleanup-not-needed-no-owned-shape-test-element-found",
      removedCount: 0,
      beforeCount: 0,
      afterCount: 0,
      selector
    };
  }

  const existedAfterCleanup =
    !!(doc && typeof doc.querySelector === "function" && doc.querySelector(selector));

  return {
    phase: 237,
    name: "custom-25d-visual-first-shape-manual-test-runner",
    ok: true,
    allowed: createResult.allowed === true,
    ran: true,
    reason:
      existedAfterCreate && !existedAfterCleanup
        ? "first-shape-manual-test-run-completed-and-cleaned"
        : createResult.reason || "first-shape-manual-test-runner-finished",
    failedRequirement: createResult.failedRequirement || null,
    missingRequirements: [],
    missingReasons: [],
    requiredSafetyGates,
    createResult,
    cleanupResult,
    existedAfterCreate,
    existedAfterCleanup,
    selector,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    temporary: true,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      automaticInvocation: true,
      rendererWork: true,
      lifecycleWork: true,
      registryWork: true,
      persistentLayerState: true,
      realLeafletLayer: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true,
      networkAccess: true,
      storageWrites: true
    }
  };
}

function getCustom25DVisualFirstShapeManualTestVerificationReport(options = {}) {
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';

  return {
    phase: 238,
    name: "custom-25d-visual-first-shape-manual-test-verification-report",
    ok: true,
    passive: true,
    reportOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "verification-only",
    failClosedByDefault: true,
    createsNothing: true,
    removesNothing: true,
    drawsNothing: true,
    attachesNothing: true,
    mutatesNothing: true,
    helperPresence: {
      createFirstShapeManualTestLayer:
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function",
      clearFirstShapeManualTestLayer:
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function",
      runFirstShapeManualTest:
        typeof runCustom25DVisualFirstShapeManualTest === "function",
      firstShapeTestContract:
        typeof getCustom25DVisualFirstShapeTestContract === "function"
    },
    ownedSelector: selector,
    requiredManualOptionPattern: {
      manual: true,
      developerIntent: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      allowLocalDevVisibleTestOverride: true,
      allowVisibleTestLayer: true,
      allowFirstShapeManualTestLayer: true,
      allowManualMapAttachment: true,
      allowTinyTestOnly: true,
      cleanupVerified: true,
      attachmentSmokeShellReady: true,
      preserveExistingMap: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      noStartupWiring: true,
      noBackendChanges: true,
      noPersistence: true,
      noAutomaticInvocation: true,
      acceptsTemporaryLocalDevVisual: true,
      allowCustom25DMapDisabledLocalDevVisibleTestBypass: true
    },
    allowedFutureManualPath: "runCustom25DVisualFirstShapeManualTest(options)",
    expectedManualFlow: [
      "create the owned first-shape manual test element through the Phase 235 helper",
      "verify the owned selector exists exactly for the temporary shape test",
      "clean up through the Phase 236 helper",
      "confirm the owned selector is removed"
    ],
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    },
    notApprovedFor: {
      startupWiring: true,
      automaticInvocation: true,
      browserTestingByThisHelper: true,
      domCreation: true,
      domMutation: true,
      drawing: true,
      cleanupExecution: true,
      backendUsage: true,
      networkUsage: true,
      storageUsage: true,
      rendererWork: true,
      lifecycleWork: true,
      registryWork: true,
      persistentLayerState: true,
      realLeafletLayer: true,
      mapGeometry: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      existingMapBehaviorChanges: true
    },
    notes: [
      "Passive/report-only helper.",
      "Does not run the manual test.",
      "The only allowed future browser-console path is runCustom25DVisualFirstShapeManualTest(options).",
      "The future manual test must create, verify, clean up, and confirm removal while preserving fail-closed default behavior."
    ]
  };
}

function getCustom25DVisualFirstShapeManualTestCloseoutReport(options = {}) {
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';
  const doc = typeof document !== "undefined" ? document : null;
  const cleanupSelectorCount =
    doc && typeof doc.querySelectorAll === "function"
      ? doc.querySelectorAll(selector).length
      : 0;
  const noOptionRunnerResult =
    typeof runCustom25DVisualFirstShapeManualTest === "function"
      ? runCustom25DVisualFirstShapeManualTest()
      : null;

  return {
    phase: 256,
    name: "custom-25d-visual-first-shape-manual-test-closeout-report",
    ok: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "closeout-only",
    failClosedByDefault: true,
    createsNothing: true,
    removesNothing: true,
    drawsNothing: true,
    attachesNothing: true,
    mutatesNothing: true,
    helperAvailability: {
      firstShapeCreator:
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function",
      firstShapeCleanup:
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function",
      firstShapeRunner:
        typeof runCustom25DVisualFirstShapeManualTest === "function",
      firstShapeVerificationReport:
        typeof getCustom25DVisualFirstShapeManualTestVerificationReport === "function"
    },
    closeoutSummary: {
      phase235CreatorAvailable:
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function",
      phase236CleanupAvailable:
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function",
      phase237RunnerAvailable:
        typeof runCustom25DVisualFirstShapeManualTest === "function",
      phase254MissingGateReportingAvailable:
        !!(
          noOptionRunnerResult &&
          Array.isArray(noOptionRunnerResult.missingRequirements) &&
          Array.isArray(noOptionRunnerResult.missingReasons) &&
          Array.isArray(noOptionRunnerResult.requiredSafetyGates)
        ),
      phase255MapDisabledBypassRemainsLocalManualTestOnly: true,
      defaultNoOptionRunnerCallBlocked:
        !!(noOptionRunnerResult && noOptionRunnerResult.allowed === false),
      fullManualTestRequiresExplicitApprovalsOnly: true,
      cleanupSelectorCountSafeWhenAvailable: cleanupSelectorCount >= 0
    },
    blockedNoOptionRunnerSnapshot: noOptionRunnerResult
      ? {
          allowed: noOptionRunnerResult.allowed === true,
          ran: noOptionRunnerResult.ran === true,
          reason: noOptionRunnerResult.reason || null,
          failedRequirement: noOptionRunnerResult.failedRequirement || null,
          missingRequirements: Array.isArray(noOptionRunnerResult.missingRequirements)
            ? noOptionRunnerResult.missingRequirements
            : [],
          missingReasons: Array.isArray(noOptionRunnerResult.missingReasons)
            ? noOptionRunnerResult.missingReasons
            : [],
          requiredSafetyGates: Array.isArray(noOptionRunnerResult.requiredSafetyGates)
            ? noOptionRunnerResult.requiredSafetyGates
            : []
        }
      : null,
    fullManualApprovalPattern: {
      manual: true,
      developerIntent: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      allowLocalDevVisibleTestOverride: true,
      allowVisibleTestLayer: true,
      allowFirstShapeManualTestLayer: true,
      allowManualMapAttachment: true,
      allowTinyTestOnly: true,
      cleanupVerified: true,
      attachmentSmokeShellReady: true,
      preserveExistingMap: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      noStartupWiring: true,
      noBackendChanges: true,
      noPersistence: true,
      noAutomaticInvocation: true,
      acceptsTemporaryLocalDevVisual: true,
      allowCustom25DMapDisabledFirstShapeManualTestBypass: true
    },
    ownedSelector: selector,
    cleanupSelectorCount,
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    },
    notApprovedFor: {
      startupWiring: true,
      automaticInvocation: true,
      testLayerCreation: true,
      manualTestExecutionByThisHelper: true,
      cleanupExecutionByThisHelper: true,
      rendererWork: true,
      lifecycleWork: true,
      registryWork: true,
      persistentLayerState: true,
      realLeafletLayer: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    notes: [
      "Passive closeout/report-only helper.",
      "Does not create the first-shape test layer.",
      "Does not run the manual test.",
      "Does not execute cleanup.",
      "Confirms the default no-option runner path remains blocked while the full manual approval path stays explicit and local-dev-only."
    ]
  };
}

function getCustom25DVisualFirstShapeToRendererHandoffReport(options = {}) {
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';
  const doc = typeof document !== "undefined" ? document : null;
  const cleanupSelectorCount =
    doc && typeof doc.querySelectorAll === "function"
      ? doc.querySelectorAll(selector).length
      : 0;
  const noOptionRunnerResult =
    typeof runCustom25DVisualFirstShapeManualTest === "function"
      ? runCustom25DVisualFirstShapeManualTest()
      : null;
  const closeoutReport =
    typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function"
      ? getCustom25DVisualFirstShapeManualTestCloseoutReport(options)
      : null;

  return {
    phase: 258,
    name: "custom-25d-visual-first-shape-to-renderer-handoff-report",
    ok: true,
    passive: true,
    reportOnly: true,
    handoffOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "planning-only-handoff",
    failClosedByDefault: true,
    createsNothing: true,
    removesNothing: true,
    drawsNothing: true,
    attachesNothing: true,
    mutatesNothing: true,
    helperAvailability: {
      firstShapeContract:
        typeof getCustom25DVisualFirstShapeTestContract === "function",
      firstShapeCreator:
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function",
      firstShapeCleanup:
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function",
      firstShapeRunner:
        typeof runCustom25DVisualFirstShapeManualTest === "function",
      firstShapeCloseout:
        typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function"
    },
    handoffSummary: {
      firstShapeContractAvailable:
        typeof getCustom25DVisualFirstShapeTestContract === "function",
      creatorAvailable:
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function",
      cleanupAvailable:
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function",
      runnerAvailable:
        typeof runCustom25DVisualFirstShapeManualTest === "function",
      closeoutHelperAvailable:
        typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function",
      missingGateReportingAvailable:
        !!(
          noOptionRunnerResult &&
          Array.isArray(noOptionRunnerResult.missingRequirements) &&
          Array.isArray(noOptionRunnerResult.missingReasons) &&
          Array.isArray(noOptionRunnerResult.requiredSafetyGates)
        ),
      phase255MapDisabledBypassRemainsLocalManualTestOnly: true,
      noOptionRunnerStillBlocked:
        !!(noOptionRunnerResult && noOptionRunnerResult.allowed === false),
      cleanupSelectorCountIsZero: cleanupSelectorCount === 0,
      recommendedNextStepPlanningOnly: true
    },
    closeoutSnapshot: closeoutReport
      ? {
          phase: closeoutReport.phase,
          ok: closeoutReport.ok === true,
          defaultDecision: closeoutReport.defaultDecision || null,
          cleanupSelectorCount: closeoutReport.cleanupSelectorCount,
          defaultNoOptionRunnerCallBlocked:
            !!(
              closeoutReport.closeoutSummary &&
              closeoutReport.closeoutSummary.defaultNoOptionRunnerCallBlocked
            )
        }
      : null,
    blockedNoOptionRunnerSnapshot: noOptionRunnerResult
      ? {
          allowed: noOptionRunnerResult.allowed === true,
          ran: noOptionRunnerResult.ran === true,
          reason: noOptionRunnerResult.reason || null,
          failedRequirement: noOptionRunnerResult.failedRequirement || null,
          missingRequirements: Array.isArray(noOptionRunnerResult.missingRequirements)
            ? noOptionRunnerResult.missingRequirements
            : [],
          missingReasons: Array.isArray(noOptionRunnerResult.missingReasons)
            ? noOptionRunnerResult.missingReasons
            : [],
          requiredSafetyGates: Array.isArray(noOptionRunnerResult.requiredSafetyGates)
            ? noOptionRunnerResult.requiredSafetyGates
            : []
        }
      : null,
    ownedSelector: selector,
    cleanupSelectorCount,
    nextStepRecommendation: {
      planningOnly: true,
      rendererBehaviorApproved: false,
      realMapAttachmentApproved: false,
      mapGeometryDrawingApproved: false,
      startupWiringApproved: false,
      recommendedNextPhase:
        "planning-only unless a future phase explicitly adds renderer behavior under separate review"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    },
    notApprovedFor: {
      startupWiring: true,
      automaticInvocation: true,
      testLayerCreation: true,
      manualTestExecutionByThisHelper: true,
      cleanupExecutionByThisHelper: true,
      rendererInitialization: true,
      rendererWork: true,
      realLeafletLayer: true,
      mapGeometryDrawing: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    notes: [
      "Passive handoff/report-only helper.",
      "Does not create the first-shape test layer.",
      "Does not run the manual test.",
      "Does not execute cleanup.",
      "The next recommended step remains planning-only unless a future phase explicitly adds renderer behavior."
    ]
  };
}

function getCustom25DVisualRendererBridgePlanReport(options = {}) {
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';
  const doc = typeof document !== "undefined" ? document : null;
  const cleanupSelectorCount =
    doc && typeof doc.querySelectorAll === "function"
      ? doc.querySelectorAll(selector).length
      : 0;
  const noOptionRunnerResult =
    typeof runCustom25DVisualFirstShapeManualTest === "function"
      ? runCustom25DVisualFirstShapeManualTest()
      : null;
  const handoffReport =
    typeof getCustom25DVisualFirstShapeToRendererHandoffReport === "function"
      ? getCustom25DVisualFirstShapeToRendererHandoffReport(options)
      : null;

  return {
    phase: 259,
    name: "custom-25d-visual-renderer-bridge-plan-report",
    ok: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    bridgeOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "planning-only-renderer-bridge",
    failClosedByDefault: true,
    createsNothing: true,
    removesNothing: true,
    drawsNothing: true,
    attachesNothing: true,
    mutatesNothing: true,
    helperAvailability: {
      firstShapeHandoff:
        typeof getCustom25DVisualFirstShapeToRendererHandoffReport === "function",
      firstShapeContract:
        typeof getCustom25DVisualFirstShapeTestContract === "function",
      firstShapeCreator:
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function",
      firstShapeCleanup:
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function",
      firstShapeRunner:
        typeof runCustom25DVisualFirstShapeManualTest === "function",
      firstShapeCloseout:
        typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function"
    },
    bridgeSummary: {
      phase258HandoffHelperAvailable:
        typeof getCustom25DVisualFirstShapeToRendererHandoffReport === "function",
      firstShapeManualTestChainClosedOut:
        !!(
          handoffReport &&
          handoffReport.handoffSummary &&
          handoffReport.handoffSummary.cleanupSelectorCountIsZero === true &&
          handoffReport.handoffSummary.noOptionRunnerStillBlocked === true
        ),
      rendererBehaviorApproved: false,
      realMapAttachmentApproved: false,
      mapGeometryDrawingApproved: false,
      startupWiringApproved: false,
      noGameplayChangesApproved: true,
      noOsmChangesApproved: true,
      noPinChangesApproved: true,
      noPlayerMarkerChangesApproved: true,
      noCaptureRadiusChangesApproved: true,
      noUiChangesApproved: true,
      noBackendChangesApproved: true,
      noStorageChangesApproved: true,
      noNetworkChangesApproved: true,
      recommendedNextWorkInertRendererShellContractPlanOnly: true
    },
    handoffSnapshot: handoffReport
      ? {
          phase: handoffReport.phase,
          ok: handoffReport.ok === true,
          defaultDecision: handoffReport.defaultDecision || null,
          cleanupSelectorCount: handoffReport.cleanupSelectorCount,
          noOptionRunnerStillBlocked:
            !!(
              handoffReport.handoffSummary &&
              handoffReport.handoffSummary.noOptionRunnerStillBlocked
            ),
          planningOnlyRecommendation:
            !!(
              handoffReport.nextStepRecommendation &&
              handoffReport.nextStepRecommendation.planningOnly
            )
        }
      : null,
    blockedNoOptionRunnerSnapshot: noOptionRunnerResult
      ? {
          allowed: noOptionRunnerResult.allowed === true,
          ran: noOptionRunnerResult.ran === true,
          reason: noOptionRunnerResult.reason || null,
          failedRequirement: noOptionRunnerResult.failedRequirement || null,
          missingRequirements: Array.isArray(noOptionRunnerResult.missingRequirements)
            ? noOptionRunnerResult.missingRequirements
            : [],
          missingReasons: Array.isArray(noOptionRunnerResult.missingReasons)
            ? noOptionRunnerResult.missingReasons
            : []
        }
      : null,
    ownedSelector: selector,
    cleanupSelectorCount,
    nextStepRecommendation: {
      planningOnly: true,
      inertRendererShellContractPlanOnly: true,
      rendererBehaviorApproved: false,
      realMapAttachmentApproved: false,
      mapGeometryDrawingApproved: false,
      startupWiringApproved: false,
      recommendedNextPhase:
        "inert renderer shell or contract plan only, under separate review"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    },
    notApprovedFor: {
      firstShapeExecutionByThisHelper: true,
      cleanupExecutionByThisHelper: true,
      rendererInitialization: true,
      rendererBehavior: true,
      realMapAttachment: true,
      mapGeometryDrawing: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    notes: [
      "Passive renderer bridge planning report only.",
      "Does not create, run, or clean up the first-shape test layer.",
      "Does not initialize a renderer or attach a real map layer.",
      "The recommended next step remains an inert renderer shell or contract plan only."
    ]
  };
}

function getCustom25DVisualInertRendererShellContractPlanReport(options = {}) {
  const selector = '[data-growgo-custom-25d-first-shape-manual-test="true"]';
  const doc = typeof document !== "undefined" ? document : null;
  const cleanupSelectorCount =
    doc && typeof doc.querySelectorAll === "function"
      ? doc.querySelectorAll(selector).length
      : 0;
  const noOptionRunnerResult =
    typeof runCustom25DVisualFirstShapeManualTest === "function"
      ? runCustom25DVisualFirstShapeManualTest()
      : null;
  const rendererBridgePlan =
    typeof getCustom25DVisualRendererBridgePlanReport === "function"
      ? getCustom25DVisualRendererBridgePlanReport(options)
      : null;

  return {
    phase: 260,
    name: "custom-25d-visual-inert-renderer-shell-contract-plan-report",
    ok: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    contractOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "planning-only-inert-renderer-shell-contract",
    failClosedByDefault: true,
    createsNothing: true,
    removesNothing: true,
    drawsNothing: true,
    attachesNothing: true,
    mutatesNothing: true,
    helperAvailability: {
      rendererBridgePlan:
        typeof getCustom25DVisualRendererBridgePlanReport === "function",
      firstShapeHandoff:
        typeof getCustom25DVisualFirstShapeToRendererHandoffReport === "function",
      firstShapeCloseout:
        typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function",
      firstShapeRunner:
        typeof runCustom25DVisualFirstShapeManualTest === "function"
    },
    contractSummary: {
      phase259BridgeHelperAvailable:
        typeof getCustom25DVisualRendererBridgePlanReport === "function",
      futureShellMustBeInertByDefault: true,
      futureShellMustNotInitializeAutomatically: true,
      futureShellMustNotAttachToRealLeafletMap: true,
      futureShellMustNotDrawGeometry: true,
      futureShellMustNotAffectOsmPinsPlayerMarkerCaptureRadiusGameplayUiBackendStorageOrNetwork:
        true,
      futureShellMayOnlyExposePassiveMetadataStateInLaterReviewedPhase: true,
      recommendedNextPhaseMayCreateInertShellObjectOnlyIfDisconnectedAndNonRendering: true
    },
    bridgeSnapshot: rendererBridgePlan
      ? {
          phase: rendererBridgePlan.phase,
          ok: rendererBridgePlan.ok === true,
          defaultDecision: rendererBridgePlan.defaultDecision || null,
          firstShapeManualTestChainClosedOut:
            !!(
              rendererBridgePlan.bridgeSummary &&
              rendererBridgePlan.bridgeSummary.firstShapeManualTestChainClosedOut
            ),
          recommendedNextWorkInertRendererShellContractPlanOnly:
            !!(
              rendererBridgePlan.bridgeSummary &&
              rendererBridgePlan.bridgeSummary.recommendedNextWorkInertRendererShellContractPlanOnly
            )
        }
      : null,
    blockedNoOptionRunnerSnapshot: noOptionRunnerResult
      ? {
          allowed: noOptionRunnerResult.allowed === true,
          ran: noOptionRunnerResult.ran === true,
          reason: noOptionRunnerResult.reason || null,
          failedRequirement: noOptionRunnerResult.failedRequirement || null,
          missingRequirements: Array.isArray(noOptionRunnerResult.missingRequirements)
            ? noOptionRunnerResult.missingRequirements
            : [],
          missingReasons: Array.isArray(noOptionRunnerResult.missingReasons)
            ? noOptionRunnerResult.missingReasons
            : []
        }
      : null,
    ownedSelector: selector,
    cleanupSelectorCount,
    nextStepRecommendation: {
      planningOnly: true,
      inertRendererShellObjectOnly: true,
      disconnectedOnly: true,
      nonRenderingOnly: true,
      rendererBehaviorApproved: false,
      realMapAttachmentApproved: false,
      mapGeometryDrawingApproved: false,
      startupWiringApproved: false,
      recommendedNextPhase:
        "a separately reviewed inert renderer shell object only, still disconnected and non-rendering"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    },
    notApprovedFor: {
      rendererShellCreationByThisHelper: true,
      rendererInitialization: true,
      rendererBehavior: true,
      realMapAttachment: true,
      mapGeometryDrawing: true,
      firstShapeExecutionByThisHelper: true,
      cleanupExecutionByThisHelper: true,
      startupWiring: true,
      automaticInvocation: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      gameplayChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    notes: [
      "Passive inert renderer shell contract planning report only.",
      "Does not create a renderer shell.",
      "Does not initialize a renderer, attach a real map layer, or draw geometry.",
      "Any future inert shell phase must remain disconnected, non-rendering, and separately reviewed."
    ]
  };
}

function createCustom25DVisualInertRendererShell(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualInertRendererShellContractPlanReport === "function"
      ? getCustom25DVisualInertRendererShellContractPlanReport(options)
      : null;

  return {
    phase: 261,
    name: "custom-25d-visual-inert-renderer-shell",
    ok: true,
    inert: true,
    disconnected: true,
    initialized: false,
    attached: false,
    drawingEnabled: false,
    mapAttached: false,
    geometryDrawn: false,
    startupWired: false,
    autoRun: false,
    layerCount: 0,
    metadataOnly: true,
    contractPlanAvailable: !!contractPlan,
    bridgePlanAvailable:
      typeof getCustom25DVisualRendererBridgePlanReport === "function",
    shellState: {
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      lifecycleConnected: false,
      mapLayerAttached: false,
      geometryDrawingActive: false,
      domNodesCreated: false
    },
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    },
    notApprovedFor: {
      globalStorage: true,
      windowAttachment: true,
      rendererInitialization: true,
      rendererBehavior: true,
      realMapAttachment: true,
      mapGeometryDrawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      firstShapeExecution: true,
      firstShapeCleanupExecution: true,
      osmChanges: true,
      pinChanges: true,
      playerMarkerChanges: true,
      captureRadiusChanges: true,
      gameplayChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    notes: [
      "Returns an inert disconnected renderer shell object only.",
      "Does not attach anything to window or store shell state globally.",
      "Does not initialize a renderer, attach a map layer, create DOM, or draw geometry."
    ]
  };
}

function getCustom25DVisualInertRendererShellCloseoutReport(options = {}) {
  const inertShell =
    typeof createCustom25DVisualInertRendererShell === "function"
      ? createCustom25DVisualInertRendererShell(options)
      : null;
  const firstShapeCloseout =
    typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function"
      ? getCustom25DVisualFirstShapeManualTestCloseoutReport(options)
      : null;
  const firstShapeHandoff =
    typeof getCustom25DVisualFirstShapeToRendererHandoffReport === "function"
      ? getCustom25DVisualFirstShapeToRendererHandoffReport(options)
      : null;
  const cleanupSelectorCount =
    firstShapeCloseout && typeof firstShapeCloseout.cleanupSelectorCount === "number"
      ? firstShapeCloseout.cleanupSelectorCount
      : firstShapeHandoff && typeof firstShapeHandoff.cleanupSelectorCount === "number"
        ? firstShapeHandoff.cleanupSelectorCount
        : null;

  return {
    phase: 262,
    name: "custom-25d-visual-inert-renderer-shell-closeout",
    ok: !!(inertShell && inertShell.ok === true),
    passive: true,
    reportOnly: true,
    inertShellAvailable: typeof createCustom25DVisualInertRendererShell === "function",
    shellOk: !!(inertShell && inertShell.ok === true),
    shellInert: !!(inertShell && inertShell.inert === true),
    shellDisconnected: !!(inertShell && inertShell.disconnected === true),
    shellInitialized: !!(inertShell && inertShell.initialized === true),
    shellAttached: !!(inertShell && inertShell.attached === true),
    shellDrawingEnabled: !!(inertShell && inertShell.drawingEnabled === true),
    shellLayerCount:
      inertShell && typeof inertShell.layerCount === "number" ? inertShell.layerCount : 0,
    cleanupSelectorCount,
    preservedSystems:
      inertShell && inertShell.preservedSystems
        ? inertShell.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    blockedBehavior: {
      rendererInitialization: true,
      mapAttachment: true,
      geometryDrawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    nextStep: "renderer-lifecycle-plan",
    safetyFlags:
      inertShell && inertShell.safetyFlags
        ? inertShell.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive inert renderer shell closeout report only.",
      "Inspects inert shell metadata only.",
      "Does not initialize a renderer, attach to the map, draw geometry, create DOM, or wire startup."
    ]
  };
}

function getCustom25DVisualRendererLifecyclePlanReport(options = {}) {
  const shellCloseout =
    typeof getCustom25DVisualInertRendererShellCloseoutReport === "function"
      ? getCustom25DVisualInertRendererShellCloseoutReport(options)
      : null;

  return {
    phase: 263,
    name: "custom-25d-visual-renderer-lifecycle-plan",
    ok: !!(shellCloseout && shellCloseout.ok === true),
    passive: true,
    reportOnly: true,
    planningOnly: true,
    shellCloseoutAvailable:
      typeof getCustom25DVisualInertRendererShellCloseoutReport === "function",
    shellCloseoutOk: !!(shellCloseout && shellCloseout.ok === true),
    approvedLifecycleStates: [
      "created-metadata-only",
      "initialized-false",
      "attached-false",
      "running-false",
      "drawing-enabled-false",
      "disposed-metadata-only"
    ],
    blockedLifecycleActions: {
      rendererInitialization: true,
      rendererInstanceCreation: true,
      mapAttachment: true,
      geometryDrawing: true,
      domCreation: true,
      startupWiring: true,
      autoRun: true,
      storageWrites: true,
      networkAccess: true
    },
    preservedSystems:
      shellCloseout && shellCloseout.preservedSystems
        ? shellCloseout.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    requiredFutureReviews: [
      "renderer-lifecycle-contract",
      "manual-initialization-guard-plan",
      "manual-attachment-guard-plan",
      "manual-drawing-guard-plan"
    ],
    nextStep: "renderer-lifecycle-contract",
    safetyFlags:
      shellCloseout && shellCloseout.safetyFlags
        ? shellCloseout.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive renderer lifecycle planning report only.",
      "Does not initialize a renderer, create a renderer instance, attach to the map, draw geometry, or create DOM.",
      "Any renderer lifecycle work remains blocked pending future contract and guard review phases."
    ]
  };
}

function getCustom25DVisualRendererLifecycleContractReport(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualRendererLifecyclePlanReport(options)
      : null;

  return {
    phase: 264,
    name: "custom-25d-visual-renderer-lifecycle-contract",
    ok: !!(lifecyclePlan && lifecyclePlan.ok === true),
    passive: true,
    reportOnly: true,
    contractOnly: true,
    lifecyclePlanAvailable:
      typeof getCustom25DVisualRendererLifecyclePlanReport === "function",
    lifecyclePlanOk: !!(lifecyclePlan && lifecyclePlan.ok === true),
    requiredLifecycleDefaults: {
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      lifecycleConnected: false,
      mapLayerAttached: false,
      geometryDrawingActive: false,
      domNodesCreated: false
    },
    requiredBlockedActions: {
      rendererInitialization: true,
      mapAttachment: true,
      geometryDrawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    preservedSystems:
      lifecyclePlan && lifecyclePlan.preservedSystems
        ? lifecyclePlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    futureManualApprovalGates: [
      "manual-create-gate",
      "manual-initialize-gate",
      "manual-attach-gate",
      "manual-draw-gate",
      "manual-dispose-gate"
    ],
    nextStep: "manual-initialization-guard-plan",
    safetyFlags:
      lifecyclePlan && lifecyclePlan.safetyFlags
        ? lifecyclePlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive renderer lifecycle contract report only.",
      "Does not initialize a renderer, create a renderer instance, attach to the map, draw geometry, or create DOM.",
      "Future lifecycle work remains blocked until separate manual guard phases are reviewed."
    ]
  };
}

function getCustom25DVisualManualInitializationGuardPlanReport(options = {}) {
  const lifecycleContract =
    typeof getCustom25DVisualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualRendererLifecycleContractReport(options)
      : null;

  return {
    phase: 265,
    name: "custom-25d-visual-manual-initialization-guard-plan",
    ok: !!(lifecycleContract && lifecycleContract.ok === true),
    passive: true,
    reportOnly: true,
    planningOnly: true,
    lifecycleContractAvailable:
      typeof getCustom25DVisualRendererLifecycleContractReport === "function",
    lifecycleContractOk: !!(lifecycleContract && lifecycleContract.ok === true),
    plannedGuardName: "canInitializeCustom25DVisualRendererManually",
    requiredManualSignals: [
      "local-dev-only",
      "browser-console-only",
      "explicit-options-only",
      "manual-renderer-initialization-approved",
      "lifecycle-contract-ok",
      "shell-inert-before-initialization"
    ],
    blockedByDefault: true,
    noOptionCallAllowed: false,
    approvedBehavior: "metadata-plan-only",
    blockedBehavior: {
      rendererCreation: true,
      rendererInitialization: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    preservedSystems:
      lifecycleContract && lifecycleContract.preservedSystems
        ? lifecycleContract.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-guard-contract",
    safetyFlags:
      lifecycleContract && lifecycleContract.safetyFlags
        ? lifecycleContract.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization guard planning report only.",
      "Does not create or initialize a renderer, attach to the map, draw geometry, or create DOM.",
      "Future manual initialization remains blocked until a dedicated guard contract is separately reviewed."
    ]
  };
}

function getCustom25DVisualManualInitializationGuardContractReport(options = {}) {
  const guardPlan =
    typeof getCustom25DVisualManualInitializationGuardPlanReport === "function"
      ? getCustom25DVisualManualInitializationGuardPlanReport(options)
      : null;

  return {
    phase: 266,
    name: "custom-25d-visual-manual-initialization-guard-contract",
    ok: !!(guardPlan && guardPlan.ok === true),
    passive: true,
    reportOnly: true,
    contractOnly: true,
    guardPlanAvailable:
      typeof getCustom25DVisualManualInitializationGuardPlanReport === "function",
    guardPlanOk: !!(guardPlan && guardPlan.ok === true),
    guardName: "canInitializeCustom25DVisualRendererManually",
    requiredSignals: {
      localDevOnly: true,
      browserConsoleOnly: true,
      explicitOptionsOnly: true,
      manualRendererInitializationApproved: true,
      lifecycleContractOk: true,
      shellInertBeforeInitialization: true
    },
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    allowedBehavior: "contract-report-only",
    blockedBehavior: {
      rendererCreation: true,
      rendererInitialization: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    preservedSystems:
      guardPlan && guardPlan.preservedSystems
        ? guardPlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-guard-shell",
    safetyFlags:
      guardPlan && guardPlan.safetyFlags
        ? guardPlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization guard contract report only.",
      "Does not initialize a renderer, create a renderer instance, attach to the map, draw geometry, or create DOM.",
      "Future manual initialization remains blocked until a separate guard shell phase is reviewed."
    ]
  };
}

function canInitializeCustom25DVisualRendererManually(options = {}) {
  const contractReport =
    typeof getCustom25DVisualManualInitializationGuardContractReport === "function"
      ? getCustom25DVisualManualInitializationGuardContractReport(options)
      : null;
  const requiredSignalsChecked = [
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "manualRendererInitializationApproved",
    "lifecycleContractOk",
    "shellInertBeforeInitialization"
  ];
  const signalStates = {
    localDevOnly: options.localDevOnly === true,
    browserConsoleOnly: options.browserConsoleOnly === true,
    explicitOptionsOnly: options.explicitOptionsOnly === true,
    manualRendererInitializationApproved:
      options.manualRendererInitializationApproved === true,
    lifecycleContractOk:
      !!(contractReport && contractReport.guardPlanOk === true),
    shellInertBeforeInitialization: options.shellInertBeforeInitialization === true
  };
  const missingSignals = requiredSignalsChecked.filter((signal) => !signalStates[signal]);
  const blockedReasons = [
    "manual-initialization-guard-shell-is-blocked-by-default",
    "real-initialization-not-approved-in-this-phase",
    ...missingSignals.map((signal) => `${signal}-required`)
  ];

  return {
    phase: 267,
    name: "custom-25d-visual-manual-initialization-guard-shell",
    ok: !!(contractReport && contractReport.ok === true),
    allowed: false,
    blocked: true,
    passive: true,
    guardOnly: true,
    shellOnly: true,
    contractAvailable:
      typeof getCustom25DVisualManualInitializationGuardContractReport === "function",
    contractOk: !!(contractReport && contractReport.ok === true),
    requiredSignalsChecked,
    missingSignals,
    blockedReasons,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "guard-shell-only",
    blockedBehavior: {
      rendererCreation: true,
      rendererInitialization: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    },
    preservedSystems:
      contractReport && contractReport.preservedSystems
        ? contractReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-guard-shell-closeout",
    safetyFlags:
      contractReport && contractReport.safetyFlags
        ? contractReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization guard shell only.",
      "A no-option call remains blocked by default.",
      "Approval-like options are inspected but do not permit any real initialization in this phase."
    ]
  };
}

function getCustom25DVisualManualInitializationGuardShellCloseoutReport(options = {}) {
  const guardResult =
    typeof canInitializeCustom25DVisualRendererManually === "function"
      ? canInitializeCustom25DVisualRendererManually(options)
      : null;
  const noOptionResult =
    typeof canInitializeCustom25DVisualRendererManually === "function"
      ? canInitializeCustom25DVisualRendererManually()
      : null;
  const approvalLikeOptionsResult =
    typeof canInitializeCustom25DVisualRendererManually === "function"
      ? canInitializeCustom25DVisualRendererManually({
          localDevOnly: true,
          browserConsoleOnly: true,
          explicitOptionsOnly: true,
          manualRendererInitializationApproved: true,
          shellInertBeforeInitialization: true
        })
      : null;

  return {
    phase: 268,
    name: "custom-25d-visual-manual-initialization-guard-shell-closeout",
    ok: !!(guardResult && guardResult.ok === true),
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    guardAvailable:
      typeof canInitializeCustom25DVisualRendererManually === "function",
    guardOk: !!(guardResult && guardResult.ok === true),
    noOptionAllowed: !!(noOptionResult && noOptionResult.allowed === true),
    noOptionBlocked: !!(noOptionResult && noOptionResult.blocked === true),
    approvalLikeOptionsStillBlocked:
      !!(approvalLikeOptionsResult && approvalLikeOptionsResult.blocked === true),
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    guardShellOnly: true,
    blockedBehavior:
      guardResult && guardResult.blockedBehavior
        ? guardResult.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      guardResult && guardResult.preservedSystems
        ? guardResult.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-approval-signal-plan",
    safetyFlags:
      guardResult && guardResult.safetyFlags
        ? guardResult.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization guard shell closeout report only.",
      "A no-option call remains blocked by default.",
      "Approval-like options still do not permit any real initialization, attachment, drawing, or mutation in this phase."
    ]
  };
}

function getCustom25DVisualManualInitializationApprovalSignalPlanReport(options = {}) {
  const guardShellCloseout =
    typeof getCustom25DVisualManualInitializationGuardShellCloseoutReport === "function"
      ? getCustom25DVisualManualInitializationGuardShellCloseoutReport(options)
      : null;

  return {
    phase: 269,
    name: "custom-25d-visual-manual-initialization-approval-signal-plan",
    ok: !!(guardShellCloseout && guardShellCloseout.ok === true),
    passive: true,
    reportOnly: true,
    planningOnly: true,
    guardShellCloseoutAvailable:
      typeof getCustom25DVisualManualInitializationGuardShellCloseoutReport === "function",
    guardShellCloseoutOk: !!(guardShellCloseout && guardShellCloseout.ok === true),
    plannedApprovalSignals: [
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "manualRendererInitializationApproved",
      "lifecycleContractOk",
      "shellInertBeforeInitialization",
      "rendererCreationStillBlocked",
      "mapAttachmentStillBlocked",
      "drawingStillBlocked"
    ],
    approvalSignalsDoNotAuthorizeYet: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "approval-signal-plan-only",
    blockedBehavior:
      guardShellCloseout && guardShellCloseout.blockedBehavior
        ? guardShellCloseout.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      guardShellCloseout && guardShellCloseout.preservedSystems
        ? guardShellCloseout.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-approval-signal-contract",
    safetyFlags:
      guardShellCloseout && guardShellCloseout.safetyFlags
        ? guardShellCloseout.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization approval signal planning report only.",
      "Approval signals are planned here but do not authorize renderer initialization in this phase.",
      "Renderer creation, initialization, map attachment, and drawing remain blocked."
    ]
  };
}

function getCustom25DVisualManualInitializationApprovalSignalContractReport(options = {}) {
  const approvalSignalPlan =
    typeof getCustom25DVisualManualInitializationApprovalSignalPlanReport === "function"
      ? getCustom25DVisualManualInitializationApprovalSignalPlanReport(options)
      : null;

  return {
    phase: 270,
    name: "custom-25d-visual-manual-initialization-approval-signal-contract",
    ok: !!(approvalSignalPlan && approvalSignalPlan.ok === true),
    passive: true,
    reportOnly: true,
    contractOnly: true,
    approvalSignalPlanAvailable:
      typeof getCustom25DVisualManualInitializationApprovalSignalPlanReport === "function",
    approvalSignalPlanOk: !!(approvalSignalPlan && approvalSignalPlan.ok === true),
    requiredApprovalSignals: {
      localDevOnly: true,
      browserConsoleOnly: true,
      explicitOptionsOnly: true,
      manualRendererInitializationApproved: true,
      lifecycleContractOk: true,
      shellInertBeforeInitialization: true,
      rendererCreationStillBlocked: true,
      mapAttachmentStillBlocked: true,
      drawingStillBlocked: true
    },
    approvalSignalsAuthorizeInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "approval-signal-contract-only",
    blockedBehavior:
      approvalSignalPlan && approvalSignalPlan.blockedBehavior
        ? approvalSignalPlan.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      approvalSignalPlan && approvalSignalPlan.preservedSystems
        ? approvalSignalPlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-signal-evaluator-shell",
    safetyFlags:
      approvalSignalPlan && approvalSignalPlan.safetyFlags
        ? approvalSignalPlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization approval signal contract report only.",
      "Approval signals are defined here but still do not authorize renderer initialization in this phase.",
      "Renderer creation, initialization, map attachment, and drawing remain blocked."
    ]
  };
}

function evaluateCustom25DVisualManualInitializationSignals(options = {}) {
  const signalContract =
    typeof getCustom25DVisualManualInitializationApprovalSignalContractReport === "function"
      ? getCustom25DVisualManualInitializationApprovalSignalContractReport(options)
      : null;
  const signalsChecked = [
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "manualRendererInitializationApproved",
    "lifecycleContractOk",
    "shellInertBeforeInitialization",
    "rendererCreationStillBlocked",
    "mapAttachmentStillBlocked",
    "drawingStillBlocked"
  ];
  const signalStates = {
    localDevOnly: options.localDevOnly === true,
    browserConsoleOnly: options.browserConsoleOnly === true,
    explicitOptionsOnly: options.explicitOptionsOnly === true,
    manualRendererInitializationApproved:
      options.manualRendererInitializationApproved === true,
    lifecycleContractOk: !!(signalContract && signalContract.approvalSignalPlanOk === true),
    shellInertBeforeInitialization: options.shellInertBeforeInitialization === true,
    rendererCreationStillBlocked: true,
    mapAttachmentStillBlocked: true,
    drawingStillBlocked: true
  };
  const presentSignals = signalsChecked.filter((signal) => signalStates[signal] === true);
  const missingSignals = signalsChecked.filter((signal) => signalStates[signal] !== true);

  return {
    phase: 271,
    name: "custom-25d-visual-manual-initialization-signal-evaluator-shell",
    ok: !!(signalContract && signalContract.ok === true),
    passive: true,
    evaluatorOnly: true,
    shellOnly: true,
    contractAvailable:
      typeof getCustom25DVisualManualInitializationApprovalSignalContractReport === "function",
    contractOk: !!(signalContract && signalContract.ok === true),
    signalsChecked,
    presentSignals,
    missingSignals,
    allowed: false,
    blocked: true,
    approvalSignalsAuthorizeInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "signal-evaluator-shell-only",
    blockedBehavior:
      signalContract && signalContract.blockedBehavior
        ? signalContract.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      signalContract && signalContract.preservedSystems
        ? signalContract.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-signal-evaluator-closeout",
    safetyFlags:
      signalContract && signalContract.safetyFlags
        ? signalContract.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization signal evaluator shell only.",
      "A no-option call remains blocked by default.",
      "Approval-like options are reported as signals only and still do not authorize initialization in this phase."
    ]
  };
}

function getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport(options = {}) {
  const evaluatorResult =
    typeof evaluateCustom25DVisualManualInitializationSignals === "function"
      ? evaluateCustom25DVisualManualInitializationSignals(options)
      : null;
  const noOptionResult =
    typeof evaluateCustom25DVisualManualInitializationSignals === "function"
      ? evaluateCustom25DVisualManualInitializationSignals()
      : null;
  const approvalLikeOptionsResult =
    typeof evaluateCustom25DVisualManualInitializationSignals === "function"
      ? evaluateCustom25DVisualManualInitializationSignals({
          localDevOnly: true,
          browserConsoleOnly: true,
          explicitOptionsOnly: true,
          manualRendererInitializationApproved: true,
          shellInertBeforeInitialization: true
        })
      : null;

  return {
    phase: 272,
    name: "custom-25d-visual-manual-initialization-signal-evaluator-closeout",
    ok: !!(evaluatorResult && evaluatorResult.ok === true),
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    evaluatorAvailable:
      typeof evaluateCustom25DVisualManualInitializationSignals === "function",
    evaluatorOk: !!(evaluatorResult && evaluatorResult.ok === true),
    noOptionAllowed: !!(noOptionResult && noOptionResult.allowed === true),
    noOptionBlocked: !!(noOptionResult && noOptionResult.blocked === true),
    approvalLikeOptionsStillBlocked:
      !!(approvalLikeOptionsResult && approvalLikeOptionsResult.blocked === true),
    approvalSignalsAuthorizeInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    evaluatorShellOnly: true,
    blockedBehavior:
      evaluatorResult && evaluatorResult.blockedBehavior
        ? evaluatorResult.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      evaluatorResult && evaluatorResult.preservedSystems
        ? evaluatorResult.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-guard-evaluator-integration-plan",
    safetyFlags:
      evaluatorResult && evaluatorResult.safetyFlags
        ? evaluatorResult.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization signal evaluator closeout report only.",
      "A no-option evaluator check remains blocked by default.",
      "Approval-like options still do not authorize initialization, creation, attachment, drawing, or mutation in this phase."
    ]
  };
}

function getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport(
  options = {}
) {
  const evaluatorCloseout =
    typeof getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport === "function"
      ? getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport(options)
      : null;
  const guardResult =
    typeof canInitializeCustom25DVisualRendererManually === "function"
      ? canInitializeCustom25DVisualRendererManually(options)
      : null;

  return {
    phase: 273,
    name: "custom-25d-visual-manual-initialization-guard-evaluator-integration-plan",
    ok:
      !!(evaluatorCloseout && evaluatorCloseout.ok === true) &&
      !!(guardResult && guardResult.ok === true),
    passive: true,
    reportOnly: true,
    planningOnly: true,
    guardAvailable: typeof canInitializeCustom25DVisualRendererManually === "function",
    guardOk: !!(guardResult && guardResult.ok === true),
    evaluatorCloseoutAvailable:
      typeof getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport ===
      "function",
    evaluatorCloseoutOk: !!(evaluatorCloseout && evaluatorCloseout.ok === true),
    integrationPurpose: "plan-only",
    plannedIntegrationSteps: [
      "check-lifecycle-contract",
      "evaluate-manual-signals",
      "require-guard-and-evaluator-agreement",
      "keep-renderer-creation-blocked",
      "keep-initialization-blocked-until-future-approved-phase"
    ],
    integrationAuthorizesInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "guard-evaluator-integration-plan-only",
    blockedBehavior:
      evaluatorCloseout && evaluatorCloseout.blockedBehavior
        ? evaluatorCloseout.blockedBehavior
        : guardResult && guardResult.blockedBehavior
          ? guardResult.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      evaluatorCloseout && evaluatorCloseout.preservedSystems
        ? evaluatorCloseout.preservedSystems
        : guardResult && guardResult.preservedSystems
          ? guardResult.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    nextStep: "manual-initialization-guard-evaluator-integration-contract",
    safetyFlags:
      evaluatorCloseout && evaluatorCloseout.safetyFlags
        ? evaluatorCloseout.safetyFlags
        : guardResult && guardResult.safetyFlags
          ? guardResult.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    notes: [
      "Passive manual initialization guard/evaluator integration planning report only.",
      "Does not make the guard allow initialization.",
      "Does not make the evaluator allow initialization.",
      "Renderer creation and initialization remain blocked until a future separately approved phase."
    ]
  };
}

function getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport(
  options = {}
) {
  const integrationPlan =
    typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport ===
    "function"
      ? getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport(options)
      : null;

  return {
    phase: 274,
    name: "custom-25d-visual-manual-initialization-guard-evaluator-integration-contract",
    ok: !!(integrationPlan && integrationPlan.ok === true),
    passive: true,
    reportOnly: true,
    contractOnly: true,
    integrationPlanAvailable:
      typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport ===
      "function",
    integrationPlanOk: !!(integrationPlan && integrationPlan.ok === true),
    requiredIntegrationChecks: {
      lifecycleContractOk: true,
      guardAvailable: true,
      evaluatorAvailable: true,
      guardAndEvaluatorMustAgree: true,
      rendererCreationStillBlocked: true,
      rendererInitializationStillBlocked: true,
      mapAttachmentStillBlocked: true,
      drawingStillBlocked: true
    },
    integrationAuthorizesInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "guard-evaluator-integration-contract-only",
    blockedBehavior:
      integrationPlan && integrationPlan.blockedBehavior
        ? integrationPlan.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      integrationPlan && integrationPlan.preservedSystems
        ? integrationPlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-initialization-integrated-guard-shell",
    safetyFlags:
      integrationPlan && integrationPlan.safetyFlags
        ? integrationPlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization guard/evaluator integration contract report only.",
      "Does not make the guard allow initialization or the evaluator authorize initialization.",
      "Renderer creation and initialization remain blocked until a future separately approved phase."
    ]
  };
}

function canUseCustom25DVisualManualRendererInitializationIntegratedGuard(options = {}) {
  const integrationContract =
    typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport ===
    "function"
      ? getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport(options)
      : null;
  const guardResult =
    typeof canInitializeCustom25DVisualRendererManually === "function"
      ? canInitializeCustom25DVisualRendererManually(options)
      : null;
  const evaluatorResult =
    typeof evaluateCustom25DVisualManualInitializationSignals === "function"
      ? evaluateCustom25DVisualManualInitializationSignals(options)
      : null;

  const blockedReasons = [
    "integrated-guard-shell-is-blocked-by-default",
    "real-initialization-not-approved-in-this-phase"
  ];
  if (!integrationContract || integrationContract.ok !== true) {
    blockedReasons.push("integration-contract-not-ready");
  }
  if (!guardResult || guardResult.ok !== true) {
    blockedReasons.push("manual-initialization-guard-not-ready");
  }
  if (!evaluatorResult || evaluatorResult.ok !== true) {
    blockedReasons.push("manual-initialization-signal-evaluator-not-ready");
  }
  if (guardResult && guardResult.allowed === false) {
    blockedReasons.push("manual-initialization-guard-blocked");
  }
  if (evaluatorResult && evaluatorResult.allowed === false) {
    blockedReasons.push("manual-initialization-signal-evaluator-blocked");
  }

  return {
    phase: 275,
    name: "custom-25d-visual-manual-initialization-integrated-guard-shell",
    ok:
      !!(integrationContract && integrationContract.ok === true) &&
      !!(guardResult && guardResult.ok === true) &&
      !!(evaluatorResult && evaluatorResult.ok === true),
    passive: true,
    guardOnly: true,
    shellOnly: true,
    integrationContractAvailable:
      typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport ===
      "function",
    integrationContractOk: !!(integrationContract && integrationContract.ok === true),
    guardAvailable: typeof canInitializeCustom25DVisualRendererManually === "function",
    guardOk: !!(guardResult && guardResult.ok === true),
    evaluatorAvailable:
      typeof evaluateCustom25DVisualManualInitializationSignals === "function",
    evaluatorOk: !!(evaluatorResult && evaluatorResult.ok === true),
    guardAllowed: !!(guardResult && guardResult.allowed === true),
    evaluatorAllowed: !!(evaluatorResult && evaluatorResult.allowed === true),
    allowed: false,
    blocked: true,
    integrationAuthorizesInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "integrated-guard-shell-only",
    blockedReasons,
    blockedBehavior:
      integrationContract && integrationContract.blockedBehavior
        ? integrationContract.blockedBehavior
        : guardResult && guardResult.blockedBehavior
          ? guardResult.blockedBehavior
          : evaluatorResult && evaluatorResult.blockedBehavior
            ? evaluatorResult.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      integrationContract && integrationContract.preservedSystems
        ? integrationContract.preservedSystems
        : guardResult && guardResult.preservedSystems
          ? guardResult.preservedSystems
          : evaluatorResult && evaluatorResult.preservedSystems
            ? evaluatorResult.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    nextStep: "manual-initialization-integrated-guard-closeout",
    safetyFlags:
      integrationContract && integrationContract.safetyFlags
        ? integrationContract.safetyFlags
        : guardResult && guardResult.safetyFlags
          ? guardResult.safetyFlags
          : evaluatorResult && evaluatorResult.safetyFlags
            ? evaluatorResult.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive manual initialization integrated guard shell only.",
      "A no-option call remains blocked by default.",
      "Approval-like options are inspected but still do not authorize renderer initialization in this phase."
    ]
  };
}

function getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport(options = {}) {
  const integratedGuardResult =
    typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function"
      ? canUseCustom25DVisualManualRendererInitializationIntegratedGuard(options)
      : null;
  const noOptionResult =
    typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function"
      ? canUseCustom25DVisualManualRendererInitializationIntegratedGuard()
      : null;
  const approvalLikeOptionsResult =
    typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function"
      ? canUseCustom25DVisualManualRendererInitializationIntegratedGuard({
          localDevOnly: true,
          browserConsoleOnly: true,
          explicitOptionsOnly: true,
          manualRendererInitializationApproved: true,
          lifecycleContractOk: true,
          shellInertBeforeInitialization: true,
          rendererCreationStillBlocked: true,
          mapAttachmentStillBlocked: true,
          drawingStillBlocked: true
        })
      : null;

  return {
    phase: 276,
    name: "custom-25d-visual-manual-initialization-integrated-guard-closeout",
    ok: !!(integratedGuardResult && integratedGuardResult.ok === true),
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    integratedGuardAvailable:
      typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function",
    integratedGuardOk: !!(integratedGuardResult && integratedGuardResult.ok === true),
    noOptionAllowed: !!(noOptionResult && noOptionResult.allowed === true),
    noOptionBlocked: !!(noOptionResult && noOptionResult.blocked === true),
    approvalLikeOptionsStillBlocked:
      !!(approvalLikeOptionsResult && approvalLikeOptionsResult.blocked === true),
    integrationAuthorizesInitialization: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    integratedGuardShellOnly: true,
    blockedBehavior:
      integratedGuardResult && integratedGuardResult.blockedBehavior
        ? integratedGuardResult.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      integratedGuardResult && integratedGuardResult.preservedSystems
        ? integratedGuardResult.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-renderer-initialization-shell-plan",
    safetyFlags:
      integratedGuardResult && integratedGuardResult.safetyFlags
        ? integratedGuardResult.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual initialization integrated guard closeout report only.",
      "A no-option integrated guard check remains blocked by default.",
      "Approval-like options still do not authorize initialization, creation, attachment, drawing, or mutation in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererInitializationShellPlanReport(options = {}) {
  const integratedGuardCloseout =
    typeof getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport === "function"
      ? getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport(options)
      : null;

  return {
    phase: 277,
    name: "custom-25d-visual-manual-renderer-initialization-shell-plan",
    ok: !!(integratedGuardCloseout && integratedGuardCloseout.ok === true),
    passive: true,
    reportOnly: true,
    planningOnly: true,
    integratedGuardCloseoutAvailable:
      typeof getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport ===
      "function",
    integratedGuardCloseoutOk:
      !!(integratedGuardCloseout && integratedGuardCloseout.ok === true),
    plannedShellName: "initializeCustom25DVisualRendererManually",
    plannedInitializationBehavior: "manual-shell-only",
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredFutureChecks: [
      "integrated-guard-allows-initialization",
      "renderer-shell-exists",
      "renderer-created-state-remains-false-before-init",
      "map-attachment-remains-blocked",
      "drawing-remains-blocked",
      "startup-wiring-remains-blocked"
    ],
    blockedBehavior:
      integratedGuardCloseout && integratedGuardCloseout.blockedBehavior
        ? integratedGuardCloseout.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      integratedGuardCloseout && integratedGuardCloseout.preservedSystems
        ? integratedGuardCloseout.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-renderer-initialization-shell-contract",
    safetyFlags:
      integratedGuardCloseout && integratedGuardCloseout.safetyFlags
        ? integratedGuardCloseout.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual renderer initialization shell planning report only.",
      "Does not create initializeCustom25DVisualRendererManually in this phase.",
      "Renderer creation and renderer start remain blocked until a future separately approved phase."
    ]
  };
}

function getCustom25DVisualManualRendererInitializationShellContractReport(options = {}) {
  const initializationShellPlan =
    typeof getCustom25DVisualManualRendererInitializationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererInitializationShellPlanReport(options)
      : null;

  return {
    phase: 278,
    name: "custom-25d-visual-manual-renderer-initialization-shell-contract",
    ok: !!(initializationShellPlan && initializationShellPlan.ok === true),
    passive: true,
    reportOnly: true,
    contractOnly: true,
    initializationShellPlanAvailable:
      typeof getCustom25DVisualManualRendererInitializationShellPlanReport === "function",
    initializationShellPlanOk:
      !!(initializationShellPlan && initializationShellPlan.ok === true),
    futureShellName: "initializeCustom25DVisualRendererManually",
    requiredShellDefaults: {
      allowed: false,
      blocked: true,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false
    },
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "manual-renderer-initialization-shell-contract-only",
    blockedBehavior:
      initializationShellPlan && initializationShellPlan.blockedBehavior
        ? initializationShellPlan.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      initializationShellPlan && initializationShellPlan.preservedSystems
        ? initializationShellPlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-renderer-initialization-shell",
    safetyFlags:
      initializationShellPlan && initializationShellPlan.safetyFlags
        ? initializationShellPlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual renderer initialization shell contract report only.",
      "Does not create initializeCustom25DVisualRendererManually in this phase.",
      "Renderer creation and renderer start remain blocked until a future separately approved phase."
    ]
  };
}

function initializeCustom25DVisualRendererManually(options = {}) {
  const initializationShellContract =
    typeof getCustom25DVisualManualRendererInitializationShellContractReport === "function"
      ? getCustom25DVisualManualRendererInitializationShellContractReport(options)
      : null;
  const integratedGuardResult =
    typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function"
      ? canUseCustom25DVisualManualRendererInitializationIntegratedGuard(options)
      : null;

  const blockedReasons = [
    "manual-renderer-initialization-shell-is-blocked-by-default",
    "real-renderer-creation-not-approved-in-this-phase",
    "real-renderer-initialization-not-approved-in-this-phase"
  ];
  if (!initializationShellContract || initializationShellContract.ok !== true) {
    blockedReasons.push("initialization-shell-contract-not-ready");
  }
  if (!integratedGuardResult || integratedGuardResult.ok !== true) {
    blockedReasons.push("integrated-initialization-guard-not-ready");
  }
  if (integratedGuardResult && integratedGuardResult.allowed === false) {
    blockedReasons.push("integrated-initialization-guard-blocked");
  }

  return {
    phase: 279,
    name: "custom-25d-visual-manual-renderer-initialization-shell",
    ok:
      !!(initializationShellContract && initializationShellContract.ok === true) &&
      !!(integratedGuardResult && integratedGuardResult.ok === true),
    passive: true,
    shellOnly: true,
    initializationOnly: true,
    contractAvailable:
      typeof getCustom25DVisualManualRendererInitializationShellContractReport ===
      "function",
    contractOk: !!(initializationShellContract && initializationShellContract.ok === true),
    integratedGuardAvailable:
      typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function",
    integratedGuardOk: !!(integratedGuardResult && integratedGuardResult.ok === true),
    integratedGuardAllowed:
      !!(integratedGuardResult && integratedGuardResult.allowed === true),
    allowed: false,
    blocked: true,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "manual-renderer-initialization-shell-only",
    blockedReasons,
    blockedBehavior:
      initializationShellContract && initializationShellContract.blockedBehavior
        ? initializationShellContract.blockedBehavior
        : integratedGuardResult && integratedGuardResult.blockedBehavior
          ? integratedGuardResult.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      initializationShellContract && initializationShellContract.preservedSystems
        ? initializationShellContract.preservedSystems
        : integratedGuardResult && integratedGuardResult.preservedSystems
          ? integratedGuardResult.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    nextStep: "manual-renderer-initialization-shell-closeout",
    safetyFlags:
      initializationShellContract && initializationShellContract.safetyFlags
        ? initializationShellContract.safetyFlags
        : integratedGuardResult && integratedGuardResult.safetyFlags
          ? integratedGuardResult.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    notes: [
      "Passive manual renderer initialization shell only.",
      "A no-option call remains blocked by default.",
      "Approval-like options are inspected but still do not create or initialize a renderer in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererInitializationShellCloseoutReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "closeoutOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellReport ===
    "function";
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 398,
      name: "custom-25d-visual-manual-renderer-initialization-shell-closeout",
      helperName:
        "getCustom25DVisualManualRendererInitializationShellCloseoutReport",
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      initializationShellAvailable: initializationShellHelperAvailable,
      initializationShellHelperAvailable,
      initializationReportHelperAvailable,
      initializationShellAddedInPhase397: true,
      initializationShellAddedThisPhase: false,
      initializationShellInvokedByCloseout: false,
      initializationPerformedByCloseout: false,
      initializationShellOk: false,
      noOptionAllowed: false,
      noOptionBlocked: true,
      approvalLikeOptionsStillBlocked: true,
      stateContainerExists,
      stateCreatedByCloseout: false,
      stateMutatedByCloseout: false,
      rendererCreated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      drawingStarted: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      initializationAuthorizesRendererCreation: false,
      initializationAuthorizesRendererStart: false,
      defaultDecision: "blocked",
      noOptionCallAllowed: false,
      initializationShellOnly: true,
      blockedBehavior,
      preservedSystems,
      nextStep: "manual-renderer-state-container-plan",
      safetyFlags,
      notes: [
        "Passive manual renderer initialization shell closeout report only.",
        "This closeout helper does not invoke the initialization shell.",
        "Renderer initialization and related runtime behavior remain blocked."
      ]
    };
  }

  return {
    ok: true,
    phase: 398,
    name: "custom-25d-visual-manual-renderer-initialization-shell-closeout",
    helperName:
      "getCustom25DVisualManualRendererInitializationShellCloseoutReport",
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase397InitializationShellClosedOut: true,
    initializationShellAvailable: initializationShellHelperAvailable,
    initializationShellHelperAvailable,
    initializationReportHelperAvailable,
    initializationShellAddedInPhase397: true,
    initializationShellAddedThisPhase: false,
    initializationShellInvokedByCloseout: false,
    initializationPerformedByCloseout: false,
    initializationShellOk: initializationShellHelperAvailable,
    noOptionAllowed: false,
    noOptionBlocked: true,
    approvalLikeOptionsStillBlocked: true,
    stateContainerExists,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    rendererCreated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    drawingStarted: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    initializationShellOnly: true,
    blockedBehavior,
    preservedSystems,
    nextStep: "manual-renderer-state-container-plan",
    safetyFlags,
    notes: [
      "Passive manual renderer initialization shell closeout report only.",
      "This closeout helper does not invoke the initialization shell.",
      "Renderer initialization and related runtime behavior remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererInitializationContractReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "readinessOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationShellReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellReport === "function";
  const initializationShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
    "function";
  const inertStateCreationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const inertStateFieldsReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInertStateFieldsReport === "function";
  const inertLifecycleFieldsReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 399,
      helperName:
        "getCustom25DVisualManualRendererInitializationContractReadinessReport",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys
    };
  }

  return {
    ok: true,
    phase: 399,
    helperName:
      "getCustom25DVisualManualRendererInitializationContractReadinessReport",
    planningOnly: true,
    readinessOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationContractReadinessOnly: true,
    initializationShellHelperAvailable,
    initializationShellReportHelperAvailable,
    initializationShellCloseoutHelperAvailable,
    inertStateCreationHelperAvailable,
    inertStateFieldsReportHelperAvailable,
    inertLifecycleFieldsReportHelperAvailable,
    stateContainerExists,
    observedLifecycleStatus:
      isObjectState &&
      stateObject.lifecycle &&
      typeof stateObject.lifecycle.status === "string"
        ? stateObject.lifecycle.status
        : null,
    observedLifecycleCreatedFlag:
      isObjectState &&
      stateObject.lifecycle &&
      stateObject.lifecycle.flags &&
      stateObject.lifecycle.flags.created === true,
    observedLifecycleInitializedFlag:
      isObjectState &&
      stateObject.lifecycle &&
      stateObject.lifecycle.flags &&
      stateObject.lifecycle.flags.initialized === true,
    observedLifecycleRunningFlag:
      isObjectState &&
      stateObject.lifecycle &&
      stateObject.lifecycle.flags &&
      stateObject.lifecycle.flags.running === true,
    plannedInitializationContractName:
      "custom25DVisualManualRendererInitializationContract",
    plannedInitializationEntryPoint:
      "initializeCustom25DVisualManualRendererShell",
    futureContractIsPlanningOnly: true,
    futureContractAddedThisPhase: false,
    initializationShellInvokedByReadiness: false,
    initializationPerformedByReadiness: false,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    lifecycleMutatedByReadiness: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    nextRecommendedPhase: "manual-renderer-initialization-contract-shell"
  };
}

function getCustom25DVisualManualRendererInitializationContractShell(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "contractShellOnly",
    "allowManualRendererInitializationContractShell",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );

  if (!windowExists) {
    return {
      ok: false,
      phase: 400,
      helperName: "getCustom25DVisualManualRendererInitializationContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      jsonSafeResult: true
    };
  }

  if (!isLocalDevHost) {
    return {
      ok: false,
      phase: 400,
      helperName: "getCustom25DVisualManualRendererInitializationContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      jsonSafeResult: true
    };
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 400,
      helperName: "getCustom25DVisualManualRendererInitializationContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 400,
    helperName: "getCustom25DVisualManualRendererInitializationContractShell",
    contractShellOnly: true,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationContractShellCreated: true,
    contractName: "custom25DVisualManualRendererInitializationContract",
    plannedEntryPoint: "initializeCustom25DVisualManualRendererShell",
    plannedStateContainerName: "custom25DVisualManualRendererState",
    plannedRequiredLifecycleStatus: "created",
    plannedNextLifecycleStatusAfterFutureInitialization: "initialized",
    futureContractAddedThisPhase: true,
    futureInitializationStillBlocked: true,
    initializationShellInvokedByContractShell: false,
    initializationPerformedByContractShell: false,
    stateContainerExists,
    stateCreatedByContractShell: false,
    stateMutatedByContractShell: false,
    lifecycleMutatedByContractShell: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationContractShellReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractReadinessReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 400,
      helperName:
        "getCustom25DVisualManualRendererInitializationContractShellReport",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 400,
    helperName:
      "getCustom25DVisualManualRendererInitializationContractShellReport",
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationContractShellReportOnly: true,
    initializationContractShellHelperAvailable,
    initializationContractShellAddedThisPhase: true,
    initializationReadinessHelperAvailable,
    initializationShellHelperAvailable,
    initializationShellInvokedByReport: false,
    contractShellInvokedByReport: false,
    initializationPerformedByReport: false,
    stateContainerExists,
    stateCreatedByReport: false,
    stateMutatedByReport: false,
    lifecycleMutatedByReport: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationContractShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "closeoutOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationContractShellReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellReport ===
    "function";
  const initializationContractReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractReadinessReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 401,
      helperName:
        "getCustom25DVisualManualRendererInitializationContractShellCloseoutReport",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 401,
    helperName:
      "getCustom25DVisualManualRendererInitializationContractShellCloseoutReport",
    closeoutOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase400InitializationContractShellClosedOut: true,
    initializationContractShellHelperAvailable,
    initializationContractShellReportHelperAvailable,
    initializationContractReadinessHelperAvailable,
    initializationShellHelperAvailable,
    initializationShellCloseoutHelperAvailable,
    initializationContractShellAddedInPhase400: true,
    contractShellInvokedByCloseout: false,
    initializationShellInvokedByCloseout: false,
    initializationPerformedByCloseout: false,
    stateContainerExists,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    lifecycleMutatedByCloseout: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked",
    nextRecommendedPhase: "manual-renderer-initialization-guard-readiness"
  };
}

function getCustom25DVisualManualRendererInitializationGuardReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "readinessOnly",
    "reportOnly",
    "confirmNoGuardCreation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainer =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : null;
  const stateContainerExists = !!stateContainer;
  const lifecycle =
    stateContainer && typeof stateContainer.lifecycle === "object"
      ? stateContainer.lifecycle
      : null;
  const lifecycleFlags =
    lifecycle && typeof lifecycle.flags === "object" ? lifecycle.flags : null;
  const observedLifecycleStatus =
    lifecycle && typeof lifecycle.status === "string" ? lifecycle.status : null;
  const observedLifecycleCreatedFlag =
    !!(lifecycleFlags && lifecycleFlags.created === true);
  const observedLifecycleInitializedFlag =
    !!(lifecycleFlags && lifecycleFlags.initialized === true);
  const observedLifecycleRunningFlag =
    !!(lifecycleFlags && lifecycleFlags.running === true);
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationContractShellReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellReport ===
    "function";
  const initializationContractShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
    "function";
  const initializationContractReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractReadinessReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 402,
      helperName:
        "getCustom25DVisualManualRendererInitializationGuardReadinessReport",
      blocked: true,
      allowed: false,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 402,
    helperName:
      "getCustom25DVisualManualRendererInitializationGuardReadinessReport",
    planningOnly: true,
    readinessOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationGuardReadinessOnly: true,
    initializationGuardAddedThisPhase: false,
    realGuardFunctionCreated: false,
    plannedInitializationGuardName:
      "canInitializeCustom25DVisualManualRenderer",
    plannedContractName:
      "custom25DVisualManualRendererInitializationContract",
    plannedEntryPoint: "initializeCustom25DVisualManualRendererShell",
    plannedRequiredStateContainerName: "custom25DVisualManualRendererState",
    plannedRequiredLifecycleStatus: "created",
    plannedBlockedDefault: true,
    plannedAllowsRendererCreation: false,
    plannedAllowsRendererStart: false,
    initializationContractShellHelperAvailable,
    initializationContractShellReportHelperAvailable,
    initializationContractShellCloseoutHelperAvailable,
    initializationContractReadinessHelperAvailable,
    initializationShellHelperAvailable,
    initializationShellCloseoutHelperAvailable,
    stateContainerExists,
    observedLifecycleStatus,
    observedLifecycleCreatedFlag,
    observedLifecycleInitializedFlag,
    observedLifecycleRunningFlag,
    contractShellInvokedByReadiness: false,
    initializationShellInvokedByReadiness: false,
    initializationPerformedByReadiness: false,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    lifecycleMutatedByReadiness: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    initializationAuthorizesRendererCreation: false,
    initializationAuthorizesRendererStart: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked",
    nextRecommendedPhase: "manual-renderer-initialization-guard-shell"
  };
}

function canInitializeCustom25DVisualManualRenderer(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "guardShellOnly",
    "allowManualRendererInitializationGuardShell",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainer =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : null;
  const stateContainerExists = !!stateContainer;
  const lifecycle =
    stateContainer && typeof stateContainer.lifecycle === "object"
      ? stateContainer.lifecycle
      : null;
  const lifecycleFlags =
    lifecycle && typeof lifecycle.flags === "object" ? lifecycle.flags : null;
  const observedLifecycleStatus =
    lifecycle && typeof lifecycle.status === "string" ? lifecycle.status : null;
  const observedLifecycleCreatedFlag =
    !!(lifecycleFlags && lifecycleFlags.created === true);
  const observedLifecycleInitializedFlag =
    !!(lifecycleFlags && lifecycleFlags.initialized === true);
  const observedLifecycleRunningFlag =
    !!(lifecycleFlags && lifecycleFlags.running === true);

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 403,
      helperName: "canInitializeCustom25DVisualManualRenderer",
      blocked: true,
      allowed: false,
      decision: "blocked",
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 403,
    helperName: "canInitializeCustom25DVisualManualRenderer",
    guardShellOnly: true,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    decision: "blocked",
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationGuardCreated: true,
    initializationGuardAddedThisPhase: true,
    guardEvaluated: true,
    guardAllowsInitialization: false,
    guardAllowsRendererCreation: false,
    guardAllowsRendererStart: false,
    guardAllowsMapAttachment: false,
    guardAllowsDrawing: false,
    contractName: "custom25DVisualManualRendererInitializationContract",
    plannedEntryPoint: "initializeCustom25DVisualManualRendererShell",
    requiredStateContainerName: "custom25DVisualManualRendererState",
    requiredLifecycleStatus: "created",
    stateContainerExists,
    observedLifecycleStatus,
    observedLifecycleCreatedFlag,
    observedLifecycleInitializedFlag,
    observedLifecycleRunningFlag,
    blockingReason: "initialization-not-authorized-yet",
    initializationShellInvokedByGuard: false,
    contractShellInvokedByGuard: false,
    initializationPerformedByGuard: false,
    stateCreatedByGuard: false,
    stateMutatedByGuard: false,
    lifecycleMutatedByGuard: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationGuardShellReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationGuardReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGuardReadinessReport ===
    "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationContractShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 403,
      helperName:
        "getCustom25DVisualManualRendererInitializationGuardShellReport",
      blocked: true,
      allowed: false,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 403,
    helperName:
      "getCustom25DVisualManualRendererInitializationGuardShellReport",
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationGuardShellReportOnly: true,
    initializationGuardHelperAvailable,
    initializationGuardAddedThisPhase: true,
    initializationGuardReadinessHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationContractShellCloseoutHelperAvailable,
    initializationShellHelperAvailable,
    guardInvokedByReport: false,
    initializationShellInvokedByReport: false,
    contractShellInvokedByReport: false,
    initializationPerformedByReport: false,
    stateContainerExists,
    stateCreatedByReport: false,
    stateMutatedByReport: false,
    lifecycleMutatedByReport: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    guardAllowsInitialization: false,
    guardAllowsRendererCreation: false,
    guardAllowsRendererStart: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "closeoutOnly",
    "reportOnly",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationGuardShellReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGuardShellReport ===
    "function";
  const initializationGuardReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGuardReadinessReport ===
    "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationContractShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 404,
      helperName:
        "getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport",
      blocked: true,
      allowed: false,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 404,
    helperName:
      "getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport",
    closeoutOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase403InitializationGuardShellClosedOut: true,
    initializationGuardHelperAvailable,
    initializationGuardShellReportHelperAvailable,
    initializationGuardReadinessHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationContractShellCloseoutHelperAvailable,
    initializationShellHelperAvailable,
    initializationShellCloseoutHelperAvailable,
    initializationGuardAddedInPhase403: true,
    guardInvokedByCloseout: false,
    contractShellInvokedByCloseout: false,
    initializationShellInvokedByCloseout: false,
    initializationPerformedByCloseout: false,
    stateContainerExists,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    lifecycleMutatedByCloseout: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    guardAllowsInitialization: false,
    guardAllowsRendererCreation: false,
    guardAllowsRendererStart: false,
    guardAllowsMapAttachment: false,
    guardAllowsDrawing: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked",
    nextRecommendedPhase:
      "manual-renderer-initialization-preflight-readiness"
  };
}

function getCustom25DVisualManualRendererInitializationPreflightReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "readinessOnly",
    "reportOnly",
    "confirmNoPreflightCreation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainer =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : null;
  const stateContainerExists = !!stateContainer;
  const lifecycle =
    stateContainer && typeof stateContainer.lifecycle === "object"
      ? stateContainer.lifecycle
      : null;
  const lifecycleFlags =
    lifecycle && typeof lifecycle.flags === "object" ? lifecycle.flags : null;
  const observedLifecycleStatus =
    lifecycle && typeof lifecycle.status === "string" ? lifecycle.status : null;
  const observedLifecycleCreatedFlag =
    !!(lifecycleFlags && lifecycleFlags.created === true);
  const observedLifecycleInitializedFlag =
    !!(lifecycleFlags && lifecycleFlags.initialized === true);
  const observedLifecycleRunningFlag =
    !!(lifecycleFlags && lifecycleFlags.running === true);
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationGuardShellReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGuardShellReport ===
    "function";
  const initializationGuardShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport ===
    "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationContractShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 405,
      helperName:
        "getCustom25DVisualManualRendererInitializationPreflightReadinessReport",
      blocked: true,
      allowed: false,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 405,
    helperName:
      "getCustom25DVisualManualRendererInitializationPreflightReadinessReport",
    planningOnly: true,
    readinessOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationPreflightReadinessOnly: true,
    initializationPreflightAddedThisPhase: false,
    realPreflightFunctionCreated: false,
    plannedInitializationPreflightName:
      "getCustom25DVisualManualRendererInitializationPreflightReport",
    plannedGuardName: "canInitializeCustom25DVisualManualRenderer",
    plannedContractName:
      "custom25DVisualManualRendererInitializationContract",
    plannedEntryPoint: "initializeCustom25DVisualManualRendererShell",
    plannedRequiredStateContainerName: "custom25DVisualManualRendererState",
    plannedRequiredLifecycleStatus: "created",
    plannedPreflightDefaultDecision: "blocked",
    plannedPreflightAllowsInitialization: false,
    plannedPreflightAllowsRendererCreation: false,
    plannedPreflightAllowsRendererStart: false,
    initializationGuardHelperAvailable,
    initializationGuardShellReportHelperAvailable,
    initializationGuardShellCloseoutHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationContractShellCloseoutHelperAvailable,
    initializationShellHelperAvailable,
    initializationShellCloseoutHelperAvailable,
    stateContainerExists,
    observedLifecycleStatus,
    observedLifecycleCreatedFlag,
    observedLifecycleInitializedFlag,
    observedLifecycleRunningFlag,
    guardInvokedByReadiness: false,
    contractShellInvokedByReadiness: false,
    initializationShellInvokedByReadiness: false,
    initializationPerformedByReadiness: false,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    lifecycleMutatedByReadiness: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    guardAllowsInitialization: false,
    guardAllowsRendererCreation: false,
    guardAllowsRendererStart: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked",
    nextRecommendedPhase: "manual-renderer-initialization-preflight-shell"
  };
}

function getCustom25DVisualManualRendererInitializationPreflightReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "preflightShellOnly",
    "allowManualRendererInitializationPreflightShell",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainer =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : null;
  const stateContainerExists = !!stateContainer;
  const lifecycle =
    stateContainer && typeof stateContainer.lifecycle === "object"
      ? stateContainer.lifecycle
      : null;
  const lifecycleFlags =
    lifecycle && typeof lifecycle.flags === "object" ? lifecycle.flags : null;
  const observedLifecycleStatus =
    lifecycle && typeof lifecycle.status === "string" ? lifecycle.status : null;
  const observedLifecycleCreatedFlag =
    !!(lifecycleFlags && lifecycleFlags.created === true);
  const observedLifecycleInitializedFlag =
    !!(lifecycleFlags && lifecycleFlags.initialized === true);
  const observedLifecycleRunningFlag =
    !!(lifecycleFlags && lifecycleFlags.running === true);

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 406,
      helperName:
        "getCustom25DVisualManualRendererInitializationPreflightReport",
      blocked: true,
      allowed: false,
      decision: "blocked",
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 406,
    helperName:
      "getCustom25DVisualManualRendererInitializationPreflightReport",
    preflightShellOnly: true,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    decision: "blocked",
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationPreflightCreated: true,
    initializationPreflightAddedThisPhase: true,
    preflightEvaluated: true,
    preflightAllowsInitialization: false,
    preflightAllowsRendererCreation: false,
    preflightAllowsRendererStart: false,
    preflightAllowsMapAttachment: false,
    preflightAllowsDrawing: false,
    plannedGuardName: "canInitializeCustom25DVisualManualRenderer",
    plannedContractName:
      "custom25DVisualManualRendererInitializationContract",
    plannedEntryPoint: "initializeCustom25DVisualManualRendererShell",
    requiredStateContainerName: "custom25DVisualManualRendererState",
    requiredLifecycleStatus: "created",
    stateContainerExists,
    observedLifecycleStatus,
    observedLifecycleCreatedFlag,
    observedLifecycleInitializedFlag,
    observedLifecycleRunningFlag,
    blockingReason: "preflight-initialization-not-authorized-yet",
    guardInvokedByPreflight: false,
    contractShellInvokedByPreflight: false,
    initializationShellInvokedByPreflight: false,
    initializationPerformedByPreflight: false,
    stateCreatedByPreflight: false,
    stateMutatedByPreflight: false,
    lifecycleMutatedByPreflight: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationPreflightShellReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "confirmNoPreflightInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationPreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const initializationPreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationGuardShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport ===
    "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationContractShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 406,
      helperName:
        "getCustom25DVisualManualRendererInitializationPreflightShellReport",
      blocked: true,
      allowed: false,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 406,
    helperName:
      "getCustom25DVisualManualRendererInitializationPreflightShellReport",
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationPreflightShellReportOnly: true,
    initializationPreflightHelperAvailable,
    initializationPreflightAddedThisPhase: true,
    initializationPreflightReadinessHelperAvailable,
    initializationGuardHelperAvailable,
    initializationGuardShellCloseoutHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationContractShellCloseoutHelperAvailable,
    initializationShellHelperAvailable,
    preflightInvokedByReport: false,
    guardInvokedByReport: false,
    contractShellInvokedByReport: false,
    initializationShellInvokedByReport: false,
    initializationPerformedByReport: false,
    stateContainerExists,
    stateCreatedByReport: false,
    stateMutatedByReport: false,
    lifecycleMutatedByReport: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    preflightAllowsInitialization: false,
    preflightAllowsRendererCreation: false,
    preflightAllowsRendererStart: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationPreflightCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoPreflightInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightShellReportSafe =
    phase406PreflightShellHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightReadinessHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 407,
      helperName:
        "getCustom25DVisualManualRendererInitializationPreflightCloseoutReport",
      blocked: true,
      allowed: false,
      closeoutOnly: true,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 407,
    helperName:
      "getCustom25DVisualManualRendererInitializationPreflightCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightShellHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightShellReportSafe,
    initializationPreflightReadinessHelperAvailable:
      phase406PreflightReadinessHelperAvailable,
    preflightInvokedByCloseout: false,
    guardInvokedByCloseout: false,
    contractShellInvokedByCloseout: false,
    initializationShellInvokedByCloseout: false,
    initializationPerformedByCloseout: false,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    lifecycleMutatedByCloseout: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationGateReviewReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "gateReviewOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 408,
      helperName:
        "getCustom25DVisualManualRendererInitializationGateReviewReport",
      blocked: true,
      allowed: false,
      gateReviewOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 408,
    helperName:
      "getCustom25DVisualManualRendererInitializationGateReviewReport",
    reportOnly: true,
    passive: true,
    gateReviewOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    initializationGuardHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationShellHelperAvailable,
    preflightInvokedByGateReview: false,
    closeoutInvokedByGateReview: false,
    guardInvokedByGateReview: false,
    contractShellInvokedByGateReview: false,
    initializationShellInvokedByGateReview: false,
    initializationPerformedByGateReview: false,
    stateCreatedByGateReview: false,
    stateMutatedByGateReview: false,
    lifecycleMutatedByGateReview: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    initializationGateReadyForFutureManualReview:
      phase406PreflightReadinessHelperAvailable &&
      phase406PreflightHelperAvailable &&
      phase406PreflightShellHelperAvailable &&
      phase407PreflightCloseoutHelperAvailable,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "authorizationChecklistOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 409,
      helperName:
        "getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport",
      blocked: true,
      allowed: false,
      authorizationChecklistOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 409,
    helperName:
      "getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport",
    reportOnly: true,
    passive: true,
    authorizationChecklistOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    initializationGuardHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationShellHelperAvailable,
    requiredFutureManualInitializationPrerequisites: [
      "stateContainerMustExistFromApprovedFuturePhase",
      "rendererContractMustBeReviewed",
      "manualPreflightMustPassInFuturePhase",
      "guardMustAllowFutureInitialization",
      "mapAttachmentMustRemainSeparate",
      "drawingMustRemainSeparate",
      "startupWiringMustRemainSeparate"
    ],
    currentAuthorizationStatus: "not-authorized",
    futureInitializationMayBeConsideredLater:
      phase406PreflightReadinessHelperAvailable &&
      phase406PreflightHelperAvailable &&
      phase406PreflightShellHelperAvailable &&
      phase407PreflightCloseoutHelperAvailable &&
      phase408InitializationGateReviewHelperAvailable,
    futureInitializationAuthorizedNow: false,
    preflightInvokedByAuthorizationChecklist: false,
    closeoutInvokedByAuthorizationChecklist: false,
    gateReviewInvokedByAuthorizationChecklist: false,
    guardInvokedByAuthorizationChecklist: false,
    contractShellInvokedByAuthorizationChecklist: false,
    initializationShellInvokedByAuthorizationChecklist: false,
    initializationPerformedByAuthorizationChecklist: false,
    stateCreatedByAuthorizationChecklist: false,
    stateMutatedByAuthorizationChecklist: false,
    lifecycleMutatedByAuthorizationChecklist: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    futureInitializationStillBlocked: true,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationLockReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "lockReportOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 410,
      helperName: "getCustom25DVisualManualRendererInitializationLockReport",
      blocked: true,
      allowed: false,
      lockReportOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 410,
    helperName: "getCustom25DVisualManualRendererInitializationLockReport",
    reportOnly: true,
    passive: true,
    lockReportOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    initializationGuardHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationShellHelperAvailable,
    currentInitializationLockStatus: "locked",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    initializationLockReadyForFutureReview:
      phase406PreflightReadinessHelperAvailable &&
      phase406PreflightHelperAvailable &&
      phase406PreflightShellHelperAvailable &&
      phase407PreflightCloseoutHelperAvailable &&
      phase408InitializationGateReviewHelperAvailable &&
      phase409AuthorizationChecklistHelperAvailable,
    preflightInvokedByLockReport: false,
    closeoutInvokedByLockReport: false,
    gateReviewInvokedByLockReport: false,
    authorizationChecklistInvokedByLockReport: false,
    guardInvokedByLockReport: false,
    contractShellInvokedByLockReport: false,
    initializationShellInvokedByLockReport: false,
    initializationPerformedByLockReport: false,
    stateCreatedByLockReport: false,
    stateMutatedByLockReport: false,
    lifecycleMutatedByLockReport: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationSequenceSummaryReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "sequenceSummaryOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const initializationGuardHelperAvailable =
    typeof canInitializeCustom25DVisualManualRenderer === "function";
  const initializationContractShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationContractShell ===
    "function";
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";
  const initializationSafetySequenceComplete =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 411,
      helperName:
        "getCustom25DVisualManualRendererInitializationSequenceSummaryReport",
      blocked: true,
      allowed: false,
      sequenceSummaryOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 411,
    helperName:
      "getCustom25DVisualManualRendererInitializationSequenceSummaryReport",
    reportOnly: true,
    passive: true,
    sequenceSummaryOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase410InitializationLockHelperAvailable,
    initializationGuardHelperAvailable,
    initializationContractShellHelperAvailable,
    initializationShellHelperAvailable,
    initializationSafetySequenceComplete,
    currentInitializationSequenceStatus: initializationSafetySequenceComplete
      ? "complete-but-locked"
      : "incomplete-and-locked",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    sequenceReadyForFutureManualReview: initializationSafetySequenceComplete,
    preflightInvokedBySequenceSummary: false,
    closeoutInvokedBySequenceSummary: false,
    gateReviewInvokedBySequenceSummary: false,
    authorizationChecklistInvokedBySequenceSummary: false,
    lockReportInvokedBySequenceSummary: false,
    guardInvokedBySequenceSummary: false,
    contractShellInvokedBySequenceSummary: false,
    initializationShellInvokedBySequenceSummary: false,
    initializationPerformedBySequenceSummary: false,
    stateCreatedBySequenceSummary: false,
    stateMutatedBySequenceSummary: false,
    lifecycleMutatedBySequenceSummary: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationSequenceCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "sequenceCloseoutOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const phase411SequenceSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
    "function";
  const initializationSafetySequenceCloseoutComplete =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable &&
    phase411SequenceSummaryHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 412,
      helperName:
        "getCustom25DVisualManualRendererInitializationSequenceCloseoutReport",
      blocked: true,
      allowed: false,
      sequenceCloseoutOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 412,
    helperName:
      "getCustom25DVisualManualRendererInitializationSequenceCloseoutReport",
    reportOnly: true,
    passive: true,
    sequenceCloseoutOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase410InitializationLockHelperAvailable,
    phase411SequenceSummaryHelperAvailable,
    initializationSafetySequenceCloseoutComplete,
    currentInitializationCloseoutStatus:
      initializationSafetySequenceCloseoutComplete
        ? "closed-out-and-locked"
        : "incomplete-and-locked",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    sequenceCloseoutReadyForFutureManualReview:
      initializationSafetySequenceCloseoutComplete,
    preflightInvokedBySequenceCloseout: false,
    closeoutInvokedBySequenceCloseout: false,
    gateReviewInvokedBySequenceCloseout: false,
    authorizationChecklistInvokedBySequenceCloseout: false,
    lockReportInvokedBySequenceCloseout: false,
    sequenceSummaryInvokedBySequenceCloseout: false,
    guardInvokedBySequenceCloseout: false,
    contractShellInvokedBySequenceCloseout: false,
    initializationShellInvokedBySequenceCloseout: false,
    initializationPerformedBySequenceCloseout: false,
    stateCreatedBySequenceCloseout: false,
    stateMutatedBySequenceCloseout: false,
    lifecycleMutatedBySequenceCloseout: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationHandoffPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "handoffPlanOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const phase411SequenceSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
    "function";
  const phase412SequenceCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
    "function";
  const initializationSafetyChainClosedOut =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable &&
    phase411SequenceSummaryHelperAvailable &&
    phase412SequenceCloseoutHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 413,
      helperName:
        "getCustom25DVisualManualRendererInitializationHandoffPlanReport",
      blocked: true,
      allowed: false,
      handoffPlanOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 413,
    helperName:
      "getCustom25DVisualManualRendererInitializationHandoffPlanReport",
    reportOnly: true,
    passive: true,
    handoffPlanOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase410InitializationLockHelperAvailable,
    phase411SequenceSummaryHelperAvailable,
    phase412SequenceCloseoutHelperAvailable,
    initializationSafetyChainClosedOut,
    currentInitializationHandoffStatus: initializationSafetyChainClosedOut
      ? "ready-for-future-manual-planning-only"
      : "not-ready",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    futureManualInitializationMayBePlannedLater:
      initializationSafetyChainClosedOut,
    recommendedNextSafePhaseTypes: [
      "manualPlanningOnly",
      "contractReviewOnly",
      "stateContainerReviewOnly",
      "readinessReportOnly"
    ],
    disallowedNextPhaseTypes: [
      "automaticInitialization",
      "rendererStart",
      "mapAttachment",
      "drawing",
      "startupWiring",
      "gameplayChange",
      "backendStorageNetworkChange"
    ],
    preflightInvokedByHandoffPlan: false,
    closeoutInvokedByHandoffPlan: false,
    gateReviewInvokedByHandoffPlan: false,
    authorizationChecklistInvokedByHandoffPlan: false,
    lockReportInvokedByHandoffPlan: false,
    sequenceSummaryInvokedByHandoffPlan: false,
    sequenceCloseoutInvokedByHandoffPlan: false,
    guardInvokedByHandoffPlan: false,
    contractShellInvokedByHandoffPlan: false,
    initializationShellInvokedByHandoffPlan: false,
    initializationPerformedByHandoffPlan: false,
    stateCreatedByHandoffPlan: false,
    stateMutatedByHandoffPlan: false,
    lifecycleMutatedByHandoffPlan: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInitializationNextStepReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "nextStepReadinessOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const phase411SequenceSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
    "function";
  const phase412SequenceCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
    "function";
  const phase413HandoffPlanHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
    "function";
  const initializationSafetyChainClosedOut =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable &&
    phase411SequenceSummaryHelperAvailable &&
    phase412SequenceCloseoutHelperAvailable &&
    phase413HandoffPlanHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 414,
      helperName:
        "getCustom25DVisualManualRendererInitializationNextStepReadinessReport",
      blocked: true,
      allowed: false,
      nextStepReadinessOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 414,
    helperName:
      "getCustom25DVisualManualRendererInitializationNextStepReadinessReport",
    reportOnly: true,
    passive: true,
    nextStepReadinessOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase406PreflightReadinessHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase410InitializationLockHelperAvailable,
    phase411SequenceSummaryHelperAvailable,
    phase412SequenceCloseoutHelperAvailable,
    phase413HandoffPlanHelperAvailable,
    initializationSafetyChainClosedOut,
    currentNextStepReadinessStatus: initializationSafetyChainClosedOut
      ? "ready-for-passive-planning-only"
      : "not-ready",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    nextSafeWorkRecommendation: "manual-planning-or-contract-review-only",
    nextUnsafeWorkBlocked: true,
    allowedNextStepTypes: [
      "manualPlanningOnly",
      "contractReviewOnly",
      "stateContainerReviewOnly",
      "readinessReportOnly",
      "closeoutReportOnly"
    ],
    blockedNextStepTypes: [
      "stateCreation",
      "stateMutation",
      "rendererInstanceCreation",
      "rendererInitialization",
      "rendererStart",
      "mapAttachment",
      "drawing",
      "startupWiring",
      "gameplayChange",
      "backendStorageNetworkChange"
    ],
    preflightInvokedByNextStepReadiness: false,
    closeoutInvokedByNextStepReadiness: false,
    gateReviewInvokedByNextStepReadiness: false,
    authorizationChecklistInvokedByNextStepReadiness: false,
    lockReportInvokedByNextStepReadiness: false,
    sequenceSummaryInvokedByNextStepReadiness: false,
    sequenceCloseoutInvokedByNextStepReadiness: false,
    handoffPlanInvokedByNextStepReadiness: false,
    guardInvokedByNextStepReadiness: false,
    contractShellInvokedByNextStepReadiness: false,
    initializationShellInvokedByNextStepReadiness: false,
    initializationPerformedByNextStepReadiness: false,
    stateCreatedByNextStepReadiness: false,
    stateMutatedByNextStepReadiness: false,
    lifecycleMutatedByNextStepReadiness: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererPlanningBoundaryReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planningBoundaryOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase414NextStepReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationNextStepReadinessReport ===
    "function";
  const phase413HandoffPlanHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
    "function";
  const phase412SequenceCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
    "function";
  const phase411SequenceSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const initializationSafetyChainClosedOut =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable &&
    phase411SequenceSummaryHelperAvailable &&
    phase412SequenceCloseoutHelperAvailable &&
    phase413HandoffPlanHelperAvailable &&
    phase414NextStepReadinessHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 415,
      helperName: "getCustom25DVisualManualRendererPlanningBoundaryReport",
      blocked: true,
      allowed: false,
      planningBoundaryOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 415,
    helperName: "getCustom25DVisualManualRendererPlanningBoundaryReport",
    reportOnly: true,
    passive: true,
    planningBoundaryOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase414NextStepReadinessHelperAvailable,
    phase413HandoffPlanHelperAvailable,
    phase412SequenceCloseoutHelperAvailable,
    phase411SequenceSummaryHelperAvailable,
    phase410InitializationLockHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightReadinessHelperAvailable,
    initializationSafetyChainClosedOut,
    currentPlanningBoundaryStatus: initializationSafetyChainClosedOut
      ? "planning-only-boundary-active"
      : "not-ready",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    planningOnlyWorkAllowed: initializationSafetyChainClosedOut,
    implementationWorkBlocked: true,
    allowedPlanningWorkTypes: [
      "manualPlanningOnly",
      "contractReviewOnly",
      "stateContainerReviewOnly",
      "readinessReportOnly",
      "closeoutReportOnly",
      "riskReviewOnly"
    ],
    blockedImplementationWorkTypes: [
      "stateCreation",
      "stateMutation",
      "rendererInstanceCreation",
      "rendererInitialization",
      "rendererStart",
      "mapAttachment",
      "drawing",
      "startupWiring",
      "gameplayChange",
      "backendStorageNetworkChange"
    ],
    preflightInvokedByPlanningBoundary: false,
    closeoutInvokedByPlanningBoundary: false,
    gateReviewInvokedByPlanningBoundary: false,
    authorizationChecklistInvokedByPlanningBoundary: false,
    lockReportInvokedByPlanningBoundary: false,
    sequenceSummaryInvokedByPlanningBoundary: false,
    sequenceCloseoutInvokedByPlanningBoundary: false,
    handoffPlanInvokedByPlanningBoundary: false,
    nextStepReadinessInvokedByPlanningBoundary: false,
    guardInvokedByPlanningBoundary: false,
    contractShellInvokedByPlanningBoundary: false,
    initializationShellInvokedByPlanningBoundary: false,
    initializationPerformedByPlanningBoundary: false,
    stateCreatedByPlanningBoundary: false,
    stateMutatedByPlanningBoundary: false,
    lifecycleMutatedByPlanningBoundary: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererPlanningRiskReviewReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "riskReviewOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase415PlanningBoundaryHelperAvailable =
    typeof getCustom25DVisualManualRendererPlanningBoundaryReport === "function";
  const phase414NextStepReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationNextStepReadinessReport ===
    "function";
  const phase413HandoffPlanHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
    "function";
  const phase412SequenceCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
    "function";
  const phase411SequenceSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const planningBoundaryActive =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable &&
    phase411SequenceSummaryHelperAvailable &&
    phase412SequenceCloseoutHelperAvailable &&
    phase413HandoffPlanHelperAvailable &&
    phase414NextStepReadinessHelperAvailable &&
    phase415PlanningBoundaryHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 416,
      helperName: "getCustom25DVisualManualRendererPlanningRiskReviewReport",
      blocked: true,
      allowed: false,
      riskReviewOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 416,
    helperName: "getCustom25DVisualManualRendererPlanningRiskReviewReport",
    reportOnly: true,
    passive: true,
    riskReviewOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase415PlanningBoundaryHelperAvailable,
    phase414NextStepReadinessHelperAvailable,
    phase413HandoffPlanHelperAvailable,
    phase412SequenceCloseoutHelperAvailable,
    phase411SequenceSummaryHelperAvailable,
    phase410InitializationLockHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightReadinessHelperAvailable,
    planningBoundaryActive,
    currentRiskReviewStatus: planningBoundaryActive
      ? "planning-risk-review-active"
      : "not-ready",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    planningOnlyWorkAllowed: planningBoundaryActive,
    implementationWorkBlocked: true,
    reviewedRiskCategories: [
      "stateLifecycleRisk",
      "rendererStartupRisk",
      "mapAttachmentRisk",
      "drawingRisk",
      "startupWiringRisk",
      "gameplayRegressionRisk",
      "backendStorageNetworkRisk"
    ],
    blockedRiskyWorkTypes: [
      "stateCreation",
      "stateMutation",
      "rendererInstanceCreation",
      "rendererInitialization",
      "rendererStart",
      "mapAttachment",
      "drawing",
      "startupWiring",
      "gameplayChange",
      "backendStorageNetworkChange"
    ],
    recommendedSafeFollowupTypes: [
      "manualPlanningOnly",
      "contractReviewOnly",
      "stateContainerReviewOnly",
      "readinessReportOnly",
      "closeoutReportOnly"
    ],
    preflightInvokedByRiskReview: false,
    closeoutInvokedByRiskReview: false,
    gateReviewInvokedByRiskReview: false,
    authorizationChecklistInvokedByRiskReview: false,
    lockReportInvokedByRiskReview: false,
    sequenceSummaryInvokedByRiskReview: false,
    sequenceCloseoutInvokedByRiskReview: false,
    handoffPlanInvokedByRiskReview: false,
    nextStepReadinessInvokedByRiskReview: false,
    planningBoundaryInvokedByRiskReview: false,
    guardInvokedByRiskReview: false,
    contractShellInvokedByRiskReview: false,
    initializationShellInvokedByRiskReview: false,
    initializationPerformedByRiskReview: false,
    stateCreatedByRiskReview: false,
    stateMutatedByRiskReview: false,
    lifecycleMutatedByRiskReview: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererPlanningRiskCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "riskCloseoutOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase416PlanningRiskReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererPlanningRiskReviewReport === "function";
  const phase415PlanningBoundaryHelperAvailable =
    typeof getCustom25DVisualManualRendererPlanningBoundaryReport === "function";
  const phase414NextStepReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationNextStepReadinessReport ===
    "function";
  const phase413HandoffPlanHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
    "function";
  const phase412SequenceCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
    "function";
  const phase411SequenceSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
    "function";
  const phase410InitializationLockHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationLockReport ===
    "function";
  const phase409AuthorizationChecklistHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
    "function";
  const phase408InitializationGateReviewHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
    "function";
  const phase407PreflightCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
    "function";
  const phase406PreflightShellHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
    "function";
  const phase406PreflightHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
    "function";
  const phase406PreflightReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
    "function";
  const planningRiskReviewClosedOut =
    phase406PreflightReadinessHelperAvailable &&
    phase406PreflightHelperAvailable &&
    phase406PreflightShellHelperAvailable &&
    phase407PreflightCloseoutHelperAvailable &&
    phase408InitializationGateReviewHelperAvailable &&
    phase409AuthorizationChecklistHelperAvailable &&
    phase410InitializationLockHelperAvailable &&
    phase411SequenceSummaryHelperAvailable &&
    phase412SequenceCloseoutHelperAvailable &&
    phase413HandoffPlanHelperAvailable &&
    phase414NextStepReadinessHelperAvailable &&
    phase415PlanningBoundaryHelperAvailable &&
    phase416PlanningRiskReviewHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 417,
      helperName: "getCustom25DVisualManualRendererPlanningRiskCloseoutReport",
      blocked: true,
      allowed: false,
      riskCloseoutOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 417,
    helperName: "getCustom25DVisualManualRendererPlanningRiskCloseoutReport",
    reportOnly: true,
    passive: true,
    riskCloseoutOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase416PlanningRiskReviewHelperAvailable,
    phase415PlanningBoundaryHelperAvailable,
    phase414NextStepReadinessHelperAvailable,
    phase413HandoffPlanHelperAvailable,
    phase412SequenceCloseoutHelperAvailable,
    phase411SequenceSummaryHelperAvailable,
    phase410InitializationLockHelperAvailable,
    phase409AuthorizationChecklistHelperAvailable,
    phase408InitializationGateReviewHelperAvailable,
    phase407PreflightCloseoutHelperAvailable,
    phase406PreflightShellHelperAvailable,
    phase406PreflightHelperAvailable,
    phase406PreflightReadinessHelperAvailable,
    planningRiskReviewClosedOut,
    currentPlanningRiskCloseoutStatus: planningRiskReviewClosedOut
      ? "planning-risk-closed-out"
      : "not-ready",
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    planningOnlyWorkAllowed: planningRiskReviewClosedOut,
    implementationWorkBlocked: true,
    recommendedSafeFollowupTypes: [
      "manualPlanningOnly",
      "contractReviewOnly",
      "stateContainerReviewOnly",
      "readinessReportOnly",
      "closeoutReportOnly"
    ],
    blockedImplementationWorkTypes: [
      "stateCreation",
      "stateMutation",
      "rendererInstanceCreation",
      "rendererInitialization",
      "rendererStart",
      "mapAttachment",
      "drawing",
      "startupWiring",
      "gameplayChange",
      "backendStorageNetworkChange"
    ],
    preflightInvokedByRiskCloseout: false,
    closeoutInvokedByRiskCloseout: false,
    gateReviewInvokedByRiskCloseout: false,
    authorizationChecklistInvokedByRiskCloseout: false,
    lockReportInvokedByRiskCloseout: false,
    sequenceSummaryInvokedByRiskCloseout: false,
    sequenceCloseoutInvokedByRiskCloseout: false,
    handoffPlanInvokedByRiskCloseout: false,
    nextStepReadinessInvokedByRiskCloseout: false,
    planningBoundaryInvokedByRiskCloseout: false,
    riskReviewInvokedByRiskCloseout: false,
    guardInvokedByRiskCloseout: false,
    contractShellInvokedByRiskCloseout: false,
    initializationShellInvokedByRiskCloseout: false,
    initializationPerformedByRiskCloseout: false,
    stateCreatedByRiskCloseout: false,
    stateMutatedByRiskCloseout: false,
    lifecycleMutatedByRiskCloseout: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererPlanningHandoffSummaryReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planningHandoffSummaryOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const planningRiskCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererPlanningRiskCloseoutReport ===
    "function";
  const planningRiskBoundaryClosedOut = planningRiskCloseoutHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 418,
      helperName:
        "getCustom25DVisualManualRendererPlanningHandoffSummaryReport",
      blocked: true,
      allowed: false,
      planningHandoffSummaryOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 418,
    helperName:
      "getCustom25DVisualManualRendererPlanningHandoffSummaryReport",
    reportOnly: true,
    passive: true,
    planningHandoffSummaryOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    planningRiskCloseoutHelperAvailable,
    planningRiskBoundaryClosedOut,
    planningHandoffSummaryStatus: "planning-handoff-summary-ready",
    currentPlanningRiskCloseoutStatus: "planning-risk-closed-out",
    futurePlanningOnlyWorkAllowed: true,
    futureImplementationWorkBlocked: true,
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateCreationReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "stateCreationReadinessOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const planningHandoffSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererPlanningHandoffSummaryReport ===
    "function";
  const planningRiskBoundaryClosedOut = planningHandoffSummaryHelperAvailable;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 419,
      helperName:
        "getCustom25DVisualManualRendererStateCreationReadinessReport",
      blocked: true,
      allowed: false,
      stateCreationReadinessOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 419,
    helperName:
      "getCustom25DVisualManualRendererStateCreationReadinessReport",
    reportOnly: true,
    passive: true,
    stateCreationReadinessOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    planningHandoffSummaryHelperAvailable,
    planningRiskBoundaryClosedOut,
    planningHandoffSummaryStatus: "planning-handoff-summary-ready",
    stateCreationReadinessStatus: "state-creation-readiness-report-ready",
    futureStateContainerCreationAllowedNow: false,
    futureStateContainerCreationStillRequiresSeparatePhase: true,
    futurePlanningOnlyWorkAllowed: true,
    futureImplementationWorkBlocked: true,
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "stateCreationReadinessOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateCreationReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererStateCreationReadinessReport ===
    "function";
  const planningHandoffSummaryHelperAvailable =
    typeof getCustom25DVisualManualRendererPlanningHandoffSummaryReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 419,
      helperName:
        "getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport",
      blocked: true,
      allowed: false,
      stateCreationReadinessCloseoutOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true
    };
  }

  return {
    ok: true,
    phase: 419,
    helperName:
      "getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    stateCreationReadinessCloseoutOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateCreationReadinessHelperAvailable,
    planningHandoffSummaryHelperAvailable,
    stateCreationReadinessStatus: "state-creation-readiness-report-ready",
    stateCreationReadinessCloseoutStatus:
      "state-creation-readiness-closed-out",
    futureStateContainerCreationAllowedNow: false,
    futureStateContainerCreationStillRequiresSeparatePhase: true,
    futurePlanningOnlyWorkAllowed: true,
    futureImplementationWorkBlocked: true,
    futureInitializationAuthorizedNow: false,
    futureInitializationStillBlocked: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}
function getCustom25DVisualManualRendererStateContainerPlanReport(options = {}) {
  const initializationShellCloseout =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererInitializationShellCloseoutReport(options)
      : null;

  return {
    phase: 281,
    name: "custom-25d-visual-manual-renderer-state-container-plan",
    ok: !!(initializationShellCloseout && initializationShellCloseout.ok === true),
    passive: true,
    reportOnly: true,
    planningOnly: true,
    initializationShellCloseoutAvailable:
      typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport === "function",
    initializationShellCloseoutOk:
      !!(initializationShellCloseout && initializationShellCloseout.ok === true),
    plannedStateContainerName: "custom25DVisualManualRendererState",
    stateContainerCreated: false,
    sharedStateMutated: false,
    plannedStateDefaults: {
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      layerCount: 0
    },
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "state-container-plan-only",
    blockedBehavior:
      initializationShellCloseout && initializationShellCloseout.blockedBehavior
        ? initializationShellCloseout.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          },
    preservedSystems:
      initializationShellCloseout && initializationShellCloseout.preservedSystems
        ? initializationShellCloseout.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          },
    nextStep: "manual-renderer-state-container-contract",
    safetyFlags:
      initializationShellCloseout && initializationShellCloseout.safetyFlags
        ? initializationShellCloseout.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          },
    notes: [
      "Passive manual renderer state container planning report only.",
      "Does not create custom25DVisualManualRendererState in this phase.",
      "Renderer creation, renderer start, and shared state mutation remain blocked until a future separately approved phase."
    ]
  };
}

function getCustom25DVisualManualRendererStateContainerContractReadinessReport(
  options = {}
) {
  const stateContainerPlan =
    typeof getCustom25DVisualManualRendererStateContainerPlanReport === "function"
      ? getCustom25DVisualManualRendererStateContainerPlanReport(options)
      : null;
  const initializationShellCloseout =
    typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererInitializationShellCloseoutReport(options)
      : null;

  return {
    phase: 282,
    name: "custom-25d-visual-manual-renderer-state-container-contract-readiness",
    ok:
      !!(stateContainerPlan && stateContainerPlan.ok === true) &&
      !!(initializationShellCloseout && initializationShellCloseout.ok === true),
    passive: true,
    reportOnly: true,
    contractOnly: true,
    readinessOnly: true,
    stateContainerPlanAvailable:
      typeof getCustom25DVisualManualRendererStateContainerPlanReport === "function",
    stateContainerPlanOk: !!(stateContainerPlan && stateContainerPlan.ok === true),
    initializationShellCloseoutAvailable:
      typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport === "function",
    initializationShellCloseoutOk:
      !!(initializationShellCloseout && initializationShellCloseout.ok === true),
    plannedStateContainerName: "custom25DVisualManualRendererState",
    stateContainerCreated: false,
    sharedStateMutated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    contractPlannedOnly: true,
    liveStateContainerPresent: false,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    approvedBehavior: "state-container-contract-readiness-report-only",
    blockedBehavior:
      stateContainerPlan && stateContainerPlan.blockedBehavior
        ? stateContainerPlan.blockedBehavior
        : initializationShellCloseout && initializationShellCloseout.blockedBehavior
          ? initializationShellCloseout.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      stateContainerPlan && stateContainerPlan.preservedSystems
        ? stateContainerPlan.preservedSystems
        : initializationShellCloseout && initializationShellCloseout.preservedSystems
          ? initializationShellCloseout.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    nextStep: "manual-renderer-state-container-shell",
    safetyFlags:
      stateContainerPlan && stateContainerPlan.safetyFlags
        ? stateContainerPlan.safetyFlags
        : initializationShellCloseout && initializationShellCloseout.safetyFlags
          ? initializationShellCloseout.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    notes: [
      "Passive manual renderer state container contract/readiness report only.",
      "Confirms the future state container remains planned and not live.",
      "Renderer creation, initialization, map attachment, drawing, and shared state mutation remain blocked in this phase."
    ]
  };
}

function createCustom25DVisualManualRendererStateContainerShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "stateContainerShellOnly",
    "allowManualRendererStateContainerCreation",
    "confirmStateCreationIsInert",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoStateCreationReadinessInvocation",
    "confirmNoGuardInvocation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateCreationReadinessCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport ===
    "function";
  const stateContainerName = "custom25DVisualManualRendererState";
  const windowExists = typeof window !== "undefined";
  const stateContainerExistedBefore =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 420,
      helperName: "createCustom25DVisualManualRendererStateContainerShell",
      manual: options.manual === true,
      localDevOnly: options.localDevOnly === true,
      browserConsoleOnly: options.browserConsoleOnly === true,
      stateContainerShellOnly: true,
      jsonSafeResult: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateCreationReadinessCloseoutHelperAvailable,
      stateContainerName,
      stateContainerExistedBefore,
      stateContainerCreated: false,
      stateContainerAlreadyExisted: stateContainerExistedBefore,
      stateContainerExistsAfter: stateContainerExistedBefore,
      lifecycleStatus: stateContainerExistedBefore ? "state-container-created" : null,
      inert: true,
      state: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (stateContainerExistedBefore) {
    const existingState = window.custom25DVisualManualRendererState;
    return {
      ok: true,
      phase: 420,
      helperName: "createCustom25DVisualManualRendererStateContainerShell",
      manual: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      stateContainerShellOnly: true,
      jsonSafeResult: true,
      blocked: false,
      allowed: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateCreationReadinessCloseoutHelperAvailable,
      stateContainerName,
      stateContainerExistedBefore: true,
      stateContainerCreated: false,
      stateContainerAlreadyExisted: true,
      stateContainerExistsAfter: true,
      lifecycleStatus:
        existingState && typeof existingState.lifecycleStatus === "string"
          ? existingState.lifecycleStatus
          : "state-container-created",
      inert: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const inertState = {
    createdByPhase: 420,
    stateContainerName: "custom25DVisualManualRendererState",
    stateContainerShellOnly: true,
    inert: true,
    initialized: false,
    rendererInstance: null,
    rendererCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    startupWired: false,
    layerCount: 0,
    layers: [],
    lifecycleStatus: "state-container-created",
    implementationAuthorized: false,
    initializationAuthorized: false,
    drawingAuthorized: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };

  if (windowExists) {
    window.custom25DVisualManualRendererState = inertState;
  }

  return {
    ok: true,
    phase: 420,
    helperName: "createCustom25DVisualManualRendererStateContainerShell",
    manual: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    stateContainerShellOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateCreationReadinessCloseoutHelperAvailable,
    stateContainerName,
    stateContainerExistedBefore: false,
    stateContainerCreated: windowExists,
    stateContainerAlreadyExisted: false,
    stateContainerExistsAfter:
      windowExists && typeof window.custom25DVisualManualRendererState !== "undefined",
    lifecycleStatus: inertState.lifecycleStatus,
    inert: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateContainerShellVerificationReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "stateContainerShellVerificationOnly",
    "stateContainerShellCloseoutOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoStateCreationReadinessInvocation",
    "confirmNoStateContainerCreationInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateContainerCreationHelperAvailable =
    typeof createCustom25DVisualManualRendererStateContainerShell === "function";
  const stateContainerName = "custom25DVisualManualRendererState";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const observedCreatedByPhase =
    stateContainer && typeof stateContainer.createdByPhase === "number"
      ? stateContainer.createdByPhase
      : null;
  const observedStateContainerShellOnly =
    !!(stateContainer && stateContainer.stateContainerShellOnly === true);
  const observedInert = !!(stateContainer && stateContainer.inert === true);
  const observedInitialized =
    stateContainer && typeof stateContainer.initialized === "boolean"
      ? stateContainer.initialized
      : null;
  const observedRendererInstanceIsNull =
    !!(stateContainer && stateContainer.rendererInstance === null);
  const observedRendererCreated =
    stateContainer && typeof stateContainer.rendererCreated === "boolean"
      ? stateContainer.rendererCreated
      : null;
  const observedRendererInitialized =
    stateContainer && typeof stateContainer.rendererInitialized === "boolean"
      ? stateContainer.rendererInitialized
      : null;
  const observedRendererStarted =
    stateContainer && typeof stateContainer.rendererStarted === "boolean"
      ? stateContainer.rendererStarted
      : null;
  const observedMapAttached =
    stateContainer && typeof stateContainer.mapAttached === "boolean"
      ? stateContainer.mapAttached
      : null;
  const observedDrawingEnabled =
    stateContainer && typeof stateContainer.drawingEnabled === "boolean"
      ? stateContainer.drawingEnabled
      : null;
  const observedStartupWired =
    stateContainer && typeof stateContainer.startupWired === "boolean"
      ? stateContainer.startupWired
      : null;
  const observedLayerCount =
    stateContainer && typeof stateContainer.layerCount === "number"
      ? stateContainer.layerCount
      : null;
  const observedLayersArray =
    stateContainer ? Array.isArray(stateContainer.layers) : false;
  const observedLifecycleStatus =
    stateContainer && typeof stateContainer.lifecycleStatus === "string"
      ? stateContainer.lifecycleStatus
      : null;
  const observedImplementationAuthorized =
    stateContainer && typeof stateContainer.implementationAuthorized === "boolean"
      ? stateContainer.implementationAuthorized
      : null;
  const observedInitializationAuthorized =
    stateContainer && typeof stateContainer.initializationAuthorized === "boolean"
      ? stateContainer.initializationAuthorized
      : null;
  const observedDrawingAuthorized =
    stateContainer && typeof stateContainer.drawingAuthorized === "boolean"
      ? stateContainer.drawingAuthorized
      : null;
  const observedGameplayChanged =
    stateContainer && typeof stateContainer.gameplayChanged === "boolean"
      ? stateContainer.gameplayChanged
      : null;
  const observedBackendStorageNetworkChanged =
    stateContainer &&
    typeof stateContainer.backendStorageNetworkChanged === "boolean"
      ? stateContainer.backendStorageNetworkChanged
      : null;
  const stateContainerShellVerified =
    stateContainerExists &&
    observedCreatedByPhase === 420 &&
    !!(stateContainer && stateContainer.stateContainerName === stateContainerName) &&
    observedStateContainerShellOnly === true &&
    observedInert === true &&
    observedInitialized === false &&
    observedRendererInstanceIsNull === true &&
    observedRendererCreated === false &&
    observedRendererInitialized === false &&
    observedRendererStarted === false &&
    observedMapAttached === false &&
    observedDrawingEnabled === false &&
    observedStartupWired === false &&
    observedLayerCount === 0 &&
    observedLayersArray === true &&
    observedLifecycleStatus === "state-container-created" &&
    observedImplementationAuthorized === false &&
    observedInitializationAuthorized === false &&
    observedDrawingAuthorized === false &&
    observedGameplayChanged === false &&
    observedBackendStorageNetworkChanged === false;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 421,
      helperName:
        "getCustom25DVisualManualRendererStateContainerShellVerificationReport",
      reportOnly: true,
      passive: true,
      stateContainerShellVerificationOnly: true,
      jsonSafeResult: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerCreationHelperAvailable,
      stateContainerName,
      stateContainerExists,
      observedCreatedByPhase,
      observedStateContainerShellOnly,
      observedInert,
      observedInitialized,
      observedRendererInstanceIsNull,
      observedRendererCreated,
      observedRendererInitialized,
      observedRendererStarted,
      observedMapAttached,
      observedDrawingEnabled,
      observedStartupWired,
      observedLayerCount,
      observedLayersArray,
      observedLifecycleStatus,
      observedImplementationAuthorized,
      observedInitializationAuthorized,
      observedDrawingAuthorized,
      observedGameplayChanged,
      observedBackendStorageNetworkChanged,
      stateContainerShellVerified: false,
      stateContainerShellVerificationStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 421,
    helperName:
      "getCustom25DVisualManualRendererStateContainerShellVerificationReport",
    reportOnly: true,
    passive: true,
    stateContainerShellVerificationOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerCreationHelperAvailable,
    stateContainerName,
    stateContainerExists,
    observedCreatedByPhase,
    observedStateContainerShellOnly,
    observedInert,
    observedInitialized,
    observedRendererInstanceIsNull,
    observedRendererCreated,
    observedRendererInitialized,
    observedRendererStarted,
    observedMapAttached,
    observedDrawingEnabled,
    observedStartupWired,
    observedLayerCount,
    observedLayersArray,
    observedLifecycleStatus,
    observedImplementationAuthorized,
    observedInitializationAuthorized,
    observedDrawingAuthorized,
    observedGameplayChanged,
    observedBackendStorageNetworkChanged,
    stateContainerShellVerified,
    stateContainerShellVerificationStatus: "state-container-shell-verified",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateContainerShellCloseoutReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "stateContainerShellVerificationOnly",
    "stateContainerShellCloseoutOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoStateCreationReadinessInvocation",
    "confirmNoStateContainerCreationInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateContainerShellVerificationHelperAvailable =
    typeof getCustom25DVisualManualRendererStateContainerShellVerificationReport ===
    "function";
  const stateContainerCreationHelperAvailable =
    typeof createCustom25DVisualManualRendererStateContainerShell === "function";
  const stateContainerName = "custom25DVisualManualRendererState";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const stateContainerShellVerified =
    stateContainerExists &&
    !!(stateContainer && stateContainer.createdByPhase === 420) &&
    !!(stateContainer && stateContainer.stateContainerName === stateContainerName) &&
    !!(stateContainer && stateContainer.stateContainerShellOnly === true) &&
    !!(stateContainer && stateContainer.inert === true) &&
    !!(stateContainer && stateContainer.initialized === false) &&
    !!(stateContainer && stateContainer.rendererInstance === null) &&
    !!(stateContainer && stateContainer.rendererCreated === false) &&
    !!(stateContainer && stateContainer.rendererInitialized === false) &&
    !!(stateContainer && stateContainer.rendererStarted === false) &&
    !!(stateContainer && stateContainer.mapAttached === false) &&
    !!(stateContainer && stateContainer.drawingEnabled === false) &&
    !!(stateContainer && stateContainer.startupWired === false) &&
    !!(stateContainer && stateContainer.layerCount === 0) &&
    !!(stateContainer && Array.isArray(stateContainer.layers)) &&
    !!(stateContainer && stateContainer.lifecycleStatus === "state-container-created") &&
    !!(stateContainer && stateContainer.implementationAuthorized === false) &&
    !!(stateContainer && stateContainer.initializationAuthorized === false) &&
    !!(stateContainer && stateContainer.drawingAuthorized === false) &&
    !!(stateContainer && stateContainer.gameplayChanged === false) &&
    !!(stateContainer && stateContainer.backendStorageNetworkChanged === false);

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 421,
      helperName:
        "getCustom25DVisualManualRendererStateContainerShellCloseoutReport",
      reportOnly: true,
      passive: true,
      stateContainerShellCloseoutOnly: true,
      jsonSafeResult: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerShellVerificationHelperAvailable,
      stateContainerCreationHelperAvailable,
      stateContainerName,
      stateContainerExists,
      stateContainerShellVerified: false,
      stateContainerShellCloseoutStatus: "blocked",
      futureStateMutationStillRequiresSeparatePhase: true,
      futureRendererCreationStillBlocked: true,
      futureInitializationStillBlocked: true,
      futureMapAttachmentStillBlocked: true,
      futureDrawingStillBlocked: true,
      futureStartupWiringStillBlocked: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 421,
    helperName:
      "getCustom25DVisualManualRendererStateContainerShellCloseoutReport",
    reportOnly: true,
    passive: true,
    stateContainerShellCloseoutOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerShellVerificationHelperAvailable,
    stateContainerCreationHelperAvailable,
    stateContainerName,
    stateContainerExists,
    stateContainerShellVerified,
    stateContainerShellCloseoutStatus: "state-container-shell-closed-out",
    futureStateMutationStillRequiresSeparatePhase: true,
    futureRendererCreationStillBlocked: true,
    futureInitializationStillBlocked: true,
    futureMapAttachmentStillBlocked: true,
    futureDrawingStillBlocked: true,
    futureStartupWiringStillBlocked: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateMutationReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "stateMutationReadinessOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoStateCreationReadinessInvocation",
    "confirmNoStateContainerCreationInvocation",
    "confirmNoStateContainerVerificationInvocation",
    "confirmNoStateContainerCloseoutInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateContainerShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
    "function";
  const stateContainerName = "custom25DVisualManualRendererState";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const observedLifecycleStatus =
    stateContainer && typeof stateContainer.lifecycleStatus === "string"
      ? stateContainer.lifecycleStatus
      : null;
  const observedInert =
    stateContainer && typeof stateContainer.inert === "boolean"
      ? stateContainer.inert
      : null;
  const observedInitialized =
    stateContainer && typeof stateContainer.initialized === "boolean"
      ? stateContainer.initialized
      : null;
  const observedRendererInstanceIsNull =
    stateContainer ? stateContainer.rendererInstance === null : false;
  const observedRendererCreated =
    stateContainer && typeof stateContainer.rendererCreated === "boolean"
      ? stateContainer.rendererCreated
      : null;
  const observedRendererInitialized =
    stateContainer && typeof stateContainer.rendererInitialized === "boolean"
      ? stateContainer.rendererInitialized
      : null;
  const observedRendererStarted =
    stateContainer && typeof stateContainer.rendererStarted === "boolean"
      ? stateContainer.rendererStarted
      : null;
  const observedMapAttached =
    stateContainer && typeof stateContainer.mapAttached === "boolean"
      ? stateContainer.mapAttached
      : null;
  const observedDrawingEnabled =
    stateContainer && typeof stateContainer.drawingEnabled === "boolean"
      ? stateContainer.drawingEnabled
      : null;
  const observedStartupWired =
    stateContainer && typeof stateContainer.startupWired === "boolean"
      ? stateContainer.startupWired
      : null;
  const observedLayerCount =
    stateContainer && typeof stateContainer.layerCount === "number"
      ? stateContainer.layerCount
      : null;
  const observedImplementationAuthorized =
    stateContainer && typeof stateContainer.implementationAuthorized === "boolean"
      ? stateContainer.implementationAuthorized
      : null;
  const observedInitializationAuthorized =
    stateContainer && typeof stateContainer.initializationAuthorized === "boolean"
      ? stateContainer.initializationAuthorized
      : null;
  const observedDrawingAuthorized =
    stateContainer && typeof stateContainer.drawingAuthorized === "boolean"
      ? stateContainer.drawingAuthorized
      : null;
  const stateContainerSafeForFutureMutationPlanning =
    stateContainerExists &&
    observedInert === true &&
    observedInitialized === false &&
    observedRendererInstanceIsNull === true &&
    observedRendererCreated === false &&
    observedRendererInitialized === false &&
    observedRendererStarted === false &&
    observedMapAttached === false &&
    observedDrawingEnabled === false &&
    observedStartupWired === false &&
    observedLayerCount === 0 &&
    observedImplementationAuthorized === false &&
    observedInitializationAuthorized === false &&
    observedDrawingAuthorized === false;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 422,
      helperName:
        "getCustom25DVisualManualRendererStateMutationReadinessReport",
      reportOnly: true,
      passive: true,
      stateMutationReadinessOnly: true,
      jsonSafeResult: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerShellCloseoutHelperAvailable,
      stateContainerName,
      stateContainerExists,
      observedLifecycleStatus,
      observedInert,
      observedInitialized,
      observedRendererInstanceIsNull,
      observedRendererCreated,
      observedRendererInitialized,
      observedRendererStarted,
      observedMapAttached,
      observedDrawingEnabled,
      observedStartupWired,
      observedLayerCount,
      observedImplementationAuthorized,
      observedInitializationAuthorized,
      observedDrawingAuthorized,
      stateContainerSafeForFutureMutationPlanning: false,
      stateMutationReadinessStatus: "blocked",
      futureStateMutationAllowedNow: false,
      futureStateMutationStillRequiresSeparatePhase: true,
      futureRendererCreationStillBlocked: true,
      futureInitializationStillBlocked: true,
      futureMapAttachmentStillBlocked: true,
      futureDrawingStillBlocked: true,
      futureStartupWiringStillBlocked: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 422,
    helperName:
      "getCustom25DVisualManualRendererStateMutationReadinessReport",
    reportOnly: true,
    passive: true,
    stateMutationReadinessOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerShellCloseoutHelperAvailable,
    stateContainerName,
    stateContainerExists,
    observedLifecycleStatus,
    observedInert,
    observedInitialized,
    observedRendererInstanceIsNull,
    observedRendererCreated,
    observedRendererInitialized,
    observedRendererStarted,
    observedMapAttached,
    observedDrawingEnabled,
    observedStartupWired,
    observedLayerCount,
    observedImplementationAuthorized,
    observedInitializationAuthorized,
    observedDrawingAuthorized,
    stateContainerSafeForFutureMutationPlanning,
    stateMutationReadinessStatus: "state-mutation-readiness-report-ready",
    futureStateMutationAllowedNow: false,
    futureStateMutationStillRequiresSeparatePhase: true,
    futureRendererCreationStillBlocked: true,
    futureInitializationStillBlocked: true,
    futureMapAttachmentStillBlocked: true,
    futureDrawingStillBlocked: true,
    futureStartupWiringStillBlocked: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "stateMutationReadinessOnly",
    "confirmNoPreflightInvocation",
    "confirmNoCloseoutInvocation",
    "confirmNoGateReviewInvocation",
    "confirmNoAuthorizationChecklistInvocation",
    "confirmNoLockReportInvocation",
    "confirmNoSequenceSummaryInvocation",
    "confirmNoSequenceCloseoutInvocation",
    "confirmNoHandoffPlanInvocation",
    "confirmNoNextStepReadinessInvocation",
    "confirmNoPlanningBoundaryInvocation",
    "confirmNoRiskReviewInvocation",
    "confirmNoRiskCloseoutInvocation",
    "confirmNoPlanningHandoffInvocation",
    "confirmNoStateCreationReadinessInvocation",
    "confirmNoStateContainerCreationInvocation",
    "confirmNoStateContainerVerificationInvocation",
    "confirmNoStateContainerCloseoutInvocation",
    "confirmNoGuardInvocation",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleMutation",
    "confirmNoContractShellInvocation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateMutationReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererStateMutationReadinessReport ===
    "function";
  const stateContainerShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const stateContainerSafeForFutureMutationPlanning =
    stateContainerExists &&
    !!(stateContainer && stateContainer.inert === true) &&
    !!(stateContainer && stateContainer.initialized === false) &&
    !!(stateContainer && stateContainer.rendererInstance === null) &&
    !!(stateContainer && stateContainer.rendererCreated === false) &&
    !!(stateContainer && stateContainer.rendererInitialized === false) &&
    !!(stateContainer && stateContainer.rendererStarted === false) &&
    !!(stateContainer && stateContainer.mapAttached === false) &&
    !!(stateContainer && stateContainer.drawingEnabled === false) &&
    !!(stateContainer && stateContainer.startupWired === false) &&
    !!(stateContainer && stateContainer.layerCount === 0) &&
    !!(stateContainer && stateContainer.implementationAuthorized === false) &&
    !!(stateContainer && stateContainer.initializationAuthorized === false) &&
    !!(stateContainer && stateContainer.drawingAuthorized === false);

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 422,
      helperName:
        "getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      stateMutationReadinessCloseoutOnly: true,
      jsonSafeResult: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateMutationReadinessHelperAvailable,
      stateContainerShellCloseoutHelperAvailable,
      stateContainerExists,
      stateContainerSafeForFutureMutationPlanning: false,
      stateMutationReadinessStatus: "blocked",
      stateMutationReadinessCloseoutStatus: "blocked",
      futureStateMutationAllowedNow: false,
      futureStateMutationStillRequiresSeparatePhase: true,
      futureRendererCreationStillBlocked: true,
      futureInitializationStillBlocked: true,
      futureMapAttachmentStillBlocked: true,
      futureDrawingStillBlocked: true,
      futureStartupWiringStillBlocked: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 422,
    helperName:
      "getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    stateMutationReadinessCloseoutOnly: true,
    jsonSafeResult: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateMutationReadinessHelperAvailable,
    stateContainerShellCloseoutHelperAvailable,
    stateContainerExists,
    stateContainerSafeForFutureMutationPlanning,
    stateMutationReadinessStatus: "state-mutation-readiness-report-ready",
    stateMutationReadinessCloseoutStatus:
      "state-mutation-readiness-closed-out",
    futureStateMutationAllowedNow: false,
    futureStateMutationStillRequiresSeparatePhase: true,
    futureRendererCreationStillBlocked: true,
    futureInitializationStillBlocked: true,
    futureMapAttachmentStillBlocked: true,
    futureDrawingStillBlocked: true,
    futureStartupWiringStillBlocked: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function applyCustom25DVisualManualRendererStateMutationShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateMutationShell",
    "confirmExistingStateContainerOnly",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateMutationReadinessCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport ===
    "function";
  const stateContainerName = "custom25DVisualManualRendererState";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 423,
      helperName: "applyCustom25DVisualManualRendererStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateMutationReadinessCloseoutHelperAvailable,
      stateContainerExists,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualStateMutationApplied: false,
      manualStateMutationPhase: null,
      manualStateMutationShellOnly: false,
      manualStateMutationCount: 0,
      lastManualStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 423,
      helperName: "applyCustom25DVisualManualRendererStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateMutationReadinessCloseoutHelperAvailable,
      stateContainerExists: false,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualStateMutationApplied: false,
      manualStateMutationPhase: null,
      manualStateMutationShellOnly: false,
      manualStateMutationCount: 0,
      lastManualStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const stateContainer = window.custom25DVisualManualRendererState;
  if (!stateContainer || typeof stateContainer !== "object" || Array.isArray(stateContainer)) {
    return {
      ok: false,
      phase: 423,
      helperName: "applyCustom25DVisualManualRendererStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateMutationReadinessCloseoutHelperAvailable,
      stateContainerExists: true,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualStateMutationApplied: false,
      manualStateMutationPhase: null,
      manualStateMutationShellOnly: false,
      manualStateMutationCount: 0,
      lastManualStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const nextManualStateMutationCount =
    typeof stateContainer.manualStateMutationCount === "number" &&
    Number.isFinite(stateContainer.manualStateMutationCount)
      ? stateContainer.manualStateMutationCount + 1
      : 1;

  stateContainer.manualStateMutationApplied = true;
  stateContainer.manualStateMutationPhase = 423;
  stateContainer.manualStateMutationShellOnly = true;
  stateContainer.manualStateMutationCount = nextManualStateMutationCount;
  stateContainer.lastManualStateMutationStatus = "state-mutation-shell-applied";

  return {
    ok: true,
    phase: 423,
    helperName: "applyCustom25DVisualManualRendererStateMutationShell",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateMutationReadinessCloseoutHelperAvailable,
    stateContainerName,
    stateContainerExists: true,
    stateContainerCreated: false,
    stateContainerReplaced: false,
    mutationApplied: true,
    manualStateMutationApplied: true,
    manualStateMutationPhase: 423,
    manualStateMutationShellOnly: true,
    manualStateMutationCount: nextManualStateMutationCount,
    lastManualStateMutationStatus: "state-mutation-shell-applied",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateMutationShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateMutationShellCloseout",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateMutationShellHelperAvailable =
    typeof applyCustom25DVisualManualRendererStateMutationShell === "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const phase423MutationDetected =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423
    );
  const mutationFieldsValid =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423 &&
      stateContainer.manualStateMutationShellOnly === true &&
      typeof stateContainer.manualStateMutationCount === "number" &&
      stateContainer.manualStateMutationCount >= 1 &&
      stateContainer.lastManualStateMutationStatus === "state-mutation-shell-applied"
    );
  const protectedFieldsStillInert =
    !!(
      stateContainer &&
      stateContainer.initialized === false &&
      stateContainer.rendererInstance === null &&
      stateContainer.rendererCreated === false &&
      stateContainer.rendererInitialized === false &&
      stateContainer.rendererStarted === false &&
      stateContainer.mapAttached === false &&
      stateContainer.drawingEnabled === false &&
      stateContainer.startupWired === false &&
      stateContainer.layerCount === 0 &&
      Array.isArray(stateContainer.layers) &&
      stateContainer.lifecycleStatus === "state-container-created" &&
      stateContainer.implementationAuthorized === false &&
      stateContainer.initializationAuthorized === false &&
      stateContainer.drawingAuthorized === false &&
      stateContainer.gameplayChanged === false &&
      stateContainer.backendStorageNetworkChanged === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 424,
      helperName:
        "getCustom25DVisualManualRendererStateMutationShellCloseoutReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateMutationShellHelperAvailable,
      stateContainerExists,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      closeoutStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 424,
      helperName:
        "getCustom25DVisualManualRendererStateMutationShellCloseoutReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateMutationShellHelperAvailable,
      stateContainerExists: false,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      closeoutStatus: "state-container-missing",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: mutationFieldsValid && protectedFieldsStillInert,
    phase: 424,
    helperName:
      "getCustom25DVisualManualRendererStateMutationShellCloseoutReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateMutationShellHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected,
    mutationFieldsValid,
    protectedFieldsStillInert,
    closeoutStatus:
      mutationFieldsValid && protectedFieldsStillInert
        ? "state-mutation-shell-closed-out"
        : "state-mutation-shell-needs-review",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateMutationShellSelfReviewReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateMutationShellCloseout",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const closeoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateMutationShellCloseoutReport ===
    "function";
  const closeoutReport = closeoutHelperAvailable
    ? getCustom25DVisualManualRendererStateMutationShellCloseoutReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 424,
      helperName:
        "getCustom25DVisualManualRendererStateMutationShellSelfReviewReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists:
        !!(closeoutReport && closeoutReport.stateContainerExists === true),
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      closeoutHelperAvailable,
      selfReviewStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(closeoutReport && closeoutReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(closeoutReport && closeoutReport.phase423MutationDetected === true);
  const mutationFieldsValid =
    !!(closeoutReport && closeoutReport.mutationFieldsValid === true);
  const protectedFieldsStillInert =
    !!(closeoutReport && closeoutReport.protectedFieldsStillInert === true);
  const safelyClosedOut =
    !!(
      closeoutReport &&
      closeoutReport.allowed === true &&
      closeoutReport.closeoutStatus === "state-mutation-shell-closed-out" &&
      mutationFieldsValid &&
      protectedFieldsStillInert
    );

  return {
    ok: closeoutHelperAvailable && safelyClosedOut,
    phase: 424,
    helperName:
      "getCustom25DVisualManualRendererStateMutationShellSelfReviewReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    phase423MutationDetected,
    mutationFieldsValid,
    protectedFieldsStillInert,
    closeoutHelperAvailable,
    selfReviewStatus: safelyClosedOut
      ? "state-mutation-shell-self-review-passed"
      : "state-mutation-shell-self-review-needs-review",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererNextStateMutationReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowNextManualRendererStateMutationReadiness",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateMutationShellCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateMutationShellCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const phase423MutationDetected =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423
    );
  const mutationFieldsValid =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423 &&
      stateContainer.manualStateMutationShellOnly === true &&
      typeof stateContainer.manualStateMutationCount === "number" &&
      stateContainer.manualStateMutationCount >= 1 &&
      stateContainer.lastManualStateMutationStatus === "state-mutation-shell-applied"
    );
  const protectedFieldsStillInert =
    !!(
      stateContainer &&
      stateContainer.initialized === false &&
      stateContainer.rendererInstance === null &&
      stateContainer.rendererCreated === false &&
      stateContainer.rendererInitialized === false &&
      stateContainer.rendererStarted === false &&
      stateContainer.mapAttached === false &&
      stateContainer.drawingEnabled === false &&
      stateContainer.startupWired === false &&
      stateContainer.layerCount === 0 &&
      Array.isArray(stateContainer.layers) &&
      stateContainer.lifecycleStatus === "state-container-created" &&
      stateContainer.implementationAuthorized === false &&
      stateContainer.initializationAuthorized === false &&
      stateContainer.drawingAuthorized === false &&
      stateContainer.gameplayChanged === false &&
      stateContainer.backendStorageNetworkChanged === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 425,
      helperName:
        "getCustom25DVisualManualRendererNextStateMutationReadinessReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedNextMutationPhase: 426,
      plannedNextMutationAllowedNow: false,
      plannedNextMutationRequiresSeparatePhase: true,
      plannedNextMutationMustUseExistingStateContainerOnly: true,
      plannedNextMutationMustNotCreateOrReplaceStateContainer: true,
      readinessStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 425,
      helperName:
        "getCustom25DVisualManualRendererNextStateMutationReadinessReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExists: false,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedNextMutationPhase: 426,
      plannedNextMutationAllowedNow: false,
      plannedNextMutationRequiresSeparatePhase: true,
      plannedNextMutationMustUseExistingStateContainerOnly: true,
      plannedNextMutationMustNotCreateOrReplaceStateContainer: true,
      readinessStatus: "state-container-missing",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: mutationFieldsValid && protectedFieldsStillInert,
    phase: 425,
    helperName:
      "getCustom25DVisualManualRendererNextStateMutationReadinessReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists: true,
    phase423MutationDetected,
    mutationFieldsValid,
    protectedFieldsStillInert,
    plannedNextMutationPhase: 426,
    plannedNextMutationAllowedNow: false,
    plannedNextMutationRequiresSeparatePhase: true,
    plannedNextMutationMustUseExistingStateContainerOnly: true,
    plannedNextMutationMustNotCreateOrReplaceStateContainer: true,
    readinessStatus:
      mutationFieldsValid && protectedFieldsStillInert
        ? "next-state-mutation-readiness-documented"
        : "next-state-mutation-readiness-needs-review",
    stateMutationShellCloseoutHelperAvailable,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowNextManualRendererStateMutationReadiness",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const readinessHelperAvailable =
    typeof getCustom25DVisualManualRendererNextStateMutationReadinessReport ===
    "function";
  const readinessReport = readinessHelperAvailable
    ? getCustom25DVisualManualRendererNextStateMutationReadinessReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 425,
      helperName:
        "getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists:
        !!(readinessReport && readinessReport.stateContainerExists === true),
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedNextMutationPhase: 426,
      plannedNextMutationAllowedNow: false,
      plannedNextMutationRequiresSeparatePhase: true,
      plannedNextMutationMustUseExistingStateContainerOnly: true,
      plannedNextMutationMustNotCreateOrReplaceStateContainer: true,
      closeoutStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(readinessReport && readinessReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(readinessReport && readinessReport.phase423MutationDetected === true);
  const mutationFieldsValid =
    !!(readinessReport && readinessReport.mutationFieldsValid === true);
  const protectedFieldsStillInert =
    !!(readinessReport && readinessReport.protectedFieldsStillInert === true);
  const readinessSafelyDocumented =
    !!(
      readinessReport &&
      readinessReport.allowed === true &&
      readinessReport.readinessStatus === "next-state-mutation-readiness-documented" &&
      mutationFieldsValid &&
      protectedFieldsStillInert
    );

  return {
    ok: readinessHelperAvailable && readinessSafelyDocumented,
    phase: 425,
    helperName:
      "getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    phase423MutationDetected,
    mutationFieldsValid,
    protectedFieldsStillInert,
    plannedNextMutationPhase: 426,
    plannedNextMutationAllowedNow: false,
    plannedNextMutationRequiresSeparatePhase: true,
    plannedNextMutationMustUseExistingStateContainerOnly: true,
    plannedNextMutationMustNotCreateOrReplaceStateContainer: true,
    closeoutStatus: readinessSafelyDocumented
      ? "next-state-mutation-readiness-closed-out"
      : "next-state-mutation-readiness-needs-review",
    readinessHelperAvailable,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function applyCustom25DVisualManualRendererSecondStateMutationShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererSecondStateMutationShell",
    "confirmExistingStateContainerOnly",
    "confirmPhase423MutationAlreadyApplied",
    "confirmProtectedFieldsStillInert",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const nextStateMutationReadinessCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 426,
      helperName: "applyCustom25DVisualManualRendererSecondStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualSecondStateMutationApplied: false,
      manualSecondStateMutationPhase: null,
      manualSecondStateMutationShellOnly: false,
      manualSecondStateMutationCount: 0,
      lastManualSecondStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 426,
      helperName: "applyCustom25DVisualManualRendererSecondStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExists: false,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualSecondStateMutationApplied: false,
      manualSecondStateMutationPhase: null,
      manualSecondStateMutationShellOnly: false,
      manualSecondStateMutationCount: 0,
      lastManualSecondStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const stateContainer = window.custom25DVisualManualRendererState;
  if (!stateContainer || typeof stateContainer !== "object" || Array.isArray(stateContainer)) {
    return {
      ok: false,
      phase: 426,
      helperName: "applyCustom25DVisualManualRendererSecondStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExists: true,
      phase423MutationDetected: false,
      mutationFieldsValid: false,
      protectedFieldsStillInert: false,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualSecondStateMutationApplied: false,
      manualSecondStateMutationPhase: null,
      manualSecondStateMutationShellOnly: false,
      manualSecondStateMutationCount: 0,
      lastManualSecondStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const phase423MutationDetected =
    stateContainer.manualStateMutationApplied === true &&
    stateContainer.manualStateMutationPhase === 423;
  const mutationFieldsValid =
    phase423MutationDetected &&
    stateContainer.manualStateMutationShellOnly === true &&
    typeof stateContainer.manualStateMutationCount === "number" &&
    stateContainer.manualStateMutationCount >= 1 &&
    stateContainer.lastManualStateMutationStatus === "state-mutation-shell-applied";
  const protectedFieldsStillInert =
    stateContainer.initialized === false &&
    stateContainer.rendererInstance === null &&
    stateContainer.rendererCreated === false &&
    stateContainer.rendererInitialized === false &&
    stateContainer.rendererStarted === false &&
    stateContainer.mapAttached === false &&
    stateContainer.drawingEnabled === false &&
    stateContainer.startupWired === false &&
    stateContainer.layerCount === 0 &&
    Array.isArray(stateContainer.layers) &&
    stateContainer.lifecycleStatus === "state-container-created" &&
    stateContainer.implementationAuthorized === false &&
    stateContainer.initializationAuthorized === false &&
    stateContainer.drawingAuthorized === false &&
    stateContainer.gameplayChanged === false &&
    stateContainer.backendStorageNetworkChanged === false;

  if (!phase423MutationDetected || !mutationFieldsValid || !protectedFieldsStillInert) {
    return {
      ok: false,
      phase: 426,
      helperName: "applyCustom25DVisualManualRendererSecondStateMutationShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      nextStateMutationReadinessCloseoutHelperAvailable,
      stateContainerExists: true,
      phase423MutationDetected,
      mutationFieldsValid,
      protectedFieldsStillInert,
      stateContainerCreated: false,
      stateContainerReplaced: false,
      mutationApplied: false,
      manualSecondStateMutationApplied: false,
      manualSecondStateMutationPhase: null,
      manualSecondStateMutationShellOnly: false,
      manualSecondStateMutationCount: 0,
      lastManualSecondStateMutationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const nextManualSecondStateMutationCount =
    typeof stateContainer.manualSecondStateMutationCount === "number" &&
    Number.isFinite(stateContainer.manualSecondStateMutationCount)
      ? stateContainer.manualSecondStateMutationCount + 1
      : 1;

  stateContainer.manualSecondStateMutationApplied = true;
  stateContainer.manualSecondStateMutationPhase = 426;
  stateContainer.manualSecondStateMutationShellOnly = true;
  stateContainer.manualSecondStateMutationCount = nextManualSecondStateMutationCount;
  stateContainer.lastManualSecondStateMutationStatus =
    "second-state-mutation-shell-applied";

  return {
    ok: true,
    phase: 426,
    helperName: "applyCustom25DVisualManualRendererSecondStateMutationShell",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    nextStateMutationReadinessCloseoutHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected: true,
    mutationFieldsValid: true,
    protectedFieldsStillInert: true,
    stateContainerCreated: false,
    stateContainerReplaced: false,
    mutationApplied: true,
    manualSecondStateMutationApplied: true,
    manualSecondStateMutationPhase: 426,
    manualSecondStateMutationShellOnly: true,
    manualSecondStateMutationCount: nextManualSecondStateMutationCount,
    lastManualSecondStateMutationStatus: "second-state-mutation-shell-applied",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererSecondStateMutationCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererSecondStateMutationCloseout",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const secondStateMutationShellHelperAvailable =
    typeof applyCustom25DVisualManualRendererSecondStateMutationShell === "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const phase423MutationDetected =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423
    );
  const phase423MutationFieldsValid =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423 &&
      stateContainer.manualStateMutationShellOnly === true &&
      typeof stateContainer.manualStateMutationCount === "number" &&
      stateContainer.manualStateMutationCount >= 1 &&
      stateContainer.lastManualStateMutationStatus === "state-mutation-shell-applied"
    );
  const phase426MutationDetected =
    !!(
      stateContainer &&
      stateContainer.manualSecondStateMutationApplied === true &&
      stateContainer.manualSecondStateMutationPhase === 426
    );
  const phase426MutationFieldsValid =
    !!(
      stateContainer &&
      stateContainer.manualSecondStateMutationApplied === true &&
      stateContainer.manualSecondStateMutationPhase === 426 &&
      stateContainer.manualSecondStateMutationShellOnly === true &&
      typeof stateContainer.manualSecondStateMutationCount === "number" &&
      stateContainer.manualSecondStateMutationCount >= 1 &&
      stateContainer.lastManualSecondStateMutationStatus ===
        "second-state-mutation-shell-applied"
    );
  const protectedFieldsStillInert =
    !!(
      stateContainer &&
      stateContainer.initialized === false &&
      stateContainer.rendererInstance === null &&
      stateContainer.rendererCreated === false &&
      stateContainer.rendererInitialized === false &&
      stateContainer.rendererStarted === false &&
      stateContainer.mapAttached === false &&
      stateContainer.drawingEnabled === false &&
      stateContainer.startupWired === false &&
      stateContainer.layerCount === 0 &&
      Array.isArray(stateContainer.layers) &&
      stateContainer.lifecycleStatus === "state-container-created" &&
      stateContainer.implementationAuthorized === false &&
      stateContainer.initializationAuthorized === false &&
      stateContainer.drawingAuthorized === false &&
      stateContainer.gameplayChanged === false &&
      stateContainer.backendStorageNetworkChanged === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 427,
      helperName:
        "getCustom25DVisualManualRendererSecondStateMutationCloseoutReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      closeoutStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 427,
      helperName:
        "getCustom25DVisualManualRendererSecondStateMutationCloseoutReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExists: false,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      closeoutStatus: "state-container-missing",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      protectedFieldsStillInert,
    phase: 427,
    helperName:
      "getCustom25DVisualManualRendererSecondStateMutationCloseoutReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    secondStateMutationShellHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    protectedFieldsStillInert,
    closeoutStatus:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      protectedFieldsStillInert
        ? "second-state-mutation-closed-out"
        : "second-state-mutation-needs-review",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererSecondStateMutationCloseout",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const closeoutHelperAvailable =
    typeof getCustom25DVisualManualRendererSecondStateMutationCloseoutReport ===
    "function";
  const closeoutReport = closeoutHelperAvailable
    ? getCustom25DVisualManualRendererSecondStateMutationCloseoutReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 427,
      helperName:
        "getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists:
        !!(closeoutReport && closeoutReport.stateContainerExists === true),
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      selfReviewStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(closeoutReport && closeoutReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(closeoutReport && closeoutReport.phase423MutationDetected === true);
  const phase423MutationFieldsValid =
    !!(closeoutReport && closeoutReport.phase423MutationFieldsValid === true);
  const phase426MutationDetected =
    !!(closeoutReport && closeoutReport.phase426MutationDetected === true);
  const phase426MutationFieldsValid =
    !!(closeoutReport && closeoutReport.phase426MutationFieldsValid === true);
  const protectedFieldsStillInert =
    !!(closeoutReport && closeoutReport.protectedFieldsStillInert === true);
  const safelyClosedOut =
    !!(
      closeoutReport &&
      closeoutReport.allowed === true &&
      closeoutReport.closeoutStatus === "second-state-mutation-closed-out" &&
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      protectedFieldsStillInert
    );

  return {
    ok: closeoutHelperAvailable && safelyClosedOut,
    phase: 427,
    helperName:
      "getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    protectedFieldsStillInert,
    selfReviewStatus: safelyClosedOut
      ? "second-state-mutation-self-review-passed"
      : "second-state-mutation-self-review-needs-review",
    closeoutHelperAvailable,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateFieldContractReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateFieldContractReadiness",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const secondStateMutationCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererSecondStateMutationCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const stateContainer = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const phase423MutationDetected =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423
    );
  const phase423MutationFieldsValid =
    !!(
      stateContainer &&
      stateContainer.manualStateMutationApplied === true &&
      stateContainer.manualStateMutationPhase === 423 &&
      stateContainer.manualStateMutationShellOnly === true &&
      typeof stateContainer.manualStateMutationCount === "number" &&
      stateContainer.manualStateMutationCount >= 1 &&
      stateContainer.lastManualStateMutationStatus === "state-mutation-shell-applied"
    );
  const phase426MutationDetected =
    !!(
      stateContainer &&
      stateContainer.manualSecondStateMutationApplied === true &&
      stateContainer.manualSecondStateMutationPhase === 426
    );
  const phase426MutationFieldsValid =
    !!(
      stateContainer &&
      stateContainer.manualSecondStateMutationApplied === true &&
      stateContainer.manualSecondStateMutationPhase === 426 &&
      stateContainer.manualSecondStateMutationShellOnly === true &&
      typeof stateContainer.manualSecondStateMutationCount === "number" &&
      stateContainer.manualSecondStateMutationCount >= 1 &&
      stateContainer.lastManualSecondStateMutationStatus ===
        "second-state-mutation-shell-applied"
    );
  const protectedFieldsStillInert =
    !!(
      stateContainer &&
      stateContainer.initialized === false &&
      stateContainer.rendererInstance === null &&
      stateContainer.rendererCreated === false &&
      stateContainer.rendererInitialized === false &&
      stateContainer.rendererStarted === false &&
      stateContainer.mapAttached === false &&
      stateContainer.drawingEnabled === false &&
      stateContainer.startupWired === false &&
      stateContainer.layerCount === 0 &&
      Array.isArray(stateContainer.layers) &&
      stateContainer.lifecycleStatus === "state-container-created" &&
      stateContainer.implementationAuthorized === false &&
      stateContainer.initializationAuthorized === false &&
      stateContainer.drawingAuthorized === false &&
      stateContainer.gameplayChanged === false &&
      stateContainer.backendStorageNetworkChanged === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 428,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractReadinessReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedFieldContractPhase: 429,
      plannedFieldContractAllowedNow: false,
      plannedFieldContractRequiresSeparatePhase: true,
      plannedFieldContractMustUseExistingStateContainerOnly: true,
      plannedFieldContractMustNotCreateOrReplaceStateContainer: true,
      plannedFieldContractMustStayInert: true,
      suggestedFutureInertFields: [
        "manualRendererFieldContractApplied",
        "manualRendererFieldContractPhase",
        "manualRendererFieldContractShellOnly",
        "manualRendererFieldContractStatus"
      ],
      readinessStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 428,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractReadinessReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExists: false,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedFieldContractPhase: 429,
      plannedFieldContractAllowedNow: false,
      plannedFieldContractRequiresSeparatePhase: true,
      plannedFieldContractMustUseExistingStateContainerOnly: true,
      plannedFieldContractMustNotCreateOrReplaceStateContainer: true,
      plannedFieldContractMustStayInert: true,
      suggestedFutureInertFields: [
        "manualRendererFieldContractApplied",
        "manualRendererFieldContractPhase",
        "manualRendererFieldContractShellOnly",
        "manualRendererFieldContractStatus"
      ],
      readinessStatus: "state-container-missing",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      protectedFieldsStillInert,
    phase: 428,
    helperName:
      "getCustom25DVisualManualRendererStateFieldContractReadinessReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    secondStateMutationCloseoutHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    protectedFieldsStillInert,
    plannedFieldContractPhase: 429,
    plannedFieldContractAllowedNow: false,
    plannedFieldContractRequiresSeparatePhase: true,
    plannedFieldContractMustUseExistingStateContainerOnly: true,
    plannedFieldContractMustNotCreateOrReplaceStateContainer: true,
    plannedFieldContractMustStayInert: true,
    suggestedFutureInertFields: [
      "manualRendererFieldContractApplied",
      "manualRendererFieldContractPhase",
      "manualRendererFieldContractShellOnly",
      "manualRendererFieldContractStatus"
    ],
    readinessStatus:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      protectedFieldsStillInert
        ? "state-field-contract-readiness-documented"
        : "state-field-contract-readiness-needs-review",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateFieldContractReadiness",
    "confirmReadOnly",
    "confirmNoStateMutation",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const readinessHelperAvailable =
    typeof getCustom25DVisualManualRendererStateFieldContractReadinessReport ===
    "function";
  const readinessReport = readinessHelperAvailable
    ? getCustom25DVisualManualRendererStateFieldContractReadinessReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 428,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists:
        !!(readinessReport && readinessReport.stateContainerExists === true),
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedFieldContractPhase: 429,
      plannedFieldContractAllowedNow: false,
      plannedFieldContractRequiresSeparatePhase: true,
      plannedFieldContractMustUseExistingStateContainerOnly: true,
      plannedFieldContractMustNotCreateOrReplaceStateContainer: true,
      plannedFieldContractMustStayInert: true,
      closeoutStatus: "blocked",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(readinessReport && readinessReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(readinessReport && readinessReport.phase423MutationDetected === true);
  const phase423MutationFieldsValid =
    !!(readinessReport && readinessReport.phase423MutationFieldsValid === true);
  const phase426MutationDetected =
    !!(readinessReport && readinessReport.phase426MutationDetected === true);
  const phase426MutationFieldsValid =
    !!(readinessReport && readinessReport.phase426MutationFieldsValid === true);
  const protectedFieldsStillInert =
    !!(readinessReport && readinessReport.protectedFieldsStillInert === true);
  const readinessSafelyDocumented =
    !!(
      readinessReport &&
      readinessReport.allowed === true &&
      readinessReport.readinessStatus ===
        "state-field-contract-readiness-documented" &&
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      protectedFieldsStillInert
    );

  return {
    ok: readinessHelperAvailable && readinessSafelyDocumented,
    phase: 428,
    helperName:
      "getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    protectedFieldsStillInert,
    plannedFieldContractPhase: 429,
    plannedFieldContractAllowedNow: false,
    plannedFieldContractRequiresSeparatePhase: true,
    plannedFieldContractMustUseExistingStateContainerOnly: true,
    plannedFieldContractMustNotCreateOrReplaceStateContainer: true,
    plannedFieldContractMustStayInert: true,
    closeoutStatus: readinessSafelyDocumented
      ? "state-field-contract-readiness-closed-out"
      : "state-field-contract-readiness-needs-review",
    readinessHelperAvailable,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function applyCustom25DVisualManualRendererStateFieldContractShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateFieldContractShell",
    "confirmExistingStateContainerOnly",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmPhase423MutationFieldsRequired",
    "confirmPhase426MutationFieldsRequired",
    "confirmProtectedFieldsMustStayInert",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateFieldContractReadinessCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 429,
      helperName: "applyCustom25DVisualManualRendererStateFieldContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateFieldContractReadinessCloseoutHelperAvailable,
      stateContainerExists,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      mutationApplied: false,
      manualRendererFieldContractApplied: false,
      manualRendererFieldContractPhase: null,
      manualRendererFieldContractShellOnly: false,
      manualRendererFieldContractStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 429,
      helperName: "applyCustom25DVisualManualRendererStateFieldContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractReadinessCloseoutHelperAvailable,
      stateContainerExists: false,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      mutationApplied: false,
      manualRendererFieldContractApplied: false,
      manualRendererFieldContractPhase: null,
      manualRendererFieldContractShellOnly: false,
      manualRendererFieldContractStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const state = window.custom25DVisualManualRendererState;
  if (!state || typeof state !== "object" || Array.isArray(state)) {
    return {
      ok: false,
      phase: 429,
      helperName: "applyCustom25DVisualManualRendererStateFieldContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractReadinessCloseoutHelperAvailable,
      stateContainerExists: true,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      protectedFieldsStillInert: false,
      mutationApplied: false,
      manualRendererFieldContractApplied: false,
      manualRendererFieldContractPhase: null,
      manualRendererFieldContractShellOnly: false,
      manualRendererFieldContractStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const phase423MutationDetected =
    state.manualStateMutationApplied === true &&
    state.manualStateMutationPhase === 423;
  const phase423MutationFieldsValid =
    phase423MutationDetected &&
    state.manualStateMutationShellOnly === true &&
    typeof state.manualStateMutationCount === "number" &&
    state.manualStateMutationCount >= 1 &&
    state.lastManualStateMutationStatus === "state-mutation-shell-applied";
  const phase426MutationDetected =
    state.manualSecondStateMutationApplied === true &&
    state.manualSecondStateMutationPhase === 426;
  const phase426MutationFieldsValid =
    phase426MutationDetected &&
    state.manualSecondStateMutationShellOnly === true &&
    typeof state.manualSecondStateMutationCount === "number" &&
    state.manualSecondStateMutationCount >= 1 &&
    state.lastManualSecondStateMutationStatus ===
      "second-state-mutation-shell-applied";
  const protectedFieldsStillInert =
    state.initialized === false &&
    state.rendererInstance === null &&
    state.rendererCreated === false &&
    state.rendererInitialized === false &&
    state.rendererStarted === false &&
    state.mapAttached === false &&
    state.drawingEnabled === false &&
    state.startupWired === false &&
    state.layerCount === 0 &&
    Array.isArray(state.layers) &&
    state.lifecycleStatus === "state-container-created" &&
    state.implementationAuthorized === false &&
    state.initializationAuthorized === false &&
    state.drawingAuthorized === false &&
    state.gameplayChanged === false &&
    state.backendStorageNetworkChanged === false;

  if (
    !phase423MutationDetected ||
    !phase423MutationFieldsValid ||
    !phase426MutationDetected ||
    !phase426MutationFieldsValid ||
    !protectedFieldsStillInert
  ) {
    return {
      ok: false,
      phase: 429,
      helperName: "applyCustom25DVisualManualRendererStateFieldContractShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractReadinessCloseoutHelperAvailable,
      stateContainerExists: true,
      phase423MutationDetected,
      phase423MutationFieldsValid,
      phase426MutationDetected,
      phase426MutationFieldsValid,
      protectedFieldsStillInert,
      mutationApplied: false,
      manualRendererFieldContractApplied: false,
      manualRendererFieldContractPhase: null,
      manualRendererFieldContractShellOnly: false,
      manualRendererFieldContractStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  state.manualRendererFieldContractApplied = true;
  state.manualRendererFieldContractPhase = 429;
  state.manualRendererFieldContractShellOnly = true;
  state.manualRendererFieldContractStatus = "state-field-contract-shell-applied";

  return {
    ok: true,
    phase: 429,
    helperName: "applyCustom25DVisualManualRendererStateFieldContractShell",
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateFieldContractReadinessCloseoutHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected: true,
    phase423MutationFieldsValid: true,
    phase426MutationDetected: true,
    phase426MutationFieldsValid: true,
    protectedFieldsStillInert: true,
    mutationApplied: true,
    manualRendererFieldContractApplied: true,
    manualRendererFieldContractPhase: 429,
    manualRendererFieldContractShellOnly: true,
    manualRendererFieldContractStatus: "state-field-contract-shell-applied",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "passiveOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateFieldContractShellHelperAvailable =
    typeof applyCustom25DVisualManualRendererStateFieldContractShell === "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const phase423MutationDetected =
    !!(
      state &&
      state.manualStateMutationApplied === true &&
      state.manualStateMutationPhase === 423
    );
  const phase423MutationFieldsValid =
    !!(
      state &&
      state.manualStateMutationApplied === true &&
      state.manualStateMutationPhase === 423 &&
      state.manualStateMutationShellOnly === true &&
      typeof state.manualStateMutationCount === "number" &&
      state.manualStateMutationCount >= 1 &&
      state.lastManualStateMutationStatus === "state-mutation-shell-applied"
    );
  const phase426MutationDetected =
    !!(
      state &&
      state.manualSecondStateMutationApplied === true &&
      state.manualSecondStateMutationPhase === 426
    );
  const phase426MutationFieldsValid =
    !!(
      state &&
      state.manualSecondStateMutationApplied === true &&
      state.manualSecondStateMutationPhase === 426 &&
      state.manualSecondStateMutationShellOnly === true &&
      typeof state.manualSecondStateMutationCount === "number" &&
      state.manualSecondStateMutationCount >= 1 &&
      state.lastManualSecondStateMutationStatus ===
        "second-state-mutation-shell-applied"
    );
  const phase429FieldContractDetected =
    !!(
      state &&
      state.manualRendererFieldContractApplied === true &&
      state.manualRendererFieldContractPhase === 429
    );
  const phase429FieldContractFieldsValid =
    !!(
      state &&
      state.manualRendererFieldContractApplied === true &&
      state.manualRendererFieldContractPhase === 429 &&
      state.manualRendererFieldContractShellOnly === true &&
      state.manualRendererFieldContractStatus ===
        "state-field-contract-shell-applied"
    );
  const protectedFieldsStillInert =
    !!(
      state &&
      state.initialized === false &&
      state.rendererInstance === null &&
      state.rendererCreated === false &&
      state.rendererInitialized === false &&
      state.rendererStarted === false &&
      state.mapAttached === false &&
      state.drawingEnabled === false &&
      state.startupWired === false &&
      state.gameplayChanged === false &&
      state.backendStorageNetworkChanged === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 430,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateFieldContractShellHelperAvailable,
      stateContainerExists,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      protectedFieldsStillInert: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      closeoutStatus: "blocked",
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 430,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractShellHelperAvailable,
      stateContainerExists: false,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      protectedFieldsStillInert: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      closeoutStatus: "state-container-missing",
      defaultDecision: "blocked"
    };
  }

  return {
    ok:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      protectedFieldsStillInert,
    phase: 430,
    helperName:
      "getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateFieldContractShellHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    phase429FieldContractDetected,
    phase429FieldContractFieldsValid,
    protectedFieldsStillInert,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    closeoutStatus:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      protectedFieldsStillInert
        ? "state-field-contract-shell-closed-out"
        : "state-field-contract-shell-needs-review",
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateFieldContractNextReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "passiveOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase430CloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport ===
    "function";
  const closeoutReport = phase430CloseoutHelperAvailable
    ? getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 430,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractNextReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase430CloseoutHelperAvailable,
      stateContainerExists:
        !!(closeoutReport && closeoutReport.stateContainerExists === true),
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedNextPhase: 431,
      plannedNextStepAllowedNow: false,
      plannedNextStepRequiresSeparatePhase: true,
      plannedNextStepMustStayInert: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      closeoutStatus: "blocked",
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(closeoutReport && closeoutReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(closeoutReport && closeoutReport.phase423MutationDetected === true);
  const phase423MutationFieldsValid =
    !!(closeoutReport && closeoutReport.phase423MutationFieldsValid === true);
  const phase426MutationDetected =
    !!(closeoutReport && closeoutReport.phase426MutationDetected === true);
  const phase426MutationFieldsValid =
    !!(closeoutReport && closeoutReport.phase426MutationFieldsValid === true);
  const phase429FieldContractDetected =
    !!(closeoutReport && closeoutReport.phase429FieldContractDetected === true);
  const phase429FieldContractFieldsValid =
    !!(closeoutReport && closeoutReport.phase429FieldContractFieldsValid === true);
  const protectedFieldsStillInert =
    !!(closeoutReport && closeoutReport.protectedFieldsStillInert === true);

  return {
    ok:
      phase430CloseoutHelperAvailable &&
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      protectedFieldsStillInert,
    phase: 430,
    helperName:
      "getCustom25DVisualManualRendererStateFieldContractNextReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase430CloseoutHelperAvailable,
    stateContainerExists,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    phase429FieldContractDetected,
    phase429FieldContractFieldsValid,
    protectedFieldsStillInert,
    plannedNextPhase: 431,
    plannedNextStepAllowedNow: false,
    plannedNextStepRequiresSeparatePhase: true,
    plannedNextStepMustStayInert: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    closeoutStatus:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      protectedFieldsStillInert
        ? "state-field-contract-next-readiness-documented"
        : "state-field-contract-next-readiness-needs-review",
    defaultDecision: "blocked"
  };
}

function applyCustom25DVisualManualRendererStateFieldContractValidationShell(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateFieldContractValidationShell",
    "confirmExistingStateContainerOnly",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmPhase423MutationFieldsRequired",
    "confirmPhase426MutationFieldsRequired",
    "confirmPhase429FieldContractFieldsRequired",
    "confirmProtectedFieldsMustStayInert",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const stateFieldContractNextReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererStateFieldContractNextReadinessReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 431,
      helperName:
        "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateFieldContractNextReadinessHelperAvailable,
      stateContainerExists,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      protectedFieldsStillInert: false,
      mutationApplied: false,
      manualRendererFieldContractValidationApplied: false,
      manualRendererFieldContractValidationPhase: null,
      manualRendererFieldContractValidationShellOnly: false,
      manualRendererFieldContractValidationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 431,
      helperName:
        "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractNextReadinessHelperAvailable,
      stateContainerExists: false,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      protectedFieldsStillInert: false,
      mutationApplied: false,
      manualRendererFieldContractValidationApplied: false,
      manualRendererFieldContractValidationPhase: null,
      manualRendererFieldContractValidationShellOnly: false,
      manualRendererFieldContractValidationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const state = window.custom25DVisualManualRendererState;
  if (!state || typeof state !== "object" || Array.isArray(state)) {
    return {
      ok: false,
      phase: 431,
      helperName:
        "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractNextReadinessHelperAvailable,
      stateContainerExists: true,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      protectedFieldsStillInert: false,
      mutationApplied: false,
      manualRendererFieldContractValidationApplied: false,
      manualRendererFieldContractValidationPhase: null,
      manualRendererFieldContractValidationShellOnly: false,
      manualRendererFieldContractValidationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  const phase423MutationDetected =
    state.manualStateMutationApplied === true &&
    state.manualStateMutationPhase === 423;
  const phase423MutationFieldsValid =
    phase423MutationDetected &&
    state.manualStateMutationShellOnly === true &&
    typeof state.manualStateMutationCount === "number" &&
    state.manualStateMutationCount >= 1 &&
    state.lastManualStateMutationStatus === "state-mutation-shell-applied";
  const phase426MutationDetected =
    state.manualSecondStateMutationApplied === true &&
    state.manualSecondStateMutationPhase === 426;
  const phase426MutationFieldsValid =
    phase426MutationDetected &&
    state.manualSecondStateMutationShellOnly === true &&
    typeof state.manualSecondStateMutationCount === "number" &&
    state.manualSecondStateMutationCount >= 1 &&
    state.lastManualSecondStateMutationStatus ===
      "second-state-mutation-shell-applied";
  const phase429FieldContractDetected =
    state.manualRendererFieldContractApplied === true &&
    state.manualRendererFieldContractPhase === 429;
  const phase429FieldContractFieldsValid =
    phase429FieldContractDetected &&
    state.manualRendererFieldContractShellOnly === true &&
    state.manualRendererFieldContractStatus ===
      "state-field-contract-shell-applied";
  const protectedFieldsStillInert =
    state.initialized === false &&
    state.rendererInstance === null &&
    state.rendererCreated === false &&
    state.rendererInitialized === false &&
    state.rendererStarted === false &&
    state.mapAttached === false &&
    state.drawingEnabled === false &&
    state.startupWired === false &&
    state.layerCount === 0 &&
    Array.isArray(state.layers) &&
    state.lifecycleStatus === "state-container-created" &&
    state.implementationAuthorized === false &&
    state.initializationAuthorized === false &&
    state.drawingAuthorized === false &&
    state.gameplayChanged === false &&
    state.backendStorageNetworkChanged === false;

  if (
    !phase423MutationDetected ||
    !phase423MutationFieldsValid ||
    !phase426MutationDetected ||
    !phase426MutationFieldsValid ||
    !phase429FieldContractDetected ||
    !phase429FieldContractFieldsValid ||
    !protectedFieldsStillInert
  ) {
    return {
      ok: false,
      phase: 431,
      helperName:
        "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateFieldContractNextReadinessHelperAvailable,
      stateContainerExists: true,
      phase423MutationDetected,
      phase423MutationFieldsValid,
      phase426MutationDetected,
      phase426MutationFieldsValid,
      phase429FieldContractDetected,
      phase429FieldContractFieldsValid,
      protectedFieldsStillInert,
      mutationApplied: false,
      manualRendererFieldContractValidationApplied: false,
      manualRendererFieldContractValidationPhase: null,
      manualRendererFieldContractValidationShellOnly: false,
      manualRendererFieldContractValidationStatus: null,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  state.manualRendererFieldContractValidationApplied = true;
  state.manualRendererFieldContractValidationPhase = 431;
  state.manualRendererFieldContractValidationShellOnly = true;
  state.manualRendererFieldContractValidationStatus =
    "state-field-contract-validation-shell-applied";

  return {
    ok: true,
    phase: 431,
    helperName:
      "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateFieldContractNextReadinessHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected: true,
    phase423MutationFieldsValid: true,
    phase426MutationDetected: true,
    phase426MutationFieldsValid: true,
    phase429FieldContractDetected: true,
    phase429FieldContractFieldsValid: true,
    protectedFieldsStillInert: true,
    mutationApplied: true,
    manualRendererFieldContractValidationApplied: true,
    manualRendererFieldContractValidationPhase: 431,
    manualRendererFieldContractValidationShellOnly: true,
    manualRendererFieldContractValidationStatus:
      "state-field-contract-validation-shell-applied",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "passiveOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const validationShellHelperAvailable =
    typeof applyCustom25DVisualManualRendererStateFieldContractValidationShell ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const phase423MutationDetected =
    !!(
      state &&
      state.manualStateMutationApplied === true &&
      state.manualStateMutationPhase === 423
    );
  const phase423MutationFieldsValid =
    !!(
      state &&
      state.manualStateMutationApplied === true &&
      state.manualStateMutationPhase === 423 &&
      state.manualStateMutationShellOnly === true &&
      typeof state.manualStateMutationCount === "number" &&
      state.manualStateMutationCount >= 1 &&
      state.lastManualStateMutationStatus === "state-mutation-shell-applied"
    );
  const phase426MutationDetected =
    !!(
      state &&
      state.manualSecondStateMutationApplied === true &&
      state.manualSecondStateMutationPhase === 426
    );
  const phase426MutationFieldsValid =
    !!(
      state &&
      state.manualSecondStateMutationApplied === true &&
      state.manualSecondStateMutationPhase === 426 &&
      state.manualSecondStateMutationShellOnly === true &&
      typeof state.manualSecondStateMutationCount === "number" &&
      state.manualSecondStateMutationCount >= 1 &&
      state.lastManualSecondStateMutationStatus ===
        "second-state-mutation-shell-applied"
    );
  const phase429FieldContractDetected =
    !!(
      state &&
      state.manualRendererFieldContractApplied === true &&
      state.manualRendererFieldContractPhase === 429
    );
  const phase429FieldContractFieldsValid =
    !!(
      state &&
      state.manualRendererFieldContractApplied === true &&
      state.manualRendererFieldContractPhase === 429 &&
      state.manualRendererFieldContractShellOnly === true &&
      state.manualRendererFieldContractStatus ===
        "state-field-contract-shell-applied"
    );
  const phase431FieldContractValidationDetected =
    !!(
      state &&
      state.manualRendererFieldContractValidationApplied === true &&
      state.manualRendererFieldContractValidationPhase === 431
    );
  const phase431FieldContractValidationFieldsValid =
    !!(
      state &&
      state.manualRendererFieldContractValidationApplied === true &&
      state.manualRendererFieldContractValidationPhase === 431 &&
      state.manualRendererFieldContractValidationShellOnly === true &&
      state.manualRendererFieldContractValidationStatus ===
        "state-field-contract-validation-shell-applied"
    );
  const protectedFieldsStillInert =
    !!(
      state &&
      state.initialized === false &&
      state.rendererInstance === null &&
      state.rendererCreated === false &&
      state.rendererInitialized === false &&
      state.rendererStarted === false &&
      state.mapAttached === false &&
      state.drawingEnabled === false &&
      state.startupWired === false &&
      state.gameplayChanged === false &&
      state.backendStorageNetworkChanged === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 432,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      phase431FieldContractValidationDetected: false,
      phase431FieldContractValidationFieldsValid: false,
      protectedFieldsStillInert: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      closeoutStatus: "blocked",
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExists) {
    return {
      ok: false,
      phase: 432,
      helperName:
        "getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExists: false,
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      phase431FieldContractValidationDetected: false,
      phase431FieldContractValidationFieldsValid: false,
      protectedFieldsStillInert: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      closeoutStatus: "state-container-missing",
      defaultDecision: "blocked"
    };
  }

  return {
    ok:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      phase431FieldContractValidationFieldsValid &&
      protectedFieldsStillInert,
    phase: 432,
    helperName:
      "getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    validationShellHelperAvailable,
    stateContainerExists: true,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    phase429FieldContractDetected,
    phase429FieldContractFieldsValid,
    phase431FieldContractValidationDetected,
    phase431FieldContractValidationFieldsValid,
    protectedFieldsStillInert,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    closeoutStatus:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      phase431FieldContractValidationFieldsValid &&
      protectedFieldsStillInert
        ? "state-field-contract-validation-closed-out"
        : "state-field-contract-validation-needs-review",
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererPostFieldContractReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "passiveOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase432CloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport ===
    "function";
  const closeoutReport = phase432CloseoutHelperAvailable
    ? getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 432,
      helperName:
        "getCustom25DVisualManualRendererPostFieldContractReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase432CloseoutHelperAvailable,
      stateContainerExists:
        !!(closeoutReport && closeoutReport.stateContainerExists === true),
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      phase431FieldContractValidationDetected: false,
      phase431FieldContractValidationFieldsValid: false,
      protectedFieldsStillInert: false,
      plannedNextPhase: 433,
      plannedNextMilestone: "manual-renderer-instance-shell-readiness",
      plannedNextStepAllowedNow: false,
      plannedNextStepRequiresSeparatePhase: true,
      plannedNextStepMustStayInert: true,
      plannedNextStepMustNotCreateRendererInstanceYet: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      readinessStatus: "blocked",
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(closeoutReport && closeoutReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(closeoutReport && closeoutReport.phase423MutationDetected === true);
  const phase423MutationFieldsValid =
    !!(closeoutReport && closeoutReport.phase423MutationFieldsValid === true);
  const phase426MutationDetected =
    !!(closeoutReport && closeoutReport.phase426MutationDetected === true);
  const phase426MutationFieldsValid =
    !!(closeoutReport && closeoutReport.phase426MutationFieldsValid === true);
  const phase429FieldContractDetected =
    !!(closeoutReport && closeoutReport.phase429FieldContractDetected === true);
  const phase429FieldContractFieldsValid =
    !!(closeoutReport && closeoutReport.phase429FieldContractFieldsValid === true);
  const phase431FieldContractValidationDetected =
    !!(closeoutReport && closeoutReport.phase431FieldContractValidationDetected === true);
  const phase431FieldContractValidationFieldsValid =
    !!(closeoutReport && closeoutReport.phase431FieldContractValidationFieldsValid === true);
  const protectedFieldsStillInert =
    !!(closeoutReport && closeoutReport.protectedFieldsStillInert === true);

  return {
    ok:
      phase432CloseoutHelperAvailable &&
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      phase431FieldContractValidationFieldsValid &&
      protectedFieldsStillInert,
    phase: 432,
    helperName:
      "getCustom25DVisualManualRendererPostFieldContractReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase432CloseoutHelperAvailable,
    stateContainerExists,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    phase429FieldContractDetected,
    phase429FieldContractFieldsValid,
    phase431FieldContractValidationDetected,
    phase431FieldContractValidationFieldsValid,
    protectedFieldsStillInert,
    plannedNextPhase: 433,
    plannedNextMilestone: "manual-renderer-instance-shell-readiness",
    plannedNextStepAllowedNow: false,
    plannedNextStepRequiresSeparatePhase: true,
    plannedNextStepMustStayInert: true,
    plannedNextStepMustNotCreateRendererInstanceYet: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    readinessStatus:
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      phase431FieldContractValidationFieldsValid &&
      protectedFieldsStillInert
        ? "post-field-contract-ready-for-renderer-instance-planning"
        : "post-field-contract-readiness-needs-review",
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "passiveOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomChange",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noStorageChanges",
    "noPersistence",
    "noNetwork",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase432ReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererPostFieldContractReadinessReport ===
    "function";
  const readinessReport = phase432ReadinessHelperAvailable
    ? getCustom25DVisualManualRendererPostFieldContractReadinessReport(options)
    : null;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 432,
      helperName:
        "getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase432ReadinessHelperAvailable,
      stateContainerExists:
        !!(readinessReport && readinessReport.stateContainerExists === true),
      phase423MutationDetected: false,
      phase423MutationFieldsValid: false,
      phase426MutationDetected: false,
      phase426MutationFieldsValid: false,
      phase429FieldContractDetected: false,
      phase429FieldContractFieldsValid: false,
      phase431FieldContractValidationDetected: false,
      phase431FieldContractValidationFieldsValid: false,
      protectedFieldsStillInert: false,
      transitionFrom: "manual-state-field-contract-foundation",
      transitionTo: "manual-renderer-instance-shell-foundation",
      nextExcitingBoundary: "renderer-instance-shell-planning",
      rendererInstanceCreationAllowedNow: false,
      rendererInitializationAllowedNow: false,
      mapAttachmentAllowedNow: false,
      drawingAllowedNow: false,
      startupWiringAllowedNow: false,
      gameplayChangeAllowedNow: false,
      recommendedNextPhase: 433,
      recommendedNextPhaseType: "passive-readiness-or-inert-shell-only",
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      transitionStatus: "blocked",
      defaultDecision: "blocked"
    };
  }

  const stateContainerExists =
    !!(readinessReport && readinessReport.stateContainerExists === true);
  const phase423MutationDetected =
    !!(readinessReport && readinessReport.phase423MutationDetected === true);
  const phase423MutationFieldsValid =
    !!(readinessReport && readinessReport.phase423MutationFieldsValid === true);
  const phase426MutationDetected =
    !!(readinessReport && readinessReport.phase426MutationDetected === true);
  const phase426MutationFieldsValid =
    !!(readinessReport && readinessReport.phase426MutationFieldsValid === true);
  const phase429FieldContractDetected =
    !!(readinessReport && readinessReport.phase429FieldContractDetected === true);
  const phase429FieldContractFieldsValid =
    !!(readinessReport && readinessReport.phase429FieldContractFieldsValid === true);
  const phase431FieldContractValidationDetected =
    !!(readinessReport && readinessReport.phase431FieldContractValidationDetected === true);
  const phase431FieldContractValidationFieldsValid =
    !!(readinessReport && readinessReport.phase431FieldContractValidationFieldsValid === true);
  const protectedFieldsStillInert =
    !!(readinessReport && readinessReport.protectedFieldsStillInert === true);

  return {
    ok:
      phase432ReadinessHelperAvailable &&
      phase423MutationFieldsValid &&
      phase426MutationFieldsValid &&
      phase429FieldContractFieldsValid &&
      phase431FieldContractValidationFieldsValid &&
      protectedFieldsStillInert,
    phase: 432,
    helperName:
      "getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase432ReadinessHelperAvailable,
    stateContainerExists,
    phase423MutationDetected,
    phase423MutationFieldsValid,
    phase426MutationDetected,
    phase426MutationFieldsValid,
    phase429FieldContractDetected,
    phase429FieldContractFieldsValid,
    phase431FieldContractValidationDetected,
    phase431FieldContractValidationFieldsValid,
    protectedFieldsStillInert,
    transitionFrom: "manual-state-field-contract-foundation",
    transitionTo: "manual-renderer-instance-shell-foundation",
    nextExcitingBoundary: "renderer-instance-shell-planning",
    rendererInstanceCreationAllowedNow: false,
    rendererInitializationAllowedNow: false,
    mapAttachmentAllowedNow: false,
    drawingAllowedNow: false,
    startupWiringAllowedNow: false,
    gameplayChangeAllowedNow: false,
    recommendedNextPhase: 433,
    recommendedNextPhaseType: "passive-readiness-or-inert-shell-only",
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    transitionStatus: "post-field-contract-transition-plan-documented",
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInstanceShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const phase423MutationHelperAvailable =
    typeof applyCustom25DVisualManualRendererStateMutationShell === "function";
  const phase426MutationHelperAvailable =
    typeof applyCustom25DVisualManualRendererSecondStateMutationShell ===
    "function";
  const phase429FieldContractHelperAvailable =
    typeof applyCustom25DVisualManualRendererStateFieldContractShell ===
    "function";
  const phase431FieldContractValidationHelperAvailable =
    typeof applyCustom25DVisualManualRendererStateFieldContractValidationShell ===
    "function";
  const phase432CloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport ===
    "function";
  const phase432ReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererPostFieldContractReadinessReport ===
    "function";
  const phase432TransitionPlanHelperAvailable =
    typeof getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 433,
      helperName:
        "getCustom25DVisualManualRendererInstanceShellReadinessReport",
      passive: true,
      reportOnly: true,
      planningOnly: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      phase423MutationHelperAvailable,
      phase426MutationHelperAvailable,
      phase429FieldContractHelperAvailable,
      phase431FieldContractValidationHelperAvailable,
      phase432CloseoutHelperAvailable,
      phase432ReadinessHelperAvailable,
      phase432TransitionPlanHelperAvailable,
      readinessStatus: "blocked",
      plannedNextMilestone: "manual-renderer-instance-shell-foundation",
      plannedNextPhase: 434,
      plannedNextStepAllowedNow: false,
      plannedNextStepRequiresSeparatePhase: true,
      plannedNextStepMustStayInert: true,
      rendererInstanceCreationAllowedNow: false,
      rendererInitializationAllowedNow: false,
      rendererStartAllowedNow: false,
      mapAttachmentAllowedNow: false,
      drawingAllowedNow: false,
      startupWiringAllowedNow: false,
      gameplayChangeAllowedNow: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 433,
    helperName: "getCustom25DVisualManualRendererInstanceShellReadinessReport",
    passive: true,
    reportOnly: true,
    planningOnly: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    phase423MutationHelperAvailable,
    phase426MutationHelperAvailable,
    phase429FieldContractHelperAvailable,
    phase431FieldContractValidationHelperAvailable,
    phase432CloseoutHelperAvailable,
    phase432ReadinessHelperAvailable,
    phase432TransitionPlanHelperAvailable,
    readinessStatus: "manual-renderer-instance-shell-readiness-planned",
    plannedNextMilestone: "manual-renderer-instance-shell-foundation",
    plannedNextPhase: 434,
    plannedNextStepAllowedNow: false,
    plannedNextStepRequiresSeparatePhase: true,
    plannedNextStepMustStayInert: true,
    rendererInstanceCreationAllowedNow: false,
    rendererInitializationAllowedNow: false,
    rendererStartAllowedNow: false,
    mapAttachmentAllowedNow: false,
    drawingAllowedNow: false,
    startupWiringAllowedNow: false,
    gameplayChangeAllowedNow: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase433ReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererInstanceShellReadinessReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 433,
      helperName:
        "getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport",
      passive: true,
      reportOnly: true,
      planningOnly: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase433ReadinessHelperAvailable,
      riskBoundaryStatus: "blocked",
      currentPhase: 433,
      nextRiskBoundaryPhase: 434,
      nextExcitingBoundary: "renderer-instance-shell-foundation",
      futureShellMayDefineRendererInstanceShape: true,
      futureShellMustStayInert: true,
      futureShellMustNotInitializeRenderer: true,
      futureShellMustNotStartRenderer: true,
      futureShellMustNotAttachMap: true,
      futureShellMustNotDraw: true,
      futureShellMustNotWireStartup: true,
      futureShellMustNotChangeGameplay: true,
      futureShellRequiresSeparatePhase: true,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 433,
    helperName: "getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport",
    passive: true,
    reportOnly: true,
    planningOnly: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase433ReadinessHelperAvailable,
    riskBoundaryStatus: "renderer-instance-shell-boundary-documented",
    currentPhase: 433,
    nextRiskBoundaryPhase: 434,
    nextExcitingBoundary: "renderer-instance-shell-foundation",
    futureShellMayDefineRendererInstanceShape: true,
    futureShellMustStayInert: true,
    futureShellMustNotInitializeRenderer: true,
    futureShellMustNotStartRenderer: true,
    futureShellMustNotAttachMap: true,
    futureShellMustNotDraw: true,
    futureShellMustNotWireStartup: true,
    futureShellMustNotChangeGameplay: true,
    futureShellRequiresSeparatePhase: true,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    defaultDecision: "blocked"
  };
}

function createCustom25DVisualManualRendererInstanceShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualManualRendererInstanceShellCreation",
    "confirmManualRendererInstanceShellCreationOnly",
    "confirmExistingManualStateContainerOnly",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExistsBefore =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 434,
      helperName: "createCustom25DVisualManualRendererInstanceShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      defaultDecision: "blocked",
      jsonSafeResult: true,
      manualOnly: true,
      localDevOnly: true,
      existingStateContainerRequired: true,
      stateContainerExistsBefore,
      stateContainerExistsAfter: stateContainerExistsBefore,
      stateContainerReplaced: false,
      rendererInstanceShellCreated: false,
      rendererInstanceShellAlreadyExisted: false,
      rendererInstanceShellExistsAfter: false,
      rendererInstanceShellCreatedByThisCall: false,
      rendererInstanceShellInert: true,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      domMutated: false,
      leafletMutated: false,
      runtimeBehaviorChanged: false
    };
  }

  if (!stateContainerExistsBefore) {
    return {
      ok: false,
      phase: 434,
      helperName: "createCustom25DVisualManualRendererInstanceShell",
      allowed: false,
      blocked: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      defaultDecision: "blocked",
      jsonSafeResult: true,
      manualOnly: true,
      localDevOnly: true,
      existingStateContainerRequired: true,
      stateContainerExistsBefore: false,
      stateContainerExistsAfter: false,
      stateContainerReplaced: false,
      rendererInstanceShellCreated: false,
      rendererInstanceShellAlreadyExisted: false,
      rendererInstanceShellExistsAfter: false,
      rendererInstanceShellCreatedByThisCall: false,
      rendererInstanceShellInert: true,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      domMutated: false,
      leafletMutated: false,
      runtimeBehaviorChanged: false
    };
  }

  const state = window.custom25DVisualManualRendererState;
  const rendererInstanceShellAlreadyExisted =
    !!(state && typeof state.rendererInstanceShell === "object" && state.rendererInstanceShell);
  let rendererInstanceShellCreatedByThisCall = false;

  if (!rendererInstanceShellAlreadyExisted) {
    state.rendererInstanceShell = {
      phaseCreated: 434,
      shellType: "manual-renderer-instance-shell",
      inert: true,
      initialized: false,
      started: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      createdAtManualOnly: true
    };
    rendererInstanceShellCreatedByThisCall = true;
  }

  const rendererInstanceShellExistsAfter =
    !!(
      state &&
      typeof state.rendererInstanceShell === "object" &&
      state.rendererInstanceShell
    );
  const rendererInstanceShellInert =
    !!(
      rendererInstanceShellExistsAfter &&
      state.rendererInstanceShell.inert === true &&
      state.rendererInstanceShell.initialized === false &&
      state.rendererInstanceShell.started === false &&
      state.rendererInstanceShell.mapAttached === false &&
      state.rendererInstanceShell.drawingStarted === false &&
      state.rendererInstanceShell.startupWired === false &&
      state.rendererInstanceShell.gameplayChanged === false &&
      state.rendererInstanceShell.backendStorageNetworkChanged === false &&
      state.rendererInstanceShell.createdAtManualOnly === true
    );

  return {
    ok: true,
    phase: 434,
    helperName: "createCustom25DVisualManualRendererInstanceShell",
    allowed: true,
    blocked: false,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    defaultDecision: "blocked",
    jsonSafeResult: true,
    manualOnly: true,
    localDevOnly: true,
    existingStateContainerRequired: true,
    stateContainerExistsBefore: true,
    stateContainerExistsAfter:
      typeof window.custom25DVisualManualRendererState !== "undefined",
    stateContainerReplaced: false,
    rendererInstanceShellCreated: rendererInstanceShellCreatedByThisCall,
    rendererInstanceShellAlreadyExisted,
    rendererInstanceShellExistsAfter,
    rendererInstanceShellCreatedByThisCall,
    rendererInstanceShellInert,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    domMutated: false,
    leafletMutated: false,
    runtimeBehaviorChanged: false
  };
}

function createCustom25DVisualManualRendererLifecycleMethodShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualManualRendererLifecycleMethodShellCreation",
    "confirmManualLifecycleMethodShellCreationOnly",
    "confirmExistingManualStateContainerOnly",
    "confirmExistingRendererInstanceShellOnly",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoRendererInstanceShellReplacement",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExistsBefore =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExistsBefore
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShellExistsBefore =
    !!(
      state &&
      typeof state.rendererInstanceShell === "object" &&
      state.rendererInstanceShell
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 437,
      helperName: "createCustom25DVisualManualRendererLifecycleMethodShell",
      allowed: false,
      blocked: true,
      failedRequirement: missingRequiredOptionKeys[0] || null,
      reason: "missing-required-options",
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExistsBefore,
      stateContainerExistsAfter: stateContainerExistsBefore,
      rendererInstanceShellExistsBefore,
      rendererInstanceShellExistsAfter: rendererInstanceShellExistsBefore,
      lifecycleMethodShellCreated: false,
      lifecycleMethodShellCreatedByThisCall: false,
      lifecycleMethodShellExistsAfter: !!(
        state &&
        state.rendererInstanceShell &&
        state.rendererInstanceShell.lifecycleMethodShell
      ),
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  if (!stateContainerExistsBefore) {
    return {
      ok: false,
      phase: 437,
      helperName: "createCustom25DVisualManualRendererLifecycleMethodShell",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmExistingManualStateContainerOnly",
      reason: "state-container-missing",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExistsBefore: false,
      stateContainerExistsAfter: false,
      rendererInstanceShellExistsBefore: false,
      rendererInstanceShellExistsAfter: false,
      lifecycleMethodShellCreated: false,
      lifecycleMethodShellCreatedByThisCall: false,
      lifecycleMethodShellExistsAfter: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  if (!rendererInstanceShellExistsBefore) {
    return {
      ok: false,
      phase: 437,
      helperName: "createCustom25DVisualManualRendererLifecycleMethodShell",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmExistingRendererInstanceShellOnly",
      reason: "renderer-instance-shell-missing",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExistsBefore: true,
      stateContainerExistsAfter: true,
      rendererInstanceShellExistsBefore: false,
      rendererInstanceShellExistsAfter: false,
      lifecycleMethodShellCreated: false,
      lifecycleMethodShellCreatedByThisCall: false,
      lifecycleMethodShellExistsAfter: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  const rendererInstanceShell = state.rendererInstanceShell;
  const rendererInstanceShellInert =
    !!(
      rendererInstanceShell &&
      rendererInstanceShell.inert === true &&
      rendererInstanceShell.initialized === false &&
      rendererInstanceShell.started === false &&
      rendererInstanceShell.mapAttached === false &&
      rendererInstanceShell.drawingStarted === false &&
      rendererInstanceShell.startupWired === false &&
      rendererInstanceShell.gameplayChanged === false &&
      rendererInstanceShell.backendStorageNetworkChanged === false
    );

  if (!rendererInstanceShellInert) {
    return {
      ok: false,
      phase: 437,
      helperName: "createCustom25DVisualManualRendererLifecycleMethodShell",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmNoRuntimeBehaviorChange",
      reason: "renderer-instance-shell-not-inert",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      stateContainerExistsBefore: true,
      stateContainerExistsAfter: true,
      rendererInstanceShellExistsBefore: true,
      rendererInstanceShellExistsAfter: true,
      lifecycleMethodShellCreated: false,
      lifecycleMethodShellCreatedByThisCall: false,
      lifecycleMethodShellExistsAfter: !!rendererInstanceShell.lifecycleMethodShell,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  const lifecycleMethodShellAlreadyExisted =
    !!(
      rendererInstanceShell &&
      typeof rendererInstanceShell.lifecycleMethodShell === "object" &&
      rendererInstanceShell.lifecycleMethodShell
    );
  let lifecycleMethodShellCreatedByThisCall = false;

  if (!lifecycleMethodShellAlreadyExisted) {
    rendererInstanceShell.lifecycleMethodShell = {
      phaseCreated: 437,
      shellType: "manual-renderer-lifecycle-method-shell",
      inert: true,
      manualOnly: true,
      callableMethodsCreated: false,
      initializeCreated: false,
      startCreated: false,
      stopCreated: false,
      attachMapCreated: false,
      drawFrameCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false,
      createdAtManualOnly: true
    };
    lifecycleMethodShellCreatedByThisCall = true;
  }

  const lifecycleMethodShell = rendererInstanceShell.lifecycleMethodShell;
  const lifecycleMethodShellExistsAfter = !!lifecycleMethodShell;
  const lifecycleMethodShellInert =
    !!(
      lifecycleMethodShell &&
      lifecycleMethodShell.phaseCreated === 437 &&
      lifecycleMethodShell.shellType === "manual-renderer-lifecycle-method-shell" &&
      lifecycleMethodShell.inert === true &&
      lifecycleMethodShell.manualOnly === true &&
      lifecycleMethodShell.callableMethodsCreated === false &&
      lifecycleMethodShell.initializeCreated === false &&
      lifecycleMethodShell.startCreated === false &&
      lifecycleMethodShell.stopCreated === false &&
      lifecycleMethodShell.attachMapCreated === false &&
      lifecycleMethodShell.drawFrameCreated === false &&
      lifecycleMethodShell.rendererInitialized === false &&
      lifecycleMethodShell.rendererStarted === false &&
      lifecycleMethodShell.rendererStopped === false &&
      lifecycleMethodShell.mapAttached === false &&
      lifecycleMethodShell.drawingStarted === false &&
      lifecycleMethodShell.startupWired === false &&
      lifecycleMethodShell.domMutated === false &&
      lifecycleMethodShell.leafletMutated === false &&
      lifecycleMethodShell.gameplayChanged === false &&
      lifecycleMethodShell.backendStorageNetworkChanged === false &&
      lifecycleMethodShell.persistenceChanged === false &&
      lifecycleMethodShell.runtimeBehaviorChanged === false &&
      lifecycleMethodShell.createdAtManualOnly === true
    );

  return {
    ok: true,
    phase: 437,
    helperName: "createCustom25DVisualManualRendererLifecycleMethodShell",
    allowed: true,
    blocked: false,
    manualOnly: true,
    localDevOnly: true,
    jsonSafeResult: true,
    defaultDecision: "blocked",
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExistsBefore: true,
    stateContainerExistsAfter: true,
    stateContainerReplaced: false,
    rendererInstanceShellExistsBefore: true,
    rendererInstanceShellExistsAfter: true,
    rendererInstanceShellReplaced: false,
    rendererInstanceShellInert,
    lifecycleMethodShellAlreadyExisted,
    lifecycleMethodShellCreated: lifecycleMethodShellCreatedByThisCall,
    lifecycleMethodShellCreatedByThisCall,
    lifecycleMethodShellExistsAfter,
    lifecycleMethodShellInert,
    callableLifecycleMethodsCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    rendererStopped: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    domMutated: false,
    leafletMutated: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    persistenceChanged: false,
    runtimeBehaviorChanged: false,
    plannedNextPhase: 438,
    plannedNextMilestone: "manual-renderer-lifecycle-method-shell-closeout"
  };
}

function createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundationCreation",
    "confirmCallableLifecycleMethodPlaceholderFoundationOnly",
    "confirmExistingManualStateContainerOnly",
    "confirmExistingRendererInstanceShellOnly",
    "confirmExistingLifecycleMethodShellOnly",
    "confirmNoStateContainerCreation",
    "confirmNoStateContainerReplacement",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoRendererInstanceShellReplacement",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoLifecycleMethodShellReplacement",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExistsBefore =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExistsBefore
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShell = state && state.rendererInstanceShell
    ? state.rendererInstanceShell
    : null;
  const rendererInstanceShellExistsBefore = !!rendererInstanceShell;
  const lifecycleMethodShell = rendererInstanceShell && rendererInstanceShell.lifecycleMethodShell
    ? rendererInstanceShell.lifecycleMethodShell
    : null;
  const lifecycleMethodShellExistsBefore = !!lifecycleMethodShell;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 440,
      helperName:
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
      allowed: false,
      blocked: true,
      failedRequirement: missingRequiredOptionKeys[0] || null,
      reason: "missing-required-options",
      requiredOptionKeys,
      missingRequiredOptionKeys,
      jsonSafeResult: true,
      stateContainerExistsBefore,
      stateContainerExistsAfter: stateContainerExistsBefore,
      stateContainerReplaced: false,
      rendererInstanceShellExistsBefore,
      rendererInstanceShellExistsAfter: rendererInstanceShellExistsBefore,
      rendererInstanceShellReplaced: false,
      lifecycleMethodShellExistsBefore,
      lifecycleMethodShellExistsAfter: lifecycleMethodShellExistsBefore,
      lifecycleMethodShellReplaced: false,
      placeholderMethodsCreated: false,
      initializePlaceholderCreated: false,
      startPlaceholderCreated: false,
      stopPlaceholderCreated: false,
      attachMapPlaceholderCreated: false,
      drawFramePlaceholderCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      startupWired: false,
      callbacksHooksTimersListenersAdded: false,
      runtimeBehaviorChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!stateContainerExistsBefore) {
    return {
      ok: false,
      phase: 440,
      helperName:
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmExistingManualStateContainerOnly",
      reason: "state-container-missing",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      jsonSafeResult: true,
      stateContainerExistsBefore: false,
      stateContainerExistsAfter: false,
      stateContainerReplaced: false,
      rendererInstanceShellExistsBefore: false,
      rendererInstanceShellExistsAfter: false,
      rendererInstanceShellReplaced: false,
      lifecycleMethodShellExistsBefore: false,
      lifecycleMethodShellExistsAfter: false,
      lifecycleMethodShellReplaced: false,
      placeholderMethodsCreated: false,
      initializePlaceholderCreated: false,
      startPlaceholderCreated: false,
      stopPlaceholderCreated: false,
      attachMapPlaceholderCreated: false,
      drawFramePlaceholderCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      startupWired: false,
      callbacksHooksTimersListenersAdded: false,
      runtimeBehaviorChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!rendererInstanceShellExistsBefore) {
    return {
      ok: false,
      phase: 440,
      helperName:
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmExistingRendererInstanceShellOnly",
      reason: "renderer-instance-shell-missing",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      jsonSafeResult: true,
      stateContainerExistsBefore: true,
      stateContainerExistsAfter: true,
      stateContainerReplaced: false,
      rendererInstanceShellExistsBefore: false,
      rendererInstanceShellExistsAfter: false,
      rendererInstanceShellReplaced: false,
      lifecycleMethodShellExistsBefore: false,
      lifecycleMethodShellExistsAfter: false,
      lifecycleMethodShellReplaced: false,
      placeholderMethodsCreated: false,
      initializePlaceholderCreated: false,
      startPlaceholderCreated: false,
      stopPlaceholderCreated: false,
      attachMapPlaceholderCreated: false,
      drawFramePlaceholderCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      startupWired: false,
      callbacksHooksTimersListenersAdded: false,
      runtimeBehaviorChanged: false,
      defaultDecision: "blocked"
    };
  }

  if (!lifecycleMethodShellExistsBefore) {
    return {
      ok: false,
      phase: 440,
      helperName:
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmExistingLifecycleMethodShellOnly",
      reason: "lifecycle-method-shell-missing",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      jsonSafeResult: true,
      stateContainerExistsBefore: true,
      stateContainerExistsAfter: true,
      stateContainerReplaced: false,
      rendererInstanceShellExistsBefore: true,
      rendererInstanceShellExistsAfter: true,
      rendererInstanceShellReplaced: false,
      lifecycleMethodShellExistsBefore: false,
      lifecycleMethodShellExistsAfter: false,
      lifecycleMethodShellReplaced: false,
      placeholderMethodsCreated: false,
      initializePlaceholderCreated: false,
      startPlaceholderCreated: false,
      stopPlaceholderCreated: false,
      attachMapPlaceholderCreated: false,
      drawFramePlaceholderCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      startupWired: false,
      callbacksHooksTimersListenersAdded: false,
      runtimeBehaviorChanged: false,
      defaultDecision: "blocked"
    };
  }

  const lifecycleMethodShellInert =
    !!(
      lifecycleMethodShell &&
      lifecycleMethodShell.inert === true &&
      lifecycleMethodShell.manualOnly === true &&
      lifecycleMethodShell.rendererInitialized === false &&
      lifecycleMethodShell.rendererStarted === false &&
      lifecycleMethodShell.rendererStopped === false &&
      lifecycleMethodShell.mapAttached === false &&
      lifecycleMethodShell.drawingStarted === false &&
      lifecycleMethodShell.startupWired === false &&
      lifecycleMethodShell.domMutated === false &&
      lifecycleMethodShell.leafletMutated === false &&
      lifecycleMethodShell.gameplayChanged === false &&
      lifecycleMethodShell.backendStorageNetworkChanged === false &&
      lifecycleMethodShell.persistenceChanged === false &&
      lifecycleMethodShell.runtimeBehaviorChanged === false
    );

  if (!lifecycleMethodShellInert) {
    return {
      ok: false,
      phase: 440,
      helperName:
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
      allowed: false,
      blocked: true,
      failedRequirement: "confirmNoRuntimeBehaviorChange",
      reason: "lifecycle-method-shell-not-inert",
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      jsonSafeResult: true,
      stateContainerExistsBefore: true,
      stateContainerExistsAfter: true,
      stateContainerReplaced: false,
      rendererInstanceShellExistsBefore: true,
      rendererInstanceShellExistsAfter: true,
      rendererInstanceShellReplaced: false,
      lifecycleMethodShellExistsBefore: true,
      lifecycleMethodShellExistsAfter: true,
      lifecycleMethodShellReplaced: false,
      placeholderMethodsCreated: false,
      initializePlaceholderCreated: false,
      startPlaceholderCreated: false,
      stopPlaceholderCreated: false,
      attachMapPlaceholderCreated: false,
      drawFramePlaceholderCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      startupWired: false,
      callbacksHooksTimersListenersAdded: false,
      runtimeBehaviorChanged: false,
      defaultDecision: "blocked"
    };
  }

  const invocationRequiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualCallableLifecycleMethodPlaceholderInvocation",
    "confirmInertCallableLifecycleMethodPlaceholderOnly",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];

  function createInertPlaceholderMethod(methodName) {
    return function custom25DVisualManualRendererCallableLifecycleMethodPlaceholder(
      invocationOptions = {}
    ) {
      const missingInvocationOptionKeys = invocationRequiredOptionKeys.filter(
        (key) => invocationOptions[key] !== true
      );

      if (missingInvocationOptionKeys.length > 0) {
        return {
          ok: false,
          phase: 440,
          methodName,
          allowed: false,
          blocked: true,
          jsonSafeResult: true,
          manualOnly: true,
          localDevOnly: true,
          browserConsoleOnly: invocationOptions.browserConsoleOnly === true,
          explicitOptionsOnly: invocationOptions.explicitOptionsOnly === true,
          requiredOptionKeys: invocationRequiredOptionKeys,
          missingRequiredOptionKeys: missingInvocationOptionKeys,
          rendererInitialized: false,
          rendererStarted: false,
          rendererStopped: false,
          mapAttached: false,
          drawingStarted: false,
          domMutated: false,
          leafletMutated: false,
          gameplayChanged: false,
          backendStorageNetworkChanged: false,
          startupWired: false,
          callbacksHooksTimersListenersAdded: false,
          defaultDecision: "blocked"
        };
      }

      return {
        ok: true,
        phase: 440,
        methodName,
        allowed: true,
        blocked: false,
        jsonSafeResult: true,
        manualOnly: true,
        localDevOnly: true,
        browserConsoleOnly: true,
        explicitOptionsOnly: true,
        requiredOptionKeys: invocationRequiredOptionKeys,
        missingRequiredOptionKeys: [],
        rendererInitialized: false,
        rendererStarted: false,
        rendererStopped: false,
        mapAttached: false,
        drawingStarted: false,
        domMutated: false,
        leafletMutated: false,
        gameplayChanged: false,
        backendStorageNetworkChanged: false,
        startupWired: false,
        callbacksHooksTimersListenersAdded: false,
        defaultDecision: "blocked"
      };
    };
  }

  const initializeAlreadyExisted =
    typeof lifecycleMethodShell.initialize === "function";
  const startAlreadyExisted =
    typeof lifecycleMethodShell.start === "function";
  const stopAlreadyExisted =
    typeof lifecycleMethodShell.stop === "function";
  const attachMapAlreadyExisted =
    typeof lifecycleMethodShell.attachMap === "function";
  const drawFrameAlreadyExisted =
    typeof lifecycleMethodShell.drawFrame === "function";

  if (!initializeAlreadyExisted) {
    lifecycleMethodShell.initialize = createInertPlaceholderMethod("initialize");
  }
  if (!startAlreadyExisted) {
    lifecycleMethodShell.start = createInertPlaceholderMethod("start");
  }
  if (!stopAlreadyExisted) {
    lifecycleMethodShell.stop = createInertPlaceholderMethod("stop");
  }
  if (!attachMapAlreadyExisted) {
    lifecycleMethodShell.attachMap = createInertPlaceholderMethod("attachMap");
  }
  if (!drawFrameAlreadyExisted) {
    lifecycleMethodShell.drawFrame = createInertPlaceholderMethod("drawFrame");
  }

  lifecycleMethodShell.callableMethodsCreated = true;
  lifecycleMethodShell.initializeCreated = true;
  lifecycleMethodShell.startCreated = true;
  lifecycleMethodShell.stopCreated = true;
  lifecycleMethodShell.attachMapCreated = true;
  lifecycleMethodShell.drawFrameCreated = true;

  const placeholderMethodsCreated =
    !initializeAlreadyExisted ||
    !startAlreadyExisted ||
    !stopAlreadyExisted ||
    !attachMapAlreadyExisted ||
    !drawFrameAlreadyExisted;

  return {
    ok: true,
    phase: 440,
    helperName:
      "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
    allowed: true,
    blocked: false,
    jsonSafeResult: true,
    defaultDecision: "blocked",
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExistsBefore: true,
    stateContainerExistsAfter: true,
    stateContainerReplaced: false,
    rendererInstanceShellExistsBefore: true,
    rendererInstanceShellExistsAfter: true,
    rendererInstanceShellReplaced: false,
    lifecycleMethodShellExistsBefore: true,
    lifecycleMethodShellExistsAfter: true,
    lifecycleMethodShellReplaced: false,
    placeholderMethodsCreated,
    initializePlaceholderCreated: !initializeAlreadyExisted,
    startPlaceholderCreated: !startAlreadyExisted,
    stopPlaceholderCreated: !stopAlreadyExisted,
    attachMapPlaceholderCreated: !attachMapAlreadyExisted,
    drawFramePlaceholderCreated: !drawFrameAlreadyExisted,
    rendererInitialized: false,
    rendererStarted: false,
    rendererStopped: false,
    mapAttached: false,
    drawingStarted: false,
    domMutated: false,
    leafletMutated: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    startupWired: false,
    callbacksHooksTimersListenersAdded: false,
    runtimeBehaviorChanged: false
  };
}

function getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "passiveOnly",
    "closeoutOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const expectedPlaceholderMethods = [
    "initialize",
    "start",
    "stop",
    "attachMap",
    "drawFrame"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const phase440HelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShell =
    state && state.rendererInstanceShell ? state.rendererInstanceShell : null;
  const lifecycleMethodShell =
    rendererInstanceShell && rendererInstanceShell.lifecycleMethodShell
      ? rendererInstanceShell.lifecycleMethodShell
      : null;
  const lifecycleMethodShellExists = !!lifecycleMethodShell;
  const presentPlaceholderMethods = lifecycleMethodShell
    ? expectedPlaceholderMethods.filter(
        (methodName) => typeof lifecycleMethodShell[methodName] === "function"
      )
    : [];
  const missingPlaceholderMethods = expectedPlaceholderMethods.filter(
    (methodName) => presentPlaceholderMethods.indexOf(methodName) === -1
  );
  const callableLifecyclePlaceholdersReady =
    lifecycleMethodShellExists &&
    missingPlaceholderMethods.length === 0 &&
    lifecycleMethodShell.inert === true &&
    lifecycleMethodShell.rendererInitialized === false &&
    lifecycleMethodShell.rendererStarted === false &&
    lifecycleMethodShell.rendererStopped === false &&
    lifecycleMethodShell.mapAttached === false &&
    lifecycleMethodShell.drawingStarted === false &&
    lifecycleMethodShell.startupWired === false &&
    lifecycleMethodShell.domMutated === false &&
    lifecycleMethodShell.leafletMutated === false &&
    lifecycleMethodShell.gameplayChanged === false &&
    lifecycleMethodShell.backendStorageNetworkChanged === false;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 441,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport",
      allowed: false,
      blocked: true,
      jsonSafeResult: true,
      reportOnly: true,
      passive: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase440HelperExists,
      lifecycleMethodShellExists,
      expectedPlaceholderMethods,
      presentPlaceholderMethods: [],
      missingPlaceholderMethods: expectedPlaceholderMethods.slice(),
      callableLifecyclePlaceholdersReady: false,
      noAutomaticInvocation: true,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      startupWired: false,
      callbacksHooksTimersListenersAdded: false,
      defaultDecision: "blocked"
    };
  }

  return {
    ok: true,
    phase: 441,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport",
    allowed: true,
    blocked: false,
    jsonSafeResult: true,
    reportOnly: true,
    passive: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase440HelperExists,
    lifecycleMethodShellExists,
    expectedPlaceholderMethods,
    presentPlaceholderMethods,
    missingPlaceholderMethods,
    callableLifecyclePlaceholdersReady,
    noAutomaticInvocation: true,
    rendererInitialized: false,
    rendererStarted: false,
    rendererStopped: false,
    mapAttached: false,
    drawingStarted: false,
    domMutated: false,
    leafletMutated: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    startupWired: false,
    callbacksHooksTimersListenersAdded: false,
    defaultDecision: "blocked"
  };
}

function getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange"
  ];
  const lifecyclePlaceholderNamesExpected = [
    "initialize",
    "start",
    "stop",
    "attachMap",
    "drawFrame"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const phase440FoundationExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const phase441CloseoutExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShell =
    state && state.rendererInstanceShell ? state.rendererInstanceShell : null;
  const lifecycleMethodShell =
    rendererInstanceShell && rendererInstanceShell.lifecycleMethodShell
      ? rendererInstanceShell.lifecycleMethodShell
      : null;
  const lifecycleMethodShellExists = !!lifecycleMethodShell;
  const lifecyclePlaceholderNamesFound = lifecycleMethodShell
    ? lifecyclePlaceholderNamesExpected.filter(
        (methodName) => typeof lifecycleMethodShell[methodName] !== "undefined"
      )
    : [];
  const missingPlaceholderMethods = lifecyclePlaceholderNamesExpected.filter(
    (methodName) => lifecyclePlaceholderNamesFound.indexOf(methodName) === -1
  );
  const allExpectedLifecyclePlaceholdersFound =
    missingPlaceholderMethods.length === 0;
  const lifecyclePlaceholdersCallable =
    lifecycleMethodShellExists &&
    lifecyclePlaceholderNamesExpected.every(
      (methodName) => typeof lifecycleMethodShell[methodName] === "function"
    );
  const lifecyclePlaceholdersRemainInert =
    !!(
      lifecycleMethodShell &&
      lifecycleMethodShell.inert === true &&
      lifecycleMethodShell.manualOnly === true &&
      lifecycleMethodShell.rendererInitialized === false &&
      lifecycleMethodShell.rendererStarted === false &&
      lifecycleMethodShell.rendererStopped === false &&
      lifecycleMethodShell.mapAttached === false &&
      lifecycleMethodShell.drawingStarted === false &&
      lifecycleMethodShell.startupWired === false &&
      lifecycleMethodShell.domMutated === false &&
      lifecycleMethodShell.leafletMutated === false &&
      lifecycleMethodShell.gameplayChanged === false &&
      lifecycleMethodShell.backendStorageNetworkChanged === false &&
      lifecycleMethodShell.persistenceChanged === false &&
      lifecycleMethodShell.runtimeBehaviorChanged === false
    );
  const readinessStatus =
    phase440FoundationExists &&
    phase441CloseoutExists &&
    lifecycleMethodShellExists &&
    allExpectedLifecyclePlaceholdersFound &&
    lifecyclePlaceholdersCallable &&
    lifecyclePlaceholdersRemainInert
      ? "callable-lifecycle-placeholder-readiness-documented"
      : "callable-lifecycle-placeholder-readiness-incomplete";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      allowed: false,
      blocked: true,
      phase: 442,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport",
      jsonSafeResult: true,
      reportOnly: true,
      passive: true,
      readinessStatus: "blocked",
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase440FoundationExists,
      phase441CloseoutExists,
      lifecycleMethodShellExists,
      lifecyclePlaceholderNamesExpected,
      lifecyclePlaceholderNamesFound: [],
      allExpectedLifecyclePlaceholdersFound: false,
      lifecyclePlaceholdersCallable: false,
      lifecyclePlaceholdersRemainInert: false,
      callableLifecycleInvocationAllowedNow: false,
      rendererInitializationAllowedNow: false,
      mapAttachmentAllowedNow: false,
      drawingAllowedNow: false,
      stateMutationAllowedNow: false,
      noDomLeafletGameplayBackendStorageNetworkChange: true,
      currentMilestone: "manual-renderer-callable-lifecycle-placeholder-closeout",
      nextMilestone: "manual-renderer-callable-lifecycle-placeholder-readiness"
    };
  }

  return {
    ok: true,
    allowed: true,
    blocked: false,
    phase: 442,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport",
    jsonSafeResult: true,
    reportOnly: true,
    passive: true,
    readinessStatus,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase440FoundationExists,
    phase441CloseoutExists,
    lifecycleMethodShellExists,
    lifecyclePlaceholderNamesExpected,
    lifecyclePlaceholderNamesFound,
    allExpectedLifecyclePlaceholdersFound,
    lifecyclePlaceholdersCallable,
    lifecyclePlaceholdersRemainInert,
    callableLifecycleInvocationAllowedNow: false,
    rendererInitializationAllowedNow: false,
    mapAttachmentAllowedNow: false,
    drawingAllowedNow: false,
    stateMutationAllowedNow: false,
    noDomLeafletGameplayBackendStorageNetworkChange: true,
    currentMilestone: "manual-renderer-callable-lifecycle-placeholder-closeout",
    nextMilestone: "manual-renderer-callable-lifecycle-placeholder-readiness"
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const plannedFutureControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const safetyNotes = [
    "Future controls remain manual-only and are not created in this phase.",
    "No lifecycle placeholder method is invoked in this phase.",
    "No DOM, Leaflet, gameplay, backend, storage, network, or startup behavior changes are introduced."
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const closeoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const readinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 443,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport",
      reportOnly: true,
      passive: true,
      allowed: false,
      blocked: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      closeoutHelperExists,
      readinessHelperExists,
      plannedManualControlBoundaryName:
        "custom25DVisualManualRendererCallableLifecycleManualControls",
      manualControlsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      manualControlPlanStatus: "blocked",
      plannedFutureControls,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 443,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport",
    reportOnly: true,
    passive: true,
    allowed: true,
    blocked: false,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    closeoutHelperExists,
    readinessHelperExists,
    plannedManualControlBoundaryName:
      "custom25DVisualManualRendererCallableLifecycleManualControls",
    manualControlsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    manualControlPlanStatus:
      "manual-callable-lifecycle-control-plan-documented",
    plannedFutureControls,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const plannedFutureControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const closeoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!closeoutHelperExists) {
    blockerNotes.push("Phase 441 closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only documents readiness for a future manual control shell.",
    "No manual controls are created in this phase.",
    "No lifecycle methods are invoked in this phase.",
    "No DOM, Leaflet, gameplay, backend, storage, network, or startup behavior changes are introduced."
  ];
  const readyForFutureManualControlShell =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    closeoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 444,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      closeoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      plannedManualControlBoundaryName:
        "custom25DVisualManualRendererCallableLifecycleManualControls",
      readinessStatus: "blocked",
      readyForFutureManualControlShell: false,
      manualControlsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      plannedFutureControls,
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 444,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    closeoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    plannedManualControlBoundaryName:
      "custom25DVisualManualRendererCallableLifecycleManualControls",
    readinessStatus:
      "manual-callable-lifecycle-control-readiness-documented",
    readyForFutureManualControlShell,
    manualControlsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    plannedFutureControls,
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const plannedFutureShellShape = {
    shellType: "manual-only-callable-lifecycle-control-shell",
    controls: [
      "manualInitialize",
      "manualStart",
      "manualStop",
      "manualAttachMap",
      "manualDrawFrame"
    ],
    defaultMode: "inert",
    startupWiring: false,
    domControls: false,
    eventListeners: false,
    mapAttachment: false,
    drawing: false,
    stateMutation: false
  };
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only documents the future manual control shell shape.",
    "No manual control shell is created in this phase.",
    "No manual controls are created in this phase.",
    "No lifecycle methods are invoked in this phase.",
    "No DOM, Leaflet, gameplay, backend, storage, network, or startup behavior changes are introduced."
  ];
  const readyForFutureManualControlShell =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 445,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      plannedManualControlBoundaryName:
        "custom25DVisualManualRendererCallableLifecycleManualControls",
      plannedManualControlShellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      shellPlanStatus: "blocked",
      readyForFutureManualControlShell: false,
      manualControlShellCreated: false,
      manualControlsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      plannedFutureShellShape,
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 445,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    plannedManualControlBoundaryName:
      "custom25DVisualManualRendererCallableLifecycleManualControls",
    plannedManualControlShellName:
      "custom25DVisualManualRendererCallableLifecycleManualControlShell",
    shellPlanStatus:
      "manual-callable-lifecycle-control-shell-plan-documented",
    readyForFutureManualControlShell,
    manualControlShellCreated: false,
    manualControlsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    plannedFutureShellShape,
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const plannedFutureShellShape = {
    shellType: "manual-only-callable-lifecycle-control-shell",
    controls: [
      "manualInitialize",
      "manualStart",
      "manualStop",
      "manualAttachMap",
      "manualDrawFrame"
    ],
    defaultMode: "inert",
    startupWiring: false,
    domControls: false,
    eventListeners: false,
    mapAttachment: false,
    drawing: false,
    stateMutation: false
  };
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only closes out the documented future manual control shell plan.",
    "No manual control shell is created in this phase.",
    "No manual controls are created in this phase.",
    "No lifecycle methods are invoked in this phase.",
    "No DOM, Leaflet, gameplay, backend, storage, network, or startup behavior changes are introduced."
  ];
  const shellPlanClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists &&
    manualControlShellPlanHelperExists;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 446,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      plannedManualControlBoundaryName:
        "custom25DVisualManualRendererCallableLifecycleManualControls",
      plannedManualControlShellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      closeoutStatus: "blocked",
      shellPlanClosedOut: false,
      manualControlShellCreated: false,
      manualControlsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      plannedFutureShellShape,
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 446,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    plannedManualControlBoundaryName:
      "custom25DVisualManualRendererCallableLifecycleManualControls",
    plannedManualControlShellName:
      "custom25DVisualManualRendererCallableLifecycleManualControlShell",
    closeoutStatus:
      "manual-callable-lifecycle-control-shell-plan-closeout-documented",
    shellPlanClosedOut,
    manualControlShellCreated: false,
    manualControlsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    plannedFutureShellShape,
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const plannedFutureShellShape = {
    shellType: "manual-only-callable-lifecycle-control-shell",
    controls: [
      "manualInitialize",
      "manualStart",
      "manualStop",
      "manualAttachMap",
      "manualDrawFrame"
    ],
    defaultMode: "inert",
    startupWiring: false,
    domControls: false,
    eventListeners: false,
    mapAttachment: false,
    drawing: false,
    stateMutation: false
  };
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only documents readiness for a future inert manual control shell.",
    "No manual control shell is created in this phase.",
    "No manual controls are created in this phase.",
    "No lifecycle methods are invoked in this phase.",
    "No DOM, Leaflet, gameplay, backend, storage, network, or startup behavior changes are introduced."
  ];
  const readyForFutureManualControlShellCreation =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists &&
    manualControlShellPlanHelperExists &&
    manualControlShellPlanCloseoutHelperExists;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 447,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      plannedManualControlBoundaryName:
        "custom25DVisualManualRendererCallableLifecycleManualControls",
      plannedManualControlShellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      readinessStatus: "blocked",
      readyForFutureManualControlShellCreation: false,
      manualControlShellCreated: false,
      manualControlsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      plannedFutureShellShape,
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 447,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    plannedManualControlBoundaryName:
      "custom25DVisualManualRendererCallableLifecycleManualControls",
    plannedManualControlShellName:
      "custom25DVisualManualRendererCallableLifecycleManualControlShell",
    readinessStatus:
      "manual-callable-lifecycle-control-shell-readiness-documented",
    readyForFutureManualControlShellCreation,
    manualControlShellCreated: false,
    manualControlsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    plannedFutureShellShape,
    blockerNotes,
    safetyNotes
  };
}

const custom25DVisualManualRendererCallableLifecycleManualControlShell = {
  shellName: "custom25DVisualManualRendererCallableLifecycleManualControlShell",
  shellType: "manual-only-callable-lifecycle-control-shell",
  phaseCreated: 448,
  created: true,
  inert: true,
  manualOnly: true,
  startupWiring: false,
  domControls: false,
  eventListeners: false,
  mapAttachment: false,
  drawing: false,
  stateMutation: false,
  runtimeBehaviorChanged: false,
  controlsCreated: false,
  lifecycleMethodsInvoked: false,
  controls: {
    manualInitialize: null,
    manualStart: null,
    manualStop: null,
    manualAttachMap: null,
    manualDrawFrame: null
  }
};

function getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const controlSlots = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.controlsCreated === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlSlotValuesAreNull =
    !!(
      shell &&
      shell.controls &&
      controlSlots.every((controlName) => shell.controls[controlName] === null)
    );
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("Phase 448 inert shell object is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase creates only the inert shell object.",
    "No callable control functions are created in this phase.",
    "No lifecycle methods are invoked in this phase.",
    "All control slots remain null placeholders only."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 448,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      shellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      shellExists,
      shellCreated: shellExists,
      shellInert,
      manualOnly,
      controlsCreated: false,
      callableControlFunctionsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      shellCreationStatus: "blocked",
      controlSlots,
      controlSlotValuesAreNull,
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 448,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    shellName:
      "custom25DVisualManualRendererCallableLifecycleManualControlShell",
    shellExists,
    shellCreated: shellExists,
    shellInert,
    manualOnly,
    controlsCreated: false,
    callableControlFunctionsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    shellCreationStatus:
      "manual-callable-lifecycle-control-shell-created-inert",
    controlSlots,
    controlSlotValuesAreNull,
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const controlSlots = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.controlsCreated === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlSlotValuesAreNull =
    !!(
      shell &&
      shell.controls &&
      controlSlots.every((controlName) => shell.controls[controlName] === null)
    );
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("Phase 448 inert shell object is missing.");
  }
  if (!shellInert) {
    blockerNotes.push("The inert shell object no longer matches its inert contract.");
  }
  if (!manualOnly) {
    blockerNotes.push("The inert shell object is not marked manual-only.");
  }
  if (!controlSlotValuesAreNull) {
    blockerNotes.push("One or more control slots are no longer null placeholders.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only verifies the inert shell after creation.",
    "No callable control functions are created in this phase.",
    "No control slots are replaced in this phase.",
    "No lifecycle methods are invoked in this phase.",
    "All control slots remain null placeholders only."
  ];
  const readyForFutureInertManualControlFunctionShells =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists &&
    manualControlShellPlanHelperExists &&
    manualControlShellPlanCloseoutHelperExists &&
    manualControlShellReadinessHelperExists &&
    manualControlShellCreationCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlSlotValuesAreNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 449,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      shellName,
      shellExists,
      shellInert,
      manualOnly,
      controlsCreated: false,
      callableControlFunctionsCreated: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      controlSlots,
      controlSlotValuesAreNull,
      readyForFutureInertManualControlFunctionShells: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 449,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    shellName,
    shellExists,
    shellInert,
    manualOnly,
    controlsCreated: false,
    callableControlFunctionsCreated: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    controlSlots,
    controlSlotValuesAreNull,
    readyForFutureInertManualControlFunctionShells,
    readinessStatus:
      "manual-callable-lifecycle-control-shell-post-creation-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualInitialize =
  function custom25DVisualManualRendererManualInitializeControlShell(
    options = {}
  ) {
    const requiredOptionKeys = [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "reportOnly",
      "confirmNoLifecycleInvocation",
      "confirmNoRendererInitialization",
      "confirmNoMapAttachment",
      "confirmNoDrawing",
      "confirmNoStateMutation",
      "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
      "confirmNoAutomaticInvocation"
    ];
    const missingRequiredOptionKeys = requiredOptionKeys.filter(
      (key) => options[key] !== true
    );

    if (missingRequiredOptionKeys.length > 0) {
      return {
        ok: false,
        phase: 450,
        controlName: "manualInitialize",
        shellName:
          "custom25DVisualManualRendererCallableLifecycleManualControlShell",
        inert: true,
        manualOnly: true,
        reportOnly: true,
        blocked: true,
        allowed: false,
        jsonSafeResult: true,
        requiredOptionKeys,
        missingRequiredOptionKeys,
        lifecycleMethodInvoked: false,
        rendererInitialized: false,
        mapAttached: false,
        frameDrawn: false,
        sharedStateMutated: false,
        runtimeBehaviorChanged: false,
        domTouched: false,
        startupWired: false,
        gameplayChanged: false,
        storageTouched: false,
        networkTouched: false,
        controlStatus: "blocked"
      };
    }

    return {
      ok: true,
      phase: 450,
      controlName: "manualInitialize",
      shellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      inert: true,
      manualOnly: true,
      reportOnly: true,
      blocked: false,
      allowed: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      lifecycleMethodInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      controlStatus: "manual-initialize-control-shell-inert"
    };
  };

function getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const manualControlShellPostCreationReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
    "function";
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualStart === null);
  const manualStopSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualStop === null);
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!manualControlShellPostCreationReadinessHelperExists) {
    blockerNotes.push("Phase 449 post-creation readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize was not created as an inert function shell.");
  }
  if (!manualStartSlotIsNull || !manualStopSlotIsNull || !manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("One or more non-initialize control slots no longer remain null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "manualInitialize is only an inert report shell in this phase.",
    "manualInitialize does not initialize the renderer.",
    "No lifecycle methods are invoked in this phase.",
    "The other four control slots remain null placeholders."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 450,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      manualControlShellPostCreationReadinessHelperExists,
      shellName,
      shellExists,
      shellInert,
      manualOnly,
      manualInitializeSlotIsFunction,
      manualStartSlotIsNull,
      manualStopSlotIsNull,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      callableControlFunctionsCreated: [],
      callableControlFunctionCount: 0,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 450,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    manualControlShellPostCreationReadinessHelperExists,
    shellName,
    shellExists,
    shellInert,
    manualOnly,
    manualInitializeSlotIsFunction,
    manualStartSlotIsNull,
    manualStopSlotIsNull,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    callableControlFunctionsCreated: ["manualInitialize"],
    callableControlFunctionCount: 1,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus: "manual-initialize-control-shell-created-inert",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const manualControlShellPostCreationReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
    "function";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualStart === null);
  const manualStopSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualStop === null);
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!manualControlShellPostCreationReadinessHelperExists) {
    blockerNotes.push("Phase 449 post-creation readiness helper is missing.");
  }
  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("Phase 450 manualInitialize closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsNull || !manualStopSlotIsNull || !manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("One or more non-initialize control slots no longer remain null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only verifies the inert manualInitialize control shell.",
    "manualInitialize is not invoked in this phase.",
    "No additional control functions are created in this phase.",
    "The other four control slots remain null placeholders."
  ];
  const readyForFutureManualStartControlShell =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists &&
    manualControlShellPlanHelperExists &&
    manualControlShellPlanCloseoutHelperExists &&
    manualControlShellReadinessHelperExists &&
    manualControlShellCreationCloseoutHelperExists &&
    manualControlShellPostCreationReadinessHelperExists &&
    manualInitializeCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsNull &&
    manualStopSlotIsNull &&
    manualAttachMapSlotIsNull &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 451,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      manualControlShellPostCreationReadinessHelperExists,
      manualInitializeCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      manualInitializeSlotIsFunction,
      manualStartSlotIsNull,
      manualStopSlotIsNull,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      callableControlFunctionsCreated: ["manualInitialize"],
      callableControlFunctionCount: 1,
      readyForFutureManualStartControlShell: false,
      manualInitializeInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 451,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    manualControlShellPostCreationReadinessHelperExists,
    manualInitializeCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    manualInitializeSlotIsFunction,
    manualStartSlotIsNull,
    manualStopSlotIsNull,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    callableControlFunctionsCreated: ["manualInitialize"],
    callableControlFunctionCount: 1,
    readyForFutureManualStartControlShell,
    manualInitializeInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    readinessStatus:
      "manual-initialize-control-shell-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStart =
  function custom25DVisualManualRendererManualStartControlShell(
    options = {}
  ) {
    const requiredOptionKeys = [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "reportOnly",
      "confirmNoLifecycleInvocation",
      "confirmNoRendererInitialization",
      "confirmNoRendererStart",
      "confirmNoMapAttachment",
      "confirmNoDrawing",
      "confirmNoStateMutation",
      "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
      "confirmNoAutomaticInvocation"
    ];
    const missingRequiredOptionKeys = requiredOptionKeys.filter(
      (key) => options[key] !== true
    );

    if (missingRequiredOptionKeys.length > 0) {
      return {
        ok: false,
        phase: 452,
        controlName: "manualStart",
        shellName:
          "custom25DVisualManualRendererCallableLifecycleManualControlShell",
        inert: true,
        manualOnly: true,
        reportOnly: true,
        blocked: true,
        allowed: false,
        jsonSafeResult: true,
        requiredOptionKeys,
        missingRequiredOptionKeys,
        lifecycleMethodInvoked: false,
        rendererStarted: false,
        rendererInitialized: false,
        mapAttached: false,
        frameDrawn: false,
        sharedStateMutated: false,
        runtimeBehaviorChanged: false,
        domTouched: false,
        startupWired: false,
        gameplayChanged: false,
        storageTouched: false,
        networkTouched: false,
        controlStatus: "blocked"
      };
    }

    return {
      ok: true,
      phase: 452,
      controlName: "manualStart",
      shellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      inert: true,
      manualOnly: true,
      reportOnly: true,
      blocked: false,
      allowed: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      lifecycleMethodInvoked: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      controlStatus: "manual-start-control-shell-inert"
    };
  };

function getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const manualControlShellPostCreationReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
    "function";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const manualInitializeReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualStop === null);
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!manualControlShellPostCreationReadinessHelperExists) {
    blockerNotes.push("Phase 449 post-creation readiness helper is missing.");
  }
  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("Phase 450 manualInitialize closeout helper is missing.");
  }
  if (!manualInitializeReadinessHelperExists) {
    blockerNotes.push("Phase 451 manualInitialize readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart was not created as an inert function shell.");
  }
  if (!manualStopSlotIsNull || !manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("One or more non-start control slots no longer remain in the expected state.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "manualStart is only an inert report shell in this phase.",
    "manualStart does not start the renderer.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "The remaining three control slots stay null placeholders."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 452,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      manualControlShellPostCreationReadinessHelperExists,
      manualInitializeCloseoutHelperExists,
      manualInitializeReadinessHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsNull,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      callableControlFunctionsCreated: ["manualInitialize"],
      callableControlFunctionCount: 1,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 452,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    manualControlShellPostCreationReadinessHelperExists,
    manualInitializeCloseoutHelperExists,
    manualInitializeReadinessHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsNull,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    callableControlFunctionsCreated: ["manualInitialize", "manualStart"],
    callableControlFunctionCount: 2,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus: "manual-start-control-shell-created-inert",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const manualControlShellPostCreationReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
    "function";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const manualInitializeReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
    "function";
  const manualStartCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualStop === null);
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!manualControlShellPostCreationReadinessHelperExists) {
    blockerNotes.push("Phase 449 post-creation readiness helper is missing.");
  }
  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("Phase 450 manualInitialize closeout helper is missing.");
  }
  if (!manualInitializeReadinessHelperExists) {
    blockerNotes.push("Phase 451 manualInitialize readiness helper is missing.");
  }
  if (!manualStartCloseoutHelperExists) {
    blockerNotes.push("Phase 452 manualStart closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsNull || !manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("One or more remaining control slots no longer remain null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only verifies the inert manualStart control shell.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "No additional control functions are created in this phase.",
    "The remaining three control slots stay null placeholders."
  ];
  const readyForFutureManualStopControlShell =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists &&
    manualControlShellPlanHelperExists &&
    manualControlShellPlanCloseoutHelperExists &&
    manualControlShellReadinessHelperExists &&
    manualControlShellCreationCloseoutHelperExists &&
    manualControlShellPostCreationReadinessHelperExists &&
    manualInitializeCloseoutHelperExists &&
    manualInitializeReadinessHelperExists &&
    manualStartCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsNull &&
    manualAttachMapSlotIsNull &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 453,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      manualControlShellPostCreationReadinessHelperExists,
      manualInitializeCloseoutHelperExists,
      manualInitializeReadinessHelperExists,
      manualStartCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsNull,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      callableControlFunctionsCreated: ["manualInitialize", "manualStart"],
      callableControlFunctionCount: 2,
      readyForFutureManualStopControlShell: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 453,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    manualControlShellPostCreationReadinessHelperExists,
    manualInitializeCloseoutHelperExists,
    manualInitializeReadinessHelperExists,
    manualStartCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsNull,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    callableControlFunctionsCreated: ["manualInitialize", "manualStart"],
    callableControlFunctionCount: 2,
    readyForFutureManualStopControlShell,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    readinessStatus:
      "manual-start-control-shell-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStop =
  function custom25DVisualManualRendererManualStopControlShell(
    options = {}
  ) {
    const requiredOptionKeys = [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "reportOnly",
      "confirmNoLifecycleInvocation",
      "confirmNoRendererInitialization",
      "confirmNoRendererStart",
      "confirmNoRendererStop",
      "confirmNoMapAttachment",
      "confirmNoDrawing",
      "confirmNoStateMutation",
      "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
      "confirmNoAutomaticInvocation"
    ];
    const missingRequiredOptionKeys = requiredOptionKeys.filter(
      (key) => options[key] !== true
    );

    if (missingRequiredOptionKeys.length > 0) {
      return {
        ok: false,
        phase: 454,
        controlName: "manualStop",
        shellName:
          "custom25DVisualManualRendererCallableLifecycleManualControlShell",
        inert: true,
        manualOnly: true,
        reportOnly: true,
        blocked: true,
        allowed: false,
        jsonSafeResult: true,
        requiredOptionKeys,
        missingRequiredOptionKeys,
        lifecycleMethodInvoked: false,
        rendererStopped: false,
        rendererStarted: false,
        rendererInitialized: false,
        mapAttached: false,
        frameDrawn: false,
        sharedStateMutated: false,
        runtimeBehaviorChanged: false,
        domTouched: false,
        startupWired: false,
        gameplayChanged: false,
        storageTouched: false,
        networkTouched: false,
        controlStatus: "blocked"
      };
    }

    return {
      ok: true,
      phase: 454,
      controlName: "manualStop",
      shellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      inert: true,
      manualOnly: true,
      reportOnly: true,
      blocked: false,
      allowed: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      lifecycleMethodInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      controlStatus: "manual-stop-control-shell-inert"
    };
  };

function getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const manualControlShellPostCreationReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
    "function";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const manualInitializeReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
    "function";
  const manualStartCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
    "function";
  const manualStartReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!manualControlShellPostCreationReadinessHelperExists) {
    blockerNotes.push("Phase 449 post-creation readiness helper is missing.");
  }
  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("Phase 450 manualInitialize closeout helper is missing.");
  }
  if (!manualInitializeReadinessHelperExists) {
    blockerNotes.push("Phase 451 manualInitialize readiness helper is missing.");
  }
  if (!manualStartCloseoutHelperExists) {
    blockerNotes.push("Phase 452 manualStart closeout helper is missing.");
  }
  if (!manualStartReadinessHelperExists) {
    blockerNotes.push("Phase 453 manualStart readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop was not created as an inert function shell.");
  }
  if (!manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("One or more remaining control slots no longer remain null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "manualStop is only an inert report shell in this phase.",
    "manualStop does not stop the renderer.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "The remaining two control slots stay null placeholders."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 454,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      manualControlShellPostCreationReadinessHelperExists,
      manualInitializeCloseoutHelperExists,
      manualInitializeReadinessHelperExists,
      manualStartCloseoutHelperExists,
      manualStartReadinessHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      callableControlFunctionsCreated: ["manualInitialize", "manualStart"],
      callableControlFunctionCount: 2,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 454,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    manualControlShellPostCreationReadinessHelperExists,
    manualInitializeCloseoutHelperExists,
    manualInitializeReadinessHelperExists,
    manualStartCloseoutHelperExists,
    manualStartReadinessHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    callableControlFunctionsCreated: [
      "manualInitialize",
      "manualStart",
      "manualStop"
    ],
    callableControlFunctionCount: 3,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus: "manual-stop-control-shell-created-inert",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const foundationHelperExists =
    typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
    "function";
  const placeholderCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
    "function";
  const placeholderReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
    "function";
  const manualControlPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
    "function";
  const manualControlReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
    "function";
  const manualControlShellPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
    "function";
  const manualControlShellPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
    "function";
  const manualControlShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
    "function";
  const manualControlShellCreationCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
    "function";
  const manualControlShellPostCreationReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
    "function";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const manualInitializeReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
    "function";
  const manualStartCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
    "function";
  const manualStartReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
    "function";
  const manualStopCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!foundationHelperExists) {
    blockerNotes.push("Phase 440 foundation helper is missing.");
  }
  if (!placeholderCloseoutHelperExists) {
    blockerNotes.push("Phase 441 placeholder closeout helper is missing.");
  }
  if (!placeholderReadinessHelperExists) {
    blockerNotes.push("Phase 442 placeholder readiness helper is missing.");
  }
  if (!manualControlPlanHelperExists) {
    blockerNotes.push("Phase 443 manual control plan helper is missing.");
  }
  if (!manualControlReadinessHelperExists) {
    blockerNotes.push("Phase 444 manual control readiness helper is missing.");
  }
  if (!manualControlShellPlanHelperExists) {
    blockerNotes.push("Phase 445 manual control shell plan helper is missing.");
  }
  if (!manualControlShellPlanCloseoutHelperExists) {
    blockerNotes.push("Phase 446 manual control shell plan closeout helper is missing.");
  }
  if (!manualControlShellReadinessHelperExists) {
    blockerNotes.push("Phase 447 manual control shell readiness helper is missing.");
  }
  if (!manualControlShellCreationCloseoutHelperExists) {
    blockerNotes.push("Phase 448 shell creation closeout helper is missing.");
  }
  if (!manualControlShellPostCreationReadinessHelperExists) {
    blockerNotes.push("Phase 449 post-creation readiness helper is missing.");
  }
  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("Phase 450 manualInitialize closeout helper is missing.");
  }
  if (!manualInitializeReadinessHelperExists) {
    blockerNotes.push("Phase 451 manualInitialize readiness helper is missing.");
  }
  if (!manualStartCloseoutHelperExists) {
    blockerNotes.push("Phase 452 manualStart closeout helper is missing.");
  }
  if (!manualStartReadinessHelperExists) {
    blockerNotes.push("Phase 453 manualStart readiness helper is missing.");
  }
  if (!manualStopCloseoutHelperExists) {
    blockerNotes.push("Phase 454 manualStop closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("One or both remaining control slots no longer remain null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This phase only verifies the inert manualStop control shell.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "The remaining two control slots stay null placeholders."
  ];
  const readyForFutureManualAttachMapControlShell =
    missingRequiredOptionKeys.length === 0 &&
    foundationHelperExists &&
    placeholderCloseoutHelperExists &&
    placeholderReadinessHelperExists &&
    manualControlPlanHelperExists &&
    manualControlReadinessHelperExists &&
    manualControlShellPlanHelperExists &&
    manualControlShellPlanCloseoutHelperExists &&
    manualControlShellReadinessHelperExists &&
    manualControlShellCreationCloseoutHelperExists &&
    manualControlShellPostCreationReadinessHelperExists &&
    manualInitializeCloseoutHelperExists &&
    manualInitializeReadinessHelperExists &&
    manualStartCloseoutHelperExists &&
    manualStartReadinessHelperExists &&
    manualStopCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsNull &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 455,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      foundationHelperExists,
      placeholderCloseoutHelperExists,
      placeholderReadinessHelperExists,
      manualControlPlanHelperExists,
      manualControlReadinessHelperExists,
      manualControlShellPlanHelperExists,
      manualControlShellPlanCloseoutHelperExists,
      manualControlShellReadinessHelperExists,
      manualControlShellCreationCloseoutHelperExists,
      manualControlShellPostCreationReadinessHelperExists,
      manualInitializeCloseoutHelperExists,
      manualInitializeReadinessHelperExists,
      manualStartCloseoutHelperExists,
      manualStartReadinessHelperExists,
      manualStopCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      callableControlFunctionsCreated: [
        "manualInitialize",
        "manualStart",
        "manualStop"
      ],
      callableControlFunctionCount: 3,
      readyForFutureManualAttachMapControlShell: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 455,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    foundationHelperExists,
    placeholderCloseoutHelperExists,
    placeholderReadinessHelperExists,
    manualControlPlanHelperExists,
    manualControlReadinessHelperExists,
    manualControlShellPlanHelperExists,
    manualControlShellPlanCloseoutHelperExists,
    manualControlShellReadinessHelperExists,
    manualControlShellCreationCloseoutHelperExists,
    manualControlShellPostCreationReadinessHelperExists,
    manualInitializeCloseoutHelperExists,
    manualInitializeReadinessHelperExists,
    manualStartCloseoutHelperExists,
    manualStartReadinessHelperExists,
    manualStopCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    callableControlFunctionsCreated: [
      "manualInitialize",
      "manualStart",
      "manualStop"
    ],
    callableControlFunctionCount: 3,
    readyForFutureManualAttachMapControlShell,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    readinessStatus:
      "manual-stop-control-shell-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualStopCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
    "function";
  const manualStopReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotExists =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualAttachMap === null);
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!manualStopCloseoutHelperExists) {
    blockerNotes.push("Phase 454 manualStop closeout helper is missing.");
  }
  if (!manualStopReadinessHelperExists) {
    blockerNotes.push("Phase 455 manualStop readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotExists) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsNull || !manualDrawFrameSlotIsNull) {
    blockerNotes.push("manualAttachMap or manualDrawFrame no longer remains null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap and manualDrawFrame remain null placeholders."
  ];
  const readinessChainClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualStopCloseoutHelperExists &&
    manualStopReadinessHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotExists &&
    manualAttachMapSlotIsNull &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 456,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualStopCloseoutHelperExists,
      manualStopReadinessHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotExists,
      manualAttachMapSlotIsNull,
      manualDrawFrameSlotIsNull,
      readinessChainClosedOut: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 456,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualStopCloseoutHelperExists,
    manualStopReadinessHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotExists,
    manualAttachMapSlotIsNull,
    manualDrawFrameSlotIsNull,
    readinessChainClosedOut,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-stop-control-shell-readiness-closeout-documented",
    blockerNotes,
    safetyNotes
  };
}

custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualAttachMap =
  function custom25DVisualManualRendererManualAttachMapControlShell(
    options = {}
  ) {
    const requiredOptionKeys = [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "reportOnly",
      "confirmNoLifecycleInvocation",
      "confirmNoRendererInitialization",
      "confirmNoRendererStart",
      "confirmNoRendererStop",
      "confirmNoMapAttachment",
      "confirmNoDrawing",
      "confirmNoStateMutation",
      "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
      "confirmNoAutomaticInvocation"
    ];
    const missingRequiredOptionKeys = requiredOptionKeys.filter(
      (key) => options[key] !== true
    );

    if (missingRequiredOptionKeys.length > 0) {
      return {
        ok: false,
        phase: 457,
        controlName: "manualAttachMap",
        shellName:
          "custom25DVisualManualRendererCallableLifecycleManualControlShell",
        inert: true,
        manualOnly: true,
        reportOnly: true,
        blocked: true,
        allowed: false,
        jsonSafeResult: true,
        requiredOptionKeys,
        missingRequiredOptionKeys,
        lifecycleMethodInvoked: false,
        rendererStopped: false,
        rendererStarted: false,
        rendererInitialized: false,
        mapAttached: false,
        frameDrawn: false,
        sharedStateMutated: false,
        runtimeBehaviorChanged: false,
        domTouched: false,
        startupWired: false,
        gameplayChanged: false,
        storageTouched: false,
        networkTouched: false,
        controlStatus: "blocked"
      };
    }

    return {
      ok: true,
      phase: 457,
      controlName: "manualAttachMap",
      shellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      inert: true,
      manualOnly: true,
      reportOnly: true,
      blocked: false,
      allowed: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      lifecycleMethodInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      controlStatus: "manual-attach-map-control-shell-inert"
    };
  };

function getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualStopCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
    "function";
  const manualStopReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
    "function";
  const manualStopReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const blockerNotes = [];

  if (!manualStopCloseoutHelperExists) {
    blockerNotes.push("Phase 454 manualStop closeout helper is missing.");
  }
  if (!manualStopReadinessHelperExists) {
    blockerNotes.push("Phase 455 manualStop readiness helper is missing.");
  }
  if (!manualStopReadinessCloseoutHelperExists) {
    blockerNotes.push("Phase 456 manualStop readiness closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap was not created as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsNull) {
    blockerNotes.push("manualDrawFrame no longer remains null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "manualAttachMap is only an inert report shell in this phase.",
    "manualAttachMap does not attach a map.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap is not invoked in this phase.",
    "manualDrawFrame remains a null placeholder."
  ];
  const attachMapControlClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualStopCloseoutHelperExists &&
    manualStopReadinessHelperExists &&
    manualStopReadinessCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 457,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualStopCloseoutHelperExists,
      manualStopReadinessHelperExists,
      manualStopReadinessCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      manualDrawFrameSlotIsNull,
      attachMapControlClosedOut: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 457,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualStopCloseoutHelperExists,
    manualStopReadinessHelperExists,
    manualStopReadinessCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    manualDrawFrameSlotIsNull,
    attachMapControlClosedOut,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-attach-map-control-shell-created-inert",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualAttachMapCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const attachMapControlAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell"
    );
  const blockerNotes = [];

  if (!manualAttachMapCloseoutHelperExists) {
    blockerNotes.push("Phase 457 manualAttachMap closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!attachMapControlAppearsReportOnly) {
    blockerNotes.push("manualAttachMap does not match the inert report-only shell pattern.");
  }
  if (!manualDrawFrameSlotIsNull) {
    blockerNotes.push("manualDrawFrame no longer remains null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap is not invoked in this phase.",
    "manualDrawFrame remains a null placeholder."
  ];
  const readyForFutureManualDrawFrameControlShell =
    missingRequiredOptionKeys.length === 0 &&
    manualAttachMapCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    attachMapControlAppearsReportOnly &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 458,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualAttachMapCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      attachMapControlAppearsReportOnly,
      manualDrawFrameSlotIsNull,
      readyForFutureManualDrawFrameControlShell: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 458,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualAttachMapCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    attachMapControlAppearsReportOnly,
    manualDrawFrameSlotIsNull,
    readyForFutureManualDrawFrameControlShell,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    readinessStatus:
      "manual-attach-map-control-shell-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualAttachMapCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
    "function";
  const manualAttachMapReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsNull =
    !!(shell && shell.controls && shell.controls.manualDrawFrame === null);
  const attachMapControlAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell"
    );
  const blockerNotes = [];

  if (!manualAttachMapCloseoutHelperExists) {
    blockerNotes.push("Phase 457 manualAttachMap closeout helper is missing.");
  }
  if (!manualAttachMapReadinessHelperExists) {
    blockerNotes.push("Phase 458 manualAttachMap readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!attachMapControlAppearsReportOnly) {
    blockerNotes.push("manualAttachMap does not match the inert report-only shell pattern.");
  }
  if (!manualDrawFrameSlotIsNull) {
    blockerNotes.push("manualDrawFrame no longer remains null.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap is not invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase.",
    "manualDrawFrame remains a null placeholder."
  ];
  const readinessChainClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualAttachMapCloseoutHelperExists &&
    manualAttachMapReadinessHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    attachMapControlAppearsReportOnly &&
    manualDrawFrameSlotIsNull;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 459,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualAttachMapCloseoutHelperExists,
      manualAttachMapReadinessHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      attachMapControlAppearsReportOnly,
      manualDrawFrameSlotIsNull,
      readinessChainClosedOut: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 459,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualAttachMapCloseoutHelperExists,
    manualAttachMapReadinessHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    attachMapControlAppearsReportOnly,
    manualDrawFrameSlotIsNull,
    readinessChainClosedOut,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-attach-map-control-shell-readiness-closeout-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualAttachMapCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
    "function";
  const manualAttachMapReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
    "function";
  const manualAttachMapReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const drawFrameControlAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const blockerNotes = [];

  if (!manualAttachMapCloseoutHelperExists) {
    blockerNotes.push("Phase 457 manualAttachMap closeout helper is missing.");
  }
  if (!manualAttachMapReadinessHelperExists) {
    blockerNotes.push("Phase 458 manualAttachMap readiness helper is missing.");
  }
  if (!manualAttachMapReadinessCloseoutHelperExists) {
    blockerNotes.push("Phase 459 prerequisite attach-map readiness closeout helper chain is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame was not created as an inert function shell.");
  }
  if (!drawFrameControlAppearsReportOnly) {
    blockerNotes.push("manualDrawFrame does not match the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap is not invoked in this phase.",
    "manualDrawFrame is not invoked in this phase.",
    "No drawing occurs and no live Leaflet map instance is read in this phase."
  ];
  const drawFrameControlClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualAttachMapCloseoutHelperExists &&
    manualAttachMapReadinessHelperExists &&
    manualAttachMapReadinessCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction &&
    drawFrameControlAppearsReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 460,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualAttachMapCloseoutHelperExists,
      manualAttachMapReadinessHelperExists,
      manualAttachMapReadinessCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      manualDrawFrameSlotIsFunction,
      drawFrameControlAppearsReportOnly,
      drawFrameControlClosedOut: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 460,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualAttachMapCloseoutHelperExists,
    manualAttachMapReadinessHelperExists,
    manualAttachMapReadinessCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    manualDrawFrameSlotIsFunction,
    drawFrameControlAppearsReportOnly,
    drawFrameControlClosedOut,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-draw-frame-control-shell-created-inert",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualDrawFrameCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const drawFrameControlAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const blockerNotes = [];

  if (!manualDrawFrameCloseoutHelperExists) {
    blockerNotes.push("Phase 460 manualDrawFrame closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame is not present as an inert function shell.");
  }
  if (!drawFrameControlAppearsReportOnly) {
    blockerNotes.push("manualDrawFrame does not match the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap is not invoked in this phase.",
    "manualDrawFrame is not invoked in this phase.",
    "No drawing occurs and no live Leaflet map instance is read in this phase."
  ];
  const readyForFullManualLifecycleControlShellReview =
    missingRequiredOptionKeys.length === 0 &&
    manualDrawFrameCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    drawFrameControlAppearsReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 461,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualDrawFrameCloseoutHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      manualDrawFrameSlotIsFunction,
      allFiveManualLifecycleControlSlotsExist,
      drawFrameControlAppearsReportOnly,
      readyForFullManualLifecycleControlShellReview: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 461,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualDrawFrameCloseoutHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    manualDrawFrameSlotIsFunction,
    allFiveManualLifecycleControlSlotsExist,
    drawFrameControlAppearsReportOnly,
    readyForFullManualLifecycleControlShellReview,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    readinessStatus:
      "manual-draw-frame-control-shell-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualDrawFrameCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
    "function";
  const manualDrawFrameReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const drawFrameControlAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const blockerNotes = [];

  if (!manualDrawFrameCloseoutHelperExists) {
    blockerNotes.push("Phase 460 manualDrawFrame closeout helper is missing.");
  }
  if (!manualDrawFrameReadinessHelperExists) {
    blockerNotes.push("Phase 461 manualDrawFrame readiness helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame is not present as an inert function shell.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (!drawFrameControlAppearsReportOnly) {
    blockerNotes.push("manualDrawFrame does not match the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "manualInitialize is not invoked in this phase.",
    "manualStart is not invoked in this phase.",
    "manualStop is not invoked in this phase.",
    "manualAttachMap is not invoked in this phase.",
    "manualDrawFrame is not invoked in this phase.",
    "No drawing occurs and no live Leaflet map instance is read or required in this phase."
  ];
  const readinessChainClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualDrawFrameCloseoutHelperExists &&
    manualDrawFrameReadinessHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    drawFrameControlAppearsReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 462,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualDrawFrameCloseoutHelperExists,
      manualDrawFrameReadinessHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      manualDrawFrameSlotIsFunction,
      allFiveManualLifecycleControlSlotsExist,
      drawFrameControlAppearsReportOnly,
      readinessChainClosedOut: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 462,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualDrawFrameCloseoutHelperExists,
    manualDrawFrameReadinessHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    manualDrawFrameSlotIsFunction,
    allFiveManualLifecycleControlSlotsExist,
    drawFrameControlAppearsReportOnly,
    readinessChainClosedOut,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-draw-frame-control-shell-readiness-closeout-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "completionOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const manualInitializeReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
    "function";
  const manualStartCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
    "function";
  const manualStartReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
    "function";
  const manualStopCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
    "function";
  const manualStopReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
    "function";
  const manualStopReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport ===
    "function";
  const manualAttachMapCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
    "function";
  const manualAttachMapReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
    "function";
  const manualAttachMapReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport ===
    "function";
  const manualDrawFrameCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
    "function";
  const manualDrawFrameReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport ===
    "function";
  const manualDrawFrameReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const manualInitializeAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell"
    );
  const manualStartAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell"
    );
  const manualStopAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell"
    );
  const manualAttachMapAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell"
    );
  const manualDrawFrameAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const allFiveManualControlsRemainReportOnly =
    manualInitializeAppearsReportOnly &&
    manualStartAppearsReportOnly &&
    manualStopAppearsReportOnly &&
    manualAttachMapAppearsReportOnly &&
    manualDrawFrameAppearsReportOnly;
  const blockerNotes = [];

  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("manualInitialize closeout helper is missing.");
  }
  if (!manualInitializeReadinessHelperExists) {
    blockerNotes.push("manualInitialize readiness helper is missing.");
  }
  if (!manualStartCloseoutHelperExists) {
    blockerNotes.push("manualStart closeout helper is missing.");
  }
  if (!manualStartReadinessHelperExists) {
    blockerNotes.push("manualStart readiness helper is missing.");
  }
  if (!manualStopCloseoutHelperExists) {
    blockerNotes.push("manualStop closeout helper is missing.");
  }
  if (!manualStopReadinessHelperExists) {
    blockerNotes.push("manualStop readiness helper is missing.");
  }
  if (!manualStopReadinessCloseoutHelperExists) {
    blockerNotes.push("manualStop readiness closeout helper is missing.");
  }
  if (!manualAttachMapCloseoutHelperExists) {
    blockerNotes.push("manualAttachMap closeout helper is missing.");
  }
  if (!manualAttachMapReadinessHelperExists) {
    blockerNotes.push("manualAttachMap readiness helper is missing.");
  }
  if (!manualAttachMapReadinessCloseoutHelperExists) {
    blockerNotes.push("manualAttachMap readiness closeout helper is missing.");
  }
  if (!manualDrawFrameCloseoutHelperExists) {
    blockerNotes.push("manualDrawFrame closeout helper is missing.");
  }
  if (!manualDrawFrameReadinessHelperExists) {
    blockerNotes.push("manualDrawFrame readiness helper is missing.");
  }
  if (!manualDrawFrameReadinessCloseoutHelperExists) {
    blockerNotes.push("manualDrawFrame readiness closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame is not present as an inert function shell.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "All five manual lifecycle controls are inspected only.",
    "No manual lifecycle control is invoked in this phase.",
    "No renderer lifecycle method is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase.",
    "No drawing occurs and no shared renderer state is mutated in this phase."
  ];
  const manualControlsComplete =
    missingRequiredOptionKeys.length === 0 &&
    manualInitializeCloseoutHelperExists &&
    manualInitializeReadinessHelperExists &&
    manualStartCloseoutHelperExists &&
    manualStartReadinessHelperExists &&
    manualStopCloseoutHelperExists &&
    manualStopReadinessHelperExists &&
    manualStopReadinessCloseoutHelperExists &&
    manualAttachMapCloseoutHelperExists &&
    manualAttachMapReadinessHelperExists &&
    manualAttachMapReadinessCloseoutHelperExists &&
    manualDrawFrameCloseoutHelperExists &&
    manualDrawFrameReadinessHelperExists &&
    manualDrawFrameReadinessCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    allFiveManualControlsRemainReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 463,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      manualInitializeCloseoutHelperExists,
      manualInitializeReadinessHelperExists,
      manualStartCloseoutHelperExists,
      manualStartReadinessHelperExists,
      manualStopCloseoutHelperExists,
      manualStopReadinessHelperExists,
      manualStopReadinessCloseoutHelperExists,
      manualAttachMapCloseoutHelperExists,
      manualAttachMapReadinessHelperExists,
      manualAttachMapReadinessCloseoutHelperExists,
      manualDrawFrameCloseoutHelperExists,
      manualDrawFrameReadinessHelperExists,
      manualDrawFrameReadinessCloseoutHelperExists,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      manualDrawFrameSlotIsFunction,
      manualInitializeAppearsReportOnly,
      manualStartAppearsReportOnly,
      manualStopAppearsReportOnly,
      manualAttachMapAppearsReportOnly,
      manualDrawFrameAppearsReportOnly,
      allFiveManualControlsRemainReportOnly,
      manualControlsComplete: false,
      reportOnlyComplete: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      completionStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 463,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    manualInitializeCloseoutHelperExists,
    manualInitializeReadinessHelperExists,
    manualStartCloseoutHelperExists,
    manualStartReadinessHelperExists,
    manualStopCloseoutHelperExists,
    manualStopReadinessHelperExists,
    manualStopReadinessCloseoutHelperExists,
    manualAttachMapCloseoutHelperExists,
    manualAttachMapReadinessHelperExists,
    manualAttachMapReadinessCloseoutHelperExists,
    manualDrawFrameCloseoutHelperExists,
    manualDrawFrameReadinessHelperExists,
    manualDrawFrameReadinessCloseoutHelperExists,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    manualDrawFrameSlotIsFunction,
    manualInitializeAppearsReportOnly,
    manualStartAppearsReportOnly,
    manualStopAppearsReportOnly,
    manualAttachMapAppearsReportOnly,
    manualDrawFrameAppearsReportOnly,
    allFiveManualControlsRemainReportOnly,
    manualControlsComplete,
    reportOnlyComplete: manualControlsComplete,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    completionStatus:
      "manual-callable-lifecycle-controls-complete-report-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const manualInitializeCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
    "function";
  const manualInitializeReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
    "function";
  const manualStartCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
    "function";
  const manualStartReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
    "function";
  const manualStopCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
    "function";
  const manualStopReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
    "function";
  const manualStopReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport ===
    "function";
  const manualAttachMapCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
    "function";
  const manualAttachMapReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
    "function";
  const manualAttachMapReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport ===
    "function";
  const manualDrawFrameCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
    "function";
  const manualDrawFrameReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport ===
    "function";
  const manualDrawFrameReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const manualInitializeAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell"
    );
  const manualStartAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell"
    );
  const manualStopAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell"
    );
  const manualAttachMapAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell"
    );
  const manualDrawFrameAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const allFiveManualControlsRemainReportOnly =
    manualInitializeAppearsReportOnly &&
    manualStartAppearsReportOnly &&
    manualStopAppearsReportOnly &&
    manualAttachMapAppearsReportOnly &&
    manualDrawFrameAppearsReportOnly;
  const blockerNotes = [];

  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("Phase 463 manual controls complete helper is missing.");
  }
  if (!manualInitializeCloseoutHelperExists) {
    blockerNotes.push("manualInitialize closeout helper is missing.");
  }
  if (!manualInitializeReadinessHelperExists) {
    blockerNotes.push("manualInitialize readiness helper is missing.");
  }
  if (!manualStartCloseoutHelperExists) {
    blockerNotes.push("manualStart closeout helper is missing.");
  }
  if (!manualStartReadinessHelperExists) {
    blockerNotes.push("manualStart readiness helper is missing.");
  }
  if (!manualStopCloseoutHelperExists) {
    blockerNotes.push("manualStop closeout helper is missing.");
  }
  if (!manualStopReadinessHelperExists) {
    blockerNotes.push("manualStop readiness helper is missing.");
  }
  if (!manualStopReadinessCloseoutHelperExists) {
    blockerNotes.push("manualStop readiness closeout helper is missing.");
  }
  if (!manualAttachMapCloseoutHelperExists) {
    blockerNotes.push("manualAttachMap closeout helper is missing.");
  }
  if (!manualAttachMapReadinessHelperExists) {
    blockerNotes.push("manualAttachMap readiness helper is missing.");
  }
  if (!manualAttachMapReadinessCloseoutHelperExists) {
    blockerNotes.push("manualAttachMap readiness closeout helper is missing.");
  }
  if (!manualDrawFrameCloseoutHelperExists) {
    blockerNotes.push("manualDrawFrame closeout helper is missing.");
  }
  if (!manualDrawFrameReadinessHelperExists) {
    blockerNotes.push("manualDrawFrame readiness helper is missing.");
  }
  if (!manualDrawFrameReadinessCloseoutHelperExists) {
    blockerNotes.push("manualDrawFrame readiness closeout helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame is not present as an inert function shell.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "All five manual lifecycle controls are inspected only.",
    "No manual lifecycle control is invoked in this phase.",
    "No renderer lifecycle method is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase.",
    "No drawing occurs and no shared renderer state is mutated in this phase."
  ];
  const manualControlsCompleteCloseout =
    missingRequiredOptionKeys.length === 0 &&
    manualControlsCompleteHelperExists &&
    manualInitializeCloseoutHelperExists &&
    manualInitializeReadinessHelperExists &&
    manualStartCloseoutHelperExists &&
    manualStartReadinessHelperExists &&
    manualStopCloseoutHelperExists &&
    manualStopReadinessHelperExists &&
    manualStopReadinessCloseoutHelperExists &&
    manualAttachMapCloseoutHelperExists &&
    manualAttachMapReadinessHelperExists &&
    manualAttachMapReadinessCloseoutHelperExists &&
    manualDrawFrameCloseoutHelperExists &&
    manualDrawFrameReadinessHelperExists &&
    manualDrawFrameReadinessCloseoutHelperExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    allFiveManualControlsRemainReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 464,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualControlsCompleteHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      manualInitializeCloseoutHelperExists,
      manualInitializeReadinessHelperExists,
      manualStartCloseoutHelperExists,
      manualStartReadinessHelperExists,
      manualStopCloseoutHelperExists,
      manualStopReadinessHelperExists,
      manualStopReadinessCloseoutHelperExists,
      manualAttachMapCloseoutHelperExists,
      manualAttachMapReadinessHelperExists,
      manualAttachMapReadinessCloseoutHelperExists,
      manualDrawFrameCloseoutHelperExists,
      manualDrawFrameReadinessHelperExists,
      manualDrawFrameReadinessCloseoutHelperExists,
      manualInitializeSlotIsFunction,
      manualStartSlotIsFunction,
      manualStopSlotIsFunction,
      manualAttachMapSlotIsFunction,
      manualDrawFrameSlotIsFunction,
      manualInitializeAppearsReportOnly,
      manualStartAppearsReportOnly,
      manualStopAppearsReportOnly,
      manualAttachMapAppearsReportOnly,
      manualDrawFrameAppearsReportOnly,
      allFiveManualControlsRemainReportOnly,
      manualControlsCompleteCloseout: false,
      reportOnlyCloseout: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 464,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualControlsCompleteHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    manualInitializeCloseoutHelperExists,
    manualInitializeReadinessHelperExists,
    manualStartCloseoutHelperExists,
    manualStartReadinessHelperExists,
    manualStopCloseoutHelperExists,
    manualStopReadinessHelperExists,
    manualStopReadinessCloseoutHelperExists,
    manualAttachMapCloseoutHelperExists,
    manualAttachMapReadinessHelperExists,
    manualAttachMapReadinessCloseoutHelperExists,
    manualDrawFrameCloseoutHelperExists,
    manualDrawFrameReadinessHelperExists,
    manualDrawFrameReadinessCloseoutHelperExists,
    manualInitializeSlotIsFunction,
    manualStartSlotIsFunction,
    manualStopSlotIsFunction,
    manualAttachMapSlotIsFunction,
    manualDrawFrameSlotIsFunction,
    manualInitializeAppearsReportOnly,
    manualStartAppearsReportOnly,
    manualStopAppearsReportOnly,
    manualAttachMapAppearsReportOnly,
    manualDrawFrameAppearsReportOnly,
    allFiveManualControlsRemainReportOnly,
    manualControlsCompleteCloseout,
    reportOnlyCloseout: manualControlsCompleteCloseout,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-callable-lifecycle-controls-complete-closeout-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planningOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const plannedBridgeName =
    "custom25DVisualManualRendererCallableLifecycleManualExecutionBridge";
  const plannedSequenceOrder = [
    "manualInitialize",
    "manualAttachMap",
    "manualStart",
    "manualDrawFrame",
    "manualStop"
  ];
  const plannedSafetyGateRequirements = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    explicitOptionsOnly: true,
    confirmNoStartupInvocation: true,
    confirmNoUiInvocation: true,
    confirmNoTimerInvocation: true,
    confirmNoAutomaticExecution: true
  };
  const manualControlsCompleteCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport ===
    "function";
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const manualInitializeAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell"
    );
  const manualStartAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell"
    );
  const manualStopAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell"
    );
  const manualAttachMapAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell"
    );
  const manualDrawFrameAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const allFiveManualControlsRemainReportOnly =
    manualInitializeAppearsReportOnly &&
    manualStartAppearsReportOnly &&
    manualStopAppearsReportOnly &&
    manualAttachMapAppearsReportOnly &&
    manualDrawFrameAppearsReportOnly;
  const blockerNotes = [];

  if (!manualControlsCompleteCloseoutHelperExists) {
    blockerNotes.push("Phase 464 manual controls complete closeout helper is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("Phase 463 manual controls complete helper is missing.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame is not present as an inert function shell.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "The future execution bridge is documented only in this phase.",
    "No execution bridge object or callable gate is created in this phase.",
    "No manual lifecycle control is invoked in this phase.",
    "No renderer lifecycle method is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 465,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      plannedBridgeName,
      plannedLocalDevOnly: true,
      plannedExplicitOptionsOnly: true,
      plannedManualInvocationOnly: true,
      plannedNoStartupWiring: true,
      plannedNoUiControls: true,
      plannedNoTimers: true,
      plannedNoAutomaticExecution: true,
      plannedSequenceOrder,
      plannedSafetyGateRequirements,
      manualControlsCompleteCloseoutHelperExists,
      manualControlsCompleteHelperExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      manualInitializeAppearsReportOnly,
      manualStartAppearsReportOnly,
      manualStopAppearsReportOnly,
      manualAttachMapAppearsReportOnly,
      manualDrawFrameAppearsReportOnly,
      allFiveManualControlsRemainReportOnly,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      bridgePlanStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 465,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    plannedBridgeName,
    plannedLocalDevOnly: true,
    plannedExplicitOptionsOnly: true,
    plannedManualInvocationOnly: true,
    plannedNoStartupWiring: true,
    plannedNoUiControls: true,
    plannedNoTimers: true,
    plannedNoAutomaticExecution: true,
    plannedSequenceOrder,
    plannedSafetyGateRequirements,
    manualControlsCompleteCloseoutHelperExists,
    manualControlsCompleteHelperExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    manualInitializeAppearsReportOnly,
    manualStartAppearsReportOnly,
    manualStopAppearsReportOnly,
    manualAttachMapAppearsReportOnly,
    manualDrawFrameAppearsReportOnly,
    allFiveManualControlsRemainReportOnly,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    bridgePlanStatus:
      "manual-callable-lifecycle-execution-bridge-plan-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const plannedBridgeName =
    "custom25DVisualManualRendererCallableLifecycleManualExecutionBridge";
  const plannedSequenceOrder = [
    "manualInitialize",
    "manualAttachMap",
    "manualStart",
    "manualDrawFrame",
    "manualStop"
  ];
  const plannedSafetyGateRequirements = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    explicitOptionsOnly: true,
    confirmNoStartupInvocation: true,
    confirmNoUiInvocation: true,
    confirmNoTimerInvocation: true,
    confirmNoAutomaticExecution: true
  };
  const manualExecutionBridgePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
    "function";
  const executionBridgeObjectExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const callableExecutionGateExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "function";
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const manualOnly = !!(shell && shell.manualOnly === true);
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const manualInitializeAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell"
    );
  const manualStartAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell"
    );
  const manualStopAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell"
    );
  const manualAttachMapAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell"
    );
  const manualDrawFrameAppearsReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const allFiveManualControlsRemainReportOnly =
    manualInitializeAppearsReportOnly &&
    manualStartAppearsReportOnly &&
    manualStopAppearsReportOnly &&
    manualAttachMapAppearsReportOnly &&
    manualDrawFrameAppearsReportOnly;
  const blockerNotes = [];

  if (!manualExecutionBridgePlanHelperExists) {
    blockerNotes.push("Phase 465 execution bridge plan helper is missing.");
  }
  if (executionBridgeObjectExists) {
    blockerNotes.push("The execution bridge object already exists, but Phase 466 expects planning-only state.");
  }
  if (callableExecutionGateExists) {
    blockerNotes.push("A callable execution gate already exists, but Phase 466 expects planning-only state.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!manualInitializeSlotIsFunction) {
    blockerNotes.push("manualInitialize is not present as an inert function shell.");
  }
  if (!manualStartSlotIsFunction) {
    blockerNotes.push("manualStart is not present as an inert function shell.");
  }
  if (!manualStopSlotIsFunction) {
    blockerNotes.push("manualStop is not present as an inert function shell.");
  }
  if (!manualAttachMapSlotIsFunction) {
    blockerNotes.push("manualAttachMap is not present as an inert function shell.");
  }
  if (!manualDrawFrameSlotIsFunction) {
    blockerNotes.push("manualDrawFrame is not present as an inert function shell.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "The execution bridge remains planning-only in this phase.",
    "No execution bridge object or callable gate is created in this phase.",
    "No manual lifecycle control is invoked in this phase.",
    "No renderer lifecycle method is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase."
  ];
  const bridgePlanClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualExecutionBridgePlanHelperExists &&
    !executionBridgeObjectExists &&
    !callableExecutionGateExists &&
    shellExists &&
    shellInert &&
    manualOnly &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    allFiveManualControlsRemainReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 466,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      plannedBridgeName,
      plannedSequenceOrder,
      plannedSafetyGateRequirements,
      manualExecutionBridgePlanHelperExists,
      bridgePlanningOnly: true,
      executionBridgeObjectExists,
      callableExecutionGateExists,
      shellExists,
      shellInert,
      manualOnly,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      allFiveManualControlsRemainReportOnly,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 466,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    plannedBridgeName,
    plannedSequenceOrder,
    plannedSafetyGateRequirements,
    manualExecutionBridgePlanHelperExists,
    bridgePlanningOnly: true,
    executionBridgeObjectExists,
    callableExecutionGateExists,
    shellExists,
    shellInert,
    manualOnly,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    allFiveManualControlsRemainReportOnly,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    startupWired: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-callable-lifecycle-execution-bridge-plan-closeout-documented",
    bridgePlanClosedOut,
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const plannedBridgeName =
    "custom25DVisualManualRendererCallableLifecycleManualExecutionBridge";
  const plannedSequenceOrder = [
    "manualInitialize",
    "manualAttachMap",
    "manualStart",
    "manualDrawFrame",
    "manualStop"
  ];
  const plannedSafetyGateRequirements = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    explicitOptionsOnly: true,
    confirmNoStartupInvocation: true,
    confirmNoUiInvocation: true,
    confirmNoTimerInvocation: true,
    confirmNoAutomaticExecution: true
  };
  const manualExecutionBridgePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
    "function";
  const manualExecutionBridgePlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeShell = bridgeShellExists
    ? custom25DVisualManualRendererCallableLifecycleManualExecutionBridge
    : null;
  const bridgeShellPassiveDataOnly =
    !!(
      bridgeShell &&
      bridgeShell.bridgeName === plannedBridgeName &&
      bridgeShell.passive === true &&
      bridgeShell.reportOnly === true &&
      bridgeShell.dataOnly === true &&
      bridgeShell.localDevOnly === true &&
      bridgeShell.explicitOptionsOnly === true &&
      bridgeShell.manualInvocationOnly === true &&
      bridgeShell.startupWiring === false &&
      bridgeShell.uiControls === false &&
      bridgeShell.timers === false &&
      bridgeShell.automaticExecution === false &&
      bridgeShell.callableExecutionGateCreated === false &&
      bridgeShell.lifecycleExecutionEnabled === false
    );
  const bridgeSequenceMatchesPlan =
    !!(
      bridgeShell &&
      Array.isArray(bridgeShell.plannedSequenceOrder) &&
      bridgeShell.plannedSequenceOrder.length === plannedSequenceOrder.length &&
      plannedSequenceOrder.every(
        (stepName, index) => bridgeShell.plannedSequenceOrder[index] === stepName
      )
    );
  const bridgeSafetyGatesRemainDocumented =
    !!(
      bridgeShell &&
      bridgeShell.requiredSafetyGates &&
      bridgeShell.requiredSafetyGates.manual === true &&
      bridgeShell.requiredSafetyGates.developerIntent === true &&
      bridgeShell.requiredSafetyGates.localDevOnly === true &&
      bridgeShell.requiredSafetyGates.explicitOptionsOnly === true &&
      bridgeShell.requiredSafetyGates.confirmNoStartupInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoUiInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoTimerInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoAutomaticExecution === true
    );
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const shellInert =
    !!(
      shell &&
      shell.inert === true &&
      shell.runtimeBehaviorChanged === false &&
      shell.lifecycleMethodsInvoked === false &&
      shell.startupWiring === false &&
      shell.domControls === false &&
      shell.eventListeners === false &&
      shell.mapAttachment === false &&
      shell.drawing === false &&
      shell.stateMutation === false
    );
  const controlsExist = !!(shell && shell.controls);
  const manualInitializeSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function"
    );
  const manualStartSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStart === "function"
    );
  const manualStopSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualStop === "function"
    );
  const manualAttachMapSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualAttachMap === "function"
    );
  const manualDrawFrameSlotIsFunction =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const allFiveManualLifecycleControlSlotsExist =
    manualInitializeSlotIsFunction &&
    manualStartSlotIsFunction &&
    manualStopSlotIsFunction &&
    manualAttachMapSlotIsFunction &&
    manualDrawFrameSlotIsFunction;
  const allFiveManualControlsRemainReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell" &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell" &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell" &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell" &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const blockerNotes = [];

  if (!manualExecutionBridgePlanHelperExists) {
    blockerNotes.push("Phase 465 execution bridge plan helper is missing.");
  }
  if (!manualExecutionBridgePlanCloseoutHelperExists) {
    blockerNotes.push("Phase 466 execution bridge plan closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!bridgeShellPassiveDataOnly) {
    blockerNotes.push("The execution bridge shell no longer matches the passive data-only contract.");
  }
  if (!bridgeSequenceMatchesPlan) {
    blockerNotes.push("The execution bridge planned sequence order no longer matches the documented plan.");
  }
  if (!bridgeSafetyGatesRemainDocumented) {
    blockerNotes.push("The execution bridge required safety gates are not fully documented.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "The execution bridge shell remains passive and data-only in this phase.",
    "No callable execution gate is created in this phase.",
    "Lifecycle execution remains disabled in this phase.",
    "No manual lifecycle control is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 467,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      plannedBridgeName,
      plannedSequenceOrder,
      plannedSafetyGateRequirements,
      manualExecutionBridgePlanHelperExists,
      manualExecutionBridgePlanCloseoutHelperExists,
      bridgeShellExists,
      bridgeShellPassiveDataOnly,
      bridgePlanningOnly: true,
      callableExecutionGateCreated: false,
      lifecycleExecutionEnabled: false,
      startupWiring: false,
      uiControls: false,
      timers: false,
      automaticExecution: false,
      bridgeSequenceMatchesPlan,
      bridgeSafetyGatesRemainDocumented,
      shellExists,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      allFiveManualControlsRemainReportOnly,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 467,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    plannedBridgeName,
    plannedSequenceOrder,
    plannedSafetyGateRequirements,
    manualExecutionBridgePlanHelperExists,
    manualExecutionBridgePlanCloseoutHelperExists,
    bridgeShellExists,
    bridgeShellPassiveDataOnly,
    bridgePlanningOnly: true,
    callableExecutionGateCreated: false,
    lifecycleExecutionEnabled: false,
    startupWiring: false,
    uiControls: false,
    timers: false,
    automaticExecution: false,
    bridgeSequenceMatchesPlan,
    bridgeSafetyGatesRemainDocumented,
    shellExists,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    allFiveManualControlsRemainReportOnly,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-callable-lifecycle-execution-bridge-shell-created-inert",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const plannedBridgeName =
    "custom25DVisualManualRendererCallableLifecycleManualExecutionBridge";
  const plannedSequenceOrder = [
    "manualInitialize",
    "manualAttachMap",
    "manualStart",
    "manualDrawFrame",
    "manualStop"
  ];
  const plannedSafetyGateRequirements = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    explicitOptionsOnly: true,
    confirmNoStartupInvocation: true,
    confirmNoUiInvocation: true,
    confirmNoTimerInvocation: true,
    confirmNoAutomaticExecution: true
  };
  const manualExecutionBridgePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
    "function";
  const manualExecutionBridgePlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport ===
    "function";
  const manualExecutionBridgeShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeShell = bridgeShellExists
    ? custom25DVisualManualRendererCallableLifecycleManualExecutionBridge
    : null;
  const bridgeShellPassiveDataOnly =
    !!(
      bridgeShell &&
      bridgeShell.bridgeName === plannedBridgeName &&
      bridgeShell.passive === true &&
      bridgeShell.reportOnly === true &&
      bridgeShell.dataOnly === true &&
      bridgeShell.localDevOnly === true &&
      bridgeShell.explicitOptionsOnly === true &&
      bridgeShell.manualInvocationOnly === true
    );
  const bridgeSequenceMatchesPlan =
    !!(
      bridgeShell &&
      Array.isArray(bridgeShell.plannedSequenceOrder) &&
      bridgeShell.plannedSequenceOrder.length === plannedSequenceOrder.length &&
      plannedSequenceOrder.every(
        (stepName, index) => bridgeShell.plannedSequenceOrder[index] === stepName
      )
    );
  const bridgeSafetyGatesRemainDocumented =
    !!(
      bridgeShell &&
      bridgeShell.requiredSafetyGates &&
      bridgeShell.requiredSafetyGates.manual === true &&
      bridgeShell.requiredSafetyGates.developerIntent === true &&
      bridgeShell.requiredSafetyGates.localDevOnly === true &&
      bridgeShell.requiredSafetyGates.explicitOptionsOnly === true &&
      bridgeShell.requiredSafetyGates.confirmNoStartupInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoUiInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoTimerInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoAutomaticExecution === true
    );
  const callableExecutionGateCreated =
    !!(bridgeShell && bridgeShell.callableExecutionGateCreated === true);
  const lifecycleExecutionEnabled =
    !!(bridgeShell && bridgeShell.lifecycleExecutionEnabled === true);
  const startupWiring = !!(bridgeShell && bridgeShell.startupWiring === true);
  const uiControls = !!(bridgeShell && bridgeShell.uiControls === true);
  const timers = !!(bridgeShell && bridgeShell.timers === true);
  const automaticExecution =
    !!(bridgeShell && bridgeShell.automaticExecution === true);
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const controlsExist = !!(shell && shell.controls);
  const allFiveManualLifecycleControlSlotsExist =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      typeof shell.controls.manualStart === "function" &&
      typeof shell.controls.manualStop === "function" &&
      typeof shell.controls.manualAttachMap === "function" &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const allFiveManualControlsRemainReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell" &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell" &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell" &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell" &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const blockerNotes = [];

  if (!manualExecutionBridgePlanHelperExists) {
    blockerNotes.push("Phase 465 bridge plan helper is missing.");
  }
  if (!manualExecutionBridgePlanCloseoutHelperExists) {
    blockerNotes.push("Phase 466 bridge plan closeout helper is missing.");
  }
  if (!manualExecutionBridgeShellCloseoutHelperExists) {
    blockerNotes.push("Phase 467 bridge shell closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!bridgeShellPassiveDataOnly) {
    blockerNotes.push("The execution bridge shell does not match the passive data-only contract.");
  }
  if (callableExecutionGateCreated) {
    blockerNotes.push("A callable execution gate has been created, but readiness must remain passive.");
  }
  if (lifecycleExecutionEnabled) {
    blockerNotes.push("Lifecycle execution is enabled, but readiness must remain passive.");
  }
  if (startupWiring || uiControls || timers || automaticExecution) {
    blockerNotes.push("One or more bridge shell runtime flags no longer remain disabled.");
  }
  if (!bridgeSequenceMatchesPlan) {
    blockerNotes.push("The execution bridge planned sequence order no longer matches the documented plan.");
  }
  if (!bridgeSafetyGatesRemainDocumented) {
    blockerNotes.push("The execution bridge required safety gates are not fully documented.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "Readiness remains blocked by default unless explicit safety options are supplied.",
    "No callable execution gate is created in this phase.",
    "Lifecycle execution remains disabled in this phase.",
    "No manual lifecycle control is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase."
  ];
  const readyForFutureExecutionGatePlanning =
    missingRequiredOptionKeys.length === 0 &&
    manualExecutionBridgePlanHelperExists &&
    manualExecutionBridgePlanCloseoutHelperExists &&
    manualExecutionBridgeShellCloseoutHelperExists &&
    bridgeShellExists &&
    bridgeShellPassiveDataOnly &&
    !callableExecutionGateCreated &&
    !lifecycleExecutionEnabled &&
    !startupWiring &&
    !uiControls &&
    !timers &&
    !automaticExecution &&
    bridgeSequenceMatchesPlan &&
    bridgeSafetyGatesRemainDocumented &&
    shellExists &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    allFiveManualControlsRemainReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 468,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualExecutionBridgePlanHelperExists,
      manualExecutionBridgePlanCloseoutHelperExists,
      manualExecutionBridgeShellCloseoutHelperExists,
      bridgeShellExists,
      bridgeShellPassiveDataOnly,
      callableExecutionGateCreated,
      lifecycleExecutionEnabled,
      startupWiring,
      uiControls,
      timers,
      automaticExecution,
      plannedBridgeName,
      plannedSequenceOrder,
      plannedSafetyGateRequirements,
      bridgeSequenceMatchesPlan,
      bridgeSafetyGatesRemainDocumented,
      shellExists,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      allFiveManualControlsRemainReportOnly,
      readyForFutureExecutionGatePlanning: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      readinessStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 468,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualExecutionBridgePlanHelperExists,
    manualExecutionBridgePlanCloseoutHelperExists,
    manualExecutionBridgeShellCloseoutHelperExists,
    bridgeShellExists,
    bridgeShellPassiveDataOnly,
    callableExecutionGateCreated,
    lifecycleExecutionEnabled,
    startupWiring,
    uiControls,
    timers,
    automaticExecution,
    plannedBridgeName,
    plannedSequenceOrder,
    plannedSafetyGateRequirements,
    bridgeSequenceMatchesPlan,
    bridgeSafetyGatesRemainDocumented,
    shellExists,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    allFiveManualControlsRemainReportOnly,
    readyForFutureExecutionGatePlanning,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    readinessStatus:
      "manual-callable-lifecycle-execution-bridge-shell-readiness-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const shellName =
    "custom25DVisualManualRendererCallableLifecycleManualControlShell";
  const plannedBridgeName =
    "custom25DVisualManualRendererCallableLifecycleManualExecutionBridge";
  const plannedSequenceOrder = [
    "manualInitialize",
    "manualAttachMap",
    "manualStart",
    "manualDrawFrame",
    "manualStop"
  ];
  const plannedSafetyGateRequirements = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    explicitOptionsOnly: true,
    confirmNoStartupInvocation: true,
    confirmNoUiInvocation: true,
    confirmNoTimerInvocation: true,
    confirmNoAutomaticExecution: true
  };
  const manualExecutionBridgePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
    "function";
  const manualExecutionBridgePlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport ===
    "function";
  const manualExecutionBridgeShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport ===
    "function";
  const manualExecutionBridgeShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeShell = bridgeShellExists
    ? custom25DVisualManualRendererCallableLifecycleManualExecutionBridge
    : null;
  const bridgeShellPassiveDataOnly =
    !!(
      bridgeShell &&
      bridgeShell.bridgeName === plannedBridgeName &&
      bridgeShell.passive === true &&
      bridgeShell.reportOnly === true &&
      bridgeShell.dataOnly === true &&
      bridgeShell.localDevOnly === true &&
      bridgeShell.explicitOptionsOnly === true &&
      bridgeShell.manualInvocationOnly === true
    );
  const bridgeSequenceMatchesPlan =
    !!(
      bridgeShell &&
      Array.isArray(bridgeShell.plannedSequenceOrder) &&
      bridgeShell.plannedSequenceOrder.length === plannedSequenceOrder.length &&
      plannedSequenceOrder.every(
        (stepName, index) => bridgeShell.plannedSequenceOrder[index] === stepName
      )
    );
  const bridgeSafetyGatesRemainDocumented =
    !!(
      bridgeShell &&
      bridgeShell.requiredSafetyGates &&
      bridgeShell.requiredSafetyGates.manual === true &&
      bridgeShell.requiredSafetyGates.developerIntent === true &&
      bridgeShell.requiredSafetyGates.localDevOnly === true &&
      bridgeShell.requiredSafetyGates.explicitOptionsOnly === true &&
      bridgeShell.requiredSafetyGates.confirmNoStartupInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoUiInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoTimerInvocation === true &&
      bridgeShell.requiredSafetyGates.confirmNoAutomaticExecution === true
    );
  const callableExecutionGateCreated =
    !!(bridgeShell && bridgeShell.callableExecutionGateCreated === true);
  const lifecycleExecutionEnabled =
    !!(bridgeShell && bridgeShell.lifecycleExecutionEnabled === true);
  const startupWiring = !!(bridgeShell && bridgeShell.startupWiring === true);
  const uiControls = !!(bridgeShell && bridgeShell.uiControls === true);
  const timers = !!(bridgeShell && bridgeShell.timers === true);
  const automaticExecution =
    !!(bridgeShell && bridgeShell.automaticExecution === true);
  const shellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const shell = shellExists
    ? custom25DVisualManualRendererCallableLifecycleManualControlShell
    : null;
  const controlsExist = !!(shell && shell.controls);
  const allFiveManualLifecycleControlSlotsExist =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      typeof shell.controls.manualStart === "function" &&
      typeof shell.controls.manualStop === "function" &&
      typeof shell.controls.manualAttachMap === "function" &&
      typeof shell.controls.manualDrawFrame === "function"
    );
  const allFiveManualControlsRemainReportOnly =
    !!(
      shell &&
      shell.controls &&
      typeof shell.controls.manualInitialize === "function" &&
      shell.controls.manualInitialize.name ===
        "custom25DVisualManualRendererManualInitializeControlShell" &&
      typeof shell.controls.manualStart === "function" &&
      shell.controls.manualStart.name ===
        "custom25DVisualManualRendererManualStartControlShell" &&
      typeof shell.controls.manualStop === "function" &&
      shell.controls.manualStop.name ===
        "custom25DVisualManualRendererManualStopControlShell" &&
      typeof shell.controls.manualAttachMap === "function" &&
      shell.controls.manualAttachMap.name ===
        "custom25DVisualManualRendererManualAttachMapControlShell" &&
      typeof shell.controls.manualDrawFrame === "function" &&
      shell.controls.manualDrawFrame.name ===
        "custom25DVisualManualRendererManualDrawFrameControlShell"
    );
  const blockerNotes = [];

  if (!manualExecutionBridgePlanHelperExists) {
    blockerNotes.push("Phase 465 bridge plan helper is missing.");
  }
  if (!manualExecutionBridgePlanCloseoutHelperExists) {
    blockerNotes.push("Phase 466 bridge plan closeout helper is missing.");
  }
  if (!manualExecutionBridgeShellCloseoutHelperExists) {
    blockerNotes.push("Phase 467 bridge shell closeout helper is missing.");
  }
  if (!manualExecutionBridgeShellReadinessHelperExists) {
    blockerNotes.push("Phase 468 bridge shell readiness helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!bridgeShellPassiveDataOnly) {
    blockerNotes.push("The execution bridge shell no longer matches the passive data-only contract.");
  }
  if (callableExecutionGateCreated) {
    blockerNotes.push("A callable execution gate has been created, but this closeout must remain passive.");
  }
  if (lifecycleExecutionEnabled) {
    blockerNotes.push("Lifecycle execution is enabled, but this closeout must remain passive.");
  }
  if (startupWiring || uiControls || timers || automaticExecution) {
    blockerNotes.push("One or more bridge shell runtime flags no longer remain disabled.");
  }
  if (!bridgeSequenceMatchesPlan) {
    blockerNotes.push("The execution bridge planned sequence order no longer matches the documented plan.");
  }
  if (!bridgeSafetyGatesRemainDocumented) {
    blockerNotes.push("The execution bridge required safety gates are not fully documented.");
  }
  if (!shellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (!allFiveManualControlsRemainReportOnly) {
    blockerNotes.push("One or more manual lifecycle controls no longer matches the inert report-only shell pattern.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "The execution bridge shell readiness chain is inspected only.",
    "No callable execution gate is created in this phase.",
    "Lifecycle execution remains disabled in this phase.",
    "No manual lifecycle control is invoked in this phase.",
    "No live Leaflet map instance is read or required in this phase."
  ];
  const readinessChainClosedOut =
    missingRequiredOptionKeys.length === 0 &&
    manualExecutionBridgePlanHelperExists &&
    manualExecutionBridgePlanCloseoutHelperExists &&
    manualExecutionBridgeShellCloseoutHelperExists &&
    manualExecutionBridgeShellReadinessHelperExists &&
    bridgeShellExists &&
    bridgeShellPassiveDataOnly &&
    !callableExecutionGateCreated &&
    !lifecycleExecutionEnabled &&
    !startupWiring &&
    !uiControls &&
    !timers &&
    !automaticExecution &&
    bridgeSequenceMatchesPlan &&
    bridgeSafetyGatesRemainDocumented &&
    shellExists &&
    controlsExist &&
    allFiveManualLifecycleControlSlotsExist &&
    allFiveManualControlsRemainReportOnly;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 469,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      shellName,
      manualExecutionBridgePlanHelperExists,
      manualExecutionBridgePlanCloseoutHelperExists,
      manualExecutionBridgeShellCloseoutHelperExists,
      manualExecutionBridgeShellReadinessHelperExists,
      bridgeShellExists,
      bridgeShellPassiveDataOnly,
      callableExecutionGateCreated,
      lifecycleExecutionEnabled,
      startupWiring,
      uiControls,
      timers,
      automaticExecution,
      plannedBridgeName,
      plannedSequenceOrder,
      plannedSafetyGateRequirements,
      bridgeSequenceMatchesPlan,
      bridgeSafetyGatesRemainDocumented,
      shellExists,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      allFiveManualControlsRemainReportOnly,
      readinessChainClosedOut: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      closeoutStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 469,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    shellName,
    manualExecutionBridgePlanHelperExists,
    manualExecutionBridgePlanCloseoutHelperExists,
    manualExecutionBridgeShellCloseoutHelperExists,
    manualExecutionBridgeShellReadinessHelperExists,
    bridgeShellExists,
    bridgeShellPassiveDataOnly,
    callableExecutionGateCreated,
    lifecycleExecutionEnabled,
    startupWiring,
    uiControls,
    timers,
    automaticExecution,
    plannedBridgeName,
    plannedSequenceOrder,
    plannedSafetyGateRequirements,
    bridgeSequenceMatchesPlan,
    bridgeSafetyGatesRemainDocumented,
    shellExists,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    allFiveManualControlsRemainReportOnly,
    readinessChainClosedOut,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    rendererStopped: false,
    rendererStarted: false,
    rendererInitialized: false,
    mapAttached: false,
    frameDrawn: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    closeoutStatus:
      "manual-callable-lifecycle-execution-bridge-shell-readiness-closeout-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planningOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const plannedGateName =
    "custom25DVisualManualRendererCallableLifecycleManualExecutionGate";
  const plannedGatePurpose =
    "future manual-only safety gate for lifecycle callable execution";
  const requiredPreconditions = [
    "bridge shell exists",
    "all five manual lifecycle control slots exist",
    "bridge readiness closeout exists"
  ];
  const blockedRuntimeEffects = [
    "startup wiring",
    "UI controls",
    "timers",
    "listeners",
    "DOM",
    "Leaflet",
    "gameplay",
    "backend",
    "storage",
    "network",
    "map attachment",
    "drawing",
    "automatic execution"
  ];
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeShell = bridgeShellExists
    ? custom25DVisualManualRendererCallableLifecycleManualExecutionBridge
    : null;
  const bridgeReadinessCloseoutExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
    "function";
  const controlShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const controlsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls
    );
  const allFiveManualLifecycleControlSlotsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualInitialize ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStart ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStop ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualAttachMap ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualDrawFrame ===
        "function"
    );
  const blockerNotes = [];

  if (!bridgeShellExists) {
    blockerNotes.push("The manual execution bridge shell object is missing.");
  }
  if (!bridgeReadinessCloseoutExists) {
    blockerNotes.push("The bridge readiness closeout helper is missing.");
  }
  if (!controlShellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  const safetyNotes = [
    "This helper is passive and report-only.",
    "The callable execution gate is planned only in this phase.",
    "No callable execution gate or function is created in this phase.",
    "Lifecycle execution remains disabled in this phase.",
    "No manual lifecycle control is invoked in this phase.",
    "No map is attached and no drawing occurs in this phase."
  ];

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 470,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport",
      reportOnly: true,
      passive: true,
      planningOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      callableExecutionGatePlanned: true,
      callableExecutionGateCreated: false,
      callableFunctionCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      plannedGateName,
      plannedGatePurpose,
      requiredPreconditions,
      blockedRuntimeEffects,
      bridgeShellExists,
      bridgeReadinessCloseoutExists,
      controlShellExists,
      controlsExist,
      allFiveManualLifecycleControlSlotsExist,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      mapAttached: false,
      frameDrawn: false,
      liveLeafletMapRead: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      planStatus: "blocked",
      blockerNotes,
      safetyNotes
    };
  }

  return {
    ok: true,
    phase: 470,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport",
    reportOnly: true,
    passive: true,
    planningOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    callableExecutionGatePlanned: true,
    callableExecutionGateCreated: false,
    callableFunctionCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    plannedGateName,
    plannedGatePurpose,
    requiredPreconditions,
    blockedRuntimeEffects,
    bridgeShellExists,
    bridgeReadinessCloseoutExists,
    controlShellExists,
    controlsExist,
    allFiveManualLifecycleControlSlotsExist,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    mapAttached: false,
    frameDrawn: false,
    liveLeafletMapRead: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    planStatus:
      "manual-callable-lifecycle-execution-gate-plan-documented",
    blockerNotes,
    safetyNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const callableExecutionGatePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
    "function";
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const controlShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const controlsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls
    );
  const allFiveManualLifecycleControlSlotsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualInitialize ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStart ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStop ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualAttachMap ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualDrawFrame ===
        "function"
    );
  const blockerNotes = [];
  const safetySummary = [
    "Phase 470 remained planning-only.",
    "No callable gate exists yet.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];

  if (!callableExecutionGatePlanHelperExists) {
    blockerNotes.push("The Phase 470 callable execution gate plan helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The manual execution bridge shell object is missing.");
  }
  if (!bridgeReadinessCloseoutHelperExists) {
    blockerNotes.push("The bridge readiness closeout helper is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (!controlShellExists) {
    blockerNotes.push("The manual control shell object is missing.");
  }
  if (!controlsExist) {
    blockerNotes.push("The manual control shell controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 471,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport",
      reportOnly: true,
      passive: true,
      closeoutOnly: true,
      planningCloseoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      callableExecutionGatePlanHelperExists,
      bridgeShellExists,
      bridgeReadinessCloseoutHelperExists,
      manualControlsCompleteHelperExists,
      allFiveManualLifecycleControlSlotsExist,
      callableExecutionGatePlanned: true,
      callableExecutionGateCreated: false,
      callableFunctionCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      startupWiring: false,
      uiControls: false,
      timers: false,
      automaticExecution: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      liveLeafletMapRead: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      safetySummary,
      closeoutStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 471,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    planningCloseoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    callableExecutionGatePlanHelperExists,
    bridgeShellExists,
    bridgeReadinessCloseoutHelperExists,
    manualControlsCompleteHelperExists,
    allFiveManualLifecycleControlSlotsExist,
    callableExecutionGatePlanned: true,
    callableExecutionGateCreated: false,
    callableFunctionCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    startupWiring: false,
    uiControls: false,
    timers: false,
    automaticExecution: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    liveLeafletMapRead: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    domTouched: false,
    gameplayChanged: false,
    storageTouched: false,
    networkTouched: false,
    safetySummary,
    closeoutStatus:
      "manual-callable-lifecycle-execution-gate-plan-closeout-documented",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gatePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
    "function";
  const gatePlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShell = gateShellExists
    ? custom25DVisualManualRendererCallableLifecycleManualExecutionGate
    : null;
  const controlShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const controlsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls
    );
  const blockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const blockerNotes = [];

  if (!gatePlanHelperExists) {
    blockerNotes.push("The execution gate plan helper is missing.");
  }
  if (!gatePlanCloseoutHelperExists) {
    blockerNotes.push("The execution gate plan closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!controlShellExists || !controlsExist) {
    blockerNotes.push("The manual control shell or controls object is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 472,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport",
      reportOnly: true,
      passive: true,
      closeoutOnly: true,
      shellCloseoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateShellExists,
      gatePlanHelperExists,
      gatePlanCloseoutHelperExists,
      bridgeShellExists,
      callableExecutionGateCreated: !!(
        gateShell && gateShell.callableExecutionGateCreated === true
      ),
      callableFunctionCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      allowedControls: [],
      blockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttached: false,
      drawingOccurred: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      lifecycleMethodsInvoked: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      blockerNotes,
      closeoutStatus: "blocked"
    };
  }

  return {
    ok: true,
    phase: 472,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    shellCloseoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateShellExists,
    gatePlanHelperExists,
    gatePlanCloseoutHelperExists,
    bridgeShellExists,
    callableExecutionGateCreated: !!(
      gateShell && gateShell.callableExecutionGateCreated === true
    ),
    callableFunctionCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    allowedControls: [],
    blockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttached: false,
    drawingOccurred: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    lifecycleMethodsInvoked: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    closeoutStatus:
      "manual-callable-lifecycle-execution-gate-shell-created-inert",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
    "function";
  const gatePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
    "function";
  const gatePlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
    "function";
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const controlShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const controlsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls
    );
  const allFiveManualLifecycleControlSlotsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualInitialize ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStart ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStop ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualAttachMap ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualDrawFrame ===
        "function"
    );
  const blockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const blockerNotes = [];
  const readinessSummary = [
    "The gate shell exists.",
    "The gate shell is still inert and data-only.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];

  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!gateShellCloseoutHelperExists) {
    blockerNotes.push("The gate shell closeout helper is missing.");
  }
  if (!gatePlanHelperExists) {
    blockerNotes.push("The execution gate plan helper is missing.");
  }
  if (!gatePlanCloseoutHelperExists) {
    blockerNotes.push("The execution gate plan closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!bridgeReadinessCloseoutHelperExists) {
    blockerNotes.push("The bridge readiness closeout helper is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (!controlShellExists || !controlsExist) {
    blockerNotes.push("The manual control shell or controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 473,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport",
      reportOnly: true,
      passive: true,
      readinessOnly: true,
      shellReadinessOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateShellExists,
      gateShellCloseoutHelperExists,
      gatePlanHelperExists,
      gatePlanCloseoutHelperExists,
      bridgeShellExists,
      bridgeReadinessCloseoutHelperExists,
      manualControlsCompleteHelperExists,
      allFiveManualLifecycleControlSlotsExist,
      callableExecutionGateCreated: true,
      callableFunctionCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      allowedControls: [],
      blockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttachmentAllowed: false,
      drawingAllowed: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      readinessSummary,
      readinessStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 473,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport",
    reportOnly: true,
    passive: true,
    readinessOnly: true,
    shellReadinessOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateShellExists,
    gateShellCloseoutHelperExists,
    gatePlanHelperExists,
    gatePlanCloseoutHelperExists,
    bridgeShellExists,
    bridgeReadinessCloseoutHelperExists,
    manualControlsCompleteHelperExists,
    allFiveManualLifecycleControlSlotsExist,
    callableExecutionGateCreated: true,
    callableFunctionCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    allowedControls: [],
    blockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttachmentAllowed: false,
    drawingAllowed: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    readinessSummary,
    readinessStatus:
      "manual-callable-lifecycle-execution-gate-shell-readiness-documented",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
    "function";
  const gateShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport ===
    "function";
  const gatePlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
    "function";
  const gatePlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const bridgeReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
    "function";
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const controlShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualControlShell ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualControlShell !== null;
  const controlsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls
    );
  const allFiveManualLifecycleControlSlotsExist =
    !!(
      controlShellExists &&
      custom25DVisualManualRendererCallableLifecycleManualControlShell.controls &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualInitialize ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStart ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualStop ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualAttachMap ===
        "function" &&
      typeof custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualDrawFrame ===
        "function"
    );
  const blockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const blockerNotes = [];
  const closeoutSummary = [
    "Phase 473 remained readiness-only.",
    "The gate shell exists.",
    "The gate shell is still inert and data-only.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];

  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!gateShellCloseoutHelperExists) {
    blockerNotes.push("The gate shell closeout helper is missing.");
  }
  if (!gateShellReadinessHelperExists) {
    blockerNotes.push("The gate shell readiness helper is missing.");
  }
  if (!gatePlanHelperExists) {
    blockerNotes.push("The execution gate plan helper is missing.");
  }
  if (!gatePlanCloseoutHelperExists) {
    blockerNotes.push("The execution gate plan closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!bridgeReadinessCloseoutHelperExists) {
    blockerNotes.push("The bridge readiness closeout helper is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (!controlShellExists || !controlsExist) {
    blockerNotes.push("The manual control shell or controls object is missing.");
  }
  if (!allFiveManualLifecycleControlSlotsExist) {
    blockerNotes.push("Not all five manual lifecycle control slots currently exist.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 474,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      closeoutOnly: true,
      readinessCloseoutOnly: true,
      shellReadinessCloseoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateShellExists,
      gateShellCloseoutHelperExists,
      gateShellReadinessHelperExists,
      gatePlanHelperExists,
      gatePlanCloseoutHelperExists,
      bridgeShellExists,
      bridgeReadinessCloseoutHelperExists,
      manualControlsCompleteHelperExists,
      allFiveManualLifecycleControlSlotsExist,
      callableExecutionGateCreated: true,
      callableFunctionCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      allowedControls: [],
      blockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttachmentAllowed: false,
      drawingAllowed: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      closeoutSummary,
      closeoutStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 474,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    readinessCloseoutOnly: true,
    shellReadinessCloseoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateShellExists,
    gateShellCloseoutHelperExists,
    gateShellReadinessHelperExists,
    gatePlanHelperExists,
    gatePlanCloseoutHelperExists,
    bridgeShellExists,
    bridgeReadinessCloseoutHelperExists,
    manualControlsCompleteHelperExists,
    allFiveManualLifecycleControlSlotsExist,
    callableExecutionGateCreated: true,
    callableFunctionCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    allowedControls: [],
    blockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttachmentAllowed: false,
    drawingAllowed: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    closeoutSummary,
    closeoutStatus:
      "manual-callable-lifecycle-execution-gate-shell-readiness-closeout-documented",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "planningOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
    "function";
  const gateShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport ===
    "function";
  const gateShellReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const blockerNotes = [];
  const plannedBlockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const planningSummary = [
    "This phase only plans the future gate contract.",
    "No contract object is created yet.",
    "No gate decision behavior is created.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];

  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!gateShellCloseoutHelperExists) {
    blockerNotes.push("The gate shell closeout helper is missing.");
  }
  if (!gateShellReadinessHelperExists) {
    blockerNotes.push("The gate shell readiness helper is missing.");
  }
  if (!gateShellReadinessCloseoutHelperExists) {
    blockerNotes.push("The gate shell readiness closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 475,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport",
      reportOnly: true,
      passive: true,
      planningOnly: true,
      contractPlanOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateShellExists,
      gateShellCloseoutHelperExists,
      gateShellReadinessHelperExists,
      gateShellReadinessCloseoutHelperExists,
      bridgeShellExists,
      manualControlsCompleteHelperExists,
      plannedContractName:
        "custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract",
      plannedContractPurpose:
        "future manual-only permission contract for lifecycle execution gate evaluation",
      plannedAllowedControls: [],
      plannedBlockedControls,
      contractCreated: false,
      contractEnforced: false,
      callableFunctionCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      planningSummary,
      blockerNotes,
      planStatus: "blocked"
    };
  }

  return {
    ok: true,
    phase: 475,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport",
    reportOnly: true,
    passive: true,
    planningOnly: true,
    contractPlanOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateShellExists,
    gateShellCloseoutHelperExists,
    gateShellReadinessHelperExists,
    gateShellReadinessCloseoutHelperExists,
    bridgeShellExists,
    manualControlsCompleteHelperExists,
    plannedContractName:
      "custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract",
    plannedContractPurpose:
      "future manual-only permission contract for lifecycle execution gate evaluation",
    plannedAllowedControls: [],
    plannedBlockedControls,
    contractCreated: false,
    contractEnforced: false,
    callableFunctionCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    planningSummary,
    blockerNotes,
    planStatus:
      "manual-callable-lifecycle-execution-gate-contract-plan-documented"
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateContractPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
    "function";
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
    "function";
  const gateShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport ===
    "function";
  const gateShellReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const plannedBlockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const closeoutSummary = [
    "Phase 475 remained planning-only.",
    "No contract object was created.",
    "No permission evaluation behavior was created.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];
  const blockerNotes = [];

  if (!gateContractPlanHelperExists) {
    blockerNotes.push("The execution gate contract plan helper is missing.");
  }
  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!gateShellCloseoutHelperExists) {
    blockerNotes.push("The gate shell closeout helper is missing.");
  }
  if (!gateShellReadinessHelperExists) {
    blockerNotes.push("The gate shell readiness helper is missing.");
  }
  if (!gateShellReadinessCloseoutHelperExists) {
    blockerNotes.push("The gate shell readiness closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 476,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport",
      reportOnly: true,
      passive: true,
      closeoutOnly: true,
      contractPlanCloseoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateContractPlanHelperExists,
      gateShellExists,
      gateShellCloseoutHelperExists,
      gateShellReadinessHelperExists,
      gateShellReadinessCloseoutHelperExists,
      bridgeShellExists,
      manualControlsCompleteHelperExists,
      plannedContractName:
        "custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract",
      contractPlanned: true,
      contractCreated: false,
      contractEnforced: false,
      permissionEvaluationCreated: false,
      callableFunctionCreated: false,
      callableGateBehaviorCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      plannedAllowedControls: [],
      plannedBlockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttachmentAllowed: false,
      drawingAllowed: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      closeoutSummary,
      closeoutStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 476,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    contractPlanCloseoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateContractPlanHelperExists,
    gateShellExists,
    gateShellCloseoutHelperExists,
    gateShellReadinessHelperExists,
    gateShellReadinessCloseoutHelperExists,
    bridgeShellExists,
    manualControlsCompleteHelperExists,
    plannedContractName:
      "custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract",
    contractPlanned: true,
    contractCreated: false,
    contractEnforced: false,
    permissionEvaluationCreated: false,
    callableFunctionCreated: false,
    callableGateBehaviorCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    plannedAllowedControls: [],
    plannedBlockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttachmentAllowed: false,
    drawingAllowed: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    closeoutSummary,
    closeoutStatus:
      "manual-callable-lifecycle-execution-gate-contract-plan-closeout-documented",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateContractShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract !==
      null;
  const gateContractPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
    "function";
  const gateContractPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport ===
    "function";
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const blockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const closeoutSummary = [
    "The contract shell exists as inert data only.",
    "The contract is not enforced.",
    "No permission evaluation behavior was created.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];
  const blockerNotes = [];

  if (!gateContractShellExists) {
    blockerNotes.push("The execution gate contract shell object is missing.");
  }
  if (!gateContractPlanHelperExists) {
    blockerNotes.push("The execution gate contract plan helper is missing.");
  }
  if (!gateContractPlanCloseoutHelperExists) {
    blockerNotes.push(
      "The execution gate contract plan closeout helper is missing."
    );
  }
  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 477,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport",
      reportOnly: true,
      passive: true,
      closeoutOnly: true,
      contractShellCloseoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateContractShellExists,
      gateContractPlanHelperExists,
      gateContractPlanCloseoutHelperExists,
      gateShellExists,
      bridgeShellExists,
      manualControlsCompleteHelperExists,
      contractCreated: gateContractShellExists,
      contractEnforced: false,
      permissionEvaluationCreated: false,
      callableFunctionCreated: false,
      callableGateBehaviorCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      allowedControls: [],
      blockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttachmentAllowed: false,
      drawingAllowed: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      gameplayChangesAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      closeoutSummary,
      closeoutStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 477,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    contractShellCloseoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateContractShellExists,
    gateContractPlanHelperExists,
    gateContractPlanCloseoutHelperExists,
    gateShellExists,
    bridgeShellExists,
    manualControlsCompleteHelperExists,
    contractCreated: gateContractShellExists,
    contractEnforced: false,
    permissionEvaluationCreated: false,
    callableFunctionCreated: false,
    callableGateBehaviorCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    allowedControls: [],
    blockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttachmentAllowed: false,
    drawingAllowed: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    gameplayChangesAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    closeoutSummary,
    closeoutStatus:
      "manual-callable-lifecycle-execution-gate-contract-shell-closeout-documented",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "readinessOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateContractShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract !==
      null;
  const gateContractShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport ===
    "function";
  const gateContractPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
    "function";
  const gateContractPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport ===
    "function";
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShellReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const contractRequiredOptions = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly"
  ];
  const contractRequiredOptionValues = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    explicitOptionsOnly: true
  };
  const blockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const readinessSummary = [
    "The contract shell exists as inert data only.",
    "The gate shell exists.",
    "The contract is not enforced.",
    "No permission evaluation behavior exists.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];
  const blockerNotes = [];

  if (!gateContractShellExists) {
    blockerNotes.push("The execution gate contract shell object is missing.");
  }
  if (!gateContractShellCloseoutHelperExists) {
    blockerNotes.push("The execution gate contract shell closeout helper is missing.");
  }
  if (!gateContractPlanHelperExists) {
    blockerNotes.push("The execution gate contract plan helper is missing.");
  }
  if (!gateContractPlanCloseoutHelperExists) {
    blockerNotes.push(
      "The execution gate contract plan closeout helper is missing."
    );
  }
  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!gateShellReadinessCloseoutHelperExists) {
    blockerNotes.push("The execution gate shell readiness closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 478,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport",
      reportOnly: true,
      passive: true,
      readinessOnly: true,
      contractShellReadinessOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateContractShellExists,
      gateContractShellCloseoutHelperExists,
      gateContractPlanHelperExists,
      gateContractPlanCloseoutHelperExists,
      gateShellExists,
      gateShellReadinessCloseoutHelperExists,
      bridgeShellExists,
      manualControlsCompleteHelperExists,
      contractCreated: gateContractShellExists,
      contractEnforced: false,
      permissionEvaluationCreated: false,
      callableFunctionCreated: false,
      callableGateBehaviorCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      requiredOptions: contractRequiredOptions,
      requiredOptionValues: contractRequiredOptionValues,
      allowedControls: [],
      blockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttachmentAllowed: false,
      drawingAllowed: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      gameplayChangesAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      readinessSummary,
      readinessStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 478,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport",
    reportOnly: true,
    passive: true,
    readinessOnly: true,
    contractShellReadinessOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateContractShellExists,
    gateContractShellCloseoutHelperExists,
    gateContractPlanHelperExists,
    gateContractPlanCloseoutHelperExists,
    gateShellExists,
    gateShellReadinessCloseoutHelperExists,
    bridgeShellExists,
    manualControlsCompleteHelperExists,
    contractCreated: gateContractShellExists,
    contractEnforced: false,
    permissionEvaluationCreated: false,
    callableFunctionCreated: false,
    callableGateBehaviorCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    requiredOptions: contractRequiredOptions,
    requiredOptionValues: contractRequiredOptionValues,
    allowedControls: [],
    blockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttachmentAllowed: false,
    drawingAllowed: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    gameplayChangesAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    readinessSummary,
    readinessStatus:
      "manual-callable-lifecycle-execution-gate-contract-shell-readiness-documented",
    blockerNotes
  };
}

function getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "closeoutOnly",
    "confirmNoLifecycleInvocation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStop",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStateMutation",
    "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const gateContractShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract !==
      null;
  const gateContractShellCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport ===
    "function";
  const gateContractShellReadinessHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport ===
    "function";
  const gateContractPlanHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
    "function";
  const gateContractPlanCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport ===
    "function";
  const gateShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionGate ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionGate !== null;
  const gateShellReadinessCloseoutHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
    "function";
  const bridgeShellExists =
    typeof custom25DVisualManualRendererCallableLifecycleManualExecutionBridge ===
    "object" &&
    custom25DVisualManualRendererCallableLifecycleManualExecutionBridge !== null;
  const manualControlsCompleteHelperExists =
    typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
    "function";
  const contractRequiredOptions = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly"
  ];
  const contractRequiredOptionValues = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    explicitOptionsOnly: true
  };
  const blockedControls = [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ];
  const closeoutSummary = [
    "Phase 478 remained readiness-only.",
    "The contract shell exists as inert data only.",
    "The gate shell exists.",
    "The contract is not enforced.",
    "No permission evaluation behavior exists.",
    "No callable execution function exists.",
    "No lifecycle execution is possible.",
    "No lifecycle controls were invoked.",
    "No map was attached.",
    "No drawing occurred.",
    "No DOM, Leaflet, gameplay, backend, storage, network, startup, timer, listener, UI, or runtime behavior changed."
  ];
  const blockerNotes = [];

  if (!gateContractShellExists) {
    blockerNotes.push("The execution gate contract shell object is missing.");
  }
  if (!gateContractShellCloseoutHelperExists) {
    blockerNotes.push("The execution gate contract shell closeout helper is missing.");
  }
  if (!gateContractShellReadinessHelperExists) {
    blockerNotes.push("The execution gate contract shell readiness helper is missing.");
  }
  if (!gateContractPlanHelperExists) {
    blockerNotes.push("The execution gate contract plan helper is missing.");
  }
  if (!gateContractPlanCloseoutHelperExists) {
    blockerNotes.push(
      "The execution gate contract plan closeout helper is missing."
    );
  }
  if (!gateShellExists) {
    blockerNotes.push("The execution gate shell object is missing.");
  }
  if (!gateShellReadinessCloseoutHelperExists) {
    blockerNotes.push("The execution gate shell readiness closeout helper is missing.");
  }
  if (!bridgeShellExists) {
    blockerNotes.push("The execution bridge shell object is missing.");
  }
  if (!manualControlsCompleteHelperExists) {
    blockerNotes.push("The manual controls complete helper is missing.");
  }
  if (missingRequiredOptionKeys.length > 0) {
    blockerNotes.push("Required explicit safety options are missing.");
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 479,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport",
      reportOnly: true,
      passive: true,
      closeoutOnly: true,
      readinessCloseoutOnly: true,
      contractShellReadinessCloseoutOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      gateContractShellExists,
      gateContractShellCloseoutHelperExists,
      gateContractShellReadinessHelperExists,
      gateContractPlanHelperExists,
      gateContractPlanCloseoutHelperExists,
      gateShellExists,
      gateShellReadinessCloseoutHelperExists,
      bridgeShellExists,
      manualControlsCompleteHelperExists,
      contractCreated: gateContractShellExists,
      contractEnforced: false,
      permissionEvaluationCreated: false,
      callableFunctionCreated: false,
      callableGateBehaviorCreated: false,
      lifecycleExecutionEnabled: false,
      manualControlInvocationAllowed: false,
      requiredOptions: contractRequiredOptions,
      requiredOptionValues: contractRequiredOptionValues,
      allowedControls: [],
      blockedControls,
      startupWiring: false,
      uiControls: false,
      timers: false,
      listeners: false,
      automaticExecution: false,
      mapAttachmentAllowed: false,
      drawingAllowed: false,
      domMutationAllowed: false,
      backendAccessAllowed: false,
      storageAccessAllowed: false,
      networkAccessAllowed: false,
      gameplayChangesAllowed: false,
      mapAttached: false,
      drawingOccurred: false,
      manualInitializeInvoked: false,
      manualStartInvoked: false,
      manualStopInvoked: false,
      manualAttachMapInvoked: false,
      manualDrawFrameInvoked: false,
      closeoutSummary,
      closeoutStatus: "blocked",
      blockerNotes
    };
  }

  return {
    ok: true,
    phase: 479,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport",
    reportOnly: true,
    passive: true,
    closeoutOnly: true,
    readinessCloseoutOnly: true,
    contractShellReadinessCloseoutOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    gateContractShellExists,
    gateContractShellCloseoutHelperExists,
    gateContractShellReadinessHelperExists,
    gateContractPlanHelperExists,
    gateContractPlanCloseoutHelperExists,
    gateShellExists,
    gateShellReadinessCloseoutHelperExists,
    bridgeShellExists,
    manualControlsCompleteHelperExists,
    contractCreated: gateContractShellExists,
    contractEnforced: false,
    permissionEvaluationCreated: false,
    callableFunctionCreated: false,
    callableGateBehaviorCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    requiredOptions: contractRequiredOptions,
    requiredOptionValues: contractRequiredOptionValues,
    allowedControls: [],
    blockedControls,
    startupWiring: false,
    uiControls: false,
    timers: false,
    listeners: false,
    automaticExecution: false,
    mapAttachmentAllowed: false,
    drawingAllowed: false,
    domMutationAllowed: false,
    backendAccessAllowed: false,
    storageAccessAllowed: false,
    networkAccessAllowed: false,
    gameplayChangesAllowed: false,
    mapAttached: false,
    drawingOccurred: false,
    manualInitializeInvoked: false,
    manualStartInvoked: false,
    manualStopInvoked: false,
    manualAttachMapInvoked: false,
    manualDrawFrameInvoked: false,
    closeoutSummary,
    closeoutStatus:
      "manual-callable-lifecycle-execution-gate-contract-shell-readiness-closeout-documented",
    blockerNotes
  };
}

const custom25DVisualManualRendererCallableLifecycleManualExecutionBridge = {
  bridgeName:
    "custom25DVisualManualRendererCallableLifecycleManualExecutionBridge",
  passive: true,
  reportOnly: true,
  dataOnly: true,
  localDevOnly: true,
  explicitOptionsOnly: true,
  manualInvocationOnly: true,
  startupWiring: false,
  uiControls: false,
  timers: false,
  automaticExecution: false,
  callableExecutionGateCreated: false,
  lifecycleExecutionEnabled: false,
  plannedSequenceOrder: [
    "manualInitialize",
    "manualAttachMap",
    "manualStart",
    "manualDrawFrame",
    "manualStop"
  ],
  requiredSafetyGates: {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    explicitOptionsOnly: true,
    confirmNoStartupInvocation: true,
    confirmNoUiInvocation: true,
    confirmNoTimerInvocation: true,
    confirmNoAutomaticExecution: true
  }
};

const custom25DVisualManualRendererCallableLifecycleManualExecutionGate = {
  phase: 472,
  shellName:
    "custom25DVisualManualRendererCallableLifecycleManualExecutionGate",
  shellOnly: true,
  passive: true,
  inert: true,
  callableExecutionGateCreated: true,
  callableFunctionCreated: false,
  lifecycleExecutionEnabled: false,
  manualControlInvocationAllowed: false,
  startupWiring: false,
  uiControls: false,
  timers: false,
  listeners: false,
  automaticExecution: false,
  mapAttachmentAllowed: false,
  drawingAllowed: false,
  domMutationAllowed: false,
  backendAccessAllowed: false,
  storageAccessAllowed: false,
  networkAccessAllowed: false,
  allowedControls: [],
  blockedControls: [
    "manualInitialize",
    "manualStart",
    "manualStop",
    "manualAttachMap",
    "manualDrawFrame"
  ],
  safetySummary:
    "Execution gate shell exists as inert data only and cannot execute lifecycle controls."
};

const custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract =
  {
    phase: 477,
    contractName:
      "custom25DVisualManualRendererCallableLifecycleManualExecutionGateContract",
    shellOnly: true,
    passive: true,
    inert: true,
    contractCreated: true,
    contractEnforced: false,
    permissionEvaluationCreated: false,
    callableFunctionCreated: false,
    callableGateBehaviorCreated: false,
    lifecycleExecutionEnabled: false,
    manualControlInvocationAllowed: false,
    requiredOptions: [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly"
    ],
    requiredOptionValues: {
      manual: true,
      developerIntent: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      explicitOptionsOnly: true
    },
    allowedControls: [],
    blockedControls: [
      "manualInitialize",
      "manualStart",
      "manualStop",
      "manualAttachMap",
      "manualDrawFrame"
    ],
    blockedRuntimeEffects: [
      "startupWiring",
      "uiControls",
      "timers",
      "listeners",
      "automaticExecution",
      "mapAttachment",
      "drawing",
      "domMutation",
      "backendAccess",
      "storageAccess",
      "networkAccess",
      "gameplayChanges"
    ],
    safetySummary:
      "Execution gate contract shell exists as inert data only and is not enforced."
  };

custom25DVisualManualRendererCallableLifecycleManualControlShell.controls.manualDrawFrame =
  function custom25DVisualManualRendererManualDrawFrameControlShell(
    options = {}
  ) {
    const requiredOptionKeys = [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "reportOnly",
      "confirmNoLifecycleInvocation",
      "confirmNoRendererInitialization",
      "confirmNoRendererStart",
      "confirmNoRendererStop",
      "confirmNoMapAttachment",
      "confirmNoDrawing",
      "confirmNoStateMutation",
      "confirmNoDomLeafletGameplayBackendStorageNetworkChange",
      "confirmNoAutomaticInvocation"
    ];
    const missingRequiredOptionKeys = requiredOptionKeys.filter(
      (key) => options[key] !== true
    );

    if (missingRequiredOptionKeys.length > 0) {
      return {
        ok: false,
        phase: 460,
        controlName: "manualDrawFrame",
        shellName:
          "custom25DVisualManualRendererCallableLifecycleManualControlShell",
        inert: true,
        manualOnly: true,
        reportOnly: true,
        blocked: true,
        allowed: false,
        jsonSafeResult: true,
        requiredOptionKeys,
        missingRequiredOptionKeys,
        lifecycleMethodInvoked: false,
        rendererStopped: false,
        rendererStarted: false,
        rendererInitialized: false,
        mapAttached: false,
        frameDrawn: false,
        sharedStateMutated: false,
        runtimeBehaviorChanged: false,
        domTouched: false,
        startupWired: false,
        gameplayChanged: false,
        storageTouched: false,
        networkTouched: false,
        controlStatus: "blocked"
      };
    }

    return {
      ok: true,
      phase: 460,
      controlName: "manualDrawFrame",
      shellName:
        "custom25DVisualManualRendererCallableLifecycleManualControlShell",
      inert: true,
      manualOnly: true,
      reportOnly: true,
      blocked: false,
      allowed: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      lifecycleMethodInvoked: false,
      rendererStopped: false,
      rendererStarted: false,
      rendererInitialized: false,
      mapAttached: false,
      frameDrawn: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      domTouched: false,
      startupWired: false,
      gameplayChanged: false,
      storageTouched: false,
      networkTouched: false,
      controlStatus: "manual-draw-frame-control-shell-inert"
    };
  };

function getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase438",
    "closeoutOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShell = state && state.rendererInstanceShell
    ? state.rendererInstanceShell
    : null;
  const rendererInstanceShellExists = !!rendererInstanceShell;
  const rendererInstanceShellInert =
    !!(
      rendererInstanceShell &&
      rendererInstanceShell.inert === true &&
      rendererInstanceShell.initialized === false &&
      rendererInstanceShell.started === false &&
      rendererInstanceShell.mapAttached === false &&
      rendererInstanceShell.drawingStarted === false &&
      rendererInstanceShell.startupWired === false &&
      rendererInstanceShell.gameplayChanged === false &&
      rendererInstanceShell.backendStorageNetworkChanged === false
    );
  const lifecycleMethodShell = rendererInstanceShell && rendererInstanceShell.lifecycleMethodShell
    ? rendererInstanceShell.lifecycleMethodShell
    : null;
  const lifecycleMethodShellExists = !!lifecycleMethodShell;
  const phase437LifecycleMethodShellDetected =
    !!(lifecycleMethodShell && lifecycleMethodShell.phaseCreated === 437);
  const phase437LifecycleMethodShellFieldsValid =
    !!(
      lifecycleMethodShell &&
      lifecycleMethodShell.phaseCreated === 437 &&
      lifecycleMethodShell.shellType === "manual-renderer-lifecycle-method-shell" &&
      lifecycleMethodShell.inert === true &&
      lifecycleMethodShell.manualOnly === true &&
      lifecycleMethodShell.callableMethodsCreated === false &&
      lifecycleMethodShell.initializeCreated === false &&
      lifecycleMethodShell.startCreated === false &&
      lifecycleMethodShell.stopCreated === false &&
      lifecycleMethodShell.attachMapCreated === false &&
      lifecycleMethodShell.drawFrameCreated === false &&
      lifecycleMethodShell.rendererInitialized === false &&
      lifecycleMethodShell.rendererStarted === false &&
      lifecycleMethodShell.rendererStopped === false &&
      lifecycleMethodShell.mapAttached === false &&
      lifecycleMethodShell.drawingStarted === false &&
      lifecycleMethodShell.startupWired === false &&
      lifecycleMethodShell.domMutated === false &&
      lifecycleMethodShell.leafletMutated === false &&
      lifecycleMethodShell.gameplayChanged === false &&
      lifecycleMethodShell.backendStorageNetworkChanged === false &&
      lifecycleMethodShell.persistenceChanged === false &&
      lifecycleMethodShell.runtimeBehaviorChanged === false &&
      lifecycleMethodShell.createdAtManualOnly === true
    );
  const lifecycleMethodShellInert = phase437LifecycleMethodShellFieldsValid;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 438,
      helperName:
        "getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      closeoutStatus: "blocked",
      stateContainerExists,
      stateContainerReplaced: false,
      rendererInstanceShellExists,
      rendererInstanceShellReplaced: false,
      rendererInstanceShellInert: false,
      phase437LifecycleMethodShellDetected: false,
      phase437LifecycleMethodShellFieldsValid: false,
      lifecycleMethodShellExists,
      lifecycleMethodShellInert: false,
      callableLifecycleMethodsCreated: false,
      initializeCreated: false,
      startCreated: false,
      stopCreated: false,
      attachMapCreated: false,
      drawFrameCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererStopped: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false,
      plannedNextPhase: 439,
      plannedNextMilestone:
        "manual-renderer-callable-lifecycle-method-readiness"
    };
  }

  return {
    ok:
      stateContainerExists &&
      rendererInstanceShellExists &&
      rendererInstanceShellInert &&
      phase437LifecycleMethodShellDetected &&
      phase437LifecycleMethodShellFieldsValid &&
      lifecycleMethodShellExists &&
      lifecycleMethodShellInert,
    phase: 438,
    helperName:
      "getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    closeoutStatus:
      "manual-renderer-lifecycle-method-shell-foundation-closed-out",
    stateContainerExists,
    stateContainerReplaced: false,
    rendererInstanceShellExists,
    rendererInstanceShellReplaced: false,
    rendererInstanceShellInert,
    phase437LifecycleMethodShellDetected,
    phase437LifecycleMethodShellFieldsValid,
    lifecycleMethodShellExists,
    lifecycleMethodShellInert,
    callableLifecycleMethodsCreated: false,
    initializeCreated: false,
    startCreated: false,
    stopCreated: false,
    attachMapCreated: false,
    drawFrameCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    rendererStopped: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    domMutated: false,
    leafletMutated: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    persistenceChanged: false,
    runtimeBehaviorChanged: false,
    plannedNextPhase: 439,
    plannedNextMilestone:
      "manual-renderer-callable-lifecycle-method-readiness"
  };
}

function getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase438",
    "readinessOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const closeoutHelperAvailable =
    typeof getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShell = state && state.rendererInstanceShell
    ? state.rendererInstanceShell
    : null;
  const rendererInstanceShellExists = !!rendererInstanceShell;
  const lifecycleMethodShell = rendererInstanceShell && rendererInstanceShell.lifecycleMethodShell
    ? rendererInstanceShell.lifecycleMethodShell
    : null;
  const lifecycleMethodShellExists = !!lifecycleMethodShell;
  const phase437LifecycleMethodShellDetected =
    !!(lifecycleMethodShell && lifecycleMethodShell.phaseCreated === 437);
  const phase437LifecycleMethodShellFieldsValid =
    !!(
      lifecycleMethodShell &&
      lifecycleMethodShell.phaseCreated === 437 &&
      lifecycleMethodShell.shellType === "manual-renderer-lifecycle-method-shell" &&
      lifecycleMethodShell.inert === true &&
      lifecycleMethodShell.manualOnly === true &&
      lifecycleMethodShell.callableMethodsCreated === false &&
      lifecycleMethodShell.initializeCreated === false &&
      lifecycleMethodShell.startCreated === false &&
      lifecycleMethodShell.stopCreated === false &&
      lifecycleMethodShell.attachMapCreated === false &&
      lifecycleMethodShell.drawFrameCreated === false
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 438,
      helperName:
        "getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      closeoutHelperAvailable,
      stateContainerExists,
      rendererInstanceShellExists,
      lifecycleMethodShellExists,
      phase437LifecycleMethodShellDetected: false,
      phase437LifecycleMethodShellFieldsValid: false,
      readinessStatus: "blocked",
      callableLifecycleMethodPlanningAllowedNext: false,
      callableLifecycleMethodCreationAllowedNow: false,
      mustRemainManualOnly: true,
      mustRemainInert: true,
      mustNotCreateCallableMethods: true,
      mustNotInitializeRenderer: true,
      mustNotStartRenderer: true,
      mustNotStopRenderer: true,
      mustNotAttachMap: true,
      mustNotDraw: true,
      mustNotWireStartup: true,
      mustNotChangeDomLeafletGameplayBackendStorageNetwork: true,
      plannedNextPhase: 439,
      plannedNextStep:
        "manual-renderer-callable-lifecycle-method-readiness-or-plan"
    };
  }

  return {
    ok: true,
    phase: 438,
    helperName:
      "getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    closeoutHelperAvailable,
    stateContainerExists,
    rendererInstanceShellExists,
    lifecycleMethodShellExists,
    phase437LifecycleMethodShellDetected,
    phase437LifecycleMethodShellFieldsValid,
    readinessStatus: "post-lifecycle-method-shell-readiness-documented",
    callableLifecycleMethodPlanningAllowedNext: true,
    callableLifecycleMethodCreationAllowedNow: false,
    mustRemainManualOnly: true,
    mustRemainInert: true,
    mustNotCreateCallableMethods: true,
    mustNotInitializeRenderer: true,
    mustNotStartRenderer: true,
    mustNotStopRenderer: true,
    mustNotAttachMap: true,
    mustNotDraw: true,
    mustNotWireStartup: true,
    mustNotChangeDomLeafletGameplayBackendStorageNetwork: true,
    plannedNextPhase: 439,
    plannedNextStep:
      "manual-renderer-callable-lifecycle-method-readiness-or-plan"
  };
}

function getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase438",
    "transitionPlanOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 438,
      helperName:
        "getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      transitionPlanOnly: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      transitionStatus: "blocked",
      previousMilestone: "manual-renderer-lifecycle-method-shell-foundation",
      currentMilestone: "manual-renderer-lifecycle-method-shell-closeout",
      nextMilestone: "manual-renderer-callable-lifecycle-method-planning",
      excitingBoundaryCrossedInPreviousPhase: true,
      nextExcitingBoundary: "manual-renderer-callable-lifecycle-method-creation",
      nextExcitingBoundaryAllowedNow: false,
      callableLifecycleMethodCreationRequiresSeparatePhase: true,
      callableLifecycleMethodCreationAllowedNow: false,
      callableLifecycleMethodsMustStayInert: true,
      callableLifecycleMethodsMustBeManualOnly: true,
      noCallableLifecycleMethodsCreated: true,
      noStateCreation: true,
      noStateMutation: true,
      noRendererInitialization: true,
      noRendererStart: true,
      noRendererStopBehavior: true,
      noMapAttachment: true,
      noDrawing: true,
      noStartupWiring: true,
      noDomLeafletGameplayBackendStorageNetworkChange: true,
      plannedNextPhase: 439
    };
  }

  return {
    ok: true,
    phase: 438,
    helperName:
      "getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    transitionPlanOnly: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    transitionStatus:
      "lifecycle-method-shell-foundation-to-callable-lifecycle-method-planning-documented",
    previousMilestone: "manual-renderer-lifecycle-method-shell-foundation",
    currentMilestone: "manual-renderer-lifecycle-method-shell-closeout",
    nextMilestone: "manual-renderer-callable-lifecycle-method-planning",
    excitingBoundaryCrossedInPreviousPhase: true,
    nextExcitingBoundary: "manual-renderer-callable-lifecycle-method-creation",
    nextExcitingBoundaryAllowedNow: false,
    callableLifecycleMethodCreationRequiresSeparatePhase: true,
    callableLifecycleMethodCreationAllowedNow: false,
    callableLifecycleMethodsMustStayInert: true,
    callableLifecycleMethodsMustBeManualOnly: true,
    noCallableLifecycleMethodsCreated: true,
    noStateCreation: true,
    noStateMutation: true,
    noRendererInitialization: true,
    noRendererStart: true,
    noRendererStopBehavior: true,
    noMapAttachment: true,
    noDrawing: true,
    noStartupWiring: true,
    noDomLeafletGameplayBackendStorageNetworkChange: true,
    plannedNextPhase: 439
  };
}

function getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase439",
    "readinessOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const rendererInstanceShell = state && state.rendererInstanceShell
    ? state.rendererInstanceShell
    : null;
  const rendererInstanceShellExists = !!rendererInstanceShell;
  const lifecycleMethodShell = rendererInstanceShell && rendererInstanceShell.lifecycleMethodShell
    ? rendererInstanceShell.lifecycleMethodShell
    : null;
  const lifecycleMethodShellExists = !!lifecycleMethodShell;
  const lifecycleMethodShellInert =
    !!(
      lifecycleMethodShell &&
      lifecycleMethodShell.inert === true &&
      lifecycleMethodShell.manualOnly === true &&
      lifecycleMethodShell.callableMethodsCreated === false &&
      lifecycleMethodShell.initializeCreated === false &&
      lifecycleMethodShell.startCreated === false &&
      lifecycleMethodShell.stopCreated === false &&
      lifecycleMethodShell.attachMapCreated === false &&
      lifecycleMethodShell.drawFrameCreated === false &&
      lifecycleMethodShell.rendererInitialized === false &&
      lifecycleMethodShell.rendererStarted === false &&
      lifecycleMethodShell.rendererStopped === false &&
      lifecycleMethodShell.mapAttached === false &&
      lifecycleMethodShell.drawingStarted === false &&
      lifecycleMethodShell.startupWired === false &&
      lifecycleMethodShell.domMutated === false &&
      lifecycleMethodShell.leafletMutated === false &&
      lifecycleMethodShell.gameplayChanged === false &&
      lifecycleMethodShell.backendStorageNetworkChanged === false &&
      lifecycleMethodShell.persistenceChanged === false &&
      lifecycleMethodShell.runtimeBehaviorChanged === false
    );
  const initializeExists =
    !!(lifecycleMethodShell && typeof lifecycleMethodShell.initialize !== "undefined");
  const startExists =
    !!(lifecycleMethodShell && typeof lifecycleMethodShell.start !== "undefined");
  const stopExists =
    !!(lifecycleMethodShell && typeof lifecycleMethodShell.stop !== "undefined");
  const attachMapExists =
    !!(lifecycleMethodShell && typeof lifecycleMethodShell.attachMap !== "undefined");
  const drawFrameExists =
    !!(lifecycleMethodShell && typeof lifecycleMethodShell.drawFrame !== "undefined");
  const callableLifecycleMethodsExist =
    initializeExists ||
    startExists ||
    stopExists ||
    attachMapExists ||
    drawFrameExists;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 439,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      jsonSafeResult: true,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      readinessStatus: "blocked",
      stateContainerExists,
      rendererInstanceShellExists,
      lifecycleMethodShellExists,
      lifecycleMethodShellInert: false,
      callableLifecycleMethodsExist: false,
      initializeExists: false,
      startExists: false,
      stopExists: false,
      attachMapExists: false,
      drawFrameExists: false,
      callableLifecycleMethodPlaceholderCreationAllowedNext: true,
      callableLifecycleMethodPlaceholderCreationAllowedNow: false,
      mustNotCreateCallableMethods: true,
      mustNotInitializeRenderer: true,
      mustNotStartRenderer: true,
      mustNotStopRenderer: true,
      mustNotAttachMap: true,
      mustNotDraw: true,
      mustNotWireStartup: true,
      mustNotChangeDomLeafletGameplayBackendStorageNetwork: true,
      plannedNextPhase: 440,
      plannedNextStep:
        "manual-renderer-inert-callable-lifecycle-method-placeholder-foundation"
    };
  }

  return {
    ok: true,
    phase: 439,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    jsonSafeResult: true,
    missingRequiredOptionKeys: [],
    requiredOptionKeys,
    readinessStatus: "callable-lifecycle-method-readiness-documented",
    stateContainerExists,
    rendererInstanceShellExists,
    lifecycleMethodShellExists,
    lifecycleMethodShellInert,
    callableLifecycleMethodsExist,
    initializeExists,
    startExists,
    stopExists,
    attachMapExists,
    drawFrameExists,
    callableLifecycleMethodPlaceholderCreationAllowedNext: true,
    callableLifecycleMethodPlaceholderCreationAllowedNow: false,
    mustNotCreateCallableMethods: true,
    mustNotInitializeRenderer: true,
    mustNotStartRenderer: true,
    mustNotStopRenderer: true,
    mustNotAttachMap: true,
    mustNotDraw: true,
    mustNotWireStartup: true,
    mustNotChangeDomLeafletGameplayBackendStorageNetwork: true,
    plannedNextPhase: 440,
    plannedNextStep:
      "manual-renderer-inert-callable-lifecycle-method-placeholder-foundation"
  };
}

function getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase439",
    "riskBoundaryOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 439,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      riskBoundaryOnly: true,
      jsonSafeResult: true,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      riskBoundaryStatus: "blocked",
      currentBoundary: "manual-renderer-callable-lifecycle-method-planning",
      nextExcitingBoundary:
        "manual-renderer-inert-callable-lifecycle-method-placeholder-creation",
      excitingBoundaryAllowedNow: false,
      futurePlaceholderCreationRequiresSeparatePhase: true,
      futurePlaceholdersMustStayInert: true,
      futurePlaceholdersMustBeManualOnly: true,
      futurePlaceholdersMustNotCallEachOther: true,
      futurePlaceholdersMustNotInitializeRenderer: true,
      futurePlaceholdersMustNotStartRenderer: true,
      futurePlaceholdersMustNotAttachMap: true,
      futurePlaceholdersMustNotDraw: true,
      futurePlaceholdersMustNotWireStartup: true,
      noCallbacksHooksTimersListenersAnimationFrames: true,
      noDomLeafletGameplayBackendStorageNetworkPersistenceChange: true,
      callableLifecycleMethodsCreatedNow: false,
      plannedNextPhase: 440
    };
  }

  return {
    ok: true,
    phase: 439,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    riskBoundaryOnly: true,
    jsonSafeResult: true,
    missingRequiredOptionKeys: [],
    requiredOptionKeys,
    riskBoundaryStatus:
      "callable-lifecycle-method-placeholder-risk-boundary-documented",
    currentBoundary: "manual-renderer-callable-lifecycle-method-planning",
    nextExcitingBoundary:
      "manual-renderer-inert-callable-lifecycle-method-placeholder-creation",
    excitingBoundaryAllowedNow: false,
    futurePlaceholderCreationRequiresSeparatePhase: true,
    futurePlaceholdersMustStayInert: true,
    futurePlaceholdersMustBeManualOnly: true,
    futurePlaceholdersMustNotCallEachOther: true,
    futurePlaceholdersMustNotInitializeRenderer: true,
    futurePlaceholdersMustNotStartRenderer: true,
    futurePlaceholdersMustNotAttachMap: true,
    futurePlaceholdersMustNotDraw: true,
    futurePlaceholdersMustNotWireStartup: true,
    noCallbacksHooksTimersListenersAnimationFrames: true,
    noDomLeafletGameplayBackendStorageNetworkPersistenceChange: true,
    callableLifecycleMethodsCreatedNow: false,
    plannedNextPhase: 440
  };
}

function getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase439",
    "transitionPlanOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoCallableLifecycleMethods",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoLeafletMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 439,
      helperName:
        "getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      transitionPlanOnly: true,
      jsonSafeResult: true,
      missingRequiredOptionKeys,
      requiredOptionKeys,
      transitionStatus: "blocked",
      previousMilestone: "manual-renderer-lifecycle-method-shell-foundation",
      currentMilestone: "manual-renderer-callable-lifecycle-method-planning",
      nextMilestone:
        "manual-renderer-inert-callable-lifecycle-method-placeholder-foundation",
      excitingBoundaryCrossedInPreviousPhase: false,
      nextExcitingBoundary:
        "manual-renderer-inert-callable-lifecycle-method-placeholder-creation",
      nextExcitingBoundaryAllowedNow: false,
      callableLifecycleMethodPlaceholderCreationAllowedNow: false,
      callableLifecycleMethodPlaceholderCreationRequiresSeparatePhase: true,
      callableLifecycleMethodPlaceholdersMustStayInert: true,
      callableLifecycleMethodPlaceholdersMustBeManualOnly: true,
      noCallableLifecycleMethodsCreated: true,
      noStateCreation: true,
      noStateMutation: true,
      noRendererInitialization: true,
      noRendererStart: true,
      noRendererStopBehavior: true,
      noMapAttachment: true,
      noDrawing: true,
      noStartupWiring: true,
      noDomLeafletGameplayBackendStorageNetworkChange: true,
      plannedNextPhase: 440
    };
  }

  return {
    ok: true,
    phase: 439,
    helperName:
      "getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    transitionPlanOnly: true,
    jsonSafeResult: true,
    missingRequiredOptionKeys: [],
    requiredOptionKeys,
    transitionStatus:
      "callable-lifecycle-method-planning-to-placeholder-foundation-documented",
    previousMilestone: "manual-renderer-lifecycle-method-shell-foundation",
    currentMilestone: "manual-renderer-callable-lifecycle-method-planning",
    nextMilestone:
      "manual-renderer-inert-callable-lifecycle-method-placeholder-foundation",
    excitingBoundaryCrossedInPreviousPhase: false,
    nextExcitingBoundary:
      "manual-renderer-inert-callable-lifecycle-method-placeholder-creation",
    nextExcitingBoundaryAllowedNow: false,
    callableLifecycleMethodPlaceholderCreationAllowedNow: false,
    callableLifecycleMethodPlaceholderCreationRequiresSeparatePhase: true,
    callableLifecycleMethodPlaceholdersMustStayInert: true,
    callableLifecycleMethodPlaceholdersMustBeManualOnly: true,
    noCallableLifecycleMethodsCreated: true,
    noStateCreation: true,
    noStateMutation: true,
    noRendererInitialization: true,
    noRendererStart: true,
    noRendererStopBehavior: true,
    noMapAttachment: true,
    noDrawing: true,
    noStartupWiring: true,
    noDomLeafletGameplayBackendStorageNetworkChange: true,
    plannedNextPhase: 440
  };
}

function getCustom25DVisualManualRendererInstanceShellCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase435",
    "closeoutOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const shell = state && state.rendererInstanceShell ? state.rendererInstanceShell : null;
  const phase434ShellDetected =
    !!(shell && shell.phaseCreated === 434);
  const phase434ShellFieldsValid =
    !!(
      shell &&
      shell.phaseCreated === 434 &&
      shell.shellType === "manual-renderer-instance-shell" &&
      shell.inert === true &&
      shell.initialized === false &&
      shell.started === false &&
      shell.mapAttached === false &&
      shell.drawingStarted === false &&
      shell.startupWired === false &&
      shell.gameplayChanged === false &&
      shell.backendStorageNetworkChanged === false &&
      shell.createdAtManualOnly === true
    );
  const rendererInstanceShellExists = !!shell;
  const rendererInstanceShellInert = phase434ShellFieldsValid;

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 435,
      helperName:
        "getCustom25DVisualManualRendererInstanceShellCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      closeoutStatus: "blocked",
      phase434ShellDetected: false,
      phase434ShellFieldsValid: false,
      stateContainerExists,
      stateContainerReplaced: false,
      rendererInstanceShellExists: false,
      rendererInstanceShellInert: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      domMutated: false,
      leafletMutated: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false,
      persistenceChanged: false,
      runtimeBehaviorChanged: false,
      plannedNextPhase: 436,
      plannedNextMilestone: "manual-renderer-lifecycle-method-shell-planning"
    };
  }

  return {
    ok:
      stateContainerExists &&
      phase434ShellDetected &&
      phase434ShellFieldsValid &&
      rendererInstanceShellInert,
    phase: 435,
    helperName:
      "getCustom25DVisualManualRendererInstanceShellCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    closeoutStatus: "manual-renderer-instance-shell-foundation-closed-out",
    phase434ShellDetected,
    phase434ShellFieldsValid,
    stateContainerExists,
    stateContainerReplaced: false,
    rendererInstanceShellExists,
    rendererInstanceShellInert,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    domMutated: false,
    leafletMutated: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false,
    persistenceChanged: false,
    runtimeBehaviorChanged: false,
    plannedNextPhase: 436,
    plannedNextMilestone: "manual-renderer-lifecycle-method-shell-planning"
  };
}

function getCustom25DVisualManualRendererPostInstanceShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase435",
    "readinessOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const closeoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInstanceShellCloseoutReport ===
    "function";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const shell = state && state.rendererInstanceShell ? state.rendererInstanceShell : null;
  const phase434ShellDetected =
    !!(shell && shell.phaseCreated === 434);
  const phase434ShellFieldsValid =
    !!(
      shell &&
      shell.phaseCreated === 434 &&
      shell.shellType === "manual-renderer-instance-shell" &&
      shell.inert === true &&
      shell.initialized === false &&
      shell.started === false &&
      shell.mapAttached === false &&
      shell.drawingStarted === false &&
      shell.startupWired === false &&
      shell.gameplayChanged === false &&
      shell.backendStorageNetworkChanged === false &&
      shell.createdAtManualOnly === true
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 435,
      helperName:
        "getCustom25DVisualManualRendererPostInstanceShellReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      closeoutHelperAvailable,
      stateContainerExists,
      phase434ShellDetected: false,
      phase434ShellFieldsValid: false,
      readinessStatus: "blocked",
      lifecycleMethodShellPlanningAllowedNext: false,
      lifecycleMethodShellCreationAllowedNow: false,
      mustRemainManualOnly: true,
      mustRemainInert: true,
      mustNotInitializeRenderer: true,
      mustNotStartRenderer: true,
      mustNotAttachMap: true,
      mustNotDraw: true,
      mustNotWireStartup: true,
      mustNotChangeDomGameplayBackendStorageNetwork: true,
      plannedNextPhase: 436,
      plannedNextStep: "manual-renderer-lifecycle-method-shell-readiness-or-plan"
    };
  }

  return {
    ok: stateContainerExists && phase434ShellDetected && phase434ShellFieldsValid,
    phase: 435,
    helperName:
      "getCustom25DVisualManualRendererPostInstanceShellReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    closeoutHelperAvailable,
    stateContainerExists,
    phase434ShellDetected,
    phase434ShellFieldsValid,
    readinessStatus: "post-instance-shell-readiness-documented",
    lifecycleMethodShellPlanningAllowedNext: true,
    lifecycleMethodShellCreationAllowedNow: false,
    mustRemainManualOnly: true,
    mustRemainInert: true,
    mustNotInitializeRenderer: true,
    mustNotStartRenderer: true,
    mustNotAttachMap: true,
    mustNotDraw: true,
    mustNotWireStartup: true,
    mustNotChangeDomGameplayBackendStorageNetwork: true,
    plannedNextPhase: 436,
    plannedNextStep: "manual-renderer-lifecycle-method-shell-readiness-or-plan"
  };
}

function getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase435",
    "transitionPlanOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const readinessHelperAvailable =
    typeof getCustom25DVisualManualRendererPostInstanceShellReadinessReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 435,
      helperName:
        "getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      transitionPlanOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      readinessHelperAvailable,
      transitionStatus: "blocked",
      previousMilestone: "manual-renderer-instance-shell-foundation",
      nextMilestone: "manual-renderer-lifecycle-method-shell-planning",
      excitingBoundaryCrossedInPreviousPhase: true,
      nextExcitingBoundary: "manual-renderer-lifecycle-method-shell-creation",
      nextExcitingBoundaryAllowedNow: false,
      futureLifecycleMethodShellMustBeSeparatePhase: true,
      futureLifecycleMethodShellMustStayInert: true,
      noStateCreation: true,
      noStateMutation: true,
      noRendererInitialization: true,
      noRendererStart: true,
      noMapAttachment: true,
      noDrawing: true,
      noStartupWiring: true,
      noDomGameplayBackendStorageNetworkChange: true,
      plannedNextPhase: 436
    };
  }

  return {
    ok: true,
    phase: 435,
    helperName:
      "getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    transitionPlanOnly: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    readinessHelperAvailable,
    transitionStatus:
      "instance-shell-to-lifecycle-method-shell-planning-documented",
    previousMilestone: "manual-renderer-instance-shell-foundation",
    nextMilestone: "manual-renderer-lifecycle-method-shell-planning",
    excitingBoundaryCrossedInPreviousPhase: true,
    nextExcitingBoundary: "manual-renderer-lifecycle-method-shell-creation",
    nextExcitingBoundaryAllowedNow: false,
    futureLifecycleMethodShellMustBeSeparatePhase: true,
    futureLifecycleMethodShellMustStayInert: true,
    noStateCreation: true,
    noStateMutation: true,
    noRendererInitialization: true,
    noRendererStart: true,
    noMapAttachment: true,
    noDrawing: true,
    noStartupWiring: true,
    noDomGameplayBackendStorageNetworkChange: true,
    plannedNextPhase: 436
  };
}

function getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase436",
    "readinessOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const phase435CloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInstanceShellCloseoutReport ===
    "function";
  const phase435ReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererPostInstanceShellReadinessReport ===
    "function";
  const phase435TransitionPlanHelperAvailable =
    typeof getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport ===
    "function";
  const windowExists = typeof window !== "undefined";
  const stateContainerExists =
    windowExists &&
    typeof window.custom25DVisualManualRendererState !== "undefined";
  const state = stateContainerExists
    ? window.custom25DVisualManualRendererState
    : null;
  const shell = state && state.rendererInstanceShell ? state.rendererInstanceShell : null;
  const rendererInstanceShellExists = !!shell;
  const rendererInstanceShellInert =
    !!(
      shell &&
      shell.phaseCreated === 434 &&
      shell.shellType === "manual-renderer-instance-shell" &&
      shell.inert === true &&
      shell.initialized === false &&
      shell.started === false &&
      shell.mapAttached === false &&
      shell.drawingStarted === false &&
      shell.startupWired === false &&
      shell.gameplayChanged === false &&
      shell.backendStorageNetworkChanged === false &&
      shell.createdAtManualOnly === true
    );
  const lifecycleMethodsDetected =
    !!(
      shell &&
      (typeof shell.initialize === "function" ||
        typeof shell.start === "function" ||
        typeof shell.stop === "function" ||
        typeof shell.attachMap === "function" ||
        typeof shell.drawFrame === "function")
    );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 436,
      helperName:
        "getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase435CloseoutHelperAvailable,
      phase435ReadinessHelperAvailable,
      phase435TransitionPlanHelperAvailable,
      stateContainerExists,
      rendererInstanceShellExists,
      rendererInstanceShellInert: false,
      lifecycleMethodsDetected: false,
      readinessStatus: "blocked",
      lifecycleMethodShellPlanningAllowedNext: false,
      lifecycleMethodShellCreationAllowedNow: false,
      mustRemainManualOnly: true,
      mustRemainInert: true,
      mustNotCreateLifecycleMethods: true,
      mustNotInitializeRenderer: true,
      mustNotStartRenderer: true,
      mustNotAttachMap: true,
      mustNotDraw: true,
      mustNotWireStartup: true,
      mustNotChangeDomGameplayBackendStorageNetwork: true,
      plannedNextPhase: 437,
      plannedNextStep: "manual-renderer-lifecycle-method-shell-foundation"
    };
  }

  return {
    ok: true,
    phase: 436,
    helperName:
      "getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase435CloseoutHelperAvailable,
    phase435ReadinessHelperAvailable,
    phase435TransitionPlanHelperAvailable,
    stateContainerExists,
    rendererInstanceShellExists,
    rendererInstanceShellInert,
    lifecycleMethodsDetected,
    readinessStatus: "manual-renderer-lifecycle-method-shell-readiness-planned",
    lifecycleMethodShellPlanningAllowedNext: true,
    lifecycleMethodShellCreationAllowedNow: false,
    mustRemainManualOnly: true,
    mustRemainInert: true,
    mustNotCreateLifecycleMethods: true,
    mustNotInitializeRenderer: true,
    mustNotStartRenderer: true,
    mustNotAttachMap: true,
    mustNotDraw: true,
    mustNotWireStartup: true,
    mustNotChangeDomGameplayBackendStorageNetwork: true,
    plannedNextPhase: 437,
    plannedNextStep: "manual-renderer-lifecycle-method-shell-foundation"
  };
}

function getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase436",
    "riskBoundaryOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 436,
      helperName:
        "getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      riskBoundaryOnly: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      riskBoundaryStatus: "blocked",
      nextExcitingBoundary: "manual-renderer-lifecycle-method-shell-creation",
      nextExcitingBoundaryAllowedNow: false,
      futureLifecycleMethodShellMustBeSeparatePhase: true,
      futureLifecycleMethodShellMustStayInert: true,
      futureLifecycleMethodShellMustBeManualOnly: true,
      futureLifecycleMethodShellMustNotInitializeRenderer: true,
      futureLifecycleMethodShellMustNotStartRenderer: true,
      futureLifecycleMethodShellMustNotAttachMap: true,
      futureLifecycleMethodShellMustNotDraw: true,
      futureLifecycleMethodShellMustNotWireStartup: true,
      futureLifecycleMethodShellMustNotChangeDomGameplayBackendStorageNetwork: true,
      plannedNextPhase: 437
    };
  }

  return {
    ok: true,
    phase: 436,
    helperName:
      "getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    riskBoundaryOnly: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    riskBoundaryStatus:
      "manual-renderer-lifecycle-method-shell-boundary-documented",
    nextExcitingBoundary: "manual-renderer-lifecycle-method-shell-creation",
    nextExcitingBoundaryAllowedNow: false,
    futureLifecycleMethodShellMustBeSeparatePhase: true,
    futureLifecycleMethodShellMustStayInert: true,
    futureLifecycleMethodShellMustBeManualOnly: true,
    futureLifecycleMethodShellMustNotInitializeRenderer: true,
    futureLifecycleMethodShellMustNotStartRenderer: true,
    futureLifecycleMethodShellMustNotAttachMap: true,
    futureLifecycleMethodShellMustNotDraw: true,
    futureLifecycleMethodShellMustNotWireStartup: true,
    futureLifecycleMethodShellMustNotChangeDomGameplayBackendStorageNetwork: true,
    plannedNextPhase: 437
  };
}

function getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "phase436",
    "transitionPlanOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceShellCreation",
    "confirmNoLifecycleMethodShellCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoRendererStopBehavior",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoDomMutation",
    "confirmNoGameplayChange",
    "confirmNoBackendStorageNetworkChange",
    "confirmNoPersistence",
    "confirmNoRuntimeBehaviorChange",
    "confirmNoAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 436,
      helperName:
        "getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      transitionPlanOnly: true,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      transitionStatus: "blocked",
      previousMilestone: "manual-renderer-instance-shell-foundation",
      currentMilestone: "manual-renderer-lifecycle-method-shell-planning",
      nextMilestone: "manual-renderer-lifecycle-method-shell-foundation",
      lifecycleMethodShellCreationAllowedNow: false,
      lifecycleMethodShellCreationRequiresSeparatePhase: true,
      lifecycleMethodShellMustStayInert: true,
      plannedLifecycleMethodNames: [
        "initialize",
        "start",
        "stop",
        "attachMap",
        "drawFrame"
      ],
      plannedLifecycleMethodNamesAreDocumentationOnly: true,
      noLifecycleMethodsCreated: true,
      noStateCreation: true,
      noStateMutation: true,
      noRendererInitialization: true,
      noRendererStart: true,
      noRendererStopBehavior: true,
      noMapAttachment: true,
      noDrawing: true,
      noStartupWiring: true,
      noDomGameplayBackendStorageNetworkChange: true,
      plannedNextPhase: 437
    };
  }

  return {
    ok: true,
    phase: 436,
    helperName:
      "getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    transitionPlanOnly: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    transitionStatus:
      "post-instance-shell-to-lifecycle-method-shell-foundation-planned",
    previousMilestone: "manual-renderer-instance-shell-foundation",
    currentMilestone: "manual-renderer-lifecycle-method-shell-planning",
    nextMilestone: "manual-renderer-lifecycle-method-shell-foundation",
    lifecycleMethodShellCreationAllowedNow: false,
    lifecycleMethodShellCreationRequiresSeparatePhase: true,
    lifecycleMethodShellMustStayInert: true,
    plannedLifecycleMethodNames: [
      "initialize",
      "start",
      "stop",
      "attachMap",
      "drawFrame"
    ],
    plannedLifecycleMethodNamesAreDocumentationOnly: true,
    noLifecycleMethodsCreated: true,
    noStateCreation: true,
    noStateMutation: true,
    noRendererInitialization: true,
    noRendererStart: true,
    noRendererStopBehavior: true,
    noMapAttachment: true,
    noDrawing: true,
    noStartupWiring: true,
    noDomGameplayBackendStorageNetworkChange: true,
    plannedNextPhase: 437
  };
}

function getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport(
  options = {}
) {
  const stateContainerShellCloseout =
    typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererStateContainerShellCloseoutReport(options)
      : null;
  const localStateContainerShell =
    typeof createCustom25DVisualManualRendererStateContainerShell === "function"
      ? createCustom25DVisualManualRendererStateContainerShell(options)
      : null;
  const contractReadiness =
    typeof getCustom25DVisualManualRendererStateContainerContractReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererStateContainerContractReadinessReport(options)
      : null;

  const requiredOptionKeys =
    stateContainerShellCloseout &&
    Array.isArray(stateContainerShellCloseout.requiredOptionKeys)
      ? stateContainerShellCloseout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];

  return {
    phase: 285,
    name: "custom-25d-visual-manual-renderer-shared-state-container-creation-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    sharedStateContainerCreated: false,
    windowStateContainerCreated: false,
    sharedStateMutated: false,
    futureSharedStateContainerReady: false,
    futureCreationContract: {
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      explicitOptionsOnly: true,
      noStartupWiring: true,
      noAutomaticInvocation: true,
      noBackendChanges: true,
      noPersistence: true,
      noNetwork: true,
      noStorage: true,
      noDomMutation: true,
      noMapMutation: true,
      noLeafletLayerMutation: true,
      noDrawing: true,
      noRendererInitialization: true,
      noRendererStart: true,
      noMapAttachment: true,
      noGameplayChanges: true
    },
    requiredOptionKeys,
    stateContainerCreatedLocallyOnly:
      !!(localStateContainerShell && localStateContainerShell.stateContainerCreated === true),
    stateContainerShared: false,
    stateContainerAttachedToWindow: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesSharedStateCreation: false,
    reviewedHelpers: {
      stateContainerShellCloseout:
        typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
        "function",
      localStateContainerShell:
        typeof createCustom25DVisualManualRendererStateContainerShell === "function",
      stateContainerContractReadiness:
        typeof getCustom25DVisualManualRendererStateContainerContractReadinessReport ===
        "function"
    },
    blockedBehavior:
      stateContainerShellCloseout && stateContainerShellCloseout.blockedBehavior
        ? stateContainerShellCloseout.blockedBehavior
        : contractReadiness && contractReadiness.blockedBehavior
          ? contractReadiness.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      stateContainerShellCloseout && stateContainerShellCloseout.preservedSystems
        ? stateContainerShellCloseout.preservedSystems
        : contractReadiness && contractReadiness.preservedSystems
          ? contractReadiness.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    nextStep: "manual-renderer-shared-state-container-contract",
    safetyFlags:
      stateContainerShellCloseout && stateContainerShellCloseout.safetyFlags
        ? stateContainerShellCloseout.safetyFlags
        : contractReadiness && contractReadiness.safetyFlags
          ? contractReadiness.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    notes: [
      "Passive manual renderer shared state container creation planning report only.",
      "Confirms the Phase 283 local state shell remains local-only and not attached to window.",
      "Shared/window state creation remains planned for a future separately approved phase."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateContainerPreflightReport(options = {}) {
  const creationPlan =
    typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererStateContainerShellCloseoutReport(options)
      : null;
  const localStateContainerShell =
    typeof createCustom25DVisualManualRendererStateContainerShell === "function"
      ? createCustom25DVisualManualRendererStateContainerShell(options)
      : null;

  const requiredOptionKeys =
    creationPlan && Array.isArray(creationPlan.requiredOptionKeys)
      ? creationPlan.requiredOptionKeys
      : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
        ? shellCloseout.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  return {
    phase: 286,
    name: "custom-25d-visual-manual-renderer-shared-state-container-preflight",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    preflightOnly: true,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    sharedStateContainerCreated: false,
    windowStateContainerCreated: false,
    sharedStateMutated: false,
    futureSharedStateContainerReady: false,
    preflightChecklist: {
      phase283LocalShellVerified: true,
      phase284CloseoutAvailable:
        typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
        "function",
      phase285CreationPlanAvailable:
        typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
        "function",
      requiredOptionKeysAvailable: requiredOptionKeys.length > 0,
      defaultPathBlocked:
        !!(shellCloseout && shellCloseout.defaultNoOptionBlocked === true),
      manualPathMustRemainExplicit: true,
      windowAttachmentStillBlocked: true,
      startupWiringStillBlocked: true,
      drawingStillBlocked: true,
      mapAttachmentStillBlocked: true,
      rendererStartStillBlocked: true,
      backendStorageNetworkStillBlocked: true,
      gameplayStillUntouched: true
    },
    requiredOptionKeys,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesSharedStateCreation: false,
    stateContainerAuthorizesWindowAttachment: false,
    reviewedHelpers: {
      sharedStateContainerCreationPlan:
        typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
        "function",
      stateContainerShellCloseout:
        typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
        "function",
      localStateContainerShell:
        typeof createCustom25DVisualManualRendererStateContainerShell === "function"
    },
    stateContainerCreatedLocallyOnly:
      !!(localStateContainerShell && localStateContainerShell.stateContainerCreated === true),
    blockedBehavior:
      creationPlan && creationPlan.blockedBehavior
        ? creationPlan.blockedBehavior
        : shellCloseout && shellCloseout.blockedBehavior
          ? shellCloseout.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      creationPlan && creationPlan.preservedSystems
        ? creationPlan.preservedSystems
        : shellCloseout && shellCloseout.preservedSystems
          ? shellCloseout.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    nextStep: "manual-renderer-shared-state-container-contract",
    safetyFlags:
      creationPlan && creationPlan.safetyFlags
        ? creationPlan.safetyFlags
        : shellCloseout && shellCloseout.safetyFlags
          ? shellCloseout.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    notes: [
      "Passive manual renderer shared state container preflight report only.",
      "Confirms the shared/window state-container step remains preflight-only and not live.",
      "Renderer creation, renderer start, window attachment, startup wiring, and gameplay-impacting behavior remain blocked in this phase."
    ]
  };
}

function createCustom25DVisualManualRendererSharedStateContainerShell(options = {}) {
  const preflight =
    typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport === "function"
      ? getCustom25DVisualManualRendererSharedStateContainerPreflightReport(options)
      : null;
  const creationPlan =
    typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport(options)
      : null;
  const localStateContainerShell =
    typeof createCustom25DVisualManualRendererStateContainerShell === "function"
      ? createCustom25DVisualManualRendererStateContainerShell(options)
      : null;

  const requiredOptionKeys =
    preflight && Array.isArray(preflight.requiredOptionKeys)
      ? preflight.requiredOptionKeys
      : creationPlan && Array.isArray(creationPlan.requiredOptionKeys)
        ? creationPlan.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  if (missingKey) {
    const reasonByKey = {
      manual: "manual-flag-required",
      developerIntent: "developer-intent-required",
      localDevOnly: "local-dev-only-required",
      browserConsoleOnly: "browser-console-only-required",
      explicitOptionsOnly: "explicit-options-only-required",
      allowManualRendererStateContainerShell: "manual-renderer-state-container-shell-not-allowed",
      noStartupWiring: "no-startup-wiring-acknowledgement-required",
      noBackendChanges: "no-backend-changes-acknowledgement-required",
      noPersistence: "no-persistence-acknowledgement-required",
      noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
    };

    return {
      phase: 287,
      name: "custom-25d-visual-manual-renderer-shared-state-container-shell",
      ok: true,
      allowed: false,
      ready: false,
      inert: true,
      manualOnly: true,
      sharedStateContainerCreated: false,
      windowStateContainerCreated: false,
      stateContainerAttachedToWindow: false,
      sharedStateMutated: false,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      state: null,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      layerCount: 0,
      stateContainerAuthorizesRendererCreation: false,
      stateContainerAuthorizesRendererStart: false,
      stateContainerAuthorizesMapAttachment: false,
      stateContainerAuthorizesDrawing: false,
      failedRequirement: missingKey,
      reason: reasonByKey[missingKey] || "required-option-missing",
      requiredOptionKeys,
      reviewedHelpers: {
        sharedStateContainerPreflight:
          typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
          "function",
        sharedStateContainerCreationPlan:
          typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
          "function",
        localStateContainerShell:
          typeof createCustom25DVisualManualRendererStateContainerShell === "function"
      },
      safetyFlags:
        preflight && preflight.safetyFlags
          ? preflight.safetyFlags
          : creationPlan && creationPlan.safetyFlags
            ? creationPlan.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
      notes: [
        "Manual/local-dev shared renderer state container shell creator.",
        "Returns a blocked inert result by default until every explicit safe manual option is present.",
        "Does not create any shared/window state container when blocked."
      ]
    };
  }

  const windowExists = typeof window !== "undefined" && window;
  if (!windowExists) {
    return {
      phase: 287,
      name: "custom-25d-visual-manual-renderer-shared-state-container-shell",
      ok: true,
      allowed: false,
      ready: false,
      inert: true,
      manualOnly: true,
      sharedStateContainerCreated: false,
      windowStateContainerCreated: false,
      stateContainerAttachedToWindow: false,
      sharedStateMutated: false,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      state: null,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      layerCount: 0,
      stateContainerAuthorizesRendererCreation: false,
      stateContainerAuthorizesRendererStart: false,
      stateContainerAuthorizesMapAttachment: false,
      stateContainerAuthorizesDrawing: false,
      failedRequirement: "windowAvailable",
      reason: "window-unavailable",
      requiredOptionKeys,
      reviewedHelpers: {
        sharedStateContainerPreflight:
          typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
          "function",
        sharedStateContainerCreationPlan:
          typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
          "function",
        localStateContainerShell:
          typeof createCustom25DVisualManualRendererStateContainerShell === "function"
      },
      safetyFlags:
        preflight && preflight.safetyFlags
          ? preflight.safetyFlags
          : creationPlan && creationPlan.safetyFlags
            ? creationPlan.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
      notes: [
        "Manual/local-dev shared renderer state container shell creator.",
        "Requires a browser window to create the inert shared/window state container.",
        "Does not create any shared/window state container when window is unavailable."
      ]
    };
  }

  const existingState =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const state =
    existingState ||
    {
      phaseCreated: 287,
      name: "custom25DVisualManualRendererState",
      inert: true,
      manualOnly: true,
      shared: true,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      layerCount: 0,
      layers: [],
      createdAt: new Date().toISOString()
    };

  if (windowExists && typeof window.custom25DVisualManualRendererState === "undefined") {
    window.custom25DVisualManualRendererState = state;
  }

  return {
    phase: 287,
    name: "custom-25d-visual-manual-renderer-shared-state-container-shell",
    ok: true,
    allowed: true,
    ready: true,
    inert: true,
    manualOnly: true,
    sharedStateContainerCreated: true,
    windowStateContainerCreated: true,
    stateContainerAttachedToWindow: true,
    sharedStateMutated: true,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    state,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    requiredOptionKeys,
    reviewedHelpers: {
      sharedStateContainerPreflight:
        typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
        "function",
      sharedStateContainerCreationPlan:
        typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
        "function",
      localStateContainerShell:
        typeof createCustom25DVisualManualRendererStateContainerShell === "function"
    },
    safetyFlags:
      preflight && preflight.safetyFlags
        ? preflight.safetyFlags
        : creationPlan && creationPlan.safetyFlags
          ? creationPlan.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    notes: [
      "Manual/local-dev shared renderer state container shell creator.",
      "Creates only inert shared/window state data after every explicit safe manual option is present.",
      "Does not initialize or start a renderer, attach to the map, draw, mutate DOM, or change gameplay."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(
  options = {}
) {
  const preflight =
    typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport === "function"
      ? getCustom25DVisualManualRendererSharedStateContainerPreflightReport(options)
      : null;
  const creationPlan =
    typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    preflight && Array.isArray(preflight.requiredOptionKeys)
      ? preflight.requiredOptionKeys
      : creationPlan && Array.isArray(creationPlan.requiredOptionKeys)
        ? creationPlan.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const existingWindowStateObserved = typeof existingWindowState !== "undefined";
  const existingWindowStateInert =
    !!(
      existingWindowState &&
      existingWindowState.inert === true &&
      existingWindowState.rendererCreated === false &&
      existingWindowState.rendererInitialized === false &&
      existingWindowState.rendererRunning === false &&
      existingWindowState.mapAttached === false &&
      existingWindowState.drawingEnabled === false &&
      existingWindowState.domCreated === false &&
      existingWindowState.startupWired === false
    );

  return {
    phase: 288,
    name: "custom-25d-visual-manual-renderer-shared-state-container-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    sharedStateContainerShellCloseout: true,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    defaultNoOptionBlocked:
      !!(
        noOptionResult &&
        noOptionResult.allowed === false &&
        noOptionResult.sharedStateContainerCreated === false &&
        noOptionResult.windowStateContainerCreated === false
      ),
    explicitManualLocalDevBrowserConsolePathCanCreateOnlyInertSharedWindowState:
      !!(
        preflight &&
        preflight.preflightChecklist &&
        preflight.preflightChecklist.manualPathMustRemainExplicit === true
      ),
    sharedStateContainerRemainsInertDataOnly:
      existingWindowStateObserved ? existingWindowStateInert : true,
    noRendererInitializationOrStart: true,
    noMapAttachment: true,
    noDrawing: true,
    noDomMutation: true,
    noLeafletLayerMutation: true,
    noBackendStorageNetworkPersistence: true,
    noGameplayBehaviorChanges: true,
    sharedStateContainerCreated: false,
    windowStateContainerCreated: false,
    sharedStateMutated: false,
    closeoutCreatesState: false,
    closeoutMutatesState: false,
    futureRendererStateUseReady: false,
    existingWindowStateObserved,
    existingWindowStateInert,
    existingWindowStateSnapshot: existingWindowStateObserved
      ? {
          inert: existingWindowState && existingWindowState.inert === true,
          rendererCreated:
            existingWindowState && existingWindowState.rendererCreated === true,
          rendererInitialized:
            existingWindowState && existingWindowState.rendererInitialized === true,
          rendererRunning:
            existingWindowState && existingWindowState.rendererRunning === true,
          mapAttached: existingWindowState && existingWindowState.mapAttached === true,
          drawingEnabled:
            existingWindowState && existingWindowState.drawingEnabled === true,
          domCreated: existingWindowState && existingWindowState.domCreated === true,
          startupWired:
            existingWindowState && existingWindowState.startupWired === true,
          layerCount:
            existingWindowState && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0
        }
      : null,
    requiredOptionKeys,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    reviewedHelpers: {
      sharedStateContainerShell:
        typeof createCustom25DVisualManualRendererSharedStateContainerShell ===
        "function",
      sharedStateContainerPreflight:
        typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
        "function",
      sharedStateContainerCreationPlan:
        typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
        "function"
    },
    blockedBehavior:
      preflight && preflight.blockedBehavior
        ? preflight.blockedBehavior
        : creationPlan && creationPlan.blockedBehavior
          ? creationPlan.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      preflight && preflight.preservedSystems
        ? preflight.preservedSystems
        : creationPlan && creationPlan.preservedSystems
          ? creationPlan.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    safetyFlags:
      preflight && preflight.safetyFlags
        ? preflight.safetyFlags
        : creationPlan && creationPlan.safetyFlags
          ? creationPlan.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    nextStep: "manual-renderer-shared-state-container-runtime-plan",
    notes: [
      "Passive closeout for the inert shared/window renderer state container shell only.",
      "Does not create or mutate window.custom25DVisualManualRendererState.",
      "Renderer initialization, renderer start, map attachment, drawing, DOM mutation, layer mutation, backend/storage/network work, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options = {}) {
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const preflight =
    typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport === "function"
      ? getCustom25DVisualManualRendererSharedStateContainerPreflightReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
      ? shellCloseout.requiredOptionKeys
      : preflight && Array.isArray(preflight.requiredOptionKeys)
        ? preflight.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);
  const nameMatches =
    stateIsObject && existingWindowState.name === "custom25DVisualManualRendererState";
  const inertFlagValid = stateIsObject && existingWindowState.inert === true;
  const manualOnlyFlagValid = stateIsObject && existingWindowState.manualOnly === true;
  const sharedFlagValid = stateIsObject && existingWindowState.shared === true;
  const rendererFlagsInert =
    stateIsObject &&
    existingWindowState.rendererCreated === false &&
    existingWindowState.rendererInitialized === false &&
    existingWindowState.rendererRunning === false;
  const mapFlagsInert = stateIsObject && existingWindowState.mapAttached === false;
  const drawingFlagsInert = stateIsObject && existingWindowState.drawingEnabled === false;
  const domFlagsInert = stateIsObject && existingWindowState.domCreated === false;
  const startupFlagsInert = stateIsObject && existingWindowState.startupWired === false;
  const layerCountInert =
    stateIsObject && typeof existingWindowState.layerCount === "number"
      ? existingWindowState.layerCount === 0
      : false;
  const layersArrayValid =
    stateIsObject &&
    Array.isArray(existingWindowState.layers) &&
    existingWindowState.layers.length === 0;
  const schemaValid =
    stateExists &&
    stateIsObject &&
    nameMatches &&
    inertFlagValid &&
    manualOnlyFlagValid &&
    sharedFlagValid &&
    rendererFlagsInert &&
    mapFlagsInert &&
    drawingFlagsInert &&
    domFlagsInert &&
    startupFlagsInert &&
    layerCountInert &&
    layersArrayValid;

  return {
    phase: 289,
    name: "custom-25d-visual-manual-renderer-shared-state-schema-review",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    schemaReviewOnly: true,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    sharedStateContainerCreated: false,
    windowStateContainerCreated: false,
    sharedStateMutated: false,
    schemaReviewCreatesState: false,
    schemaReviewMutatesState: false,
    futureRendererStateUseReady: false,
    requiredOptionKeys,
    expectedSharedStateSchema: {
      phaseCreated: "number-or-phase-marker",
      name: "custom25DVisualManualRendererState",
      inert: true,
      manualOnly: true,
      shared: true,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      layerCount: 0,
      layers: []
    },
    schemaReview: {
      stateExists,
      stateIsObject,
      nameMatches,
      inertFlagValid,
      manualOnlyFlagValid,
      sharedFlagValid,
      rendererFlagsInert,
      mapFlagsInert,
      drawingFlagsInert,
      domFlagsInert,
      startupFlagsInert,
      layerCountInert,
      layersArrayValid,
      schemaValid
    },
    reason: stateExists ? "shared-state-schema-reviewed" : "shared-state-container-not-present",
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      sharedStateContainerShellCloseout:
        typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
        "function",
      sharedStateContainerShell:
        typeof createCustom25DVisualManualRendererSharedStateContainerShell ===
        "function",
      sharedStateContainerPreflight:
        typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
        "function"
    },
    defaultNoOptionBlocked:
      !!(
        noOptionResult &&
        noOptionResult.allowed === false &&
        noOptionResult.sharedStateContainerCreated === false &&
        noOptionResult.windowStateContainerCreated === false
      ),
    blockedBehavior:
      shellCloseout && shellCloseout.blockedBehavior
        ? shellCloseout.blockedBehavior
        : preflight && preflight.blockedBehavior
          ? preflight.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      shellCloseout && shellCloseout.preservedSystems
        ? shellCloseout.preservedSystems
        : preflight && preflight.preservedSystems
          ? preflight.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    safetyFlags:
      shellCloseout && shellCloseout.safetyFlags
        ? shellCloseout.safetyFlags
        : preflight && preflight.safetyFlags
          ? preflight.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    nextStep: "manual-renderer-shared-state-usage-plan",
    notes: [
      "Passive shared/window renderer state schema review only.",
      "Does not create or mutate window.custom25DVisualManualRendererState.",
      "Renderer initialization, map attachment, drawing, DOM mutation, layer mutation, backend/storage/network work, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options = {}) {
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
      ? schemaReview.requiredOptionKeys
      : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
        ? shellCloseout.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const schemaValid =
    !!(
      schemaReview &&
      schemaReview.schemaReview &&
      schemaReview.schemaReview.schemaValid === true
    );

  return {
    phase: 290,
    name: "custom-25d-visual-manual-renderer-shared-state-read-access-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    readAccessPlanOnly: true,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    sharedStateContainerCreated: false,
    windowStateContainerCreated: false,
    sharedStateMutated: false,
    readAccessCreatesState: false,
    readAccessMutatesState: false,
    futureReadAccessReady: false,
    futureRendererStateUseReady: false,
    readAccessContract: {
      readOnly: true,
      neverMutateState: true,
      neverCreateMissingState: true,
      neverReplaceExistingState: true,
      neverInitializeRenderer: true,
      neverStartRenderer: true,
      neverAttachMap: true,
      neverDraw: true,
      neverCreateDom: true,
      neverCreateLeafletLayers: true,
      neverCallBackendNetworkStoragePersistence: true,
      neverAffectGameplay: true,
      neverAffectOsm: true,
      neverAffectPins: true,
      neverAffectPlayerMarker: true,
      neverAffectCaptureRadius: true,
      neverAffectUi: true,
      neverAffectBackend: true,
      neverAffectStorage: true,
      neverAffectNetwork: true
    },
    schemaReviewSummary: {
      schemaReviewAvailable:
        typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
        "function",
      stateExists,
      schemaValid,
      stateReadOnlyInspectionAllowed: true,
      stateMutationAllowed: false
    },
    requiredOptionKeys,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    stateContainerAuthorizesStateCreation: false,
    reviewedHelpers: {
      sharedStateSchemaReview:
        typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
        "function",
      sharedStateContainerShellCloseout:
        typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
        "function",
      sharedStateContainerShell:
        typeof createCustom25DVisualManualRendererSharedStateContainerShell ===
        "function"
    },
    defaultNoOptionBlocked:
      !!(
        noOptionResult &&
        noOptionResult.allowed === false &&
        noOptionResult.sharedStateContainerCreated === false &&
        noOptionResult.windowStateContainerCreated === false
      ),
    blockedBehavior:
      schemaReview && schemaReview.blockedBehavior
        ? schemaReview.blockedBehavior
        : shellCloseout && shellCloseout.blockedBehavior
          ? shellCloseout.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            },
    preservedSystems:
      schemaReview && schemaReview.preservedSystems
        ? schemaReview.preservedSystems
        : shellCloseout && shellCloseout.preservedSystems
          ? shellCloseout.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            },
    safetyFlags:
      schemaReview && schemaReview.safetyFlags
        ? schemaReview.safetyFlags
        : shellCloseout && shellCloseout.safetyFlags
          ? shellCloseout.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            },
    nextStep: "manual-renderer-shared-state-read-access-contract",
    notes: [
      "Passive shared/window renderer state read access plan only.",
      "Does not create or mutate window.custom25DVisualManualRendererState.",
      "Any future read helper must remain read-only and must not initialize, attach, draw, mutate state, or change gameplay/system behavior."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(
  options = {}
) {
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
      ? readAccessPlan.requiredOptionKeys
      : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
        ? schemaReview.requiredOptionKeys
        : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
          ? shellCloseout.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);
  const stateAppearsInert =
    !!(
      stateIsObject &&
      existingWindowState.inert === true &&
      existingWindowState.rendererInitialized === false &&
      existingWindowState.rendererRunning === false &&
      existingWindowState.mapAttached === false &&
      existingWindowState.drawingEnabled === false &&
      existingWindowState.domCreated === false &&
      existingWindowState.startupWired === false
    );

  return {
    phase: 291,
    name: "custom-25d-visual-manual-renderer-shared-state-read-access-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    readAccessPlanCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    phase290PlanAvailable:
      typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
      "function",
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionResult &&
          noOptionResult.allowed === false &&
          noOptionResult.sharedStateContainerCreated === false &&
          noOptionResult.windowStateContainerCreated === false
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      readAccessStayedPassive: true,
      createsState: false,
      mutatesState: false
    },
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readAccessStayedPassive: true,
    futureReadAccessReady: false,
    stateExists,
    stateReadOnlyInspectionOnly: true,
    stateAppearsInert,
    sharedStateSnapshot: stateExists
      ? {
          name: stateIsObject ? existingWindowState.name || null : null,
          inert: stateIsObject ? existingWindowState.inert === true : false,
          rendererInitialized:
            stateIsObject ? existingWindowState.rendererInitialized === true : false,
          rendererRunning:
            stateIsObject ? existingWindowState.rendererRunning === true : false,
          mapAttached: stateIsObject ? existingWindowState.mapAttached === true : false,
          drawingEnabled:
            stateIsObject ? existingWindowState.drawingEnabled === true : false,
          domCreated: stateIsObject ? existingWindowState.domCreated === true : false,
          startupWired: stateIsObject ? existingWindowState.startupWired === true : false,
          layerCount:
            stateIsObject && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0
        }
      : null,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      readAccessPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
        "function",
      schemaReview:
        typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
        "function",
      shellCloseout:
        typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
        "function"
    },
    blockedBehavior:
      readAccessPlan && readAccessPlan.blockedBehavior
        ? readAccessPlan.blockedBehavior
        : schemaReview && schemaReview.blockedBehavior
          ? schemaReview.blockedBehavior
          : shellCloseout && shellCloseout.blockedBehavior
            ? shellCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      readAccessPlan && readAccessPlan.preservedSystems
        ? readAccessPlan.preservedSystems
        : schemaReview && schemaReview.preservedSystems
          ? schemaReview.preservedSystems
          : shellCloseout && shellCloseout.preservedSystems
            ? shellCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      readAccessPlan && readAccessPlan.safetyFlags
        ? readAccessPlan.safetyFlags
        : schemaReview && schemaReview.safetyFlags
          ? schemaReview.safetyFlags
          : shellCloseout && shellCloseout.safetyFlags
            ? shellCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    nextStep: "manual-renderer-shared-state-read-access-contract-closeout-reviewed",
    notes: [
      "Passive closeout for the future read-only shared renderer state access plan.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "Renderer initialization, renderer start, map attachment, drawing, DOM mutation, layer mutation, backend/storage/network work, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(
  options = {}
) {
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const readAccessCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    readAccessCloseout && Array.isArray(readAccessCloseout.requiredOptionKeys)
      ? readAccessCloseout.requiredOptionKeys
      : readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
        ? readAccessPlan.requiredOptionKeys
        : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
          ? schemaReview.requiredOptionKeys
          : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
            ? shellCloseout.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);

  return {
    phase: 292,
    name: "custom-25d-visual-manual-renderer-shared-state-read-result-contract-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    contractPlanOnly: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    plannedReadResultMode: "snapshot-summary-only",
    plannedResultFields: [
      "stateAvailable",
      "stateName",
      "phaseCreated",
      "inert",
      "manualOnly",
      "shared",
      "rendererCreated",
      "rendererInitialized",
      "rendererRunning",
      "mapAttached",
      "drawingEnabled",
      "domCreated",
      "startupWired",
      "layerCount",
      "layersIsArray"
    ],
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionResult &&
          noOptionResult.allowed === false &&
          noOptionResult.sharedStateContainerCreated === false &&
          noOptionResult.windowStateContainerCreated === false
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      returnsSnapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      createsState: false,
      mutatesState: false
    },
    sharedStateSnapshotSummary: stateExists
      ? {
          stateAvailable: true,
          stateName: stateIsObject ? existingWindowState.name || null : null,
          phaseCreated:
            stateIsObject && typeof existingWindowState.phaseCreated !== "undefined"
              ? existingWindowState.phaseCreated
              : null,
          inert: stateIsObject ? existingWindowState.inert === true : false,
          manualOnly: stateIsObject ? existingWindowState.manualOnly === true : false,
          shared: stateIsObject ? existingWindowState.shared === true : false,
          rendererCreated:
            stateIsObject ? existingWindowState.rendererCreated === true : false,
          rendererInitialized:
            stateIsObject ? existingWindowState.rendererInitialized === true : false,
          rendererRunning:
            stateIsObject ? existingWindowState.rendererRunning === true : false,
          mapAttached: stateIsObject ? existingWindowState.mapAttached === true : false,
          drawingEnabled:
            stateIsObject ? existingWindowState.drawingEnabled === true : false,
          domCreated: stateIsObject ? existingWindowState.domCreated === true : false,
          startupWired: stateIsObject ? existingWindowState.startupWired === true : false,
          layerCount:
            stateIsObject && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0,
          layersIsArray: stateIsObject && Array.isArray(existingWindowState.layers)
        }
      : {
          stateAvailable: false,
          stateName: null,
          phaseCreated: null,
          inert: false,
          manualOnly: false,
          shared: false,
          rendererCreated: false,
          rendererInitialized: false,
          rendererRunning: false,
          mapAttached: false,
          drawingEnabled: false,
          domCreated: false,
          startupWired: false,
          layerCount: 0,
          layersIsArray: false
        },
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    futureReadResultContractReady: false,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      readAccessPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
        "function",
      readAccessPlanCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
        "function",
      schemaReview:
        typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
        "function",
      shellCloseout:
        typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
        "function"
    },
    blockedBehavior:
      readAccessCloseout && readAccessCloseout.blockedBehavior
        ? readAccessCloseout.blockedBehavior
        : readAccessPlan && readAccessPlan.blockedBehavior
          ? readAccessPlan.blockedBehavior
          : schemaReview && schemaReview.blockedBehavior
            ? schemaReview.blockedBehavior
            : shellCloseout && shellCloseout.blockedBehavior
              ? shellCloseout.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      readAccessCloseout && readAccessCloseout.preservedSystems
        ? readAccessCloseout.preservedSystems
        : readAccessPlan && readAccessPlan.preservedSystems
          ? readAccessPlan.preservedSystems
          : schemaReview && schemaReview.preservedSystems
            ? schemaReview.preservedSystems
            : shellCloseout && shellCloseout.preservedSystems
              ? shellCloseout.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      readAccessCloseout && readAccessCloseout.safetyFlags
        ? readAccessCloseout.safetyFlags
        : readAccessPlan && readAccessPlan.safetyFlags
          ? readAccessPlan.safetyFlags
          : schemaReview && schemaReview.safetyFlags
            ? schemaReview.safetyFlags
            : shellCloseout && shellCloseout.safetyFlags
              ? shellCloseout.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    nextStep: "manual-renderer-shared-state-read-result-contract-closeout-plan",
    notes: [
      "Passive plan for the future shared renderer state read result contract only.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "Any future read result must remain snapshot-summary-only and must not return a live shared state reference."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(
  options = {}
) {
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const readAccessCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    readResultContractPlan && Array.isArray(readResultContractPlan.requiredOptionKeys)
      ? readResultContractPlan.requiredOptionKeys
      : readAccessCloseout && Array.isArray(readAccessCloseout.requiredOptionKeys)
        ? readAccessCloseout.requiredOptionKeys
        : readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
          ? readAccessPlan.requiredOptionKeys
          : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
            ? schemaReview.requiredOptionKeys
            : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
              ? shellCloseout.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);
  const stateAppearsInert =
    !!(
      stateIsObject &&
      existingWindowState.inert === true &&
      existingWindowState.rendererInitialized === false &&
      existingWindowState.rendererRunning === false &&
      existingWindowState.mapAttached === false &&
      existingWindowState.drawingEnabled === false &&
      existingWindowState.domCreated === false &&
      existingWindowState.startupWired === false
    );

  return {
    phase: 293,
    name: "custom-25d-visual-manual-renderer-shared-state-read-result-contract-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    readResultContractCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    phase292PlanAvailable:
      typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
      "function",
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionResult &&
          noOptionResult.allowed === false &&
          noOptionResult.sharedStateContainerCreated === false &&
          noOptionResult.windowStateContainerCreated === false
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      createsState: false,
      mutatesState: false
    },
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    plannedReadResultMode: "snapshot-summary-only",
    snapshotContractStayedPassive: true,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    futureReadResultContractReady: false,
    stateExists,
    stateReadOnlyInspectionOnly: true,
    stateAppearsInert,
    sharedStateSnapshotSummary: stateExists
      ? {
          stateAvailable: true,
          stateName: stateIsObject ? existingWindowState.name || null : null,
          phaseCreated:
            stateIsObject && typeof existingWindowState.phaseCreated !== "undefined"
              ? existingWindowState.phaseCreated
              : null,
          inert: stateIsObject ? existingWindowState.inert === true : false,
          manualOnly: stateIsObject ? existingWindowState.manualOnly === true : false,
          shared: stateIsObject ? existingWindowState.shared === true : false,
          rendererInitialized:
            stateIsObject ? existingWindowState.rendererInitialized === true : false,
          rendererRunning:
            stateIsObject ? existingWindowState.rendererRunning === true : false,
          mapAttached: stateIsObject ? existingWindowState.mapAttached === true : false,
          drawingEnabled:
            stateIsObject ? existingWindowState.drawingEnabled === true : false,
          domCreated: stateIsObject ? existingWindowState.domCreated === true : false,
          startupWired: stateIsObject ? existingWindowState.startupWired === true : false,
          layerCount:
            stateIsObject && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0,
          layersIsArray: stateIsObject && Array.isArray(existingWindowState.layers)
        }
      : {
          stateAvailable: false,
          stateName: null,
          phaseCreated: null,
          inert: false,
          manualOnly: false,
          shared: false,
          rendererInitialized: false,
          rendererRunning: false,
          mapAttached: false,
          drawingEnabled: false,
          domCreated: false,
          startupWired: false,
          layerCount: 0,
          layersIsArray: false
        },
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      readResultContractPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
        "function",
      readAccessPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
        "function",
      readAccessPlanCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
        "function",
      schemaReview:
        typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
        "function"
    },
    blockedBehavior:
      readResultContractPlan && readResultContractPlan.blockedBehavior
        ? readResultContractPlan.blockedBehavior
        : readAccessCloseout && readAccessCloseout.blockedBehavior
          ? readAccessCloseout.blockedBehavior
          : readAccessPlan && readAccessPlan.blockedBehavior
            ? readAccessPlan.blockedBehavior
            : schemaReview && schemaReview.blockedBehavior
              ? schemaReview.blockedBehavior
              : shellCloseout && shellCloseout.blockedBehavior
                ? shellCloseout.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      readResultContractPlan && readResultContractPlan.preservedSystems
        ? readResultContractPlan.preservedSystems
        : readAccessCloseout && readAccessCloseout.preservedSystems
          ? readAccessCloseout.preservedSystems
          : readAccessPlan && readAccessPlan.preservedSystems
            ? readAccessPlan.preservedSystems
            : schemaReview && schemaReview.preservedSystems
              ? schemaReview.preservedSystems
              : shellCloseout && shellCloseout.preservedSystems
                ? shellCloseout.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      readResultContractPlan && readResultContractPlan.safetyFlags
        ? readResultContractPlan.safetyFlags
        : readAccessCloseout && readAccessCloseout.safetyFlags
          ? readAccessCloseout.safetyFlags
          : readAccessPlan && readAccessPlan.safetyFlags
            ? readAccessPlan.safetyFlags
            : schemaReview && schemaReview.safetyFlags
              ? schemaReview.safetyFlags
              : shellCloseout && shellCloseout.safetyFlags
                ? shellCloseout.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  },
    nextStep: "manual-renderer-shared-state-read-result-contract-next-phase",
    notes: [
      "Passive closeout for the future shared renderer state read result contract.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "The planned read result remains snapshot-summary-only and must not return a live shared state reference."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(
  options = {}
) {
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const readAccessCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    readResultContractCloseout &&
    Array.isArray(readResultContractCloseout.requiredOptionKeys)
      ? readResultContractCloseout.requiredOptionKeys
      : readResultContractPlan && Array.isArray(readResultContractPlan.requiredOptionKeys)
        ? readResultContractPlan.requiredOptionKeys
        : readAccessCloseout && Array.isArray(readAccessCloseout.requiredOptionKeys)
          ? readAccessCloseout.requiredOptionKeys
          : readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
            ? readAccessPlan.requiredOptionKeys
            : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
              ? schemaReview.requiredOptionKeys
              : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
                ? shellCloseout.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);

  return {
    phase: 294,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-reader-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    snapshotReaderPlanOnly: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    actualSnapshotReaderAdded: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    plannedReaderMode: "snapshot-summary-only",
    plannedReaderCreatesState: false,
    plannedReaderMutatesState: false,
    plannedReaderReturnsLiveReference: false,
    phase292ContractAvailable:
      typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
      "function",
    phase293CloseoutAvailable:
      typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
      "function",
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionResult &&
          noOptionResult.allowed === false &&
          noOptionResult.sharedStateContainerCreated === false &&
          noOptionResult.windowStateContainerCreated === false
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      createsState: false,
      mutatesState: false
    },
    sharedStateSnapshotSummary: stateExists
      ? {
          stateAvailable: true,
          stateName: stateIsObject ? existingWindowState.name || null : null,
          inert: stateIsObject ? existingWindowState.inert === true : false,
          rendererInitialized:
            stateIsObject ? existingWindowState.rendererInitialized === true : false,
          rendererRunning:
            stateIsObject ? existingWindowState.rendererRunning === true : false,
          mapAttached: stateIsObject ? existingWindowState.mapAttached === true : false,
          drawingEnabled:
            stateIsObject ? existingWindowState.drawingEnabled === true : false,
          domCreated: stateIsObject ? existingWindowState.domCreated === true : false,
          startupWired: stateIsObject ? existingWindowState.startupWired === true : false,
          layerCount:
            stateIsObject && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0,
          layersIsArray: stateIsObject && Array.isArray(existingWindowState.layers)
        }
      : {
          stateAvailable: false,
          stateName: null,
          inert: false,
          rendererInitialized: false,
          rendererRunning: false,
          mapAttached: false,
          drawingEnabled: false,
          domCreated: false,
          startupWired: false,
          layerCount: 0,
          layersIsArray: false
        },
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    futureSnapshotReaderReady: false,
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      readResultContractPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
        "function",
      readResultContractCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
        "function",
      readAccessPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
        "function",
      readAccessPlanCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
        "function"
    },
    blockedBehavior:
      readResultContractCloseout && readResultContractCloseout.blockedBehavior
        ? readResultContractCloseout.blockedBehavior
        : readResultContractPlan && readResultContractPlan.blockedBehavior
          ? readResultContractPlan.blockedBehavior
          : readAccessCloseout && readAccessCloseout.blockedBehavior
            ? readAccessCloseout.blockedBehavior
            : readAccessPlan && readAccessPlan.blockedBehavior
              ? readAccessPlan.blockedBehavior
              : schemaReview && schemaReview.blockedBehavior
                ? schemaReview.blockedBehavior
                : shellCloseout && shellCloseout.blockedBehavior
                  ? shellCloseout.blockedBehavior
                  : {
                      rendererCreation: true,
                      rendererInitialization: true,
                      mapAttachment: true,
                      drawing: true,
                      domCreation: true,
                      startupWiring: true,
                      automaticInvocation: true,
                      gameplayChanges: true,
                      pinChanges: true,
                      uiChanges: true,
                      backendChanges: true,
                      storageWrites: true,
                      networkAccess: true
                    },
    preservedSystems:
      readResultContractCloseout && readResultContractCloseout.preservedSystems
        ? readResultContractCloseout.preservedSystems
        : readResultContractPlan && readResultContractPlan.preservedSystems
          ? readResultContractPlan.preservedSystems
          : readAccessCloseout && readAccessCloseout.preservedSystems
            ? readAccessCloseout.preservedSystems
            : readAccessPlan && readAccessPlan.preservedSystems
              ? readAccessPlan.preservedSystems
              : schemaReview && schemaReview.preservedSystems
                ? schemaReview.preservedSystems
                : shellCloseout && shellCloseout.preservedSystems
                  ? shellCloseout.preservedSystems
                  : {
                      existingLeafletMapBehavior: true,
                      osmBehavior: true,
                      gameplay: true,
                      pins: true,
                      playerMarker: true,
                      captureRadius: true,
                      ui: true,
                      backend: true,
                      storage: true,
                      network: true
                    },
    safetyFlags:
      readResultContractCloseout && readResultContractCloseout.safetyFlags
        ? readResultContractCloseout.safetyFlags
        : readResultContractPlan && readResultContractPlan.safetyFlags
          ? readResultContractPlan.safetyFlags
          : readAccessCloseout && readAccessCloseout.safetyFlags
            ? readAccessCloseout.safetyFlags
            : readAccessPlan && readAccessPlan.safetyFlags
              ? readAccessPlan.safetyFlags
              : schemaReview && schemaReview.safetyFlags
                ? schemaReview.safetyFlags
                : shellCloseout && shellCloseout.safetyFlags
                  ? shellCloseout.safetyFlags
                  : {
                      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                    },
    nextStep: "manual-renderer-shared-state-snapshot-reader-contract-plan",
    notes: [
      "Passive plan for a future shared renderer state snapshot reader only.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "The future reader remains snapshot-summary-only and is not added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport(
  options = {}
) {
  const snapshotReaderPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(options)
      : null;
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const readAccessCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    snapshotReaderPlan && Array.isArray(snapshotReaderPlan.requiredOptionKeys)
      ? snapshotReaderPlan.requiredOptionKeys
      : readResultContractCloseout &&
          Array.isArray(readResultContractCloseout.requiredOptionKeys)
        ? readResultContractCloseout.requiredOptionKeys
        : readResultContractPlan && Array.isArray(readResultContractPlan.requiredOptionKeys)
          ? readResultContractPlan.requiredOptionKeys
          : readAccessCloseout && Array.isArray(readAccessCloseout.requiredOptionKeys)
            ? readAccessCloseout.requiredOptionKeys
            : readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
              ? readAccessPlan.requiredOptionKeys
              : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
                ? schemaReview.requiredOptionKeys
                : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
                  ? shellCloseout.requiredOptionKeys
                  : [
                      "manual",
                      "developerIntent",
                      "localDevOnly",
                      "browserConsoleOnly",
                      "explicitOptionsOnly",
                      "allowManualRendererStateContainerShell",
                      "noStartupWiring",
                      "noBackendChanges",
                      "noPersistence",
                      "noAutomaticInvocation"
                    ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);

  return {
    phase: 295,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-reader-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    snapshotReaderPlanCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    actualSnapshotReaderAdded: false,
    phase294PlanAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
      "function",
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionResult &&
          noOptionResult.allowed === false &&
          noOptionResult.sharedStateContainerCreated === false &&
          noOptionResult.windowStateContainerCreated === false
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      createsState: false,
      mutatesState: false
    },
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    plannedReaderMode: "snapshot-summary-only",
    plannedReaderCreatesState: false,
    plannedReaderMutatesState: false,
    plannedReaderReturnsLiveReference: false,
    snapshotReaderPlanStayedPassive: true,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    futureSnapshotReaderReady: false,
    stateExists,
    stateReadOnlyInspectionOnly: true,
    sharedStateSnapshotSummary: stateExists
      ? {
          stateAvailable: true,
          stateName: stateIsObject ? existingWindowState.name || null : null,
          inert: stateIsObject ? existingWindowState.inert === true : false,
          rendererInitialized:
            stateIsObject ? existingWindowState.rendererInitialized === true : false,
          rendererRunning:
            stateIsObject ? existingWindowState.rendererRunning === true : false,
          mapAttached: stateIsObject ? existingWindowState.mapAttached === true : false,
          drawingEnabled:
            stateIsObject ? existingWindowState.drawingEnabled === true : false,
          domCreated: stateIsObject ? existingWindowState.domCreated === true : false,
          startupWired: stateIsObject ? existingWindowState.startupWired === true : false,
          layerCount:
            stateIsObject && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0,
          layersIsArray: stateIsObject && Array.isArray(existingWindowState.layers)
        }
      : {
          stateAvailable: false,
          stateName: null,
          inert: false,
          rendererInitialized: false,
          rendererRunning: false,
          mapAttached: false,
          drawingEnabled: false,
          domCreated: false,
          startupWired: false,
          layerCount: 0,
          layersIsArray: false
        },
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      snapshotReaderPlan:
        typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
        "function",
      readResultContractPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
        "function",
      readResultContractCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
        "function",
      readAccessPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
        "function",
      readAccessPlanCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
        "function"
    },
    blockedBehavior:
      snapshotReaderPlan && snapshotReaderPlan.blockedBehavior
        ? snapshotReaderPlan.blockedBehavior
        : readResultContractCloseout && readResultContractCloseout.blockedBehavior
          ? readResultContractCloseout.blockedBehavior
          : readResultContractPlan && readResultContractPlan.blockedBehavior
            ? readResultContractPlan.blockedBehavior
            : readAccessCloseout && readAccessCloseout.blockedBehavior
              ? readAccessCloseout.blockedBehavior
              : readAccessPlan && readAccessPlan.blockedBehavior
                ? readAccessPlan.blockedBehavior
                : schemaReview && schemaReview.blockedBehavior
                  ? schemaReview.blockedBehavior
                  : shellCloseout && shellCloseout.blockedBehavior
                    ? shellCloseout.blockedBehavior
                    : {
                        rendererCreation: true,
                        rendererInitialization: true,
                        mapAttachment: true,
                        drawing: true,
                        domCreation: true,
                        startupWiring: true,
                        automaticInvocation: true,
                        gameplayChanges: true,
                        pinChanges: true,
                        uiChanges: true,
                        backendChanges: true,
                        storageWrites: true,
                        networkAccess: true
                      },
    preservedSystems:
      snapshotReaderPlan && snapshotReaderPlan.preservedSystems
        ? snapshotReaderPlan.preservedSystems
        : readResultContractCloseout && readResultContractCloseout.preservedSystems
          ? readResultContractCloseout.preservedSystems
          : readResultContractPlan && readResultContractPlan.preservedSystems
            ? readResultContractPlan.preservedSystems
            : readAccessCloseout && readAccessCloseout.preservedSystems
              ? readAccessCloseout.preservedSystems
              : readAccessPlan && readAccessPlan.preservedSystems
                ? readAccessPlan.preservedSystems
                : schemaReview && schemaReview.preservedSystems
                  ? schemaReview.preservedSystems
                  : shellCloseout && shellCloseout.preservedSystems
                    ? shellCloseout.preservedSystems
                    : {
                        existingLeafletMapBehavior: true,
                        osmBehavior: true,
                        gameplay: true,
                        pins: true,
                        playerMarker: true,
                        captureRadius: true,
                        ui: true,
                        backend: true,
                        storage: true,
                        network: true
                      },
    safetyFlags:
      snapshotReaderPlan && snapshotReaderPlan.safetyFlags
        ? snapshotReaderPlan.safetyFlags
        : readResultContractCloseout && readResultContractCloseout.safetyFlags
          ? readResultContractCloseout.safetyFlags
          : readResultContractPlan && readResultContractPlan.safetyFlags
            ? readResultContractPlan.safetyFlags
            : readAccessCloseout && readAccessCloseout.safetyFlags
              ? readAccessCloseout.safetyFlags
              : readAccessPlan && readAccessPlan.safetyFlags
                ? readAccessPlan.safetyFlags
                : schemaReview && schemaReview.safetyFlags
                  ? schemaReview.safetyFlags
                  : shellCloseout && shellCloseout.safetyFlags
                    ? shellCloseout.safetyFlags
                    : {
                        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                      },
    nextStep: "manual-renderer-shared-state-snapshot-reader-readiness-review",
    notes: [
      "Passive closeout for the future shared renderer state snapshot reader plan.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "The future reader remains snapshot-summary-only and is still not added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport(
  options = {}
) {
  const snapshotReaderPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(options)
      : null;
  const snapshotReaderPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport(options)
      : null;
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const readAccessCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
      ? createCustom25DVisualManualRendererSharedStateContainerShell()
      : null;

  const requiredOptionKeys =
    snapshotReaderPlanCloseout &&
    Array.isArray(snapshotReaderPlanCloseout.requiredOptionKeys)
      ? snapshotReaderPlanCloseout.requiredOptionKeys
      : snapshotReaderPlan && Array.isArray(snapshotReaderPlan.requiredOptionKeys)
        ? snapshotReaderPlan.requiredOptionKeys
        : readResultContractCloseout &&
            Array.isArray(readResultContractCloseout.requiredOptionKeys)
          ? readResultContractCloseout.requiredOptionKeys
          : readResultContractPlan && Array.isArray(readResultContractPlan.requiredOptionKeys)
            ? readResultContractPlan.requiredOptionKeys
            : readAccessCloseout && Array.isArray(readAccessCloseout.requiredOptionKeys)
              ? readAccessCloseout.requiredOptionKeys
              : readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
                ? readAccessPlan.requiredOptionKeys
                : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
                  ? schemaReview.requiredOptionKeys
                  : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
                    ? shellCloseout.requiredOptionKeys
                    : [
                        "manual",
                        "developerIntent",
                        "localDevOnly",
                        "browserConsoleOnly",
                        "explicitOptionsOnly",
                        "allowManualRendererStateContainerShell",
                        "noStartupWiring",
                        "noBackendChanges",
                        "noPersistence",
                        "noAutomaticInvocation"
                      ];

  const existingWindowState =
    typeof window !== "undefined" && window
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);

  return {
    phase: 295,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-reader-readiness",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    snapshotReaderReadiness: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    actualSnapshotReaderAdded: false,
    phase294PlanAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
      "function",
    phase295CloseoutAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
      "function",
    readResultContractAvailable:
      typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
      "function" &&
      typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
        "function",
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionResult &&
          noOptionResult.allowed === false &&
          noOptionResult.sharedStateContainerCreated === false &&
          noOptionResult.windowStateContainerCreated === false
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      createsState: false,
      mutatesState: false
    },
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    plannedReaderMode: "snapshot-summary-only",
    futureReaderCreatesState: false,
    futureReaderMutatesState: false,
    futureReaderReturnsLiveReference: false,
    futureReaderShouldReturnPrimitiveSnapshotOnly: true,
    readyForActualSnapshotReaderPhase: true,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    stateExists,
    stateReadOnlyInspectionOnly: true,
    sharedStateSnapshotSummary: stateExists
      ? {
          stateAvailable: true,
          stateName: stateIsObject ? existingWindowState.name || null : null,
          inert: stateIsObject ? existingWindowState.inert === true : false,
          rendererInitialized:
            stateIsObject ? existingWindowState.rendererInitialized === true : false,
          rendererRunning:
            stateIsObject ? existingWindowState.rendererRunning === true : false,
          mapAttached: stateIsObject ? existingWindowState.mapAttached === true : false,
          drawingEnabled:
            stateIsObject ? existingWindowState.drawingEnabled === true : false,
          domCreated: stateIsObject ? existingWindowState.domCreated === true : false,
          startupWired: stateIsObject ? existingWindowState.startupWired === true : false,
          layerCount:
            stateIsObject && typeof existingWindowState.layerCount === "number"
              ? existingWindowState.layerCount
              : 0,
          layersIsArray: stateIsObject && Array.isArray(existingWindowState.layers)
        }
      : {
          stateAvailable: false,
          stateName: null,
          inert: false,
          rendererInitialized: false,
          rendererRunning: false,
          mapAttached: false,
          drawingEnabled: false,
          domCreated: false,
          startupWired: false,
          layerCount: 0,
          layersIsArray: false
        },
    stateContainerAuthorizesRendererCreation: false,
    stateContainerAuthorizesRendererStart: false,
    stateContainerAuthorizesMapAttachment: false,
    stateContainerAuthorizesDrawing: false,
    stateContainerAuthorizesDomMutation: false,
    stateContainerAuthorizesLayerMutation: false,
    stateContainerAuthorizesStateMutation: false,
    reviewedHelpers: {
      snapshotReaderPlan:
        typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
        "function",
      snapshotReaderPlanCloseout:
        typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
        "function",
      readResultContractPlan:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
        "function",
      readResultContractCloseout:
        typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
        "function"
    },
    blockedBehavior:
      snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.blockedBehavior
        ? snapshotReaderPlanCloseout.blockedBehavior
        : snapshotReaderPlan && snapshotReaderPlan.blockedBehavior
          ? snapshotReaderPlan.blockedBehavior
          : readResultContractCloseout && readResultContractCloseout.blockedBehavior
            ? readResultContractCloseout.blockedBehavior
            : readResultContractPlan && readResultContractPlan.blockedBehavior
              ? readResultContractPlan.blockedBehavior
              : readAccessCloseout && readAccessCloseout.blockedBehavior
                ? readAccessCloseout.blockedBehavior
                : readAccessPlan && readAccessPlan.blockedBehavior
                  ? readAccessPlan.blockedBehavior
                  : schemaReview && schemaReview.blockedBehavior
                    ? schemaReview.blockedBehavior
                    : shellCloseout && shellCloseout.blockedBehavior
                      ? shellCloseout.blockedBehavior
                      : {
                          rendererCreation: true,
                          rendererInitialization: true,
                          mapAttachment: true,
                          drawing: true,
                          domCreation: true,
                          startupWiring: true,
                          automaticInvocation: true,
                          gameplayChanges: true,
                          pinChanges: true,
                          uiChanges: true,
                          backendChanges: true,
                          storageWrites: true,
                          networkAccess: true
                        },
    preservedSystems:
      snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.preservedSystems
        ? snapshotReaderPlanCloseout.preservedSystems
        : snapshotReaderPlan && snapshotReaderPlan.preservedSystems
          ? snapshotReaderPlan.preservedSystems
          : readResultContractCloseout && readResultContractCloseout.preservedSystems
            ? readResultContractCloseout.preservedSystems
            : readResultContractPlan && readResultContractPlan.preservedSystems
              ? readResultContractPlan.preservedSystems
              : readAccessCloseout && readAccessCloseout.preservedSystems
                ? readAccessCloseout.preservedSystems
                : readAccessPlan && readAccessPlan.preservedSystems
                  ? readAccessPlan.preservedSystems
                  : schemaReview && schemaReview.preservedSystems
                    ? schemaReview.preservedSystems
                    : shellCloseout && shellCloseout.preservedSystems
                      ? shellCloseout.preservedSystems
                      : {
                          existingLeafletMapBehavior: true,
                          osmBehavior: true,
                          gameplay: true,
                          pins: true,
                          playerMarker: true,
                          captureRadius: true,
                          ui: true,
                          backend: true,
                          storage: true,
                          network: true
                        },
    safetyFlags:
      snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.safetyFlags
        ? snapshotReaderPlanCloseout.safetyFlags
        : snapshotReaderPlan && snapshotReaderPlan.safetyFlags
          ? snapshotReaderPlan.safetyFlags
          : readResultContractCloseout && readResultContractCloseout.safetyFlags
            ? readResultContractCloseout.safetyFlags
            : readResultContractPlan && readResultContractPlan.safetyFlags
              ? readResultContractPlan.safetyFlags
              : readAccessCloseout && readAccessCloseout.safetyFlags
                ? readAccessCloseout.safetyFlags
                : readAccessPlan && readAccessPlan.safetyFlags
                  ? readAccessPlan.safetyFlags
                  : schemaReview && schemaReview.safetyFlags
                    ? schemaReview.safetyFlags
                    : shellCloseout && shellCloseout.safetyFlags
                      ? shellCloseout.safetyFlags
                      : {
                          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                        },
    nextStep: "manual-renderer-shared-state-actual-snapshot-reader-phase",
    notes: [
      "Passive readiness report for a future shared renderer state snapshot reader.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "The future reader should return primitive snapshot summaries only and remains not-yet-implemented in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshot(options = {}) {
  const snapshotReaderPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(options)
      : null;
  const snapshotReaderPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport(options)
      : null;
  const snapshotReaderReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport(options)
      : null;
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;

  const requiredOptionKeys =
    snapshotReaderReadiness && Array.isArray(snapshotReaderReadiness.requiredOptionKeys)
      ? snapshotReaderReadiness.requiredOptionKeys
      : snapshotReaderPlanCloseout &&
          Array.isArray(snapshotReaderPlanCloseout.requiredOptionKeys)
        ? snapshotReaderPlanCloseout.requiredOptionKeys
        : snapshotReaderPlan && Array.isArray(snapshotReaderPlan.requiredOptionKeys)
          ? snapshotReaderPlan.requiredOptionKeys
          : readResultContractCloseout &&
              Array.isArray(readResultContractCloseout.requiredOptionKeys)
            ? readResultContractCloseout.requiredOptionKeys
            : readResultContractPlan &&
                Array.isArray(readResultContractPlan.requiredOptionKeys)
              ? readResultContractPlan.requiredOptionKeys
              : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
                ? schemaReview.requiredOptionKeys
                : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
                  ? shellCloseout.requiredOptionKeys
                  : [
                      "manual",
                      "developerIntent",
                      "localDevOnly",
                      "browserConsoleOnly",
                      "explicitOptionsOnly",
                      "allowManualRendererStateContainerShell",
                      "noStartupWiring",
                      "noBackendChanges",
                      "noPersistence",
                      "noAutomaticInvocation"
                    ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell: "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const baseResult = {
    phase: 296,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot",
    ok: true,
    source: "custom-25d-visual-manual-renderer-shared-state-snapshot",
    snapshotOnly: true,
    reportOnly: false,
    requiredOptionKeys,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    layersIsArray: false,
    stateName: null,
    phaseCreated: null,
    inert: false,
    manualOnly: false,
    shared: false,
    rendererCreated: false,
    blockedBehavior:
      snapshotReaderReadiness && snapshotReaderReadiness.blockedBehavior
        ? snapshotReaderReadiness.blockedBehavior
        : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.blockedBehavior
          ? snapshotReaderPlanCloseout.blockedBehavior
          : snapshotReaderPlan && snapshotReaderPlan.blockedBehavior
            ? snapshotReaderPlan.blockedBehavior
            : readResultContractCloseout && readResultContractCloseout.blockedBehavior
              ? readResultContractCloseout.blockedBehavior
              : readResultContractPlan && readResultContractPlan.blockedBehavior
                ? readResultContractPlan.blockedBehavior
                : schemaReview && schemaReview.blockedBehavior
                  ? schemaReview.blockedBehavior
                  : shellCloseout && shellCloseout.blockedBehavior
                    ? shellCloseout.blockedBehavior
                    : {
                        rendererCreation: true,
                        rendererInitialization: true,
                        mapAttachment: true,
                        drawing: true,
                        domCreation: true,
                        startupWiring: true,
                        automaticInvocation: true,
                        gameplayChanges: true,
                        pinChanges: true,
                        uiChanges: true,
                        backendChanges: true,
                        storageWrites: true,
                        networkAccess: true
                      },
    preservedSystems:
      snapshotReaderReadiness && snapshotReaderReadiness.preservedSystems
        ? snapshotReaderReadiness.preservedSystems
        : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.preservedSystems
          ? snapshotReaderPlanCloseout.preservedSystems
          : snapshotReaderPlan && snapshotReaderPlan.preservedSystems
            ? snapshotReaderPlan.preservedSystems
            : readResultContractCloseout && readResultContractCloseout.preservedSystems
              ? readResultContractCloseout.preservedSystems
              : readResultContractPlan && readResultContractPlan.preservedSystems
                ? readResultContractPlan.preservedSystems
                : schemaReview && schemaReview.preservedSystems
                  ? schemaReview.preservedSystems
                  : shellCloseout && shellCloseout.preservedSystems
                    ? shellCloseout.preservedSystems
                    : {
                        existingLeafletMapBehavior: true,
                        osmBehavior: true,
                        gameplay: true,
                        pins: true,
                        playerMarker: true,
                        captureRadius: true,
                        ui: true,
                        backend: true,
                        storage: true,
                        network: true
                      },
    safetyFlags:
      snapshotReaderReadiness && snapshotReaderReadiness.safetyFlags
        ? snapshotReaderReadiness.safetyFlags
        : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.safetyFlags
          ? snapshotReaderPlanCloseout.safetyFlags
          : snapshotReaderPlan && snapshotReaderPlan.safetyFlags
            ? snapshotReaderPlan.safetyFlags
            : readResultContractCloseout && readResultContractCloseout.safetyFlags
              ? readResultContractCloseout.safetyFlags
              : readResultContractPlan && readResultContractPlan.safetyFlags
                ? readResultContractPlan.safetyFlags
                : schemaReview && schemaReview.safetyFlags
                  ? schemaReview.safetyFlags
                  : shellCloseout && shellCloseout.safetyFlags
                    ? shellCloseout.safetyFlags
                    : {
                        custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                        landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                        landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                        dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                      }
  };

  if (missingKey) {
    return {
      ...baseResult,
      blocked: true,
      allowed: false,
      sharedStateAvailable: false,
      failedRequirement: missingKey,
      reason: reasonByKey[missingKey] || "required-option-missing",
      notes: [
        "Default/no-option snapshot access stays blocked.",
        "Requires explicit manual/local-dev/browser-console approvals before any shared state inspection.",
        "Returns snapshot-summary data only and never a live shared state reference."
      ]
    };
  }

  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";

  if (!windowExists) {
    return {
      ...baseResult,
      blocked: true,
      allowed: false,
      sharedStateAvailable: false,
      failedRequirement: "windowAvailable",
      reason: "window-unavailable",
      notes: [
        "Snapshot access requires a browser window.",
        "Does not create shared state when window is unavailable.",
        "Returns snapshot-summary data only and never a live shared state reference."
      ]
    };
  }

  if (!isLocalDevHost) {
    return {
      ...baseResult,
      blocked: true,
      allowed: false,
      sharedStateAvailable: false,
      failedRequirement: "localDevHost",
      reason: "local-dev-host-required",
      notes: [
        "Snapshot access is restricted to local development hosts.",
        "Does not create shared state on blocked hosts.",
        "Returns snapshot-summary data only and never a live shared state reference."
      ]
    };
  }

  const existingWindowState = window.custom25DVisualManualRendererState;
  const stateExists = typeof existingWindowState !== "undefined";
  const stateIsObject =
    !!existingWindowState &&
    typeof existingWindowState === "object" &&
    !Array.isArray(existingWindowState);

  if (!stateExists || !stateIsObject) {
    return {
      ...baseResult,
      blocked: true,
      allowed: false,
      sharedStateAvailable: false,
      reason: "shared-state-container-not-present",
      failedRequirement: "sharedStateAvailable",
      notes: [
        "Shared state was not present for snapshot reading.",
        "This helper never creates missing shared state.",
        "Returns snapshot-summary data only and never a live shared state reference."
      ]
    };
  }

  return {
    ...baseResult,
    blocked: false,
    allowed: true,
    sharedStateAvailable: true,
    stateName:
      typeof existingWindowState.name === "string" ? existingWindowState.name : null,
    phaseCreated:
      typeof existingWindowState.phaseCreated === "number"
        ? existingWindowState.phaseCreated
        : null,
    inert: existingWindowState.inert === true,
    manualOnly: existingWindowState.manualOnly === true,
    shared: existingWindowState.shared === true,
    rendererCreated: existingWindowState.rendererCreated === true,
    rendererInitialized: existingWindowState.rendererInitialized === true,
    rendererRunning: existingWindowState.rendererRunning === true,
    mapAttached: existingWindowState.mapAttached === true,
    drawingEnabled: existingWindowState.drawingEnabled === true,
    domCreated: existingWindowState.domCreated === true,
    startupWired: existingWindowState.startupWired === true,
    layerCount:
      typeof existingWindowState.layerCount === "number"
        ? existingWindowState.layerCount
        : Array.isArray(existingWindowState.layers)
          ? existingWindowState.layers.length
          : 0,
    layersIsArray: Array.isArray(existingWindowState.layers),
    notes: [
      "Returns only primitive snapshot-summary fields from shared state.",
      "Does not return the shared state object or the live layers array.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(
  options = {}
) {
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const snapshotReaderReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport(options)
      : null;
  const snapshotReaderPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport(options)
      : null;
  const snapshotReaderPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(options)
      : null;
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;
  const readAccessPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport(options)
      : null;
  const readAccessCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport(options)
      : null;
  const schemaReview =
    typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport(options)
      : null;

  const requiredOptionKeys =
    snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
      ? snapshotResult.requiredOptionKeys
      : snapshotReaderReadiness && Array.isArray(snapshotReaderReadiness.requiredOptionKeys)
        ? snapshotReaderReadiness.requiredOptionKeys
        : snapshotReaderPlanCloseout &&
            Array.isArray(snapshotReaderPlanCloseout.requiredOptionKeys)
          ? snapshotReaderPlanCloseout.requiredOptionKeys
          : snapshotReaderPlan && Array.isArray(snapshotReaderPlan.requiredOptionKeys)
            ? snapshotReaderPlan.requiredOptionKeys
            : readResultContractCloseout &&
                Array.isArray(readResultContractCloseout.requiredOptionKeys)
              ? readResultContractCloseout.requiredOptionKeys
              : readResultContractPlan &&
                  Array.isArray(readResultContractPlan.requiredOptionKeys)
                ? readResultContractPlan.requiredOptionKeys
                : readAccessCloseout && Array.isArray(readAccessCloseout.requiredOptionKeys)
                  ? readAccessCloseout.requiredOptionKeys
                  : readAccessPlan && Array.isArray(readAccessPlan.requiredOptionKeys)
                    ? readAccessPlan.requiredOptionKeys
                    : schemaReview && Array.isArray(schemaReview.requiredOptionKeys)
                      ? schemaReview.requiredOptionKeys
                      : [
                          "manual",
                          "developerIntent",
                          "localDevOnly",
                          "browserConsoleOnly",
                          "explicitOptionsOnly",
                          "allowManualRendererStateContainerShell",
                          "noStartupWiring",
                          "noBackendChanges",
                          "noPersistence",
                          "noAutomaticInvocation"
                        ];

  return {
    phase: 297,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    snapshotCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    phase296SnapshotAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function",
    snapshotOnly: true,
    defaultNoOptionCheck: {
      blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    layersReturned: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    snapshotReaderStayedSafe: true,
    sharedStateAvailable:
      !!(snapshotResult && snapshotResult.allowed === true && snapshotResult.sharedStateAvailable),
    snapshotStateName:
      snapshotResult && snapshotResult.allowed === true ? snapshotResult.stateName || null : null,
    snapshotPhaseCreated:
      snapshotResult && snapshotResult.allowed === true ? snapshotResult.phaseCreated : null,
    snapshotLayerCount:
      snapshotResult && snapshotResult.allowed === true ? snapshotResult.layerCount || 0 : 0,
    snapshotLayersIsArray:
      !!(snapshotResult && snapshotResult.allowed === true && snapshotResult.layersIsArray),
    blockedBehavior:
      snapshotResult && snapshotResult.blockedBehavior
        ? snapshotResult.blockedBehavior
        : snapshotReaderReadiness && snapshotReaderReadiness.blockedBehavior
          ? snapshotReaderReadiness.blockedBehavior
          : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.blockedBehavior
            ? snapshotReaderPlanCloseout.blockedBehavior
            : snapshotReaderPlan && snapshotReaderPlan.blockedBehavior
              ? snapshotReaderPlan.blockedBehavior
              : readResultContractCloseout && readResultContractCloseout.blockedBehavior
                ? readResultContractCloseout.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      snapshotResult && snapshotResult.preservedSystems
        ? snapshotResult.preservedSystems
        : snapshotReaderReadiness && snapshotReaderReadiness.preservedSystems
          ? snapshotReaderReadiness.preservedSystems
          : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.preservedSystems
            ? snapshotReaderPlanCloseout.preservedSystems
            : snapshotReaderPlan && snapshotReaderPlan.preservedSystems
              ? snapshotReaderPlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      snapshotResult && snapshotResult.safetyFlags
        ? snapshotResult.safetyFlags
        : snapshotReaderReadiness && snapshotReaderReadiness.safetyFlags
          ? snapshotReaderReadiness.safetyFlags
          : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.safetyFlags
            ? snapshotReaderPlanCloseout.safetyFlags
            : snapshotReaderPlan && snapshotReaderPlan.safetyFlags
              ? snapshotReaderPlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive closeout for the safe shared renderer state snapshot reader.",
      "Does not create, replace, or mutate window.custom25DVisualManualRendererState.",
      "Does not return the live shared state object or nested live references such as layers."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(
  options = {}
) {
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const snapshotReaderReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport(options)
      : null;
  const snapshotReaderPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport(options)
      : null;
  const snapshotReaderPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(options)
      : null;
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;

  const requiredOptionKeys =
    snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
      ? snapshotCloseout.requiredOptionKeys
      : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
        ? snapshotResult.requiredOptionKeys
        : snapshotReaderReadiness && Array.isArray(snapshotReaderReadiness.requiredOptionKeys)
          ? snapshotReaderReadiness.requiredOptionKeys
          : snapshotReaderPlanCloseout &&
              Array.isArray(snapshotReaderPlanCloseout.requiredOptionKeys)
            ? snapshotReaderPlanCloseout.requiredOptionKeys
            : snapshotReaderPlan && Array.isArray(snapshotReaderPlan.requiredOptionKeys)
              ? snapshotReaderPlan.requiredOptionKeys
              : readResultContractCloseout &&
                  Array.isArray(readResultContractCloseout.requiredOptionKeys)
                ? readResultContractCloseout.requiredOptionKeys
                : readResultContractPlan &&
                    Array.isArray(readResultContractPlan.requiredOptionKeys)
                  ? readResultContractPlan.requiredOptionKeys
                  : [
                      "manual",
                      "developerIntent",
                      "localDevOnly",
                      "browserConsoleOnly",
                      "explicitOptionsOnly",
                      "allowManualRendererStateContainerShell",
                      "noStartupWiring",
                      "noBackendChanges",
                      "noPersistence",
                      "noAutomaticInvocation"
                    ];

  return {
    phase: 297,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer-readiness",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    snapshotConsumerReadiness: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    actualSnapshotConsumerAdded: false,
    phase296SnapshotAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function",
    phase297CloseoutAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport ===
      "function",
    plannedConsumerMode: "snapshot-summary-consumer-only",
    futureConsumerShouldUseSnapshotOnly: true,
    futureConsumerShouldNotUseLiveStateReference: true,
    futureConsumerCreatesState: false,
    futureConsumerMutatesState: false,
    futureConsumerStartsRenderer: false,
    futureConsumerAttachesMap: false,
    futureConsumerDrawsGeometry: false,
    futureConsumerCreatesDom: false,
    readyForFutureSnapshotConsumerPlan: true,
    defaultNoOptionCheck: {
      blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    layersReturned: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    sharedStateAvailable:
      !!(snapshotResult && snapshotResult.allowed === true && snapshotResult.sharedStateAvailable),
    blockedBehavior:
      snapshotCloseout && snapshotCloseout.blockedBehavior
        ? snapshotCloseout.blockedBehavior
        : snapshotResult && snapshotResult.blockedBehavior
          ? snapshotResult.blockedBehavior
          : snapshotReaderReadiness && snapshotReaderReadiness.blockedBehavior
            ? snapshotReaderReadiness.blockedBehavior
            : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.blockedBehavior
              ? snapshotReaderPlanCloseout.blockedBehavior
              : snapshotReaderPlan && snapshotReaderPlan.blockedBehavior
                ? snapshotReaderPlan.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      snapshotCloseout && snapshotCloseout.preservedSystems
        ? snapshotCloseout.preservedSystems
        : snapshotResult && snapshotResult.preservedSystems
          ? snapshotResult.preservedSystems
          : snapshotReaderReadiness && snapshotReaderReadiness.preservedSystems
            ? snapshotReaderReadiness.preservedSystems
            : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.preservedSystems
              ? snapshotReaderPlanCloseout.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      snapshotCloseout && snapshotCloseout.safetyFlags
        ? snapshotCloseout.safetyFlags
        : snapshotResult && snapshotResult.safetyFlags
          ? snapshotResult.safetyFlags
          : snapshotReaderReadiness && snapshotReaderReadiness.safetyFlags
            ? snapshotReaderReadiness.safetyFlags
            : snapshotReaderPlanCloseout && snapshotReaderPlanCloseout.safetyFlags
              ? snapshotReaderPlanCloseout.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive readiness report for a future snapshot consumer only.",
      "A future consumer must use snapshot-summary values only and not the live shared state object.",
      "No actual snapshot consumer is added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport(
  options = {}
) {
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotConsumerReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(options)
      : null;
  const snapshotReaderReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport(options)
      : null;
  const snapshotReaderPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport(options)
      : null;
  const snapshotReaderPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport(options)
      : null;
  const readResultContractPlan =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport(options)
      : null;
  const readResultContractCloseout =
    typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport(options)
      : null;

  const requiredOptionKeys =
    snapshotConsumerReadiness &&
    Array.isArray(snapshotConsumerReadiness.requiredOptionKeys)
      ? snapshotConsumerReadiness.requiredOptionKeys
      : snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
        ? snapshotCloseout.requiredOptionKeys
        : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
          ? snapshotResult.requiredOptionKeys
          : snapshotReaderReadiness && Array.isArray(snapshotReaderReadiness.requiredOptionKeys)
            ? snapshotReaderReadiness.requiredOptionKeys
            : snapshotReaderPlanCloseout &&
                Array.isArray(snapshotReaderPlanCloseout.requiredOptionKeys)
              ? snapshotReaderPlanCloseout.requiredOptionKeys
              : snapshotReaderPlan && Array.isArray(snapshotReaderPlan.requiredOptionKeys)
                ? snapshotReaderPlan.requiredOptionKeys
                : readResultContractCloseout &&
                    Array.isArray(readResultContractCloseout.requiredOptionKeys)
                  ? readResultContractCloseout.requiredOptionKeys
                  : readResultContractPlan &&
                      Array.isArray(readResultContractPlan.requiredOptionKeys)
                    ? readResultContractPlan.requiredOptionKeys
                    : [
                        "manual",
                        "developerIntent",
                        "localDevOnly",
                        "browserConsoleOnly",
                        "explicitOptionsOnly",
                        "allowManualRendererStateContainerShell",
                        "noStartupWiring",
                        "noBackendChanges",
                        "noPersistence",
                        "noAutomaticInvocation"
                      ];

  return {
    phase: 298,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    snapshotConsumerPlan: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    actualSnapshotConsumerAdded: false,
    phase296SnapshotAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function",
    phase297ReadinessAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
      "function",
    plannedConsumerMode: "snapshot-summary-consumer-only",
    plannedConsumerUsesSnapshotOnly: true,
    plannedConsumerUsesLiveStateReference: false,
    plannedConsumerCreatesState: false,
    plannedConsumerMutatesState: false,
    plannedConsumerStartsRenderer: false,
    plannedConsumerAttachesMap: false,
    plannedConsumerDrawsGeometry: false,
    plannedConsumerCreatesDom: false,
    futureSnapshotConsumerReady: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    layersReturned: false,
    sharedStateAvailable:
      !!(snapshotResult && snapshotResult.allowed === true && snapshotResult.sharedStateAvailable),
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      snapshotConsumerReadiness && snapshotConsumerReadiness.blockedBehavior
        ? snapshotConsumerReadiness.blockedBehavior
        : snapshotCloseout && snapshotCloseout.blockedBehavior
          ? snapshotCloseout.blockedBehavior
          : snapshotResult && snapshotResult.blockedBehavior
            ? snapshotResult.blockedBehavior
            : snapshotReaderReadiness && snapshotReaderReadiness.blockedBehavior
              ? snapshotReaderReadiness.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      snapshotConsumerReadiness && snapshotConsumerReadiness.preservedSystems
        ? snapshotConsumerReadiness.preservedSystems
        : snapshotCloseout && snapshotCloseout.preservedSystems
          ? snapshotCloseout.preservedSystems
          : snapshotResult && snapshotResult.preservedSystems
            ? snapshotResult.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      snapshotConsumerReadiness && snapshotConsumerReadiness.safetyFlags
        ? snapshotConsumerReadiness.safetyFlags
        : snapshotCloseout && snapshotCloseout.safetyFlags
          ? snapshotCloseout.safetyFlags
          : snapshotResult && snapshotResult.safetyFlags
            ? snapshotResult.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive plan for a future snapshot consumer only.",
      "A future consumer must use snapshot-summary values only and never the live shared state object.",
      "No renderer, map, drawing, DOM, startup, or gameplay consumer is added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport(
  options = {}
) {
  const consumerPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport(options)
      : null;
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotConsumerReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(options)
      : null;

  const requiredOptionKeys =
    consumerPlan && Array.isArray(consumerPlan.requiredOptionKeys)
      ? consumerPlan.requiredOptionKeys
      : snapshotConsumerReadiness &&
          Array.isArray(snapshotConsumerReadiness.requiredOptionKeys)
        ? snapshotConsumerReadiness.requiredOptionKeys
        : snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
          ? snapshotCloseout.requiredOptionKeys
          : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
            ? snapshotResult.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  return {
    phase: 298,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer-contract",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    snapshotConsumerContract: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    actualSnapshotConsumerAdded: false,
    allowedInputMode: "snapshot-summary-only",
    disallowedInputModes: [
      "live-shared-state-reference",
      "live-layers-array",
      "leaflet-layer-reference",
      "dom-node-reference"
    ],
    allowedOutputMode: "primitive-report-only",
    consumerMayCreateState: false,
    consumerMayMutateState: false,
    consumerMayStartRenderer: false,
    consumerMayAttachMap: false,
    consumerMayDrawGeometry: false,
    consumerMayCreateDom: false,
    consumerMayWireStartup: false,
    consumerMayChangeGameplay: false,
    contractReadyForFutureConsumer: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    layersReturned: false,
    sharedStateAvailable:
      !!(snapshotResult && snapshotResult.allowed === true && snapshotResult.sharedStateAvailable),
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      consumerPlan && consumerPlan.blockedBehavior
        ? consumerPlan.blockedBehavior
        : snapshotConsumerReadiness && snapshotConsumerReadiness.blockedBehavior
          ? snapshotConsumerReadiness.blockedBehavior
          : snapshotCloseout && snapshotCloseout.blockedBehavior
            ? snapshotCloseout.blockedBehavior
            : snapshotResult && snapshotResult.blockedBehavior
              ? snapshotResult.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      consumerPlan && consumerPlan.preservedSystems
        ? consumerPlan.preservedSystems
        : snapshotConsumerReadiness && snapshotConsumerReadiness.preservedSystems
          ? snapshotConsumerReadiness.preservedSystems
          : snapshotCloseout && snapshotCloseout.preservedSystems
            ? snapshotCloseout.preservedSystems
            : snapshotResult && snapshotResult.preservedSystems
              ? snapshotResult.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      consumerPlan && consumerPlan.safetyFlags
        ? consumerPlan.safetyFlags
        : snapshotConsumerReadiness && snapshotConsumerReadiness.safetyFlags
          ? snapshotConsumerReadiness.safetyFlags
          : snapshotCloseout && snapshotCloseout.safetyFlags
            ? snapshotCloseout.safetyFlags
            : snapshotResult && snapshotResult.safetyFlags
              ? snapshotResult.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive contract for a future snapshot consumer only.",
      "A future consumer may accept snapshot-summary input only and must output primitive report data only.",
      "Live shared-state references and nested live references remain disallowed."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport(
  options = {}
) {
  const consumerPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport(options)
      : null;
  const consumerContract =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport(options)
      : null;
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotConsumerReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(options)
      : null;

  const requiredOptionKeys =
    consumerContract && Array.isArray(consumerContract.requiredOptionKeys)
      ? consumerContract.requiredOptionKeys
      : consumerPlan && Array.isArray(consumerPlan.requiredOptionKeys)
        ? consumerPlan.requiredOptionKeys
        : snapshotConsumerReadiness &&
            Array.isArray(snapshotConsumerReadiness.requiredOptionKeys)
          ? snapshotConsumerReadiness.requiredOptionKeys
          : snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
            ? snapshotCloseout.requiredOptionKeys
            : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
              ? snapshotResult.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  return {
    phase: 298,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    snapshotConsumerPlanCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    phase298PlanAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
      "function",
    phase298ContractAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
      "function",
    actualSnapshotConsumerAdded: false,
    snapshotConsumerPlanStayedPassive: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureActualSnapshotConsumer: true,
    sharedStateAvailable:
      !!(snapshotResult && snapshotResult.allowed === true && snapshotResult.sharedStateAvailable),
    blockedBehavior:
      consumerContract && consumerContract.blockedBehavior
        ? consumerContract.blockedBehavior
        : consumerPlan && consumerPlan.blockedBehavior
          ? consumerPlan.blockedBehavior
          : snapshotConsumerReadiness && snapshotConsumerReadiness.blockedBehavior
            ? snapshotConsumerReadiness.blockedBehavior
            : snapshotCloseout && snapshotCloseout.blockedBehavior
              ? snapshotCloseout.blockedBehavior
              : snapshotResult && snapshotResult.blockedBehavior
                ? snapshotResult.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      consumerContract && consumerContract.preservedSystems
        ? consumerContract.preservedSystems
        : consumerPlan && consumerPlan.preservedSystems
          ? consumerPlan.preservedSystems
          : snapshotConsumerReadiness && snapshotConsumerReadiness.preservedSystems
            ? snapshotConsumerReadiness.preservedSystems
            : snapshotCloseout && snapshotCloseout.preservedSystems
              ? snapshotCloseout.preservedSystems
              : snapshotResult && snapshotResult.preservedSystems
                ? snapshotResult.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      consumerContract && consumerContract.safetyFlags
        ? consumerContract.safetyFlags
        : consumerPlan && consumerPlan.safetyFlags
          ? consumerPlan.safetyFlags
          : snapshotConsumerReadiness && snapshotConsumerReadiness.safetyFlags
            ? snapshotConsumerReadiness.safetyFlags
            : snapshotCloseout && snapshotCloseout.safetyFlags
              ? snapshotCloseout.safetyFlags
              : snapshotResult && snapshotResult.safetyFlags
                ? snapshotResult.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  },
    notes: [
      "Passive closeout for the future snapshot consumer plan and contract only.",
      "No actual snapshot consumer is added in this phase.",
      "Shared-state creation, mutation, live references, renderer start, drawing, DOM work, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(
  options = {}
) {
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const consumerPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport(options)
      : null;
  const consumerContract =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport(options)
      : null;
  const consumerPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport(options)
      : null;
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotConsumerReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(options)
      : null;

  const requiredOptionKeys =
    consumerPlanCloseout && Array.isArray(consumerPlanCloseout.requiredOptionKeys)
      ? consumerPlanCloseout.requiredOptionKeys
      : consumerContract && Array.isArray(consumerContract.requiredOptionKeys)
        ? consumerContract.requiredOptionKeys
        : consumerPlan && Array.isArray(consumerPlan.requiredOptionKeys)
          ? consumerPlan.requiredOptionKeys
          : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
            ? snapshotResult.requiredOptionKeys
            : snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
              ? snapshotCloseout.requiredOptionKeys
              : snapshotConsumerReadiness &&
                  Array.isArray(snapshotConsumerReadiness.requiredOptionKeys)
                ? snapshotConsumerReadiness.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  const baseResult = {
    phase: 299,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer",
    ok: true,
    source: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer",
    snapshotConsumer: true,
    consumerMode: "snapshot-summary-consumer-only",
    requiredOptionKeys,
    sharedStateCreated: false,
    sharedStateMutated: false,
    consumedSnapshotOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    snapshotAvailable: false,
    stateName: null,
    phaseCreated: null,
    inert: false,
    manualOnly: false,
    shared: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    layerCount: 0,
    layersIsArray: false,
    blockedBehavior:
      consumerPlanCloseout && consumerPlanCloseout.blockedBehavior
        ? consumerPlanCloseout.blockedBehavior
        : consumerContract && consumerContract.blockedBehavior
          ? consumerContract.blockedBehavior
          : consumerPlan && consumerPlan.blockedBehavior
            ? consumerPlan.blockedBehavior
            : snapshotCloseout && snapshotCloseout.blockedBehavior
              ? snapshotCloseout.blockedBehavior
              : snapshotResult && snapshotResult.blockedBehavior
                ? snapshotResult.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      consumerPlanCloseout && consumerPlanCloseout.preservedSystems
        ? consumerPlanCloseout.preservedSystems
        : consumerContract && consumerContract.preservedSystems
          ? consumerContract.preservedSystems
          : consumerPlan && consumerPlan.preservedSystems
            ? consumerPlan.preservedSystems
            : snapshotCloseout && snapshotCloseout.preservedSystems
              ? snapshotCloseout.preservedSystems
              : snapshotResult && snapshotResult.preservedSystems
                ? snapshotResult.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      consumerPlanCloseout && consumerPlanCloseout.safetyFlags
        ? consumerPlanCloseout.safetyFlags
        : consumerContract && consumerContract.safetyFlags
          ? consumerContract.safetyFlags
          : consumerPlan && consumerPlan.safetyFlags
            ? consumerPlan.safetyFlags
            : snapshotCloseout && snapshotCloseout.safetyFlags
              ? snapshotCloseout.safetyFlags
              : snapshotResult && snapshotResult.safetyFlags
                ? snapshotResult.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
  };

  if (!snapshotResult || snapshotResult.blocked === true || snapshotResult.allowed !== true) {
    return {
      ...baseResult,
      blocked: true,
      allowed: false,
      failedRequirement:
        snapshotResult && snapshotResult.failedRequirement
          ? snapshotResult.failedRequirement
          : "snapshotAvailable",
      reason:
        snapshotResult && snapshotResult.reason
          ? snapshotResult.reason
          : "snapshot-unavailable",
      defaultNoOptionCheck: {
        blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
        requiredOptionKeys
      },
      manualPathStyleSummary: {
        explicitOptionsRequired: true,
        requiredOptionKeys,
        snapshotSummaryOnly: true,
        returnsLiveStateReference: false,
        returnsNestedLiveReference: false
      },
      notes: [
        "Consumes only the Phase 296 primitive snapshot summary.",
        "Does not read or return the live shared state object directly.",
        "Remains blocked unless safe snapshot access succeeds first."
      ]
    };
  }

  return {
    ...baseResult,
    blocked: false,
    allowed: true,
    snapshotAvailable: snapshotResult.sharedStateAvailable === true,
    stateName: snapshotResult.stateName || null,
    phaseCreated:
      typeof snapshotResult.phaseCreated === "number" ? snapshotResult.phaseCreated : null,
    inert: snapshotResult.inert === true,
    manualOnly: snapshotResult.manualOnly === true,
    shared: snapshotResult.shared === true,
    rendererCreated: snapshotResult.rendererCreated === true,
    rendererInitialized: snapshotResult.rendererInitialized === true,
    rendererRunning: snapshotResult.rendererRunning === true,
    mapAttached: snapshotResult.mapAttached === true,
    drawingEnabled: snapshotResult.drawingEnabled === true,
    domCreated: snapshotResult.domCreated === true,
    startupWired: snapshotResult.startupWired === true,
    layerCount:
      typeof snapshotResult.layerCount === "number" ? snapshotResult.layerCount : 0,
    layersIsArray: snapshotResult.layersIsArray === true,
    defaultNoOptionCheck: {
      blocked: !!(snapshotNoOptionResult && snapshotNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    notes: [
      "Consumes only the Phase 296 primitive snapshot summary.",
      "Does not read or return the live shared state object directly.",
      "Does not return nested live references such as the layers array."
    ]
  };
}

function getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport(
  options = {}
) {
  const consumerResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const consumerNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport()
      : null;
  const consumerPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport(options)
      : null;
  const consumerContract =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport(options)
      : null;
  const consumerPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport(options)
      : null;
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotConsumerReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(options)
      : null;

  const requiredOptionKeys =
    consumerResult && Array.isArray(consumerResult.requiredOptionKeys)
      ? consumerResult.requiredOptionKeys
      : consumerPlanCloseout && Array.isArray(consumerPlanCloseout.requiredOptionKeys)
        ? consumerPlanCloseout.requiredOptionKeys
        : consumerContract && Array.isArray(consumerContract.requiredOptionKeys)
          ? consumerContract.requiredOptionKeys
          : consumerPlan && Array.isArray(consumerPlan.requiredOptionKeys)
            ? consumerPlan.requiredOptionKeys
            : snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
              ? snapshotCloseout.requiredOptionKeys
              : snapshotConsumerReadiness &&
                  Array.isArray(snapshotConsumerReadiness.requiredOptionKeys)
                ? snapshotConsumerReadiness.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  return {
    phase: 299,
    name: "custom-25d-visual-manual-renderer-shared-state-snapshot-consumer-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    snapshotConsumerCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(consumerNoOptionResult && consumerNoOptionResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    phase299ConsumerAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
      "function",
    consumedSnapshotOnly: true,
    consumedLiveStateReference: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    snapshotConsumerStayedSafe: true,
    readyForFutureConsumerIntegrationPlan: true,
    snapshotAvailable:
      !!(consumerResult && consumerResult.allowed === true && consumerResult.snapshotAvailable),
    blockedBehavior:
      consumerResult && consumerResult.blockedBehavior
        ? consumerResult.blockedBehavior
        : consumerPlanCloseout && consumerPlanCloseout.blockedBehavior
          ? consumerPlanCloseout.blockedBehavior
          : consumerContract && consumerContract.blockedBehavior
            ? consumerContract.blockedBehavior
            : consumerPlan && consumerPlan.blockedBehavior
              ? consumerPlan.blockedBehavior
              : snapshotCloseout && snapshotCloseout.blockedBehavior
                ? snapshotCloseout.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      consumerResult && consumerResult.preservedSystems
        ? consumerResult.preservedSystems
        : consumerPlanCloseout && consumerPlanCloseout.preservedSystems
          ? consumerPlanCloseout.preservedSystems
          : consumerContract && consumerContract.preservedSystems
            ? consumerContract.preservedSystems
            : consumerPlan && consumerPlan.preservedSystems
              ? consumerPlan.preservedSystems
              : snapshotCloseout && snapshotCloseout.preservedSystems
                ? snapshotCloseout.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      consumerResult && consumerResult.safetyFlags
        ? consumerResult.safetyFlags
        : consumerPlanCloseout && consumerPlanCloseout.safetyFlags
          ? consumerPlanCloseout.safetyFlags
          : consumerContract && consumerContract.safetyFlags
            ? consumerContract.safetyFlags
            : consumerPlan && consumerPlan.safetyFlags
              ? consumerPlan.safetyFlags
              : snapshotCloseout && snapshotCloseout.safetyFlags
                ? snapshotCloseout.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  },
    notes: [
      "Passive closeout for the first safe snapshot consumer.",
      "Confirms the consumer consumes only primitive snapshot-summary data.",
      "Shared-state creation, mutation, live references, renderer start, drawing, DOM work, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport(
  options = {}
) {
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const consumerCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport(options)
      : null;
  const consumerPlan =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport(options)
      : null;
  const consumerContract =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport(options)
      : null;
  const consumerPlanCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport(options)
      : null;
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;
  const snapshotCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport(options)
      : null;
  const snapshotConsumerReadiness =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport(options)
      : null;

  const requiredOptionKeys =
    consumerCloseout && Array.isArray(consumerCloseout.requiredOptionKeys)
      ? consumerCloseout.requiredOptionKeys
      : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
        ? consumerReport.requiredOptionKeys
        : consumerPlanCloseout && Array.isArray(consumerPlanCloseout.requiredOptionKeys)
          ? consumerPlanCloseout.requiredOptionKeys
          : consumerContract && Array.isArray(consumerContract.requiredOptionKeys)
            ? consumerContract.requiredOptionKeys
            : consumerPlan && Array.isArray(consumerPlan.requiredOptionKeys)
              ? consumerPlan.requiredOptionKeys
              : snapshotCloseout && Array.isArray(snapshotCloseout.requiredOptionKeys)
                ? snapshotCloseout.requiredOptionKeys
                : snapshotConsumerReadiness &&
                    Array.isArray(snapshotConsumerReadiness.requiredOptionKeys)
                  ? snapshotConsumerReadiness.requiredOptionKeys
                  : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
                    ? snapshotResult.requiredOptionKeys
                    : [
                        "manual",
                        "developerIntent",
                        "localDevOnly",
                        "browserConsoleOnly",
                        "explicitOptionsOnly",
                        "allowManualRendererStateContainerShell",
                        "noStartupWiring",
                        "noBackendChanges",
                        "noPersistence",
                        "noAutomaticInvocation"
                      ];

  return {
    phase: 300,
    name: "custom-25d-visual-manual-renderer-snapshot-consumer-integration-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    snapshotConsumerIntegrationPlan: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          (consumerReport && consumerReport.blocked === true) ||
          (snapshotNoOptionResult && snapshotNoOptionResult.blocked === true)
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      snapshotSummaryOnly: true,
      consumesPhase299ReportOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    actualIntegrationAdded: false,
    phase299ConsumerAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
      "function",
    phase299CloseoutAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
      "function",
    plannedIntegrationMode: "snapshot-consumer-report-only",
    plannedIntegrationUsesLiveStateReference: false,
    plannedIntegrationCreatesState: false,
    plannedIntegrationMutatesState: false,
    plannedIntegrationStartsRenderer: false,
    plannedIntegrationAttachesMap: false,
    plannedIntegrationDrawsGeometry: false,
    plannedIntegrationCreatesDom: false,
    futureIntegrationReady: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    layersReturned: false,
    snapshotAvailable:
      !!(consumerReport && consumerReport.allowed === true && consumerReport.snapshotAvailable),
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      consumerCloseout && consumerCloseout.blockedBehavior
        ? consumerCloseout.blockedBehavior
        : consumerReport && consumerReport.blockedBehavior
          ? consumerReport.blockedBehavior
          : consumerPlanCloseout && consumerPlanCloseout.blockedBehavior
            ? consumerPlanCloseout.blockedBehavior
            : consumerContract && consumerContract.blockedBehavior
              ? consumerContract.blockedBehavior
              : consumerPlan && consumerPlan.blockedBehavior
                ? consumerPlan.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      consumerCloseout && consumerCloseout.preservedSystems
        ? consumerCloseout.preservedSystems
        : consumerReport && consumerReport.preservedSystems
          ? consumerReport.preservedSystems
          : consumerPlanCloseout && consumerPlanCloseout.preservedSystems
            ? consumerPlanCloseout.preservedSystems
            : consumerContract && consumerContract.preservedSystems
              ? consumerContract.preservedSystems
              : consumerPlan && consumerPlan.preservedSystems
                ? consumerPlan.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      consumerCloseout && consumerCloseout.safetyFlags
        ? consumerCloseout.safetyFlags
        : consumerReport && consumerReport.safetyFlags
          ? consumerReport.safetyFlags
          : consumerPlanCloseout && consumerPlanCloseout.safetyFlags
            ? consumerPlanCloseout.safetyFlags
            : consumerContract && consumerContract.safetyFlags
              ? consumerContract.safetyFlags
              : consumerPlan && consumerPlan.safetyFlags
                ? consumerPlan.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  },
    notes: [
      "Passive plan for a future snapshot-consumer integration path only.",
      "A future integration may consume the Phase 299 report only and not live shared state.",
      "No renderer, map, drawing, DOM, startup, or gameplay integration is added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport(
  options = {}
) {
  const integrationPlan =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport(options)
      : null;
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const consumerCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport(options)
      : null;
  const consumerContract =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport(options)
      : null;
  const snapshotResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;

  const requiredOptionKeys =
    integrationPlan && Array.isArray(integrationPlan.requiredOptionKeys)
      ? integrationPlan.requiredOptionKeys
      : consumerCloseout && Array.isArray(consumerCloseout.requiredOptionKeys)
        ? consumerCloseout.requiredOptionKeys
        : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
          ? consumerReport.requiredOptionKeys
          : consumerContract && Array.isArray(consumerContract.requiredOptionKeys)
            ? consumerContract.requiredOptionKeys
            : snapshotResult && Array.isArray(snapshotResult.requiredOptionKeys)
              ? snapshotResult.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  return {
    phase: 300,
    name: "custom-25d-visual-manual-renderer-snapshot-consumer-integration-contract",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    snapshotConsumerIntegrationContract: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          (consumerReport && consumerReport.blocked === true) ||
          (snapshotNoOptionResult && snapshotNoOptionResult.blocked === true)
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      consumesPhase299ReportOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    actualIntegrationAdded: false,
    allowedInputMode: "phase-299-snapshot-consumer-report-only",
    disallowedInputModes: [
      "live-shared-state-reference",
      "live-layers-array",
      "leaflet-layer-reference",
      "dom-node-reference",
      "renderer-instance-reference"
    ],
    allowedOutputMode: "primitive-report-only",
    integrationMayCreateState: false,
    integrationMayMutateState: false,
    integrationMayStartRenderer: false,
    integrationMayAttachMap: false,
    integrationMayDrawGeometry: false,
    integrationMayCreateDom: false,
    integrationMayWireStartup: false,
    integrationMayChangeGameplay: false,
    contractReadyForFutureIntegration: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    layersReturned: false,
    snapshotAvailable:
      !!(consumerReport && consumerReport.allowed === true && consumerReport.snapshotAvailable),
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      integrationPlan && integrationPlan.blockedBehavior
        ? integrationPlan.blockedBehavior
        : consumerCloseout && consumerCloseout.blockedBehavior
          ? consumerCloseout.blockedBehavior
          : consumerReport && consumerReport.blockedBehavior
            ? consumerReport.blockedBehavior
            : consumerContract && consumerContract.blockedBehavior
              ? consumerContract.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      integrationPlan && integrationPlan.preservedSystems
        ? integrationPlan.preservedSystems
        : consumerCloseout && consumerCloseout.preservedSystems
          ? consumerCloseout.preservedSystems
          : consumerReport && consumerReport.preservedSystems
            ? consumerReport.preservedSystems
            : consumerContract && consumerContract.preservedSystems
              ? consumerContract.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      integrationPlan && integrationPlan.safetyFlags
        ? integrationPlan.safetyFlags
        : consumerCloseout && consumerCloseout.safetyFlags
          ? consumerCloseout.safetyFlags
          : consumerReport && consumerReport.safetyFlags
            ? consumerReport.safetyFlags
            : consumerContract && consumerContract.safetyFlags
              ? consumerContract.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive contract for a future snapshot-consumer integration only.",
      "A future integration may accept the Phase 299 consumer report only and must output primitive report data only.",
      "Live references, renderer instances, DOM nodes, and map-layer references remain disallowed."
    ]
  };
}

function getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport(
  options = {}
) {
  const integrationPlan =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport(options)
      : null;
  const integrationContract =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport(options)
      : null;
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const consumerCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport(options)
      : null;
  const snapshotNoOptionResult =
    typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshot()
      : null;

  const requiredOptionKeys =
    integrationContract && Array.isArray(integrationContract.requiredOptionKeys)
      ? integrationContract.requiredOptionKeys
      : integrationPlan && Array.isArray(integrationPlan.requiredOptionKeys)
        ? integrationPlan.requiredOptionKeys
        : consumerCloseout && Array.isArray(consumerCloseout.requiredOptionKeys)
          ? consumerCloseout.requiredOptionKeys
          : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
            ? consumerReport.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  return {
    phase: 300,
    name: "custom-25d-visual-manual-renderer-snapshot-consumer-integration-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    snapshotConsumerIntegrationPlanCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked:
        !!(
          (consumerReport && consumerReport.blocked === true) ||
          (snapshotNoOptionResult && snapshotNoOptionResult.blocked === true)
        ),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      consumesPhase299ReportOnly: true,
      returnsLiveStateReference: false,
      returnsNestedLiveReference: false
    },
    phase300PlanAvailable:
      typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
      "function",
    phase300ContractAvailable:
      typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
      "function",
    actualIntegrationAdded: false,
    integrationPlanStayedPassive: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureManualIntegrationShellPlan: true,
    snapshotAvailable:
      !!(consumerReport && consumerReport.allowed === true && consumerReport.snapshotAvailable),
    blockedBehavior:
      integrationContract && integrationContract.blockedBehavior
        ? integrationContract.blockedBehavior
        : integrationPlan && integrationPlan.blockedBehavior
          ? integrationPlan.blockedBehavior
          : consumerCloseout && consumerCloseout.blockedBehavior
            ? consumerCloseout.blockedBehavior
            : consumerReport && consumerReport.blockedBehavior
              ? consumerReport.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      integrationContract && integrationContract.preservedSystems
        ? integrationContract.preservedSystems
        : integrationPlan && integrationPlan.preservedSystems
          ? integrationPlan.preservedSystems
          : consumerCloseout && consumerCloseout.preservedSystems
            ? consumerCloseout.preservedSystems
            : consumerReport && consumerReport.preservedSystems
              ? consumerReport.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      integrationContract && integrationContract.safetyFlags
        ? integrationContract.safetyFlags
        : integrationPlan && integrationPlan.safetyFlags
          ? integrationPlan.safetyFlags
          : consumerCloseout && consumerCloseout.safetyFlags
            ? consumerCloseout.safetyFlags
            : consumerReport && consumerReport.safetyFlags
              ? consumerReport.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive closeout for the future snapshot-consumer integration plan and contract only.",
      "No actual renderer integration is added in this phase.",
      "Shared-state creation, mutation, live references, renderer start, drawing, DOM work, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererIntegrationShellPlanReport(options = {}) {
  const integrationPlan =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport(options)
      : null;
  const integrationContract =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport(options)
      : null;
  const integrationCloseout =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport(options)
      : null;
  const snapshotConsumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const snapshotConsumerCloseout =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    integrationCloseout && Array.isArray(integrationCloseout.requiredOptionKeys)
      ? integrationCloseout.requiredOptionKeys
      : integrationContract && Array.isArray(integrationContract.requiredOptionKeys)
        ? integrationContract.requiredOptionKeys
        : integrationPlan && Array.isArray(integrationPlan.requiredOptionKeys)
          ? integrationPlan.requiredOptionKeys
          : snapshotConsumerCloseout &&
              Array.isArray(snapshotConsumerCloseout.requiredOptionKeys)
            ? snapshotConsumerCloseout.requiredOptionKeys
            : snapshotConsumerReport &&
                Array.isArray(snapshotConsumerReport.requiredOptionKeys)
              ? snapshotConsumerReport.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  return {
    phase: 301,
    name: "custom-25d-visual-manual-renderer-integration-shell-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    integrationShellPlan: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: true,
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      plannedShellUsesSnapshotConsumerReportOnly: true,
      plannedShellUsesLiveStateReference: false
    },
    actualIntegrationShellAdded: false,
    phase300PlanAvailable:
      typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
      "function",
    phase300ContractAvailable:
      typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
      "function",
    plannedShellMode: "manual-snapshot-consumer-integration-shell",
    plannedShellUsesSnapshotConsumerReportOnly: true,
    plannedShellUsesLiveStateReference: false,
    plannedShellCreatesState: false,
    plannedShellMutatesState: false,
    plannedShellStartsRenderer: false,
    plannedShellAttachesMap: false,
    plannedShellDrawsGeometry: false,
    plannedShellCreatesDom: false,
    futureIntegrationShellReady: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      integrationCloseout && integrationCloseout.blockedBehavior
        ? integrationCloseout.blockedBehavior
        : integrationContract && integrationContract.blockedBehavior
          ? integrationContract.blockedBehavior
          : integrationPlan && integrationPlan.blockedBehavior
            ? integrationPlan.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      integrationCloseout && integrationCloseout.preservedSystems
        ? integrationCloseout.preservedSystems
        : integrationContract && integrationContract.preservedSystems
          ? integrationContract.preservedSystems
          : integrationPlan && integrationPlan.preservedSystems
            ? integrationPlan.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      integrationCloseout && integrationCloseout.safetyFlags
        ? integrationCloseout.safetyFlags
        : integrationContract && integrationContract.safetyFlags
          ? integrationContract.safetyFlags
          : integrationPlan && integrationPlan.safetyFlags
            ? integrationPlan.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive plan for a future manual integration shell only.",
      "A future shell may consume the Phase 299 snapshot-consumer report only.",
      "No shared state, renderer, map, drawing, DOM, startup, backend, or gameplay integration is added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererIntegrationShellContractReport(options = {}) {
  const shellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const integrationPlan =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport(options)
      : null;
  const integrationContract =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport(options)
      : null;
  const integrationCloseout =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    shellPlan && Array.isArray(shellPlan.requiredOptionKeys)
      ? shellPlan.requiredOptionKeys
      : integrationCloseout && Array.isArray(integrationCloseout.requiredOptionKeys)
        ? integrationCloseout.requiredOptionKeys
        : integrationContract && Array.isArray(integrationContract.requiredOptionKeys)
          ? integrationContract.requiredOptionKeys
          : integrationPlan && Array.isArray(integrationPlan.requiredOptionKeys)
            ? integrationPlan.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  return {
    phase: 301,
    name: "custom-25d-visual-manual-renderer-integration-shell-contract",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    integrationShellContract: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: true,
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      allowedInputMode: "phase-299-snapshot-consumer-report-only",
      allowedOutputMode: "primitive-report-only"
    },
    actualIntegrationShellAdded: false,
    allowedInputMode: "phase-299-snapshot-consumer-report-only",
    allowedOutputMode: "primitive-report-only",
    shellMayCreateState: false,
    shellMayMutateState: false,
    shellMayStartRenderer: false,
    shellMayAttachMap: false,
    shellMayDrawGeometry: false,
    shellMayCreateDom: false,
    shellMayWireStartup: false,
    shellMayChangeGameplay: false,
    contractReadyForFutureManualShell: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      shellPlan && shellPlan.blockedBehavior
        ? shellPlan.blockedBehavior
        : integrationCloseout && integrationCloseout.blockedBehavior
          ? integrationCloseout.blockedBehavior
          : integrationContract && integrationContract.blockedBehavior
            ? integrationContract.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      shellPlan && shellPlan.preservedSystems
        ? shellPlan.preservedSystems
        : integrationCloseout && integrationCloseout.preservedSystems
          ? integrationCloseout.preservedSystems
          : integrationContract && integrationContract.preservedSystems
            ? integrationContract.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      shellPlan && shellPlan.safetyFlags
        ? shellPlan.safetyFlags
        : integrationCloseout && integrationCloseout.safetyFlags
          ? integrationCloseout.safetyFlags
          : integrationContract && integrationContract.safetyFlags
            ? integrationContract.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive contract for a future manual integration shell only.",
      "The future shell may accept only the Phase 299 snapshot-consumer report and output primitive report data only.",
      "State creation, mutation, renderer lifecycle work, map attachment, drawing, DOM work, startup wiring, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport(
  options = {}
) {
  const shellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const shellContract =
    typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellContractReport(options)
      : null;
  const integrationCloseout =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    shellContract && Array.isArray(shellContract.requiredOptionKeys)
      ? shellContract.requiredOptionKeys
      : shellPlan && Array.isArray(shellPlan.requiredOptionKeys)
        ? shellPlan.requiredOptionKeys
        : integrationCloseout && Array.isArray(integrationCloseout.requiredOptionKeys)
          ? integrationCloseout.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  return {
    phase: 301,
    name: "custom-25d-visual-manual-renderer-integration-shell-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    integrationShellPlanCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: true,
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      integrationShellPlanStayedPassive: true
    },
    phase301PlanAvailable:
      typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function",
    phase301ContractAvailable:
      typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
      "function",
    actualIntegrationShellAdded: false,
    integrationShellPlanStayedPassive: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureActualManualIntegrationShell: true,
    blockedBehavior:
      shellContract && shellContract.blockedBehavior
        ? shellContract.blockedBehavior
        : shellPlan && shellPlan.blockedBehavior
          ? shellPlan.blockedBehavior
          : integrationCloseout && integrationCloseout.blockedBehavior
            ? integrationCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      shellContract && shellContract.preservedSystems
        ? shellContract.preservedSystems
        : shellPlan && shellPlan.preservedSystems
          ? shellPlan.preservedSystems
          : integrationCloseout && integrationCloseout.preservedSystems
            ? integrationCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      shellContract && shellContract.safetyFlags
        ? shellContract.safetyFlags
        : shellPlan && shellPlan.safetyFlags
          ? shellPlan.safetyFlags
          : integrationCloseout && integrationCloseout.safetyFlags
            ? integrationCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive closeout for the future manual integration shell planning bundle.",
      "No actual integration shell, renderer, map attachment, drawing path, or DOM path is added here.",
      "The next implementation phase may add an actual manual integration shell only after a separate reviewed request."
    ]
  };
}

function createCustom25DVisualManualRendererIntegrationShell(options = {}) {
  const shellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const shellContract =
    typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellContractReport(options)
      : null;
  const shellPlanCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport(options)
      : null;
  const integrationCloseout =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport(options)
      : null;
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;

  const requiredOptionKeys =
    shellPlanCloseout && Array.isArray(shellPlanCloseout.requiredOptionKeys)
      ? shellPlanCloseout.requiredOptionKeys
      : shellContract && Array.isArray(shellContract.requiredOptionKeys)
        ? shellContract.requiredOptionKeys
        : shellPlan && Array.isArray(shellPlan.requiredOptionKeys)
          ? shellPlan.requiredOptionKeys
          : integrationCloseout && Array.isArray(integrationCloseout.requiredOptionKeys)
            ? integrationCloseout.requiredOptionKeys
            : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
              ? consumerReport.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell: "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  if (missingKey) {
    return {
      phase: 302,
      name: "custom-25d-visual-manual-renderer-integration-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualIntegrationShell: true,
      inert: true,
      shellCreated: false,
      consumesSnapshotConsumerReportOnly: true,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      integrationMode: "manual-snapshot-consumer-report-shell-only",
      snapshotConsumerAvailable:
        typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
        "function",
      snapshotConsumerBlocked:
        !(consumerReport && consumerReport.allowed === true && consumerReport.blocked === false),
      blockedBehavior:
        shellPlanCloseout && shellPlanCloseout.blockedBehavior
          ? shellPlanCloseout.blockedBehavior
          : shellContract && shellContract.blockedBehavior
            ? shellContract.blockedBehavior
            : shellPlan && shellPlan.blockedBehavior
              ? shellPlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
      preservedSystems:
        shellPlanCloseout && shellPlanCloseout.preservedSystems
          ? shellPlanCloseout.preservedSystems
          : shellContract && shellContract.preservedSystems
            ? shellContract.preservedSystems
            : shellPlan && shellPlan.preservedSystems
              ? shellPlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
      safetyFlags:
        shellPlanCloseout && shellPlanCloseout.safetyFlags
          ? shellPlanCloseout.safetyFlags
          : shellContract && shellContract.safetyFlags
            ? shellContract.safetyFlags
            : shellPlan && shellPlan.safetyFlags
              ? shellPlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                }
    };
  }

  const snapshotAvailable =
    !!(consumerReport && consumerReport.allowed === true && consumerReport.snapshotAvailable);

  return {
    phase: 302,
    name: "custom-25d-visual-manual-renderer-integration-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: snapshotAvailable ? null : "snapshot-consumer-report-not-available",
    failedRequirement: snapshotAvailable ? null : "snapshotConsumerAvailable",
    requiredOptionKeys,
    manualIntegrationShell: true,
    inert: true,
    shellCreated: snapshotAvailable,
    consumesSnapshotConsumerReportOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    integrationMode: "manual-snapshot-consumer-report-shell-only",
    snapshotConsumerAvailable:
      typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
      "function",
    snapshotConsumerConsumed: snapshotAvailable,
    snapshotConsumerSummary:
      snapshotAvailable && consumerReport
        ? {
            snapshotAvailable: consumerReport.snapshotAvailable === true,
            stateName:
              typeof consumerReport.stateName === "string" ? consumerReport.stateName : null,
            phaseCreated:
              typeof consumerReport.phaseCreated === "number"
                ? consumerReport.phaseCreated
                : null,
            inert: consumerReport.inert === true,
            manualOnly: consumerReport.manualOnly === true,
            shared: consumerReport.shared === true,
            rendererCreated: consumerReport.rendererCreated === true,
            rendererInitialized: consumerReport.rendererInitialized === true,
            rendererRunning: consumerReport.rendererRunning === true,
            mapAttached: consumerReport.mapAttached === true,
            drawingEnabled: consumerReport.drawingEnabled === true,
            domCreated: consumerReport.domCreated === true,
            startupWired: consumerReport.startupWired === true,
            layerCount:
              typeof consumerReport.layerCount === "number" ? consumerReport.layerCount : 0,
            layersIsArray: consumerReport.layersIsArray === true
          }
        : null,
    blockedBehavior:
      shellPlanCloseout && shellPlanCloseout.blockedBehavior
        ? shellPlanCloseout.blockedBehavior
        : shellContract && shellContract.blockedBehavior
          ? shellContract.blockedBehavior
          : shellPlan && shellPlan.blockedBehavior
            ? shellPlan.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      shellPlanCloseout && shellPlanCloseout.preservedSystems
        ? shellPlanCloseout.preservedSystems
        : shellContract && shellContract.preservedSystems
          ? shellContract.preservedSystems
          : shellPlan && shellPlan.preservedSystems
            ? shellPlan.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      shellPlanCloseout && shellPlanCloseout.safetyFlags
        ? shellPlanCloseout.safetyFlags
        : shellContract && shellContract.safetyFlags
          ? shellContract.safetyFlags
          : shellPlan && shellPlan.safetyFlags
            ? shellPlan.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              }
  };
}

function getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options = {}) {
  const shellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const shellContract =
    typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellContractReport(options)
      : null;
  const shellPlanCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport(options)
      : null;
  const integrationCloseout =
    typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport(options)
      : null;
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell(options)
      : null;
  const noOptionResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell()
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : shellPlanCloseout && Array.isArray(shellPlanCloseout.requiredOptionKeys)
        ? shellPlanCloseout.requiredOptionKeys
        : shellContract && Array.isArray(shellContract.requiredOptionKeys)
          ? shellContract.requiredOptionKeys
          : shellPlan && Array.isArray(shellPlan.requiredOptionKeys)
            ? shellPlan.requiredOptionKeys
            : integrationCloseout && Array.isArray(integrationCloseout.requiredOptionKeys)
              ? integrationCloseout.requiredOptionKeys
              : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
                ? consumerReport.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  return {
    phase: 302,
    name: "custom-25d-visual-manual-renderer-integration-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    manualIntegrationShellCloseout: true,
    phase302ShellAvailable:
      typeof createCustom25DVisualManualRendererIntegrationShell === "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionResult && noOptionResult.blocked === true),
      requiredOptionKeys
    },
    shellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    shellConsumedSnapshotConsumerReportOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureIntegrationShellReadinessBundle: true,
    blockedBehavior:
      shellResult && shellResult.blockedBehavior
        ? shellResult.blockedBehavior
        : shellPlanCloseout && shellPlanCloseout.blockedBehavior
          ? shellPlanCloseout.blockedBehavior
          : shellContract && shellContract.blockedBehavior
            ? shellContract.blockedBehavior
            : shellPlan && shellPlan.blockedBehavior
              ? shellPlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      shellResult && shellResult.preservedSystems
        ? shellResult.preservedSystems
        : shellPlanCloseout && shellPlanCloseout.preservedSystems
          ? shellPlanCloseout.preservedSystems
          : shellContract && shellContract.preservedSystems
            ? shellContract.preservedSystems
            : shellPlan && shellPlan.preservedSystems
              ? shellPlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      shellResult && shellResult.safetyFlags
        ? shellResult.safetyFlags
        : shellPlanCloseout && shellPlanCloseout.safetyFlags
          ? shellPlanCloseout.safetyFlags
          : shellContract && shellContract.safetyFlags
            ? shellContract.safetyFlags
            : shellPlan && shellPlan.safetyFlags
              ? shellPlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              }
  };
}

function getCustom25DVisualManualRendererIntegrationShellReadinessReport(options = {}) {
  const shellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options)
      : null;
  const shellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const shellContract =
    typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellContractReport(options)
      : null;
  const shellPlanCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport(options)
      : null;
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell()
      : null;

  const requiredOptionKeys =
    shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
      ? shellCloseout.requiredOptionKeys
      : shellResult && Array.isArray(shellResult.requiredOptionKeys)
        ? shellResult.requiredOptionKeys
        : shellPlanCloseout && Array.isArray(shellPlanCloseout.requiredOptionKeys)
          ? shellPlanCloseout.requiredOptionKeys
          : shellContract && Array.isArray(shellContract.requiredOptionKeys)
            ? shellContract.requiredOptionKeys
            : shellPlan && Array.isArray(shellPlan.requiredOptionKeys)
              ? shellPlan.requiredOptionKeys
              : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
                ? consumerReport.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  return {
    phase: 303,
    name: "custom-25d-visual-manual-renderer-integration-shell-readiness",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    integrationShellReadiness: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      shellConsumesSnapshotConsumerReportOnly: true,
      actualStatusReaderAdded: false
    },
    phase302ShellAvailable:
      typeof createCustom25DVisualManualRendererIntegrationShell === "function",
    phase302CloseoutAvailable:
      typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
      "function",
    shellStayedInert:
      !!(
        shellCloseout &&
        shellCloseout.shellStayedInert === true
      ),
    shellConsumesSnapshotConsumerReportOnly: true,
    actualStatusReaderAdded: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureStatusReaderPlan: true,
    blockedBehavior:
      shellCloseout && shellCloseout.blockedBehavior
        ? shellCloseout.blockedBehavior
        : shellResult && shellResult.blockedBehavior
          ? shellResult.blockedBehavior
          : shellPlanCloseout && shellPlanCloseout.blockedBehavior
            ? shellPlanCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      shellCloseout && shellCloseout.preservedSystems
        ? shellCloseout.preservedSystems
        : shellResult && shellResult.preservedSystems
          ? shellResult.preservedSystems
          : shellPlanCloseout && shellPlanCloseout.preservedSystems
            ? shellPlanCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      shellCloseout && shellCloseout.safetyFlags
        ? shellCloseout.safetyFlags
        : shellResult && shellResult.safetyFlags
          ? shellResult.safetyFlags
          : shellPlanCloseout && shellPlanCloseout.safetyFlags
            ? shellPlanCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive readiness report after the Phase 302 manual integration shell.",
      "No actual status reader is added in this phase.",
      "The shell remains inert and report-only, with no renderer, map, DOM, startup, backend, or gameplay effects."
    ]
  };
}

function getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport(
  options = {}
) {
  const readiness =
    typeof getCustom25DVisualManualRendererIntegrationShellReadinessReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellReadinessReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options)
      : null;
  const shellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const shellContract =
    typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellContractReport(options)
      : null;
  const shellPlanCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport(options)
      : null;
  const consumerReport =
    typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell()
      : null;

  const requiredOptionKeys =
    readiness && Array.isArray(readiness.requiredOptionKeys)
      ? readiness.requiredOptionKeys
      : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
        ? shellCloseout.requiredOptionKeys
        : shellPlanCloseout && Array.isArray(shellPlanCloseout.requiredOptionKeys)
          ? shellPlanCloseout.requiredOptionKeys
          : shellContract && Array.isArray(shellContract.requiredOptionKeys)
            ? shellContract.requiredOptionKeys
            : shellPlan && Array.isArray(shellPlan.requiredOptionKeys)
              ? shellPlan.requiredOptionKeys
              : consumerReport && Array.isArray(consumerReport.requiredOptionKeys)
                ? consumerReport.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  return {
    phase: 303,
    name: "custom-25d-visual-manual-renderer-integration-shell-status-reader-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    integrationShellStatusReaderPlan: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      plannedStatusReaderConsumesShellReportOnly: true,
      actualStatusReaderAdded: false
    },
    actualStatusReaderAdded: false,
    plannedStatusReaderMode: "manual-integration-shell-status-summary-only",
    plannedStatusReaderConsumesShellReportOnly: true,
    plannedStatusReaderUsesLiveStateReference: false,
    plannedStatusReaderCreatesState: false,
    plannedStatusReaderMutatesState: false,
    plannedStatusReaderStartsRenderer: false,
    plannedStatusReaderAttachesMap: false,
    plannedStatusReaderDrawsGeometry: false,
    plannedStatusReaderCreatesDom: false,
    futureStatusReaderReady: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      readiness && readiness.blockedBehavior
        ? readiness.blockedBehavior
        : shellCloseout && shellCloseout.blockedBehavior
          ? shellCloseout.blockedBehavior
          : shellPlanCloseout && shellPlanCloseout.blockedBehavior
            ? shellPlanCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              },
    preservedSystems:
      readiness && readiness.preservedSystems
        ? readiness.preservedSystems
        : shellCloseout && shellCloseout.preservedSystems
          ? shellCloseout.preservedSystems
          : shellPlanCloseout && shellPlanCloseout.preservedSystems
            ? shellPlanCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              },
    safetyFlags:
      readiness && readiness.safetyFlags
        ? readiness.safetyFlags
        : shellCloseout && shellCloseout.safetyFlags
          ? shellCloseout.safetyFlags
          : shellPlanCloseout && shellPlanCloseout.safetyFlags
            ? shellPlanCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              },
    notes: [
      "Passive plan for a future integration-shell status reader only.",
      "A future status reader may consume the Phase 302 shell report only.",
      "No renderer, map, drawing, DOM, startup, backend, or gameplay integration is added in this phase."
    ]
  };
}

function getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport(
  options = {}
) {
  const readiness =
    typeof getCustom25DVisualManualRendererIntegrationShellReadinessReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellReadinessReport(options)
      : null;
  const statusReaderPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell()
      : null;

  const requiredOptionKeys =
    statusReaderPlan && Array.isArray(statusReaderPlan.requiredOptionKeys)
      ? statusReaderPlan.requiredOptionKeys
      : readiness && Array.isArray(readiness.requiredOptionKeys)
        ? readiness.requiredOptionKeys
        : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
          ? shellCloseout.requiredOptionKeys
          : shellResult && Array.isArray(shellResult.requiredOptionKeys)
            ? shellResult.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  return {
    phase: 303,
    name: "custom-25d-visual-manual-renderer-integration-shell-status-reader-contract",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    integrationShellStatusReaderContract: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    manualPathStyleSummary: {
      explicitOptionsRequired: true,
      requiredOptionKeys,
      allowedInputMode: "phase-302-manual-integration-shell-report-only",
      allowedOutputMode: "primitive-status-summary-only"
    },
    actualStatusReaderAdded: false,
    allowedInputMode: "phase-302-manual-integration-shell-report-only",
    allowedOutputMode: "primitive-status-summary-only",
    disallowedInputModes: [
      "live-shared-state-reference",
      "live-layers-array",
      "leaflet-layer-reference",
      "dom-node-reference",
      "renderer-instance-reference"
    ],
    statusReaderMayCreateState: false,
    statusReaderMayMutateState: false,
    statusReaderMayStartRenderer: false,
    statusReaderMayAttachMap: false,
    statusReaderMayDrawGeometry: false,
    statusReaderMayCreateDom: false,
    statusReaderMayWireStartup: false,
    statusReaderMayChangeGameplay: false,
    contractReadyForFutureStatusReader: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      statusReaderPlan && statusReaderPlan.blockedBehavior
        ? statusReaderPlan.blockedBehavior
        : readiness && readiness.blockedBehavior
          ? readiness.blockedBehavior
          : shellCloseout && shellCloseout.blockedBehavior
            ? shellCloseout.blockedBehavior
            : shellResult && shellResult.blockedBehavior
              ? shellResult.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      statusReaderPlan && statusReaderPlan.preservedSystems
        ? statusReaderPlan.preservedSystems
        : readiness && readiness.preservedSystems
          ? readiness.preservedSystems
          : shellCloseout && shellCloseout.preservedSystems
            ? shellCloseout.preservedSystems
            : shellResult && shellResult.preservedSystems
              ? shellResult.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      statusReaderPlan && statusReaderPlan.safetyFlags
        ? statusReaderPlan.safetyFlags
        : readiness && readiness.safetyFlags
          ? readiness.safetyFlags
          : shellCloseout && shellCloseout.safetyFlags
            ? shellCloseout.safetyFlags
            : shellResult && shellResult.safetyFlags
              ? shellResult.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive contract for a future integration-shell status reader only.",
      "The future status reader may accept only the Phase 302 manual integration shell report.",
      "Live state references, nested live references, renderer instances, DOM nodes, and map-layer references remain disallowed."
    ]
  };
}

function getCustom25DVisualManualRendererIntegrationShellStatus(options = {}) {
  const readiness =
    typeof getCustom25DVisualManualRendererIntegrationShellReadinessReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellReadinessReport(options)
      : null;
  const statusReaderPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport(options)
      : null;
  const statusReaderContract =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell(options)
      : null;

  const requiredOptionKeys =
    statusReaderContract && Array.isArray(statusReaderContract.requiredOptionKeys)
      ? statusReaderContract.requiredOptionKeys
      : statusReaderPlan && Array.isArray(statusReaderPlan.requiredOptionKeys)
        ? statusReaderPlan.requiredOptionKeys
        : readiness && Array.isArray(readiness.requiredOptionKeys)
          ? readiness.requiredOptionKeys
          : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
            ? shellCloseout.requiredOptionKeys
            : shellResult && Array.isArray(shellResult.requiredOptionKeys)
              ? shellResult.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const blocked = !shellResult || shellResult.blocked === true;

  return {
    phase: 304,
    name: "custom-25d-visual-manual-renderer-integration-shell-status",
    ok: true,
    allowed: blocked ? false : true,
    blocked,
    reason: shellResult ? shellResult.reason || null : "integration-shell-unavailable",
    failedRequirement: shellResult ? shellResult.failedRequirement || null : "shellAvailable",
    requiredOptionKeys,
    integrationShellStatus: true,
    statusOnly: true,
    consumedShellReportOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    shellAvailable:
      typeof createCustom25DVisualManualRendererIntegrationShell === "function",
    shellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    statusMode: "manual-integration-shell-status-summary-only",
    shellCreated: !!(shellResult && shellResult.shellCreated === true),
    snapshotConsumerConsumed:
      !!(shellResult && shellResult.snapshotConsumerConsumed === true),
    blockedBehavior:
      statusReaderContract && statusReaderContract.blockedBehavior
        ? statusReaderContract.blockedBehavior
        : statusReaderPlan && statusReaderPlan.blockedBehavior
          ? statusReaderPlan.blockedBehavior
          : readiness && readiness.blockedBehavior
            ? readiness.blockedBehavior
            : shellCloseout && shellCloseout.blockedBehavior
              ? shellCloseout.blockedBehavior
              : shellResult && shellResult.blockedBehavior
                ? shellResult.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      statusReaderContract && statusReaderContract.preservedSystems
        ? statusReaderContract.preservedSystems
        : statusReaderPlan && statusReaderPlan.preservedSystems
          ? statusReaderPlan.preservedSystems
          : readiness && readiness.preservedSystems
            ? readiness.preservedSystems
            : shellCloseout && shellCloseout.preservedSystems
              ? shellCloseout.preservedSystems
              : shellResult && shellResult.preservedSystems
                ? shellResult.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      statusReaderContract && statusReaderContract.safetyFlags
        ? statusReaderContract.safetyFlags
        : statusReaderPlan && statusReaderPlan.safetyFlags
          ? statusReaderPlan.safetyFlags
          : readiness && readiness.safetyFlags
            ? readiness.safetyFlags
            : shellCloseout && shellCloseout.safetyFlags
              ? shellCloseout.safetyFlags
              : shellResult && shellResult.safetyFlags
                ? shellResult.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers:
                      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData:
                      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData:
                      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
  };
}

function getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport(
  options = {}
) {
  const readiness =
    typeof getCustom25DVisualManualRendererIntegrationShellReadinessReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellReadinessReport(options)
      : null;
  const statusReaderPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport(options)
      : null;
  const statusReaderContract =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options)
      : null;
  const statusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus(options)
      : null;
  const noOptionStatusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus()
      : null;

  const requiredOptionKeys =
    statusResult && Array.isArray(statusResult.requiredOptionKeys)
      ? statusResult.requiredOptionKeys
      : statusReaderContract && Array.isArray(statusReaderContract.requiredOptionKeys)
        ? statusReaderContract.requiredOptionKeys
        : statusReaderPlan && Array.isArray(statusReaderPlan.requiredOptionKeys)
          ? statusReaderPlan.requiredOptionKeys
          : readiness && Array.isArray(readiness.requiredOptionKeys)
            ? readiness.requiredOptionKeys
            : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
              ? shellCloseout.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  return {
    phase: 304,
    name: "custom-25d-visual-manual-renderer-integration-shell-status-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    integrationShellStatusCloseout: true,
    phase304StatusAvailable:
      typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionStatusResult && noOptionStatusResult.blocked === true),
      requiredOptionKeys
    },
    statusReaderStayedSafe:
      !!(
        statusResult &&
        statusResult.consumedShellReportOnly === true &&
        statusResult.consumedLiveStateReference === false &&
        statusResult.returnedLiveStateReference === false &&
        statusResult.returnedNestedLiveReference === false &&
        statusResult.sharedStateCreated === false &&
        statusResult.sharedStateMutated === false &&
        statusResult.rendererCreated === false &&
        statusResult.rendererInitialized === false &&
        statusResult.rendererRunning === false &&
        statusResult.mapAttached === false &&
        statusResult.drawingEnabled === false &&
        statusResult.domCreated === false &&
        statusResult.startupWired === false &&
        statusResult.gameplayChanged === false
      ),
    consumedShellReportOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererLifecyclePlanBundle: true,
    blockedBehavior:
      statusResult && statusResult.blockedBehavior
        ? statusResult.blockedBehavior
        : statusReaderContract && statusReaderContract.blockedBehavior
          ? statusReaderContract.blockedBehavior
          : statusReaderPlan && statusReaderPlan.blockedBehavior
            ? statusReaderPlan.blockedBehavior
            : readiness && readiness.blockedBehavior
              ? readiness.blockedBehavior
              : shellCloseout && shellCloseout.blockedBehavior
                ? shellCloseout.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      statusResult && statusResult.preservedSystems
        ? statusResult.preservedSystems
        : statusReaderContract && statusReaderContract.preservedSystems
          ? statusReaderContract.preservedSystems
          : statusReaderPlan && statusReaderPlan.preservedSystems
            ? statusReaderPlan.preservedSystems
            : readiness && readiness.preservedSystems
              ? readiness.preservedSystems
              : shellCloseout && shellCloseout.preservedSystems
                ? shellCloseout.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      statusResult && statusResult.safetyFlags
        ? statusResult.safetyFlags
        : statusReaderContract && statusReaderContract.safetyFlags
          ? statusReaderContract.safetyFlags
          : statusReaderPlan && statusReaderPlan.safetyFlags
            ? statusReaderPlan.safetyFlags
            : readiness && readiness.safetyFlags
              ? readiness.safetyFlags
              : shellCloseout && shellCloseout.safetyFlags
                ? shellCloseout.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers:
                      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData:
                      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData:
                      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
  };
}

function getCustom25DVisualManualRendererLifecyclePlanReport(options = {}) {
  const statusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus(options)
      : null;
  const statusCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell(options)
      : null;
  const shellCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport(options)
      : null;

  const requiredOptionKeys =
    statusCloseout && Array.isArray(statusCloseout.requiredOptionKeys)
      ? statusCloseout.requiredOptionKeys
      : statusResult && Array.isArray(statusResult.requiredOptionKeys)
        ? statusResult.requiredOptionKeys
        : shellCloseout && Array.isArray(shellCloseout.requiredOptionKeys)
          ? shellCloseout.requiredOptionKeys
          : shellResult && Array.isArray(shellResult.requiredOptionKeys)
            ? shellResult.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const noOptionStatusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus()
      : null;

  return {
    phase: 305,
    name: "custom-25d-visual-manual-renderer-lifecycle-plan",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    rendererLifecyclePlan: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(noOptionStatusResult && noOptionStatusResult.blocked === true),
      requiredOptionKeys
    },
    actualLifecycleShellAdded: false,
    phase304StatusAvailable:
      typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function",
    plannedLifecycleMode: "manual-inert-renderer-lifecycle-shell",
    plannedRendererCreated: false,
    plannedRendererInitialized: false,
    plannedRendererRunning: false,
    plannedMapAttached: false,
    plannedDrawingEnabled: false,
    plannedDomCreated: false,
    plannedStartupWired: false,
    futureLifecycleShellReady: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      statusCloseout && statusCloseout.blockedBehavior
        ? statusCloseout.blockedBehavior
        : statusResult && statusResult.blockedBehavior
          ? statusResult.blockedBehavior
          : shellCloseout && shellCloseout.blockedBehavior
            ? shellCloseout.blockedBehavior
            : shellResult && shellResult.blockedBehavior
              ? shellResult.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      statusCloseout && statusCloseout.preservedSystems
        ? statusCloseout.preservedSystems
        : statusResult && statusResult.preservedSystems
          ? statusResult.preservedSystems
          : shellCloseout && shellCloseout.preservedSystems
            ? shellCloseout.preservedSystems
            : shellResult && shellResult.preservedSystems
              ? shellResult.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      statusCloseout && statusCloseout.safetyFlags
        ? statusCloseout.safetyFlags
        : statusResult && statusResult.safetyFlags
          ? statusResult.safetyFlags
          : shellCloseout && shellCloseout.safetyFlags
            ? shellCloseout.safetyFlags
            : shellResult && shellResult.safetyFlags
              ? shellResult.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive lifecycle planning helper only.",
      "No renderer object, lifecycle shell, loop, map attachment, drawing path, or DOM path is added in this phase.",
      "The future lifecycle shell remains planning-only and inert by default."
    ]
  };
}

function getCustom25DVisualManualRendererLifecycleContractReport(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const statusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus(options)
      : null;
  const statusCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererIntegrationShell === "function"
      ? createCustom25DVisualManualRendererIntegrationShell(options)
      : null;

  const requiredOptionKeys =
    lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
      ? lifecyclePlan.requiredOptionKeys
      : statusCloseout && Array.isArray(statusCloseout.requiredOptionKeys)
        ? statusCloseout.requiredOptionKeys
        : statusResult && Array.isArray(statusResult.requiredOptionKeys)
          ? statusResult.requiredOptionKeys
          : shellResult && Array.isArray(shellResult.requiredOptionKeys)
            ? shellResult.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const noOptionStatusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus()
      : null;

  return {
    phase: 305,
    name: "custom-25d-visual-manual-renderer-lifecycle-contract",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    rendererLifecycleContract: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(noOptionStatusResult && noOptionStatusResult.blocked === true),
      requiredOptionKeys
    },
    actualLifecycleShellAdded: false,
    allowedInputMode: "phase-304-integration-shell-status-summary-only",
    allowedOutputMode: "primitive-lifecycle-status-only",
    lifecycleMayCreateRenderer: false,
    lifecycleMayInitializeRenderer: false,
    lifecycleMayStartRenderer: false,
    lifecycleMayAttachMap: false,
    lifecycleMayDrawGeometry: false,
    lifecycleMayCreateDom: false,
    lifecycleMayWireStartup: false,
    lifecycleMayChangeGameplay: false,
    contractReadyForFutureLifecycleShell: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    blockedBehavior:
      lifecyclePlan && lifecyclePlan.blockedBehavior
        ? lifecyclePlan.blockedBehavior
        : statusCloseout && statusCloseout.blockedBehavior
          ? statusCloseout.blockedBehavior
          : statusResult && statusResult.blockedBehavior
            ? statusResult.blockedBehavior
            : shellResult && shellResult.blockedBehavior
              ? shellResult.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      lifecyclePlan && lifecyclePlan.preservedSystems
        ? lifecyclePlan.preservedSystems
        : statusCloseout && statusCloseout.preservedSystems
          ? statusCloseout.preservedSystems
          : statusResult && statusResult.preservedSystems
            ? statusResult.preservedSystems
            : shellResult && shellResult.preservedSystems
              ? shellResult.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      lifecyclePlan && lifecyclePlan.safetyFlags
        ? lifecyclePlan.safetyFlags
        : statusCloseout && statusCloseout.safetyFlags
          ? statusCloseout.safetyFlags
          : statusResult && statusResult.safetyFlags
            ? statusResult.safetyFlags
            : shellResult && shellResult.safetyFlags
              ? shellResult.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive lifecycle contract helper only.",
      "The future lifecycle shell may consume only the Phase 304 status summary and return primitive lifecycle status only.",
      "Renderer creation, initialization, start/run, map attachment, drawing, DOM work, startup wiring, and gameplay changes remain blocked."
    ]
  };
}

function getCustom25DVisualManualRendererLifecyclePlanCloseoutReport(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const lifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const statusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus(options)
      : null;
  const statusCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport(options)
      : null;

  const requiredOptionKeys =
    lifecycleContract && Array.isArray(lifecycleContract.requiredOptionKeys)
      ? lifecycleContract.requiredOptionKeys
      : lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
        ? lifecyclePlan.requiredOptionKeys
        : statusCloseout && Array.isArray(statusCloseout.requiredOptionKeys)
          ? statusCloseout.requiredOptionKeys
          : statusResult && Array.isArray(statusResult.requiredOptionKeys)
            ? statusResult.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const noOptionStatusResult =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus()
      : null;

  return {
    phase: 305,
    name: "custom-25d-visual-manual-renderer-lifecycle-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    rendererLifecyclePlanCloseout: true,
    allowed: false,
    blocked: true,
    defaultDecision: "blocked",
    noOptionCallAllowed: false,
    requiredOptionKeys,
    defaultNoOptionCheck: {
      blocked: !!(noOptionStatusResult && noOptionStatusResult.blocked === true),
      requiredOptionKeys
    },
    phase305PlanAvailable:
      typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function",
    phase305ContractAvailable:
      typeof getCustom25DVisualManualRendererLifecycleContractReport === "function",
    actualLifecycleShellAdded: false,
    lifecyclePlanStayedPassive: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureManualLifecycleShell: true,
    blockedBehavior:
      lifecycleContract && lifecycleContract.blockedBehavior
        ? lifecycleContract.blockedBehavior
        : lifecyclePlan && lifecyclePlan.blockedBehavior
          ? lifecyclePlan.blockedBehavior
          : statusCloseout && statusCloseout.blockedBehavior
            ? statusCloseout.blockedBehavior
            : statusResult && statusResult.blockedBehavior
              ? statusResult.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      lifecycleContract && lifecycleContract.preservedSystems
        ? lifecycleContract.preservedSystems
        : lifecyclePlan && lifecyclePlan.preservedSystems
          ? lifecyclePlan.preservedSystems
          : statusCloseout && statusCloseout.preservedSystems
            ? statusCloseout.preservedSystems
            : statusResult && statusResult.preservedSystems
              ? statusResult.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      lifecycleContract && lifecycleContract.safetyFlags
        ? lifecycleContract.safetyFlags
        : lifecyclePlan && lifecyclePlan.safetyFlags
          ? lifecyclePlan.safetyFlags
          : statusCloseout && statusCloseout.safetyFlags
            ? statusCloseout.safetyFlags
            : statusResult && statusResult.safetyFlags
              ? statusResult.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                },
    notes: [
      "Passive closeout for the lifecycle plan and contract bundle only.",
      "No actual lifecycle shell, renderer object, renderer state initialization, start/run loop, map attachment, drawing path, or DOM path is added here.",
      "A future implementation phase may add an actual manual lifecycle shell only after a separate reviewed request."
    ]
  };
}

function createCustom25DVisualManualRendererLifecycleShell(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const lifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const lifecyclePlanCloseout =
    typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport(options)
      : null;
  const integrationStatus =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus(options)
      : null;
  const integrationStatusCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport(options)
      : null;

  const requiredOptionKeys =
    lifecyclePlanCloseout && Array.isArray(lifecyclePlanCloseout.requiredOptionKeys)
      ? lifecyclePlanCloseout.requiredOptionKeys
      : lifecycleContract && Array.isArray(lifecycleContract.requiredOptionKeys)
        ? lifecycleContract.requiredOptionKeys
        : lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
          ? lifecyclePlan.requiredOptionKeys
          : integrationStatusCloseout &&
              Array.isArray(integrationStatusCloseout.requiredOptionKeys)
            ? integrationStatusCloseout.requiredOptionKeys
            : integrationStatus && Array.isArray(integrationStatus.requiredOptionKeys)
              ? integrationStatus.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell: "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  if (missingKey) {
    return {
      phase: 306,
      name: "custom-25d-visual-manual-renderer-lifecycle-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualLifecycleShell: true,
      inert: true,
      lifecycleShellCreated: false,
      consumedIntegrationShellStatusOnly: true,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      lifecycleMode: "manual-inert-renderer-lifecycle-shell-only",
      blockedBehavior:
        lifecyclePlanCloseout && lifecyclePlanCloseout.blockedBehavior
          ? lifecyclePlanCloseout.blockedBehavior
          : lifecycleContract && lifecycleContract.blockedBehavior
            ? lifecycleContract.blockedBehavior
            : lifecyclePlan && lifecyclePlan.blockedBehavior
              ? lifecyclePlan.blockedBehavior
              : integrationStatusCloseout && integrationStatusCloseout.blockedBehavior
                ? integrationStatusCloseout.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
      preservedSystems:
        lifecyclePlanCloseout && lifecyclePlanCloseout.preservedSystems
          ? lifecyclePlanCloseout.preservedSystems
          : lifecycleContract && lifecycleContract.preservedSystems
            ? lifecycleContract.preservedSystems
            : lifecyclePlan && lifecyclePlan.preservedSystems
              ? lifecyclePlan.preservedSystems
              : integrationStatusCloseout && integrationStatusCloseout.preservedSystems
                ? integrationStatusCloseout.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
      safetyFlags:
        lifecyclePlanCloseout && lifecyclePlanCloseout.safetyFlags
          ? lifecyclePlanCloseout.safetyFlags
          : lifecycleContract && lifecycleContract.safetyFlags
            ? lifecycleContract.safetyFlags
            : lifecyclePlan && lifecyclePlan.safetyFlags
              ? lifecyclePlan.safetyFlags
              : integrationStatusCloseout && integrationStatusCloseout.safetyFlags
                ? integrationStatusCloseout.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
    };
  }

  const integrationStatusAvailable =
    !!(
      integrationStatus &&
      integrationStatus.allowed === true &&
      integrationStatus.blocked === false
    );

  return {
    phase: 306,
    name: "custom-25d-visual-manual-renderer-lifecycle-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: integrationStatusAvailable ? null : "integration-shell-status-not-available",
    failedRequirement: integrationStatusAvailable ? null : "integrationShellStatusAvailable",
    requiredOptionKeys,
    manualLifecycleShell: true,
    inert: true,
    lifecycleShellCreated: integrationStatusAvailable,
    consumedIntegrationShellStatusOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    lifecycleMode: "manual-inert-renderer-lifecycle-shell-only",
    integrationShellStatusSummary:
      integrationStatusAvailable && integrationStatus
        ? {
            shellAvailable: integrationStatus.shellAvailable === true,
            shellStayedInert: integrationStatus.shellStayedInert === true,
            shellCreated: integrationStatus.shellCreated === true,
            snapshotConsumerConsumed:
              integrationStatus.snapshotConsumerConsumed === true,
            rendererCreated: integrationStatus.rendererCreated === true,
            rendererInitialized: integrationStatus.rendererInitialized === true,
            rendererRunning: integrationStatus.rendererRunning === true,
            mapAttached: integrationStatus.mapAttached === true,
            drawingEnabled: integrationStatus.drawingEnabled === true,
            domCreated: integrationStatus.domCreated === true,
            startupWired: integrationStatus.startupWired === true,
            gameplayChanged: integrationStatus.gameplayChanged === true
          }
        : null,
    blockedBehavior:
      lifecyclePlanCloseout && lifecyclePlanCloseout.blockedBehavior
        ? lifecyclePlanCloseout.blockedBehavior
        : lifecycleContract && lifecycleContract.blockedBehavior
          ? lifecycleContract.blockedBehavior
          : lifecyclePlan && lifecyclePlan.blockedBehavior
            ? lifecyclePlan.blockedBehavior
            : integrationStatusCloseout && integrationStatusCloseout.blockedBehavior
              ? integrationStatusCloseout.blockedBehavior
              : integrationStatus && integrationStatus.blockedBehavior
                ? integrationStatus.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      lifecyclePlanCloseout && lifecyclePlanCloseout.preservedSystems
        ? lifecyclePlanCloseout.preservedSystems
        : lifecycleContract && lifecycleContract.preservedSystems
          ? lifecycleContract.preservedSystems
          : lifecyclePlan && lifecyclePlan.preservedSystems
            ? lifecyclePlan.preservedSystems
            : integrationStatusCloseout && integrationStatusCloseout.preservedSystems
              ? integrationStatusCloseout.preservedSystems
              : integrationStatus && integrationStatus.preservedSystems
                ? integrationStatus.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      lifecyclePlanCloseout && lifecyclePlanCloseout.safetyFlags
        ? lifecyclePlanCloseout.safetyFlags
        : lifecycleContract && lifecycleContract.safetyFlags
          ? lifecycleContract.safetyFlags
          : lifecyclePlan && lifecyclePlan.safetyFlags
            ? lifecyclePlan.safetyFlags
            : integrationStatusCloseout && integrationStatusCloseout.safetyFlags
              ? integrationStatusCloseout.safetyFlags
              : integrationStatus && integrationStatus.safetyFlags
                ? integrationStatus.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
  };
}

function getCustom25DVisualManualRendererLifecycleShellCloseoutReport(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const lifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const lifecyclePlanCloseout =
    typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport(options)
      : null;
  const integrationStatus =
    typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatus(options)
      : null;
  const integrationStatusCloseout =
    typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : lifecyclePlanCloseout && Array.isArray(lifecyclePlanCloseout.requiredOptionKeys)
        ? lifecyclePlanCloseout.requiredOptionKeys
        : lifecycleContract && Array.isArray(lifecycleContract.requiredOptionKeys)
          ? lifecycleContract.requiredOptionKeys
          : lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
            ? lifecyclePlan.requiredOptionKeys
            : integrationStatusCloseout &&
                Array.isArray(integrationStatusCloseout.requiredOptionKeys)
              ? integrationStatusCloseout.requiredOptionKeys
              : integrationStatus && Array.isArray(integrationStatus.requiredOptionKeys)
                ? integrationStatus.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  return {
    phase: 306,
    name: "custom-25d-visual-manual-renderer-lifecycle-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    lifecycleShellCloseout: true,
    phase306ShellAvailable:
      typeof createCustom25DVisualManualRendererLifecycleShell === "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    lifecycleShellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.consumedIntegrationShellStatusOnly === true &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.rendererLoopStarted === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    consumedIntegrationShellStatusOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureLifecycleStatusBundle: true,
    blockedBehavior:
      shellResult && shellResult.blockedBehavior
        ? shellResult.blockedBehavior
        : lifecyclePlanCloseout && lifecyclePlanCloseout.blockedBehavior
          ? lifecyclePlanCloseout.blockedBehavior
          : lifecycleContract && lifecycleContract.blockedBehavior
            ? lifecycleContract.blockedBehavior
            : lifecyclePlan && lifecyclePlan.blockedBehavior
              ? lifecyclePlan.blockedBehavior
              : integrationStatusCloseout && integrationStatusCloseout.blockedBehavior
                ? integrationStatusCloseout.blockedBehavior
                : integrationStatus && integrationStatus.blockedBehavior
                  ? integrationStatus.blockedBehavior
                  : {
                      rendererCreation: true,
                      rendererInitialization: true,
                      mapAttachment: true,
                      drawing: true,
                      domCreation: true,
                      startupWiring: true,
                      automaticInvocation: true,
                      gameplayChanges: true,
                      pinChanges: true,
                      uiChanges: true,
                      backendChanges: true,
                      storageWrites: true,
                      networkAccess: true
                    },
    preservedSystems:
      shellResult && shellResult.preservedSystems
        ? shellResult.preservedSystems
        : lifecyclePlanCloseout && lifecyclePlanCloseout.preservedSystems
          ? lifecyclePlanCloseout.preservedSystems
          : lifecycleContract && lifecycleContract.preservedSystems
            ? lifecycleContract.preservedSystems
            : lifecyclePlan && lifecyclePlan.preservedSystems
              ? lifecyclePlan.preservedSystems
              : integrationStatusCloseout && integrationStatusCloseout.preservedSystems
                ? integrationStatusCloseout.preservedSystems
                : integrationStatus && integrationStatus.preservedSystems
                  ? integrationStatus.preservedSystems
                  : {
                      existingLeafletMapBehavior: true,
                      osmBehavior: true,
                      gameplay: true,
                      pins: true,
                      playerMarker: true,
                      captureRadius: true,
                      ui: true,
                      backend: true,
                      storage: true,
                      network: true
                    },
    safetyFlags:
      shellResult && shellResult.safetyFlags
        ? shellResult.safetyFlags
        : lifecyclePlanCloseout && lifecyclePlanCloseout.safetyFlags
          ? lifecyclePlanCloseout.safetyFlags
          : lifecycleContract && lifecycleContract.safetyFlags
            ? lifecycleContract.safetyFlags
            : lifecyclePlan && lifecyclePlan.safetyFlags
              ? lifecyclePlan.safetyFlags
              : integrationStatusCloseout && integrationStatusCloseout.safetyFlags
                ? integrationStatusCloseout.safetyFlags
                : integrationStatus && integrationStatus.safetyFlags
                  ? integrationStatus.safetyFlags
                  : {
                      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                    }
  };
}

function getCustom25DVisualManualRendererLifecycleShellStatusReport(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const lifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const lifecyclePlanCloseout =
    typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport(options)
      : null;
  const lifecycleShellCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport(options)
      : null;
  const lifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell(options)
      : null;
  const noOptionLifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    lifecycleShellResult && Array.isArray(lifecycleShellResult.requiredOptionKeys)
      ? lifecycleShellResult.requiredOptionKeys
      : lifecycleShellCloseout &&
            Array.isArray(lifecycleShellCloseout.requiredOptionKeys)
        ? lifecycleShellCloseout.requiredOptionKeys
        : lifecyclePlanCloseout && Array.isArray(lifecyclePlanCloseout.requiredOptionKeys)
          ? lifecyclePlanCloseout.requiredOptionKeys
          : lifecycleContract && Array.isArray(lifecycleContract.requiredOptionKeys)
            ? lifecycleContract.requiredOptionKeys
            : lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
              ? lifecyclePlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    lifecycleShellResult && lifecycleShellResult.blockedBehavior
      ? lifecycleShellResult.blockedBehavior
      : lifecycleShellCloseout && lifecycleShellCloseout.blockedBehavior
        ? lifecycleShellCloseout.blockedBehavior
        : lifecyclePlanCloseout && lifecyclePlanCloseout.blockedBehavior
          ? lifecyclePlanCloseout.blockedBehavior
          : lifecycleContract && lifecycleContract.blockedBehavior
            ? lifecycleContract.blockedBehavior
            : lifecyclePlan && lifecyclePlan.blockedBehavior
              ? lifecyclePlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    lifecycleShellResult && lifecycleShellResult.preservedSystems
      ? lifecycleShellResult.preservedSystems
      : lifecycleShellCloseout && lifecycleShellCloseout.preservedSystems
        ? lifecycleShellCloseout.preservedSystems
        : lifecyclePlanCloseout && lifecyclePlanCloseout.preservedSystems
          ? lifecyclePlanCloseout.preservedSystems
          : lifecycleContract && lifecycleContract.preservedSystems
            ? lifecycleContract.preservedSystems
            : lifecyclePlan && lifecyclePlan.preservedSystems
              ? lifecyclePlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    lifecycleShellResult && lifecycleShellResult.safetyFlags
      ? lifecycleShellResult.safetyFlags
      : lifecycleShellCloseout && lifecycleShellCloseout.safetyFlags
        ? lifecycleShellCloseout.safetyFlags
        : lifecyclePlanCloseout && lifecyclePlanCloseout.safetyFlags
          ? lifecyclePlanCloseout.safetyFlags
          : lifecycleContract && lifecycleContract.safetyFlags
            ? lifecycleContract.safetyFlags
            : lifecyclePlan && lifecyclePlan.safetyFlags
              ? lifecyclePlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 307,
      name: "custom-25d-visual-manual-renderer-lifecycle-shell-status",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      statusOnly: true,
      lifecycleShellStatus: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase306ShellAvailable:
        typeof createCustom25DVisualManualRendererLifecycleShell === "function",
      phase306CloseoutAvailable:
        typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionLifecycleShellResult &&
            noOptionLifecycleShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      consumedLifecycleShellReportOnly: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      lifecycleShellStayedInert: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 307,
    name: "custom-25d-visual-manual-renderer-lifecycle-shell-status",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    statusOnly: true,
    lifecycleShellStatus: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase306ShellAvailable:
      typeof createCustom25DVisualManualRendererLifecycleShell === "function",
    phase306CloseoutAvailable:
      typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionLifecycleShellResult && noOptionLifecycleShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    consumedLifecycleShellReportOnly: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    lifecycleShellStayedInert: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererLifecycleShellReadinessReport(options = {}) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const lifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const lifecyclePlanCloseout =
    typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport(options)
      : null;
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport(options)
      : null;
  const noOptionLifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
      ? lifecycleShellStatus.requiredOptionKeys
      : lifecycleShellCloseout &&
            Array.isArray(lifecycleShellCloseout.requiredOptionKeys)
        ? lifecycleShellCloseout.requiredOptionKeys
        : lifecyclePlanCloseout && Array.isArray(lifecyclePlanCloseout.requiredOptionKeys)
          ? lifecyclePlanCloseout.requiredOptionKeys
          : lifecycleContract && Array.isArray(lifecycleContract.requiredOptionKeys)
            ? lifecycleContract.requiredOptionKeys
            : lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
              ? lifecyclePlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
      ? lifecycleShellStatus.blockedBehavior
      : lifecycleShellCloseout && lifecycleShellCloseout.blockedBehavior
        ? lifecycleShellCloseout.blockedBehavior
        : lifecyclePlanCloseout && lifecyclePlanCloseout.blockedBehavior
          ? lifecyclePlanCloseout.blockedBehavior
          : lifecycleContract && lifecycleContract.blockedBehavior
            ? lifecycleContract.blockedBehavior
            : lifecyclePlan && lifecyclePlan.blockedBehavior
              ? lifecyclePlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    lifecycleShellStatus && lifecycleShellStatus.preservedSystems
      ? lifecycleShellStatus.preservedSystems
      : lifecycleShellCloseout && lifecycleShellCloseout.preservedSystems
        ? lifecycleShellCloseout.preservedSystems
        : lifecyclePlanCloseout && lifecyclePlanCloseout.preservedSystems
          ? lifecyclePlanCloseout.preservedSystems
          : lifecycleContract && lifecycleContract.preservedSystems
            ? lifecycleContract.preservedSystems
            : lifecyclePlan && lifecyclePlan.preservedSystems
              ? lifecyclePlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    lifecycleShellStatus && lifecycleShellStatus.safetyFlags
      ? lifecycleShellStatus.safetyFlags
      : lifecycleShellCloseout && lifecycleShellCloseout.safetyFlags
        ? lifecycleShellCloseout.safetyFlags
        : lifecyclePlanCloseout && lifecyclePlanCloseout.safetyFlags
          ? lifecyclePlanCloseout.safetyFlags
          : lifecycleContract && lifecycleContract.safetyFlags
            ? lifecycleContract.safetyFlags
            : lifecyclePlan && lifecyclePlan.safetyFlags
              ? lifecyclePlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 307,
      name: "custom-25d-visual-manual-renderer-lifecycle-shell-readiness",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      lifecycleShellReadiness: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase306ShellAvailable:
        typeof createCustom25DVisualManualRendererLifecycleShell === "function",
      phase307StatusAvailable:
        typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionLifecycleShellResult &&
            noOptionLifecycleShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      readyForFutureLifecycleStagePlan: true,
      futureLifecycleStageShouldRemainManual: true,
      futureLifecycleStageMayCreateRenderer: false,
      futureLifecycleStageMayInitializeRenderer: false,
      futureLifecycleStageMayStartRenderer: false,
      futureLifecycleStageMayAttachMap: false,
      futureLifecycleStageMayDrawGeometry: false,
      futureLifecycleStageMayCreateDom: false,
      futureLifecycleStageMayWireStartup: false,
      futureLifecycleStageMayChangeGameplay: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 307,
    name: "custom-25d-visual-manual-renderer-lifecycle-shell-readiness",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    lifecycleShellReadiness: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase306ShellAvailable:
      typeof createCustom25DVisualManualRendererLifecycleShell === "function",
    phase307StatusAvailable:
      typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionLifecycleShellResult && noOptionLifecycleShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    readyForFutureLifecycleStagePlan: true,
    futureLifecycleStageShouldRemainManual: true,
    futureLifecycleStageMayCreateRenderer: false,
    futureLifecycleStageMayInitializeRenderer: false,
    futureLifecycleStageMayStartRenderer: false,
    futureLifecycleStageMayAttachMap: false,
    futureLifecycleStageMayDrawGeometry: false,
    futureLifecycleStageMayCreateDom: false,
    futureLifecycleStageMayWireStartup: false,
    futureLifecycleStageMayChangeGameplay: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport(
  options = {}
) {
  const lifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const lifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const lifecyclePlanCloseout =
    typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport(options)
      : null;
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellReadiness =
    typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellReadinessReport(options)
      : null;
  const lifecycleShellCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport(options)
      : null;
  const noOptionLifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
      ? lifecycleShellStatus.requiredOptionKeys
      : lifecycleShellReadiness &&
            Array.isArray(lifecycleShellReadiness.requiredOptionKeys)
        ? lifecycleShellReadiness.requiredOptionKeys
        : lifecycleShellCloseout &&
              Array.isArray(lifecycleShellCloseout.requiredOptionKeys)
          ? lifecycleShellCloseout.requiredOptionKeys
          : lifecyclePlanCloseout &&
                Array.isArray(lifecyclePlanCloseout.requiredOptionKeys)
            ? lifecyclePlanCloseout.requiredOptionKeys
            : lifecycleContract && Array.isArray(lifecycleContract.requiredOptionKeys)
              ? lifecycleContract.requiredOptionKeys
              : lifecyclePlan && Array.isArray(lifecyclePlan.requiredOptionKeys)
                ? lifecyclePlan.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
      ? lifecycleShellStatus.blockedBehavior
      : lifecycleShellReadiness && lifecycleShellReadiness.blockedBehavior
        ? lifecycleShellReadiness.blockedBehavior
        : lifecycleShellCloseout && lifecycleShellCloseout.blockedBehavior
          ? lifecycleShellCloseout.blockedBehavior
          : lifecyclePlanCloseout && lifecyclePlanCloseout.blockedBehavior
            ? lifecyclePlanCloseout.blockedBehavior
            : lifecycleContract && lifecycleContract.blockedBehavior
              ? lifecycleContract.blockedBehavior
              : lifecyclePlan && lifecyclePlan.blockedBehavior
                ? lifecyclePlan.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    rendererRun: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  };

  const preservedSystems =
    lifecycleShellStatus && lifecycleShellStatus.preservedSystems
      ? lifecycleShellStatus.preservedSystems
      : lifecycleShellReadiness && lifecycleShellReadiness.preservedSystems
        ? lifecycleShellReadiness.preservedSystems
        : lifecycleShellCloseout && lifecycleShellCloseout.preservedSystems
          ? lifecycleShellCloseout.preservedSystems
          : lifecyclePlanCloseout && lifecyclePlanCloseout.preservedSystems
            ? lifecyclePlanCloseout.preservedSystems
            : lifecycleContract && lifecycleContract.preservedSystems
              ? lifecycleContract.preservedSystems
              : lifecyclePlan && lifecyclePlan.preservedSystems
                ? lifecyclePlan.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  };

  const safetyFlags =
    lifecycleShellStatus && lifecycleShellStatus.safetyFlags
      ? lifecycleShellStatus.safetyFlags
      : lifecycleShellReadiness && lifecycleShellReadiness.safetyFlags
        ? lifecycleShellReadiness.safetyFlags
        : lifecycleShellCloseout && lifecycleShellCloseout.safetyFlags
          ? lifecycleShellCloseout.safetyFlags
          : lifecyclePlanCloseout && lifecyclePlanCloseout.safetyFlags
            ? lifecyclePlanCloseout.safetyFlags
            : lifecycleContract && lifecycleContract.safetyFlags
              ? lifecycleContract.safetyFlags
              : lifecyclePlan && lifecyclePlan.safetyFlags
                ? lifecyclePlan.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  };

  if (missingKey) {
    return {
      phase: 307,
      name: "custom-25d-visual-manual-renderer-lifecycle-shell-status-closeout",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      lifecycleShellStatusCloseout: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase307StatusAvailable:
        typeof getCustom25DVisualManualRendererLifecycleShellStatusReport ===
        "function",
      phase307ReadinessAvailable:
        typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionLifecycleShellResult &&
            noOptionLifecycleShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      lifecycleStatusBundleStayedPassive: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      readyForFutureRendererObjectPlanBundle: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 307,
    name: "custom-25d-visual-manual-renderer-lifecycle-shell-status-closeout",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    lifecycleShellStatusCloseout: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase307StatusAvailable:
      typeof getCustom25DVisualManualRendererLifecycleShellStatusReport ===
      "function",
    phase307ReadinessAvailable:
      typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionLifecycleShellResult && noOptionLifecycleShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    lifecycleStatusBundleStayedPassive: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererObjectPlanBundle: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectPlanReport(options = {}) {
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellReadiness =
    typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellReadinessReport(options)
      : null;
  const lifecycleShellStatusCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport(options)
      : null;
  const lifecycleShellCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport(options)
      : null;
  const lifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell(options)
      : null;
  const noOptionLifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
      ? lifecycleShellStatus.requiredOptionKeys
      : lifecycleShellReadiness &&
            Array.isArray(lifecycleShellReadiness.requiredOptionKeys)
        ? lifecycleShellReadiness.requiredOptionKeys
        : lifecycleShellStatusCloseout &&
              Array.isArray(lifecycleShellStatusCloseout.requiredOptionKeys)
          ? lifecycleShellStatusCloseout.requiredOptionKeys
          : lifecycleShellCloseout &&
                Array.isArray(lifecycleShellCloseout.requiredOptionKeys)
            ? lifecycleShellCloseout.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
      ? lifecycleShellStatus.blockedBehavior
      : lifecycleShellReadiness && lifecycleShellReadiness.blockedBehavior
        ? lifecycleShellReadiness.blockedBehavior
        : lifecycleShellStatusCloseout &&
              lifecycleShellStatusCloseout.blockedBehavior
          ? lifecycleShellStatusCloseout.blockedBehavior
          : lifecycleShellCloseout && lifecycleShellCloseout.blockedBehavior
            ? lifecycleShellCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    lifecycleShellStatus && lifecycleShellStatus.preservedSystems
      ? lifecycleShellStatus.preservedSystems
      : lifecycleShellReadiness && lifecycleShellReadiness.preservedSystems
        ? lifecycleShellReadiness.preservedSystems
        : lifecycleShellStatusCloseout &&
              lifecycleShellStatusCloseout.preservedSystems
          ? lifecycleShellStatusCloseout.preservedSystems
          : lifecycleShellCloseout && lifecycleShellCloseout.preservedSystems
            ? lifecycleShellCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    lifecycleShellStatus && lifecycleShellStatus.safetyFlags
      ? lifecycleShellStatus.safetyFlags
      : lifecycleShellReadiness && lifecycleShellReadiness.safetyFlags
        ? lifecycleShellReadiness.safetyFlags
        : lifecycleShellStatusCloseout && lifecycleShellStatusCloseout.safetyFlags
          ? lifecycleShellStatusCloseout.safetyFlags
          : lifecycleShellCloseout && lifecycleShellCloseout.safetyFlags
            ? lifecycleShellCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  if (missingKey) {
    return {
      phase: 308,
      name: "custom-25d-visual-manual-renderer-object-plan",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      planOnly: true,
      rendererObjectPlan: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualRendererObjectAdded: false,
      phase307StatusAvailable:
        typeof getCustom25DVisualManualRendererLifecycleShellStatusReport ===
        "function",
      phase307ReadinessAvailable:
        typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionLifecycleShellResult &&
            noOptionLifecycleShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      plannedObjectMode: "manual-inert-renderer-object-shell",
      plannedRendererObjectCreated: false,
      plannedRendererInitialized: false,
      plannedRendererRunning: false,
      plannedRendererLoopStarted: false,
      plannedMapAttached: false,
      plannedDrawingEnabled: false,
      plannedDomCreated: false,
      plannedStartupWired: false,
      futureRendererObjectShellReady: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 308,
    name: "custom-25d-visual-manual-renderer-object-plan",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    rendererObjectPlan: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualRendererObjectAdded: false,
    phase307StatusAvailable:
      typeof getCustom25DVisualManualRendererLifecycleShellStatusReport ===
      "function",
    phase307ReadinessAvailable:
      typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionLifecycleShellResult && noOptionLifecycleShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    plannedObjectMode: "manual-inert-renderer-object-shell",
    plannedRendererObjectCreated: false,
    plannedRendererInitialized: false,
    plannedRendererRunning: false,
    plannedRendererLoopStarted: false,
    plannedMapAttached: false,
    plannedDrawingEnabled: false,
    plannedDomCreated: false,
    plannedStartupWired: false,
    futureRendererObjectShellReady: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectContractReport(options = {}) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellReadiness =
    typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellReadinessReport(options)
      : null;
  const lifecycleShellStatusCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport(options)
      : null;
  const noOptionLifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
      ? objectPlan.requiredOptionKeys
      : lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
        ? lifecycleShellStatus.requiredOptionKeys
        : lifecycleShellReadiness &&
              Array.isArray(lifecycleShellReadiness.requiredOptionKeys)
          ? lifecycleShellReadiness.requiredOptionKeys
          : lifecycleShellStatusCloseout &&
                Array.isArray(lifecycleShellStatusCloseout.requiredOptionKeys)
            ? lifecycleShellStatusCloseout.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectPlan && objectPlan.blockedBehavior
      ? objectPlan.blockedBehavior
      : lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
        ? lifecycleShellStatus.blockedBehavior
        : lifecycleShellReadiness && lifecycleShellReadiness.blockedBehavior
          ? lifecycleShellReadiness.blockedBehavior
          : lifecycleShellStatusCloseout &&
                lifecycleShellStatusCloseout.blockedBehavior
            ? lifecycleShellStatusCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    objectPlan && objectPlan.preservedSystems
      ? objectPlan.preservedSystems
      : lifecycleShellStatus && lifecycleShellStatus.preservedSystems
        ? lifecycleShellStatus.preservedSystems
        : lifecycleShellReadiness && lifecycleShellReadiness.preservedSystems
          ? lifecycleShellReadiness.preservedSystems
          : lifecycleShellStatusCloseout &&
                lifecycleShellStatusCloseout.preservedSystems
            ? lifecycleShellStatusCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    objectPlan && objectPlan.safetyFlags
      ? objectPlan.safetyFlags
      : lifecycleShellStatus && lifecycleShellStatus.safetyFlags
        ? lifecycleShellStatus.safetyFlags
        : lifecycleShellReadiness && lifecycleShellReadiness.safetyFlags
          ? lifecycleShellReadiness.safetyFlags
          : lifecycleShellStatusCloseout && lifecycleShellStatusCloseout.safetyFlags
            ? lifecycleShellStatusCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  if (missingKey) {
    return {
      phase: 308,
      name: "custom-25d-visual-manual-renderer-object-contract",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      contractOnly: true,
      rendererObjectContract: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualRendererObjectAdded: false,
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionLifecycleShellResult &&
            noOptionLifecycleShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      allowedInputMode: "phase-307-lifecycle-shell-status-summary-only",
      allowedOutputMode: "primitive-renderer-object-status-only",
      rendererObjectMayCreateSharedState: false,
      rendererObjectMayMutateSharedState: false,
      rendererObjectMayReturnLiveReference: false,
      rendererObjectMayCreateRendererInstance: false,
      rendererObjectMayInitializeRenderer: false,
      rendererObjectMayStartRenderer: false,
      rendererObjectMayAttachMap: false,
      rendererObjectMayDrawGeometry: false,
      rendererObjectMayCreateDom: false,
      rendererObjectMayWireStartup: false,
      rendererObjectMayChangeGameplay: false,
      contractReadyForFutureRendererObjectShell: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 308,
    name: "custom-25d-visual-manual-renderer-object-contract",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    rendererObjectContract: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualRendererObjectAdded: false,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionLifecycleShellResult && noOptionLifecycleShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    allowedInputMode: "phase-307-lifecycle-shell-status-summary-only",
    allowedOutputMode: "primitive-renderer-object-status-only",
    rendererObjectMayCreateSharedState: false,
    rendererObjectMayMutateSharedState: false,
    rendererObjectMayReturnLiveReference: false,
    rendererObjectMayCreateRendererInstance: false,
    rendererObjectMayInitializeRenderer: false,
    rendererObjectMayStartRenderer: false,
    rendererObjectMayAttachMap: false,
    rendererObjectMayDrawGeometry: false,
    rendererObjectMayCreateDom: false,
    rendererObjectMayWireStartup: false,
    rendererObjectMayChangeGameplay: false,
    contractReadyForFutureRendererObjectShell: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectPlanCloseoutReport(options = {}) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const objectContract =
    typeof getCustom25DVisualManualRendererObjectContractReport === "function"
      ? getCustom25DVisualManualRendererObjectContractReport(options)
      : null;
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellReadiness =
    typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellReadinessReport(options)
      : null;
  const lifecycleShellStatusCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport(options)
      : null;
  const noOptionLifecycleShellResult =
    typeof createCustom25DVisualManualRendererLifecycleShell === "function"
      ? createCustom25DVisualManualRendererLifecycleShell()
      : null;

  const requiredOptionKeys =
    objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
      ? objectPlan.requiredOptionKeys
      : objectContract && Array.isArray(objectContract.requiredOptionKeys)
        ? objectContract.requiredOptionKeys
        : lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
          ? lifecycleShellStatus.requiredOptionKeys
          : lifecycleShellReadiness &&
                Array.isArray(lifecycleShellReadiness.requiredOptionKeys)
            ? lifecycleShellReadiness.requiredOptionKeys
            : lifecycleShellStatusCloseout &&
                  Array.isArray(lifecycleShellStatusCloseout.requiredOptionKeys)
              ? lifecycleShellStatusCloseout.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectPlan && objectPlan.blockedBehavior
      ? objectPlan.blockedBehavior
      : objectContract && objectContract.blockedBehavior
        ? objectContract.blockedBehavior
        : lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
          ? lifecycleShellStatus.blockedBehavior
          : lifecycleShellReadiness && lifecycleShellReadiness.blockedBehavior
            ? lifecycleShellReadiness.blockedBehavior
            : lifecycleShellStatusCloseout &&
                  lifecycleShellStatusCloseout.blockedBehavior
              ? lifecycleShellStatusCloseout.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectPlan && objectPlan.preservedSystems
      ? objectPlan.preservedSystems
      : objectContract && objectContract.preservedSystems
        ? objectContract.preservedSystems
        : lifecycleShellStatus && lifecycleShellStatus.preservedSystems
          ? lifecycleShellStatus.preservedSystems
          : lifecycleShellReadiness && lifecycleShellReadiness.preservedSystems
            ? lifecycleShellReadiness.preservedSystems
            : lifecycleShellStatusCloseout &&
                  lifecycleShellStatusCloseout.preservedSystems
              ? lifecycleShellStatusCloseout.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectPlan && objectPlan.safetyFlags
      ? objectPlan.safetyFlags
      : objectContract && objectContract.safetyFlags
        ? objectContract.safetyFlags
        : lifecycleShellStatus && lifecycleShellStatus.safetyFlags
          ? lifecycleShellStatus.safetyFlags
          : lifecycleShellReadiness && lifecycleShellReadiness.safetyFlags
            ? lifecycleShellReadiness.safetyFlags
            : lifecycleShellStatusCloseout && lifecycleShellStatusCloseout.safetyFlags
              ? lifecycleShellStatusCloseout.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 308,
      name: "custom-25d-visual-manual-renderer-object-plan-closeout",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      rendererObjectPlanCloseout: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase308PlanAvailable:
        typeof getCustom25DVisualManualRendererObjectPlanReport === "function",
      phase308ContractAvailable:
        typeof getCustom25DVisualManualRendererObjectContractReport === "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionLifecycleShellResult &&
            noOptionLifecycleShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      actualRendererObjectAdded: false,
      rendererObjectPlanStayedPassive: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      rendererObjectCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      readyForFutureManualRendererObjectShell: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 308,
    name: "custom-25d-visual-manual-renderer-object-plan-closeout",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    rendererObjectPlanCloseout: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase308PlanAvailable:
      typeof getCustom25DVisualManualRendererObjectPlanReport === "function",
    phase308ContractAvailable:
      typeof getCustom25DVisualManualRendererObjectContractReport === "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionLifecycleShellResult && noOptionLifecycleShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    actualRendererObjectAdded: false,
    rendererObjectPlanStayedPassive: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererObjectCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureManualRendererObjectShell: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererObjectShell(options = {}) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const objectContract =
    typeof getCustom25DVisualManualRendererObjectContractReport === "function"
      ? getCustom25DVisualManualRendererObjectContractReport(options)
      : null;
  const objectPlanCloseout =
    typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanCloseoutReport(options)
      : null;
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellReadiness =
    typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellReadinessReport(options)
      : null;

  const requiredOptionKeys =
    objectPlanCloseout && Array.isArray(objectPlanCloseout.requiredOptionKeys)
      ? objectPlanCloseout.requiredOptionKeys
      : objectContract && Array.isArray(objectContract.requiredOptionKeys)
        ? objectContract.requiredOptionKeys
        : objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
          ? objectPlan.requiredOptionKeys
          : lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
            ? lifecycleShellStatus.requiredOptionKeys
            : lifecycleShellReadiness &&
                  Array.isArray(lifecycleShellReadiness.requiredOptionKeys)
              ? lifecycleShellReadiness.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectPlanCloseout && objectPlanCloseout.blockedBehavior
      ? objectPlanCloseout.blockedBehavior
      : objectContract && objectContract.blockedBehavior
        ? objectContract.blockedBehavior
        : objectPlan && objectPlan.blockedBehavior
          ? objectPlan.blockedBehavior
          : lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
            ? lifecycleShellStatus.blockedBehavior
            : lifecycleShellReadiness && lifecycleShellReadiness.blockedBehavior
              ? lifecycleShellReadiness.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectPlanCloseout && objectPlanCloseout.preservedSystems
      ? objectPlanCloseout.preservedSystems
      : objectContract && objectContract.preservedSystems
        ? objectContract.preservedSystems
        : objectPlan && objectPlan.preservedSystems
          ? objectPlan.preservedSystems
          : lifecycleShellStatus && lifecycleShellStatus.preservedSystems
            ? lifecycleShellStatus.preservedSystems
            : lifecycleShellReadiness && lifecycleShellReadiness.preservedSystems
              ? lifecycleShellReadiness.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectPlanCloseout && objectPlanCloseout.safetyFlags
      ? objectPlanCloseout.safetyFlags
      : objectContract && objectContract.safetyFlags
        ? objectContract.safetyFlags
        : objectPlan && objectPlan.safetyFlags
          ? objectPlan.safetyFlags
          : lifecycleShellStatus && lifecycleShellStatus.safetyFlags
            ? lifecycleShellStatus.safetyFlags
            : lifecycleShellReadiness && lifecycleShellReadiness.safetyFlags
              ? lifecycleShellReadiness.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 309,
      name: "custom-25d-visual-manual-renderer-object-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualRendererObjectShell: true,
      inert: true,
      objectShellCreated: false,
      consumedLifecycleShellStatusOnly: true,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      rendererObjectShellCreated: false,
      liveRendererInstanceCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      objectShellMode: "manual-inert-renderer-object-shell-only",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  const lifecycleStatusAvailable =
    !!(
      lifecycleShellStatus &&
      lifecycleShellStatus.allowed === true &&
      lifecycleShellStatus.blocked === false
    );

  return {
    phase: 309,
    name: "custom-25d-visual-manual-renderer-object-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: lifecycleStatusAvailable ? null : "lifecycle-shell-status-not-available",
    failedRequirement: lifecycleStatusAvailable ? null : "lifecycleShellStatusAvailable",
    requiredOptionKeys,
    manualRendererObjectShell: true,
    inert: true,
    objectShellCreated: lifecycleStatusAvailable,
    consumedLifecycleShellStatusOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    rendererObjectShellCreated: lifecycleStatusAvailable,
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    objectShellMode: "manual-inert-renderer-object-shell-only",
    lifecycleShellStatusSummary:
      lifecycleStatusAvailable && lifecycleShellStatus
        ? {
            phase306ShellAvailable:
              lifecycleShellStatus.phase306ShellAvailable === true,
            phase306CloseoutAvailable:
              lifecycleShellStatus.phase306CloseoutAvailable === true,
            lifecycleShellStayedInert:
              lifecycleShellStatus.lifecycleShellStayedInert === true,
            rendererCreated: lifecycleShellStatus.rendererCreated === true,
            rendererInitialized:
              lifecycleShellStatus.rendererInitialized === true,
            rendererRunning: lifecycleShellStatus.rendererRunning === true,
            rendererLoopStarted:
              lifecycleShellStatus.rendererLoopStarted === true,
            mapAttached: lifecycleShellStatus.mapAttached === true,
            drawingEnabled: lifecycleShellStatus.drawingEnabled === true,
            domCreated: lifecycleShellStatus.domCreated === true,
            startupWired: lifecycleShellStatus.startupWired === true,
            gameplayChanged: lifecycleShellStatus.gameplayChanged === true
          }
        : null,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShellCloseoutReport(options = {}) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const objectContract =
    typeof getCustom25DVisualManualRendererObjectContractReport === "function"
      ? getCustom25DVisualManualRendererObjectContractReport(options)
      : null;
  const objectPlanCloseout =
    typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanCloseoutReport(options)
      : null;
  const lifecycleShellStatus =
    typeof getCustom25DVisualManualRendererLifecycleShellStatusReport === "function"
      ? getCustom25DVisualManualRendererLifecycleShellStatusReport(options)
      : null;
  const lifecycleShellReadiness =
    typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellReadinessReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : objectPlanCloseout && Array.isArray(objectPlanCloseout.requiredOptionKeys)
        ? objectPlanCloseout.requiredOptionKeys
        : objectContract && Array.isArray(objectContract.requiredOptionKeys)
          ? objectContract.requiredOptionKeys
          : objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
            ? objectPlan.requiredOptionKeys
            : lifecycleShellStatus && Array.isArray(lifecycleShellStatus.requiredOptionKeys)
              ? lifecycleShellStatus.requiredOptionKeys
              : lifecycleShellReadiness &&
                    Array.isArray(lifecycleShellReadiness.requiredOptionKeys)
                ? lifecycleShellReadiness.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  return {
    phase: 309,
    name: "custom-25d-visual-manual-renderer-object-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    rendererObjectShellCloseout: true,
    phase309ObjectShellAvailable:
      typeof createCustom25DVisualManualRendererObjectShell === "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    objectShellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.consumedLifecycleShellStatusOnly === true &&
        shellResult.liveRendererInstanceCreated === false &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.rendererLoopStarted === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    consumedLifecycleShellStatusOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererObjectStatusBundle: true,
    blockedBehavior:
      shellResult && shellResult.blockedBehavior
        ? shellResult.blockedBehavior
        : objectPlanCloseout && objectPlanCloseout.blockedBehavior
          ? objectPlanCloseout.blockedBehavior
          : objectContract && objectContract.blockedBehavior
            ? objectContract.blockedBehavior
            : objectPlan && objectPlan.blockedBehavior
              ? objectPlan.blockedBehavior
              : lifecycleShellStatus && lifecycleShellStatus.blockedBehavior
                ? lifecycleShellStatus.blockedBehavior
                : lifecycleShellReadiness && lifecycleShellReadiness.blockedBehavior
                  ? lifecycleShellReadiness.blockedBehavior
                  : {
                      rendererCreation: true,
                      rendererInitialization: true,
                      rendererRun: true,
                      mapAttachment: true,
                      drawing: true,
                      domCreation: true,
                      startupWiring: true,
                      automaticInvocation: true,
                      gameplayChanges: true,
                      pinChanges: true,
                      uiChanges: true,
                      backendChanges: true,
                      storageWrites: true,
                      networkAccess: true
                    },
    preservedSystems:
      shellResult && shellResult.preservedSystems
        ? shellResult.preservedSystems
        : objectPlanCloseout && objectPlanCloseout.preservedSystems
          ? objectPlanCloseout.preservedSystems
          : objectContract && objectContract.preservedSystems
            ? objectContract.preservedSystems
            : objectPlan && objectPlan.preservedSystems
              ? objectPlan.preservedSystems
              : lifecycleShellStatus && lifecycleShellStatus.preservedSystems
                ? lifecycleShellStatus.preservedSystems
                : lifecycleShellReadiness && lifecycleShellReadiness.preservedSystems
                  ? lifecycleShellReadiness.preservedSystems
                  : {
                      existingLeafletMapBehavior: true,
                      osmBehavior: true,
                      gameplay: true,
                      pins: true,
                      playerMarker: true,
                      captureRadius: true,
                      ui: true,
                      backend: true,
                      storage: true,
                      network: true
                    },
    safetyFlags:
      shellResult && shellResult.safetyFlags
        ? shellResult.safetyFlags
        : objectPlanCloseout && objectPlanCloseout.safetyFlags
          ? objectPlanCloseout.safetyFlags
          : objectContract && objectContract.safetyFlags
            ? objectContract.safetyFlags
            : objectPlan && objectPlan.safetyFlags
              ? objectPlan.safetyFlags
              : lifecycleShellStatus && lifecycleShellStatus.safetyFlags
                ? lifecycleShellStatus.safetyFlags
                : lifecycleShellReadiness && lifecycleShellReadiness.safetyFlags
                  ? lifecycleShellReadiness.safetyFlags
                  : {
                      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                    }
  };
}

function getCustom25DVisualManualRendererObjectShellStatusReport(options = {}) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const objectContract =
    typeof getCustom25DVisualManualRendererObjectContractReport === "function"
      ? getCustom25DVisualManualRendererObjectContractReport(options)
      : null;
  const objectPlanCloseout =
    typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanCloseoutReport(options)
      : null;
  const objectShellCloseout =
    typeof getCustom25DVisualManualRendererObjectShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectShellCloseoutReport(options)
      : null;
  const objectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell(options)
      : null;
  const noOptionObjectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    objectShellResult && Array.isArray(objectShellResult.requiredOptionKeys)
      ? objectShellResult.requiredOptionKeys
      : objectShellCloseout && Array.isArray(objectShellCloseout.requiredOptionKeys)
        ? objectShellCloseout.requiredOptionKeys
        : objectPlanCloseout && Array.isArray(objectPlanCloseout.requiredOptionKeys)
          ? objectPlanCloseout.requiredOptionKeys
          : objectContract && Array.isArray(objectContract.requiredOptionKeys)
            ? objectContract.requiredOptionKeys
            : objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
              ? objectPlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShellResult && objectShellResult.blockedBehavior
      ? objectShellResult.blockedBehavior
      : objectShellCloseout && objectShellCloseout.blockedBehavior
        ? objectShellCloseout.blockedBehavior
        : objectPlanCloseout && objectPlanCloseout.blockedBehavior
          ? objectPlanCloseout.blockedBehavior
          : objectContract && objectContract.blockedBehavior
            ? objectContract.blockedBehavior
            : objectPlan && objectPlan.blockedBehavior
              ? objectPlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShellResult && objectShellResult.preservedSystems
      ? objectShellResult.preservedSystems
      : objectShellCloseout && objectShellCloseout.preservedSystems
        ? objectShellCloseout.preservedSystems
        : objectPlanCloseout && objectPlanCloseout.preservedSystems
          ? objectPlanCloseout.preservedSystems
          : objectContract && objectContract.preservedSystems
            ? objectContract.preservedSystems
            : objectPlan && objectPlan.preservedSystems
              ? objectPlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShellResult && objectShellResult.safetyFlags
      ? objectShellResult.safetyFlags
      : objectShellCloseout && objectShellCloseout.safetyFlags
        ? objectShellCloseout.safetyFlags
        : objectPlanCloseout && objectPlanCloseout.safetyFlags
          ? objectPlanCloseout.safetyFlags
          : objectContract && objectContract.safetyFlags
            ? objectContract.safetyFlags
            : objectPlan && objectPlan.safetyFlags
              ? objectPlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 310,
      name: "custom-25d-visual-manual-renderer-object-shell-status",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      statusOnly: true,
      rendererObjectShellStatus: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase309ObjectShellAvailable:
        typeof createCustom25DVisualManualRendererObjectShell === "function",
      phase309CloseoutAvailable:
        typeof getCustom25DVisualManualRendererObjectShellCloseoutReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShellResult &&
            noOptionObjectShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      consumedObjectShellReportOnly: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      rendererObjectShellCreated: false,
      liveRendererInstanceCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      objectShellStayedInert: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 310,
    name: "custom-25d-visual-manual-renderer-object-shell-status",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    statusOnly: true,
    rendererObjectShellStatus: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase309ObjectShellAvailable:
      typeof createCustom25DVisualManualRendererObjectShell === "function",
    phase309CloseoutAvailable:
      typeof getCustom25DVisualManualRendererObjectShellCloseoutReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShellResult && noOptionObjectShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    consumedObjectShellReportOnly: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    rendererObjectShellCreated:
      !!(
        objectShellResult &&
        objectShellResult.rendererObjectShellCreated === true
      ),
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    objectShellStayedInert: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShellReadinessReport(options = {}) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const objectContract =
    typeof getCustom25DVisualManualRendererObjectContractReport === "function"
      ? getCustom25DVisualManualRendererObjectContractReport(options)
      : null;
  const objectPlanCloseout =
    typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanCloseoutReport(options)
      : null;
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;
  const objectShellCloseout =
    typeof getCustom25DVisualManualRendererObjectShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectShellCloseoutReport(options)
      : null;
  const noOptionObjectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
      ? objectShellStatus.requiredOptionKeys
      : objectShellCloseout && Array.isArray(objectShellCloseout.requiredOptionKeys)
        ? objectShellCloseout.requiredOptionKeys
        : objectPlanCloseout && Array.isArray(objectPlanCloseout.requiredOptionKeys)
          ? objectPlanCloseout.requiredOptionKeys
          : objectContract && Array.isArray(objectContract.requiredOptionKeys)
            ? objectContract.requiredOptionKeys
            : objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
              ? objectPlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShellStatus && objectShellStatus.blockedBehavior
      ? objectShellStatus.blockedBehavior
      : objectShellCloseout && objectShellCloseout.blockedBehavior
        ? objectShellCloseout.blockedBehavior
        : objectPlanCloseout && objectPlanCloseout.blockedBehavior
          ? objectPlanCloseout.blockedBehavior
          : objectContract && objectContract.blockedBehavior
            ? objectContract.blockedBehavior
            : objectPlan && objectPlan.blockedBehavior
              ? objectPlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShellStatus && objectShellStatus.preservedSystems
      ? objectShellStatus.preservedSystems
      : objectShellCloseout && objectShellCloseout.preservedSystems
        ? objectShellCloseout.preservedSystems
        : objectPlanCloseout && objectPlanCloseout.preservedSystems
          ? objectPlanCloseout.preservedSystems
          : objectContract && objectContract.preservedSystems
            ? objectContract.preservedSystems
            : objectPlan && objectPlan.preservedSystems
              ? objectPlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShellStatus && objectShellStatus.safetyFlags
      ? objectShellStatus.safetyFlags
      : objectShellCloseout && objectShellCloseout.safetyFlags
        ? objectShellCloseout.safetyFlags
        : objectPlanCloseout && objectPlanCloseout.safetyFlags
          ? objectPlanCloseout.safetyFlags
          : objectContract && objectContract.safetyFlags
            ? objectContract.safetyFlags
            : objectPlan && objectPlan.safetyFlags
              ? objectPlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 310,
      name: "custom-25d-visual-manual-renderer-object-shell-readiness",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      rendererObjectShellReadiness: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase309ObjectShellAvailable:
        typeof createCustom25DVisualManualRendererObjectShell === "function",
      phase310StatusAvailable:
        typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShellResult &&
            noOptionObjectShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      readyForFutureRendererObjectShapePlan: true,
      futureRendererObjectShapeShouldRemainManual: true,
      futureRendererObjectShapeMayCreateLiveInstance: false,
      futureRendererObjectShapeMayInitializeRenderer: false,
      futureRendererObjectShapeMayStartRenderer: false,
      futureRendererObjectShapeMayAttachMap: false,
      futureRendererObjectShapeMayDrawGeometry: false,
      futureRendererObjectShapeMayCreateDom: false,
      futureRendererObjectShapeMayWireStartup: false,
      futureRendererObjectShapeMayChangeGameplay: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 310,
    name: "custom-25d-visual-manual-renderer-object-shell-readiness",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    rendererObjectShellReadiness: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase309ObjectShellAvailable:
      typeof createCustom25DVisualManualRendererObjectShell === "function",
    phase310StatusAvailable:
      typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShellResult && noOptionObjectShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    readyForFutureRendererObjectShapePlan: true,
    futureRendererObjectShapeShouldRemainManual: true,
    futureRendererObjectShapeMayCreateLiveInstance: false,
    futureRendererObjectShapeMayInitializeRenderer: false,
    futureRendererObjectShapeMayStartRenderer: false,
    futureRendererObjectShapeMayAttachMap: false,
    futureRendererObjectShapeMayDrawGeometry: false,
    futureRendererObjectShapeMayCreateDom: false,
    futureRendererObjectShapeMayWireStartup: false,
    futureRendererObjectShapeMayChangeGameplay: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShellStatusCloseoutReport(
  options = {}
) {
  const objectPlan =
    typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanReport(options)
      : null;
  const objectContract =
    typeof getCustom25DVisualManualRendererObjectContractReport === "function"
      ? getCustom25DVisualManualRendererObjectContractReport(options)
      : null;
  const objectPlanCloseout =
    typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectPlanCloseoutReport(options)
      : null;
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;
  const objectShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellReadinessReport(options)
      : null;
  const objectShellCloseout =
    typeof getCustom25DVisualManualRendererObjectShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectShellCloseoutReport(options)
      : null;
  const noOptionObjectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
      ? objectShellStatus.requiredOptionKeys
      : objectShellReadiness &&
            Array.isArray(objectShellReadiness.requiredOptionKeys)
        ? objectShellReadiness.requiredOptionKeys
        : objectShellCloseout && Array.isArray(objectShellCloseout.requiredOptionKeys)
          ? objectShellCloseout.requiredOptionKeys
          : objectPlanCloseout && Array.isArray(objectPlanCloseout.requiredOptionKeys)
            ? objectPlanCloseout.requiredOptionKeys
            : objectContract && Array.isArray(objectContract.requiredOptionKeys)
              ? objectContract.requiredOptionKeys
              : objectPlan && Array.isArray(objectPlan.requiredOptionKeys)
                ? objectPlan.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShellStatus && objectShellStatus.blockedBehavior
      ? objectShellStatus.blockedBehavior
      : objectShellReadiness && objectShellReadiness.blockedBehavior
        ? objectShellReadiness.blockedBehavior
        : objectShellCloseout && objectShellCloseout.blockedBehavior
          ? objectShellCloseout.blockedBehavior
          : objectPlanCloseout && objectPlanCloseout.blockedBehavior
            ? objectPlanCloseout.blockedBehavior
            : objectContract && objectContract.blockedBehavior
              ? objectContract.blockedBehavior
              : objectPlan && objectPlan.blockedBehavior
                ? objectPlan.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    rendererRun: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  };

  const preservedSystems =
    objectShellStatus && objectShellStatus.preservedSystems
      ? objectShellStatus.preservedSystems
      : objectShellReadiness && objectShellReadiness.preservedSystems
        ? objectShellReadiness.preservedSystems
        : objectShellCloseout && objectShellCloseout.preservedSystems
          ? objectShellCloseout.preservedSystems
          : objectPlanCloseout && objectPlanCloseout.preservedSystems
            ? objectPlanCloseout.preservedSystems
            : objectContract && objectContract.preservedSystems
              ? objectContract.preservedSystems
              : objectPlan && objectPlan.preservedSystems
                ? objectPlan.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  };

  const safetyFlags =
    objectShellStatus && objectShellStatus.safetyFlags
      ? objectShellStatus.safetyFlags
      : objectShellReadiness && objectShellReadiness.safetyFlags
        ? objectShellReadiness.safetyFlags
        : objectShellCloseout && objectShellCloseout.safetyFlags
          ? objectShellCloseout.safetyFlags
          : objectPlanCloseout && objectPlanCloseout.safetyFlags
            ? objectPlanCloseout.safetyFlags
            : objectContract && objectContract.safetyFlags
              ? objectContract.safetyFlags
              : objectPlan && objectPlan.safetyFlags
                ? objectPlan.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  };

  if (missingKey) {
    return {
      phase: 310,
      name: "custom-25d-visual-manual-renderer-object-shell-status-closeout",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      rendererObjectShellStatusCloseout: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase310StatusAvailable:
        typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
        "function",
      phase310ReadinessAvailable:
        typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShellResult &&
            noOptionObjectShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      objectShellStatusBundleStayedPassive: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      liveRendererInstanceCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      readyForFutureRendererObjectShapePlanBundle: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 310,
    name: "custom-25d-visual-manual-renderer-object-shell-status-closeout",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    rendererObjectShellStatusCloseout: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase310StatusAvailable:
      typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
      "function",
    phase310ReadinessAvailable:
      typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShellResult && noOptionObjectShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    objectShellStatusBundleStayedPassive: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererObjectShapePlanBundle: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShapePlanReport(options = {}) {
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;
  const objectShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellReadinessReport(options)
      : null;
  const objectShellStatusCloseout =
    typeof getCustom25DVisualManualRendererObjectShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellStatusCloseoutReport(options)
      : null;
  const objectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell(options)
      : null;
  const objectShellCloseout =
    typeof getCustom25DVisualManualRendererObjectShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererObjectShellCloseoutReport(options)
      : null;
  const noOptionObjectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
      ? objectShellStatus.requiredOptionKeys
      : objectShellReadiness &&
            Array.isArray(objectShellReadiness.requiredOptionKeys)
        ? objectShellReadiness.requiredOptionKeys
        : objectShellStatusCloseout &&
              Array.isArray(objectShellStatusCloseout.requiredOptionKeys)
          ? objectShellStatusCloseout.requiredOptionKeys
          : objectShellResult && Array.isArray(objectShellResult.requiredOptionKeys)
            ? objectShellResult.requiredOptionKeys
            : objectShellCloseout &&
                  Array.isArray(objectShellCloseout.requiredOptionKeys)
              ? objectShellCloseout.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShellStatus && objectShellStatus.blockedBehavior
      ? objectShellStatus.blockedBehavior
      : objectShellReadiness && objectShellReadiness.blockedBehavior
        ? objectShellReadiness.blockedBehavior
        : objectShellStatusCloseout && objectShellStatusCloseout.blockedBehavior
          ? objectShellStatusCloseout.blockedBehavior
          : objectShellResult && objectShellResult.blockedBehavior
            ? objectShellResult.blockedBehavior
            : objectShellCloseout && objectShellCloseout.blockedBehavior
              ? objectShellCloseout.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShellStatus && objectShellStatus.preservedSystems
      ? objectShellStatus.preservedSystems
      : objectShellReadiness && objectShellReadiness.preservedSystems
        ? objectShellReadiness.preservedSystems
        : objectShellStatusCloseout && objectShellStatusCloseout.preservedSystems
          ? objectShellStatusCloseout.preservedSystems
          : objectShellResult && objectShellResult.preservedSystems
            ? objectShellResult.preservedSystems
            : objectShellCloseout && objectShellCloseout.preservedSystems
              ? objectShellCloseout.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShellStatus && objectShellStatus.safetyFlags
      ? objectShellStatus.safetyFlags
      : objectShellReadiness && objectShellReadiness.safetyFlags
        ? objectShellReadiness.safetyFlags
        : objectShellStatusCloseout && objectShellStatusCloseout.safetyFlags
          ? objectShellStatusCloseout.safetyFlags
          : objectShellResult && objectShellResult.safetyFlags
            ? objectShellResult.safetyFlags
            : objectShellCloseout && objectShellCloseout.safetyFlags
              ? objectShellCloseout.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 311,
      name: "custom-25d-visual-manual-renderer-object-shape-plan",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      planOnly: true,
      rendererObjectShapePlan: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualObjectShapeAdded: false,
      phase310StatusAvailable:
        typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
        "function",
      phase310ReadinessAvailable:
        typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShellResult &&
            noOptionObjectShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      plannedObjectShapeMode: "manual-inert-renderer-object-shape",
      plannedShapeFields: [
        "phase",
        "name",
        "inert",
        "manualOnly",
        "rendererCreated",
        "rendererInitialized",
        "rendererRunning",
        "rendererLoopStarted",
        "mapAttached",
        "drawingEnabled",
        "domCreated",
        "startupWired"
      ],
      plannedObjectShapeCreatesLiveInstance: false,
      plannedObjectShapeHasStartMethod: false,
      plannedObjectShapeHasDrawMethod: false,
      plannedObjectShapeHasAttachMethod: false,
      futureRendererObjectShapeReady: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 311,
    name: "custom-25d-visual-manual-renderer-object-shape-plan",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    rendererObjectShapePlan: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualObjectShapeAdded: false,
    phase310StatusAvailable:
      typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
      "function",
    phase310ReadinessAvailable:
      typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShellResult && noOptionObjectShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    plannedObjectShapeMode: "manual-inert-renderer-object-shape",
    plannedShapeFields: [
      "phase",
      "name",
      "inert",
      "manualOnly",
      "rendererCreated",
      "rendererInitialized",
      "rendererRunning",
      "rendererLoopStarted",
      "mapAttached",
      "drawingEnabled",
      "domCreated",
      "startupWired"
    ],
    plannedObjectShapeCreatesLiveInstance: false,
    plannedObjectShapeHasStartMethod: false,
    plannedObjectShapeHasDrawMethod: false,
    plannedObjectShapeHasAttachMethod: false,
    futureRendererObjectShapeReady: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShapeContractReport(options = {}) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;
  const objectShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellReadinessReport(options)
      : null;
  const objectShellStatusCloseout =
    typeof getCustom25DVisualManualRendererObjectShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellStatusCloseoutReport(options)
      : null;
  const objectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell(options)
      : null;
  const noOptionObjectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
      ? objectShapePlan.requiredOptionKeys
      : objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
        ? objectShellStatus.requiredOptionKeys
        : objectShellReadiness &&
              Array.isArray(objectShellReadiness.requiredOptionKeys)
          ? objectShellReadiness.requiredOptionKeys
          : objectShellStatusCloseout &&
                Array.isArray(objectShellStatusCloseout.requiredOptionKeys)
            ? objectShellStatusCloseout.requiredOptionKeys
            : objectShellResult && Array.isArray(objectShellResult.requiredOptionKeys)
              ? objectShellResult.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShapePlan && objectShapePlan.blockedBehavior
      ? objectShapePlan.blockedBehavior
      : objectShellStatus && objectShellStatus.blockedBehavior
        ? objectShellStatus.blockedBehavior
        : objectShellReadiness && objectShellReadiness.blockedBehavior
          ? objectShellReadiness.blockedBehavior
          : objectShellStatusCloseout && objectShellStatusCloseout.blockedBehavior
            ? objectShellStatusCloseout.blockedBehavior
            : objectShellResult && objectShellResult.blockedBehavior
              ? objectShellResult.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShapePlan && objectShapePlan.preservedSystems
      ? objectShapePlan.preservedSystems
      : objectShellStatus && objectShellStatus.preservedSystems
        ? objectShellStatus.preservedSystems
        : objectShellReadiness && objectShellReadiness.preservedSystems
          ? objectShellReadiness.preservedSystems
          : objectShellStatusCloseout && objectShellStatusCloseout.preservedSystems
            ? objectShellStatusCloseout.preservedSystems
            : objectShellResult && objectShellResult.preservedSystems
              ? objectShellResult.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShapePlan && objectShapePlan.safetyFlags
      ? objectShapePlan.safetyFlags
      : objectShellStatus && objectShellStatus.safetyFlags
        ? objectShellStatus.safetyFlags
        : objectShellReadiness && objectShellReadiness.safetyFlags
          ? objectShellReadiness.safetyFlags
          : objectShellStatusCloseout && objectShellStatusCloseout.safetyFlags
            ? objectShellStatusCloseout.safetyFlags
            : objectShellResult && objectShellResult.safetyFlags
              ? objectShellResult.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 311,
      name: "custom-25d-visual-manual-renderer-object-shape-contract",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      contractOnly: true,
      rendererObjectShapeContract: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualObjectShapeAdded: false,
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShellResult &&
            noOptionObjectShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      allowedInputMode: "phase-310-renderer-object-shell-status-summary-only",
      allowedOutputMode: "primitive-object-shape-plan-only",
      objectShapeMayCreateSharedState: false,
      objectShapeMayMutateSharedState: false,
      objectShapeMayReturnLiveReference: false,
      objectShapeMayCreateLiveRendererInstance: false,
      objectShapeMayInitializeRenderer: false,
      objectShapeMayStartRenderer: false,
      objectShapeMayAttachMap: false,
      objectShapeMayDrawGeometry: false,
      objectShapeMayCreateDom: false,
      objectShapeMayWireStartup: false,
      objectShapeMayChangeGameplay: false,
      contractReadyForFutureObjectShape: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 311,
    name: "custom-25d-visual-manual-renderer-object-shape-contract",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    rendererObjectShapeContract: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualObjectShapeAdded: false,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShellResult && noOptionObjectShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    allowedInputMode: "phase-310-renderer-object-shell-status-summary-only",
    allowedOutputMode: "primitive-object-shape-plan-only",
    objectShapeMayCreateSharedState: false,
    objectShapeMayMutateSharedState: false,
    objectShapeMayReturnLiveReference: false,
    objectShapeMayCreateLiveRendererInstance: false,
    objectShapeMayInitializeRenderer: false,
    objectShapeMayStartRenderer: false,
    objectShapeMayAttachMap: false,
    objectShapeMayDrawGeometry: false,
    objectShapeMayCreateDom: false,
    objectShapeMayWireStartup: false,
    objectShapeMayChangeGameplay: false,
    contractReadyForFutureObjectShape: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShapePlanCloseoutReport(
  options = {}
) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShapeContract =
    typeof getCustom25DVisualManualRendererObjectShapeContractReport === "function"
      ? getCustom25DVisualManualRendererObjectShapeContractReport(options)
      : null;
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;
  const objectShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellReadinessReport(options)
      : null;
  const objectShellStatusCloseout =
    typeof getCustom25DVisualManualRendererObjectShellStatusCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShellStatusCloseoutReport(options)
      : null;
  const objectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell(options)
      : null;
  const noOptionObjectShellResult =
    typeof createCustom25DVisualManualRendererObjectShell === "function"
      ? createCustom25DVisualManualRendererObjectShell()
      : null;

  const requiredOptionKeys =
    objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
      ? objectShapePlan.requiredOptionKeys
      : objectShapeContract && Array.isArray(objectShapeContract.requiredOptionKeys)
        ? objectShapeContract.requiredOptionKeys
        : objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
          ? objectShellStatus.requiredOptionKeys
          : objectShellReadiness &&
                Array.isArray(objectShellReadiness.requiredOptionKeys)
            ? objectShellReadiness.requiredOptionKeys
            : objectShellStatusCloseout &&
                  Array.isArray(objectShellStatusCloseout.requiredOptionKeys)
              ? objectShellStatusCloseout.requiredOptionKeys
              : objectShellResult && Array.isArray(objectShellResult.requiredOptionKeys)
                ? objectShellResult.requiredOptionKeys
                : [
                    "manual",
                    "developerIntent",
                    "localDevOnly",
                    "browserConsoleOnly",
                    "explicitOptionsOnly",
                    "allowManualRendererStateContainerShell",
                    "noStartupWiring",
                    "noBackendChanges",
                    "noPersistence",
                    "noAutomaticInvocation"
                  ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShapePlan && objectShapePlan.blockedBehavior
      ? objectShapePlan.blockedBehavior
      : objectShapeContract && objectShapeContract.blockedBehavior
        ? objectShapeContract.blockedBehavior
        : objectShellStatus && objectShellStatus.blockedBehavior
          ? objectShellStatus.blockedBehavior
          : objectShellReadiness && objectShellReadiness.blockedBehavior
            ? objectShellReadiness.blockedBehavior
            : objectShellStatusCloseout && objectShellStatusCloseout.blockedBehavior
              ? objectShellStatusCloseout.blockedBehavior
              : objectShellResult && objectShellResult.blockedBehavior
                ? objectShellResult.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    rendererRun: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  };

  const preservedSystems =
    objectShapePlan && objectShapePlan.preservedSystems
      ? objectShapePlan.preservedSystems
      : objectShapeContract && objectShapeContract.preservedSystems
        ? objectShapeContract.preservedSystems
        : objectShellStatus && objectShellStatus.preservedSystems
          ? objectShellStatus.preservedSystems
          : objectShellReadiness && objectShellReadiness.preservedSystems
            ? objectShellReadiness.preservedSystems
            : objectShellStatusCloseout && objectShellStatusCloseout.preservedSystems
              ? objectShellStatusCloseout.preservedSystems
              : objectShellResult && objectShellResult.preservedSystems
                ? objectShellResult.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  };

  const safetyFlags =
    objectShapePlan && objectShapePlan.safetyFlags
      ? objectShapePlan.safetyFlags
      : objectShapeContract && objectShapeContract.safetyFlags
        ? objectShapeContract.safetyFlags
        : objectShellStatus && objectShellStatus.safetyFlags
          ? objectShellStatus.safetyFlags
          : objectShellReadiness && objectShellReadiness.safetyFlags
            ? objectShellReadiness.safetyFlags
            : objectShellStatusCloseout && objectShellStatusCloseout.safetyFlags
              ? objectShellStatusCloseout.safetyFlags
              : objectShellResult && objectShellResult.safetyFlags
                ? objectShellResult.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  };

  if (missingKey) {
    return {
      phase: 311,
      name: "custom-25d-visual-manual-renderer-object-shape-plan-closeout",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      rendererObjectShapePlanCloseout: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase311PlanAvailable:
        typeof getCustom25DVisualManualRendererObjectShapePlanReport ===
        "function",
      phase311ContractAvailable:
        typeof getCustom25DVisualManualRendererObjectShapeContractReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShellResult &&
            noOptionObjectShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      actualObjectShapeAdded: false,
      objectShapePlanStayedPassive: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      liveRendererInstanceCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      readyForFutureManualObjectShapeShell: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 311,
    name: "custom-25d-visual-manual-renderer-object-shape-plan-closeout",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    rendererObjectShapePlanCloseout: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase311PlanAvailable:
      typeof getCustom25DVisualManualRendererObjectShapePlanReport ===
      "function",
    phase311ContractAvailable:
      typeof getCustom25DVisualManualRendererObjectShapeContractReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShellResult && noOptionObjectShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    actualObjectShapeAdded: false,
    objectShapePlanStayedPassive: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureManualObjectShapeShell: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererObjectShapeShell(options = {}) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShapeContract =
    typeof getCustom25DVisualManualRendererObjectShapeContractReport === "function"
      ? getCustom25DVisualManualRendererObjectShapeContractReport(options)
      : null;
  const objectShapePlanCloseout =
    typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport(options)
      : null;
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;

  const requiredOptionKeys =
    objectShapePlanCloseout &&
    Array.isArray(objectShapePlanCloseout.requiredOptionKeys)
      ? objectShapePlanCloseout.requiredOptionKeys
      : objectShapeContract && Array.isArray(objectShapeContract.requiredOptionKeys)
        ? objectShapeContract.requiredOptionKeys
        : objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
          ? objectShapePlan.requiredOptionKeys
          : objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
            ? objectShellStatus.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShapePlanCloseout && objectShapePlanCloseout.blockedBehavior
      ? objectShapePlanCloseout.blockedBehavior
      : objectShapeContract && objectShapeContract.blockedBehavior
        ? objectShapeContract.blockedBehavior
        : objectShapePlan && objectShapePlan.blockedBehavior
          ? objectShapePlan.blockedBehavior
          : objectShellStatus && objectShellStatus.blockedBehavior
            ? objectShellStatus.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    objectShapePlanCloseout && objectShapePlanCloseout.preservedSystems
      ? objectShapePlanCloseout.preservedSystems
      : objectShapeContract && objectShapeContract.preservedSystems
        ? objectShapeContract.preservedSystems
        : objectShapePlan && objectShapePlan.preservedSystems
          ? objectShapePlan.preservedSystems
          : objectShellStatus && objectShellStatus.preservedSystems
            ? objectShellStatus.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    objectShapePlanCloseout && objectShapePlanCloseout.safetyFlags
      ? objectShapePlanCloseout.safetyFlags
      : objectShapeContract && objectShapeContract.safetyFlags
        ? objectShapeContract.safetyFlags
        : objectShapePlan && objectShapePlan.safetyFlags
          ? objectShapePlan.safetyFlags
          : objectShellStatus && objectShellStatus.safetyFlags
            ? objectShellStatus.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  if (missingKey) {
    return {
      phase: 312,
      name: "custom-25d-visual-manual-renderer-object-shape-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualRendererObjectShapeShell: true,
      inert: true,
      objectShapeShellCreated: false,
      consumedObjectShellStatusOnly: true,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveRendererInstanceCreated: false,
      rendererObjectShapeCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      objectShapeMode: "manual-inert-renderer-object-shape-shell-only",
      shapeFields: [
        "phase",
        "name",
        "inert",
        "manualOnly",
        "rendererCreated",
        "rendererInitialized",
        "rendererRunning",
        "rendererLoopStarted",
        "mapAttached",
        "drawingEnabled",
        "domCreated",
        "startupWired"
      ],
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  const objectShellStatusAvailable =
    !!(
      objectShellStatus &&
      objectShellStatus.allowed === true &&
      objectShellStatus.blocked === false
    );

  return {
    phase: 312,
    name: "custom-25d-visual-manual-renderer-object-shape-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: objectShellStatusAvailable ? null : "object-shell-status-not-available",
    failedRequirement: objectShellStatusAvailable ? null : "objectShellStatusAvailable",
    requiredOptionKeys,
    manualRendererObjectShapeShell: true,
    inert: true,
    objectShapeShellCreated: objectShellStatusAvailable,
    consumedObjectShellStatusOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    rendererObjectShapeCreated: objectShellStatusAvailable,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    objectShapeMode: "manual-inert-renderer-object-shape-shell-only",
    shapeFields: [
      "phase",
      "name",
      "inert",
      "manualOnly",
      "rendererCreated",
      "rendererInitialized",
      "rendererRunning",
      "rendererLoopStarted",
      "mapAttached",
      "drawingEnabled",
      "domCreated",
      "startupWired"
    ],
    objectShellStatusSummary:
      objectShellStatusAvailable && objectShellStatus
        ? {
            phase309ObjectShellAvailable:
              objectShellStatus.phase309ObjectShellAvailable === true,
            phase309CloseoutAvailable:
              objectShellStatus.phase309CloseoutAvailable === true,
            objectShellStayedInert:
              objectShellStatus.objectShellStayedInert === true,
            rendererObjectShellCreated:
              objectShellStatus.rendererObjectShellCreated === true,
            liveRendererInstanceCreated:
              objectShellStatus.liveRendererInstanceCreated === true,
            rendererCreated: objectShellStatus.rendererCreated === true,
            rendererInitialized:
              objectShellStatus.rendererInitialized === true,
            rendererRunning: objectShellStatus.rendererRunning === true,
            rendererLoopStarted:
              objectShellStatus.rendererLoopStarted === true,
            mapAttached: objectShellStatus.mapAttached === true,
            drawingEnabled: objectShellStatus.drawingEnabled === true,
            domCreated: objectShellStatus.domCreated === true,
            startupWired: objectShellStatus.startupWired === true,
            gameplayChanged: objectShellStatus.gameplayChanged === true
          }
        : null,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShapeShellCloseoutReport(
  options = {}
) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShapeContract =
    typeof getCustom25DVisualManualRendererObjectShapeContractReport === "function"
      ? getCustom25DVisualManualRendererObjectShapeContractReport(options)
      : null;
  const objectShapePlanCloseout =
    typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport(options)
      : null;
  const objectShellStatus =
    typeof getCustom25DVisualManualRendererObjectShellStatusReport === "function"
      ? getCustom25DVisualManualRendererObjectShellStatusReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
      ? createCustom25DVisualManualRendererObjectShapeShell(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
      ? createCustom25DVisualManualRendererObjectShapeShell()
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : objectShapePlanCloseout &&
            Array.isArray(objectShapePlanCloseout.requiredOptionKeys)
        ? objectShapePlanCloseout.requiredOptionKeys
        : objectShapeContract && Array.isArray(objectShapeContract.requiredOptionKeys)
          ? objectShapeContract.requiredOptionKeys
          : objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
            ? objectShapePlan.requiredOptionKeys
            : objectShellStatus && Array.isArray(objectShellStatus.requiredOptionKeys)
              ? objectShellStatus.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  return {
    phase: 312,
    name: "custom-25d-visual-manual-renderer-object-shape-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    rendererObjectShapeShellCloseout: true,
    phase312ObjectShapeShellAvailable:
      typeof createCustom25DVisualManualRendererObjectShapeShell === "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    objectShapeShellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.consumedObjectShellStatusOnly === true &&
        shellResult.liveRendererInstanceCreated === false &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.rendererLoopStarted === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    consumedObjectShellStatusOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererObjectShapeStatusBundle: true,
    blockedBehavior:
      shellResult && shellResult.blockedBehavior
        ? shellResult.blockedBehavior
        : objectShapePlanCloseout && objectShapePlanCloseout.blockedBehavior
          ? objectShapePlanCloseout.blockedBehavior
          : objectShapeContract && objectShapeContract.blockedBehavior
            ? objectShapeContract.blockedBehavior
            : objectShapePlan && objectShapePlan.blockedBehavior
              ? objectShapePlan.blockedBehavior
              : objectShellStatus && objectShellStatus.blockedBehavior
                ? objectShellStatus.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    rendererRun: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      shellResult && shellResult.preservedSystems
        ? shellResult.preservedSystems
        : objectShapePlanCloseout && objectShapePlanCloseout.preservedSystems
          ? objectShapePlanCloseout.preservedSystems
          : objectShapeContract && objectShapeContract.preservedSystems
            ? objectShapeContract.preservedSystems
            : objectShapePlan && objectShapePlan.preservedSystems
              ? objectShapePlan.preservedSystems
              : objectShellStatus && objectShellStatus.preservedSystems
                ? objectShellStatus.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      shellResult && shellResult.safetyFlags
        ? shellResult.safetyFlags
        : objectShapePlanCloseout && objectShapePlanCloseout.safetyFlags
          ? objectShapePlanCloseout.safetyFlags
          : objectShapeContract && objectShapeContract.safetyFlags
            ? objectShapeContract.safetyFlags
            : objectShapePlan && objectShapePlan.safetyFlags
              ? objectShapePlan.safetyFlags
              : objectShellStatus && objectShellStatus.safetyFlags
                ? objectShellStatus.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
  };
}

function getCustom25DVisualManualRendererObjectShapeShellStatusReport(
  options = {}
) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShapeContract =
    typeof getCustom25DVisualManualRendererObjectShapeContractReport === "function"
      ? getCustom25DVisualManualRendererObjectShapeContractReport(options)
      : null;
  const objectShapePlanCloseout =
    typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport(options)
      : null;
  const objectShapeShellCloseout =
    typeof getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellCloseoutReport(options)
      : null;
  const objectShapeShellResult =
    typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
      ? createCustom25DVisualManualRendererObjectShapeShell(options)
      : null;
  const noOptionObjectShapeShellResult =
    typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
      ? createCustom25DVisualManualRendererObjectShapeShell()
      : null;

  const requiredOptionKeys =
    objectShapeShellResult && Array.isArray(objectShapeShellResult.requiredOptionKeys)
      ? objectShapeShellResult.requiredOptionKeys
      : objectShapeShellCloseout &&
            Array.isArray(objectShapeShellCloseout.requiredOptionKeys)
        ? objectShapeShellCloseout.requiredOptionKeys
        : objectShapePlanCloseout &&
              Array.isArray(objectShapePlanCloseout.requiredOptionKeys)
          ? objectShapePlanCloseout.requiredOptionKeys
          : objectShapeContract && Array.isArray(objectShapeContract.requiredOptionKeys)
            ? objectShapeContract.requiredOptionKeys
            : objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
              ? objectShapePlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShapeShellResult && objectShapeShellResult.blockedBehavior
      ? objectShapeShellResult.blockedBehavior
      : objectShapeShellCloseout && objectShapeShellCloseout.blockedBehavior
        ? objectShapeShellCloseout.blockedBehavior
        : objectShapePlanCloseout && objectShapePlanCloseout.blockedBehavior
          ? objectShapePlanCloseout.blockedBehavior
          : objectShapeContract && objectShapeContract.blockedBehavior
            ? objectShapeContract.blockedBehavior
            : objectShapePlan && objectShapePlan.blockedBehavior
              ? objectShapePlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShapeShellResult && objectShapeShellResult.preservedSystems
      ? objectShapeShellResult.preservedSystems
      : objectShapeShellCloseout && objectShapeShellCloseout.preservedSystems
        ? objectShapeShellCloseout.preservedSystems
        : objectShapePlanCloseout && objectShapePlanCloseout.preservedSystems
          ? objectShapePlanCloseout.preservedSystems
          : objectShapeContract && objectShapeContract.preservedSystems
            ? objectShapeContract.preservedSystems
            : objectShapePlan && objectShapePlan.preservedSystems
              ? objectShapePlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShapeShellResult && objectShapeShellResult.safetyFlags
      ? objectShapeShellResult.safetyFlags
      : objectShapeShellCloseout && objectShapeShellCloseout.safetyFlags
        ? objectShapeShellCloseout.safetyFlags
        : objectShapePlanCloseout && objectShapePlanCloseout.safetyFlags
          ? objectShapePlanCloseout.safetyFlags
          : objectShapeContract && objectShapeContract.safetyFlags
            ? objectShapeContract.safetyFlags
            : objectShapePlan && objectShapePlan.safetyFlags
              ? objectShapePlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 313,
      name: "custom-25d-visual-manual-renderer-object-shape-shell-status",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      statusOnly: true,
      rendererObjectShapeShellStatus: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase312ObjectShapeShellAvailable:
        typeof createCustom25DVisualManualRendererObjectShapeShell === "function",
      phase312CloseoutAvailable:
        typeof getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShapeShellResult &&
            noOptionObjectShapeShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      consumedObjectShapeShellReportOnly: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      liveRendererInstanceCreated: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      objectShapeShellStayedInert: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 313,
    name: "custom-25d-visual-manual-renderer-object-shape-shell-status",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    statusOnly: true,
    rendererObjectShapeShellStatus: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase312ObjectShapeShellAvailable:
      typeof createCustom25DVisualManualRendererObjectShapeShell === "function",
    phase312CloseoutAvailable:
      typeof getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShapeShellResult &&
          noOptionObjectShapeShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    consumedObjectShapeShellReportOnly: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    liveRendererInstanceCreated: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    objectShapeShellStayedInert: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererObjectShapeShellReadinessReport(
  options = {}
) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShapeContract =
    typeof getCustom25DVisualManualRendererObjectShapeContractReport === "function"
      ? getCustom25DVisualManualRendererObjectShapeContractReport(options)
      : null;
  const objectShapePlanCloseout =
    typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport(options)
      : null;
  const objectShapeShellStatus =
    typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellStatusReport(options)
      : null;
  const objectShapeShellCloseout =
    typeof getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellCloseoutReport(options)
      : null;
  const noOptionObjectShapeShellResult =
    typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
      ? createCustom25DVisualManualRendererObjectShapeShell()
      : null;

  const requiredOptionKeys =
    objectShapeShellStatus &&
    Array.isArray(objectShapeShellStatus.requiredOptionKeys)
      ? objectShapeShellStatus.requiredOptionKeys
      : objectShapeShellCloseout &&
            Array.isArray(objectShapeShellCloseout.requiredOptionKeys)
        ? objectShapeShellCloseout.requiredOptionKeys
        : objectShapePlanCloseout &&
              Array.isArray(objectShapePlanCloseout.requiredOptionKeys)
          ? objectShapePlanCloseout.requiredOptionKeys
          : objectShapeContract && Array.isArray(objectShapeContract.requiredOptionKeys)
            ? objectShapeContract.requiredOptionKeys
            : objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
              ? objectShapePlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShapeShellStatus && objectShapeShellStatus.blockedBehavior
      ? objectShapeShellStatus.blockedBehavior
      : objectShapeShellCloseout && objectShapeShellCloseout.blockedBehavior
        ? objectShapeShellCloseout.blockedBehavior
        : objectShapePlanCloseout && objectShapePlanCloseout.blockedBehavior
          ? objectShapePlanCloseout.blockedBehavior
          : objectShapeContract && objectShapeContract.blockedBehavior
            ? objectShapeContract.blockedBehavior
            : objectShapePlan && objectShapePlan.blockedBehavior
              ? objectShapePlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShapeShellStatus && objectShapeShellStatus.preservedSystems
      ? objectShapeShellStatus.preservedSystems
      : objectShapeShellCloseout && objectShapeShellCloseout.preservedSystems
        ? objectShapeShellCloseout.preservedSystems
        : objectShapePlanCloseout && objectShapePlanCloseout.preservedSystems
          ? objectShapePlanCloseout.preservedSystems
          : objectShapeContract && objectShapeContract.preservedSystems
            ? objectShapeContract.preservedSystems
            : objectShapePlan && objectShapePlan.preservedSystems
              ? objectShapePlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShapeShellStatus && objectShapeShellStatus.safetyFlags
      ? objectShapeShellStatus.safetyFlags
      : objectShapeShellCloseout && objectShapeShellCloseout.safetyFlags
        ? objectShapeShellCloseout.safetyFlags
        : objectShapePlanCloseout && objectShapePlanCloseout.safetyFlags
          ? objectShapePlanCloseout.safetyFlags
          : objectShapeContract && objectShapeContract.safetyFlags
            ? objectShapeContract.safetyFlags
            : objectShapePlan && objectShapePlan.safetyFlags
              ? objectShapePlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 313,
      name: "custom-25d-visual-manual-renderer-object-shape-shell-readiness",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      rendererObjectShapeShellReadiness: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase312ObjectShapeShellAvailable:
        typeof createCustom25DVisualManualRendererObjectShapeShell === "function",
      phase313StatusAvailable:
        typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShapeShellResult &&
            noOptionObjectShapeShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      readyForFutureRendererMetadataPlan: true,
      futureRendererMetadataShouldRemainManual: true,
      futureRendererMetadataMayCreateLiveInstance: false,
      futureRendererMetadataMayInitializeRenderer: false,
      futureRendererMetadataMayStartRenderer: false,
      futureRendererMetadataMayAttachMap: false,
      futureRendererMetadataMayDrawGeometry: false,
      futureRendererMetadataMayCreateDom: false,
      futureRendererMetadataMayWireStartup: false,
      futureRendererMetadataMayChangeGameplay: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 313,
    name: "custom-25d-visual-manual-renderer-object-shape-shell-readiness",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    rendererObjectShapeShellReadiness: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase312ObjectShapeShellAvailable:
      typeof createCustom25DVisualManualRendererObjectShapeShell === "function",
    phase313StatusAvailable:
      typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShapeShellResult &&
          noOptionObjectShapeShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    readyForFutureRendererMetadataPlan: true,
    futureRendererMetadataShouldRemainManual: true,
    futureRendererMetadataMayCreateLiveInstance: false,
    futureRendererMetadataMayInitializeRenderer: false,
    futureRendererMetadataMayStartRenderer: false,
    futureRendererMetadataMayAttachMap: false,
    futureRendererMetadataMayDrawGeometry: false,
    futureRendererMetadataMayCreateDom: false,
    futureRendererMetadataMayWireStartup: false,
    futureRendererMetadataMayChangeGameplay: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataPlanReport(options = {}) {
  const objectShapePlan =
    typeof getCustom25DVisualManualRendererObjectShapePlanReport === "function"
      ? getCustom25DVisualManualRendererObjectShapePlanReport(options)
      : null;
  const objectShapeContract =
    typeof getCustom25DVisualManualRendererObjectShapeContractReport === "function"
      ? getCustom25DVisualManualRendererObjectShapeContractReport(options)
      : null;
  const objectShapePlanCloseout =
    typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport(options)
      : null;
  const objectShapeShellStatus =
    typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellStatusReport(options)
      : null;
  const objectShapeShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellReadinessReport(options)
      : null;
  const noOptionObjectShapeShellResult =
    typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
      ? createCustom25DVisualManualRendererObjectShapeShell()
      : null;

  const requiredOptionKeys =
    objectShapeShellStatus &&
    Array.isArray(objectShapeShellStatus.requiredOptionKeys)
      ? objectShapeShellStatus.requiredOptionKeys
      : objectShapeShellReadiness &&
            Array.isArray(objectShapeShellReadiness.requiredOptionKeys)
        ? objectShapeShellReadiness.requiredOptionKeys
        : objectShapePlanCloseout &&
              Array.isArray(objectShapePlanCloseout.requiredOptionKeys)
          ? objectShapePlanCloseout.requiredOptionKeys
          : objectShapeContract && Array.isArray(objectShapeContract.requiredOptionKeys)
            ? objectShapeContract.requiredOptionKeys
            : objectShapePlan && Array.isArray(objectShapePlan.requiredOptionKeys)
              ? objectShapePlan.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    objectShapeShellStatus && objectShapeShellStatus.blockedBehavior
      ? objectShapeShellStatus.blockedBehavior
      : objectShapeShellReadiness && objectShapeShellReadiness.blockedBehavior
        ? objectShapeShellReadiness.blockedBehavior
        : objectShapePlanCloseout && objectShapePlanCloseout.blockedBehavior
          ? objectShapePlanCloseout.blockedBehavior
          : objectShapeContract && objectShapeContract.blockedBehavior
            ? objectShapeContract.blockedBehavior
            : objectShapePlan && objectShapePlan.blockedBehavior
              ? objectShapePlan.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };

  const preservedSystems =
    objectShapeShellStatus && objectShapeShellStatus.preservedSystems
      ? objectShapeShellStatus.preservedSystems
      : objectShapeShellReadiness && objectShapeShellReadiness.preservedSystems
        ? objectShapeShellReadiness.preservedSystems
        : objectShapePlanCloseout && objectShapePlanCloseout.preservedSystems
          ? objectShapePlanCloseout.preservedSystems
          : objectShapeContract && objectShapeContract.preservedSystems
            ? objectShapeContract.preservedSystems
            : objectShapePlan && objectShapePlan.preservedSystems
              ? objectShapePlan.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };

  const safetyFlags =
    objectShapeShellStatus && objectShapeShellStatus.safetyFlags
      ? objectShapeShellStatus.safetyFlags
      : objectShapeShellReadiness && objectShapeShellReadiness.safetyFlags
        ? objectShapeShellReadiness.safetyFlags
        : objectShapePlanCloseout && objectShapePlanCloseout.safetyFlags
          ? objectShapePlanCloseout.safetyFlags
          : objectShapeContract && objectShapeContract.safetyFlags
            ? objectShapeContract.safetyFlags
            : objectShapePlan && objectShapePlan.safetyFlags
              ? objectShapePlan.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };

  if (missingKey) {
    return {
      phase: 313,
      name: "custom-25d-visual-manual-renderer-metadata-plan",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      planOnly: true,
      rendererMetadataPlan: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualRendererMetadataAdded: false,
      phase313StatusAvailable:
        typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
        "function",
      phase313ReadinessAvailable:
        typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionObjectShapeShellResult &&
            noOptionObjectShapeShellResult.blocked === true
          ),
        requiredOptionKeys
      },
      plannedMetadataMode: "manual-inert-renderer-metadata-only",
      plannedMetadataFields: [
        "rendererName",
        "rendererVersion",
        "rendererMode",
        "inert",
        "manualOnly",
        "localDevOnly",
        "rendererCreated",
        "rendererInitialized",
        "rendererRunning",
        "rendererLoopStarted",
        "mapAttached",
        "drawingEnabled",
        "domCreated",
        "startupWired"
      ],
      plannedMetadataCreatesLiveInstance: false,
      plannedMetadataAddsCallableMethods: false,
      futureRendererMetadataReady: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 313,
    name: "custom-25d-visual-manual-renderer-metadata-plan",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    rendererMetadataPlan: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualRendererMetadataAdded: false,
    phase313StatusAvailable:
      typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
      "function",
    phase313ReadinessAvailable:
      typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionObjectShapeShellResult &&
          noOptionObjectShapeShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    plannedMetadataMode: "manual-inert-renderer-metadata-only",
    plannedMetadataFields: [
      "rendererName",
      "rendererVersion",
      "rendererMode",
      "inert",
      "manualOnly",
      "localDevOnly",
      "rendererCreated",
      "rendererInitialized",
      "rendererRunning",
      "rendererLoopStarted",
      "mapAttached",
      "drawingEnabled",
      "domCreated",
      "startupWired"
    ],
    plannedMetadataCreatesLiveInstance: false,
    plannedMetadataAddsCallableMethods: false,
    futureRendererMetadataReady: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererMetadataShell(options = {}) {
  const metadataPlan =
    typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataPlanReport(options)
      : null;
  const objectShapeShellStatus =
    typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellStatusReport(options)
      : null;
  const objectShapeShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellReadinessReport(options)
      : null;

  const requiredOptionKeys =
    metadataPlan && Array.isArray(metadataPlan.requiredOptionKeys)
      ? metadataPlan.requiredOptionKeys
      : objectShapeShellReadiness &&
            Array.isArray(objectShapeShellReadiness.requiredOptionKeys)
        ? objectShapeShellReadiness.requiredOptionKeys
        : objectShapeShellStatus &&
              Array.isArray(objectShapeShellStatus.requiredOptionKeys)
          ? objectShapeShellStatus.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataPlan && metadataPlan.blockedBehavior
      ? metadataPlan.blockedBehavior
      : objectShapeShellReadiness && objectShapeShellReadiness.blockedBehavior
        ? objectShapeShellReadiness.blockedBehavior
        : objectShapeShellStatus && objectShapeShellStatus.blockedBehavior
          ? objectShapeShellStatus.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataPlan && metadataPlan.preservedSystems
      ? metadataPlan.preservedSystems
      : objectShapeShellReadiness && objectShapeShellReadiness.preservedSystems
        ? objectShapeShellReadiness.preservedSystems
        : objectShapeShellStatus && objectShapeShellStatus.preservedSystems
          ? objectShapeShellStatus.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataPlan && metadataPlan.safetyFlags
      ? metadataPlan.safetyFlags
      : objectShapeShellReadiness && objectShapeShellReadiness.safetyFlags
        ? objectShapeShellReadiness.safetyFlags
        : objectShapeShellStatus && objectShapeShellStatus.safetyFlags
          ? objectShapeShellStatus.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 314,
      name: "custom-25d-visual-manual-renderer-metadata-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualRendererMetadataShell: true,
      inert: true,
      metadataShellCreated: false,
      consumedObjectShapeShellStatusOnly: true,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveRendererInstanceCreated: false,
      callableRendererMethodsAdded: false,
      rendererName: "custom25DVisualManualRenderer",
      rendererVersion: "manual-inert-metadata-shell-v1",
      rendererMode: "manual-inert-metadata-only",
      manualOnly: true,
      localDevOnly: true,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  const objectShapeShellStatusAvailable =
    !!(
      objectShapeShellStatus &&
      objectShapeShellStatus.allowed === true &&
      objectShapeShellStatus.blocked === false
    );

  return {
    phase: 314,
    name: "custom-25d-visual-manual-renderer-metadata-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: objectShapeShellStatusAvailable
      ? null
      : "object-shape-shell-status-not-available",
    failedRequirement: objectShapeShellStatusAvailable
      ? null
      : "objectShapeShellStatusAvailable",
    requiredOptionKeys,
    manualRendererMetadataShell: true,
    inert: true,
    metadataShellCreated: objectShapeShellStatusAvailable,
    consumedObjectShapeShellStatusOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    rendererName: "custom25DVisualManualRenderer",
    rendererVersion: "manual-inert-metadata-shell-v1",
    rendererMode: "manual-inert-metadata-only",
    manualOnly: true,
    localDevOnly: true,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    objectShapeShellStatusSummary:
      objectShapeShellStatusAvailable && objectShapeShellStatus
        ? {
            phase312ObjectShapeShellAvailable:
              objectShapeShellStatus.phase312ObjectShapeShellAvailable === true,
            phase312CloseoutAvailable:
              objectShapeShellStatus.phase312CloseoutAvailable === true,
            objectShapeShellStayedInert:
              objectShapeShellStatus.objectShapeShellStayedInert === true,
            liveRendererInstanceCreated:
              objectShapeShellStatus.liveRendererInstanceCreated === true,
            rendererCreated: objectShapeShellStatus.rendererCreated === true,
            rendererInitialized:
              objectShapeShellStatus.rendererInitialized === true,
            rendererRunning: objectShapeShellStatus.rendererRunning === true,
            rendererLoopStarted:
              objectShapeShellStatus.rendererLoopStarted === true,
            mapAttached: objectShapeShellStatus.mapAttached === true,
            drawingEnabled: objectShapeShellStatus.drawingEnabled === true,
            domCreated: objectShapeShellStatus.domCreated === true,
            startupWired: objectShapeShellStatus.startupWired === true,
            gameplayChanged: objectShapeShellStatus.gameplayChanged === true
          }
        : null,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataShellCloseoutReport(options = {}) {
  const metadataPlan =
    typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataPlanReport(options)
      : null;
  const objectShapeShellStatus =
    typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellStatusReport(options)
      : null;
  const objectShapeShellReadiness =
    typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellReadinessReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell()
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : metadataPlan && Array.isArray(metadataPlan.requiredOptionKeys)
        ? metadataPlan.requiredOptionKeys
        : objectShapeShellReadiness &&
              Array.isArray(objectShapeShellReadiness.requiredOptionKeys)
          ? objectShapeShellReadiness.requiredOptionKeys
          : objectShapeShellStatus &&
                Array.isArray(objectShapeShellStatus.requiredOptionKeys)
            ? objectShapeShellStatus.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  return {
    phase: 314,
    name: "custom-25d-visual-manual-renderer-metadata-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    rendererMetadataShellCloseout: true,
    phase314MetadataShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataShell === "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    metadataShellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.consumedObjectShapeShellStatusOnly === true &&
        shellResult.liveRendererInstanceCreated === false &&
        shellResult.callableRendererMethodsAdded === false &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.rendererLoopStarted === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    consumedObjectShapeShellStatusOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererMetadataStatusBundle: true,
    blockedBehavior:
      shellResult && shellResult.blockedBehavior
        ? shellResult.blockedBehavior
        : metadataPlan && metadataPlan.blockedBehavior
          ? metadataPlan.blockedBehavior
          : objectShapeShellReadiness && objectShapeShellReadiness.blockedBehavior
            ? objectShapeShellReadiness.blockedBehavior
            : objectShapeShellStatus && objectShapeShellStatus.blockedBehavior
              ? objectShapeShellStatus.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                },
    preservedSystems:
      shellResult && shellResult.preservedSystems
        ? shellResult.preservedSystems
        : metadataPlan && metadataPlan.preservedSystems
          ? metadataPlan.preservedSystems
          : objectShapeShellReadiness && objectShapeShellReadiness.preservedSystems
            ? objectShapeShellReadiness.preservedSystems
            : objectShapeShellStatus && objectShapeShellStatus.preservedSystems
              ? objectShapeShellStatus.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                },
    safetyFlags:
      shellResult && shellResult.safetyFlags
        ? shellResult.safetyFlags
        : metadataPlan && metadataPlan.safetyFlags
          ? metadataPlan.safetyFlags
          : objectShapeShellReadiness && objectShapeShellReadiness.safetyFlags
            ? objectShapeShellReadiness.safetyFlags
            : objectShapeShellStatus && objectShapeShellStatus.safetyFlags
              ? objectShapeShellStatus.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                }
  };
}

function getCustom25DVisualManualRendererMetadataShellStatusReport(options = {}) {
  const metadataPlan =
    typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataPlanReport(options)
      : null;
  const metadataShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellCloseoutReport(options)
      : null;
  const objectShapeShellStatus =
    typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererObjectShapeShellStatusReport(options)
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell()
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : metadataShellCloseout &&
            Array.isArray(metadataShellCloseout.requiredOptionKeys)
        ? metadataShellCloseout.requiredOptionKeys
        : metadataPlan && Array.isArray(metadataPlan.requiredOptionKeys)
          ? metadataPlan.requiredOptionKeys
          : objectShapeShellStatus &&
                Array.isArray(objectShapeShellStatus.requiredOptionKeys)
            ? objectShapeShellStatus.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    shellResult && shellResult.blockedBehavior
      ? shellResult.blockedBehavior
      : metadataShellCloseout && metadataShellCloseout.blockedBehavior
        ? metadataShellCloseout.blockedBehavior
        : metadataPlan && metadataPlan.blockedBehavior
          ? metadataPlan.blockedBehavior
          : objectShapeShellStatus && objectShapeShellStatus.blockedBehavior
            ? objectShapeShellStatus.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    shellResult && shellResult.preservedSystems
      ? shellResult.preservedSystems
      : metadataShellCloseout && metadataShellCloseout.preservedSystems
        ? metadataShellCloseout.preservedSystems
        : metadataPlan && metadataPlan.preservedSystems
          ? metadataPlan.preservedSystems
          : objectShapeShellStatus && objectShapeShellStatus.preservedSystems
            ? objectShapeShellStatus.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    shellResult && shellResult.safetyFlags
      ? shellResult.safetyFlags
      : metadataShellCloseout && metadataShellCloseout.safetyFlags
        ? metadataShellCloseout.safetyFlags
        : metadataPlan && metadataPlan.safetyFlags
          ? metadataPlan.safetyFlags
          : objectShapeShellStatus && objectShapeShellStatus.safetyFlags
            ? objectShapeShellStatus.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  if (missingKey) {
    return {
      phase: 315,
      name: "custom-25d-visual-manual-renderer-metadata-shell-status",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      statusOnly: true,
      rendererMetadataShellStatus: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase314MetadataShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataShell === "function",
      phase314CloseoutAvailable:
        typeof getCustom25DVisualManualRendererMetadataShellCloseoutReport ===
        "function",
      defaultNoOptionCheck: {
        blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
        requiredOptionKeys
      },
      consumedMetadataShellReportOnly: true,
      metadataShellStayedInert: true,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveStateReferenceReturned: false,
      nestedLiveReferenceReturned: false,
      liveRendererInstanceCreated: false,
      callableRendererMethodsAdded: false,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 315,
    name: "custom-25d-visual-manual-renderer-metadata-shell-status",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    statusOnly: true,
    rendererMetadataShellStatus: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase314MetadataShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataShell === "function",
    phase314CloseoutAvailable:
      typeof getCustom25DVisualManualRendererMetadataShellCloseoutReport ===
      "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    consumedMetadataShellReportOnly: true,
    metadataShellStayedInert:
      !!(
        shellResult &&
        shellResult.inert === true &&
        shellResult.metadataShellCreated === true &&
        shellResult.liveRendererInstanceCreated === false &&
        shellResult.callableRendererMethodsAdded === false &&
        shellResult.rendererCreated === false &&
        shellResult.rendererInitialized === false &&
        shellResult.rendererRunning === false &&
        shellResult.rendererLoopStarted === false &&
        shellResult.mapAttached === false &&
        shellResult.drawingEnabled === false &&
        shellResult.domCreated === false &&
        shellResult.startupWired === false &&
        shellResult.gameplayChanged === false
      ),
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataShellReadinessReport(
  options = {}
) {
  const metadataPlan =
    typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataPlanReport(options)
      : null;
  const metadataShellStatus =
    typeof getCustom25DVisualManualRendererMetadataShellStatusReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellStatusReport(options)
      : null;
  const metadataShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataShellCloseoutReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellCloseoutReport(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell()
      : null;

  const requiredOptionKeys =
    metadataShellStatus && Array.isArray(metadataShellStatus.requiredOptionKeys)
      ? metadataShellStatus.requiredOptionKeys
      : metadataShellCloseout &&
            Array.isArray(metadataShellCloseout.requiredOptionKeys)
        ? metadataShellCloseout.requiredOptionKeys
        : metadataPlan && Array.isArray(metadataPlan.requiredOptionKeys)
          ? metadataPlan.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataShellStatus && metadataShellStatus.blockedBehavior
      ? metadataShellStatus.blockedBehavior
      : metadataShellCloseout && metadataShellCloseout.blockedBehavior
        ? metadataShellCloseout.blockedBehavior
        : metadataPlan && metadataPlan.blockedBehavior
          ? metadataPlan.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataShellStatus && metadataShellStatus.preservedSystems
      ? metadataShellStatus.preservedSystems
      : metadataShellCloseout && metadataShellCloseout.preservedSystems
        ? metadataShellCloseout.preservedSystems
        : metadataPlan && metadataPlan.preservedSystems
          ? metadataPlan.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataShellStatus && metadataShellStatus.safetyFlags
      ? metadataShellStatus.safetyFlags
      : metadataShellCloseout && metadataShellCloseout.safetyFlags
        ? metadataShellCloseout.safetyFlags
        : metadataPlan && metadataPlan.safetyFlags
          ? metadataPlan.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 315,
      name: "custom-25d-visual-manual-renderer-metadata-shell-readiness",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      rendererMetadataShellReadiness: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase314MetadataShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataShell === "function",
      phase315StatusAvailable:
        typeof getCustom25DVisualManualRendererMetadataShellStatusReport ===
        "function",
      defaultNoOptionCheck: {
        blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
        requiredOptionKeys
      },
      readyForFutureRendererMetadataContractPlan: true,
      futureRendererMetadataContractShouldRemainManual: true,
      futureRendererMetadataContractMayCreateLiveInstance: false,
      futureRendererMetadataContractMayInitializeRenderer: false,
      futureRendererMetadataContractMayStartRenderer: false,
      futureRendererMetadataContractMayAttachMap: false,
      futureRendererMetadataContractMayDrawGeometry: false,
      futureRendererMetadataContractMayCreateDom: false,
      futureRendererMetadataContractMayWireStartup: false,
      futureRendererMetadataContractMayChangeGameplay: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 315,
    name: "custom-25d-visual-manual-renderer-metadata-shell-readiness",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    rendererMetadataShellReadiness: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase314MetadataShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataShell === "function",
    phase315StatusAvailable:
      typeof getCustom25DVisualManualRendererMetadataShellStatusReport ===
      "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    readyForFutureRendererMetadataContractPlan: true,
    futureRendererMetadataContractShouldRemainManual: true,
    futureRendererMetadataContractMayCreateLiveInstance: false,
    futureRendererMetadataContractMayInitializeRenderer: false,
    futureRendererMetadataContractMayStartRenderer: false,
    futureRendererMetadataContractMayAttachMap: false,
    futureRendererMetadataContractMayDrawGeometry: false,
    futureRendererMetadataContractMayCreateDom: false,
    futureRendererMetadataContractMayWireStartup: false,
    futureRendererMetadataContractMayChangeGameplay: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataContractPlanReport(options = {}) {
  const metadataPlan =
    typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataPlanReport(options)
      : null;
  const metadataShellStatus =
    typeof getCustom25DVisualManualRendererMetadataShellStatusReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellStatusReport(options)
      : null;
  const metadataShellReadiness =
    typeof getCustom25DVisualManualRendererMetadataShellReadinessReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellReadinessReport(options)
      : null;
  const noOptionShellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell()
      : null;

  const requiredOptionKeys =
    metadataShellStatus && Array.isArray(metadataShellStatus.requiredOptionKeys)
      ? metadataShellStatus.requiredOptionKeys
      : metadataShellReadiness &&
            Array.isArray(metadataShellReadiness.requiredOptionKeys)
        ? metadataShellReadiness.requiredOptionKeys
        : metadataPlan && Array.isArray(metadataPlan.requiredOptionKeys)
          ? metadataPlan.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataShellReadiness && metadataShellReadiness.blockedBehavior
      ? metadataShellReadiness.blockedBehavior
      : metadataShellStatus && metadataShellStatus.blockedBehavior
        ? metadataShellStatus.blockedBehavior
        : metadataPlan && metadataPlan.blockedBehavior
          ? metadataPlan.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataShellReadiness && metadataShellReadiness.preservedSystems
      ? metadataShellReadiness.preservedSystems
      : metadataShellStatus && metadataShellStatus.preservedSystems
        ? metadataShellStatus.preservedSystems
        : metadataPlan && metadataPlan.preservedSystems
          ? metadataPlan.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataShellReadiness && metadataShellReadiness.safetyFlags
      ? metadataShellReadiness.safetyFlags
      : metadataShellStatus && metadataShellStatus.safetyFlags
        ? metadataShellStatus.safetyFlags
        : metadataPlan && metadataPlan.safetyFlags
          ? metadataPlan.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 315,
      name: "custom-25d-visual-manual-renderer-metadata-contract-plan",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      planOnly: true,
      rendererMetadataContractPlan: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualMetadataContractAdded: false,
      phase315StatusAvailable:
        typeof getCustom25DVisualManualRendererMetadataShellStatusReport ===
        "function",
      phase315ReadinessAvailable:
        typeof getCustom25DVisualManualRendererMetadataShellReadinessReport ===
        "function",
      defaultNoOptionCheck: {
        blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
        requiredOptionKeys
      },
      plannedContractMode: "manual-inert-renderer-metadata-contract-only",
      plannedRequiredMetadataFields: [
        "rendererName",
        "rendererVersion",
        "rendererMode",
        "inert",
        "manualOnly",
        "localDevOnly",
        "rendererCreated",
        "rendererInitialized",
        "rendererRunning",
        "rendererLoopStarted",
        "mapAttached",
        "drawingEnabled",
        "domCreated",
        "startupWired"
      ],
      plannedContractCreatesLiveInstance: false,
      plannedContractAddsCallableMethods: false,
      plannedContractEnforcesRuntimeBehavior: false,
      futureRendererMetadataContractReady: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 315,
    name: "custom-25d-visual-manual-renderer-metadata-contract-plan",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    rendererMetadataContractPlan: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualMetadataContractAdded: false,
    phase315StatusAvailable:
      typeof getCustom25DVisualManualRendererMetadataShellStatusReport ===
      "function",
    phase315ReadinessAvailable:
      typeof getCustom25DVisualManualRendererMetadataShellReadinessReport ===
      "function",
    defaultNoOptionCheck: {
      blocked: !!(noOptionShellResult && noOptionShellResult.blocked === true),
      requiredOptionKeys
    },
    plannedContractMode: "manual-inert-renderer-metadata-contract-only",
    plannedRequiredMetadataFields: [
      "rendererName",
      "rendererVersion",
      "rendererMode",
      "inert",
      "manualOnly",
      "localDevOnly",
      "rendererCreated",
      "rendererInitialized",
      "rendererRunning",
      "rendererLoopStarted",
      "mapAttached",
      "drawingEnabled",
      "domCreated",
      "startupWired"
    ],
    plannedContractCreatesLiveInstance: false,
    plannedContractAddsCallableMethods: false,
    plannedContractEnforcesRuntimeBehavior: false,
    futureRendererMetadataContractReady: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererMetadataContractShell(options = {}) {
  const metadataContractPlan =
    typeof getCustom25DVisualManualRendererMetadataContractPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataContractPlanReport(options)
      : null;
  const metadataShellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell(options)
      : null;
  const metadataShellStatus =
    typeof getCustom25DVisualManualRendererMetadataShellStatusReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellStatusReport(options)
      : null;
  const requiredMetadataFields =
    metadataContractPlan &&
    Array.isArray(metadataContractPlan.plannedRequiredMetadataFields)
      ? metadataContractPlan.plannedRequiredMetadataFields
      : [
          "rendererName",
          "rendererVersion",
          "rendererMode",
          "inert",
          "manualOnly",
          "localDevOnly",
          "rendererCreated",
          "rendererInitialized",
          "rendererRunning",
          "rendererLoopStarted",
          "mapAttached",
          "drawingEnabled",
          "domCreated",
          "startupWired"
        ];

  const requiredOptionKeys =
    metadataContractPlan && Array.isArray(metadataContractPlan.requiredOptionKeys)
      ? metadataContractPlan.requiredOptionKeys
      : metadataShellResult && Array.isArray(metadataShellResult.requiredOptionKeys)
        ? metadataShellResult.requiredOptionKeys
        : metadataShellStatus && Array.isArray(metadataShellStatus.requiredOptionKeys)
          ? metadataShellStatus.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataContractPlan && metadataContractPlan.blockedBehavior
      ? metadataContractPlan.blockedBehavior
      : metadataShellResult && metadataShellResult.blockedBehavior
        ? metadataShellResult.blockedBehavior
        : metadataShellStatus && metadataShellStatus.blockedBehavior
          ? metadataShellStatus.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataContractPlan && metadataContractPlan.preservedSystems
      ? metadataContractPlan.preservedSystems
      : metadataShellResult && metadataShellResult.preservedSystems
        ? metadataShellResult.preservedSystems
        : metadataShellStatus && metadataShellStatus.preservedSystems
          ? metadataShellStatus.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataContractPlan && metadataContractPlan.safetyFlags
      ? metadataContractPlan.safetyFlags
      : metadataShellResult && metadataShellResult.safetyFlags
        ? metadataShellResult.safetyFlags
        : metadataShellStatus && metadataShellStatus.safetyFlags
          ? metadataShellStatus.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 316,
      name: "custom-25d-visual-manual-renderer-metadata-contract-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualRendererMetadataContractShell: true,
      inert: true,
      metadataContractShellCreated: false,
      consumedMetadataShellReportOnly: true,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveRendererInstanceCreated: false,
      callableRendererMethodsAdded: false,
      runtimeMetadataEnforcementAdded: false,
      requiredMetadataFields,
      metadataContractMode: "manual-inert-renderer-metadata-contract-only",
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  const metadataShellAvailable =
    !!(
      metadataShellResult &&
      metadataShellResult.allowed === true &&
      metadataShellResult.blocked === false
    );

  return {
    phase: 316,
    name: "custom-25d-visual-manual-renderer-metadata-contract-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: metadataShellAvailable ? null : "metadata-shell-not-available",
    failedRequirement: metadataShellAvailable ? null : "metadataShellAvailable",
    requiredOptionKeys,
    manualRendererMetadataContractShell: true,
    inert: true,
    metadataContractShellCreated: metadataShellAvailable,
    consumedMetadataShellReportOnly: true,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    runtimeMetadataEnforcementAdded: false,
    requiredMetadataFields,
    metadataContractMode: "manual-inert-renderer-metadata-contract-only",
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    metadataShellSummary:
      metadataShellAvailable && metadataShellResult
        ? {
            metadataShellCreated: metadataShellResult.metadataShellCreated === true,
            liveRendererInstanceCreated:
              metadataShellResult.liveRendererInstanceCreated === true,
            callableRendererMethodsAdded:
              metadataShellResult.callableRendererMethodsAdded === true,
            rendererCreated: metadataShellResult.rendererCreated === true,
            rendererInitialized: metadataShellResult.rendererInitialized === true,
            rendererRunning: metadataShellResult.rendererRunning === true,
            rendererLoopStarted: metadataShellResult.rendererLoopStarted === true,
            mapAttached: metadataShellResult.mapAttached === true,
            drawingEnabled: metadataShellResult.drawingEnabled === true,
            domCreated: metadataShellResult.domCreated === true,
            startupWired: metadataShellResult.startupWired === true,
            gameplayChanged: metadataShellResult.gameplayChanged === true
          }
        : null,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataContractShellCloseoutReport(
  options = {}
) {
  const metadataContractPlan =
    typeof getCustom25DVisualManualRendererMetadataContractPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataContractPlanReport(options)
      : null;
  const metadataShellResult =
    typeof createCustom25DVisualManualRendererMetadataShell === "function"
      ? createCustom25DVisualManualRendererMetadataShell(options)
      : null;
  const metadataShellStatus =
    typeof getCustom25DVisualManualRendererMetadataShellStatusReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellStatusReport(options)
      : null;
  const metadataShellReadiness =
    typeof getCustom25DVisualManualRendererMetadataShellReadinessReport === "function"
      ? getCustom25DVisualManualRendererMetadataShellReadinessReport(options)
      : null;
  const contractShellResult =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;
  const noOptionContractShellResult =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell()
      : null;

  const requiredOptionKeys =
    contractShellResult && Array.isArray(contractShellResult.requiredOptionKeys)
      ? contractShellResult.requiredOptionKeys
      : metadataContractPlan && Array.isArray(metadataContractPlan.requiredOptionKeys)
        ? metadataContractPlan.requiredOptionKeys
        : metadataShellResult && Array.isArray(metadataShellResult.requiredOptionKeys)
          ? metadataShellResult.requiredOptionKeys
          : metadataShellStatus && Array.isArray(metadataShellStatus.requiredOptionKeys)
            ? metadataShellStatus.requiredOptionKeys
            : metadataShellReadiness &&
                  Array.isArray(metadataShellReadiness.requiredOptionKeys)
              ? metadataShellReadiness.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];

  return {
    phase: 316,
    name: "custom-25d-visual-manual-renderer-metadata-contract-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    rendererMetadataContractShellCloseout: true,
    phase316MetadataContractShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataContractShell === "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionContractShellResult &&
          noOptionContractShellResult.blocked === true
        ),
      requiredOptionKeys
    },
    metadataContractShellStayedInert:
      !!(
        contractShellResult &&
        contractShellResult.inert === true &&
        contractShellResult.consumedMetadataShellReportOnly === true &&
        contractShellResult.liveRendererInstanceCreated === false &&
        contractShellResult.callableRendererMethodsAdded === false &&
        contractShellResult.runtimeMetadataEnforcementAdded === false &&
        contractShellResult.rendererCreated === false &&
        contractShellResult.rendererInitialized === false &&
        contractShellResult.rendererRunning === false &&
        contractShellResult.rendererLoopStarted === false &&
        contractShellResult.mapAttached === false &&
        contractShellResult.drawingEnabled === false &&
        contractShellResult.domCreated === false &&
        contractShellResult.startupWired === false &&
        contractShellResult.gameplayChanged === false
      ),
    consumedMetadataShellReportOnly: true,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    runtimeMetadataEnforcementAdded: false,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererMetadataValidationPlanBundle: true,
    blockedBehavior:
      contractShellResult && contractShellResult.blockedBehavior
        ? contractShellResult.blockedBehavior
        : metadataContractPlan && metadataContractPlan.blockedBehavior
          ? metadataContractPlan.blockedBehavior
          : metadataShellResult && metadataShellResult.blockedBehavior
            ? metadataShellResult.blockedBehavior
            : metadataShellStatus && metadataShellStatus.blockedBehavior
              ? metadataShellStatus.blockedBehavior
              : metadataShellReadiness && metadataShellReadiness.blockedBehavior
                ? metadataShellReadiness.blockedBehavior
                : {
                    rendererCreation: true,
                    rendererInitialization: true,
                    rendererRun: true,
                    mapAttachment: true,
                    drawing: true,
                    domCreation: true,
                    startupWiring: true,
                    automaticInvocation: true,
                    gameplayChanges: true,
                    pinChanges: true,
                    uiChanges: true,
                    backendChanges: true,
                    storageWrites: true,
                    networkAccess: true
                  },
    preservedSystems:
      contractShellResult && contractShellResult.preservedSystems
        ? contractShellResult.preservedSystems
        : metadataContractPlan && metadataContractPlan.preservedSystems
          ? metadataContractPlan.preservedSystems
          : metadataShellResult && metadataShellResult.preservedSystems
            ? metadataShellResult.preservedSystems
            : metadataShellStatus && metadataShellStatus.preservedSystems
              ? metadataShellStatus.preservedSystems
              : metadataShellReadiness && metadataShellReadiness.preservedSystems
                ? metadataShellReadiness.preservedSystems
                : {
                    existingLeafletMapBehavior: true,
                    osmBehavior: true,
                    gameplay: true,
                    pins: true,
                    playerMarker: true,
                    captureRadius: true,
                    ui: true,
                    backend: true,
                    storage: true,
                    network: true
                  },
    safetyFlags:
      contractShellResult && contractShellResult.safetyFlags
        ? contractShellResult.safetyFlags
        : metadataContractPlan && metadataContractPlan.safetyFlags
          ? metadataContractPlan.safetyFlags
          : metadataShellResult && metadataShellResult.safetyFlags
            ? metadataShellResult.safetyFlags
            : metadataShellStatus && metadataShellStatus.safetyFlags
              ? metadataShellStatus.safetyFlags
              : metadataShellReadiness && metadataShellReadiness.safetyFlags
                ? metadataShellReadiness.safetyFlags
                : {
                    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                  }
  };
}

function getCustom25DVisualManualRendererMetadataValidationPlanReport(
  options = {}
) {
  const metadataContractPlan =
    typeof getCustom25DVisualManualRendererMetadataContractPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataContractPlanReport(options)
      : null;
  const metadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;
  const metadataContractShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataContractShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataContractShellCloseoutReport(options)
      : null;
  const noOptionMetadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell()
      : null;

  const requiredOptionKeys =
    metadataContractShell && Array.isArray(metadataContractShell.requiredOptionKeys)
      ? metadataContractShell.requiredOptionKeys
      : metadataContractShellCloseout &&
            Array.isArray(metadataContractShellCloseout.requiredOptionKeys)
        ? metadataContractShellCloseout.requiredOptionKeys
        : metadataContractPlan && Array.isArray(metadataContractPlan.requiredOptionKeys)
          ? metadataContractPlan.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataContractShellCloseout && metadataContractShellCloseout.blockedBehavior
      ? metadataContractShellCloseout.blockedBehavior
      : metadataContractPlan && metadataContractPlan.blockedBehavior
        ? metadataContractPlan.blockedBehavior
        : metadataContractShell && metadataContractShell.blockedBehavior
          ? metadataContractShell.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataContractShellCloseout && metadataContractShellCloseout.preservedSystems
      ? metadataContractShellCloseout.preservedSystems
      : metadataContractPlan && metadataContractPlan.preservedSystems
        ? metadataContractPlan.preservedSystems
        : metadataContractShell && metadataContractShell.preservedSystems
          ? metadataContractShell.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataContractShellCloseout && metadataContractShellCloseout.safetyFlags
      ? metadataContractShellCloseout.safetyFlags
      : metadataContractPlan && metadataContractPlan.safetyFlags
        ? metadataContractPlan.safetyFlags
        : metadataContractShell && metadataContractShell.safetyFlags
          ? metadataContractShell.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 317,
      name: "custom-25d-visual-manual-renderer-metadata-validation-plan",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      planOnly: true,
      rendererMetadataValidationPlan: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualMetadataValidationAdded: false,
      phase316ContractShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataContractShell ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionMetadataContractShell &&
            noOptionMetadataContractShell.blocked === true
          ),
        requiredOptionKeys
      },
      plannedValidationMode: "manual-inert-metadata-validation-plan-only",
      plannedValidationConsumesMetadataContractOnly: true,
      plannedValidationEnforcesRuntimeBehavior: false,
      plannedValidationCreatesLiveInstance: false,
      plannedValidationAddsCallableMethods: false,
      futureRendererMetadataValidationReady: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 317,
    name: "custom-25d-visual-manual-renderer-metadata-validation-plan",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    planOnly: true,
    rendererMetadataValidationPlan: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualMetadataValidationAdded: false,
    phase316ContractShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataContractShell ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataContractShell &&
          noOptionMetadataContractShell.blocked === true
        ),
      requiredOptionKeys
    },
    plannedValidationMode: "manual-inert-metadata-validation-plan-only",
    plannedValidationConsumesMetadataContractOnly: true,
    plannedValidationEnforcesRuntimeBehavior: false,
    plannedValidationCreatesLiveInstance: false,
    plannedValidationAddsCallableMethods: false,
    futureRendererMetadataValidationReady: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationContractReport(
  options = {}
) {
  const metadataValidationPlan =
    typeof getCustom25DVisualManualRendererMetadataValidationPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataValidationPlanReport(options)
      : null;
  const metadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;
  const metadataContractShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataContractShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataContractShellCloseoutReport(options)
      : null;
  const noOptionMetadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell()
      : null;

  const requiredOptionKeys =
    metadataValidationPlan && Array.isArray(metadataValidationPlan.requiredOptionKeys)
      ? metadataValidationPlan.requiredOptionKeys
      : metadataContractShell && Array.isArray(metadataContractShell.requiredOptionKeys)
        ? metadataContractShell.requiredOptionKeys
        : metadataContractShellCloseout &&
              Array.isArray(metadataContractShellCloseout.requiredOptionKeys)
          ? metadataContractShellCloseout.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataValidationPlan && metadataValidationPlan.blockedBehavior
      ? metadataValidationPlan.blockedBehavior
      : metadataContractShellCloseout && metadataContractShellCloseout.blockedBehavior
        ? metadataContractShellCloseout.blockedBehavior
        : metadataContractShell && metadataContractShell.blockedBehavior
          ? metadataContractShell.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataValidationPlan && metadataValidationPlan.preservedSystems
      ? metadataValidationPlan.preservedSystems
      : metadataContractShellCloseout && metadataContractShellCloseout.preservedSystems
        ? metadataContractShellCloseout.preservedSystems
        : metadataContractShell && metadataContractShell.preservedSystems
          ? metadataContractShell.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataValidationPlan && metadataValidationPlan.safetyFlags
      ? metadataValidationPlan.safetyFlags
      : metadataContractShellCloseout && metadataContractShellCloseout.safetyFlags
        ? metadataContractShellCloseout.safetyFlags
        : metadataContractShell && metadataContractShell.safetyFlags
          ? metadataContractShell.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 317,
      name: "custom-25d-visual-manual-renderer-metadata-validation-contract",
      ok: true,
      allowed: false,
      blocked: true,
      inert: true,
      passive: true,
      reportOnly: true,
      contractOnly: true,
      rendererMetadataValidationContract: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      actualMetadataValidationAdded: false,
      allowedInputMode: "phase-316-metadata-contract-shell-report-only",
      allowedOutputMode: "primitive-validation-plan-report-only",
      validationMayCreateSharedState: false,
      validationMayMutateSharedState: false,
      validationMayReadLiveSharedState: false,
      validationMayCreateLiveRendererInstance: false,
      validationMayAddCallableMethods: false,
      validationMayEnforceRuntimeBehavior: false,
      validationMayStartRenderer: false,
      validationMayAttachMap: false,
      validationMayDrawGeometry: false,
      validationMayCreateDom: false,
      validationMayWireStartup: false,
      validationMayChangeGameplay: false,
      contractReadyForFutureMetadataValidation: true,
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionMetadataContractShell &&
            noOptionMetadataContractShell.blocked === true
          ),
        requiredOptionKeys
      },
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 317,
    name: "custom-25d-visual-manual-renderer-metadata-validation-contract",
    ok: true,
    allowed: true,
    blocked: false,
    inert: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    rendererMetadataValidationContract: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    actualMetadataValidationAdded: false,
    allowedInputMode: "phase-316-metadata-contract-shell-report-only",
    allowedOutputMode: "primitive-validation-plan-report-only",
    validationMayCreateSharedState: false,
    validationMayMutateSharedState: false,
    validationMayReadLiveSharedState: false,
    validationMayCreateLiveRendererInstance: false,
    validationMayAddCallableMethods: false,
    validationMayEnforceRuntimeBehavior: false,
    validationMayStartRenderer: false,
    validationMayAttachMap: false,
    validationMayDrawGeometry: false,
    validationMayCreateDom: false,
    validationMayWireStartup: false,
    validationMayChangeGameplay: false,
    contractReadyForFutureMetadataValidation: true,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataContractShell &&
          noOptionMetadataContractShell.blocked === true
        ),
      requiredOptionKeys
    },
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport(
  options = {}
) {
  const metadataValidationPlan =
    typeof getCustom25DVisualManualRendererMetadataValidationPlanReport === "function"
      ? getCustom25DVisualManualRendererMetadataValidationPlanReport(options)
      : null;
  const metadataValidationContract =
    typeof getCustom25DVisualManualRendererMetadataValidationContractReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationContractReport(options)
      : null;
  const metadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;
  const noOptionMetadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell()
      : null;

  const requiredOptionKeys =
    metadataValidationPlan && Array.isArray(metadataValidationPlan.requiredOptionKeys)
      ? metadataValidationPlan.requiredOptionKeys
      : metadataValidationContract &&
            Array.isArray(metadataValidationContract.requiredOptionKeys)
        ? metadataValidationContract.requiredOptionKeys
        : metadataContractShell && Array.isArray(metadataContractShell.requiredOptionKeys)
          ? metadataContractShell.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const blockedBehavior =
    metadataValidationContract && metadataValidationContract.blockedBehavior
      ? metadataValidationContract.blockedBehavior
      : metadataValidationPlan && metadataValidationPlan.blockedBehavior
        ? metadataValidationPlan.blockedBehavior
        : metadataContractShell && metadataContractShell.blockedBehavior
          ? metadataContractShell.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    metadataValidationContract && metadataValidationContract.preservedSystems
      ? metadataValidationContract.preservedSystems
      : metadataValidationPlan && metadataValidationPlan.preservedSystems
        ? metadataValidationPlan.preservedSystems
        : metadataContractShell && metadataContractShell.preservedSystems
          ? metadataContractShell.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    metadataValidationContract && metadataValidationContract.safetyFlags
      ? metadataValidationContract.safetyFlags
      : metadataValidationPlan && metadataValidationPlan.safetyFlags
        ? metadataValidationPlan.safetyFlags
        : metadataContractShell && metadataContractShell.safetyFlags
          ? metadataContractShell.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  return {
    phase: 317,
    name: "custom-25d-visual-manual-renderer-metadata-validation-plan-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    rendererMetadataValidationPlanCloseout: true,
    phase317PlanAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationPlanReport ===
      "function",
    phase317ContractAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationContractReport ===
      "function",
    actualMetadataValidationAdded: false,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataContractShell &&
          noOptionMetadataContractShell.blocked === true
        ),
      requiredOptionKeys
    },
    metadataValidationPlanStayedPassive: true,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    runtimeMetadataEnforcementAdded: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    readyForFutureManualMetadataValidationShell: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererMetadataValidationShell(
  options = {}
) {
  const metadataValidationPlan =
    typeof getCustom25DVisualManualRendererMetadataValidationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationPlanReport(options)
      : null;
  const metadataValidationContract =
    typeof getCustom25DVisualManualRendererMetadataValidationContractReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationContractReport(options)
      : null;
  const metadataValidationPlanCloseout =
    typeof getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport(options)
      : null;
  const metadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;

  const requiredOptionKeys =
    metadataValidationPlanCloseout &&
    Array.isArray(metadataValidationPlanCloseout.requiredOptionKeys)
      ? metadataValidationPlanCloseout.requiredOptionKeys
      : metadataValidationContract &&
            Array.isArray(metadataValidationContract.requiredOptionKeys)
        ? metadataValidationContract.requiredOptionKeys
        : metadataValidationPlan &&
              Array.isArray(metadataValidationPlan.requiredOptionKeys)
          ? metadataValidationPlan.requiredOptionKeys
          : metadataContractShell &&
                Array.isArray(metadataContractShell.requiredOptionKeys)
            ? metadataContractShell.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataValidationPlanCloseout && metadataValidationPlanCloseout.blockedBehavior
      ? metadataValidationPlanCloseout.blockedBehavior
      : metadataValidationContract && metadataValidationContract.blockedBehavior
        ? metadataValidationContract.blockedBehavior
        : metadataValidationPlan && metadataValidationPlan.blockedBehavior
          ? metadataValidationPlan.blockedBehavior
          : metadataContractShell && metadataContractShell.blockedBehavior
            ? metadataContractShell.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    metadataValidationPlanCloseout && metadataValidationPlanCloseout.preservedSystems
      ? metadataValidationPlanCloseout.preservedSystems
      : metadataValidationContract && metadataValidationContract.preservedSystems
        ? metadataValidationContract.preservedSystems
        : metadataValidationPlan && metadataValidationPlan.preservedSystems
          ? metadataValidationPlan.preservedSystems
          : metadataContractShell && metadataContractShell.preservedSystems
            ? metadataContractShell.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    metadataValidationPlanCloseout && metadataValidationPlanCloseout.safetyFlags
      ? metadataValidationPlanCloseout.safetyFlags
      : metadataValidationContract && metadataValidationContract.safetyFlags
        ? metadataValidationContract.safetyFlags
        : metadataValidationPlan && metadataValidationPlan.safetyFlags
          ? metadataValidationPlan.safetyFlags
          : metadataContractShell && metadataContractShell.safetyFlags
            ? metadataContractShell.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  if (missingKey) {
    return {
      phase: 318,
      name: "custom-25d-visual-manual-renderer-metadata-validation-shell",
      ok: true,
      allowed: false,
      blocked: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manualRendererMetadataValidationShell: true,
      inert: true,
      metadataValidationShellCreated: false,
      consumedMetadataContractShellReportOnly: true,
      validationMode: "manual-inert-metadata-validation-shell-only",
      validationReportOnly: true,
      runtimeMetadataEnforcementAdded: false,
      consumedLiveStateReference: false,
      returnedLiveStateReference: false,
      returnedNestedLiveReference: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      liveRendererInstanceCreated: false,
      callableRendererMethodsAdded: false,
      metadataFieldsCheckedAsReportOnly: true,
      rendererCreated: false,
      rendererInitialized: false,
      rendererRunning: false,
      rendererLoopStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domCreated: false,
      startupWired: false,
      gameplayChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  const metadataContractShellAvailable =
    !!(
      metadataContractShell &&
      metadataContractShell.allowed === true &&
      metadataContractShell.blocked === false
    );

  return {
    phase: 318,
    name: "custom-25d-visual-manual-renderer-metadata-validation-shell",
    ok: true,
    allowed: true,
    blocked: false,
    reason: metadataContractShellAvailable
      ? null
      : "metadata-contract-shell-not-available",
    failedRequirement: metadataContractShellAvailable
      ? null
      : "metadataContractShellAvailable",
    requiredOptionKeys,
    manualRendererMetadataValidationShell: true,
    inert: true,
    metadataValidationShellCreated: metadataContractShellAvailable,
    consumedMetadataContractShellReportOnly: true,
    validationMode: "manual-inert-metadata-validation-shell-only",
    validationReportOnly: true,
    runtimeMetadataEnforcementAdded: false,
    consumedLiveStateReference: false,
    returnedLiveStateReference: false,
    returnedNestedLiveReference: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    metadataFieldsCheckedAsReportOnly: true,
    rendererCreated: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    metadataContractShellSummary:
      metadataContractShellAvailable && metadataContractShell
        ? {
            metadataContractShellCreated:
              metadataContractShell.metadataContractShellCreated === true,
            runtimeMetadataEnforcementAdded:
              metadataContractShell.runtimeMetadataEnforcementAdded === true,
            liveRendererInstanceCreated:
              metadataContractShell.liveRendererInstanceCreated === true,
            callableRendererMethodsAdded:
              metadataContractShell.callableRendererMethodsAdded === true,
            rendererCreated: metadataContractShell.rendererCreated === true,
            rendererInitialized:
              metadataContractShell.rendererInitialized === true,
            rendererRunning: metadataContractShell.rendererRunning === true,
            rendererLoopStarted:
              metadataContractShell.rendererLoopStarted === true,
            mapAttached: metadataContractShell.mapAttached === true,
            drawingEnabled: metadataContractShell.drawingEnabled === true,
            domCreated: metadataContractShell.domCreated === true,
            startupWired: metadataContractShell.startupWired === true,
            gameplayChanged: metadataContractShell.gameplayChanged === true
          }
        : null,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport(
  options = {}
) {
  const metadataValidationPlanCloseout =
    typeof getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport(options)
      : null;
  const metadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;
  const metadataContractShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataContractShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataContractShellCloseoutReport(options)
      : null;
  const metadataValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell === "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell(options)
      : null;
  const noOptionMetadataValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell()
      : null;

  const requiredOptionKeys =
    metadataValidationShell && Array.isArray(metadataValidationShell.requiredOptionKeys)
      ? metadataValidationShell.requiredOptionKeys
      : metadataValidationPlanCloseout &&
            Array.isArray(metadataValidationPlanCloseout.requiredOptionKeys)
        ? metadataValidationPlanCloseout.requiredOptionKeys
        : metadataContractShell && Array.isArray(metadataContractShell.requiredOptionKeys)
          ? metadataContractShell.requiredOptionKeys
          : metadataContractShellCloseout &&
                Array.isArray(metadataContractShellCloseout.requiredOptionKeys)
            ? metadataContractShellCloseout.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const blockedBehavior =
    metadataValidationShell && metadataValidationShell.blockedBehavior
      ? metadataValidationShell.blockedBehavior
      : metadataValidationPlanCloseout && metadataValidationPlanCloseout.blockedBehavior
        ? metadataValidationPlanCloseout.blockedBehavior
        : metadataContractShell && metadataContractShell.blockedBehavior
          ? metadataContractShell.blockedBehavior
          : metadataContractShellCloseout && metadataContractShellCloseout.blockedBehavior
            ? metadataContractShellCloseout.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    metadataValidationShell && metadataValidationShell.preservedSystems
      ? metadataValidationShell.preservedSystems
      : metadataValidationPlanCloseout && metadataValidationPlanCloseout.preservedSystems
        ? metadataValidationPlanCloseout.preservedSystems
        : metadataContractShell && metadataContractShell.preservedSystems
          ? metadataContractShell.preservedSystems
          : metadataContractShellCloseout && metadataContractShellCloseout.preservedSystems
            ? metadataContractShellCloseout.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    metadataValidationShell && metadataValidationShell.safetyFlags
      ? metadataValidationShell.safetyFlags
      : metadataValidationPlanCloseout && metadataValidationPlanCloseout.safetyFlags
        ? metadataValidationPlanCloseout.safetyFlags
        : metadataContractShell && metadataContractShell.safetyFlags
          ? metadataContractShell.safetyFlags
          : metadataContractShellCloseout && metadataContractShellCloseout.safetyFlags
            ? metadataContractShellCloseout.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  return {
    phase: 318,
    name: "custom-25d-visual-manual-renderer-metadata-validation-shell-closeout",
    ok: true,
    inert: true,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    requiredOptionKeys,
    rendererMetadataValidationShellCloseout: true,
    phase318MetadataValidationShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationShell ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataValidationShell &&
          noOptionMetadataValidationShell.blocked === true
        ),
      requiredOptionKeys
    },
    metadataValidationShellStayedInert:
      !!(
        metadataValidationShell &&
        metadataValidationShell.inert === true &&
        metadataValidationShell.consumedMetadataContractShellReportOnly === true &&
        metadataValidationShell.validationReportOnly === true &&
        metadataValidationShell.runtimeMetadataEnforcementAdded === false &&
        metadataValidationShell.liveRendererInstanceCreated === false &&
        metadataValidationShell.callableRendererMethodsAdded === false &&
        metadataValidationShell.rendererInitialized === false &&
        metadataValidationShell.rendererRunning === false &&
        metadataValidationShell.rendererLoopStarted === false &&
        metadataValidationShell.mapAttached === false &&
        metadataValidationShell.drawingEnabled === false &&
        metadataValidationShell.domCreated === false &&
        metadataValidationShell.startupWired === false &&
        metadataValidationShell.gameplayChanged === false
      ),
    consumedMetadataContractShellReportOnly: true,
    validationStayedReportOnly: true,
    runtimeMetadataEnforcementAdded: false,
    liveStateReferenceReturned: false,
    nestedLiveReferenceReturned: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    liveRendererInstanceCreated: false,
    callableRendererMethodsAdded: false,
    rendererInitialized: false,
    rendererRunning: false,
    rendererLoopStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domCreated: false,
    startupWired: false,
    gameplayChanged: false,
    readyForFutureRendererMetadataValidationStatusBundle: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationShellStatusReport(
  options = {}
) {
  const metadataValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell(options)
      : null;
  const metadataValidationShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport(options)
      : null;
  const metadataValidationPlan =
    typeof getCustom25DVisualManualRendererMetadataValidationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationPlanReport(options)
      : null;
  const metadataContractShell =
    typeof createCustom25DVisualManualRendererMetadataContractShell === "function"
      ? createCustom25DVisualManualRendererMetadataContractShell(options)
      : null;
  const noOptionMetadataValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell()
      : null;

  const requiredOptionKeys =
    metadataValidationShell && Array.isArray(metadataValidationShell.requiredOptionKeys)
      ? metadataValidationShell.requiredOptionKeys
      : metadataValidationShellCloseout &&
            Array.isArray(metadataValidationShellCloseout.requiredOptionKeys)
        ? metadataValidationShellCloseout.requiredOptionKeys
        : metadataValidationPlan &&
              Array.isArray(metadataValidationPlan.requiredOptionKeys)
          ? metadataValidationPlan.requiredOptionKeys
          : metadataContractShell &&
                Array.isArray(metadataContractShell.requiredOptionKeys)
            ? metadataContractShell.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataValidationShell && metadataValidationShell.blockedBehavior
      ? metadataValidationShell.blockedBehavior
      : metadataValidationShellCloseout && metadataValidationShellCloseout.blockedBehavior
        ? metadataValidationShellCloseout.blockedBehavior
        : metadataValidationPlan && metadataValidationPlan.blockedBehavior
          ? metadataValidationPlan.blockedBehavior
          : metadataContractShell && metadataContractShell.blockedBehavior
            ? metadataContractShell.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };

  const preservedSystems =
    metadataValidationShell && metadataValidationShell.preservedSystems
      ? metadataValidationShell.preservedSystems
      : metadataValidationShellCloseout && metadataValidationShellCloseout.preservedSystems
        ? metadataValidationShellCloseout.preservedSystems
        : metadataValidationPlan && metadataValidationPlan.preservedSystems
          ? metadataValidationPlan.preservedSystems
          : metadataContractShell && metadataContractShell.preservedSystems
            ? metadataContractShell.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };

  const safetyFlags =
    metadataValidationShell && metadataValidationShell.safetyFlags
      ? metadataValidationShell.safetyFlags
      : metadataValidationShellCloseout && metadataValidationShellCloseout.safetyFlags
        ? metadataValidationShellCloseout.safetyFlags
        : metadataValidationPlan && metadataValidationPlan.safetyFlags
          ? metadataValidationPlan.safetyFlags
          : metadataContractShell && metadataContractShell.safetyFlags
            ? metadataContractShell.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };

  if (missingKey) {
    return {
      phase: 319,
      name: "custom-25d-visual-manual-renderer-metadata-validation-shell-status",
      helperName: "createCustom25DVisualManualRendererMetadataValidationShell",
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      statusOnly: true,
      rendererMetadataValidationShellStatus: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      validationShellHelperAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationShell ===
        "function",
      validationShellReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionMetadataValidationShell &&
            noOptionMetadataValidationShell.blocked === true
          ),
        requiredOptionKeys
      },
      validationRuntimeEnforced: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 319,
    name: "custom-25d-visual-manual-renderer-metadata-validation-shell-status",
    helperName: "createCustom25DVisualManualRendererMetadataValidationShell",
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    statusOnly: true,
    rendererMetadataValidationShellStatus: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    validationShellHelperAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationShell === "function",
    validationShellReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataValidationShell &&
          noOptionMetadataValidationShell.blocked === true
        ),
      requiredOptionKeys
    },
    validationRuntimeEnforced: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationShellReadinessReport(
  options = {}
) {
  const metadataValidationShellStatus =
    typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellStatusReport(options)
      : null;
  const metadataValidationShellCloseout =
    typeof getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport(options)
      : null;
  const noOptionMetadataValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell()
      : null;

  const requiredOptionKeys =
    metadataValidationShellStatus &&
    Array.isArray(metadataValidationShellStatus.requiredOptionKeys)
      ? metadataValidationShellStatus.requiredOptionKeys
      : metadataValidationShellCloseout &&
            Array.isArray(metadataValidationShellCloseout.requiredOptionKeys)
        ? metadataValidationShellCloseout.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataValidationShellStatus && metadataValidationShellStatus.blockedBehavior
      ? metadataValidationShellStatus.blockedBehavior
      : metadataValidationShellCloseout && metadataValidationShellCloseout.blockedBehavior
        ? metadataValidationShellCloseout.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };

  const preservedSystems =
    metadataValidationShellStatus && metadataValidationShellStatus.preservedSystems
      ? metadataValidationShellStatus.preservedSystems
      : metadataValidationShellCloseout && metadataValidationShellCloseout.preservedSystems
        ? metadataValidationShellCloseout.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };

  const safetyFlags =
    metadataValidationShellStatus && metadataValidationShellStatus.safetyFlags
      ? metadataValidationShellStatus.safetyFlags
      : metadataValidationShellCloseout && metadataValidationShellCloseout.safetyFlags
        ? metadataValidationShellCloseout.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };

  if (missingKey) {
    return {
      phase: 319,
      name: "custom-25d-visual-manual-renderer-metadata-validation-shell-readiness",
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      readinessOnly: true,
      rendererMetadataValidationShellReadiness: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase318ValidationShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationShell ===
        "function",
      phase319StatusAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionMetadataValidationShell &&
            noOptionMetadataValidationShell.blocked === true
          ),
        requiredOptionKeys
      },
      futureValidationResultPlanningCanProceed: true,
      futureValidationResultBlockedBySafetyPolicy: false,
      futureValidationResultMustStayPrimitiveOnly: true,
      futureValidationResultMayEnforceRuntimeBehavior: false,
      futureValidationResultMayCreateLiveRenderer: false,
      futureValidationResultMayCreateCallableMethods: false,
      futureValidationResultMayReadSharedState: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 319,
    name: "custom-25d-visual-manual-renderer-metadata-validation-shell-readiness",
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    readinessOnly: true,
    rendererMetadataValidationShellReadiness: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase318ValidationShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationShell ===
      "function",
    phase319StatusAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataValidationShell &&
          noOptionMetadataValidationShell.blocked === true
        ),
      requiredOptionKeys
    },
    futureValidationResultPlanningCanProceed: true,
    futureValidationResultBlockedBySafetyPolicy: false,
    futureValidationResultMustStayPrimitiveOnly: true,
    futureValidationResultMayEnforceRuntimeBehavior: false,
    futureValidationResultMayCreateLiveRenderer: false,
    futureValidationResultMayCreateCallableMethods: false,
    futureValidationResultMayReadSharedState: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultPlanReport(
  options = {}
) {
  const metadataValidationShellStatus =
    typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellStatusReport(options)
      : null;
  const metadataValidationShellReadiness =
    typeof getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellReadinessReport(options)
      : null;
  const noOptionMetadataValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell()
      : null;

  const requiredOptionKeys =
    metadataValidationShellStatus &&
    Array.isArray(metadataValidationShellStatus.requiredOptionKeys)
      ? metadataValidationShellStatus.requiredOptionKeys
      : metadataValidationShellReadiness &&
            Array.isArray(metadataValidationShellReadiness.requiredOptionKeys)
        ? metadataValidationShellReadiness.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    metadataValidationShellReadiness && metadataValidationShellReadiness.blockedBehavior
      ? metadataValidationShellReadiness.blockedBehavior
      : metadataValidationShellStatus && metadataValidationShellStatus.blockedBehavior
        ? metadataValidationShellStatus.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };

  const preservedSystems =
    metadataValidationShellReadiness && metadataValidationShellReadiness.preservedSystems
      ? metadataValidationShellReadiness.preservedSystems
      : metadataValidationShellStatus && metadataValidationShellStatus.preservedSystems
        ? metadataValidationShellStatus.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };

  const safetyFlags =
    metadataValidationShellReadiness && metadataValidationShellReadiness.safetyFlags
      ? metadataValidationShellReadiness.safetyFlags
      : metadataValidationShellStatus && metadataValidationShellStatus.safetyFlags
        ? metadataValidationShellStatus.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };

  const plannedPrimitiveFields = [
    "phase",
    "ok",
    "reportOnly",
    "resultCreated",
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "validationPassed",
    "validationBlocked",
    "checkedFieldCount",
    "missingFieldCount",
    "invalidFieldCount",
    "requiredOptionKeys"
  ];
  const plannedFailureReasonFields = [
    "reason",
    "failedRequirement",
    "missingRequiredFields",
    "invalidFields",
    "blockedBySafetyPolicy"
  ];
  const plannedStatusFields = [
    "sharedStateCreated",
    "sharedStateMutated",
    "sharedStateDirectlyRead",
    "liveRendererCreated",
    "callableMethodsCreated",
    "mapAttached",
    "drawingEnabled",
    "domMutated",
    "startupWired"
  ];

  if (missingKey) {
    return {
      phase: 319,
      name: "custom-25d-visual-manual-renderer-metadata-validation-result-plan",
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      planOnly: true,
      rendererMetadataValidationResultPlan: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedResultName: "custom25DVisualManualRendererMetadataValidationResult",
      resultCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionMetadataValidationShell &&
            noOptionMetadataValidationShell.blocked === true
          ),
        requiredOptionKeys
      },
      plannedPrimitiveFields,
      plannedFailureReasonFields,
      plannedStatusFields,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 319,
    name: "custom-25d-visual-manual-renderer-metadata-validation-result-plan",
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    planOnly: true,
    rendererMetadataValidationResultPlan: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedResultName: "custom25DVisualManualRendererMetadataValidationResult",
    resultCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionMetadataValidationShell &&
          noOptionMetadataValidationShell.blocked === true
        ),
      requiredOptionKeys
    },
    plannedPrimitiveFields,
    plannedFailureReasonFields,
    plannedStatusFields,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport(
  options = {}
) {
  const statusReport =
    typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellStatusReport(options)
      : null;
  const readinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationShellReadinessReport(options)
      : null;
  const resultPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultPlanReport(options)
      : null;
  const noOptionValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell()
      : null;

  const requiredOptionKeys =
    statusReport && Array.isArray(statusReport.requiredOptionKeys)
      ? statusReport.requiredOptionKeys
      : readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
        ? readinessReport.requiredOptionKeys
        : resultPlanReport && Array.isArray(resultPlanReport.requiredOptionKeys)
          ? resultPlanReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    statusReport && statusReport.blockedBehavior
      ? statusReport.blockedBehavior
      : readinessReport && readinessReport.blockedBehavior
        ? readinessReport.blockedBehavior
        : resultPlanReport && resultPlanReport.blockedBehavior
          ? resultPlanReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };

  const preservedSystems =
    statusReport && statusReport.preservedSystems
      ? statusReport.preservedSystems
      : readinessReport && readinessReport.preservedSystems
        ? readinessReport.preservedSystems
        : resultPlanReport && resultPlanReport.preservedSystems
          ? resultPlanReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };

  const safetyFlags =
    statusReport && statusReport.safetyFlags
      ? statusReport.safetyFlags
      : readinessReport && readinessReport.safetyFlags
        ? readinessReport.safetyFlags
        : resultPlanReport && resultPlanReport.safetyFlags
          ? resultPlanReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };

  if (missingKey) {
    return {
      phase: 320,
      phaseClosedOut: 319,
      name: "custom-25d-visual-manual-renderer-metadata-validation-status-result-plan-closeout",
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      statusReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
        "function",
      readinessReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
        "function",
      resultPlanReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
        "function",
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionValidationShell &&
            noOptionValidationShell.blocked === true
          ),
        requiredOptionKeys
      },
      resultPlanStillPlanOnly: true,
      runtimeValidationEnforced: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 320,
    phaseClosedOut: 319,
    name: "custom-25d-visual-manual-renderer-metadata-validation-status-result-plan-closeout",
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    statusReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
      "function",
    readinessReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
      "function",
    resultPlanReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
      "function",
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionValidationShell &&
          noOptionValidationShell.blocked === true
        ),
      requiredOptionKeys
    },
    resultPlanStillPlanOnly: true,
    runtimeValidationEnforced: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport(
  options = {}
) {
  const closeoutReport =
    typeof getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport(
          options
        )
      : null;
  const noOptionValidationShell =
    typeof createCustom25DVisualManualRendererMetadataValidationShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationShell()
      : null;

  const requiredOptionKeys =
    closeoutReport && Array.isArray(closeoutReport.requiredOptionKeys)
      ? closeoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    closeoutReport && closeoutReport.blockedBehavior
      ? closeoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };

  const preservedSystems =
    closeoutReport && closeoutReport.preservedSystems
      ? closeoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };

  const safetyFlags =
    closeoutReport && closeoutReport.safetyFlags
      ? closeoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };

  const blockers = [];
  const concerns = [];
  if (!closeoutReport) blockers.push("phase-320-closeout-report-unavailable");
  if (closeoutReport && closeoutReport.statusReportAvailable !== true) {
    blockers.push("phase-319-status-report-unavailable");
  }
  if (closeoutReport && closeoutReport.readinessReportAvailable !== true) {
    blockers.push("phase-319-readiness-report-unavailable");
  }
  if (closeoutReport && closeoutReport.resultPlanReportAvailable !== true) {
    blockers.push("phase-319-result-plan-report-unavailable");
  }
  if (closeoutReport && closeoutReport.resultPlanStillPlanOnly !== true) {
    concerns.push("result-plan-no-longer-plan-only");
  }
  if (closeoutReport && closeoutReport.runtimeValidationEnforced !== false) {
    concerns.push("runtime-validation-appears-enabled");
  }

  if (missingKey) {
    return {
      phase: 320,
      name: "custom-25d-visual-manual-renderer-metadata-validation-status-result-plan-self-review",
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReadyForNextPassivePlanningStep: blockers.length === 0,
      blockers,
      concerns,
      defaultNoOptionCheck: {
        blocked:
          !!(
            noOptionValidationShell &&
            noOptionValidationShell.blocked === true
          ),
        requiredOptionKeys
      },
      runtimeValidationEnforced: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 320,
    name: "custom-25d-visual-manual-renderer-metadata-validation-status-result-plan-self-review",
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReadyForNextPassivePlanningStep: blockers.length === 0,
    blockers,
    concerns,
    defaultNoOptionCheck: {
      blocked:
        !!(
          noOptionValidationShell &&
          noOptionValidationShell.blocked === true
        ),
      requiredOptionKeys
    },
    runtimeValidationEnforced: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport(
  options = {}
) {
  const resultPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultPlanReport(options)
      : null;
  const scriptSizeSelfReviewReport =
    typeof getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport ===
    "function"
      ? getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport(options)
      : null;

  const requiredOptionKeys =
    resultPlanReport && Array.isArray(resultPlanReport.requiredOptionKeys)
      ? resultPlanReport.requiredOptionKeys
      : scriptSizeSelfReviewReport &&
          Array.isArray(scriptSizeSelfReviewReport.requiredOptionKeys)
        ? scriptSizeSelfReviewReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];

  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    resultPlanReport && resultPlanReport.blockedBehavior
      ? resultPlanReport.blockedBehavior
      : scriptSizeSelfReviewReport && scriptSizeSelfReviewReport.blockedBehavior
        ? scriptSizeSelfReviewReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };

  const preservedSystems =
    resultPlanReport && resultPlanReport.preservedSystems
      ? resultPlanReport.preservedSystems
      : scriptSizeSelfReviewReport && scriptSizeSelfReviewReport.preservedSystems
        ? scriptSizeSelfReviewReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };

  const safetyFlags =
    resultPlanReport && resultPlanReport.safetyFlags
      ? resultPlanReport.safetyFlags
      : scriptSizeSelfReviewReport && scriptSizeSelfReviewReport.safetyFlags
        ? scriptSizeSelfReviewReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };

  const plannedRequiredPrimitiveFields = [
    "phase",
    "helperName",
    "ok",
    "reportOnly",
    "planOnly",
    "contractCreated",
    "resultObjectCreated",
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "requiredOptionKeys"
  ];
  const plannedOptionalPrimitiveFields = [
    "reason",
    "failedRequirement",
    "status",
    "statusValue",
    "failureReason",
    "warningCount",
    "noteCount"
  ];
  const plannedStatusValues = [
    "blocked",
    "plan-only",
    "ready-for-inert-shell",
    "not-ready-for-runtime-validation"
  ];
  const plannedFailureReasonValues = [
    "required-option-missing",
    "validation-not-enabled",
    "runtime-enforcement-blocked",
    "shared-state-read-blocked",
    "live-result-object-not-created"
  ];
  const plannedSafetyFields = [
    "resultObjectCreated",
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "sharedStateDirectlyRead",
    "sharedStateMutated",
    "liveRendererCreated",
    "callableMethodsCreated",
    "mapAttached",
    "drawingEnabled",
    "domMutated",
    "startupWired"
  ];

  if (missingKey) {
    return {
      phase: 323,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedContractName:
        "custom25DVisualManualRendererMetadataValidationResultContract",
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      plannedRequiredPrimitiveFields,
      plannedOptionalPrimitiveFields,
      plannedStatusValues,
      plannedFailureReasonValues,
      plannedSafetyFields,
      sharedStateDirectlyRead: false,
      sharedStateMutated: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 323,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedContractName:
      "custom25DVisualManualRendererMetadataValidationResultContract",
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    plannedRequiredPrimitiveFields,
    plannedOptionalPrimitiveFields,
    plannedStatusValues,
    plannedFailureReasonValues,
    plannedSafetyFields,
    sharedStateDirectlyRead: false,
    sharedStateMutated: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport(
  options = {}
) {
  const contractPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport(
          options
        )
      : null;

  const requiredOptionKeys =
    contractPlanReport && Array.isArray(contractPlanReport.requiredOptionKeys)
      ? contractPlanReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    contractPlanReport && contractPlanReport.blockedBehavior
      ? contractPlanReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    contractPlanReport && contractPlanReport.preservedSystems
      ? contractPlanReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    contractPlanReport && contractPlanReport.safetyFlags
      ? contractPlanReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [
    "future-shell-should-remain-inert-and-manual-only",
    "runtime-validation-must-stay-disabled",
    "result-contract-should-not-read-shared-state-directly"
  ];
  const concerns = [
    "future-shell-must-remain-primitive-report-only",
    "validation-enforcement-should-stay-out-of-scope-for-next-phase"
  ];

  if (missingKey) {
    return {
      phase: 323,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFutureInertResultContractShell: true,
      readyForRuntimeValidation: false,
      blockers,
      concerns,
      recommendedNextStep:
        "future-inert-manual-result-contract-shell-phase-only",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 323,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFutureInertResultContractShell: true,
    readyForRuntimeValidation: false,
    blockers,
    concerns,
    recommendedNextStep:
      "future-inert-manual-result-contract-shell-phase-only",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport(
  options = {}
) {
  const contractPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport(
          options
        )
      : null;
  const readinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport(
          options
        )
      : null;

  const requiredOptionKeys =
    contractPlanReport && Array.isArray(contractPlanReport.requiredOptionKeys)
      ? contractPlanReport.requiredOptionKeys
      : readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
        ? readinessReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    readinessReport && readinessReport.blockedBehavior
      ? readinessReport.blockedBehavior
      : contractPlanReport && contractPlanReport.blockedBehavior
        ? contractPlanReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    readinessReport && readinessReport.preservedSystems
      ? readinessReport.preservedSystems
      : contractPlanReport && contractPlanReport.preservedSystems
        ? contractPlanReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    readinessReport && readinessReport.safetyFlags
      ? readinessReport.safetyFlags
      : contractPlanReport && contractPlanReport.safetyFlags
        ? contractPlanReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };

  if (missingKey) {
    return {
      phase: 323,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultContractPlanAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
        "function",
      resultContractReadinessAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
        "function",
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 323,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultContractPlanAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
      "function",
    resultContractReadinessAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
      "function",
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererMetadataValidationResultContractShell(
  options = {}
) {
  const contractPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport(
          options
        )
      : null;
  const readinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport(
          options
        )
      : null;

  const requiredOptionKeys =
    contractPlanReport && Array.isArray(contractPlanReport.requiredOptionKeys)
      ? contractPlanReport.requiredOptionKeys
      : readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
        ? readinessReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };

  const blockedBehavior =
    readinessReport && readinessReport.blockedBehavior
      ? readinessReport.blockedBehavior
      : contractPlanReport && contractPlanReport.blockedBehavior
        ? contractPlanReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    readinessReport && readinessReport.preservedSystems
      ? readinessReport.preservedSystems
      : contractPlanReport && contractPlanReport.preservedSystems
        ? contractPlanReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    readinessReport && readinessReport.safetyFlags
      ? readinessReport.safetyFlags
      : contractPlanReport && contractPlanReport.safetyFlags
        ? contractPlanReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const plannedRequiredPrimitiveFields =
    contractPlanReport &&
    Array.isArray(contractPlanReport.plannedRequiredPrimitiveFields)
      ? contractPlanReport.plannedRequiredPrimitiveFields
      : [
          "phase",
          "helperName",
          "ok",
          "reportOnly",
          "planOnly",
          "contractCreated",
          "resultObjectCreated",
          "runtimeValidationEnabled",
          "enforcementEnabled",
          "requiredOptionKeys"
        ];
  const plannedOptionalPrimitiveFields =
    contractPlanReport &&
    Array.isArray(contractPlanReport.plannedOptionalPrimitiveFields)
      ? contractPlanReport.plannedOptionalPrimitiveFields
      : [
          "reason",
          "failedRequirement",
          "status",
          "statusValue",
          "failureReason",
          "warningCount",
          "noteCount"
        ];
  const plannedStatusValues =
    contractPlanReport && Array.isArray(contractPlanReport.plannedStatusValues)
      ? contractPlanReport.plannedStatusValues
      : [
          "blocked",
          "plan-only",
          "ready-for-inert-shell",
          "not-ready-for-runtime-validation"
        ];
  const plannedFailureReasonValues =
    contractPlanReport &&
    Array.isArray(contractPlanReport.plannedFailureReasonValues)
      ? contractPlanReport.plannedFailureReasonValues
      : [
          "required-option-missing",
          "validation-not-enabled",
          "runtime-enforcement-blocked",
          "shared-state-read-blocked",
          "live-result-object-not-created"
        ];
  const plannedSafetyFields =
    contractPlanReport && Array.isArray(contractPlanReport.plannedSafetyFields)
      ? contractPlanReport.plannedSafetyFields
      : [
          "resultObjectCreated",
          "runtimeValidationEnabled",
          "enforcementEnabled",
          "sharedStateDirectlyRead",
          "sharedStateMutated",
          "liveRendererCreated",
          "callableMethodsCreated",
          "mapAttached",
          "drawingEnabled",
          "domMutated",
          "startupWired"
        ];

  if (missingKey) {
    return {
      phase: 324,
      helperName:
        "createCustom25DVisualManualRendererMetadataValidationResultContractShell",
      ok: true,
      allowed: false,
      blocked: true,
      shellOnly: true,
      inert: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractShellName:
        "custom25DVisualManualRendererMetadataValidationResultContractShell",
      contractShellCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      plannedRequiredPrimitiveFields,
      plannedOptionalPrimitiveFields,
      plannedStatusValues,
      plannedFailureReasonValues,
      plannedSafetyFields,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 324,
    helperName:
      "createCustom25DVisualManualRendererMetadataValidationResultContractShell",
    ok: true,
    allowed: true,
    blocked: false,
    shellOnly: true,
    inert: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractShellName:
      "custom25DVisualManualRendererMetadataValidationResultContractShell",
    contractShellCreated: true,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    plannedRequiredPrimitiveFields,
    plannedOptionalPrimitiveFields,
    plannedStatusValues,
    plannedFailureReasonValues,
    plannedSafetyFields,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport(
  options = {}
) {
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultContractShell(
          options
        )
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    shellResult && shellResult.blockedBehavior
      ? shellResult.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    shellResult && shellResult.preservedSystems
      ? shellResult.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    shellResult && shellResult.safetyFlags
      ? shellResult.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const concerns = [];
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell !==
    "function"
  ) {
    concerns.push("contract-shell-helper-unavailable");
  }
  if (shellResult && shellResult.validatesLiveMetadata !== false) {
    concerns.push("shell-no-longer-inert");
  }

  if (missingKey) {
    return {
      phase: 324,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
        "function",
      contractShellCreated: !!(
        shellResult && shellResult.contractShellCreated === true
      ),
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 324,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
      "function",
    contractShellCreated: !!(
      shellResult && shellResult.contractShellCreated === true
    ),
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport(
  options = {}
) {
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultContractShell(
          options
        )
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    shellResult && shellResult.blockedBehavior
      ? shellResult.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    shellResult && shellResult.preservedSystems
      ? shellResult.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    shellResult && shellResult.safetyFlags
      ? shellResult.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const concerns = [];
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell !==
    "function"
  ) {
    concerns.push("phase-324-contract-shell-helper-unavailable");
  }
  if (!(shellResult && shellResult.inert === true)) {
    concerns.push("contract-shell-not-confirmed-inert");
  }
  if (shellResult && shellResult.validatesLiveMetadata !== false) {
    concerns.push("contract-shell-appears-to-validate-live-metadata");
  }

  if (missingKey) {
    return {
      phase: 325,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport",
      ok: true,
      allowed: false,
      blocked: true,
      statusOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractShellHelperAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
        "function",
      contractShellReportAvailable: !!shellResult,
      contractShellStillInert: !!(shellResult && shellResult.inert === true),
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 325,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport",
    ok: true,
    allowed: true,
    blocked: false,
    statusOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractShellHelperAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
      "function",
    contractShellReportAvailable: !!shellResult,
    contractShellStillInert: !!(shellResult && shellResult.inert === true),
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport(
  options = {}
) {
  const statusReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport(
          options
        )
      : null;

  const requiredOptionKeys =
    statusReport && Array.isArray(statusReport.requiredOptionKeys)
      ? statusReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    statusReport && statusReport.blockedBehavior
      ? statusReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    statusReport && statusReport.preservedSystems
      ? statusReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    statusReport && statusReport.safetyFlags
      ? statusReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [
    "future-result-shell-must-stay-inert",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-disabled"
  ];
  const concerns =
    statusReport && Array.isArray(statusReport.concerns)
      ? statusReport.concerns.slice()
      : [];
  const safetyNotes = [
    "next-step-should-remain-manual-inert-planning-only",
    "do-not-create-runtime-validation-result-object-in-next-phase",
    "keep-shared-state-and-live-metadata-access-blocked"
  ];

  if (missingKey) {
    return {
      phase: 325,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFutureInertValidationResultShell: true,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      recommendedNextStep:
        "future-inert-manual-validation-result-shell-plan-only",
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 325,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFutureInertValidationResultShell: true,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    recommendedNextStep:
      "future-inert-manual-validation-result-shell-plan-only",
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport(
  options = {}
) {
  const readinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport(
          options
        )
      : null;
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultContractShell(
          options
        )
      : null;

  const requiredOptionKeys =
    readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
      ? readinessReport.requiredOptionKeys
      : shellResult && Array.isArray(shellResult.requiredOptionKeys)
        ? shellResult.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    readinessReport && readinessReport.blockedBehavior
      ? readinessReport.blockedBehavior
      : shellResult && shellResult.blockedBehavior
        ? shellResult.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    readinessReport && readinessReport.preservedSystems
      ? readinessReport.preservedSystems
      : shellResult && shellResult.preservedSystems
        ? shellResult.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    readinessReport && readinessReport.safetyFlags
      ? readinessReport.safetyFlags
      : shellResult && shellResult.safetyFlags
        ? shellResult.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const plannedResultFields = [
    "phase",
    "helperName",
    "ok",
    "reportOnly",
    "shellOnly",
    "resultCreated",
    "validationPassed",
    "validationBlocked",
    "statusValue",
    "failureReason",
    "requiredOptionKeys"
  ];
  const plannedSafetyFields =
    shellResult && Array.isArray(shellResult.plannedSafetyFields)
      ? shellResult.plannedSafetyFields
      : [
          "resultObjectCreated",
          "runtimeValidationEnabled",
          "enforcementEnabled",
          "sharedStateDirectlyRead",
          "sharedStateMutated",
          "liveRendererCreated",
          "callableMethodsCreated",
          "mapAttached",
          "drawingEnabled",
          "domMutated",
          "startupWired"
        ];
  const plannedStatusValues =
    shellResult && Array.isArray(shellResult.plannedStatusValues)
      ? shellResult.plannedStatusValues
      : [
          "blocked",
          "plan-only",
          "ready-for-inert-shell",
          "not-ready-for-runtime-validation"
        ];
  const plannedFailureReasonValues =
    shellResult && Array.isArray(shellResult.plannedFailureReasonValues)
      ? shellResult.plannedFailureReasonValues
      : [
          "required-option-missing",
          "validation-not-enabled",
          "runtime-enforcement-blocked",
          "shared-state-read-blocked",
          "live-result-object-not-created"
        ];

  if (missingKey) {
    return {
      phase: 325,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedShellName:
        "custom25DVisualManualRendererMetadataValidationResultShell",
      resultShellCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      plannedResultFields,
      plannedSafetyFields,
      plannedStatusValues,
      plannedFailureReasonValues,
      recommendedNextStep:
        "future-inert-manual-validation-result-shell-phase-only",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 325,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedShellName:
      "custom25DVisualManualRendererMetadataValidationResultShell",
    resultShellCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    plannedResultFields,
    plannedSafetyFields,
    plannedStatusValues,
    plannedFailureReasonValues,
    recommendedNextStep:
      "future-inert-manual-validation-result-shell-phase-only",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport(
  options = {}
) {
  const contractShellStatusReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport(
          options
        )
      : null;
  const contractShellReadinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport(
          options
        )
      : null;
  const resultShellPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport(
          options
        )
      : null;

  const requiredOptionKeys =
    contractShellStatusReport &&
    Array.isArray(contractShellStatusReport.requiredOptionKeys)
      ? contractShellStatusReport.requiredOptionKeys
      : contractShellReadinessReport &&
          Array.isArray(contractShellReadinessReport.requiredOptionKeys)
        ? contractShellReadinessReport.requiredOptionKeys
        : resultShellPlanReport &&
            Array.isArray(resultShellPlanReport.requiredOptionKeys)
          ? resultShellPlanReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    resultShellPlanReport && resultShellPlanReport.blockedBehavior
      ? resultShellPlanReport.blockedBehavior
      : contractShellReadinessReport && contractShellReadinessReport.blockedBehavior
        ? contractShellReadinessReport.blockedBehavior
        : contractShellStatusReport && contractShellStatusReport.blockedBehavior
          ? contractShellStatusReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    resultShellPlanReport && resultShellPlanReport.preservedSystems
      ? resultShellPlanReport.preservedSystems
      : contractShellReadinessReport && contractShellReadinessReport.preservedSystems
        ? contractShellReadinessReport.preservedSystems
        : contractShellStatusReport && contractShellStatusReport.preservedSystems
          ? contractShellStatusReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    resultShellPlanReport && resultShellPlanReport.safetyFlags
      ? resultShellPlanReport.safetyFlags
      : contractShellReadinessReport && contractShellReadinessReport.safetyFlags
        ? contractShellReadinessReport.safetyFlags
        : contractShellStatusReport && contractShellStatusReport.safetyFlags
          ? contractShellStatusReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport !==
    "function"
  ) {
    concerns.push("phase-325-contract-shell-status-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport !==
    "function"
  ) {
    concerns.push("phase-325-contract-shell-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport !==
    "function"
  ) {
    concerns.push("phase-325-result-shell-plan-report-unavailable");
  }
  if (
    resultShellPlanReport &&
    resultShellPlanReport.planOnly !== true
  ) {
    concerns.push("result-shell-plan-no-longer-plan-only");
  }

  if (missingKey) {
    return {
      phase: 326,
      phaseClosedOut: 325,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractShellStatusReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
        "function",
      contractShellReadinessReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
        "function",
      resultShellPlanReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport ===
        "function",
      resultShellPlanStillPlanOnly: true,
      resultShellCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 326,
    phaseClosedOut: 325,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractShellStatusReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
      "function",
    contractShellReadinessReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
      "function",
    resultShellPlanReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport ===
      "function",
    resultShellPlanStillPlanOnly: true,
    resultShellCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport(
  options = {}
) {
  const closeoutReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport(
          options
        )
      : null;

  const requiredOptionKeys =
    closeoutReport && Array.isArray(closeoutReport.requiredOptionKeys)
      ? closeoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeoutReport && closeoutReport.blockedBehavior
      ? closeoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeoutReport && closeoutReport.preservedSystems
      ? closeoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeoutReport && closeoutReport.safetyFlags
      ? closeoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (
    closeoutReport &&
    closeoutReport.contractShellStatusReportAvailable !== true
  ) {
    blockers.push("phase-325-status-report-unavailable");
  }
  if (
    closeoutReport &&
    closeoutReport.contractShellReadinessReportAvailable !== true
  ) {
    blockers.push("phase-325-readiness-report-unavailable");
  }
  if (
    closeoutReport &&
    closeoutReport.resultShellPlanReportAvailable !== true
  ) {
    blockers.push("phase-325-result-shell-plan-report-unavailable");
  }
  const inheritedConcerns =
    closeoutReport && Array.isArray(closeoutReport.concerns)
      ? closeoutReport.concerns
      : [];
  const concerns = inheritedConcerns.slice();
  const safetyNotes = [
    "future-result-shell-should-remain-inert",
    "future-result-shell-should-remain-manual-only",
    "runtime-and-live-metadata-validation-must-stay-blocked"
  ];

  if (missingKey) {
    return {
      phase: 326,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      sequenceReviewed: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFutureInertValidationResultShell: blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureResultShellShouldRemainInert: true,
      futureResultShellShouldBeManualOnly: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep:
        "future-inert-manual-validation-result-shell-phase-only",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 326,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    sequenceReviewed: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFutureInertValidationResultShell: blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureResultShellShouldRemainInert: true,
    futureResultShellShouldBeManualOnly: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep:
      "future-inert-manual-validation-result-shell-phase-only",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualManualRendererMetadataValidationResultShell(
  options = {}
) {
  const contractShellResult =
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultContractShell(
          options
        )
      : null;
  const selfReviewReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport(
          options
        )
      : null;

  const requiredOptionKeys =
    selfReviewReport && Array.isArray(selfReviewReport.requiredOptionKeys)
      ? selfReviewReport.requiredOptionKeys
      : contractShellResult && Array.isArray(contractShellResult.requiredOptionKeys)
        ? contractShellResult.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    selfReviewReport && selfReviewReport.blockedBehavior
      ? selfReviewReport.blockedBehavior
      : contractShellResult && contractShellResult.blockedBehavior
        ? contractShellResult.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    selfReviewReport && selfReviewReport.preservedSystems
      ? selfReviewReport.preservedSystems
      : contractShellResult && contractShellResult.preservedSystems
        ? contractShellResult.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    selfReviewReport && selfReviewReport.safetyFlags
      ? selfReviewReport.safetyFlags
      : contractShellResult && contractShellResult.safetyFlags
        ? contractShellResult.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const plannedResultFields =
    contractShellResult && Array.isArray(contractShellResult.plannedRequiredPrimitiveFields)
      ? contractShellResult.plannedRequiredPrimitiveFields.concat([
          "status",
          "reason",
          "validationExecuted"
        ])
      : [
          "phase",
          "helperName",
          "ok",
          "status",
          "reason",
          "validationExecuted",
          "runtimeValidationEnabled",
          "enforcementEnabled",
          "requiredOptionKeys"
        ];
  const plannedSafetyFields =
    contractShellResult && Array.isArray(contractShellResult.plannedSafetyFields)
      ? contractShellResult.plannedSafetyFields
      : [
          "resultObjectCreated",
          "runtimeValidationEnabled",
          "enforcementEnabled",
          "sharedStateDirectlyRead",
          "sharedStateMutated",
          "liveRendererCreated",
          "callableMethodsCreated",
          "mapAttached",
          "drawingEnabled",
          "domMutated",
          "startupWired"
        ];
  const plannedStatusValues =
    contractShellResult && Array.isArray(contractShellResult.plannedStatusValues)
      ? contractShellResult.plannedStatusValues
      : [
          "blocked",
          "plan-only",
          "ready-for-inert-shell",
          "not-ready-for-runtime-validation"
        ];
  const plannedFailureReasonValues =
    contractShellResult && Array.isArray(contractShellResult.plannedFailureReasonValues)
      ? contractShellResult.plannedFailureReasonValues
      : [
          "required-option-missing",
          "validation-not-enabled",
          "runtime-enforcement-blocked",
          "shared-state-read-blocked",
          "live-result-object-not-created"
        ];
  const samplePrimitiveResultShape = {
    ok: false,
    status: "not-run",
    reason: "metadata-validation-not-executed",
    validationExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false
  };

  if (missingKey) {
    return {
      phase: 327,
      helperName:
        "createCustom25DVisualManualRendererMetadataValidationResultShell",
      ok: true,
      allowed: false,
      blocked: true,
      shellOnly: true,
      inert: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellName:
        "custom25DVisualManualRendererMetadataValidationResultShell",
      resultShellCreated: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      plannedResultFields,
      plannedSafetyFields,
      plannedStatusValues,
      plannedFailureReasonValues,
      samplePrimitiveResultShape,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 327,
    helperName:
      "createCustom25DVisualManualRendererMetadataValidationResultShell",
    ok: true,
    allowed: true,
    blocked: false,
    shellOnly: true,
    inert: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellName:
      "custom25DVisualManualRendererMetadataValidationResultShell",
    resultShellCreated: true,
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    plannedResultFields,
    plannedSafetyFields,
    plannedStatusValues,
    plannedFailureReasonValues,
    samplePrimitiveResultShape,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport(
  options = {}
) {
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultShell(options)
      : null;

  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    shellResult && shellResult.blockedBehavior
      ? shellResult.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    shellResult && shellResult.preservedSystems
      ? shellResult.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    shellResult && shellResult.safetyFlags
      ? shellResult.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const concerns = [];
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell !==
    "function"
  ) {
    concerns.push("result-shell-helper-unavailable");
  }
  if (shellResult && shellResult.validatesLiveMetadata !== false) {
    concerns.push("result-shell-appears-to-validate-live-metadata");
  }
  if (shellResult && shellResult.validationExecuted !== false) {
    concerns.push("result-shell-appears-to-execute-validation");
  }

  if (missingKey) {
    return {
      phase: 327,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellHelperAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
        "function",
      resultShellReportAvailable: !!shellResult,
      resultShellCreated: !!(shellResult && shellResult.resultShellCreated === true),
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 327,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellHelperAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
      "function",
    resultShellReportAvailable: !!shellResult,
    resultShellCreated: !!(shellResult && shellResult.resultShellCreated === true),
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport(
  options = {}
) {
  const resultShell =
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultShell(options)
      : null;

  const requiredOptionKeys =
    resultShell && Array.isArray(resultShell.requiredOptionKeys)
      ? resultShell.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    resultShell && resultShell.blockedBehavior
      ? resultShell.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    resultShell && resultShell.preservedSystems
      ? resultShell.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    resultShell && resultShell.safetyFlags
      ? resultShell.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const concerns = [];
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell !==
    "function"
  ) {
    concerns.push("phase-327-result-shell-helper-unavailable");
  }
  if (!(resultShell && resultShell.inert === true)) {
    concerns.push("result-shell-not-confirmed-inert");
  }
  if (resultShell && resultShell.validatesLiveMetadata !== false) {
    concerns.push("result-shell-appears-to-validate-live-metadata");
  }
  if (resultShell && resultShell.validationExecuted !== false) {
    concerns.push("result-shell-appears-to-execute-validation");
  }

  if (missingKey) {
    return {
      phase: 328,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport",
      ok: true,
      allowed: false,
      blocked: true,
      statusOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellHelperAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
        "function",
      resultShellReportAvailable: !!resultShell,
      resultShellStillInert: !!(resultShell && resultShell.inert === true),
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 328,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport",
    ok: true,
    allowed: true,
    blocked: false,
    statusOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellHelperAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
      "function",
    resultShellReportAvailable: !!resultShell,
    resultShellStillInert: !!(resultShell && resultShell.inert === true),
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport(
  options = {}
) {
  const statusReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport(
          options
        )
      : null;

  const requiredOptionKeys =
    statusReport && Array.isArray(statusReport.requiredOptionKeys)
      ? statusReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    statusReport && statusReport.blockedBehavior
      ? statusReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    statusReport && statusReport.preservedSystems
      ? statusReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    statusReport && statusReport.safetyFlags
      ? statusReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [
    "future-review-step-must-remain-passive",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-disabled"
  ];
  const concerns =
    statusReport && Array.isArray(statusReport.concerns)
      ? statusReport.concerns.slice()
      : [];
  const safetyNotes = [
    "next-step-should-stay-passive-and-report-only",
    "do-not-create-validator-or-review-helper-yet",
    "keep-live-metadata-and-runtime-validation-blocked"
  ];

  if (missingKey) {
    return {
      phase: 328,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFuturePassiveResultReview: true,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      recommendedNextStep: "future-passive-result-review-plan-only",
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 328,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFuturePassiveResultReview: true,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    recommendedNextStep: "future-passive-result-review-plan-only",
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport(
  options = {}
) {
  const readinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport(
          options
        )
      : null;
  const resultShell =
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultShell(options)
      : null;

  const requiredOptionKeys =
    readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
      ? readinessReport.requiredOptionKeys
      : resultShell && Array.isArray(resultShell.requiredOptionKeys)
        ? resultShell.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    readinessReport && readinessReport.blockedBehavior
      ? readinessReport.blockedBehavior
      : resultShell && resultShell.blockedBehavior
        ? resultShell.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    readinessReport && readinessReport.preservedSystems
      ? readinessReport.preservedSystems
      : resultShell && resultShell.preservedSystems
        ? resultShell.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    readinessReport && readinessReport.safetyFlags
      ? readinessReport.safetyFlags
      : resultShell && resultShell.safetyFlags
        ? resultShell.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const plannedReviewChecks = [
    "confirm-shell-remains-inert",
    "confirm-sample-result-shape-stays-primitive-only",
    "confirm-runtime-validation-stays-disabled",
    "confirm-live-metadata-validation-stays-disabled",
    "confirm-no-shared-state-or-renderer-side-effects"
  ];
  const plannedSafetyChecks = [
    "resultSystemCreated-false",
    "resultObjectCreated-false",
    "runtimeValidationEnabled-false",
    "enforcementEnabled-false",
    "validatesLiveMetadata-false",
    "validationExecuted-false"
  ];

  if (missingKey) {
    return {
      phase: 328,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedReviewName:
        "custom25DVisualManualRendererMetadataValidationResultReview",
      reviewCreated: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      plannedReviewChecks,
      plannedSafetyChecks,
      recommendedNextStep: "future-passive-result-review-phase-only",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 328,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedReviewName:
      "custom25DVisualManualRendererMetadataValidationResultReview",
    reviewCreated: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    plannedReviewChecks,
    plannedSafetyChecks,
    recommendedNextStep: "future-passive-result-review-phase-only",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport(
  options = {}
) {
  const statusReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport(
          options
        )
      : null;
  const readinessReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport(
          options
        )
      : null;
  const reviewPlanReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport(
          options
        )
      : null;
  const requiredOptionKeys =
    reviewPlanReport && Array.isArray(reviewPlanReport.requiredOptionKeys)
      ? reviewPlanReport.requiredOptionKeys
      : readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
        ? readinessReport.requiredOptionKeys
        : statusReport && Array.isArray(statusReport.requiredOptionKeys)
          ? statusReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    reviewPlanReport && reviewPlanReport.blockedBehavior
      ? reviewPlanReport.blockedBehavior
      : readinessReport && readinessReport.blockedBehavior
        ? readinessReport.blockedBehavior
        : statusReport && statusReport.blockedBehavior
          ? statusReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    reviewPlanReport && reviewPlanReport.preservedSystems
      ? reviewPlanReport.preservedSystems
      : readinessReport && readinessReport.preservedSystems
        ? readinessReport.preservedSystems
        : statusReport && statusReport.preservedSystems
          ? statusReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    reviewPlanReport && reviewPlanReport.safetyFlags
      ? reviewPlanReport.safetyFlags
      : readinessReport && readinessReport.safetyFlags
        ? readinessReport.safetyFlags
        : statusReport && statusReport.safetyFlags
          ? statusReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const concerns = [
    "phase-328-review-plan-remains-passive-only",
    "future-review-helper-not-created-yet",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-blocked"
  ];

  if (missingKey) {
    return {
      phase: 329,
      phaseClosedOut: 328,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellStatusReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
        "function",
      resultShellReadinessReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
        "function",
      resultReviewPlanReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport ===
        "function",
      resultReviewPlanStillPlanOnly: true,
      reviewHelperCreated: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 329,
    phaseClosedOut: 328,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellStatusReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
      "function",
    resultShellReadinessReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
      "function",
    resultReviewPlanReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport ===
      "function",
    resultReviewPlanStillPlanOnly: true,
    reviewHelperCreated: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport(
  options = {}
) {
  const closeoutReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    closeoutReport && Array.isArray(closeoutReport.requiredOptionKeys)
      ? closeoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeoutReport && closeoutReport.blockedBehavior
      ? closeoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeoutReport && closeoutReport.preservedSystems
      ? closeoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeoutReport && closeoutReport.safetyFlags
      ? closeoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (
    !closeoutReport ||
    closeoutReport.resultShellStatusReportAvailable !== true ||
    closeoutReport.resultShellReadinessReportAvailable !== true ||
    closeoutReport.resultReviewPlanReportAvailable !== true
  ) {
    blockers.push("phase-328-helper-availability-incomplete");
  }
  const safetyNotes = [
    "future-review-helper-should-remain-passive",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked",
    "future-result-review-work-should-stay-primitive-report-only"
  ];
  const concerns = [
    "sequence-still-planning-only",
    "future-runtime-enforcement-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-result-review-helper-plan";

  if (missingKey) {
    return {
      phase: 329,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      readyForFuturePassiveResultReviewHelper: blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureReviewShouldRemainPassive: true,
      futureReviewShouldNotValidateLiveMetadata: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 329,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    readyForFuturePassiveResultReviewHelper: blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureReviewShouldRemainPassive: true,
    futureReviewShouldNotValidateLiveMetadata: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport(
  options = {}
) {
  const shellResult =
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultShell(options)
      : null;
  const selfReviewReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    shellResult && Array.isArray(shellResult.requiredOptionKeys)
      ? shellResult.requiredOptionKeys
      : selfReviewReport && Array.isArray(selfReviewReport.requiredOptionKeys)
        ? selfReviewReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    shellResult && shellResult.blockedBehavior
      ? shellResult.blockedBehavior
      : selfReviewReport && selfReviewReport.blockedBehavior
        ? selfReviewReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    shellResult && shellResult.preservedSystems
      ? shellResult.preservedSystems
      : selfReviewReport && selfReviewReport.preservedSystems
        ? selfReviewReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    shellResult && shellResult.safetyFlags
      ? shellResult.safetyFlags
      : selfReviewReport && selfReviewReport.safetyFlags
        ? selfReviewReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const requiredPrimitiveFields = [
    "phase",
    "helperName",
    "ok",
    "status",
    "reason",
    "validationExecuted",
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "requiredOptionKeys"
  ];
  const safetyFields = Array.isArray(shellResult && shellResult.plannedSafetyFields)
    ? shellResult.plannedSafetyFields
    : [
        "resultObjectCreated",
        "runtimeValidationEnabled",
        "enforcementEnabled",
        "sharedStateDirectlyRead",
        "sharedStateMutated",
        "liveRendererCreated",
        "callableMethodsCreated",
        "mapAttached",
        "drawingEnabled",
        "domMutated",
        "startupWired"
      ];
  const statusValues = Array.isArray(shellResult && shellResult.plannedStatusValues)
    ? shellResult.plannedStatusValues
    : [];
  const failureReasonValues = Array.isArray(
    shellResult && shellResult.plannedFailureReasonValues
  )
    ? shellResult.plannedFailureReasonValues
    : [];
  const resultShellShapeReviewed = !!shellResult;
  const requiredPrimitiveFieldsPresent = requiredPrimitiveFields.every((field) =>
    Array.isArray(shellResult && shellResult.plannedResultFields)
      ? shellResult.plannedResultFields.includes(field)
      : false
  );
  const safetyFieldsPresent = safetyFields.length > 0;
  const statusValuesPresent = statusValues.length > 0;
  const failureReasonValuesPresent = failureReasonValues.length > 0;
  const blockers = [];
  if (typeof createCustom25DVisualManualRendererMetadataValidationResultShell !== "function") {
    blockers.push("phase-327-result-shell-helper-unavailable");
  }
  if (!requiredPrimitiveFieldsPresent) {
    blockers.push("required-primitive-fields-incomplete");
  }
  if (!safetyFieldsPresent) {
    blockers.push("safety-fields-missing");
  }
  if (!statusValuesPresent) {
    blockers.push("status-values-missing");
  }
  if (!failureReasonValuesPresent) {
    blockers.push("failure-reason-values-missing");
  }
  const concerns = [
    "review-is-limited-to-primitive-shell-shape-only",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-blocked"
  ];
  const safetyNotes = [
    "shell-review-must-not-create-a-result-system",
    "shell-review-must-not-read-shared-renderer-state",
    "shell-review-must-not-enforce-runtime-validation"
  ];

  if (missingKey) {
    return {
      phase: 330,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      reviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellHelperAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
        "function",
      resultShellShapeReviewed,
      requiredPrimitiveFieldsPresent,
      safetyFieldsPresent,
      statusValuesPresent,
      failureReasonValuesPresent,
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 330,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    reviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellHelperAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
      "function",
    resultShellShapeReviewed,
    requiredPrimitiveFieldsPresent,
    safetyFieldsPresent,
    statusValuesPresent,
    failureReasonValuesPresent,
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport(
  options = {}
) {
  const reviewReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    reviewReport && Array.isArray(reviewReport.requiredOptionKeys)
      ? reviewReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    reviewReport && reviewReport.blockedBehavior
      ? reviewReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    reviewReport && reviewReport.preservedSystems
      ? reviewReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    reviewReport && reviewReport.safetyFlags
      ? reviewReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const concerns = [];
  if (!reviewReport) {
    concerns.push("result-shell-review-report-unavailable");
  }
  if (reviewReport && reviewReport.resultShellShapeReviewed !== true) {
    concerns.push("result-shell-shape-review-incomplete");
  }
  if (reviewReport && Array.isArray(reviewReport.blockers) && reviewReport.blockers.length > 0) {
    concerns.push("result-shell-review-has-blockers");
  }

  if (missingKey) {
    return {
      phase: 330,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellReviewAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
        "function",
      resultShellReviewCompleted: !!(reviewReport && reviewReport.resultShellShapeReviewed),
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 330,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellReviewAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
      "function",
    resultShellReviewCompleted: !!(reviewReport && reviewReport.resultShellShapeReviewed),
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport(
  options = {}
) {
  const reviewReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport(
          options
        )
      : null;
  const reviewCloseoutReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    reviewCloseoutReport && Array.isArray(reviewCloseoutReport.requiredOptionKeys)
      ? reviewCloseoutReport.requiredOptionKeys
      : reviewReport && Array.isArray(reviewReport.requiredOptionKeys)
        ? reviewReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    reviewCloseoutReport && reviewCloseoutReport.blockedBehavior
      ? reviewCloseoutReport.blockedBehavior
      : reviewReport && reviewReport.blockedBehavior
        ? reviewReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    reviewCloseoutReport && reviewCloseoutReport.preservedSystems
      ? reviewCloseoutReport.preservedSystems
      : reviewReport && reviewReport.preservedSystems
        ? reviewReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    reviewCloseoutReport && reviewCloseoutReport.safetyFlags
      ? reviewCloseoutReport.safetyFlags
      : reviewReport && reviewReport.safetyFlags
        ? reviewReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const resultShellReviewCompleted = !!(
    reviewReport &&
    reviewReport.resultShellShapeReviewed === true &&
    reviewCloseoutReport &&
    reviewCloseoutReport.resultShellReviewCompleted === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport !==
    "function"
  ) {
    concerns.push("result-shell-review-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport !==
    "function"
  ) {
    concerns.push("result-shell-review-closeout-report-unavailable");
  }
  if (!resultShellReviewCompleted) {
    concerns.push("result-shell-review-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 331,
      phaseClosedOut: 330,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      resultShellReviewReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
        "function",
      resultShellReviewCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport ===
        "function",
      resultShellReviewCompleted,
      resultSystemCreated: false,
      resultObjectCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 331,
    phaseClosedOut: 330,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    resultShellReviewReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
      "function",
    resultShellReviewCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport ===
      "function",
    resultShellReviewCompleted,
    resultSystemCreated: false,
    resultObjectCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const resultShellReviewed = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.resultShellReviewCompleted === true
  );
  const blockers = [];
  if (!resultShellReviewed) {
    blockers.push("result-shell-review-sequence-not-complete");
  }
  const safetyNotes = [
    "future-validation-planning-must-remain-passive",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked",
    "future-work-must-not-create-a-runtime-result-system"
  ];
  const concerns = [
    "sequence-remains-review-only",
    "non-runtime-validation-planning-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-non-runtime-validation-planning-phase";

  if (missingKey) {
    return {
      phase: 331,
      helperName:
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      resultShellReviewed,
      readyForFutureNonRuntimeValidationPlanning: blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureValidationPlanningShouldRemainPassive: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 331,
    helperName:
      "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    resultShellReviewed,
    readyForFutureNonRuntimeValidationPlanning: blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureValidationPlanningShouldRemainPassive: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererNonRuntimeValidationPlanReport(
  options = {}
) {
  const sequenceSelfReviewReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport(
          options
        )
      : null;
  const shellReviewReport =
    typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceSelfReviewReport &&
    Array.isArray(sequenceSelfReviewReport.requiredOptionKeys)
      ? sequenceSelfReviewReport.requiredOptionKeys
      : shellReviewReport && Array.isArray(shellReviewReport.requiredOptionKeys)
        ? shellReviewReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceSelfReviewReport && sequenceSelfReviewReport.blockedBehavior
      ? sequenceSelfReviewReport.blockedBehavior
      : shellReviewReport && shellReviewReport.blockedBehavior
        ? shellReviewReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    sequenceSelfReviewReport && sequenceSelfReviewReport.preservedSystems
      ? sequenceSelfReviewReport.preservedSystems
      : shellReviewReport && shellReviewReport.preservedSystems
        ? shellReviewReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    sequenceSelfReviewReport && sequenceSelfReviewReport.safetyFlags
      ? sequenceSelfReviewReport.safetyFlags
      : shellReviewReport && shellReviewReport.safetyFlags
        ? shellReviewReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const plannedPrimitiveShapeChecks = [
    "compare-required-primitive-fields-presence",
    "compare-safety-fields-presence",
    "compare-status-values-presence",
    "compare-failure-reason-values-presence",
    "confirm-snapshot-report-shape-remains-primitive-only"
  ];
  const plannedSafetyChecks = [
    "runtimeValidationEnabled-false",
    "enforcementEnabled-false",
    "validatesLiveMetadata-false",
    "validationExecuted-false",
    "sharedStateDirectlyRead-false",
    "sharedStateMutated-false"
  ];
  const plannedBlockedRuntimeChecks = [
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-blocked",
    "runtime-result-system-must-not-be-created",
    "validator-helper-must-not-be-created"
  ];
  const blockers = [];
  if (
    !sequenceSelfReviewReport ||
    sequenceSelfReviewReport.readyForFutureNonRuntimeValidationPlanning !== true
  ) {
    blockers.push("prior-review-sequence-not-ready");
  }
  const concerns = [
    "future-non-runtime-review-must-only-compare-primitive-report-shapes",
    "non-runtime-validation-plan-must-not-turn-into-runtime-enforcement"
  ];
  const recommendedNextStep = "future-passive-non-runtime-validation-review-helper-plan";

  if (missingKey) {
    return {
      phase: 332,
      helperName: "getCustom25DVisualManualRendererNonRuntimeValidationPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedReviewName: "custom25DVisualManualRendererNonRuntimeValidationReview",
      plannedReviewCreated: false,
      nonRuntimeOnly: true,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      plannedPrimitiveShapeChecks,
      plannedSafetyChecks,
      plannedBlockedRuntimeChecks,
      recommendedNextStep,
      blockers,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 332,
    helperName: "getCustom25DVisualManualRendererNonRuntimeValidationPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedReviewName: "custom25DVisualManualRendererNonRuntimeValidationReview",
    plannedReviewCreated: false,
    nonRuntimeOnly: true,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    plannedPrimitiveShapeChecks,
    plannedSafetyChecks,
    plannedBlockedRuntimeChecks,
    recommendedNextStep,
    blockers,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport(
  options = {}
) {
  const planReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationPlanReport(options)
      : null;
  const requiredOptionKeys =
    planReport && Array.isArray(planReport.requiredOptionKeys)
      ? planReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    planReport && planReport.blockedBehavior
      ? planReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    planReport && planReport.preservedSystems
      ? planReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    planReport && planReport.safetyFlags
      ? planReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!planReport || (Array.isArray(planReport.blockers) && planReport.blockers.length > 0)) {
    blockers.push("non-runtime-validation-plan-not-ready");
  }
  const safetyNotes = [
    "future-review-helper-must-remain-passive",
    "future-review-helper-must-use-only-primitive-report-shapes",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked"
  ];
  const concerns = [
    "readiness-does-not-authorize-runtime-validation",
    "future-helper-must-not-read-live-shared-state"
  ];
  const recommendedNextStep = "future-passive-non-runtime-validation-review-helper-plan";

  if (missingKey) {
    return {
      phase: 332,
      helperName:
        "getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFutureNonRuntimeValidationReviewHelper: blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureReviewShouldRemainPassive: true,
      futureReviewShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 332,
    helperName:
      "getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFutureNonRuntimeValidationReviewHelper: blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureReviewShouldRemainPassive: true,
    futureReviewShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport(
  options = {}
) {
  const planReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationPlanReport(options)
      : null;
  const readinessReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport(
          options
        )
      : null;
  const requiredOptionKeys =
    readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
      ? readinessReport.requiredOptionKeys
      : planReport && Array.isArray(planReport.requiredOptionKeys)
        ? planReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    readinessReport && readinessReport.blockedBehavior
      ? readinessReport.blockedBehavior
      : planReport && planReport.blockedBehavior
        ? planReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    readinessReport && readinessReport.preservedSystems
      ? readinessReport.preservedSystems
      : planReport && planReport.preservedSystems
        ? planReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    readinessReport && readinessReport.safetyFlags
      ? readinessReport.safetyFlags
      : planReport && planReport.safetyFlags
        ? planReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [];
  if (!planReport) {
    concerns.push("non-runtime-validation-plan-report-unavailable");
  }
  if (!readinessReport) {
    concerns.push("non-runtime-validation-readiness-report-unavailable");
  }
  if (planReport && Array.isArray(planReport.blockers) && planReport.blockers.length > 0) {
    concerns.push("non-runtime-validation-plan-has-blockers");
  }

  if (missingKey) {
    return {
      phase: 332,
      helperName:
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      nonRuntimeValidationPlanAvailable:
        typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
        "function",
      nonRuntimeValidationReadinessAvailable:
        typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
        "function",
      plannedReviewCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 332,
    helperName:
      "getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    nonRuntimeValidationPlanAvailable:
      typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
      "function",
    nonRuntimeValidationReadinessAvailable:
      typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
      "function",
    plannedReviewCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport(
  options = {}
) {
  const planReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationPlanReport(options)
      : null;
  const readinessReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport(
          options
        )
      : null;
  const planCloseoutReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    planCloseoutReport && Array.isArray(planCloseoutReport.requiredOptionKeys)
      ? planCloseoutReport.requiredOptionKeys
      : readinessReport && Array.isArray(readinessReport.requiredOptionKeys)
        ? readinessReport.requiredOptionKeys
        : planReport && Array.isArray(planReport.requiredOptionKeys)
          ? planReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    planCloseoutReport && planCloseoutReport.blockedBehavior
      ? planCloseoutReport.blockedBehavior
      : readinessReport && readinessReport.blockedBehavior
        ? readinessReport.blockedBehavior
        : planReport && planReport.blockedBehavior
          ? planReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    planCloseoutReport && planCloseoutReport.preservedSystems
      ? planCloseoutReport.preservedSystems
      : readinessReport && readinessReport.preservedSystems
        ? readinessReport.preservedSystems
        : planReport && planReport.preservedSystems
          ? planReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    planCloseoutReport && planCloseoutReport.safetyFlags
      ? planCloseoutReport.safetyFlags
      : readinessReport && readinessReport.safetyFlags
        ? readinessReport.safetyFlags
        : planReport && planReport.safetyFlags
          ? planReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const nonRuntimeValidationPlanComplete = !!(
    planReport &&
    readinessReport &&
    planCloseoutReport &&
    Array.isArray(planReport.blockers) &&
    planReport.blockers.length === 0 &&
    readinessReport.readyForFutureNonRuntimeValidationReviewHelper === true &&
    planCloseoutReport.nonRuntimeValidationPlanAvailable === true &&
    planCloseoutReport.nonRuntimeValidationReadinessAvailable === true
  );
  const concerns = [];
  if (typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport !== "function") {
    concerns.push("non-runtime-validation-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport !==
    "function"
  ) {
    concerns.push("non-runtime-validation-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport !==
    "function"
  ) {
    concerns.push("non-runtime-validation-plan-closeout-report-unavailable");
  }
  if (!nonRuntimeValidationPlanComplete) {
    concerns.push("non-runtime-validation-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 333,
      phaseClosedOut: 332,
      helperName:
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      nonRuntimeValidationPlanReportAvailable:
        typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
        "function",
      nonRuntimeValidationReadinessReportAvailable:
        typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
        "function",
      nonRuntimeValidationPlanCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport ===
        "function",
      nonRuntimeValidationPlanComplete,
      futureReviewHelperCreated: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 333,
    phaseClosedOut: 332,
    helperName:
      "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    nonRuntimeValidationPlanReportAvailable:
      typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
      "function",
    nonRuntimeValidationReadinessReportAvailable:
      typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
      "function",
    nonRuntimeValidationPlanCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport ===
      "function",
    nonRuntimeValidationPlanComplete,
    futureReviewHelperCreated: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (
    !sequenceCloseoutReport ||
    sequenceCloseoutReport.nonRuntimeValidationPlanComplete !== true
  ) {
    blockers.push("non-runtime-validation-plan-sequence-not-complete");
  }
  const safetyNotes = [
    "future-review-helper-must-remain-passive",
    "future-review-helper-must-use-only-primitive-report-shapes",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-non-runtime-review-helper-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-non-runtime-validation-review-helper-phase";

  if (missingKey) {
    return {
      phase: 333,
      helperName:
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      readyForFuturePassiveNonRuntimeValidationReviewHelper:
        blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureReviewShouldRemainPassive: true,
      futureReviewShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 333,
    helperName:
      "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    readyForFuturePassiveNonRuntimeValidationReviewHelper:
      blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureReviewShouldRemainPassive: true,
    futureReviewShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
  options = {}
) {
  const contractShell =
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultContractShell(
          options
        )
      : null;
  const resultShell =
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultShell(options)
      : null;
  const planningSelfReview =
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    planningSelfReview && Array.isArray(planningSelfReview.requiredOptionKeys)
      ? planningSelfReview.requiredOptionKeys
      : resultShell && Array.isArray(resultShell.requiredOptionKeys)
        ? resultShell.requiredOptionKeys
        : contractShell && Array.isArray(contractShell.requiredOptionKeys)
          ? contractShell.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    planningSelfReview && planningSelfReview.blockedBehavior
      ? planningSelfReview.blockedBehavior
      : resultShell && resultShell.blockedBehavior
        ? resultShell.blockedBehavior
        : contractShell && contractShell.blockedBehavior
          ? contractShell.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    planningSelfReview && planningSelfReview.preservedSystems
      ? planningSelfReview.preservedSystems
      : resultShell && resultShell.preservedSystems
        ? resultShell.preservedSystems
        : contractShell && contractShell.preservedSystems
          ? contractShell.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    planningSelfReview && planningSelfReview.safetyFlags
      ? planningSelfReview.safetyFlags
      : resultShell && resultShell.safetyFlags
        ? resultShell.safetyFlags
        : contractShell && contractShell.safetyFlags
          ? contractShell.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const contractRequiredFields = Array.isArray(
    contractShell && contractShell.plannedRequiredPrimitiveFields
  )
    ? contractShell.plannedRequiredPrimitiveFields
    : [];
  const resultPlannedFields = Array.isArray(resultShell && resultShell.plannedResultFields)
    ? resultShell.plannedResultFields
    : [];
  const contractSafetyFields = Array.isArray(
    contractShell && contractShell.plannedSafetyFields
  )
    ? contractShell.plannedSafetyFields
    : [];
  const resultSafetyFields = Array.isArray(resultShell && resultShell.plannedSafetyFields)
    ? resultShell.plannedSafetyFields
    : [];
  const contractStatusValues = Array.isArray(
    contractShell && contractShell.plannedStatusValues
  )
    ? contractShell.plannedStatusValues
    : [];
  const resultStatusValues = Array.isArray(resultShell && resultShell.plannedStatusValues)
    ? resultShell.plannedStatusValues
    : [];
  const contractFailureReasonValues = Array.isArray(
    contractShell && contractShell.plannedFailureReasonValues
  )
    ? contractShell.plannedFailureReasonValues
    : [];
  const resultFailureReasonValues = Array.isArray(
    resultShell && resultShell.plannedFailureReasonValues
  )
    ? resultShell.plannedFailureReasonValues
    : [];
  const primitiveShapeCompared = !!(contractShell && resultShell);
  const plannedRequiredFieldsCovered =
    contractRequiredFields.length > 0 &&
    contractRequiredFields.every((field) => resultPlannedFields.includes(field));
  const plannedSafetyFieldsCovered =
    contractSafetyFields.length > 0 &&
    contractSafetyFields.every((field) => resultSafetyFields.includes(field));
  const plannedStatusValuesCovered =
    contractStatusValues.length > 0 &&
    contractStatusValues.every((value) => resultStatusValues.includes(value));
  const plannedFailureReasonValuesCovered =
    contractFailureReasonValues.length > 0 &&
    contractFailureReasonValues.every((value) =>
      resultFailureReasonValues.includes(value)
    );
  const blockers = [];
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell !==
    "function"
  ) {
    blockers.push("contract-shell-helper-unavailable");
  }
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell !==
    "function"
  ) {
    blockers.push("result-shell-helper-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport !==
    "function"
  ) {
    blockers.push("planning-self-review-unavailable");
  }
  if (!primitiveShapeCompared) {
    blockers.push("primitive-shape-comparison-not-available");
  }
  if (!plannedRequiredFieldsCovered) {
    blockers.push("planned-required-fields-not-fully-covered");
  }
  if (!plannedSafetyFieldsCovered) {
    blockers.push("planned-safety-fields-not-fully-covered");
  }
  if (!plannedStatusValuesCovered) {
    blockers.push("planned-status-values-not-fully-covered");
  }
  if (!plannedFailureReasonValuesCovered) {
    blockers.push("planned-failure-reason-values-not-fully-covered");
  }
  const concerns = [
    "comparison-must-remain-shape-only",
    "passive-non-runtime-review-must-not-become-runtime-enforcement"
  ];
  const safetyNotes = [
    "only-primitive-report-shapes-may-be-compared",
    "live-metadata-validation-must-stay-blocked",
    "runtime-validation-must-stay-disabled",
    "shared-renderer-state-must-not-be-read"
  ];

  if (missingKey) {
    return {
      phase: 334,
      helperName:
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      reviewOnly: true,
      reportOnly: true,
      passiveNonRuntimeOnly: true,
      shapeOnlyComparison: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
        "function",
      resultShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
        "function",
      planningSelfReviewAvailable:
        typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport ===
        "function",
      primitiveShapeCompared,
      plannedRequiredFieldsCovered,
      plannedSafetyFieldsCovered,
      plannedStatusValuesCovered,
      plannedFailureReasonValuesCovered,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 334,
    helperName:
      "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    reviewOnly: true,
    reportOnly: true,
    passiveNonRuntimeOnly: true,
    shapeOnlyComparison: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
      "function",
    resultShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
      "function",
    planningSelfReviewAvailable:
      typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport ===
      "function",
    primitiveShapeCompared,
    plannedRequiredFieldsCovered,
    plannedSafetyFieldsCovered,
    plannedStatusValuesCovered,
    plannedFailureReasonValuesCovered,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport(
  options = {}
) {
  const passiveReviewReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    passiveReviewReport && Array.isArray(passiveReviewReport.requiredOptionKeys)
      ? passiveReviewReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveReviewReport && passiveReviewReport.blockedBehavior
      ? passiveReviewReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    passiveReviewReport && passiveReviewReport.preservedSystems
      ? passiveReviewReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    passiveReviewReport && passiveReviewReport.safetyFlags
      ? passiveReviewReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const passiveReviewCompleted = !!(
    passiveReviewReport &&
    passiveReviewReport.primitiveShapeCompared === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport !==
    "function"
  ) {
    concerns.push("passive-review-report-unavailable");
  }
  if (!passiveReviewCompleted) {
    concerns.push("passive-review-not-complete");
  }
  if (
    passiveReviewReport &&
    Array.isArray(passiveReviewReport.blockers) &&
    passiveReviewReport.blockers.length > 0
  ) {
    concerns.push("passive-review-has-blockers");
  }

  if (missingKey) {
    return {
      phase: 334,
      helperName:
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveReviewAvailable:
        typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
        "function",
      passiveReviewCompleted,
      shapeOnlyComparison: !!(
        passiveReviewReport && passiveReviewReport.shapeOnlyComparison === true
      ),
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 334,
    helperName:
      "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveReviewAvailable:
      typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
      "function",
    passiveReviewCompleted,
    shapeOnlyComparison: !!(
      passiveReviewReport && passiveReviewReport.shapeOnlyComparison === true
    ),
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport(
  options = {}
) {
  const passiveReviewReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
          options
        )
      : null;
  const passiveReviewCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    passiveReviewCloseoutReport &&
    Array.isArray(passiveReviewCloseoutReport.requiredOptionKeys)
      ? passiveReviewCloseoutReport.requiredOptionKeys
      : passiveReviewReport && Array.isArray(passiveReviewReport.requiredOptionKeys)
        ? passiveReviewReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveReviewCloseoutReport && passiveReviewCloseoutReport.blockedBehavior
      ? passiveReviewCloseoutReport.blockedBehavior
      : passiveReviewReport && passiveReviewReport.blockedBehavior
        ? passiveReviewReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    passiveReviewCloseoutReport && passiveReviewCloseoutReport.preservedSystems
      ? passiveReviewCloseoutReport.preservedSystems
      : passiveReviewReport && passiveReviewReport.preservedSystems
        ? passiveReviewReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    passiveReviewCloseoutReport && passiveReviewCloseoutReport.safetyFlags
      ? passiveReviewCloseoutReport.safetyFlags
      : passiveReviewReport && passiveReviewReport.safetyFlags
        ? passiveReviewReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const passiveReviewCompleted = !!(
    passiveReviewReport &&
    passiveReviewCloseoutReport &&
    passiveReviewReport.primitiveShapeCompared === true &&
    passiveReviewCloseoutReport.passiveReviewCompleted === true
  );
  const shapeOnlyComparisonConfirmed = !!(
    passiveReviewReport &&
    passiveReviewReport.shapeOnlyComparison === true &&
    passiveReviewCloseoutReport &&
    passiveReviewCloseoutReport.shapeOnlyComparison === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport !==
    "function"
  ) {
    concerns.push("passive-review-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-review-closeout-report-unavailable");
  }
  if (!passiveReviewCompleted) {
    concerns.push("passive-review-sequence-incomplete");
  }
  if (!shapeOnlyComparisonConfirmed) {
    concerns.push("shape-only-comparison-not-confirmed");
  }

  if (missingKey) {
    return {
      phase: 335,
      phaseClosedOut: 334,
      helperName:
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveReviewReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
        "function",
      passiveReviewCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport ===
        "function",
      passiveReviewCompleted,
      shapeOnlyComparisonConfirmed,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 335,
    phaseClosedOut: 334,
    helperName:
      "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveReviewReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
      "function",
    passiveReviewCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport ===
      "function",
    passiveReviewCompleted,
    shapeOnlyComparisonConfirmed,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const passiveReviewSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.passiveReviewCompleted === true &&
    sequenceCloseoutReport.shapeOnlyComparisonConfirmed === true
  );
  if (!passiveReviewSequenceComplete) {
    blockers.push("passive-non-runtime-validation-review-sequence-not-complete");
  }
  const safetyNotes = [
    "future-work-must-remain-passive",
    "future-work-must-use-only-primitive-report-shapes",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-passive-validation-checklist-planning-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-validation-checklist-planning-phase";

  if (missingKey) {
    return {
      phase: 335,
      helperName:
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      passiveReviewSequenceComplete,
      readyForFuturePassiveValidationChecklistPlanning:
        blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureWorkShouldRemainPassive: true,
      futureWorkShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 335,
    helperName:
      "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    passiveReviewSequenceComplete,
    readyForFuturePassiveValidationChecklistPlanning:
      blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureWorkShouldRemainPassive: true,
    futureWorkShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport(
  options = {}
) {
  const sequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveReviewReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceSelfReview && Array.isArray(sequenceSelfReview.requiredOptionKeys)
      ? sequenceSelfReview.requiredOptionKeys
      : passiveReviewReport && Array.isArray(passiveReviewReport.requiredOptionKeys)
        ? passiveReviewReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceSelfReview && sequenceSelfReview.blockedBehavior
      ? sequenceSelfReview.blockedBehavior
      : passiveReviewReport && passiveReviewReport.blockedBehavior
        ? passiveReviewReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    sequenceSelfReview && sequenceSelfReview.preservedSystems
      ? sequenceSelfReview.preservedSystems
      : passiveReviewReport && passiveReviewReport.preservedSystems
        ? passiveReviewReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    sequenceSelfReview && sequenceSelfReview.safetyFlags
      ? sequenceSelfReview.safetyFlags
      : passiveReviewReport && passiveReviewReport.safetyFlags
        ? passiveReviewReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const plannedChecklistCategories = [
    "required-primitive-shape-fields",
    "safety-flag-presence",
    "status-value-inertness",
    "failure-reason-shape",
    "blocked-runtime-guard-confirmation"
  ];
  const plannedRequiredFieldChecks = [
    "phase",
    "helperName",
    "ok",
    "reportOnly",
    "passiveOnly",
    "shapeOnly"
  ];
  const plannedSafetyFieldChecks = [
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "validatesLiveMetadata",
    "validationExecuted"
  ];
  const plannedStatusValueChecks = [
    "liveRendererCreated",
    "callableMethodsCreated",
    "sharedStateCreated",
    "sharedStateMutated",
    "sharedStateDirectlyRead"
  ];
  const plannedFailureReasonChecks = [
    "reason",
    "failedRequirement",
    "requiredOptionKeys",
    "blockers",
    "concerns"
  ];
  const plannedBlockedRuntimeChecks = [
    "mapAttached",
    "drawingEnabled",
    "domMutated",
    "startupWired"
  ];
  const blockers = [];
  if (
    !sequenceSelfReview ||
    sequenceSelfReview.readyForFuturePassiveValidationChecklistPlanning !== true
  ) {
    blockers.push("passive-non-runtime-validation-review-sequence-not-ready");
  }
  if (
    !passiveReviewReport ||
    passiveReviewReport.reportOnly !== true ||
    passiveReviewReport.passiveOnly !== true
  ) {
    blockers.push("passive-non-runtime-validation-review-report-not-ready");
  }
  const concerns = [
    "checklist-plan-does-not-authorize-runtime-validation",
    "future-checklist-helper-must-remain-primitive-and-passive"
  ];
  const recommendedNextStep =
    "future-passive-validation-checklist-helper-planning-phase";

  if (missingKey) {
    return {
      phase: 336,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      passiveOnly: true,
      shapeOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedChecklistName:
        "custom25DVisualManualRendererPassiveValidationChecklist",
      checklistCreated: false,
      checklistExecuted: false,
      validationRuntimeEnforced: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      plannedChecklistCategories,
      plannedRequiredFieldChecks,
      plannedSafetyFieldChecks,
      plannedStatusValueChecks,
      plannedFailureReasonChecks,
      plannedBlockedRuntimeChecks,
      recommendedNextStep,
      blockers,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false
    };
  }

  return {
    phase: 336,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    passiveOnly: true,
    shapeOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedChecklistName:
      "custom25DVisualManualRendererPassiveValidationChecklist",
    checklistCreated: false,
    checklistExecuted: false,
    validationRuntimeEnforced: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    plannedChecklistCategories,
    plannedRequiredFieldChecks,
    plannedSafetyFieldChecks,
    plannedStatusValueChecks,
    plannedFailureReasonChecks,
    plannedBlockedRuntimeChecks,
    recommendedNextStep,
    blockers,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport(
  options = {}
) {
  const checklistPlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport(
          options
        )
      : null;
  const requiredOptionKeys =
    checklistPlanReport && Array.isArray(checklistPlanReport.requiredOptionKeys)
      ? checklistPlanReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    checklistPlanReport && checklistPlanReport.blockedBehavior
      ? checklistPlanReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    checklistPlanReport && checklistPlanReport.preservedSystems
      ? checklistPlanReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    checklistPlanReport && checklistPlanReport.safetyFlags
      ? checklistPlanReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const readyForFuturePassiveChecklistHelper = !!(
    checklistPlanReport &&
    Array.isArray(checklistPlanReport.blockers) &&
    checklistPlanReport.blockers.length === 0
  );
  if (!readyForFuturePassiveChecklistHelper) {
    blockers.push("passive-validation-checklist-plan-not-ready");
  }
  const safetyNotes = [
    "future-checklist-helper-must-remain-passive",
    "future-checklist-helper-must-use-only-primitive-report-shapes",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-blocked"
  ];
  const concerns = [
    "readiness-report-does-not-authorize-checklist-execution",
    "future-checklist-work-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep =
    "future-passive-validation-checklist-helper-phase";

  if (missingKey) {
    return {
      phase: 336,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFuturePassiveChecklistHelper,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureChecklistShouldRemainPassive: true,
      futureChecklistShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 336,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFuturePassiveChecklistHelper,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureChecklistShouldRemainPassive: true,
    futureChecklistShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport(
  options = {}
) {
  const checklistPlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport(
          options
        )
      : null;
  const checklistReadinessReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport(
          options
        )
      : null;
  const requiredOptionKeys =
    checklistReadinessReport &&
    Array.isArray(checklistReadinessReport.requiredOptionKeys)
      ? checklistReadinessReport.requiredOptionKeys
      : checklistPlanReport && Array.isArray(checklistPlanReport.requiredOptionKeys)
        ? checklistPlanReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    checklistReadinessReport && checklistReadinessReport.blockedBehavior
      ? checklistReadinessReport.blockedBehavior
      : checklistPlanReport && checklistPlanReport.blockedBehavior
        ? checklistPlanReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    checklistReadinessReport && checklistReadinessReport.preservedSystems
      ? checklistReadinessReport.preservedSystems
      : checklistPlanReport && checklistPlanReport.preservedSystems
        ? checklistPlanReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    checklistReadinessReport && checklistReadinessReport.safetyFlags
      ? checklistReadinessReport.safetyFlags
      : checklistPlanReport && checklistPlanReport.safetyFlags
        ? checklistPlanReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-readiness-report-unavailable");
  }
  if (
    !checklistPlanReport ||
    !checklistReadinessReport ||
    checklistReadinessReport.readyForFuturePassiveChecklistHelper !== true
  ) {
    concerns.push("passive-validation-checklist-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 336,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      checklistPlanAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
        "function",
      checklistReadinessAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
        "function",
      checklistCreated: false,
      checklistExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 336,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    checklistPlanAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
      "function",
    checklistReadinessAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
      "function",
    checklistCreated: false,
    checklistExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport(
  options = {}
) {
  const checklistPlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport(
          options
        )
      : null;
  const checklistReadinessReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport(
          options
        )
      : null;
  const checklistPlanCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    checklistPlanCloseoutReport &&
    Array.isArray(checklistPlanCloseoutReport.requiredOptionKeys)
      ? checklistPlanCloseoutReport.requiredOptionKeys
      : checklistReadinessReport &&
          Array.isArray(checklistReadinessReport.requiredOptionKeys)
        ? checklistReadinessReport.requiredOptionKeys
        : checklistPlanReport && Array.isArray(checklistPlanReport.requiredOptionKeys)
          ? checklistPlanReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    checklistPlanCloseoutReport && checklistPlanCloseoutReport.blockedBehavior
      ? checklistPlanCloseoutReport.blockedBehavior
      : checklistReadinessReport && checklistReadinessReport.blockedBehavior
        ? checklistReadinessReport.blockedBehavior
        : checklistPlanReport && checklistPlanReport.blockedBehavior
          ? checklistPlanReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    checklistPlanCloseoutReport && checklistPlanCloseoutReport.preservedSystems
      ? checklistPlanCloseoutReport.preservedSystems
      : checklistReadinessReport && checklistReadinessReport.preservedSystems
        ? checklistReadinessReport.preservedSystems
        : checklistPlanReport && checklistPlanReport.preservedSystems
          ? checklistPlanReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    checklistPlanCloseoutReport && checklistPlanCloseoutReport.safetyFlags
      ? checklistPlanCloseoutReport.safetyFlags
      : checklistReadinessReport && checklistReadinessReport.safetyFlags
        ? checklistReadinessReport.safetyFlags
        : checklistPlanReport && checklistPlanReport.safetyFlags
          ? checklistPlanReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const checklistPlanningComplete = !!(
    checklistPlanReport &&
    checklistReadinessReport &&
    checklistPlanCloseoutReport &&
    Array.isArray(checklistPlanReport.blockers) &&
    checklistPlanReport.blockers.length === 0 &&
    checklistReadinessReport.readyForFuturePassiveChecklistHelper === true &&
    checklistPlanCloseoutReport.checklistPlanAvailable === true &&
    checklistPlanCloseoutReport.checklistReadinessAvailable === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-plan-closeout-report-unavailable");
  }
  if (!checklistPlanningComplete) {
    concerns.push("passive-validation-checklist-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 337,
      phaseClosedOut: 336,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      checklistPlanReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
        "function",
      checklistReadinessReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
        "function",
      checklistPlanCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport ===
        "function",
      checklistPlanningComplete,
      checklistHelperCreated: false,
      checklistExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 337,
    phaseClosedOut: 336,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    checklistPlanReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
      "function",
    checklistReadinessReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
      "function",
    checklistPlanCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport ===
      "function",
    checklistPlanningComplete,
    checklistHelperCreated: false,
    checklistExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const checklistPlanningSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.checklistPlanningComplete === true
  );
  if (!checklistPlanningSequenceComplete) {
    blockers.push("passive-validation-checklist-plan-sequence-not-complete");
  }
  const safetyNotes = [
    "future-checklist-helper-must-remain-passive",
    "future-checklist-helper-must-use-only-primitive-report-shapes",
    "future-checklist-helper-must-not-execute-live-validation",
    "runtime-validation-must-stay-disabled"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-passive-validation-checklist-helper-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-validation-checklist-helper-phase";

  if (missingKey) {
    return {
      phase: 337,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      checklistPlanningSequenceComplete,
      readyForFuturePassiveChecklistHelper: blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureChecklistShouldRemainPassive: true,
      futureChecklistShouldUseOnlyPrimitiveReportShapes: true,
      futureChecklistShouldNotExecuteLiveValidation: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 337,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    checklistPlanningSequenceComplete,
    readyForFuturePassiveChecklistHelper: blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureChecklistShouldRemainPassive: true,
    futureChecklistShouldUseOnlyPrimitiveReportShapes: true,
    futureChecklistShouldNotExecuteLiveValidation: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistReport(
  options = {}
) {
  const checklistPlanSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport(
          options
        )
      : null;
  const contractShell =
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultContractShell(
          options
        )
      : null;
  const resultShell =
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
    "function"
      ? createCustom25DVisualManualRendererMetadataValidationResultShell(options)
      : null;
  const passiveReviewReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    checklistPlanSequenceSelfReview &&
    Array.isArray(checklistPlanSequenceSelfReview.requiredOptionKeys)
      ? checklistPlanSequenceSelfReview.requiredOptionKeys
      : resultShell && Array.isArray(resultShell.requiredOptionKeys)
        ? resultShell.requiredOptionKeys
        : contractShell && Array.isArray(contractShell.requiredOptionKeys)
          ? contractShell.requiredOptionKeys
          : passiveReviewReport && Array.isArray(passiveReviewReport.requiredOptionKeys)
            ? passiveReviewReport.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    checklistPlanSequenceSelfReview && checklistPlanSequenceSelfReview.blockedBehavior
      ? checklistPlanSequenceSelfReview.blockedBehavior
      : passiveReviewReport && passiveReviewReport.blockedBehavior
        ? passiveReviewReport.blockedBehavior
        : resultShell && resultShell.blockedBehavior
          ? resultShell.blockedBehavior
          : contractShell && contractShell.blockedBehavior
            ? contractShell.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };
  const preservedSystems =
    checklistPlanSequenceSelfReview && checklistPlanSequenceSelfReview.preservedSystems
      ? checklistPlanSequenceSelfReview.preservedSystems
      : passiveReviewReport && passiveReviewReport.preservedSystems
        ? passiveReviewReport.preservedSystems
        : resultShell && resultShell.preservedSystems
          ? resultShell.preservedSystems
          : contractShell && contractShell.preservedSystems
            ? contractShell.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };
  const safetyFlags =
    checklistPlanSequenceSelfReview && checklistPlanSequenceSelfReview.safetyFlags
      ? checklistPlanSequenceSelfReview.safetyFlags
      : passiveReviewReport && passiveReviewReport.safetyFlags
        ? passiveReviewReport.safetyFlags
        : resultShell && resultShell.safetyFlags
          ? resultShell.safetyFlags
          : contractShell && contractShell.safetyFlags
            ? contractShell.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };
  const requiredFieldsToCheck = [
    "phase",
    "ok",
    "allowed",
    "blocked",
    "requiredOptionKeys",
    "blockedBehavior",
    "preservedSystems",
    "safetyFlags"
  ];
  const safetyFieldsToCheck = [
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "validatesLiveMetadata",
    "validationExecuted"
  ];
  const statusFieldsToCheck = [
    "liveRendererCreated",
    "callableMethodsCreated",
    "sharedStateCreated",
    "sharedStateMutated",
    "sharedStateDirectlyRead",
    "mapAttached",
    "drawingEnabled",
    "domMutated",
    "startupWired"
  ];
  const failureReasonFieldsToCheck = [
    "reason",
    "failedRequirement",
    "requiredOptionKeys"
  ];
  const requiredFieldChecklistPassed = [
    checklistPlanSequenceSelfReview,
    passiveReviewReport,
    contractShell,
    resultShell
  ].every(
    (report) =>
      !!report &&
      requiredFieldsToCheck.every((fieldName) =>
        Object.prototype.hasOwnProperty.call(report, fieldName)
      )
  );
  const safetyFieldChecklistPassed = [passiveReviewReport, resultShell].every(
    (report) =>
      !!report &&
      safetyFieldsToCheck.every((fieldName) =>
        Object.prototype.hasOwnProperty.call(report, fieldName)
      )
  );
  const statusValueChecklistPassed = [passiveReviewReport, contractShell, resultShell].every(
    (report) =>
      !!report &&
      statusFieldsToCheck.every(
        (fieldName) =>
          Object.prototype.hasOwnProperty.call(report, fieldName) &&
          report[fieldName] === false
      )
  );
  const failureReasonChecklistPassed = [contractShell, resultShell].every(
    (report) =>
      !!report &&
      failureReasonFieldsToCheck.every((fieldName) =>
        Object.prototype.hasOwnProperty.call(report, fieldName)
      )
  );
  const blockedRuntimeChecklistPassed =
    blockedBehavior.rendererCreation === true &&
    blockedBehavior.rendererInitialization === true &&
    blockedBehavior.rendererRun === true &&
    blockedBehavior.mapAttachment === true &&
    blockedBehavior.drawing === true &&
    blockedBehavior.domCreation === true &&
    blockedBehavior.startupWiring === true &&
    blockedBehavior.backendChanges === true &&
    blockedBehavior.storageWrites === true &&
    blockedBehavior.networkAccess === true;
  const blockers = [];
  if (
    !checklistPlanSequenceSelfReview ||
    checklistPlanSequenceSelfReview.readyForFuturePassiveChecklistHelper !== true
  ) {
    blockers.push("passive-validation-checklist-plan-sequence-not-ready");
  }
  if (!requiredFieldChecklistPassed) {
    blockers.push("required-field-checklist-not-passed");
  }
  if (!safetyFieldChecklistPassed) {
    blockers.push("safety-field-checklist-not-passed");
  }
  if (!statusValueChecklistPassed) {
    blockers.push("status-value-checklist-not-passed");
  }
  if (!failureReasonChecklistPassed) {
    blockers.push("failure-reason-checklist-not-passed");
  }
  if (!blockedRuntimeChecklistPassed) {
    blockers.push("blocked-runtime-checklist-not-passed");
  }
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-plan-sequence-self-review-unavailable");
  }
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell !==
    "function"
  ) {
    concerns.push("metadata-validation-result-contract-shell-unavailable");
  }
  if (
    typeof createCustom25DVisualManualRendererMetadataValidationResultShell !==
    "function"
  ) {
    concerns.push("metadata-validation-result-shell-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport !==
    "function"
  ) {
    concerns.push("passive-non-runtime-validation-review-report-unavailable");
  }
  const safetyNotes = [
    "checklist-checks-remain-shape-only",
    "checklist-uses-only-primitive-report-shapes",
    "runtime-validation-remains-disabled",
    "live-metadata-validation-remains-blocked"
  ];

  if (missingKey) {
    return {
      phase: 338,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistReport",
      ok: true,
      allowed: false,
      blocked: true,
      checklistOnly: true,
      reportOnly: true,
      passiveOnly: true,
      shapeOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      checklistExecutedAgainstLiveMetadata: false,
      checklistUsedOnlyPrimitiveReportShapes: true,
      checklistPlanAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport ===
        "function",
      contractShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
        "function",
      resultShellAvailable:
        typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
        "function",
      passiveReviewAvailable:
        typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
        "function",
      requiredFieldChecklistPassed,
      safetyFieldChecklistPassed,
      statusValueChecklistPassed,
      failureReasonChecklistPassed,
      blockedRuntimeChecklistPassed,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 338,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistReport",
    ok: true,
    allowed: true,
    blocked: false,
    checklistOnly: true,
    reportOnly: true,
    passiveOnly: true,
    shapeOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    checklistExecutedAgainstLiveMetadata: false,
    checklistUsedOnlyPrimitiveReportShapes: true,
    checklistPlanAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport ===
      "function",
    contractShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
      "function",
    resultShellAvailable:
      typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
      "function",
    passiveReviewAvailable:
      typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
      "function",
    requiredFieldChecklistPassed,
    safetyFieldChecklistPassed,
    statusValueChecklistPassed,
    failureReasonChecklistPassed,
    blockedRuntimeChecklistPassed,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport(
  options = {}
) {
  const passiveChecklistReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReport(options)
      : null;
  const requiredOptionKeys =
    passiveChecklistReport && Array.isArray(passiveChecklistReport.requiredOptionKeys)
      ? passiveChecklistReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveChecklistReport && passiveChecklistReport.blockedBehavior
      ? passiveChecklistReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    passiveChecklistReport && passiveChecklistReport.preservedSystems
      ? passiveChecklistReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    passiveChecklistReport && passiveChecklistReport.safetyFlags
      ? passiveChecklistReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const passiveChecklistCompleted = !!(
    passiveChecklistReport &&
    passiveChecklistReport.requiredFieldChecklistPassed === true &&
    passiveChecklistReport.safetyFieldChecklistPassed === true &&
    passiveChecklistReport.statusValueChecklistPassed === true &&
    passiveChecklistReport.failureReasonChecklistPassed === true &&
    passiveChecklistReport.blockedRuntimeChecklistPassed === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-report-unavailable");
  }
  if (!passiveChecklistCompleted) {
    concerns.push("passive-validation-checklist-not-complete");
  }

  if (missingKey) {
    return {
      phase: 338,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveChecklistAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
        "function",
      passiveChecklistCompleted,
      checklistUsedOnlyPrimitiveReportShapes:
        passiveChecklistReport &&
        passiveChecklistReport.checklistUsedOnlyPrimitiveReportShapes === true,
      checklistExecutedAgainstLiveMetadata: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 338,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveChecklistAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
      "function",
    passiveChecklistCompleted,
    checklistUsedOnlyPrimitiveReportShapes:
      passiveChecklistReport &&
      passiveChecklistReport.checklistUsedOnlyPrimitiveReportShapes === true,
    checklistExecutedAgainstLiveMetadata: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport(
  options = {}
) {
  const passiveChecklistReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReport(options)
      : null;
  const passiveChecklistCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    passiveChecklistCloseoutReport &&
    Array.isArray(passiveChecklistCloseoutReport.requiredOptionKeys)
      ? passiveChecklistCloseoutReport.requiredOptionKeys
      : passiveChecklistReport && Array.isArray(passiveChecklistReport.requiredOptionKeys)
        ? passiveChecklistReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveChecklistCloseoutReport && passiveChecklistCloseoutReport.blockedBehavior
      ? passiveChecklistCloseoutReport.blockedBehavior
      : passiveChecklistReport && passiveChecklistReport.blockedBehavior
        ? passiveChecklistReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    passiveChecklistCloseoutReport && passiveChecklistCloseoutReport.preservedSystems
      ? passiveChecklistCloseoutReport.preservedSystems
      : passiveChecklistReport && passiveChecklistReport.preservedSystems
        ? passiveChecklistReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    passiveChecklistCloseoutReport && passiveChecklistCloseoutReport.safetyFlags
      ? passiveChecklistCloseoutReport.safetyFlags
      : passiveChecklistReport && passiveChecklistReport.safetyFlags
        ? passiveChecklistReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const passiveChecklistCompleted = !!(
    passiveChecklistReport &&
    passiveChecklistCloseoutReport &&
    passiveChecklistCloseoutReport.passiveChecklistAvailable === true &&
    passiveChecklistCloseoutReport.passiveChecklistCompleted === true &&
    passiveChecklistReport.checklistUsedOnlyPrimitiveReportShapes === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-validation-checklist-closeout-report-unavailable");
  }
  if (!passiveChecklistCompleted) {
    concerns.push("passive-validation-checklist-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 339,
      phaseClosedOut: 338,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveChecklistReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
        "function",
      passiveChecklistCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport ===
        "function",
      passiveChecklistCompleted,
      checklistUsedOnlyPrimitiveReportShapes:
        passiveChecklistReport &&
        passiveChecklistReport.checklistUsedOnlyPrimitiveReportShapes === true,
      checklistExecutedAgainstLiveMetadata: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 339,
    phaseClosedOut: 338,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveChecklistReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
      "function",
    passiveChecklistCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport ===
      "function",
    passiveChecklistCompleted,
    checklistUsedOnlyPrimitiveReportShapes:
      passiveChecklistReport &&
      passiveChecklistReport.checklistUsedOnlyPrimitiveReportShapes === true,
    checklistExecutedAgainstLiveMetadata: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const passiveChecklistSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.passiveChecklistCompleted === true &&
    sequenceCloseoutReport.checklistUsedOnlyPrimitiveReportShapes === true
  );
  if (!passiveChecklistSequenceComplete) {
    blockers.push("passive-validation-checklist-sequence-not-complete");
  }
  const safetyNotes = [
    "future-work-must-remain-passive",
    "future-work-must-use-only-primitive-report-shapes",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-passive-validation-summary-planning-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-validation-summary-planning-phase";

  if (missingKey) {
    return {
      phase: 339,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      passiveChecklistSequenceComplete,
      readyForFuturePassiveValidationSummaryPlanning:
        blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureWorkShouldRemainPassive: true,
      futureWorkShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 339,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    passiveChecklistSequenceComplete,
    readyForFuturePassiveValidationSummaryPlanning:
      blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureWorkShouldRemainPassive: true,
    futureWorkShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport(
  options = {}
) {
  const checklistSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveChecklistReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReport(options)
      : null;
  const nonRuntimeReviewSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    checklistSequenceSelfReview &&
    Array.isArray(checklistSequenceSelfReview.requiredOptionKeys)
      ? checklistSequenceSelfReview.requiredOptionKeys
      : passiveChecklistReport && Array.isArray(passiveChecklistReport.requiredOptionKeys)
        ? passiveChecklistReport.requiredOptionKeys
        : nonRuntimeReviewSequenceSelfReview &&
            Array.isArray(nonRuntimeReviewSequenceSelfReview.requiredOptionKeys)
          ? nonRuntimeReviewSequenceSelfReview.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    checklistSequenceSelfReview && checklistSequenceSelfReview.blockedBehavior
      ? checklistSequenceSelfReview.blockedBehavior
      : passiveChecklistReport && passiveChecklistReport.blockedBehavior
        ? passiveChecklistReport.blockedBehavior
        : nonRuntimeReviewSequenceSelfReview &&
            nonRuntimeReviewSequenceSelfReview.blockedBehavior
          ? nonRuntimeReviewSequenceSelfReview.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    checklistSequenceSelfReview && checklistSequenceSelfReview.preservedSystems
      ? checklistSequenceSelfReview.preservedSystems
      : passiveChecklistReport && passiveChecklistReport.preservedSystems
        ? passiveChecklistReport.preservedSystems
        : nonRuntimeReviewSequenceSelfReview &&
            nonRuntimeReviewSequenceSelfReview.preservedSystems
          ? nonRuntimeReviewSequenceSelfReview.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    checklistSequenceSelfReview && checklistSequenceSelfReview.safetyFlags
      ? checklistSequenceSelfReview.safetyFlags
      : passiveChecklistReport && passiveChecklistReport.safetyFlags
        ? passiveChecklistReport.safetyFlags
        : nonRuntimeReviewSequenceSelfReview &&
            nonRuntimeReviewSequenceSelfReview.safetyFlags
          ? nonRuntimeReviewSequenceSelfReview.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const plannedSummarySections = [
    "checklist-sequence-status",
    "checklist-summary-status",
    "non-runtime-review-sequence-status",
    "safety-summary",
    "blocked-runtime-summary"
  ];
  const plannedChecklistSummaryFields = [
    "passiveChecklistSequenceComplete",
    "passiveChecklistCompleted",
    "checklistUsedOnlyPrimitiveReportShapes",
    "checklistExecutedAgainstLiveMetadata"
  ];
  const plannedNonRuntimeReviewSummaryFields = [
    "passiveReviewSequenceComplete",
    "readyForFuturePassiveValidationChecklistPlanning",
    "futureWorkShouldRemainPassive"
  ];
  const plannedSafetySummaryFields = [
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "validatesLiveMetadata",
    "validationExecuted",
    "sharedStateCreated",
    "sharedStateMutated"
  ];
  const plannedBlockedRuntimeSummaryFields = [
    "mapAttached",
    "drawingEnabled",
    "domMutated",
    "startupWired",
    "liveRendererCreated",
    "callableMethodsCreated"
  ];
  const blockers = [];
  if (
    !checklistSequenceSelfReview ||
    checklistSequenceSelfReview.readyForFuturePassiveValidationSummaryPlanning !==
      true
  ) {
    blockers.push("passive-validation-checklist-sequence-not-ready");
  }
  if (
    !nonRuntimeReviewSequenceSelfReview ||
    nonRuntimeReviewSequenceSelfReview
      .readyForFuturePassiveValidationChecklistPlanning !== true
  ) {
    blockers.push("passive-non-runtime-review-sequence-not-ready");
  }
  const concerns = [
    "summary-plan-does-not-authorize-runtime-validation",
    "future-summary-helper-must-remain-primitive-and-passive"
  ];
  const recommendedNextStep =
    "future-passive-validation-summary-helper-planning-phase";

  if (missingKey) {
    return {
      phase: 340,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      passiveOnly: true,
      shapeOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedSummaryName:
        "custom25DVisualManualRendererPassiveValidationSummary",
      summaryCreated: false,
      summaryExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      plannedSummarySections,
      plannedChecklistSummaryFields,
      plannedNonRuntimeReviewSummaryFields,
      plannedSafetySummaryFields,
      plannedBlockedRuntimeSummaryFields,
      recommendedNextStep,
      blockers,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 340,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    passiveOnly: true,
    shapeOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedSummaryName:
      "custom25DVisualManualRendererPassiveValidationSummary",
    summaryCreated: false,
    summaryExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    plannedSummarySections,
    plannedChecklistSummaryFields,
    plannedNonRuntimeReviewSummaryFields,
    plannedSafetySummaryFields,
    plannedBlockedRuntimeSummaryFields,
    recommendedNextStep,
    blockers,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport(
  options = {}
) {
  const summaryPlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport(
          options
        )
      : null;
  const requiredOptionKeys =
    summaryPlanReport && Array.isArray(summaryPlanReport.requiredOptionKeys)
      ? summaryPlanReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    summaryPlanReport && summaryPlanReport.blockedBehavior
      ? summaryPlanReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    summaryPlanReport && summaryPlanReport.preservedSystems
      ? summaryPlanReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    summaryPlanReport && summaryPlanReport.safetyFlags
      ? summaryPlanReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const readyForFuturePassiveValidationSummaryHelper = !!(
    summaryPlanReport &&
    Array.isArray(summaryPlanReport.blockers) &&
    summaryPlanReport.blockers.length === 0
  );
  if (!readyForFuturePassiveValidationSummaryHelper) {
    blockers.push("passive-validation-summary-plan-not-ready");
  }
  const safetyNotes = [
    "future-summary-helper-must-remain-passive",
    "future-summary-helper-must-use-only-primitive-report-shapes",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-blocked"
  ];
  const concerns = [
    "readiness-report-does-not-authorize-summary-execution",
    "future-summary-helper-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-validation-summary-helper-phase";

  if (missingKey) {
    return {
      phase: 340,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFuturePassiveValidationSummaryHelper,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureSummaryShouldRemainPassive: true,
      futureSummaryShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 340,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFuturePassiveValidationSummaryHelper,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureSummaryShouldRemainPassive: true,
    futureSummaryShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport(
  options = {}
) {
  const summaryPlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport(
          options
        )
      : null;
  const summaryReadinessReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport(
          options
        )
      : null;
  const requiredOptionKeys =
    summaryReadinessReport && Array.isArray(summaryReadinessReport.requiredOptionKeys)
      ? summaryReadinessReport.requiredOptionKeys
      : summaryPlanReport && Array.isArray(summaryPlanReport.requiredOptionKeys)
        ? summaryPlanReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    summaryReadinessReport && summaryReadinessReport.blockedBehavior
      ? summaryReadinessReport.blockedBehavior
      : summaryPlanReport && summaryPlanReport.blockedBehavior
        ? summaryPlanReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    summaryReadinessReport && summaryReadinessReport.preservedSystems
      ? summaryReadinessReport.preservedSystems
      : summaryPlanReport && summaryPlanReport.preservedSystems
        ? summaryPlanReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    summaryReadinessReport && summaryReadinessReport.safetyFlags
      ? summaryReadinessReport.safetyFlags
      : summaryPlanReport && summaryPlanReport.safetyFlags
        ? summaryPlanReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-readiness-report-unavailable");
  }
  if (
    !summaryPlanReport ||
    !summaryReadinessReport ||
    summaryReadinessReport.readyForFuturePassiveValidationSummaryHelper !== true
  ) {
    concerns.push("passive-validation-summary-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 340,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      summaryPlanAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
        "function",
      summaryReadinessAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
        "function",
      summaryCreated: false,
      summaryExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 340,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    summaryPlanAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
      "function",
    summaryReadinessAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
      "function",
    summaryCreated: false,
    summaryExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport(
  options = {}
) {
  const summaryPlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport(
          options
        )
      : null;
  const summaryReadinessReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport(
          options
        )
      : null;
  const summaryPlanCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    summaryPlanCloseoutReport &&
    Array.isArray(summaryPlanCloseoutReport.requiredOptionKeys)
      ? summaryPlanCloseoutReport.requiredOptionKeys
      : summaryReadinessReport &&
          Array.isArray(summaryReadinessReport.requiredOptionKeys)
        ? summaryReadinessReport.requiredOptionKeys
        : summaryPlanReport && Array.isArray(summaryPlanReport.requiredOptionKeys)
          ? summaryPlanReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    summaryPlanCloseoutReport && summaryPlanCloseoutReport.blockedBehavior
      ? summaryPlanCloseoutReport.blockedBehavior
      : summaryReadinessReport && summaryReadinessReport.blockedBehavior
        ? summaryReadinessReport.blockedBehavior
        : summaryPlanReport && summaryPlanReport.blockedBehavior
          ? summaryPlanReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    summaryPlanCloseoutReport && summaryPlanCloseoutReport.preservedSystems
      ? summaryPlanCloseoutReport.preservedSystems
      : summaryReadinessReport && summaryReadinessReport.preservedSystems
        ? summaryReadinessReport.preservedSystems
        : summaryPlanReport && summaryPlanReport.preservedSystems
          ? summaryPlanReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    summaryPlanCloseoutReport && summaryPlanCloseoutReport.safetyFlags
      ? summaryPlanCloseoutReport.safetyFlags
      : summaryReadinessReport && summaryReadinessReport.safetyFlags
        ? summaryReadinessReport.safetyFlags
        : summaryPlanReport && summaryPlanReport.safetyFlags
          ? summaryPlanReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const summaryPlanningComplete = !!(
    summaryPlanReport &&
    summaryReadinessReport &&
    summaryPlanCloseoutReport &&
    Array.isArray(summaryPlanReport.blockers) &&
    summaryPlanReport.blockers.length === 0 &&
    summaryReadinessReport.readyForFuturePassiveValidationSummaryHelper ===
      true &&
    summaryPlanCloseoutReport.summaryPlanAvailable === true &&
    summaryPlanCloseoutReport.summaryReadinessAvailable === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-plan-closeout-report-unavailable");
  }
  if (!summaryPlanningComplete) {
    concerns.push("passive-validation-summary-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 341,
      phaseClosedOut: 340,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      summaryPlanReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
        "function",
      summaryReadinessReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
        "function",
      summaryPlanCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport ===
        "function",
      summaryPlanningComplete,
      summaryHelperCreated: false,
      summaryExecuted: false,
      checklistChecksExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 341,
    phaseClosedOut: 340,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    summaryPlanReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
      "function",
    summaryReadinessReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
      "function",
    summaryPlanCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport ===
      "function",
    summaryPlanningComplete,
    summaryHelperCreated: false,
    summaryExecuted: false,
    checklistChecksExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const summaryPlanningSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.summaryPlanningComplete === true
  );
  if (!summaryPlanningSequenceComplete) {
    blockers.push("passive-validation-summary-plan-sequence-not-complete");
  }
  const safetyNotes = [
    "future-summary-helper-must-remain-passive",
    "future-summary-helper-must-use-only-primitive-report-shapes",
    "future-summary-helper-must-not-execute-live-validation",
    "runtime-validation-must-stay-disabled"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-passive-validation-summary-helper-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-validation-summary-helper-phase";

  if (missingKey) {
    return {
      phase: 341,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      summaryPlanningSequenceComplete,
      readyForFuturePassiveValidationSummaryHelper:
        blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureSummaryShouldRemainPassive: true,
      futureSummaryShouldUseOnlyPrimitiveReportShapes: true,
      futureSummaryShouldNotExecuteLiveValidation: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 341,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    summaryPlanningSequenceComplete,
    readyForFuturePassiveValidationSummaryHelper:
      blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureSummaryShouldRemainPassive: true,
    futureSummaryShouldUseOnlyPrimitiveReportShapes: true,
    futureSummaryShouldNotExecuteLiveValidation: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryReport(
  options = {}
) {
  const summaryPlanningSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport(
          options
        )
      : null;
  const checklistSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveChecklistReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReport(options)
      : null;
  const passiveNonRuntimeReviewSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveNonRuntimeReviewReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    summaryPlanningSequenceSelfReview &&
    Array.isArray(summaryPlanningSequenceSelfReview.requiredOptionKeys)
      ? summaryPlanningSequenceSelfReview.requiredOptionKeys
      : checklistSequenceSelfReview &&
          Array.isArray(checklistSequenceSelfReview.requiredOptionKeys)
        ? checklistSequenceSelfReview.requiredOptionKeys
        : passiveChecklistReport &&
            Array.isArray(passiveChecklistReport.requiredOptionKeys)
          ? passiveChecklistReport.requiredOptionKeys
          : passiveNonRuntimeReviewSequenceSelfReview &&
              Array.isArray(
                passiveNonRuntimeReviewSequenceSelfReview.requiredOptionKeys
              )
            ? passiveNonRuntimeReviewSequenceSelfReview.requiredOptionKeys
            : passiveNonRuntimeReviewReport &&
                Array.isArray(passiveNonRuntimeReviewReport.requiredOptionKeys)
              ? passiveNonRuntimeReviewReport.requiredOptionKeys
              : [
                  "manual",
                  "developerIntent",
                  "localDevOnly",
                  "browserConsoleOnly",
                  "explicitOptionsOnly",
                  "allowManualRendererStateContainerShell",
                  "noStartupWiring",
                  "noBackendChanges",
                  "noPersistence",
                  "noAutomaticInvocation"
                ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    summaryPlanningSequenceSelfReview &&
    summaryPlanningSequenceSelfReview.blockedBehavior
      ? summaryPlanningSequenceSelfReview.blockedBehavior
      : checklistSequenceSelfReview && checklistSequenceSelfReview.blockedBehavior
        ? checklistSequenceSelfReview.blockedBehavior
        : passiveChecklistReport && passiveChecklistReport.blockedBehavior
          ? passiveChecklistReport.blockedBehavior
          : passiveNonRuntimeReviewSequenceSelfReview &&
              passiveNonRuntimeReviewSequenceSelfReview.blockedBehavior
            ? passiveNonRuntimeReviewSequenceSelfReview.blockedBehavior
            : passiveNonRuntimeReviewReport &&
                passiveNonRuntimeReviewReport.blockedBehavior
              ? passiveNonRuntimeReviewReport.blockedBehavior
              : {
                  rendererCreation: true,
                  rendererInitialization: true,
                  rendererRun: true,
                  mapAttachment: true,
                  drawing: true,
                  domCreation: true,
                  startupWiring: true,
                  automaticInvocation: true,
                  gameplayChanges: true,
                  pinChanges: true,
                  uiChanges: true,
                  backendChanges: true,
                  storageWrites: true,
                  networkAccess: true
                };
  const preservedSystems =
    summaryPlanningSequenceSelfReview &&
    summaryPlanningSequenceSelfReview.preservedSystems
      ? summaryPlanningSequenceSelfReview.preservedSystems
      : checklistSequenceSelfReview && checklistSequenceSelfReview.preservedSystems
        ? checklistSequenceSelfReview.preservedSystems
        : passiveChecklistReport && passiveChecklistReport.preservedSystems
          ? passiveChecklistReport.preservedSystems
          : passiveNonRuntimeReviewSequenceSelfReview &&
              passiveNonRuntimeReviewSequenceSelfReview.preservedSystems
            ? passiveNonRuntimeReviewSequenceSelfReview.preservedSystems
            : passiveNonRuntimeReviewReport &&
                passiveNonRuntimeReviewReport.preservedSystems
              ? passiveNonRuntimeReviewReport.preservedSystems
              : {
                  existingLeafletMapBehavior: true,
                  osmBehavior: true,
                  gameplay: true,
                  pins: true,
                  playerMarker: true,
                  captureRadius: true,
                  ui: true,
                  backend: true,
                  storage: true,
                  network: true
                };
  const safetyFlags =
    summaryPlanningSequenceSelfReview &&
    summaryPlanningSequenceSelfReview.safetyFlags
      ? summaryPlanningSequenceSelfReview.safetyFlags
      : checklistSequenceSelfReview && checklistSequenceSelfReview.safetyFlags
        ? checklistSequenceSelfReview.safetyFlags
        : passiveChecklistReport && passiveChecklistReport.safetyFlags
          ? passiveChecklistReport.safetyFlags
          : passiveNonRuntimeReviewSequenceSelfReview &&
              passiveNonRuntimeReviewSequenceSelfReview.safetyFlags
            ? passiveNonRuntimeReviewSequenceSelfReview.safetyFlags
            : passiveNonRuntimeReviewReport &&
                passiveNonRuntimeReviewReport.safetyFlags
              ? passiveNonRuntimeReviewReport.safetyFlags
              : {
                  custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                  landmarkTestMarkers:
                    ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                  landmarkSampleData:
                    ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                  dinosaurSitesAuData:
                    ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                };
  const passiveChecklistAvailable = !!(
    passiveChecklistReport &&
    passiveChecklistReport.checklistOnly === true
  );
  const passiveChecklistSequenceAvailable = !!(
    checklistSequenceSelfReview &&
    checklistSequenceSelfReview.sequenceReviewed === true
  );
  const passiveNonRuntimeReviewAvailable = !!(
    passiveNonRuntimeReviewReport &&
    passiveNonRuntimeReviewReport.reportOnly === true
  );
  const passiveNonRuntimeReviewSequenceAvailable = !!(
    passiveNonRuntimeReviewSequenceSelfReview &&
    passiveNonRuntimeReviewSequenceSelfReview.sequenceReviewed === true
  );
  const summaryPlanningSequenceAvailable = !!(
    summaryPlanningSequenceSelfReview &&
    summaryPlanningSequenceSelfReview.sequenceReviewed === true
  );
  const shapeOnlyComparisonConfirmed = !!(
    passiveChecklistReport &&
    passiveChecklistReport.shapeOnly === true &&
    passiveChecklistReport.checklistUsedOnlyPrimitiveReportShapes === true &&
    passiveNonRuntimeReviewReport &&
    passiveNonRuntimeReviewReport.shapeOnlyComparison === true
  );
  const blockedRuntimeChecksConfirmed = !!(
    blockedBehavior.rendererCreation === true &&
    blockedBehavior.rendererInitialization === true &&
    blockedBehavior.rendererRun === true &&
    blockedBehavior.mapAttachment === true &&
    blockedBehavior.drawing === true &&
    blockedBehavior.domCreation === true &&
    blockedBehavior.startupWiring === true &&
    blockedBehavior.backendChanges === true &&
    blockedBehavior.storageWrites === true &&
    blockedBehavior.networkAccess === true
  );
  const passiveValidationSummaryComplete = !!(
    passiveChecklistAvailable &&
    passiveChecklistSequenceAvailable &&
    passiveNonRuntimeReviewAvailable &&
    passiveNonRuntimeReviewSequenceAvailable &&
    summaryPlanningSequenceAvailable &&
    shapeOnlyComparisonConfirmed &&
    blockedRuntimeChecksConfirmed
  );
  const blockers = [];
  if (!passiveChecklistAvailable) {
    blockers.push("passive-checklist-report-unavailable");
  }
  if (!passiveChecklistSequenceAvailable) {
    blockers.push("passive-checklist-sequence-unavailable");
  }
  if (!passiveNonRuntimeReviewAvailable) {
    blockers.push("passive-non-runtime-review-report-unavailable");
  }
  if (!passiveNonRuntimeReviewSequenceAvailable) {
    blockers.push("passive-non-runtime-review-sequence-unavailable");
  }
  if (!summaryPlanningSequenceAvailable) {
    blockers.push("passive-validation-summary-planning-sequence-unavailable");
  }
  if (!shapeOnlyComparisonConfirmed) {
    blockers.push("shape-only-comparison-not-confirmed");
  }
  if (!blockedRuntimeChecksConfirmed) {
    blockers.push("blocked-runtime-checks-not-confirmed");
  }
  const concerns = [
    "summary-report-does-not-authorize-runtime-validation",
    "summary-report-remains-primitive-and-passive-only"
  ];
  const safetyNotes = [
    "summary-uses-only-primitive-report-shapes",
    "summary-does-not-read-live-metadata",
    "runtime-validation-remains-disabled",
    "live-metadata-validation-remains-blocked"
  ];

  if (missingKey) {
    return {
      phase: 342,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryReport",
      ok: true,
      allowed: false,
      blocked: true,
      summaryOnly: true,
      reportOnly: true,
      passiveOnly: true,
      shapeOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      summaryUsedOnlyPrimitiveReportShapes: true,
      summaryExecutedAgainstLiveMetadata: false,
      checklistChecksExecuted: false,
      passiveChecklistAvailable,
      passiveChecklistSequenceAvailable,
      passiveNonRuntimeReviewAvailable,
      passiveNonRuntimeReviewSequenceAvailable,
      summaryPlanningSequenceAvailable,
      passiveValidationSummaryComplete,
      shapeOnlyComparisonConfirmed,
      blockedRuntimeChecksConfirmed,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 342,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryReport",
    ok: true,
    allowed: true,
    blocked: false,
    summaryOnly: true,
    reportOnly: true,
    passiveOnly: true,
    shapeOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    summaryUsedOnlyPrimitiveReportShapes: true,
    summaryExecutedAgainstLiveMetadata: false,
    checklistChecksExecuted: false,
    passiveChecklistAvailable,
    passiveChecklistSequenceAvailable,
    passiveNonRuntimeReviewAvailable,
    passiveNonRuntimeReviewSequenceAvailable,
    summaryPlanningSequenceAvailable,
    passiveValidationSummaryComplete,
    shapeOnlyComparisonConfirmed,
    blockedRuntimeChecksConfirmed,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport(
  options = {}
) {
  const passiveSummaryReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryReport(options)
      : null;
  const requiredOptionKeys =
    passiveSummaryReport && Array.isArray(passiveSummaryReport.requiredOptionKeys)
      ? passiveSummaryReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveSummaryReport && passiveSummaryReport.blockedBehavior
      ? passiveSummaryReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    passiveSummaryReport && passiveSummaryReport.preservedSystems
      ? passiveSummaryReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    passiveSummaryReport && passiveSummaryReport.safetyFlags
      ? passiveSummaryReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const passiveSummaryCompleted = !!(
    passiveSummaryReport &&
    passiveSummaryReport.passiveValidationSummaryComplete === true &&
    passiveSummaryReport.summaryUsedOnlyPrimitiveReportShapes === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-report-unavailable");
  }
  if (!passiveSummaryCompleted) {
    concerns.push("passive-validation-summary-not-complete");
  }

  if (missingKey) {
    return {
      phase: 342,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveSummaryAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
        "function",
      passiveSummaryCompleted,
      summaryUsedOnlyPrimitiveReportShapes:
        passiveSummaryReport &&
        passiveSummaryReport.summaryUsedOnlyPrimitiveReportShapes === true,
      summaryExecutedAgainstLiveMetadata: false,
      checklistChecksExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 342,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveSummaryAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
      "function",
    passiveSummaryCompleted,
    summaryUsedOnlyPrimitiveReportShapes:
      passiveSummaryReport &&
      passiveSummaryReport.summaryUsedOnlyPrimitiveReportShapes === true,
    summaryExecutedAgainstLiveMetadata: false,
    checklistChecksExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport(
  options = {}
) {
  const passiveSummaryReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryReport(options)
      : null;
  const passiveSummaryCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    passiveSummaryCloseoutReport &&
    Array.isArray(passiveSummaryCloseoutReport.requiredOptionKeys)
      ? passiveSummaryCloseoutReport.requiredOptionKeys
      : passiveSummaryReport && Array.isArray(passiveSummaryReport.requiredOptionKeys)
        ? passiveSummaryReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveSummaryCloseoutReport && passiveSummaryCloseoutReport.blockedBehavior
      ? passiveSummaryCloseoutReport.blockedBehavior
      : passiveSummaryReport && passiveSummaryReport.blockedBehavior
        ? passiveSummaryReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    passiveSummaryCloseoutReport && passiveSummaryCloseoutReport.preservedSystems
      ? passiveSummaryCloseoutReport.preservedSystems
      : passiveSummaryReport && passiveSummaryReport.preservedSystems
        ? passiveSummaryReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    passiveSummaryCloseoutReport && passiveSummaryCloseoutReport.safetyFlags
      ? passiveSummaryCloseoutReport.safetyFlags
      : passiveSummaryReport && passiveSummaryReport.safetyFlags
        ? passiveSummaryReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const passiveSummaryCompleted = !!(
    passiveSummaryReport &&
    passiveSummaryCloseoutReport &&
    passiveSummaryCloseoutReport.passiveSummaryAvailable === true &&
    passiveSummaryCloseoutReport.passiveSummaryCompleted === true &&
    passiveSummaryReport.summaryUsedOnlyPrimitiveReportShapes === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-validation-summary-closeout-report-unavailable");
  }
  if (!passiveSummaryCompleted) {
    concerns.push("passive-validation-summary-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 343,
      phaseClosedOut: 342,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveSummaryReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
        "function",
      passiveSummaryCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport ===
        "function",
      passiveSummaryCompleted,
      summaryUsedOnlyPrimitiveReportShapes:
        passiveSummaryReport &&
        passiveSummaryReport.summaryUsedOnlyPrimitiveReportShapes === true,
      summaryExecutedAgainstLiveMetadata: false,
      checklistChecksExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 343,
    phaseClosedOut: 342,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveSummaryReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
      "function",
    passiveSummaryCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport ===
      "function",
    passiveSummaryCompleted,
    summaryUsedOnlyPrimitiveReportShapes:
      passiveSummaryReport &&
      passiveSummaryReport.summaryUsedOnlyPrimitiveReportShapes === true,
    summaryExecutedAgainstLiveMetadata: false,
    checklistChecksExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const passiveSummarySequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.passiveSummaryCompleted === true &&
    sequenceCloseoutReport.summaryUsedOnlyPrimitiveReportShapes === true
  );
  if (!passiveSummarySequenceComplete) {
    blockers.push("passive-validation-summary-sequence-not-complete");
  }
  const passiveValidationChainSummaryAvailable = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.passiveSummaryReportAvailable === true &&
    sequenceCloseoutReport.passiveSummaryCloseoutReportAvailable === true
  );
  if (!passiveValidationChainSummaryAvailable) {
    blockers.push("passive-validation-chain-summary-not-available");
  }
  const safetyNotes = [
    "future-work-must-remain-passive",
    "future-work-must-use-only-primitive-report-shapes",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-passive-validation-milestone-planning-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep =
    "future-passive-validation-milestone-planning-phase";

  if (missingKey) {
    return {
      phase: 343,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      passiveSummarySequenceComplete,
      passiveValidationChainSummaryAvailable,
      readyForFuturePassiveValidationMilestonePlanning:
        blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureWorkShouldRemainPassive: true,
      futureWorkShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 343,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    passiveSummarySequenceComplete,
    passiveValidationChainSummaryAvailable,
    readyForFuturePassiveValidationMilestonePlanning:
      blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureWorkShouldRemainPassive: true,
    futureWorkShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport(
  options = {}
) {
  const summarySequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport(
          options
        )
      : null;
  const passiveSummaryReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryReport(options)
      : null;
  const checklistSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport(
          options
        )
      : null;
  const nonRuntimeReviewSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    summarySequenceSelfReview &&
    Array.isArray(summarySequenceSelfReview.requiredOptionKeys)
      ? summarySequenceSelfReview.requiredOptionKeys
      : passiveSummaryReport && Array.isArray(passiveSummaryReport.requiredOptionKeys)
        ? passiveSummaryReport.requiredOptionKeys
        : checklistSequenceSelfReview &&
            Array.isArray(checklistSequenceSelfReview.requiredOptionKeys)
          ? checklistSequenceSelfReview.requiredOptionKeys
          : nonRuntimeReviewSequenceSelfReview &&
              Array.isArray(nonRuntimeReviewSequenceSelfReview.requiredOptionKeys)
            ? nonRuntimeReviewSequenceSelfReview.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    summarySequenceSelfReview && summarySequenceSelfReview.blockedBehavior
      ? summarySequenceSelfReview.blockedBehavior
      : passiveSummaryReport && passiveSummaryReport.blockedBehavior
        ? passiveSummaryReport.blockedBehavior
        : checklistSequenceSelfReview && checklistSequenceSelfReview.blockedBehavior
          ? checklistSequenceSelfReview.blockedBehavior
          : nonRuntimeReviewSequenceSelfReview &&
              nonRuntimeReviewSequenceSelfReview.blockedBehavior
            ? nonRuntimeReviewSequenceSelfReview.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };
  const preservedSystems =
    summarySequenceSelfReview && summarySequenceSelfReview.preservedSystems
      ? summarySequenceSelfReview.preservedSystems
      : passiveSummaryReport && passiveSummaryReport.preservedSystems
        ? passiveSummaryReport.preservedSystems
        : checklistSequenceSelfReview && checklistSequenceSelfReview.preservedSystems
          ? checklistSequenceSelfReview.preservedSystems
          : nonRuntimeReviewSequenceSelfReview &&
              nonRuntimeReviewSequenceSelfReview.preservedSystems
            ? nonRuntimeReviewSequenceSelfReview.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };
  const safetyFlags =
    summarySequenceSelfReview && summarySequenceSelfReview.safetyFlags
      ? summarySequenceSelfReview.safetyFlags
      : passiveSummaryReport && passiveSummaryReport.safetyFlags
        ? passiveSummaryReport.safetyFlags
        : checklistSequenceSelfReview && checklistSequenceSelfReview.safetyFlags
          ? checklistSequenceSelfReview.safetyFlags
          : nonRuntimeReviewSequenceSelfReview &&
              nonRuntimeReviewSequenceSelfReview.safetyFlags
            ? nonRuntimeReviewSequenceSelfReview.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };
  const plannedMilestoneSections = [
    "validation-chain-overview",
    "summary-chain-status",
    "checklist-chain-status",
    "non-runtime-review-chain-status",
    "blocked-runtime-safety-status"
  ];
  const plannedSummaryMilestoneFields = [
    "passiveSummarySequenceComplete",
    "passiveValidationChainSummaryAvailable",
    "summaryUsedOnlyPrimitiveReportShapes",
    "summaryExecutedAgainstLiveMetadata"
  ];
  const plannedChecklistMilestoneFields = [
    "passiveChecklistSequenceComplete",
    "passiveChecklistCompleted",
    "checklistUsedOnlyPrimitiveReportShapes",
    "checklistExecutedAgainstLiveMetadata"
  ];
  const plannedNonRuntimeReviewMilestoneFields = [
    "passiveReviewSequenceComplete",
    "readyForFuturePassiveValidationChecklistPlanning",
    "futureWorkShouldRemainPassive"
  ];
  const plannedBlockedRuntimeMilestoneFields = [
    "runtimeValidationEnabled",
    "enforcementEnabled",
    "validatesLiveMetadata",
    "validationExecuted",
    "resultSystemCreated",
    "sharedStateCreated",
    "mapAttached",
    "drawingEnabled",
    "startupWired"
  ];
  const blockers = [];
  if (
    !summarySequenceSelfReview ||
    summarySequenceSelfReview.readyForFuturePassiveValidationMilestonePlanning !==
      true
  ) {
    blockers.push("passive-validation-summary-sequence-not-ready");
  }
  if (
    !checklistSequenceSelfReview ||
    checklistSequenceSelfReview.readyForFuturePassiveValidationSummaryPlanning !==
      true
  ) {
    blockers.push("passive-validation-checklist-sequence-not-ready");
  }
  if (
    !nonRuntimeReviewSequenceSelfReview ||
    nonRuntimeReviewSequenceSelfReview
      .readyForFuturePassiveValidationChecklistPlanning !== true
  ) {
    blockers.push("passive-non-runtime-review-sequence-not-ready");
  }
  const concerns = [
    "milestone-plan-does-not-authorize-runtime-validation",
    "future-milestone-helper-must-remain-primitive-and-passive"
  ];
  const recommendedNextStep =
    "future-passive-validation-milestone-helper-planning-phase";

  if (missingKey) {
    return {
      phase: 344,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      passiveOnly: true,
      shapeOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedMilestoneName:
        "custom25DVisualManualRendererPassiveValidationMilestone",
      milestoneCreated: false,
      milestoneExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      plannedMilestoneSections,
      plannedSummaryMilestoneFields,
      plannedChecklistMilestoneFields,
      plannedNonRuntimeReviewMilestoneFields,
      plannedBlockedRuntimeMilestoneFields,
      recommendedNextStep,
      blockers,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 344,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    passiveOnly: true,
    shapeOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedMilestoneName:
      "custom25DVisualManualRendererPassiveValidationMilestone",
    milestoneCreated: false,
    milestoneExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    plannedMilestoneSections,
    plannedSummaryMilestoneFields,
    plannedChecklistMilestoneFields,
    plannedNonRuntimeReviewMilestoneFields,
    plannedBlockedRuntimeMilestoneFields,
    recommendedNextStep,
    blockers,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport(
  options = {}
) {
  const milestonePlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport(
          options
        )
      : null;
  const requiredOptionKeys =
    milestonePlanReport && Array.isArray(milestonePlanReport.requiredOptionKeys)
      ? milestonePlanReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    milestonePlanReport && milestonePlanReport.blockedBehavior
      ? milestonePlanReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    milestonePlanReport && milestonePlanReport.preservedSystems
      ? milestonePlanReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    milestonePlanReport && milestonePlanReport.safetyFlags
      ? milestonePlanReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const readyForFuturePassiveValidationMilestoneHelper = !!(
    milestonePlanReport &&
    Array.isArray(milestonePlanReport.blockers) &&
    milestonePlanReport.blockers.length === 0
  );
  if (!readyForFuturePassiveValidationMilestoneHelper) {
    blockers.push("passive-validation-milestone-plan-not-ready");
  }
  const safetyNotes = [
    "future-milestone-helper-must-remain-passive",
    "future-milestone-helper-must-use-only-primitive-report-shapes",
    "runtime-validation-must-remain-disabled",
    "live-metadata-validation-must-remain-blocked"
  ];
  const concerns = [
    "readiness-report-does-not-authorize-milestone-execution",
    "future-milestone-helper-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep =
    "future-passive-validation-milestone-helper-phase";

  if (missingKey) {
    return {
      phase: 344,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForFuturePassiveValidationMilestoneHelper,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureMilestoneShouldRemainPassive: true,
      futureMilestoneShouldUseOnlyPrimitiveReportShapes: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 344,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForFuturePassiveValidationMilestoneHelper,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureMilestoneShouldRemainPassive: true,
    futureMilestoneShouldUseOnlyPrimitiveReportShapes: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport(
  options = {}
) {
  const milestonePlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport(
          options
        )
      : null;
  const milestoneReadinessReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport(
          options
        )
      : null;
  const requiredOptionKeys =
    milestoneReadinessReport &&
    Array.isArray(milestoneReadinessReport.requiredOptionKeys)
      ? milestoneReadinessReport.requiredOptionKeys
      : milestonePlanReport && Array.isArray(milestonePlanReport.requiredOptionKeys)
        ? milestonePlanReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    milestoneReadinessReport && milestoneReadinessReport.blockedBehavior
      ? milestoneReadinessReport.blockedBehavior
      : milestonePlanReport && milestonePlanReport.blockedBehavior
        ? milestonePlanReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    milestoneReadinessReport && milestoneReadinessReport.preservedSystems
      ? milestoneReadinessReport.preservedSystems
      : milestonePlanReport && milestonePlanReport.preservedSystems
        ? milestonePlanReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    milestoneReadinessReport && milestoneReadinessReport.safetyFlags
      ? milestoneReadinessReport.safetyFlags
      : milestonePlanReport && milestonePlanReport.safetyFlags
        ? milestonePlanReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-readiness-report-unavailable");
  }
  if (
    !milestonePlanReport ||
    !milestoneReadinessReport ||
    milestoneReadinessReport.readyForFuturePassiveValidationMilestoneHelper !==
      true
  ) {
    concerns.push("passive-validation-milestone-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 344,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      milestonePlanAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
        "function",
      milestoneReadinessAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
        "function",
      milestoneCreated: false,
      milestoneExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 344,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    milestonePlanAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
      "function",
    milestoneReadinessAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
      "function",
    milestoneCreated: false,
    milestoneExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport(
  options = {}
) {
  const milestonePlanReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport(
          options
        )
      : null;
  const milestoneReadinessReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport(
          options
        )
      : null;
  const milestonePlanCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    milestonePlanCloseoutReport &&
    Array.isArray(milestonePlanCloseoutReport.requiredOptionKeys)
      ? milestonePlanCloseoutReport.requiredOptionKeys
      : milestoneReadinessReport &&
          Array.isArray(milestoneReadinessReport.requiredOptionKeys)
        ? milestoneReadinessReport.requiredOptionKeys
        : milestonePlanReport && Array.isArray(milestonePlanReport.requiredOptionKeys)
          ? milestonePlanReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    milestonePlanCloseoutReport && milestonePlanCloseoutReport.blockedBehavior
      ? milestonePlanCloseoutReport.blockedBehavior
      : milestoneReadinessReport && milestoneReadinessReport.blockedBehavior
        ? milestoneReadinessReport.blockedBehavior
        : milestonePlanReport && milestonePlanReport.blockedBehavior
          ? milestonePlanReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    milestonePlanCloseoutReport && milestonePlanCloseoutReport.preservedSystems
      ? milestonePlanCloseoutReport.preservedSystems
      : milestoneReadinessReport && milestoneReadinessReport.preservedSystems
        ? milestoneReadinessReport.preservedSystems
        : milestonePlanReport && milestonePlanReport.preservedSystems
          ? milestonePlanReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    milestonePlanCloseoutReport && milestonePlanCloseoutReport.safetyFlags
      ? milestonePlanCloseoutReport.safetyFlags
      : milestoneReadinessReport && milestoneReadinessReport.safetyFlags
        ? milestoneReadinessReport.safetyFlags
        : milestonePlanReport && milestonePlanReport.safetyFlags
          ? milestonePlanReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const milestonePlanningComplete = !!(
    milestonePlanReport &&
    milestoneReadinessReport &&
    milestonePlanCloseoutReport &&
    Array.isArray(milestonePlanReport.blockers) &&
    milestonePlanReport.blockers.length === 0 &&
    milestoneReadinessReport.readyForFuturePassiveValidationMilestoneHelper ===
      true &&
    milestonePlanCloseoutReport.milestonePlanAvailable === true &&
    milestonePlanCloseoutReport.milestoneReadinessAvailable === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-plan-closeout-report-unavailable");
  }
  if (!milestonePlanningComplete) {
    concerns.push("passive-validation-milestone-plan-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 345,
      phaseClosedOut: 344,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      milestonePlanReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
        "function",
      milestoneReadinessReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
        "function",
      milestonePlanCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport ===
        "function",
      milestonePlanningComplete,
      milestoneHelperCreated: false,
      milestoneExecuted: false,
      checklistChecksExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 345,
    phaseClosedOut: 344,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    milestonePlanReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
      "function",
    milestoneReadinessReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
      "function",
    milestonePlanCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport ===
      "function",
    milestonePlanningComplete,
    milestoneHelperCreated: false,
    milestoneExecuted: false,
    checklistChecksExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const milestonePlanningSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.milestonePlanningComplete === true
  );
  if (!milestonePlanningSequenceComplete) {
    blockers.push("passive-validation-milestone-plan-sequence-not-complete");
  }
  const safetyNotes = [
    "future-milestone-helper-must-remain-passive",
    "future-milestone-helper-must-use-only-primitive-report-shapes",
    "future-milestone-helper-must-not-execute-live-validation",
    "runtime-validation-must-stay-disabled"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "future-passive-validation-milestone-helper-requires-separate-reviewed-phase"
  ];
  const recommendedNextStep = "future-passive-validation-milestone-helper-phase";

  if (missingKey) {
    return {
      phase: 345,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      milestonePlanningSequenceComplete,
      readyForFuturePassiveValidationMilestoneHelper:
        blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureMilestoneShouldRemainPassive: true,
      futureMilestoneShouldUseOnlyPrimitiveReportShapes: true,
      futureMilestoneShouldNotExecuteLiveValidation: true,
      blockers,
      safetyNotes,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 345,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    milestonePlanningSequenceComplete,
    readyForFuturePassiveValidationMilestoneHelper:
      blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureMilestoneShouldRemainPassive: true,
    futureMilestoneShouldUseOnlyPrimitiveReportShapes: true,
    futureMilestoneShouldNotExecuteLiveValidation: true,
    blockers,
    safetyNotes,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestoneReport(
  options = {}
) {
  const milestonePlanningSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveSummarySequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport(
          options
        )
      : null;
  const passiveSummaryReport =
    typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationSummaryReport(options)
      : null;
  const passiveChecklistSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveChecklistReport =
    typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationChecklistReport(options)
      : null;
  const passiveNonRuntimeReviewSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport(
          options
        )
      : null;
  const passiveNonRuntimeReviewReport =
    typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    milestonePlanningSequenceSelfReview &&
    Array.isArray(milestonePlanningSequenceSelfReview.requiredOptionKeys)
      ? milestonePlanningSequenceSelfReview.requiredOptionKeys
      : passiveSummarySequenceSelfReview &&
          Array.isArray(passiveSummarySequenceSelfReview.requiredOptionKeys)
        ? passiveSummarySequenceSelfReview.requiredOptionKeys
        : passiveSummaryReport &&
            Array.isArray(passiveSummaryReport.requiredOptionKeys)
          ? passiveSummaryReport.requiredOptionKeys
          : passiveChecklistSequenceSelfReview &&
              Array.isArray(passiveChecklistSequenceSelfReview.requiredOptionKeys)
            ? passiveChecklistSequenceSelfReview.requiredOptionKeys
            : passiveChecklistReport &&
                Array.isArray(passiveChecklistReport.requiredOptionKeys)
              ? passiveChecklistReport.requiredOptionKeys
              : passiveNonRuntimeReviewSequenceSelfReview &&
                  Array.isArray(
                    passiveNonRuntimeReviewSequenceSelfReview.requiredOptionKeys
                  )
                ? passiveNonRuntimeReviewSequenceSelfReview.requiredOptionKeys
                : passiveNonRuntimeReviewReport &&
                    Array.isArray(passiveNonRuntimeReviewReport.requiredOptionKeys)
                  ? passiveNonRuntimeReviewReport.requiredOptionKeys
                  : [
                      "manual",
                      "developerIntent",
                      "localDevOnly",
                      "browserConsoleOnly",
                      "explicitOptionsOnly",
                      "allowManualRendererStateContainerShell",
                      "noStartupWiring",
                      "noBackendChanges",
                      "noPersistence",
                      "noAutomaticInvocation"
                    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    milestonePlanningSequenceSelfReview &&
    milestonePlanningSequenceSelfReview.blockedBehavior
      ? milestonePlanningSequenceSelfReview.blockedBehavior
      : passiveSummarySequenceSelfReview &&
          passiveSummarySequenceSelfReview.blockedBehavior
        ? passiveSummarySequenceSelfReview.blockedBehavior
        : passiveSummaryReport && passiveSummaryReport.blockedBehavior
          ? passiveSummaryReport.blockedBehavior
          : passiveChecklistSequenceSelfReview &&
              passiveChecklistSequenceSelfReview.blockedBehavior
            ? passiveChecklistSequenceSelfReview.blockedBehavior
            : passiveChecklistReport && passiveChecklistReport.blockedBehavior
              ? passiveChecklistReport.blockedBehavior
              : passiveNonRuntimeReviewSequenceSelfReview &&
                  passiveNonRuntimeReviewSequenceSelfReview.blockedBehavior
                ? passiveNonRuntimeReviewSequenceSelfReview.blockedBehavior
                : passiveNonRuntimeReviewReport &&
                    passiveNonRuntimeReviewReport.blockedBehavior
                  ? passiveNonRuntimeReviewReport.blockedBehavior
                  : {
                      rendererCreation: true,
                      rendererInitialization: true,
                      rendererRun: true,
                      mapAttachment: true,
                      drawing: true,
                      domCreation: true,
                      startupWiring: true,
                      automaticInvocation: true,
                      gameplayChanges: true,
                      pinChanges: true,
                      uiChanges: true,
                      backendChanges: true,
                      storageWrites: true,
                      networkAccess: true
                    };
  const preservedSystems =
    milestonePlanningSequenceSelfReview &&
    milestonePlanningSequenceSelfReview.preservedSystems
      ? milestonePlanningSequenceSelfReview.preservedSystems
      : passiveSummarySequenceSelfReview &&
          passiveSummarySequenceSelfReview.preservedSystems
        ? passiveSummarySequenceSelfReview.preservedSystems
        : passiveSummaryReport && passiveSummaryReport.preservedSystems
          ? passiveSummaryReport.preservedSystems
          : passiveChecklistSequenceSelfReview &&
              passiveChecklistSequenceSelfReview.preservedSystems
            ? passiveChecklistSequenceSelfReview.preservedSystems
            : passiveChecklistReport && passiveChecklistReport.preservedSystems
              ? passiveChecklistReport.preservedSystems
              : passiveNonRuntimeReviewSequenceSelfReview &&
                  passiveNonRuntimeReviewSequenceSelfReview.preservedSystems
                ? passiveNonRuntimeReviewSequenceSelfReview.preservedSystems
                : passiveNonRuntimeReviewReport &&
                    passiveNonRuntimeReviewReport.preservedSystems
                  ? passiveNonRuntimeReviewReport.preservedSystems
                  : {
                      existingLeafletMapBehavior: true,
                      osmBehavior: true,
                      gameplay: true,
                      pins: true,
                      playerMarker: true,
                      captureRadius: true,
                      ui: true,
                      backend: true,
                      storage: true,
                      network: true
                    };
  const safetyFlags =
    milestonePlanningSequenceSelfReview &&
    milestonePlanningSequenceSelfReview.safetyFlags
      ? milestonePlanningSequenceSelfReview.safetyFlags
      : passiveSummarySequenceSelfReview &&
          passiveSummarySequenceSelfReview.safetyFlags
        ? passiveSummarySequenceSelfReview.safetyFlags
        : passiveSummaryReport && passiveSummaryReport.safetyFlags
          ? passiveSummaryReport.safetyFlags
          : passiveChecklistSequenceSelfReview &&
              passiveChecklistSequenceSelfReview.safetyFlags
            ? passiveChecklistSequenceSelfReview.safetyFlags
            : passiveChecklistReport && passiveChecklistReport.safetyFlags
              ? passiveChecklistReport.safetyFlags
              : passiveNonRuntimeReviewSequenceSelfReview &&
                  passiveNonRuntimeReviewSequenceSelfReview.safetyFlags
                ? passiveNonRuntimeReviewSequenceSelfReview.safetyFlags
                : passiveNonRuntimeReviewReport &&
                    passiveNonRuntimeReviewReport.safetyFlags
                  ? passiveNonRuntimeReviewReport.safetyFlags
                  : {
                      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                      landmarkTestMarkers:
                        ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                      landmarkSampleData:
                        ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                      dinosaurSitesAuData:
                        ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
                    };
  const milestonePlanningSequenceAvailable = !!(
    milestonePlanningSequenceSelfReview &&
    milestonePlanningSequenceSelfReview.sequenceReviewed === true
  );
  const passiveSummaryAvailable = !!(
    passiveSummaryReport &&
    passiveSummaryReport.summaryOnly === true
  );
  const passiveSummarySequenceAvailable = !!(
    passiveSummarySequenceSelfReview &&
    passiveSummarySequenceSelfReview.sequenceReviewed === true
  );
  const passiveChecklistAvailable = !!(
    passiveChecklistReport &&
    passiveChecklistReport.checklistOnly === true
  );
  const passiveChecklistSequenceAvailable = !!(
    passiveChecklistSequenceSelfReview &&
    passiveChecklistSequenceSelfReview.sequenceReviewed === true
  );
  const passiveNonRuntimeReviewAvailable = !!(
    passiveNonRuntimeReviewReport &&
    passiveNonRuntimeReviewReport.reportOnly === true
  );
  const passiveNonRuntimeReviewSequenceAvailable = !!(
    passiveNonRuntimeReviewSequenceSelfReview &&
    passiveNonRuntimeReviewSequenceSelfReview.sequenceReviewed === true
  );
  const shapeOnlyComparisonConfirmed = !!(
    passiveSummaryReport &&
    passiveSummaryReport.shapeOnlyComparisonConfirmed === true &&
    passiveChecklistReport &&
    passiveChecklistReport.shapeOnly === true &&
    passiveNonRuntimeReviewReport &&
    passiveNonRuntimeReviewReport.shapeOnlyComparison === true
  );
  const blockedRuntimeChecksConfirmed = !!(
    blockedBehavior.rendererCreation === true &&
    blockedBehavior.rendererInitialization === true &&
    blockedBehavior.rendererRun === true &&
    blockedBehavior.mapAttachment === true &&
    blockedBehavior.drawing === true &&
    blockedBehavior.domCreation === true &&
    blockedBehavior.startupWiring === true &&
    blockedBehavior.backendChanges === true &&
    blockedBehavior.storageWrites === true &&
    blockedBehavior.networkAccess === true
  );
  const passiveValidationMilestoneComplete = !!(
    milestonePlanningSequenceAvailable &&
    passiveSummaryAvailable &&
    passiveSummarySequenceAvailable &&
    passiveChecklistAvailable &&
    passiveChecklistSequenceAvailable &&
    passiveNonRuntimeReviewAvailable &&
    passiveNonRuntimeReviewSequenceAvailable &&
    shapeOnlyComparisonConfirmed &&
    blockedRuntimeChecksConfirmed
  );
  const passiveValidationChainReadyForFuturePlanning = !!(
    passiveValidationMilestoneComplete &&
    milestonePlanningSequenceSelfReview &&
    milestonePlanningSequenceSelfReview.readyForFuturePassiveValidationMilestoneHelper ===
      true
  );
  const blockers = [];
  if (!milestonePlanningSequenceAvailable) {
    blockers.push("milestone-planning-sequence-unavailable");
  }
  if (!passiveSummaryAvailable) {
    blockers.push("passive-summary-report-unavailable");
  }
  if (!passiveSummarySequenceAvailable) {
    blockers.push("passive-summary-sequence-unavailable");
  }
  if (!passiveChecklistAvailable) {
    blockers.push("passive-checklist-report-unavailable");
  }
  if (!passiveChecklistSequenceAvailable) {
    blockers.push("passive-checklist-sequence-unavailable");
  }
  if (!passiveNonRuntimeReviewAvailable) {
    blockers.push("passive-non-runtime-review-report-unavailable");
  }
  if (!passiveNonRuntimeReviewSequenceAvailable) {
    blockers.push("passive-non-runtime-review-sequence-unavailable");
  }
  if (!shapeOnlyComparisonConfirmed) {
    blockers.push("shape-only-comparison-not-confirmed");
  }
  if (!blockedRuntimeChecksConfirmed) {
    blockers.push("blocked-runtime-checks-not-confirmed");
  }
  const concerns = [
    "milestone-report-does-not-authorize-runtime-validation",
    "milestone-report-remains-primitive-and-passive-only"
  ];
  const safetyNotes = [
    "milestone-uses-only-primitive-report-shapes",
    "milestone-does-not-read-live-metadata",
    "runtime-validation-remains-disabled",
    "live-metadata-validation-remains-blocked"
  ];

  if (missingKey) {
    return {
      phase: 346,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestoneReport",
      ok: true,
      allowed: false,
      blocked: true,
      milestoneOnly: true,
      reportOnly: true,
      passiveOnly: true,
      shapeOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      milestoneUsedOnlyPrimitiveReportShapes: true,
      milestoneExecutedAgainstLiveMetadata: false,
      checklistChecksExecuted: false,
      milestonePlanningSequenceAvailable,
      passiveSummaryAvailable,
      passiveSummarySequenceAvailable,
      passiveChecklistAvailable,
      passiveChecklistSequenceAvailable,
      passiveNonRuntimeReviewAvailable,
      passiveNonRuntimeReviewSequenceAvailable,
      passiveValidationMilestoneComplete,
      passiveValidationChainReadyForFuturePlanning,
      shapeOnlyComparisonConfirmed,
      blockedRuntimeChecksConfirmed,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      blockers,
      concerns,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 346,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestoneReport",
    ok: true,
    allowed: true,
    blocked: false,
    milestoneOnly: true,
    reportOnly: true,
    passiveOnly: true,
    shapeOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    milestoneUsedOnlyPrimitiveReportShapes: true,
    milestoneExecutedAgainstLiveMetadata: false,
    checklistChecksExecuted: false,
    milestonePlanningSequenceAvailable,
    passiveSummaryAvailable,
    passiveSummarySequenceAvailable,
    passiveChecklistAvailable,
    passiveChecklistSequenceAvailable,
    passiveNonRuntimeReviewAvailable,
    passiveNonRuntimeReviewSequenceAvailable,
    passiveValidationMilestoneComplete,
    passiveValidationChainReadyForFuturePlanning,
    shapeOnlyComparisonConfirmed,
    blockedRuntimeChecksConfirmed,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    blockers,
    concerns,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport(
  options = {}
) {
  const passiveMilestoneReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneReport(options)
      : null;
  const requiredOptionKeys =
    passiveMilestoneReport &&
    Array.isArray(passiveMilestoneReport.requiredOptionKeys)
      ? passiveMilestoneReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveMilestoneReport && passiveMilestoneReport.blockedBehavior
      ? passiveMilestoneReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    passiveMilestoneReport && passiveMilestoneReport.preservedSystems
      ? passiveMilestoneReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    passiveMilestoneReport && passiveMilestoneReport.safetyFlags
      ? passiveMilestoneReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const passiveMilestoneCompleted = !!(
    passiveMilestoneReport &&
    passiveMilestoneReport.passiveValidationMilestoneComplete === true &&
    passiveMilestoneReport.milestoneUsedOnlyPrimitiveReportShapes === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-report-unavailable");
  }
  if (!passiveMilestoneCompleted) {
    concerns.push("passive-validation-milestone-not-complete");
  }

  if (missingKey) {
    return {
      phase: 346,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveMilestoneAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
        "function",
      passiveMilestoneCompleted,
      milestoneUsedOnlyPrimitiveReportShapes:
        passiveMilestoneReport &&
        passiveMilestoneReport.milestoneUsedOnlyPrimitiveReportShapes === true,
      milestoneExecutedAgainstLiveMetadata: false,
      checklistChecksExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 346,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveMilestoneAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
      "function",
    passiveMilestoneCompleted,
    milestoneUsedOnlyPrimitiveReportShapes:
      passiveMilestoneReport &&
      passiveMilestoneReport.milestoneUsedOnlyPrimitiveReportShapes === true,
    milestoneExecutedAgainstLiveMetadata: false,
    checklistChecksExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport(
  options = {}
) {
  const passiveMilestoneReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneReport(options)
      : null;
  const passiveMilestoneCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    passiveMilestoneCloseoutReport &&
    Array.isArray(passiveMilestoneCloseoutReport.requiredOptionKeys)
      ? passiveMilestoneCloseoutReport.requiredOptionKeys
      : passiveMilestoneReport &&
          Array.isArray(passiveMilestoneReport.requiredOptionKeys)
        ? passiveMilestoneReport.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passiveMilestoneCloseoutReport &&
    passiveMilestoneCloseoutReport.blockedBehavior
      ? passiveMilestoneCloseoutReport.blockedBehavior
      : passiveMilestoneReport && passiveMilestoneReport.blockedBehavior
        ? passiveMilestoneReport.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    passiveMilestoneCloseoutReport &&
    passiveMilestoneCloseoutReport.preservedSystems
      ? passiveMilestoneCloseoutReport.preservedSystems
      : passiveMilestoneReport && passiveMilestoneReport.preservedSystems
        ? passiveMilestoneReport.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    passiveMilestoneCloseoutReport && passiveMilestoneCloseoutReport.safetyFlags
      ? passiveMilestoneCloseoutReport.safetyFlags
      : passiveMilestoneReport && passiveMilestoneReport.safetyFlags
        ? passiveMilestoneReport.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const passiveMilestoneCompleted = !!(
    passiveMilestoneReport &&
    passiveMilestoneCloseoutReport &&
    passiveMilestoneReport.passiveValidationMilestoneComplete === true &&
    passiveMilestoneCloseoutReport.passiveMilestoneCompleted === true &&
    passiveMilestoneReport.milestoneUsedOnlyPrimitiveReportShapes === true &&
    passiveMilestoneCloseoutReport.milestoneUsedOnlyPrimitiveReportShapes === true
  );
  const concerns = [];
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-report-unavailable");
  }
  if (
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-validation-milestone-closeout-report-unavailable");
  }
  if (!passiveMilestoneCompleted) {
    concerns.push("passive-validation-milestone-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 347,
      phaseClosedOut: 346,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveMilestoneReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
        "function",
      passiveMilestoneCloseoutReportAvailable:
        typeof getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport ===
        "function",
      passiveMilestoneCompleted,
      milestoneUsedOnlyPrimitiveReportShapes:
        passiveMilestoneReport &&
        passiveMilestoneReport.milestoneUsedOnlyPrimitiveReportShapes === true,
      milestoneExecutedAgainstLiveMetadata: false,
      checklistChecksExecuted: false,
      runtimeValidationEnabled: false,
      enforcementEnabled: false,
      validatesLiveMetadata: false,
      validationExecuted: false,
      resultSystemCreated: false,
      resultObjectCreated: false,
      liveRendererCreated: false,
      callableMethodsCreated: false,
      sharedStateCreated: false,
      sharedStateMutated: false,
      sharedStateDirectlyRead: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      startupWired: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 347,
    phaseClosedOut: 346,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveMilestoneReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
      "function",
    passiveMilestoneCloseoutReportAvailable:
      typeof getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport ===
      "function",
    passiveMilestoneCompleted,
    milestoneUsedOnlyPrimitiveReportShapes:
      passiveMilestoneReport &&
      passiveMilestoneReport.milestoneUsedOnlyPrimitiveReportShapes === true,
    milestoneExecutedAgainstLiveMetadata: false,
    checklistChecksExecuted: false,
    runtimeValidationEnabled: false,
    enforcementEnabled: false,
    validatesLiveMetadata: false,
    validationExecuted: false,
    resultSystemCreated: false,
    resultObjectCreated: false,
    liveRendererCreated: false,
    callableMethodsCreated: false,
    sharedStateCreated: false,
    sharedStateMutated: false,
    sharedStateDirectlyRead: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    startupWired: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  const passiveMilestoneSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.passiveMilestoneCompleted === true
  );
  const passiveValidationChainMilestoneAvailable = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.passiveMilestoneReportAvailable === true &&
    sequenceCloseoutReport.passiveMilestoneCloseoutReportAvailable === true
  );
  if (!passiveMilestoneSequenceComplete) {
    blockers.push("passive-validation-milestone-sequence-not-complete");
  }
  if (!passiveValidationChainMilestoneAvailable) {
    blockers.push("passive-validation-milestone-chain-not-fully-available");
  }
  const safetyNotes = [
    "future-passive-validation-work-must-remain-passive",
    "future-passive-validation-work-must-use-only-primitive-report-shapes",
    "runtime-validation-must-stay-disabled",
    "live-metadata-validation-must-stay-blocked"
  ];
  const concerns = [
    "self-review-does-not-authorize-runtime-validation",
    "module-split-maintenance-pressure-should-be-tracked-separately",
    "script-js-helper-density-remains-a-maintenance-concern"
  ];
  const recommendedNextStep = "future-passive-validation-summary-milestone-phase";

  if (missingKey) {
    return {
      phase: 347,
      helperName:
        "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      passiveMilestoneSequenceComplete,
      passiveValidationChainMilestoneAvailable,
      readyForFuturePassiveValidationWork: blockers.length === 0,
      readyForRuntimeValidation: false,
      readyForLiveMetadataValidation: false,
      futureWorkShouldRemainPassive: true,
      futureWorkShouldUseOnlyPrimitiveReportShapes: true,
      moduleSplitMaintenanceConcernAcknowledged: true,
      recommendedNextStep,
      blockers,
      safetyNotes,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 347,
    helperName:
      "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    passiveMilestoneSequenceComplete,
    passiveValidationChainMilestoneAvailable,
    readyForFuturePassiveValidationWork: blockers.length === 0,
    readyForRuntimeValidation: false,
    readyForLiveMetadataValidation: false,
    futureWorkShouldRemainPassive: true,
    futureWorkShouldUseOnlyPrimitiveReportShapes: true,
    moduleSplitMaintenanceConcernAcknowledged: true,
    recommendedNextStep,
    blockers,
    safetyNotes,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualHelperDensityMaintenanceStatusReport(options = {}) {
  const moduleSplitSelfReview =
    typeof getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport ===
    "function"
      ? getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport(options)
      : null;
  const passiveMilestoneSequenceSelfReview =
    typeof getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    moduleSplitSelfReview &&
    Array.isArray(moduleSplitSelfReview.requiredOptionKeys)
      ? moduleSplitSelfReview.requiredOptionKeys
      : passiveMilestoneSequenceSelfReview &&
          Array.isArray(passiveMilestoneSequenceSelfReview.requiredOptionKeys)
        ? passiveMilestoneSequenceSelfReview.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    moduleSplitSelfReview && moduleSplitSelfReview.blockedBehavior
      ? moduleSplitSelfReview.blockedBehavior
      : passiveMilestoneSequenceSelfReview &&
          passiveMilestoneSequenceSelfReview.blockedBehavior
        ? passiveMilestoneSequenceSelfReview.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    moduleSplitSelfReview && moduleSplitSelfReview.preservedSystems
      ? moduleSplitSelfReview.preservedSystems
      : passiveMilestoneSequenceSelfReview &&
          passiveMilestoneSequenceSelfReview.preservedSystems
        ? passiveMilestoneSequenceSelfReview.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    moduleSplitSelfReview && moduleSplitSelfReview.safetyFlags
      ? moduleSplitSelfReview.safetyFlags
      : passiveMilestoneSequenceSelfReview &&
          passiveMilestoneSequenceSelfReview.safetyFlags
        ? passiveMilestoneSequenceSelfReview.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [
    "custom25dvisual-helper-density-in-script-js-is-a-maintenance-pressure",
    "script-js-file-size-pressure-remains-acknowledged",
    "future-extraction-should-stay-planning-only-until-reviewed-tiny-phases",
    "runtime-and-startup-code-should-not-be-the-first-extraction-target"
  ];
  const recommendedNextStep = "future-safe-extraction-candidate-plan-only";

  if (missingKey) {
    return {
      phase: 348,
      helperName: "getCustom25DVisualHelperDensityMaintenanceStatusReport",
      ok: true,
      allowed: false,
      blocked: true,
      statusOnly: true,
      reportOnly: true,
      maintenanceOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      helperDensityConcernAcknowledged: true,
      scriptSizeConcernAcknowledged: true,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 348,
    helperName: "getCustom25DVisualHelperDensityMaintenanceStatusReport",
    ok: true,
    allowed: true,
    blocked: false,
    statusOnly: true,
    reportOnly: true,
    maintenanceOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    helperDensityConcernAcknowledged: true,
    scriptSizeConcernAcknowledged: true,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFutureSafeExtractionCandidateReport(options = {}) {
  const maintenanceStatus =
    typeof getCustom25DVisualHelperDensityMaintenanceStatusReport === "function"
      ? getCustom25DVisualHelperDensityMaintenanceStatusReport(options)
      : null;
  const requiredOptionKeys =
    maintenanceStatus && Array.isArray(maintenanceStatus.requiredOptionKeys)
      ? maintenanceStatus.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    maintenanceStatus && maintenanceStatus.blockedBehavior
      ? maintenanceStatus.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    maintenanceStatus && maintenanceStatus.preservedSystems
      ? maintenanceStatus.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    maintenanceStatus && maintenanceStatus.safetyFlags
      ? maintenanceStatus.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const safestFirstCandidate = "passive-custom25dvisual-report-planning-helpers";
  const safeCandidateReason =
    "lowest-runtime-risk-and-most-isolated-from-startup-map-and-gameplay-behavior";
  const unsafeToMoveFirst = [
    "app-startup",
    "map-initialization",
    "leaflet-layer-creation-or-mutation",
    "player-marker",
    "capture-radius",
    "osm-pin-loading",
    "gameplay-capture-logic",
    "backend-storage-network-code",
    "dom-menu-social-qr-behavior",
    "any-helper-that-creates-or-mutates-shared-state"
  ];

  if (missingKey) {
    return {
      phase: 348,
      helperName: "getCustom25DVisualFutureSafeExtractionCandidateReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      extractionPerformed: false,
      safestFirstCandidate,
      safeCandidateReason,
      unsafeToMoveFirst,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 348,
    helperName: "getCustom25DVisualFutureSafeExtractionCandidateReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    extractionPerformed: false,
    safestFirstCandidate,
    safeCandidateReason,
    unsafeToMoveFirst,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualHelperDensityMaintenanceReadinessReport(options = {}) {
  const maintenanceStatus =
    typeof getCustom25DVisualHelperDensityMaintenanceStatusReport === "function"
      ? getCustom25DVisualHelperDensityMaintenanceStatusReport(options)
      : null;
  const extractionCandidateReport =
    typeof getCustom25DVisualFutureSafeExtractionCandidateReport === "function"
      ? getCustom25DVisualFutureSafeExtractionCandidateReport(options)
      : null;
  const requiredOptionKeys =
    extractionCandidateReport &&
    Array.isArray(extractionCandidateReport.requiredOptionKeys)
      ? extractionCandidateReport.requiredOptionKeys
      : maintenanceStatus && Array.isArray(maintenanceStatus.requiredOptionKeys)
        ? maintenanceStatus.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    extractionCandidateReport && extractionCandidateReport.blockedBehavior
      ? extractionCandidateReport.blockedBehavior
      : maintenanceStatus && maintenanceStatus.blockedBehavior
        ? maintenanceStatus.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    extractionCandidateReport && extractionCandidateReport.preservedSystems
      ? extractionCandidateReport.preservedSystems
      : maintenanceStatus && maintenanceStatus.preservedSystems
        ? maintenanceStatus.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    extractionCandidateReport && extractionCandidateReport.safetyFlags
      ? extractionCandidateReport.safetyFlags
      : maintenanceStatus && maintenanceStatus.safetyFlags
        ? maintenanceStatus.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const blockers = [
    "do-not-extract-code-directly-from-readiness-phase",
    "future-extraction-should-begin-with-manifest-planning-only",
    "runtime-and-startup-areas-still-need-extra-caution"
  ];
  const safetyNotes = [
    "first-future-extraction-should-target-passive-report-helpers-only",
    "future-extraction-phases-should-be-tiny-and-reversible",
    "runtime-behavior-must-remain-unchanged-during-any-future-extraction"
  ];
  const concerns = [
    "helper-density-remains-a-maintenance-concern",
    "script-size-pressure-is-real-but-not-a-license-to-move-runtime-code-first",
    "future-extraction-should-stay-planning-led"
  ];
  const recommendedNextPhaseType = "future-extraction-manifest-planning-phase";
  const recommendedFirstExtractionArea =
    extractionCandidateReport &&
    typeof extractionCandidateReport.safestFirstCandidate === "string"
      ? extractionCandidateReport.safestFirstCandidate
      : "passive-custom25dvisual-report-planning-helpers";
  const readyForFutureExtractionManifestPlanning = !!(
    maintenanceStatus &&
    maintenanceStatus.helperDensityConcernAcknowledged === true &&
    extractionCandidateReport &&
    extractionCandidateReport.futureExtractionShouldBeTinyPhases === true
  );

  if (missingKey) {
    return {
      phase: 348,
      helperName: "getCustom25DVisualHelperDensityMaintenanceReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForImmediateCodeExtraction: false,
      readyForFutureExtractionManifestPlanning,
      recommendedNextPhaseType,
      recommendedFirstExtractionArea,
      blockers,
      safetyNotes,
      concerns,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 348,
    helperName: "getCustom25DVisualHelperDensityMaintenanceReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForImmediateCodeExtraction: false,
    readyForFutureExtractionManifestPlanning,
    recommendedNextPhaseType,
    recommendedFirstExtractionArea,
    blockers,
    safetyNotes,
    concerns,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport(
  options = {}
) {
  const maintenanceStatusReport =
    typeof getCustom25DVisualHelperDensityMaintenanceStatusReport === "function"
      ? getCustom25DVisualHelperDensityMaintenanceStatusReport(options)
      : null;
  const safeExtractionCandidateReport =
    typeof getCustom25DVisualFutureSafeExtractionCandidateReport === "function"
      ? getCustom25DVisualFutureSafeExtractionCandidateReport(options)
      : null;
  const maintenanceReadinessReport =
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessReport ===
    "function"
      ? getCustom25DVisualHelperDensityMaintenanceReadinessReport(options)
      : null;
  const requiredOptionKeys =
    maintenanceReadinessReport &&
    Array.isArray(maintenanceReadinessReport.requiredOptionKeys)
      ? maintenanceReadinessReport.requiredOptionKeys
      : safeExtractionCandidateReport &&
          Array.isArray(safeExtractionCandidateReport.requiredOptionKeys)
        ? safeExtractionCandidateReport.requiredOptionKeys
        : maintenanceStatusReport &&
            Array.isArray(maintenanceStatusReport.requiredOptionKeys)
          ? maintenanceStatusReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    maintenanceReadinessReport && maintenanceReadinessReport.blockedBehavior
      ? maintenanceReadinessReport.blockedBehavior
      : safeExtractionCandidateReport &&
          safeExtractionCandidateReport.blockedBehavior
        ? safeExtractionCandidateReport.blockedBehavior
        : maintenanceStatusReport && maintenanceStatusReport.blockedBehavior
          ? maintenanceStatusReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    maintenanceReadinessReport && maintenanceReadinessReport.preservedSystems
      ? maintenanceReadinessReport.preservedSystems
      : safeExtractionCandidateReport &&
          safeExtractionCandidateReport.preservedSystems
        ? safeExtractionCandidateReport.preservedSystems
        : maintenanceStatusReport && maintenanceStatusReport.preservedSystems
          ? maintenanceStatusReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    maintenanceReadinessReport && maintenanceReadinessReport.safetyFlags
      ? maintenanceReadinessReport.safetyFlags
      : safeExtractionCandidateReport && safeExtractionCandidateReport.safetyFlags
        ? safeExtractionCandidateReport.safetyFlags
        : maintenanceStatusReport && maintenanceStatusReport.safetyFlags
          ? maintenanceStatusReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers:
                ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData:
                ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData:
                ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const concerns = [];
  if (
    typeof getCustom25DVisualHelperDensityMaintenanceStatusReport !== "function"
  ) {
    concerns.push("helper-density-maintenance-status-report-unavailable");
  }
  if (
    typeof getCustom25DVisualFutureSafeExtractionCandidateReport !== "function"
  ) {
    concerns.push("future-safe-extraction-candidate-report-unavailable");
  }
  if (
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessReport !==
    "function"
  ) {
    concerns.push("helper-density-maintenance-readiness-report-unavailable");
  }

  if (missingKey) {
    return {
      phase: 349,
      phaseClosedOut: 348,
      helperName:
        "getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      maintenanceOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      maintenanceStatusReportAvailable:
        typeof getCustom25DVisualHelperDensityMaintenanceStatusReport ===
        "function",
      safeExtractionCandidateReportAvailable:
        typeof getCustom25DVisualFutureSafeExtractionCandidateReport ===
        "function",
      maintenanceReadinessReportAvailable:
        typeof getCustom25DVisualHelperDensityMaintenanceReadinessReport ===
        "function",
      helperDensityConcernAcknowledged:
        maintenanceStatusReport &&
        maintenanceStatusReport.helperDensityConcernAcknowledged === true,
      scriptSizeConcernAcknowledged:
        maintenanceStatusReport &&
        maintenanceStatusReport.scriptSizeConcernAcknowledged === true,
      extractionPerformed: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 349,
    phaseClosedOut: 348,
    helperName:
      "getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    maintenanceOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    maintenanceStatusReportAvailable:
      typeof getCustom25DVisualHelperDensityMaintenanceStatusReport ===
      "function",
    safeExtractionCandidateReportAvailable:
      typeof getCustom25DVisualFutureSafeExtractionCandidateReport ===
      "function",
    maintenanceReadinessReportAvailable:
      typeof getCustom25DVisualHelperDensityMaintenanceReadinessReport ===
      "function",
    helperDensityConcernAcknowledged:
      maintenanceStatusReport &&
      maintenanceStatusReport.helperDensityConcernAcknowledged === true,
    scriptSizeConcernAcknowledged:
      maintenanceStatusReport &&
      maintenanceStatusReport.scriptSizeConcernAcknowledged === true,
    extractionPerformed: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport(
  options = {}
) {
  const closeoutReport =
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport ===
    "function"
      ? getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    closeoutReport && Array.isArray(closeoutReport.requiredOptionKeys)
      ? closeoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeoutReport && closeoutReport.blockedBehavior
      ? closeoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeoutReport && closeoutReport.preservedSystems
      ? closeoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeoutReport && closeoutReport.safetyFlags
      ? closeoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const maintenanceReadinessSequenceComplete = !!(
    closeoutReport &&
    closeoutReport.maintenanceStatusReportAvailable === true &&
    closeoutReport.safeExtractionCandidateReportAvailable === true &&
    closeoutReport.maintenanceReadinessReportAvailable === true &&
    closeoutReport.helperDensityConcernAcknowledged === true &&
    closeoutReport.scriptSizeConcernAcknowledged === true
  );
  const readyForFutureExtractionManifestPlanning = !!(
    maintenanceReadinessSequenceComplete &&
    closeoutReport &&
    closeoutReport.extractionPerformed === false
  );
  const recommendedFirstExtractionArea =
    "passive-custom25dvisual-report-planning-helpers";
  const blockers = [];
  if (!maintenanceReadinessSequenceComplete) {
    blockers.push("helper-density-maintenance-readiness-sequence-incomplete");
  }
  if (!readyForFutureExtractionManifestPlanning) {
    blockers.push("future-extraction-manifest-planning-not-ready");
  }
  const safetyNotes = [
    "immediate-code-extraction-should-remain-blocked",
    "future-extraction-should-begin-with-manifest-planning-only",
    "future-extraction-phases-should-stay-tiny-and-reversible",
    "runtime-code-should-not-be-the-first-extraction-target"
  ];
  const concerns = [
    "helper-density-remains-a-real-maintenance-concern",
    "script-size-pressure-should-not-trigger-runtime-code-moves",
    "future-extraction-work-needs-cautious-sequencing"
  ];

  if (missingKey) {
    return {
      phase: 349,
      helperName:
        "getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      maintenanceReadinessSequenceComplete,
      readyForImmediateCodeExtraction: false,
      readyForFutureExtractionManifestPlanning,
      recommendedNextPhaseType: "passive-extraction-manifest-plan",
      recommendedFirstExtractionArea,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      blockers,
      safetyNotes,
      concerns,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 349,
    helperName:
      "getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    sequenceReviewed: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    maintenanceReadinessSequenceComplete,
    readyForImmediateCodeExtraction: false,
    readyForFutureExtractionManifestPlanning,
    recommendedNextPhaseType: "passive-extraction-manifest-plan",
    recommendedFirstExtractionArea,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    blockers,
    safetyNotes,
    concerns,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionManifestPlanReport(options = {}) {
  const maintenanceSelfReview =
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
    "function"
      ? getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport(
          options
        )
      : null;
  const safeExtractionCandidate =
    typeof getCustom25DVisualFutureSafeExtractionCandidateReport === "function"
      ? getCustom25DVisualFutureSafeExtractionCandidateReport(options)
      : null;
  const requiredOptionKeys =
    maintenanceSelfReview &&
    Array.isArray(maintenanceSelfReview.requiredOptionKeys)
      ? maintenanceSelfReview.requiredOptionKeys
      : safeExtractionCandidate &&
          Array.isArray(safeExtractionCandidate.requiredOptionKeys)
        ? safeExtractionCandidate.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    maintenanceSelfReview && maintenanceSelfReview.blockedBehavior
      ? maintenanceSelfReview.blockedBehavior
      : safeExtractionCandidate && safeExtractionCandidate.blockedBehavior
        ? safeExtractionCandidate.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    maintenanceSelfReview && maintenanceSelfReview.preservedSystems
      ? maintenanceSelfReview.preservedSystems
      : safeExtractionCandidate && safeExtractionCandidate.preservedSystems
        ? safeExtractionCandidate.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    maintenanceSelfReview && maintenanceSelfReview.safetyFlags
      ? maintenanceSelfReview.safetyFlags
      : safeExtractionCandidate && safeExtractionCandidate.safetyFlags
        ? safeExtractionCandidate.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const safestFirstExtractionArea =
    safeExtractionCandidate &&
    typeof safeExtractionCandidate.safestFirstCandidate === "string"
      ? safeExtractionCandidate.safestFirstCandidate
      : "passive-custom25dvisual-report-planning-helpers";
  const plannedExtractionGroups = [
    "passive-custom25dvisual-report-planning-helpers",
    "passive-custom25dvisual-closeout-self-review-helpers",
    "passive-custom25dvisual-maintenance-planning-helpers"
  ];
  const unsafeToMoveFirst =
    safeExtractionCandidate &&
    Array.isArray(safeExtractionCandidate.unsafeToMoveFirst)
      ? safeExtractionCandidate.unsafeToMoveFirst
      : [
          "app-startup",
          "map-initialization",
          "leaflet-layer-creation-or-mutation",
          "player-marker",
          "capture-radius",
          "osm-pin-loading",
          "gameplay-capture-logic",
          "backend-storage-network-code",
          "dom-menu-social-qr-behavior",
          "any-helper-that-creates-or-mutates-shared-state"
        ];
  const requiredPreExtractionChecks = [
    "confirm-passive-only-helper-group",
    "confirm-no-runtime-side-effects",
    "confirm-no-shared-state-touchpoints",
    "confirm-no-startup-order-dependency"
  ];
  const plannedRollbackChecks = [
    "preserve-original-function-names",
    "verify-namespace-exposure-parity",
    "verify-helper-presence-report-parity",
    "revert-single-tiny-phase-if-any-runtime-diff-appears"
  ];
  const concerns = [
    "manifest-plan-does-not-authorize-code-movement",
    "extraction-should-remain-tiny-and-reviewed",
    "runtime-code-must-not-be-the-first-target"
  ];
  const recommendedNextStep = "future-passive-extraction-manifest-helper-phase";

  if (missingKey) {
    return {
      phase: 350,
      helperName: "getCustom25DVisualPassiveExtractionManifestPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      manifestOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedManifestName: "custom25DVisualPassiveExtractionManifest",
      manifestCreated: false,
      extractionPerformed: false,
      safestFirstExtractionArea,
      plannedExtractionGroups,
      unsafeToMoveFirst,
      requiredPreExtractionChecks,
      plannedRollbackChecks,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 350,
    helperName: "getCustom25DVisualPassiveExtractionManifestPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    manifestOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedManifestName: "custom25DVisualPassiveExtractionManifest",
    manifestCreated: false,
    extractionPerformed: false,
    safestFirstExtractionArea,
    plannedExtractionGroups,
    unsafeToMoveFirst,
    requiredPreExtractionChecks,
    plannedRollbackChecks,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionManifestReadinessReport(
  options = {}
) {
  const manifestPlan =
    typeof getCustom25DVisualPassiveExtractionManifestPlanReport === "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanReport(options)
      : null;
  const requiredOptionKeys =
    manifestPlan && Array.isArray(manifestPlan.requiredOptionKeys)
      ? manifestPlan.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    manifestPlan && manifestPlan.blockedBehavior
      ? manifestPlan.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    manifestPlan && manifestPlan.preservedSystems
      ? manifestPlan.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    manifestPlan && manifestPlan.safetyFlags
      ? manifestPlan.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [
    "immediate-code-extraction-must-stay-blocked",
    "future-manifest-helper-should-remain-planning-only",
    "runtime-code-still-must-not-move-first"
  ];
  const safetyNotes = [
    "manifest-readiness-does-not-authorize-extraction",
    "future-manifest-work-should-stay-tiny-and-reversible",
    "passive-helper-groups-remain-the-safest-first-target"
  ];
  const concerns = [
    "manifest-readiness-remains-planning-only",
    "runtime-behavior-must-stay-unchanged-during-any-future-extraction-phase"
  ];
  const recommendedFirstExtractionArea =
    manifestPlan &&
    typeof manifestPlan.safestFirstExtractionArea === "string"
      ? manifestPlan.safestFirstExtractionArea
      : "passive-custom25dvisual-report-planning-helpers";
  const readyForFutureExtractionManifestHelper = !!(
    manifestPlan &&
    manifestPlan.manifestOnly === true &&
    manifestPlan.extractionPerformed === false &&
    manifestPlan.futureExtractionShouldBeTinyPhases === true
  );

  if (missingKey) {
    return {
      phase: 350,
      helperName: "getCustom25DVisualPassiveExtractionManifestReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForImmediateCodeExtraction: false,
      readyForFutureExtractionManifestHelper,
      recommendedNextPhaseType: "passive-extraction-manifest-helper",
      recommendedFirstExtractionArea,
      blockers,
      safetyNotes,
      concerns,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 350,
    helperName: "getCustom25DVisualPassiveExtractionManifestReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForImmediateCodeExtraction: false,
    readyForFutureExtractionManifestHelper,
    recommendedNextPhaseType: "passive-extraction-manifest-helper",
    recommendedFirstExtractionArea,
    blockers,
    safetyNotes,
    concerns,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport(
  options = {}
) {
  const manifestPlan =
    typeof getCustom25DVisualPassiveExtractionManifestPlanReport === "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanReport(options)
      : null;
  const manifestReadiness =
    typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionManifestReadinessReport(options)
      : null;
  const requiredOptionKeys =
    manifestReadiness && Array.isArray(manifestReadiness.requiredOptionKeys)
      ? manifestReadiness.requiredOptionKeys
      : manifestPlan && Array.isArray(manifestPlan.requiredOptionKeys)
        ? manifestPlan.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    manifestReadiness && manifestReadiness.blockedBehavior
      ? manifestReadiness.blockedBehavior
      : manifestPlan && manifestPlan.blockedBehavior
        ? manifestPlan.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    manifestReadiness && manifestReadiness.preservedSystems
      ? manifestReadiness.preservedSystems
      : manifestPlan && manifestPlan.preservedSystems
        ? manifestPlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    manifestReadiness && manifestReadiness.safetyFlags
      ? manifestReadiness.safetyFlags
      : manifestPlan && manifestPlan.safetyFlags
        ? manifestPlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [];
  if (typeof getCustom25DVisualPassiveExtractionManifestPlanReport !== "function") {
    concerns.push("passive-extraction-manifest-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionManifestReadinessReport !==
    "function"
  ) {
    concerns.push("passive-extraction-manifest-readiness-report-unavailable");
  }

  if (missingKey) {
    return {
      phase: 350,
      helperName: "getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manifestPlanAvailable:
        typeof getCustom25DVisualPassiveExtractionManifestPlanReport ===
        "function",
      manifestReadinessAvailable:
        typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
        "function",
      manifestCreated: false,
      extractionPerformed: false,
      readyForImmediateCodeExtraction: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 350,
    helperName: "getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    manifestPlanAvailable:
      typeof getCustom25DVisualPassiveExtractionManifestPlanReport ===
      "function",
    manifestReadinessAvailable:
      typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
      "function",
    manifestCreated: false,
    extractionPerformed: false,
    readyForImmediateCodeExtraction: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport(
  options = {}
) {
  const manifestPlanReport =
    typeof getCustom25DVisualPassiveExtractionManifestPlanReport === "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanReport(options)
      : null;
  const manifestReadinessReport =
    typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionManifestReadinessReport(options)
      : null;
  const manifestPlanCloseoutReport =
    typeof getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    manifestPlanCloseoutReport &&
    Array.isArray(manifestPlanCloseoutReport.requiredOptionKeys)
      ? manifestPlanCloseoutReport.requiredOptionKeys
      : manifestReadinessReport &&
          Array.isArray(manifestReadinessReport.requiredOptionKeys)
        ? manifestReadinessReport.requiredOptionKeys
        : manifestPlanReport && Array.isArray(manifestPlanReport.requiredOptionKeys)
          ? manifestPlanReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    manifestPlanCloseoutReport && manifestPlanCloseoutReport.blockedBehavior
      ? manifestPlanCloseoutReport.blockedBehavior
      : manifestReadinessReport && manifestReadinessReport.blockedBehavior
        ? manifestReadinessReport.blockedBehavior
        : manifestPlanReport && manifestPlanReport.blockedBehavior
          ? manifestPlanReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    manifestPlanCloseoutReport && manifestPlanCloseoutReport.preservedSystems
      ? manifestPlanCloseoutReport.preservedSystems
      : manifestReadinessReport && manifestReadinessReport.preservedSystems
        ? manifestReadinessReport.preservedSystems
        : manifestPlanReport && manifestPlanReport.preservedSystems
          ? manifestPlanReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    manifestPlanCloseoutReport && manifestPlanCloseoutReport.safetyFlags
      ? manifestPlanCloseoutReport.safetyFlags
      : manifestReadinessReport && manifestReadinessReport.safetyFlags
        ? manifestReadinessReport.safetyFlags
        : manifestPlanReport && manifestPlanReport.safetyFlags
          ? manifestPlanReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers:
                ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData:
                ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData:
                ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const manifestPlanningComplete = !!(
    manifestPlanReport &&
    manifestReadinessReport &&
    manifestPlanCloseoutReport &&
    manifestPlanCloseoutReport.manifestPlanAvailable === true &&
    manifestPlanCloseoutReport.manifestReadinessAvailable === true &&
    manifestPlanReport.manifestOnly === true &&
    manifestReadinessReport.readyForImmediateCodeExtraction === false &&
    manifestPlanCloseoutReport.extractionPerformed === false
  );
  const concerns = [];
  if (typeof getCustom25DVisualPassiveExtractionManifestPlanReport !== "function") {
    concerns.push("passive-extraction-manifest-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionManifestReadinessReport !==
    "function"
  ) {
    concerns.push("passive-extraction-manifest-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-extraction-manifest-plan-closeout-report-unavailable");
  }
  if (!manifestPlanningComplete) {
    concerns.push("passive-extraction-manifest-planning-sequence-incomplete");
  }

  if (missingKey) {
    return {
      phase: 351,
      phaseClosedOut: 350,
      helperName:
        "getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      manifestOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      manifestPlanReportAvailable:
        typeof getCustom25DVisualPassiveExtractionManifestPlanReport ===
        "function",
      manifestReadinessReportAvailable:
        typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
        "function",
      manifestPlanCloseoutReportAvailable:
        typeof getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport ===
        "function",
      manifestPlanningComplete,
      manifestCreated: false,
      extractionPerformed: false,
      readyForImmediateCodeExtraction: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 351,
    phaseClosedOut: 350,
    helperName:
      "getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    manifestOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    manifestPlanReportAvailable:
      typeof getCustom25DVisualPassiveExtractionManifestPlanReport ===
      "function",
    manifestReadinessReportAvailable:
      typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
      "function",
    manifestPlanCloseoutReportAvailable:
      typeof getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport ===
      "function",
    manifestPlanningComplete,
    manifestCreated: false,
    extractionPerformed: false,
    readyForImmediateCodeExtraction: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseoutReport =
    typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseoutReport &&
    Array.isArray(sequenceCloseoutReport.requiredOptionKeys)
      ? sequenceCloseoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseoutReport && sequenceCloseoutReport.blockedBehavior
      ? sequenceCloseoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseoutReport && sequenceCloseoutReport.preservedSystems
      ? sequenceCloseoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseoutReport && sequenceCloseoutReport.safetyFlags
      ? sequenceCloseoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const manifestPlanningSequenceComplete = !!(
    sequenceCloseoutReport &&
    sequenceCloseoutReport.manifestPlanningComplete === true
  );
  const readyForFutureExtractionPreflightPlanning = !!(
    manifestPlanningSequenceComplete &&
    sequenceCloseoutReport &&
    sequenceCloseoutReport.extractionPerformed === false
  );
  const recommendedFirstExtractionArea =
    "passive-custom25dvisual-report-planning-helpers";
  const blockers = [];
  if (!manifestPlanningSequenceComplete) {
    blockers.push("passive-extraction-manifest-planning-sequence-incomplete");
  }
  if (!readyForFutureExtractionPreflightPlanning) {
    blockers.push("future-extraction-preflight-planning-not-ready");
  }
  const safetyNotes = [
    "immediate-code-extraction-must-remain-blocked",
    "future-preflight-planning-should-stay-passive",
    "future-extraction-should-remain-tiny-and-reversible",
    "runtime-code-should-still-not-be-the-first-target"
  ];
  const concerns = [
    "manifest-planning-sequence-remains-planning-only",
    "future-extraction-preflight-work-needs-the-same-caution",
    "runtime-behavior-must-stay-unchanged-throughout-any-future-extraction"
  ];

  if (missingKey) {
    return {
      phase: 351,
      helperName:
        "getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      manifestPlanningSequenceComplete,
      readyForImmediateCodeExtraction: false,
      readyForFutureExtractionPreflightPlanning,
      recommendedNextPhaseType: "passive-extraction-preflight-plan",
      recommendedFirstExtractionArea,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      blockers,
      safetyNotes,
      concerns,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 351,
    helperName:
      "getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    sequenceReviewed: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    manifestPlanningSequenceComplete,
    readyForImmediateCodeExtraction: false,
    readyForFutureExtractionPreflightPlanning,
    recommendedNextPhaseType: "passive-extraction-preflight-plan",
    recommendedFirstExtractionArea,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    blockers,
    safetyNotes,
    concerns,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightPlanReport(options = {}) {
  const manifestSequenceSelfReview =
    typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport(
          options
        )
      : null;
  const maintenanceSelfReview =
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
    "function"
      ? getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport(
          options
        )
      : null;
  const requiredOptionKeys =
    manifestSequenceSelfReview &&
    Array.isArray(manifestSequenceSelfReview.requiredOptionKeys)
      ? manifestSequenceSelfReview.requiredOptionKeys
      : maintenanceSelfReview &&
          Array.isArray(maintenanceSelfReview.requiredOptionKeys)
        ? maintenanceSelfReview.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    manifestSequenceSelfReview && manifestSequenceSelfReview.blockedBehavior
      ? manifestSequenceSelfReview.blockedBehavior
      : maintenanceSelfReview && maintenanceSelfReview.blockedBehavior
        ? maintenanceSelfReview.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    manifestSequenceSelfReview && manifestSequenceSelfReview.preservedSystems
      ? manifestSequenceSelfReview.preservedSystems
      : maintenanceSelfReview && maintenanceSelfReview.preservedSystems
        ? maintenanceSelfReview.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    manifestSequenceSelfReview && manifestSequenceSelfReview.safetyFlags
      ? manifestSequenceSelfReview.safetyFlags
      : maintenanceSelfReview && maintenanceSelfReview.safetyFlags
        ? maintenanceSelfReview.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const recommendedFirstExtractionArea =
    manifestSequenceSelfReview &&
    typeof manifestSequenceSelfReview.recommendedFirstExtractionArea === "string"
      ? manifestSequenceSelfReview.recommendedFirstExtractionArea
      : "passive-custom25dvisual-report-planning-helpers";
  const plannedPreflightChecks = [
    "confirm-passive-helper-group-boundary",
    "confirm-no-runtime-side-effects",
    "confirm-no-shared-state-touchpoints",
    "confirm-no-startup-sequencing-risk"
  ];
  const plannedDependencyChecks = [
    "check-local-helper-call-dependencies",
    "check-no-hidden-runtime-boot-dependencies",
    "check-no-leaflet-map-runtime-coupling"
  ];
  const plannedNamespaceExposureChecks = [
    "verify-early-bootstrap-namespace-parity",
    "verify-main-local-dev-namespace-parity",
    "verify-no-new-global-namespace-style"
  ];
  const plannedHelperPresenceChecks = [
    "verify-helper-presence-report-parity",
    "verify-new-helper-names-present-in-registry",
    "verify-no-helper-name-collisions"
  ];
  const plannedRollbackChecks = [
    "single-phase-revert-possible",
    "function-name-stability-preserved",
    "no-runtime-behavior-diff-before-accepting-extraction"
  ];
  const plannedNoRuntimeBehaviorChecks = [
    "no-code-move-in-preflight-phase",
    "no-script-load-order-change",
    "no-renderer-runtime-change",
    "no-map-or-gameplay-change"
  ];
  const plannedPostExtractionChecks = [
    "node-check-script-still-passes",
    "namespace-exposure-still-works",
    "helper-presence-report-still-complete",
    "safety-flags-still-false"
  ];
  const concerns = [
    "preflight-plan-does-not-authorize-extraction",
    "future-extraction-must-remain-tiny-and-reversible",
    "runtime-code-must-stay-out-of-scope-for-first-move"
  ];
  const recommendedNextStep = "future-passive-extraction-preflight-helper-phase";

  if (missingKey) {
    return {
      phase: 352,
      helperName: "getCustom25DVisualPassiveExtractionPreflightPlanReport",
      ok: true,
      allowed: false,
      blocked: true,
      planOnly: true,
      reportOnly: true,
      preflightOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      plannedPreflightName: "custom25DVisualPassiveExtractionPreflight",
      preflightCreated: false,
      preflightExecuted: false,
      extractionPerformed: false,
      readyForImmediateCodeExtraction: false,
      recommendedFirstExtractionArea,
      plannedPreflightChecks,
      plannedDependencyChecks,
      plannedNamespaceExposureChecks,
      plannedHelperPresenceChecks,
      plannedRollbackChecks,
      plannedNoRuntimeBehaviorChecks,
      plannedPostExtractionChecks,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      recommendedNextStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 352,
    helperName: "getCustom25DVisualPassiveExtractionPreflightPlanReport",
    ok: true,
    allowed: true,
    blocked: false,
    planOnly: true,
    reportOnly: true,
    preflightOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    plannedPreflightName: "custom25DVisualPassiveExtractionPreflight",
    preflightCreated: false,
    preflightExecuted: false,
    extractionPerformed: false,
    readyForImmediateCodeExtraction: false,
    recommendedFirstExtractionArea,
    plannedPreflightChecks,
    plannedDependencyChecks,
    plannedNamespaceExposureChecks,
    plannedHelperPresenceChecks,
    plannedRollbackChecks,
    plannedNoRuntimeBehaviorChecks,
    plannedPostExtractionChecks,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    recommendedNextStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightReadinessReport(
  options = {}
) {
  const preflightPlan =
    typeof getCustom25DVisualPassiveExtractionPreflightPlanReport === "function"
      ? getCustom25DVisualPassiveExtractionPreflightPlanReport(options)
      : null;
  const requiredOptionKeys =
    preflightPlan && Array.isArray(preflightPlan.requiredOptionKeys)
      ? preflightPlan.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    preflightPlan && preflightPlan.blockedBehavior
      ? preflightPlan.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    preflightPlan && preflightPlan.preservedSystems
      ? preflightPlan.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    preflightPlan && preflightPlan.safetyFlags
      ? preflightPlan.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const recommendedFirstExtractionArea =
    preflightPlan &&
    typeof preflightPlan.recommendedFirstExtractionArea === "string"
      ? preflightPlan.recommendedFirstExtractionArea
      : "passive-custom25dvisual-report-planning-helpers";
  const blockers = [
    "immediate-code-extraction-must-stay-blocked",
    "future-preflight-helper-must-remain-planning-only",
    "runtime-code-still-cannot-be-first-extraction-target"
  ];
  const safetyNotes = [
    "preflight-readiness-does-not-authorize-extraction",
    "future-preflight-work-should-stay-tiny-and-reversible",
    "runtime-behavior-must-remain-unchanged"
  ];
  const concerns = [
    "preflight-readiness-remains-planning-only",
    "future-extraction-work-still-needs-manifest-and-preflight-discipline"
  ];
  const readyForFutureExtractionPreflightHelper = !!(
    preflightPlan &&
    preflightPlan.preflightOnly === true &&
    preflightPlan.preflightExecuted === false &&
    preflightPlan.futureExtractionShouldBeTinyPhases === true
  );

  if (missingKey) {
    return {
      phase: 352,
      helperName: "getCustom25DVisualPassiveExtractionPreflightReadinessReport",
      ok: true,
      allowed: false,
      blocked: true,
      readinessOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForImmediateCodeExtraction: false,
      readyForFutureExtractionPreflightHelper,
      recommendedNextPhaseType: "passive-extraction-preflight-helper",
      recommendedFirstExtractionArea,
      blockers,
      safetyNotes,
      concerns,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 352,
    helperName: "getCustom25DVisualPassiveExtractionPreflightReadinessReport",
    ok: true,
    allowed: true,
    blocked: false,
    readinessOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForImmediateCodeExtraction: false,
    readyForFutureExtractionPreflightHelper,
    recommendedNextPhaseType: "passive-extraction-preflight-helper",
    recommendedFirstExtractionArea,
    blockers,
    safetyNotes,
    concerns,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport(
  options = {}
) {
  const preflightPlan =
    typeof getCustom25DVisualPassiveExtractionPreflightPlanReport === "function"
      ? getCustom25DVisualPassiveExtractionPreflightPlanReport(options)
      : null;
  const preflightReadiness =
    typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightReadinessReport(options)
      : null;
  const requiredOptionKeys =
    preflightReadiness && Array.isArray(preflightReadiness.requiredOptionKeys)
      ? preflightReadiness.requiredOptionKeys
      : preflightPlan && Array.isArray(preflightPlan.requiredOptionKeys)
        ? preflightPlan.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    preflightReadiness && preflightReadiness.blockedBehavior
      ? preflightReadiness.blockedBehavior
      : preflightPlan && preflightPlan.blockedBehavior
        ? preflightPlan.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    preflightReadiness && preflightReadiness.preservedSystems
      ? preflightReadiness.preservedSystems
      : preflightPlan && preflightPlan.preservedSystems
        ? preflightPlan.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    preflightReadiness && preflightReadiness.safetyFlags
      ? preflightReadiness.safetyFlags
      : preflightPlan && preflightPlan.safetyFlags
        ? preflightPlan.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const concerns = [];
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightPlanReport !== "function"
  ) {
    concerns.push("passive-extraction-preflight-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport !==
    "function"
  ) {
    concerns.push("passive-extraction-preflight-readiness-report-unavailable");
  }

  if (missingKey) {
    return {
      phase: 352,
      helperName: "getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      preflightPlanAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightPlanReport ===
        "function",
      preflightReadinessAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
        "function",
      preflightCreated: false,
      preflightExecuted: false,
      extractionPerformed: false,
      readyForImmediateCodeExtraction: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 352,
    helperName: "getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    preflightPlanAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightPlanReport ===
      "function",
    preflightReadinessAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
      "function",
    preflightCreated: false,
    preflightExecuted: false,
    extractionPerformed: false,
    readyForImmediateCodeExtraction: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport(
  options = {}
) {
  const preflightPlan =
    typeof getCustom25DVisualPassiveExtractionPreflightPlanReport === "function"
      ? getCustom25DVisualPassiveExtractionPreflightPlanReport(options)
      : null;
  const preflightReadiness =
    typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightReadinessReport(options)
      : null;
  const preflightCloseout =
    typeof getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    preflightCloseout && Array.isArray(preflightCloseout.requiredOptionKeys)
      ? preflightCloseout.requiredOptionKeys
      : preflightReadiness && Array.isArray(preflightReadiness.requiredOptionKeys)
        ? preflightReadiness.requiredOptionKeys
        : preflightPlan && Array.isArray(preflightPlan.requiredOptionKeys)
          ? preflightPlan.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    preflightCloseout && preflightCloseout.blockedBehavior
      ? preflightCloseout.blockedBehavior
      : preflightReadiness && preflightReadiness.blockedBehavior
        ? preflightReadiness.blockedBehavior
        : preflightPlan && preflightPlan.blockedBehavior
          ? preflightPlan.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    preflightCloseout && preflightCloseout.preservedSystems
      ? preflightCloseout.preservedSystems
      : preflightReadiness && preflightReadiness.preservedSystems
        ? preflightReadiness.preservedSystems
        : preflightPlan && preflightPlan.preservedSystems
          ? preflightPlan.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    preflightCloseout && preflightCloseout.safetyFlags
      ? preflightCloseout.safetyFlags
      : preflightReadiness && preflightReadiness.safetyFlags
        ? preflightReadiness.safetyFlags
        : preflightPlan && preflightPlan.safetyFlags
          ? preflightPlan.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const concerns = [];
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightPlanReport !== "function"
  ) {
    concerns.push("passive-extraction-preflight-plan-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport !==
    "function"
  ) {
    concerns.push("passive-extraction-preflight-readiness-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-extraction-preflight-plan-closeout-report-unavailable");
  }
  const preflightPlanningComplete = !!(
    preflightPlan &&
    preflightReadiness &&
    preflightCloseout &&
    preflightPlan.reportOnly === true &&
    preflightReadiness.reportOnly === true &&
    preflightCloseout.reportOnly === true
  );

  if (missingKey) {
    return {
      phase: 353,
      phaseClosedOut: 352,
      helperName:
        "getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      preflightOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      preflightPlanReportAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightPlanReport ===
        "function",
      preflightReadinessReportAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
        "function",
      preflightPlanCloseoutReportAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport ===
        "function",
      preflightPlanningComplete,
      preflightCreated: false,
      preflightExecuted: false,
      extractionPerformed: false,
      readyForImmediateCodeExtraction: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      externalFilesInspected: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 353,
    phaseClosedOut: 352,
    helperName:
      "getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    preflightOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    preflightPlanReportAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightPlanReport ===
      "function",
    preflightReadinessReportAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
      "function",
    preflightPlanCloseoutReportAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport ===
      "function",
    preflightPlanningComplete,
    preflightCreated: false,
    preflightExecuted: false,
    extractionPerformed: false,
    readyForImmediateCodeExtraction: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    externalFilesInspected: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport(
  options = {}
) {
  const preflightSequenceCloseout =
    typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    preflightSequenceCloseout &&
    Array.isArray(preflightSequenceCloseout.requiredOptionKeys)
      ? preflightSequenceCloseout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    preflightSequenceCloseout && preflightSequenceCloseout.blockedBehavior
      ? preflightSequenceCloseout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    preflightSequenceCloseout && preflightSequenceCloseout.preservedSystems
      ? preflightSequenceCloseout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    preflightSequenceCloseout && preflightSequenceCloseout.safetyFlags
      ? preflightSequenceCloseout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const recommendedFirstExtractionArea =
    preflightSequenceCloseout &&
    typeof preflightSequenceCloseout.recommendedFirstExtractionArea === "string"
      ? preflightSequenceCloseout.recommendedFirstExtractionArea
      : "passive-custom25dvisual-report-planning-helpers";
  const preflightPlanningSequenceComplete = !!(
    preflightSequenceCloseout &&
    preflightSequenceCloseout.preflightPlanningComplete === true
  );
  const readyForFutureExtractionPreflightHelper = !!preflightPlanningSequenceComplete;
  const blockers = [
    "immediate-code-extraction-must-remain-blocked",
    "future-preflight-helper-must-stay-passive-first",
    "runtime-code-cannot-be-the-first-extraction-target"
  ];
  const safetyNotes = [
    "preflight-sequence-self-review-does-not-authorize-extraction",
    "future-extraction-work-should-stay-tiny-and-reversible",
    "runtime-behavior-must-remain-unchanged-during-planning"
  ];
  const concerns = preflightPlanningSequenceComplete
    ? [
        "preflight-sequence-is-complete-but-still-planning-only",
        "future-extraction-helper-must-preserve-existing-namespace-and-presence-patterns"
      ]
    : ["preflight-planning-sequence-not-yet-complete"];

  if (missingKey) {
    return {
      phase: 353,
      helperName:
        "getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      preflightPlanningSequenceComplete,
      readyForImmediateCodeExtraction: false,
      readyForFutureExtractionPreflightHelper,
      recommendedNextPhaseType: "passive-extraction-preflight-helper",
      recommendedFirstExtractionArea,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      blockers,
      safetyNotes,
      concerns,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      externalFilesInspected: false,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 353,
    helperName:
      "getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    preflightPlanningSequenceComplete,
    readyForImmediateCodeExtraction: false,
    readyForFutureExtractionPreflightHelper,
    recommendedNextPhaseType: "passive-extraction-preflight-helper",
    recommendedFirstExtractionArea,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    blockers,
    safetyNotes,
    concerns,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    externalFilesInspected: false,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightReport(options = {}) {
  const maintenanceReadiness =
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
    "function"
      ? getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport(
          options
        )
      : null;
  const manifestPlanning =
    typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport(
          options
        )
      : null;
  const preflightPlanning =
    typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport(
          options
        )
      : null;
  const safeExtractionCandidate =
    typeof getCustom25DVisualFutureSafeExtractionCandidateReport === "function"
      ? getCustom25DVisualFutureSafeExtractionCandidateReport(options)
      : null;
  const requiredOptionKeys =
    preflightPlanning && Array.isArray(preflightPlanning.requiredOptionKeys)
      ? preflightPlanning.requiredOptionKeys
      : manifestPlanning && Array.isArray(manifestPlanning.requiredOptionKeys)
        ? manifestPlanning.requiredOptionKeys
        : maintenanceReadiness &&
            Array.isArray(maintenanceReadiness.requiredOptionKeys)
          ? maintenanceReadiness.requiredOptionKeys
          : safeExtractionCandidate &&
              Array.isArray(safeExtractionCandidate.requiredOptionKeys)
            ? safeExtractionCandidate.requiredOptionKeys
            : [
                "manual",
                "developerIntent",
                "localDevOnly",
                "browserConsoleOnly",
                "explicitOptionsOnly",
                "allowManualRendererStateContainerShell",
                "noStartupWiring",
                "noBackendChanges",
                "noPersistence",
                "noAutomaticInvocation"
              ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    preflightPlanning && preflightPlanning.blockedBehavior
      ? preflightPlanning.blockedBehavior
      : manifestPlanning && manifestPlanning.blockedBehavior
        ? manifestPlanning.blockedBehavior
        : maintenanceReadiness && maintenanceReadiness.blockedBehavior
          ? maintenanceReadiness.blockedBehavior
          : safeExtractionCandidate && safeExtractionCandidate.blockedBehavior
            ? safeExtractionCandidate.blockedBehavior
            : {
                rendererCreation: true,
                rendererInitialization: true,
                rendererRun: true,
                mapAttachment: true,
                drawing: true,
                domCreation: true,
                startupWiring: true,
                automaticInvocation: true,
                gameplayChanges: true,
                pinChanges: true,
                uiChanges: true,
                backendChanges: true,
                storageWrites: true,
                networkAccess: true
              };
  const preservedSystems =
    preflightPlanning && preflightPlanning.preservedSystems
      ? preflightPlanning.preservedSystems
      : manifestPlanning && manifestPlanning.preservedSystems
        ? manifestPlanning.preservedSystems
        : maintenanceReadiness && maintenanceReadiness.preservedSystems
          ? maintenanceReadiness.preservedSystems
          : safeExtractionCandidate && safeExtractionCandidate.preservedSystems
            ? safeExtractionCandidate.preservedSystems
            : {
                existingLeafletMapBehavior: true,
                osmBehavior: true,
                gameplay: true,
                pins: true,
                playerMarker: true,
                captureRadius: true,
                ui: true,
                backend: true,
                storage: true,
                network: true
              };
  const safetyFlags =
    preflightPlanning && preflightPlanning.safetyFlags
      ? preflightPlanning.safetyFlags
      : manifestPlanning && manifestPlanning.safetyFlags
        ? manifestPlanning.safetyFlags
        : maintenanceReadiness && maintenanceReadiness.safetyFlags
          ? maintenanceReadiness.safetyFlags
          : safeExtractionCandidate && safeExtractionCandidate.safetyFlags
            ? safeExtractionCandidate.safetyFlags
            : {
                custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
                landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
                landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
                dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
              };
  const safestFirstExtractionArea =
    (safeExtractionCandidate &&
      typeof safeExtractionCandidate.safestFirstExtractionArea === "string" &&
      safeExtractionCandidate.safestFirstExtractionArea) ||
    (safeExtractionCandidate &&
      typeof safeExtractionCandidate.recommendedFirstExtractionArea === "string" &&
      safeExtractionCandidate.recommendedFirstExtractionArea) ||
    (preflightPlanning &&
      typeof preflightPlanning.recommendedFirstExtractionArea === "string" &&
      preflightPlanning.recommendedFirstExtractionArea) ||
    "passive-custom25dvisual-report-planning-helpers";
  const readyForFutureTinyExtractionPlanning = !!(
    maintenanceReadiness &&
    manifestPlanning &&
    preflightPlanning &&
    maintenanceReadiness.reportOnly === true &&
    manifestPlanning.reportOnly === true &&
    preflightPlanning.reportOnly === true
  );
  const blockers = [
    "immediate-code-extraction-must-remain-blocked",
    "future-extraction-must-stay-tiny-and-reversible",
    "runtime-code-must-stay-out-of-scope-for-the-first-extraction-step"
  ];
  const safetyNotes = [
    "preflight-uses-only-primitive-report-output",
    "preflight-does-not-inspect-external-files",
    "preflight-does-not-authorize-runtime-behavior"
  ];
  const concerns = [];
  if (
    typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport !==
    "function"
  ) {
    concerns.push("helper-density-maintenance-self-review-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport !==
    "function"
  ) {
    concerns.push("passive-extraction-manifest-sequence-self-review-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport !==
    "function"
  ) {
    concerns.push("passive-extraction-preflight-sequence-self-review-unavailable");
  }
  if (typeof getCustom25DVisualFutureSafeExtractionCandidateReport !== "function") {
    concerns.push("future-safe-extraction-candidate-report-unavailable");
  }

  if (missingKey) {
    return {
      phase: 354,
      helperName: "getCustom25DVisualPassiveExtractionPreflightReport",
      ok: true,
      allowed: false,
      blocked: true,
      preflightOnly: true,
      reportOnly: true,
      passiveOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      maintenanceReadinessAvailable:
        typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
        "function",
      manifestPlanningAvailable:
        typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
        "function",
      preflightPlanningAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport ===
        "function",
      safeExtractionCandidateAvailable:
        typeof getCustom25DVisualFutureSafeExtractionCandidateReport === "function",
      safestFirstExtractionArea,
      readyForImmediateCodeExtraction: false,
      readyForFutureTinyExtractionPlanning,
      extractionPerformed: false,
      externalFilesInspected: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      futureExtractionShouldBeTinyPhases: true,
      futureExtractionShouldBeReversible: true,
      futureExtractionShouldAvoidRuntimeCodeFirst: true,
      blockers,
      safetyNotes,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 354,
    helperName: "getCustom25DVisualPassiveExtractionPreflightReport",
    ok: true,
    allowed: true,
    blocked: false,
    preflightOnly: true,
    reportOnly: true,
    passiveOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    maintenanceReadinessAvailable:
      typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
      "function",
    manifestPlanningAvailable:
      typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
      "function",
    preflightPlanningAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport ===
      "function",
    safeExtractionCandidateAvailable:
      typeof getCustom25DVisualFutureSafeExtractionCandidateReport === "function",
    safestFirstExtractionArea,
    readyForImmediateCodeExtraction: false,
    readyForFutureTinyExtractionPlanning,
    extractionPerformed: false,
    externalFilesInspected: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    futureExtractionShouldBeTinyPhases: true,
    futureExtractionShouldBeReversible: true,
    futureExtractionShouldAvoidRuntimeCodeFirst: true,
    blockers,
    safetyNotes,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightCloseoutReport(
  options = {}
) {
  const passivePreflight =
    typeof getCustom25DVisualPassiveExtractionPreflightReport === "function"
      ? getCustom25DVisualPassiveExtractionPreflightReport(options)
      : null;
  const requiredOptionKeys =
    passivePreflight && Array.isArray(passivePreflight.requiredOptionKeys)
      ? passivePreflight.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passivePreflight && passivePreflight.blockedBehavior
      ? passivePreflight.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    passivePreflight && passivePreflight.preservedSystems
      ? passivePreflight.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    passivePreflight && passivePreflight.safetyFlags
      ? passivePreflight.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const passivePreflightCompleted = !!(
    passivePreflight &&
    passivePreflight.reportOnly === true &&
    passivePreflight.passiveOnly === true
  );
  const readyForFutureTinyExtractionPlanning = !!(
    passivePreflight &&
    passivePreflight.readyForFutureTinyExtractionPlanning === true
  );
  const concerns = [];
  if (typeof getCustom25DVisualPassiveExtractionPreflightReport !== "function") {
    concerns.push("passive-extraction-preflight-report-unavailable");
  }
  if (
    passivePreflight &&
    passivePreflight.readyForImmediateCodeExtraction === true
  ) {
    concerns.push("immediate-code-extraction-must-remain-blocked");
  }

  if (missingKey) {
    return {
      phase: 354,
      helperName: "getCustom25DVisualPassiveExtractionPreflightCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passivePreflightAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightReport ===
        "function",
      passivePreflightCompleted,
      readyForImmediateCodeExtraction: false,
      readyForFutureTinyExtractionPlanning,
      extractionPerformed: false,
      externalFilesInspected: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 354,
    helperName: "getCustom25DVisualPassiveExtractionPreflightCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passivePreflightAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightReport === "function",
    passivePreflightCompleted,
    readyForImmediateCodeExtraction: false,
    readyForFutureTinyExtractionPlanning,
    extractionPerformed: false,
    externalFilesInspected: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport(
  options = {}
) {
  const passivePreflight =
    typeof getCustom25DVisualPassiveExtractionPreflightReport === "function"
      ? getCustom25DVisualPassiveExtractionPreflightReport(options)
      : null;
  const passivePreflightCloseout =
    typeof getCustom25DVisualPassiveExtractionPreflightCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    passivePreflightCloseout &&
    Array.isArray(passivePreflightCloseout.requiredOptionKeys)
      ? passivePreflightCloseout.requiredOptionKeys
      : passivePreflight && Array.isArray(passivePreflight.requiredOptionKeys)
        ? passivePreflight.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    passivePreflightCloseout && passivePreflightCloseout.blockedBehavior
      ? passivePreflightCloseout.blockedBehavior
      : passivePreflight && passivePreflight.blockedBehavior
        ? passivePreflight.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    passivePreflightCloseout && passivePreflightCloseout.preservedSystems
      ? passivePreflightCloseout.preservedSystems
      : passivePreflight && passivePreflight.preservedSystems
        ? passivePreflight.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    passivePreflightCloseout && passivePreflightCloseout.safetyFlags
      ? passivePreflightCloseout.safetyFlags
      : passivePreflight && passivePreflight.safetyFlags
        ? passivePreflight.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const safestFirstCandidate =
    (passivePreflight &&
      typeof passivePreflight.safestFirstExtractionArea === "string" &&
      passivePreflight.safestFirstExtractionArea) ||
    "passive-custom25dvisual-report-planning-helpers";
  const laterTinyExtractionCandidateSupported = !!(
    passivePreflight &&
    passivePreflight.readyForFutureTinyExtractionPlanning === true
  );
  const unsafeFirstMoveAreas = [
    "runtime-renderer-logic",
    "startup-wiring",
    "map-attachment",
    "drawing-geometry",
    "shared-live-renderer-state",
    "dom-behavior",
    "backend-storage-network",
    "gameplay-and-map-systems"
  ];
  const blockers = [
    "immediate-extraction-must-remain-blocked",
    "first-extraction-candidate-must-stay-passive-and-reversible",
    "runtime-startup-map-gameplay-shared-state-areas-are-not-valid-first-moves"
  ];
  const concerns = [];
  if (typeof getCustom25DVisualPassiveExtractionPreflightReport !== "function") {
    concerns.push("passive-extraction-preflight-report-unavailable");
  }
  if (
    typeof getCustom25DVisualPassiveExtractionPreflightCloseoutReport !==
    "function"
  ) {
    concerns.push("passive-extraction-preflight-closeout-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 355,
      helperName:
        "getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      externalFilesInspected: false,
      runtimeBehaviorChanged: false,
      immediateExtractionBlocked: true,
      phase354PreflightAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightReport ===
        "function",
      phase354CloseoutAvailable:
        typeof getCustom25DVisualPassiveExtractionPreflightCloseoutReport ===
        "function",
      laterTinyExtractionCandidateSupported,
      safestFirstCandidate,
      unsafeFirstMoveAreas,
      blockers,
      nextRecommendedStep: "plan-first-tiny-passive-extraction-phase",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 355,
    helperName:
      "getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    externalFilesInspected: false,
    runtimeBehaviorChanged: false,
    immediateExtractionBlocked: true,
    phase354PreflightAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightReport === "function",
    phase354CloseoutAvailable:
      typeof getCustom25DVisualPassiveExtractionPreflightCloseoutReport ===
      "function",
    laterTinyExtractionCandidateSupported,
    safestFirstCandidate,
    unsafeFirstMoveAreas,
    blockers,
    nextRecommendedStep: "plan-first-tiny-passive-extraction-phase",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport(
  options = {}
) {
  const sequenceCloseout =
    typeof getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    sequenceCloseout && Array.isArray(sequenceCloseout.requiredOptionKeys)
      ? sequenceCloseout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sequenceCloseout && sequenceCloseout.blockedBehavior
      ? sequenceCloseout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sequenceCloseout && sequenceCloseout.preservedSystems
      ? sequenceCloseout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sequenceCloseout && sequenceCloseout.safetyFlags
      ? sequenceCloseout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!sequenceCloseout) {
    blockers.push("phase355-sequence-closeout-report-unavailable");
  }
  blockers.push("immediate-extraction-remains-blocked");
  const concerns = [
    "readiness-is-only-for-planning-the-first-tiny-extraction-phase",
    "broad-extraction-remains-out-of-scope"
  ];
  const safetyNotes = [
    "no-code-move-file-creation-or-loading-order-change-occurred",
    "no-runtime-startup-map-gameplay-or-shared-state-behavior-was-touched",
    "all-four-custom25dvisual-safety-flags-remain-false"
  ];

  if (missingKey) {
    return {
      ok: true,
      phase: 355,
      helperName:
        "getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      sequenceReviewed: true,
      readyForPlanningFirstTinyExtractionPhase:
        !!(sequenceCloseout && sequenceCloseout.laterTinyExtractionCandidateSupported),
      readyForBroadExtraction: false,
      codeMoved: false,
      filesCreated: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      externalFilesInspected: false,
      runtimeBehaviorChanged: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      rendererRunning: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayTouched: false,
      pinsTouched: false,
      playerMarkerTouched: false,
      osmTouched: false,
      sharedStateTouched: false,
      backendTouched: false,
      storageTouched: false,
      networkTouched: false,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      blockers,
      safetyNotes,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 355,
    helperName:
      "getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    sequenceReviewed: true,
    readyForPlanningFirstTinyExtractionPhase:
      !!(sequenceCloseout && sequenceCloseout.laterTinyExtractionCandidateSupported),
    readyForBroadExtraction: false,
    codeMoved: false,
    filesCreated: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    externalFilesInspected: false,
    runtimeBehaviorChanged: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    gameplayTouched: false,
    pinsTouched: false,
    playerMarkerTouched: false,
    osmTouched: false,
    sharedStateTouched: false,
    backendTouched: false,
    storageTouched: false,
    networkTouched: false,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    blockers,
    safetyNotes,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFirstPassiveHelperMoveCloseoutReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const movedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
    "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport"
  ];
  const movedHelpersAvailableOnWindow = movedHelpers.every(
    (name) =>
      typeof globalThis !== "undefined" &&
      typeof globalThis[name] === "function"
  );
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const movedHelpersAvailableInManualNamespace = manualNamespace
    ? movedHelpers.every((name) => typeof manualNamespace[name] === "function")
    : false;
  const blockers = [];
  if (!passiveReportsNamespace || passiveReportsNamespace.phase358LoadPathAvailable !== true) {
    blockers.push("phase358-load-marker-unavailable");
  }
  if (!movedHelpersAvailableOnWindow) {
    blockers.push("moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableInManualNamespace) {
    blockers.push("moved-helpers-unavailable-in-manual-namespace");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 359,
      helperName: "getCustom25DVisualFirstPassiveHelperMoveCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers,
      indexHtmlUntouched: true,
      phase358LoadMarkerAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      movedHelpersAvailableOnWindow,
      movedHelpersAvailableInManualNamespace,
      helperPresenceRegistryPreserved:
        typeof manualNamespace?.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport === "function" &&
        typeof manualNamespace?.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport === "function",
      importsExportsAdded: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayChanged: false,
      mapChanged: false,
      drawingChanged: false,
      domChanged: false,
      leafletChanged: false,
      sharedStateChanged: false,
      backendStorageNetworkChanged: false,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 359,
    helperName: "getCustom25DVisualFirstPassiveHelperMoveCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers,
    indexHtmlUntouched: true,
    phase358LoadMarkerAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    movedHelpersAvailableOnWindow,
    movedHelpersAvailableInManualNamespace,
    helperPresenceRegistryPreserved:
      typeof manualNamespace?.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport === "function" &&
      typeof manualNamespace?.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport === "function",
    importsExportsAdded: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayChanged: false,
    mapChanged: false,
    drawingChanged: false,
    domChanged: false,
    leafletChanged: false,
    sharedStateChanged: false,
    backendStorageNetworkChanged: false,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualFirstPassiveHelperMoveCloseoutReport === "function"
      ? getCustom25DVisualFirstPassiveHelperMoveCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    closeout && Array.isArray(closeout.requiredOptionKeys)
      ? closeout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeout && closeout.blockedBehavior
      ? closeout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeout && closeout.preservedSystems
      ? closeout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeout && closeout.safetyFlags
      ? closeout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!closeout) {
    blockers.push("phase359-closeout-report-unavailable");
  }
  if (closeout && closeout.movedHelpersAvailableOnWindow !== true) {
    blockers.push("moved-helpers-not-available-on-window");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 359,
      helperName: "getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers: closeout ? closeout.movedHelpers : [],
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      mapBehaviorChanged: false,
      gameplayBehaviorChanged: false,
      drawingBehaviorChanged: false,
      domBehaviorChanged: false,
      leafletBehaviorChanged: false,
      sharedStateBehaviorChanged: false,
      backendStorageNetworkBehaviorChanged: false,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 359,
    helperName: "getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers: closeout ? closeout.movedHelpers : [],
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    mapBehaviorChanged: false,
    gameplayBehaviorChanged: false,
    drawingBehaviorChanged: false,
    domBehaviorChanged: false,
    leafletBehaviorChanged: false,
    sharedStateBehaviorChanged: false,
    backendStorageNetworkBehaviorChanged: false,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualSecondPassiveHelperMoveCloseoutReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const phase359MovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
    "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport"
  ];
  const movedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport"
  ];
  const phase359HelpersRemainPresent = phase359MovedHelpers.every(
    (name) =>
      typeof globalThis !== "undefined" &&
      typeof globalThis[name] === "function"
  );
  const movedHelpersAvailableOnWindow = movedHelpers.every(
    (name) =>
      typeof globalThis !== "undefined" &&
      typeof globalThis[name] === "function"
  );
  const movedHelpersAvailableInManualNamespace = manualNamespace
    ? movedHelpers.every((name) => typeof manualNamespace[name] === "function")
    : false;
  const helperPresenceRegistryPreserved =
    !!manualNamespace &&
    phase359MovedHelpers.every((name) => typeof manualNamespace[name] === "function") &&
    movedHelpers.every((name) => typeof manualNamespace[name] === "function");
  const blockers = [];
  if (!passiveReportsNamespace || passiveReportsNamespace.phase358LoadPathAvailable !== true) {
    blockers.push("phase358-load-marker-unavailable");
  }
  if (!phase359HelpersRemainPresent) {
    blockers.push("phase359-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableOnWindow) {
    blockers.push("phase361-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableInManualNamespace) {
    blockers.push("phase361-moved-helpers-unavailable-in-manual-namespace");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 361,
      helperName: "getCustom25DVisualSecondPassiveHelperMoveCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers,
      indexHtmlUntouched: true,
      filesCreated: false,
      phase358LoadMarkerAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      phase359MovedHelpersRemainPresent: phase359HelpersRemainPresent,
      movedHelpersAvailableOnWindow,
      movedHelpersAvailableInManualNamespace,
      helperPresenceRegistryPreserved,
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayChanged: false,
      mapChanged: false,
      drawingChanged: false,
      domChanged: false,
      leafletChanged: false,
      sharedStateChanged: false,
      backendStorageNetworkChanged: false,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 361,
    helperName: "getCustom25DVisualSecondPassiveHelperMoveCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers,
    indexHtmlUntouched: true,
    filesCreated: false,
    phase358LoadMarkerAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    phase359MovedHelpersRemainPresent: phase359HelpersRemainPresent,
    movedHelpersAvailableOnWindow,
    movedHelpersAvailableInManualNamespace,
    helperPresenceRegistryPreserved,
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayChanged: false,
    mapChanged: false,
    drawingChanged: false,
    domChanged: false,
    leafletChanged: false,
    sharedStateChanged: false,
    backendStorageNetworkChanged: false,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualSecondPassiveHelperMoveCloseoutReport === "function"
      ? getCustom25DVisualSecondPassiveHelperMoveCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    closeout && Array.isArray(closeout.requiredOptionKeys)
      ? closeout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeout && closeout.blockedBehavior
      ? closeout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeout && closeout.preservedSystems
      ? closeout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeout && closeout.safetyFlags
      ? closeout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!closeout) {
    blockers.push("phase361-closeout-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 361,
      helperName: "getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers: closeout ? closeout.movedHelpers : [],
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayBehaviorChanged: false,
      mapBehaviorChanged: false,
      drawingBehaviorChanged: false,
      domBehaviorChanged: false,
      leafletBehaviorChanged: false,
      sharedStateBehaviorChanged: false,
      backendStorageNetworkBehaviorChanged: false,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 361,
    helperName: "getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers: closeout ? closeout.movedHelpers : [],
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayBehaviorChanged: false,
    mapBehaviorChanged: false,
    drawingBehaviorChanged: false,
    domBehaviorChanged: false,
    leafletBehaviorChanged: false,
    sharedStateBehaviorChanged: false,
    backendStorageNetworkBehaviorChanged: false,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualBundledPassiveHelperMoveCloseoutReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const phase359MovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
    "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport"
  ];
  const phase361MovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport"
  ];
  const movedHelpers = [
    "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
    "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
    "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport"
  ];
  const phase359HelpersRemainPresent = phase359MovedHelpers.every(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const phase361HelpersRemainPresent = phase361MovedHelpers.every(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const movedHelpersAvailableOnWindow = movedHelpers.every(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const movedHelpersAvailableInManualNamespace = manualNamespace
    ? movedHelpers.every((name) => typeof manualNamespace[name] === "function")
    : false;
  const helperPresenceRegistryPreserved =
    !!manualNamespace &&
    [...phase359MovedHelpers, ...phase361MovedHelpers, ...movedHelpers].every(
      (name) => typeof manualNamespace[name] === "function"
    );
  const blockers = [];
  if (!passiveReportsNamespace || passiveReportsNamespace.phase358LoadPathAvailable !== true) {
    blockers.push("phase358-load-marker-unavailable");
  }
  if (!phase359HelpersRemainPresent) {
    blockers.push("phase359-moved-helpers-unavailable-on-window");
  }
  if (!phase361HelpersRemainPresent) {
    blockers.push("phase361-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableOnWindow) {
    blockers.push("phase365-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableInManualNamespace) {
    blockers.push("phase365-moved-helpers-unavailable-in-manual-namespace");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 365,
      helperName: "getCustom25DVisualBundledPassiveHelperMoveCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers,
      indexHtmlUntouched: true,
      filesCreated: false,
      phase358LoadMarkerAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      phase359MovedHelpersRemainPresent: phase359HelpersRemainPresent,
      phase361MovedHelpersRemainPresent: phase361HelpersRemainPresent,
      movedHelpersAvailableOnWindow,
      movedHelpersAvailableInManualNamespace,
      helperPresenceRegistryPreserved,
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayChanged: false,
      mapChanged: false,
      drawingChanged: false,
      domChanged: false,
      leafletChanged: false,
      sharedStateChanged: false,
      backendStorageNetworkChanged: false,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 365,
    helperName: "getCustom25DVisualBundledPassiveHelperMoveCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers,
    indexHtmlUntouched: true,
    filesCreated: false,
    phase358LoadMarkerAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    phase359MovedHelpersRemainPresent: phase359HelpersRemainPresent,
    phase361MovedHelpersRemainPresent: phase361HelpersRemainPresent,
    movedHelpersAvailableOnWindow,
    movedHelpersAvailableInManualNamespace,
    helperPresenceRegistryPreserved,
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayChanged: false,
    mapChanged: false,
    drawingChanged: false,
    domChanged: false,
    leafletChanged: false,
    sharedStateChanged: false,
    backendStorageNetworkChanged: false,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualBundledPassiveHelperMoveCloseoutReport === "function"
      ? getCustom25DVisualBundledPassiveHelperMoveCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    closeout && Array.isArray(closeout.requiredOptionKeys)
      ? closeout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeout && closeout.blockedBehavior
      ? closeout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeout && closeout.preservedSystems
      ? closeout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeout && closeout.safetyFlags
      ? closeout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!closeout) {
    blockers.push("phase365-closeout-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 365,
      helperName: "getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers: closeout ? closeout.movedHelpers : [],
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayBehaviorChanged: false,
      mapBehaviorChanged: false,
      drawingBehaviorChanged: false,
      domBehaviorChanged: false,
      leafletBehaviorChanged: false,
      sharedStateBehaviorChanged: false,
      backendStorageNetworkBehaviorChanged: false,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 365,
    helperName: "getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers: closeout ? closeout.movedHelpers : [],
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayBehaviorChanged: false,
    mapBehaviorChanged: false,
    drawingBehaviorChanged: false,
    domBehaviorChanged: false,
    leafletBehaviorChanged: false,
    sharedStateBehaviorChanged: false,
    backendStorageNetworkBehaviorChanged: false,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const previousMovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
    "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport",
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
    "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
    "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport",
    "getCustom25DVisualExtractedPassiveReportsFileHealthReport",
    "getCustom25DVisualExtractedPassiveReportsFileReadinessReport",
    "getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport"
  ];
  const movedHelpers = [
    "getCustom25DVisualBundledPassiveMoveHealthReport",
    "getCustom25DVisualBundledPassiveMoveReadinessReport",
    "getCustom25DVisualBundledPassiveMoveHealthCloseoutReport"
  ];
  const previouslyMovedHelpersRemainPresent = Array.isArray(previousMovedHelpers)
    ? previousMovedHelpers.every(
        (name) =>
          typeof globalThis !== "undefined" &&
          typeof globalThis[name] === "function"
      )
    : false;
  const movedHelpersAvailableOnWindow = movedHelpers.every(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const movedHelpersAvailableInManualNamespace =
    !!manualNamespace &&
    movedHelpers.every((name) => typeof manualNamespace[name] === "function");
  const helperPresenceRegistryPreserved =
    !!manualNamespace &&
    [...previousMovedHelpers, ...movedHelpers].every(
      (name) => typeof manualNamespace[name] === "function"
    );
  const blockers = [];
  if (!passiveReportsNamespace || passiveReportsNamespace.phase358LoadPathAvailable !== true) {
    blockers.push("phase358-load-marker-unavailable");
  }
  if (!previouslyMovedHelpersRemainPresent) {
    blockers.push("previously-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableOnWindow) {
    blockers.push("phase371-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableInManualNamespace) {
    blockers.push("phase371-moved-helpers-unavailable-in-manual-namespace");
  }
  if (!helperPresenceRegistryPreserved) {
    blockers.push("helper-presence-registry-unavailable-for-moved-helpers");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 371,
      helperName: "getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers,
      indexHtmlUntouched: true,
      filesCreated: false,
      phase358LoadMarkerAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      referenceFixPhase: 385,
      previouslyMovedHelpersRemainPresentDefined: true,
      previouslyMovedHelpersRemainPresent,
      movedHelpersAvailableOnWindow,
      movedHelpersAvailableInManualNamespace,
      helperPresenceRegistryPreserved,
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayChanged: false,
      mapChanged: false,
      drawingChanged: false,
      domChanged: false,
      leafletChanged: false,
      sharedStateChanged: false,
      backendStorageNetworkChanged: false,
      nextRecommendedStep: "reassessment-health-phase",
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 371,
    helperName: "getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers,
    indexHtmlUntouched: true,
    filesCreated: false,
    phase358LoadMarkerAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    referenceFixPhase: 385,
    previouslyMovedHelpersRemainPresentDefined: true,
    previouslyMovedHelpersRemainPresent,
    movedHelpersAvailableOnWindow,
    movedHelpersAvailableInManualNamespace,
    helperPresenceRegistryPreserved,
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayChanged: false,
    mapChanged: false,
    drawingChanged: false,
    domChanged: false,
    leafletChanged: false,
    sharedStateChanged: false,
    backendStorageNetworkChanged: false,
    nextRecommendedStep: "reassessment-health-phase",
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport === "function"
      ? getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    closeout && Array.isArray(closeout.requiredOptionKeys)
      ? closeout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeout && closeout.blockedBehavior
      ? closeout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeout && closeout.preservedSystems
      ? closeout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeout && closeout.safetyFlags
      ? closeout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!closeout) {
    blockers.push("phase371-closeout-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 371,
      helperName: "getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers: closeout ? closeout.movedHelpers : [],
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayBehaviorChanged: false,
      mapBehaviorChanged: false,
      drawingBehaviorChanged: false,
      domBehaviorChanged: false,
      leafletBehaviorChanged: false,
      sharedStateBehaviorChanged: false,
      backendStorageNetworkBehaviorChanged: false,
      recommendReassessmentNext: true,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 371,
    helperName: "getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers: closeout ? closeout.movedHelpers : [],
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayBehaviorChanged: false,
    mapBehaviorChanged: false,
    drawingBehaviorChanged: false,
    domBehaviorChanged: false,
    leafletBehaviorChanged: false,
    sharedStateBehaviorChanged: false,
    backendStorageNetworkBehaviorChanged: false,
    recommendReassessmentNext: true,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionReassessmentReport(options = {}) {
  const health =
    typeof getCustom25DVisualPostReconciliationMoveHealthReport === "function"
      ? getCustom25DVisualPostReconciliationMoveHealthReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualPostReconciliationMoveReadinessReport === "function"
      ? getCustom25DVisualPostReconciliationMoveReadinessReport(options)
      : null;
  const finalMoveCloseout =
    typeof getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport ===
    "function"
      ? getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport(options)
      : null;
  const finalMoveSelfReview =
    typeof getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport ===
    "function"
      ? getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport(options)
      : null;
  const acceptance =
    typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReport(options)
      : null;
  const requiredOptionKeys =
    (health && Array.isArray(health.requiredOptionKeys) && health.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) ||
    (finalMoveCloseout &&
      Array.isArray(finalMoveCloseout.requiredOptionKeys) &&
      finalMoveCloseout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (health && health.blockedBehavior) ||
    (finalMoveCloseout && finalMoveCloseout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (health && health.preservedSystems) ||
    (finalMoveCloseout && finalMoveCloseout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (health && health.safetyFlags) ||
    (finalMoveCloseout && finalMoveCloseout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const acceptedDedupedExtractedHelpers =
    acceptance && Array.isArray(acceptance.acceptedDedupedExtractedHelpers)
      ? acceptance.acceptedDedupedExtractedHelpers
      : [
          "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
          "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport",
          "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
          "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport",
          "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
          "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
          "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport"
        ];
  const phase369MovedHelpers = [
    "getCustom25DVisualExtractedPassiveReportsFileHealthReport",
    "getCustom25DVisualExtractedPassiveReportsFileReadinessReport",
    "getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport"
  ];
  const phase371MovedHelpers = [
    "getCustom25DVisualBundledPassiveMoveHealthReport",
    "getCustom25DVisualBundledPassiveMoveReadinessReport",
    "getCustom25DVisualBundledPassiveMoveHealthCloseoutReport"
  ];
  const expectedMovedHelpers = Array.from(
    new Set([
      ...acceptedDedupedExtractedHelpers,
      ...phase369MovedHelpers,
      ...phase371MovedHelpers
    ])
  );
  const availableMovedHelpers = expectedMovedHelpers.filter(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const missingMovedHelpers = expectedMovedHelpers.filter(
    (name) => !(typeof globalThis !== "undefined" && typeof globalThis[name] === "function")
  );
  const passiveReportsNamespaceAvailable = !!passiveReportsNamespace;
  const phase358LoadPathAvailable = !!(
    passiveReportsNamespace &&
    passiveReportsNamespace.phase358LoadPathAvailable === true
  );
  const extractedPassiveFileStillAppropriate =
    passiveReportsNamespaceAvailable &&
    phase358LoadPathAvailable &&
    Array.isArray(passiveReportsNamespace.movedHelperNames);
  const scriptJsDensityPressureImproved = expectedMovedHelpers.length >= 13;
  const passiveExtractionMiniSequenceComplete =
    !!finalMoveCloseout &&
    !!finalMoveSelfReview &&
    missingMovedHelpers.length === 0;
  const blockers = [];
  if (!health) blockers.push("phase370-health-report-unavailable");
  if (!readiness) blockers.push("phase370-readiness-report-unavailable");
  if (!finalMoveCloseout) blockers.push("phase371-closeout-report-unavailable");
  if (!finalMoveSelfReview) blockers.push("phase371-self-review-report-unavailable");
  if (!acceptance) blockers.push("phase368-acceptance-report-unavailable");
  if (!passiveReportsNamespaceAvailable) blockers.push("passive-reports-namespace-unavailable");
  if (!phase358LoadPathAvailable) blockers.push("phase358-load-path-marker-unavailable");
  if (missingMovedHelpers.length > 0) blockers.push("expected-moved-helpers-missing");
  const recommendedExtractionDecision =
    missingMovedHelpers.length > 0
      ? "investigateBlockerBeforeMoreMoves"
      : scriptJsDensityPressureImproved && extractedPassiveFileStillAppropriate
        ? "pausePassiveExtraction"
        : "oneMoreTinyPassiveCleanup";
  const nextRecommendedStep =
    recommendedExtractionDecision === "pausePassiveExtraction"
      ? "pause-passive-extraction-and-return-to-health-or-planning-work"
      : recommendedExtractionDecision === "oneMoreTinyPassiveCleanup"
        ? "prepare-one-more-tiny-passive-cleanup-only-after-review"
        : "investigate-missing-helper-or-namespace-blocker-before-any-more-moves";

  if (missingKey) {
    return {
      ok: true,
      phase: 372,
      helperName: "getCustom25DVisualPassiveExtractionReassessmentReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveReportsNamespaceAvailable,
      phase358LoadPathAvailable,
      expectedMovedHelpers,
      availableMovedHelpers,
      missingMovedHelpers,
      extractedPassiveFileStillAppropriate,
      scriptJsDensityPressureImproved,
      passiveExtractionMiniSequenceComplete,
      recommendedExtractionDecision,
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      immediateBroadExtractionBlocked: true,
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 372,
    helperName: "getCustom25DVisualPassiveExtractionReassessmentReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveReportsNamespaceAvailable,
    phase358LoadPathAvailable,
    expectedMovedHelpers,
    availableMovedHelpers,
    missingMovedHelpers,
    extractedPassiveFileStillAppropriate,
    scriptJsDensityPressureImproved,
    passiveExtractionMiniSequenceComplete,
    recommendedExtractionDecision,
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    immediateBroadExtractionBlocked: true,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionReassessmentReadinessReport(options = {}) {
  const reassessment =
    typeof getCustom25DVisualPassiveExtractionReassessmentReport === "function"
      ? getCustom25DVisualPassiveExtractionReassessmentReport(options)
      : null;
  const requiredOptionKeys =
    reassessment && Array.isArray(reassessment.requiredOptionKeys)
      ? reassessment.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (reassessment && reassessment.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (reassessment && reassessment.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (reassessment && reassessment.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers =
    reassessment && Array.isArray(reassessment.blockers) ? [...reassessment.blockers] : [];
  if (!reassessment) blockers.push("phase372-reassessment-report-unavailable");
  const recommendedExtractionDecision =
    !reassessment || (reassessment.missingMovedHelpers || []).length > 0
      ? "investigateBlockerBeforeMoreMoves"
      : reassessment.scriptJsDensityPressureImproved === true &&
          reassessment.extractedPassiveFileStillAppropriate === true
        ? "pausePassiveExtraction"
        : "oneMoreTinyPassiveCleanup";

  if (missingKey) {
    return {
      ok: true,
      phase: 372,
      helperName: "getCustom25DVisualPassiveExtractionReassessmentReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      recommendedExtractionDecision,
      readyForBroadExtraction: false,
      runtimeExtractionBlocked: true,
      startupExtractionBlocked: true,
      mapExtractionBlocked: true,
      gameplayExtractionBlocked: true,
      sharedStateExtractionBlocked: true,
      drawingExtractionBlocked: true,
      domExtractionBlocked: true,
      backendStorageNetworkExtractionBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 372,
    helperName: "getCustom25DVisualPassiveExtractionReassessmentReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    recommendedExtractionDecision,
    readyForBroadExtraction: false,
    runtimeExtractionBlocked: true,
    startupExtractionBlocked: true,
    mapExtractionBlocked: true,
    gameplayExtractionBlocked: true,
    sharedStateExtractionBlocked: true,
    drawingExtractionBlocked: true,
    domExtractionBlocked: true,
    backendStorageNetworkExtractionBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionReassessmentCloseoutReport(options = {}) {
  const reassessment =
    typeof getCustom25DVisualPassiveExtractionReassessmentReport === "function"
      ? getCustom25DVisualPassiveExtractionReassessmentReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualPassiveExtractionReassessmentReadinessReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionReassessmentReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) ||
    (reassessment &&
      Array.isArray(reassessment.requiredOptionKeys) &&
      reassessment.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) ||
    (reassessment && reassessment.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) ||
    (reassessment && reassessment.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) ||
    (reassessment && reassessment.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!reassessment) blockers.push("phase372-reassessment-report-unavailable");
  if (!readiness) blockers.push("phase372-readiness-report-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 372,
      helperName: "getCustom25DVisualPassiveExtractionReassessmentCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      reassessmentOnly: true,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      rollbackNote:
        "remove only the three Phase 372 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 372,
    helperName: "getCustom25DVisualPassiveExtractionReassessmentCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    reassessmentOnly: true,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    rollbackNote:
      "remove only the three Phase 372 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionSequenceCloseoutReport(options = {}) {
  const reassessment =
    typeof getCustom25DVisualPassiveExtractionReassessmentReport === "function"
      ? getCustom25DVisualPassiveExtractionReassessmentReport(options)
      : null;
  const reassessmentReadiness =
    typeof getCustom25DVisualPassiveExtractionReassessmentReadinessReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionReassessmentReadinessReport(options)
      : null;
  const reassessmentCloseout =
    typeof getCustom25DVisualPassiveExtractionReassessmentCloseoutReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionReassessmentCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (reassessmentCloseout &&
      Array.isArray(reassessmentCloseout.requiredOptionKeys) &&
      reassessmentCloseout.requiredOptionKeys) ||
    (reassessment &&
      Array.isArray(reassessment.requiredOptionKeys) &&
      reassessment.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (reassessmentCloseout && reassessmentCloseout.blockedBehavior) ||
    (reassessment && reassessment.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (reassessmentCloseout && reassessmentCloseout.preservedSystems) ||
    (reassessment && reassessment.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (reassessmentCloseout && reassessmentCloseout.safetyFlags) ||
    (reassessment && reassessment.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!reassessment) blockers.push("phase372-reassessment-report-unavailable");
  if (!reassessmentReadiness) blockers.push("phase372-reassessment-readiness-report-unavailable");
  if (!reassessmentCloseout) blockers.push("phase372-reassessment-closeout-report-unavailable");
  const passiveExtractionMiniSequenceComplete = !!(
    reassessment &&
    reassessmentCloseout &&
    reassessmentReadiness &&
    reassessment.passiveExtractionMiniSequenceComplete === true
  );
  const passiveExtractionPaused = !!(
    reassessment &&
    reassessment.recommendedExtractionDecision === "pausePassiveExtraction"
  );
  const futureExtractionRequiresSpecificJustification = true;
  const nextRecommendedStep = passiveExtractionPaused
    ? "return-focus-to-custom25dvisual-renderer-planning-or-implementation"
    : "review-reassessment-blockers-before-any-further-passive-extraction";

  if (missingKey) {
    return {
      ok: true,
      phase: 373,
      helperName: "getCustom25DVisualPassiveExtractionSequenceCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      reassessmentAvailable: !!reassessment,
      reassessmentReadinessAvailable: !!reassessmentReadiness,
      reassessmentCloseoutAvailable: !!reassessmentCloseout,
      passiveExtractionMiniSequenceComplete,
      passiveExtractionPaused,
      futureExtractionRequiresSpecificJustification,
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      immediateBroadExtractionBlocked: true,
      nextRecommendedStep,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 373,
    helperName: "getCustom25DVisualPassiveExtractionSequenceCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    reassessmentAvailable: !!reassessment,
    reassessmentReadinessAvailable: !!reassessmentReadiness,
    reassessmentCloseoutAvailable: !!reassessmentCloseout,
    passiveExtractionMiniSequenceComplete,
    passiveExtractionPaused,
    futureExtractionRequiresSpecificJustification,
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    immediateBroadExtractionBlocked: true,
    nextRecommendedStep,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionSequenceSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualPassiveExtractionSequenceCloseoutReport === "function"
      ? getCustom25DVisualPassiveExtractionSequenceCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (closeout && Array.isArray(closeout.requiredOptionKeys) && closeout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (closeout && closeout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (closeout && closeout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (closeout && closeout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!closeout) blockers.push("phase373-sequence-closeout-report-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 373,
      helperName: "getCustom25DVisualPassiveExtractionSequenceSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      importsExportsAdded: false,
      buildToolingChanged: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      gameplayBehaviorChanged: false,
      mapBehaviorChanged: false,
      drawingBehaviorChanged: false,
      domBehaviorChanged: false,
      leafletBehaviorChanged: false,
      sharedStateBehaviorChanged: false,
      backendStorageNetworkBehaviorChanged: false,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      rollbackNote:
        "remove only the three Phase 373 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 373,
    helperName: "getCustom25DVisualPassiveExtractionSequenceSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    importsExportsAdded: false,
    buildToolingChanged: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    gameplayBehaviorChanged: false,
    mapBehaviorChanged: false,
    drawingBehaviorChanged: false,
    domBehaviorChanged: false,
    leafletBehaviorChanged: false,
    sharedStateBehaviorChanged: false,
    backendStorageNetworkBehaviorChanged: false,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    rollbackNote:
      "remove only the three Phase 373 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport(
  options = {}
) {
  const closeout =
    typeof getCustom25DVisualPassiveExtractionSequenceCloseoutReport === "function"
      ? getCustom25DVisualPassiveExtractionSequenceCloseoutReport(options)
      : null;
  const selfReview =
    typeof getCustom25DVisualPassiveExtractionSequenceSelfReviewReport === "function"
      ? getCustom25DVisualPassiveExtractionSequenceSelfReviewReport(options)
      : null;
  const requiredOptionKeys =
    (closeout && Array.isArray(closeout.requiredOptionKeys) && closeout.requiredOptionKeys) ||
    (selfReview && Array.isArray(selfReview.requiredOptionKeys) && selfReview.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (closeout && closeout.blockedBehavior) ||
    (selfReview && selfReview.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (closeout && closeout.preservedSystems) ||
    (selfReview && selfReview.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (closeout && closeout.safetyFlags) ||
    (selfReview && selfReview.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!closeout) blockers.push("phase373-sequence-closeout-report-unavailable");
  if (!selfReview) blockers.push("phase373-sequence-self-review-report-unavailable");
  const passiveExtractionPaused = !!(
    closeout && closeout.passiveExtractionPaused === true
  );
  const recommendedNextWorkArea =
    "custom25dvisual-renderer-planning-or-implementation";

  if (missingKey) {
    return {
      ok: true,
      phase: 373,
      helperName: "getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      recommendationOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveExtractionPaused,
      recommendedNextWorkArea,
      broadExtractionBlocked: true,
      runtimeExtractionBlocked: true,
      startupExtractionBlocked: true,
      mapExtractionBlocked: true,
      gameplayExtractionBlocked: true,
      sharedStateExtractionBlocked: true,
      drawingExtractionBlocked: true,
      domExtractionBlocked: true,
      backendStorageNetworkExtractionBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 373,
    helperName: "getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    recommendationOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveExtractionPaused,
    recommendedNextWorkArea,
    broadExtractionBlocked: true,
    runtimeExtractionBlocked: true,
    startupExtractionBlocked: true,
    mapExtractionBlocked: true,
    gameplayExtractionBlocked: true,
    sharedStateExtractionBlocked: true,
    drawingExtractionBlocked: true,
    domExtractionBlocked: true,
    backendStorageNetworkExtractionBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererPathResumeReadinessReport(options = {}) {
  const pauseRecommendation =
    typeof getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport ===
    "function"
      ? getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport(options)
      : null;
  const rendererStateContainerPlan =
    typeof getCustom25DVisualManualRendererStateContainerPlanReport === "function"
      ? getCustom25DVisualManualRendererStateContainerPlanReport(options)
      : null;
  const rendererStateContainerReadiness =
    typeof getCustom25DVisualManualRendererStateContainerContractReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererStateContainerContractReadinessReport(options)
      : null;
  const rendererManualInitPlan =
    typeof getCustom25DVisualManualRendererInitializationShellPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererInitializationShellPlanReport(options)
      : null;
  const rendererManualInitContract =
    typeof getCustom25DVisualManualRendererInitializationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererInitializationShellContractReport(options)
      : null;
  const rendererLifecyclePlan =
    typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
      ? getCustom25DVisualManualRendererLifecyclePlanReport(options)
      : null;
  const rendererLifecycleContract =
    typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
      ? getCustom25DVisualManualRendererLifecycleContractReport(options)
      : null;
  const sharedStatePlan =
    typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport(options)
      : null;
  const sharedStatePreflight =
    typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerPreflightReport(options)
      : null;
  const sharedStateCloseout =
    typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport(options)
      : null;
  const integrationShellPlan =
    typeof getCustom25DVisualManualRendererIntegrationShellPlanReport === "function"
      ? getCustom25DVisualManualRendererIntegrationShellPlanReport(options)
      : null;
  const integrationShellContract =
    typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
    "function"
      ? getCustom25DVisualManualRendererIntegrationShellContractReport(options)
      : null;
  const lifecycleShellCloseout =
    typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (pauseRecommendation &&
      Array.isArray(pauseRecommendation.requiredOptionKeys) &&
      pauseRecommendation.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveExtractionPaused = !!(
    pauseRecommendation && pauseRecommendation.passiveExtractionPaused === true
  );
  const rendererPlanningReportsAvailable = !!(
    rendererLifecyclePlan &&
    rendererLifecycleContract &&
    rendererStateContainerPlan &&
    rendererStateContainerReadiness
  );
  const rendererShellReportsAvailable = !!(
    rendererManualInitPlan &&
    rendererManualInitContract &&
    integrationShellPlan &&
    integrationShellContract &&
    lifecycleShellCloseout
  );
  const sharedStateReportsAvailable = !!(
    sharedStatePlan &&
    sharedStatePreflight &&
    sharedStateCloseout
  );
  const safestNextRendererMilestone = rendererStateContainerReadiness
    ? "rendererStateContainerReadiness"
    : rendererManualInitPlan
      ? "rendererManualInitReadiness"
      : rendererLifecyclePlan
        ? "rendererLayerContractReadiness"
        : "pauseForManualReview";
  const blockers = [];
  if (!pauseRecommendation) blockers.push("phase373-pause-recommendation-unavailable");
  if (passiveExtractionPaused !== true) blockers.push("passive-extraction-not-confirmed-paused");
  if (!rendererPlanningReportsAvailable) blockers.push("renderer-planning-reports-incomplete");
  if (!rendererShellReportsAvailable) blockers.push("renderer-shell-reports-incomplete");
  if (!sharedStateReportsAvailable) blockers.push("shared-state-reports-incomplete");
  const nextRecommendedStep =
    safestNextRendererMilestone === "rendererStateContainerReadiness"
      ? "prepare-passive-or-inert-state-container-readiness-phase"
      : safestNextRendererMilestone === "rendererManualInitReadiness"
        ? "prepare-passive-manual-init-readiness-phase"
        : safestNextRendererMilestone === "rendererLayerContractReadiness"
          ? "prepare-passive-renderer-layer-contract-readiness-phase"
          : "pause-for-manual-review-before-resuming-renderer-path";

  if (missingKey) {
    return {
      ok: true,
      phase: 374,
      helperName: "getCustom25DVisualRendererPathResumeReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveExtractionPaused,
      rendererPathResumed: passiveExtractionPaused === true,
      rendererPlanningReportsAvailable,
      rendererShellReportsAvailable,
      sharedStateReportsAvailable,
      safestNextRendererMilestone,
      blockedRuntimeAreas: [
        "renderer-init-start-run",
        "map-attachment",
        "drawing",
        "dom-mutation",
        "shared-state-mutation",
        "backend-storage-network",
        "gameplay-pins-player-marker-osm-leaflet"
      ],
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      nextRecommendedStep,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 374,
    helperName: "getCustom25DVisualRendererPathResumeReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveExtractionPaused,
    rendererPathResumed: passiveExtractionPaused === true,
    rendererPlanningReportsAvailable,
    rendererShellReportsAvailable,
    sharedStateReportsAvailable,
    safestNextRendererMilestone,
    blockedRuntimeAreas: [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm-leaflet"
    ],
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    nextRecommendedStep,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererPathResumeNextStepReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualRendererPathResumeReadinessReport === "function"
      ? getCustom25DVisualRendererPathResumeReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = readiness && Array.isArray(readiness.blockers) ? [...readiness.blockers] : [];
  if (!readiness) blockers.push("phase374-readiness-report-unavailable");
  const nextPhaseType =
    readiness && readiness.safestNextRendererMilestone
      ? readiness.safestNextRendererMilestone
      : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 374,
      helperName: "getCustom25DVisualRendererPathResumeNextStepReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      nextStepOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      nextPhaseType,
      rendererStateContainerReadiness: nextPhaseType === "rendererStateContainerReadiness",
      rendererManualInitReadiness: nextPhaseType === "rendererManualInitReadiness",
      rendererLayerContractReadiness: nextPhaseType === "rendererLayerContractReadiness",
      rendererDrawPathPlanning: nextPhaseType === "rendererDrawPathPlanning",
      pauseForManualReview: nextPhaseType === "pauseForManualReview",
      broadExtractionBlocked: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags,
      blockers
    };
  }

  return {
    ok: true,
    phase: 374,
    helperName: "getCustom25DVisualRendererPathResumeNextStepReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    nextStepOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    nextPhaseType,
    rendererStateContainerReadiness: nextPhaseType === "rendererStateContainerReadiness",
    rendererManualInitReadiness: nextPhaseType === "rendererManualInitReadiness",
    rendererLayerContractReadiness: nextPhaseType === "rendererLayerContractReadiness",
    rendererDrawPathPlanning: nextPhaseType === "rendererDrawPathPlanning",
    pauseForManualReview: nextPhaseType === "pauseForManualReview",
    broadExtractionBlocked: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags,
    blockers
  };
}

function getCustom25DVisualRendererPathResumeCloseoutReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualRendererPathResumeReadinessReport === "function"
      ? getCustom25DVisualRendererPathResumeReadinessReport(options)
      : null;
  const nextStep =
    typeof getCustom25DVisualRendererPathResumeNextStepReport === "function"
      ? getCustom25DVisualRendererPathResumeNextStepReport(options)
      : null;
  const requiredOptionKeys =
    (nextStep && Array.isArray(nextStep.requiredOptionKeys) && nextStep.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (nextStep && nextStep.blockedBehavior) ||
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (nextStep && nextStep.preservedSystems) ||
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (nextStep && nextStep.safetyFlags) ||
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!readiness) blockers.push("phase374-readiness-report-unavailable");
  if (!nextStep) blockers.push("phase374-next-step-report-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 374,
      helperName: "getCustom25DVisualRendererPathResumeCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      rendererPathPlanningOnly: true,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      sharedStateMutated: false,
      runtimeBehaviorChanged: false,
      rollbackNote:
        "remove only the three Phase 374 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 374,
    helperName: "getCustom25DVisualRendererPathResumeCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    rendererPathPlanningOnly: true,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    sharedStateMutated: false,
    runtimeBehaviorChanged: false,
    rollbackNote:
      "remove only the three Phase 374 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererStateContainerReadinessReport(options = {}) {
  const resumeReadiness =
    typeof getCustom25DVisualRendererPathResumeReadinessReport === "function"
      ? getCustom25DVisualRendererPathResumeReadinessReport(options)
      : null;
  const resumeNextStep =
    typeof getCustom25DVisualRendererPathResumeNextStepReport === "function"
      ? getCustom25DVisualRendererPathResumeNextStepReport(options)
      : null;
  const stateContainerPlan =
    typeof getCustom25DVisualManualRendererStateContainerPlanReport === "function"
      ? getCustom25DVisualManualRendererStateContainerPlanReport(options)
      : null;
  const stateContainerContractReadiness =
    typeof getCustom25DVisualManualRendererStateContainerContractReadinessReport ===
    "function"
      ? getCustom25DVisualManualRendererStateContainerContractReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (resumeReadiness &&
      Array.isArray(resumeReadiness.requiredOptionKeys) &&
      resumeReadiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (resumeReadiness && resumeReadiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (resumeReadiness && resumeReadiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (resumeReadiness && resumeReadiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const rendererPathResumeAvailable = !!resumeReadiness;
  const rendererPathNextStepAvailable = !!resumeNextStep;
  const stateContainerPlanAvailable = !!stateContainerPlan;
  const stateContainerContractReadinessAvailable = !!stateContainerContractReadiness;
  const blockers = [];
  if (!rendererPathResumeAvailable) blockers.push("phase374-resume-readiness-unavailable");
  if (!rendererPathNextStepAvailable) blockers.push("phase374-resume-next-step-unavailable");
  if (!stateContainerPlanAvailable) blockers.push("state-container-plan-unavailable");
  if (!stateContainerContractReadinessAvailable) {
    blockers.push("state-container-contract-readiness-unavailable");
  }
  const safestNextMilestone =
    stateContainerContractReadinessAvailable === true
      ? "stateContainerPlanCloseout"
      : stateContainerPlanAvailable === true
        ? "inertStateContainerContractPlan"
        : rendererPathNextStepAvailable === true
          ? "manualRendererStateSchemaReadiness"
          : "pauseForManualReview";
  const nextRecommendedStep =
    safestNextMilestone === "stateContainerPlanCloseout"
      ? "prepare-passive-state-container-plan-closeout-phase"
      : safestNextMilestone === "inertStateContainerContractPlan"
        ? "prepare-inert-state-container-contract-plan-phase"
        : safestNextMilestone === "manualRendererStateSchemaReadiness"
          ? "prepare-manual-renderer-state-schema-readiness-phase"
          : "pause-for-manual-review-before-state-container-work";

  if (missingKey) {
    return {
      ok: true,
      phase: 375,
      helperName: "getCustom25DVisualRendererStateContainerReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      rendererPathResumeAvailable,
      rendererPathNextStepAvailable,
      stateContainerPlanAvailable,
      stateContainerContractReadinessAvailable,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      safestNextMilestone,
      blockedRuntimeAreas: [
        "renderer-init-start-run",
        "map-attachment",
        "drawing",
        "dom-mutation",
        "leaflet-mutation",
        "shared-state-mutation",
        "backend-storage-network",
        "gameplay-pins-player-marker-osm"
      ],
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 375,
    helperName: "getCustom25DVisualRendererStateContainerReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    rendererPathResumeAvailable,
    rendererPathNextStepAvailable,
    stateContainerPlanAvailable,
    stateContainerContractReadinessAvailable,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    safestNextMilestone,
    blockedRuntimeAreas: [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "leaflet-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm"
    ],
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererStateContainerNextStepReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualRendererStateContainerReadinessReport === "function"
      ? getCustom25DVisualRendererStateContainerReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = readiness && Array.isArray(readiness.blockers) ? [...readiness.blockers] : [];
  if (!readiness) blockers.push("phase375-readiness-report-unavailable");
  const recommendedNextRendererMilestone =
    readiness && typeof readiness.safestNextMilestone === "string"
      ? readiness.safestNextMilestone
      : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 375,
      helperName: "getCustom25DVisualRendererStateContainerNextStepReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      nextStepOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      recommendedNextRendererMilestone,
      stateContainerPlanCloseout: recommendedNextRendererMilestone === "stateContainerPlanCloseout",
      inertStateContainerContractPlan:
        recommendedNextRendererMilestone === "inertStateContainerContractPlan",
      manualRendererStateSchemaReadiness:
        recommendedNextRendererMilestone === "manualRendererStateSchemaReadiness",
      pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
      broadExtractionBlocked: true,
      immediateRuntimeStateContainerWorkBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 375,
    helperName: "getCustom25DVisualRendererStateContainerNextStepReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    nextStepOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    recommendedNextRendererMilestone,
    stateContainerPlanCloseout: recommendedNextRendererMilestone === "stateContainerPlanCloseout",
    inertStateContainerContractPlan:
      recommendedNextRendererMilestone === "inertStateContainerContractPlan",
    manualRendererStateSchemaReadiness:
      recommendedNextRendererMilestone === "manualRendererStateSchemaReadiness",
    pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
    broadExtractionBlocked: true,
    immediateRuntimeStateContainerWorkBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererStateContainerReadinessCloseoutReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualRendererStateContainerReadinessReport === "function"
      ? getCustom25DVisualRendererStateContainerReadinessReport(options)
      : null;
  const nextStep =
    typeof getCustom25DVisualRendererStateContainerNextStepReport === "function"
      ? getCustom25DVisualRendererStateContainerNextStepReport(options)
      : null;
  const requiredOptionKeys =
    (nextStep && Array.isArray(nextStep.requiredOptionKeys) && nextStep.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (nextStep && nextStep.blockedBehavior) ||
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (nextStep && nextStep.preservedSystems) ||
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (nextStep && nextStep.safetyFlags) ||
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!readiness) blockers.push("phase375-readiness-report-unavailable");
  if (!nextStep) blockers.push("phase375-next-step-report-unavailable");
  const recommendedNextRendererMilestone =
    nextStep && typeof nextStep.recommendedNextRendererMilestone === "string"
      ? nextStep.recommendedNextRendererMilestone
      : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 375,
      helperName: "getCustom25DVisualRendererStateContainerReadinessCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      rendererStateContainerReadinessPlanningOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      recommendedNextRendererMilestone,
      rollbackNote:
        "remove only the three Phase 375 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 375,
    helperName: "getCustom25DVisualRendererStateContainerReadinessCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    rendererStateContainerReadinessPlanningOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    recommendedNextRendererMilestone,
    rollbackNote:
      "remove only the three Phase 375 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererStateContainerPlanCloseoutReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualRendererStateContainerReadinessReport === "function"
      ? getCustom25DVisualRendererStateContainerReadinessReport(options)
      : null;
  const nextStep =
    typeof getCustom25DVisualRendererStateContainerNextStepReport === "function"
      ? getCustom25DVisualRendererStateContainerNextStepReport(options)
      : null;
  const readinessCloseout =
    typeof getCustom25DVisualRendererStateContainerReadinessCloseoutReport ===
    "function"
      ? getCustom25DVisualRendererStateContainerReadinessCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (readinessCloseout &&
      Array.isArray(readinessCloseout.requiredOptionKeys) &&
      readinessCloseout.requiredOptionKeys) ||
    (nextStep && Array.isArray(nextStep.requiredOptionKeys) && nextStep.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readinessCloseout && readinessCloseout.blockedBehavior) ||
    (nextStep && nextStep.blockedBehavior) ||
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readinessCloseout && readinessCloseout.preservedSystems) ||
    (nextStep && nextStep.preservedSystems) ||
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readinessCloseout && readinessCloseout.safetyFlags) ||
    (nextStep && nextStep.safetyFlags) ||
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const phase375ReadinessAvailable = !!readiness;
  const phase375NextStepAvailable = !!nextStep;
  const phase375CloseoutAvailable = !!readinessCloseout;
  const blockedRuntimeAreas =
    (readiness && Array.isArray(readiness.blockedRuntimeAreas) && readiness.blockedRuntimeAreas) || [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "leaflet-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm"
    ];
  const blockers = [];
  if (!phase375ReadinessAvailable) blockers.push("phase375-readiness-unavailable");
  if (!phase375NextStepAvailable) blockers.push("phase375-next-step-unavailable");
  if (!phase375CloseoutAvailable) blockers.push("phase375-closeout-unavailable");
  const safestNextMilestone =
    phase375ReadinessAvailable &&
    readiness &&
    readiness.safestNextMilestone === "stateContainerPlanCloseout"
      ? "inertStateContainerContractPlan"
      : phase375ReadinessAvailable &&
          readiness &&
          readiness.stateContainerPlanAvailable === true
        ? "manualRendererStateSchemaReadiness"
        : "pauseForManualReview";
  const nextRecommendedStep =
    safestNextMilestone === "inertStateContainerContractPlan"
      ? "prepare-passive-inert-state-container-contract-plan-phase"
      : safestNextMilestone === "manualRendererStateSchemaReadiness"
        ? "prepare-passive-manual-renderer-state-schema-readiness-phase"
        : "pause-for-manual-review-before-state-container-contract-planning";

  if (missingKey) {
    return {
      ok: true,
      phase: 376,
      helperName: "getCustom25DVisualRendererStateContainerPlanCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase375ReadinessAvailable,
      phase375NextStepAvailable,
      phase375CloseoutAvailable,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      safestNextMilestone,
      blockedRuntimeAreas,
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 376,
    helperName: "getCustom25DVisualRendererStateContainerPlanCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase375ReadinessAvailable,
    phase375NextStepAvailable,
    phase375CloseoutAvailable,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    safestNextMilestone,
    blockedRuntimeAreas,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererStateContainerPlanSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualRendererStateContainerPlanCloseoutReport === "function"
      ? getCustom25DVisualRendererStateContainerPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (closeout && Array.isArray(closeout.requiredOptionKeys) && closeout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (closeout && closeout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (closeout && closeout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (closeout && closeout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!closeout) blockers.push("phase376-closeout-report-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 376,
      helperName: "getCustom25DVisualRendererStateContainerPlanSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phaseAddsPlanningOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      rollbackNote:
        "remove only the three Phase 376 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 376,
    helperName: "getCustom25DVisualRendererStateContainerPlanSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phaseAddsPlanningOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    rollbackNote:
      "remove only the three Phase 376 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualRendererStateContainerPlanNextStepReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualRendererStateContainerPlanCloseoutReport === "function"
      ? getCustom25DVisualRendererStateContainerPlanCloseoutReport(options)
      : null;
  const selfReview =
    typeof getCustom25DVisualRendererStateContainerPlanSelfReviewReport === "function"
      ? getCustom25DVisualRendererStateContainerPlanSelfReviewReport(options)
      : null;
  const requiredOptionKeys =
    (selfReview && Array.isArray(selfReview.requiredOptionKeys) && selfReview.requiredOptionKeys) ||
    (closeout && Array.isArray(closeout.requiredOptionKeys) && closeout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (selfReview && selfReview.blockedBehavior) ||
    (closeout && closeout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (selfReview && selfReview.preservedSystems) ||
    (closeout && closeout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (selfReview && selfReview.safetyFlags) ||
    (closeout && closeout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!closeout) blockers.push("phase376-closeout-report-unavailable");
  if (!selfReview) blockers.push("phase376-self-review-report-unavailable");
  const recommendedNextRendererMilestone =
    blockers.length === 0
      ? "inertStateContainerContractPlan"
      : closeout && closeout.phase375ReadinessAvailable === true
        ? "manualRendererStateSchemaReadiness"
        : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 376,
      helperName: "getCustom25DVisualRendererStateContainerPlanNextStepReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      nextStepOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      recommendedNextRendererMilestone,
      inertStateContainerContractPlan:
        recommendedNextRendererMilestone === "inertStateContainerContractPlan",
      manualRendererStateSchemaReadiness:
        recommendedNextRendererMilestone === "manualRendererStateSchemaReadiness",
      pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
      actualStateContainerCreationBlocked: true,
      broadRuntimeBehaviorBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 376,
    helperName: "getCustom25DVisualRendererStateContainerPlanNextStepReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    nextStepOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    recommendedNextRendererMilestone,
    inertStateContainerContractPlan:
      recommendedNextRendererMilestone === "inertStateContainerContractPlan",
    manualRendererStateSchemaReadiness:
      recommendedNextRendererMilestone === "manualRendererStateSchemaReadiness",
    pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
    actualStateContainerCreationBlocked: true,
    broadRuntimeBehaviorBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerContractPlanReport(options = {}) {
  const planCloseout =
    typeof getCustom25DVisualRendererStateContainerPlanCloseoutReport === "function"
      ? getCustom25DVisualRendererStateContainerPlanCloseoutReport(options)
      : null;
  const planSelfReview =
    typeof getCustom25DVisualRendererStateContainerPlanSelfReviewReport === "function"
      ? getCustom25DVisualRendererStateContainerPlanSelfReviewReport(options)
      : null;
  const planNextStep =
    typeof getCustom25DVisualRendererStateContainerPlanNextStepReport === "function"
      ? getCustom25DVisualRendererStateContainerPlanNextStepReport(options)
      : null;
  const requiredOptionKeys =
    (planNextStep && Array.isArray(planNextStep.requiredOptionKeys) && planNextStep.requiredOptionKeys) ||
    (planSelfReview &&
      Array.isArray(planSelfReview.requiredOptionKeys) &&
      planSelfReview.requiredOptionKeys) ||
    (planCloseout && Array.isArray(planCloseout.requiredOptionKeys) && planCloseout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (planNextStep && planNextStep.blockedBehavior) ||
    (planSelfReview && planSelfReview.blockedBehavior) ||
    (planCloseout && planCloseout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (planNextStep && planNextStep.preservedSystems) ||
    (planSelfReview && planSelfReview.preservedSystems) ||
    (planCloseout && planCloseout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (planNextStep && planNextStep.safetyFlags) ||
    (planSelfReview && planSelfReview.safetyFlags) ||
    (planCloseout && planCloseout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const phase376CloseoutAvailable = !!planCloseout;
  const phase376SelfReviewAvailable = !!planSelfReview;
  const phase376NextStepAvailable = !!planNextStep;
  const plannedInitialState = {
    created: false,
    initialized: false,
    running: false,
    mapAttached: false,
    drawingEnabled: false,
    layerCount: 0,
    rendererInstance: null,
    mapReference: null,
    layers: [],
    lastManualAction: null,
    lastError: null
  };
  const blockedRuntimeAreas =
    (planCloseout &&
      Array.isArray(planCloseout.blockedRuntimeAreas) &&
      planCloseout.blockedRuntimeAreas) || [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "leaflet-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm"
    ];
  const blockers = [];
  if (!phase376CloseoutAvailable) blockers.push("phase376-closeout-unavailable");
  if (!phase376SelfReviewAvailable) blockers.push("phase376-self-review-unavailable");
  if (!phase376NextStepAvailable) blockers.push("phase376-next-step-unavailable");
  const safestNextMilestone =
    phase376NextStepAvailable &&
    planNextStep &&
    planNextStep.recommendedNextRendererMilestone === "inertStateContainerContractPlan"
      ? "manualRendererStateSchemaReadiness"
      : "pauseForManualReview";
  const nextRecommendedStep =
    safestNextMilestone === "manualRendererStateSchemaReadiness"
      ? "prepare-passive-manual-renderer-state-schema-readiness-phase"
      : "pause-for-manual-review-before-state-schema-readiness";

  if (missingKey) {
    return {
      ok: true,
      phase: 377,
      helperName: "getCustom25DVisualInertStateContainerContractPlanReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase376CloseoutAvailable,
      phase376SelfReviewAvailable,
      phase376NextStepAvailable,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      plannedContractOnly: true,
      plannedInitialState,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      safestNextMilestone,
      blockedRuntimeAreas,
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 377,
    helperName: "getCustom25DVisualInertStateContainerContractPlanReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase376CloseoutAvailable,
    phase376SelfReviewAvailable,
    phase376NextStepAvailable,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    plannedContractOnly: true,
    plannedInitialState,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    safestNextMilestone,
    blockedRuntimeAreas,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerContractReadinessReport(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualInertStateContainerContractPlanReport === "function"
      ? getCustom25DVisualInertStateContainerContractPlanReport(options)
      : null;
  const requiredOptionKeys =
    (contractPlan &&
      Array.isArray(contractPlan.requiredOptionKeys) &&
      contractPlan.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (contractPlan && contractPlan.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (contractPlan && contractPlan.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (contractPlan && contractPlan.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!contractPlan) blockers.push("phase377-contract-plan-unavailable");
  const recommendedNextRendererMilestone =
    blockers.length === 0 ? "manualRendererStateSchemaReadiness" : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 377,
      helperName: "getCustom25DVisualInertStateContainerContractReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractPlanAvailable: !!contractPlan,
      readyForManualRendererStateSchemaReadiness:
        recommendedNextRendererMilestone === "manualRendererStateSchemaReadiness",
      stateContainerCreationBlocked: true,
      broadRuntimeBehaviorBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 377,
    helperName: "getCustom25DVisualInertStateContainerContractReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractPlanAvailable: !!contractPlan,
    readyForManualRendererStateSchemaReadiness:
      recommendedNextRendererMilestone === "manualRendererStateSchemaReadiness",
    stateContainerCreationBlocked: true,
    broadRuntimeBehaviorBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerContractPlanCloseoutReport(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualInertStateContainerContractPlanReport === "function"
      ? getCustom25DVisualInertStateContainerContractPlanReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualInertStateContainerContractReadinessReport === "function"
      ? getCustom25DVisualInertStateContainerContractReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) ||
    (contractPlan &&
      Array.isArray(contractPlan.requiredOptionKeys) &&
      contractPlan.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) ||
    (contractPlan && contractPlan.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) ||
    (contractPlan && contractPlan.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) ||
    (contractPlan && contractPlan.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!contractPlan) blockers.push("phase377-contract-plan-unavailable");
  if (!readiness) blockers.push("phase377-readiness-report-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 377,
      helperName: "getCustom25DVisualInertStateContainerContractPlanCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      contractPlanningOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      rollbackNote:
        "remove only the three Phase 377 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 377,
    helperName: "getCustom25DVisualInertStateContainerContractPlanCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    contractPlanningOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    rollbackNote:
      "remove only the three Phase 377 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererStateSchemaReadinessReport(options = {}) {
  const contractPlan =
    typeof getCustom25DVisualInertStateContainerContractPlanReport === "function"
      ? getCustom25DVisualInertStateContainerContractPlanReport(options)
      : null;
  const contractReadiness =
    typeof getCustom25DVisualInertStateContainerContractReadinessReport === "function"
      ? getCustom25DVisualInertStateContainerContractReadinessReport(options)
      : null;
  const contractCloseout =
    typeof getCustom25DVisualInertStateContainerContractPlanCloseoutReport ===
    "function"
      ? getCustom25DVisualInertStateContainerContractPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (contractCloseout &&
      Array.isArray(contractCloseout.requiredOptionKeys) &&
      contractCloseout.requiredOptionKeys) ||
    (contractReadiness &&
      Array.isArray(contractReadiness.requiredOptionKeys) &&
      contractReadiness.requiredOptionKeys) ||
    (contractPlan &&
      Array.isArray(contractPlan.requiredOptionKeys) &&
      contractPlan.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (contractCloseout && contractCloseout.blockedBehavior) ||
    (contractReadiness && contractReadiness.blockedBehavior) ||
    (contractPlan && contractPlan.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (contractCloseout && contractCloseout.preservedSystems) ||
    (contractReadiness && contractReadiness.preservedSystems) ||
    (contractPlan && contractPlan.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (contractCloseout && contractCloseout.safetyFlags) ||
    (contractReadiness && contractReadiness.safetyFlags) ||
    (contractPlan && contractPlan.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const phase377PlanAvailable = !!contractPlan;
  const phase377ReadinessAvailable = !!contractReadiness;
  const phase377CloseoutAvailable = !!contractCloseout;
  const expectedFieldTypes = {
    created: "boolean",
    initialized: "boolean",
    running: "boolean",
    mapAttached: "boolean",
    drawingEnabled: "boolean",
    layerCount: "number",
    rendererInstance: "object|null",
    mapReference: "object|null",
    layers: "array",
    lastManualAction: "string|null",
    lastError: "string|null"
  };
  const requiredFields = [
    "created",
    "initialized",
    "running",
    "mapAttached",
    "drawingEnabled",
    "layerCount",
    "rendererInstance",
    "mapReference",
    "layers"
  ];
  const optionalFields = ["lastManualAction", "lastError"];
  const blockedRuntimeAreas =
    (contractPlan &&
      Array.isArray(contractPlan.blockedRuntimeAreas) &&
      contractPlan.blockedRuntimeAreas) || [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "leaflet-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm"
    ];
  const blockers = [];
  if (!phase377PlanAvailable) blockers.push("phase377-contract-plan-unavailable");
  if (!phase377ReadinessAvailable) blockers.push("phase377-contract-readiness-unavailable");
  if (!phase377CloseoutAvailable) blockers.push("phase377-contract-closeout-unavailable");
  const safestNextMilestone =
    blockers.length === 0 ? "stateSchemaCloseout" : "pauseForManualReview";
  const nextRecommendedStep =
    safestNextMilestone === "stateSchemaCloseout"
      ? "prepare-passive-state-schema-closeout-phase"
      : "pause-for-manual-review-before-state-schema-closeout";

  if (missingKey) {
    return {
      ok: true,
      phase: 378,
      helperName: "getCustom25DVisualManualRendererStateSchemaReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase377PlanAvailable,
      phase377ReadinessAvailable,
      phase377CloseoutAvailable,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      schemaReviewedOnly: true,
      expectedFieldTypes,
      requiredFields,
      optionalFields,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      safestNextMilestone,
      blockedRuntimeAreas,
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 378,
    helperName: "getCustom25DVisualManualRendererStateSchemaReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase377PlanAvailable,
    phase377ReadinessAvailable,
    phase377CloseoutAvailable,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    schemaReviewedOnly: true,
    expectedFieldTypes,
    requiredFields,
    optionalFields,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    safestNextMilestone,
    blockedRuntimeAreas,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererStateSchemaNextStepReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualManualRendererStateSchemaReadinessReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = readiness && Array.isArray(readiness.blockers) ? [...readiness.blockers] : [];
  if (!readiness) blockers.push("phase378-schema-readiness-unavailable");
  const recommendedNextRendererMilestone =
    blockers.length === 0
      ? "stateSchemaCloseout"
      : readiness && readiness.phase377PlanAvailable === true
        ? "inertStateContainerCreationReadiness"
        : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 378,
      helperName: "getCustom25DVisualManualRendererStateSchemaNextStepReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      nextStepOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      recommendedNextRendererMilestone,
      stateSchemaCloseout: recommendedNextRendererMilestone === "stateSchemaCloseout",
      inertStateContainerCreationReadiness:
        recommendedNextRendererMilestone === "inertStateContainerCreationReadiness",
      pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
      actualStateContainerCreationBlocked: true,
      broadRuntimeBehaviorBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 378,
    helperName: "getCustom25DVisualManualRendererStateSchemaNextStepReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    nextStepOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    recommendedNextRendererMilestone,
    stateSchemaCloseout: recommendedNextRendererMilestone === "stateSchemaCloseout",
    inertStateContainerCreationReadiness:
      recommendedNextRendererMilestone === "inertStateContainerCreationReadiness",
    pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
    actualStateContainerCreationBlocked: true,
    broadRuntimeBehaviorBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualManualRendererStateSchemaReadinessReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaReadinessReport(options)
      : null;
  const nextStep =
    typeof getCustom25DVisualManualRendererStateSchemaNextStepReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaNextStepReport(options)
      : null;
  const requiredOptionKeys =
    (nextStep && Array.isArray(nextStep.requiredOptionKeys) && nextStep.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (nextStep && nextStep.blockedBehavior) ||
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (nextStep && nextStep.preservedSystems) ||
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (nextStep && nextStep.safetyFlags) ||
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!readiness) blockers.push("phase378-schema-readiness-unavailable");
  if (!nextStep) blockers.push("phase378-schema-next-step-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 378,
      helperName: "getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      schemaReadinessOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      rollbackNote:
        "remove only the three Phase 378 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 378,
    helperName: "getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    schemaReadinessOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    rollbackNote:
      "remove only the three Phase 378 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererStateSchemaCloseoutReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualManualRendererStateSchemaReadinessReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaReadinessReport(options)
      : null;
  const nextStep =
    typeof getCustom25DVisualManualRendererStateSchemaNextStepReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaNextStepReport(options)
      : null;
  const readinessCloseout =
    typeof getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport ===
    "function"
      ? getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (readinessCloseout &&
      Array.isArray(readinessCloseout.requiredOptionKeys) &&
      readinessCloseout.requiredOptionKeys) ||
    (nextStep && Array.isArray(nextStep.requiredOptionKeys) && nextStep.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readinessCloseout && readinessCloseout.blockedBehavior) ||
    (nextStep && nextStep.blockedBehavior) ||
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readinessCloseout && readinessCloseout.preservedSystems) ||
    (nextStep && nextStep.preservedSystems) ||
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readinessCloseout && readinessCloseout.safetyFlags) ||
    (nextStep && nextStep.safetyFlags) ||
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const phase378ReadinessAvailable = !!readiness;
  const phase378NextStepAvailable = !!nextStep;
  const phase378CloseoutAvailable = !!readinessCloseout;
  const expectedFieldSchema =
    (readiness && readiness.expectedFieldTypes) || {
      created: "boolean",
      initialized: "boolean",
      running: "boolean",
      mapAttached: "boolean",
      drawingEnabled: "boolean",
      layerCount: "number",
      rendererInstance: "object|null",
      mapReference: "object|null",
      layers: "array",
      lastManualAction: "string|null",
      lastError: "string|null"
    };
  const blockedRuntimeAreas =
    (readiness && Array.isArray(readiness.blockedRuntimeAreas) && readiness.blockedRuntimeAreas) || [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "leaflet-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm"
    ];
  const blockers = [];
  if (!phase378ReadinessAvailable) blockers.push("phase378-schema-readiness-unavailable");
  if (!phase378NextStepAvailable) blockers.push("phase378-schema-next-step-unavailable");
  if (!phase378CloseoutAvailable) blockers.push("phase378-schema-closeout-unavailable");
  const safestNextMilestone =
    blockers.length === 0 ? "inertStateContainerCreationReadiness" : "pauseForManualReview";
  const nextRecommendedStep =
    safestNextMilestone === "inertStateContainerCreationReadiness"
      ? "prepare-passive-inert-state-container-creation-readiness-phase"
      : "pause-for-manual-review-before-state-container-creation-readiness";

  if (missingKey) {
    return {
      ok: true,
      phase: 379,
      helperName: "getCustom25DVisualManualRendererStateSchemaCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase378ReadinessAvailable,
      phase378NextStepAvailable,
      phase378CloseoutAvailable,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      expectedFieldSchema,
      schemaCloseoutOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      safestNextMilestone,
      blockedRuntimeAreas,
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 379,
    helperName: "getCustom25DVisualManualRendererStateSchemaCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase378ReadinessAvailable,
    phase378NextStepAvailable,
    phase378CloseoutAvailable,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    expectedFieldSchema,
    schemaCloseoutOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    safestNextMilestone,
    blockedRuntimeAreas,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererStateSchemaSelfReviewReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualManualRendererStateSchemaCloseoutReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    (closeout && Array.isArray(closeout.requiredOptionKeys) && closeout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (closeout && closeout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (closeout && closeout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (closeout && closeout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!closeout) blockers.push("phase379-schema-closeout-report-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 379,
      helperName: "getCustom25DVisualManualRendererStateSchemaSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phaseAddsPlanningOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      rollbackNote:
        "remove only the three Phase 379 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 379,
    helperName: "getCustom25DVisualManualRendererStateSchemaSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phaseAddsPlanningOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    rollbackNote:
      "remove only the three Phase 379 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport(options = {}) {
  const closeout =
    typeof getCustom25DVisualManualRendererStateSchemaCloseoutReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaCloseoutReport(options)
      : null;
  const selfReview =
    typeof getCustom25DVisualManualRendererStateSchemaSelfReviewReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaSelfReviewReport(options)
      : null;
  const requiredOptionKeys =
    (selfReview && Array.isArray(selfReview.requiredOptionKeys) && selfReview.requiredOptionKeys) ||
    (closeout && Array.isArray(closeout.requiredOptionKeys) && closeout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (selfReview && selfReview.blockedBehavior) ||
    (closeout && closeout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (selfReview && selfReview.preservedSystems) ||
    (closeout && closeout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (selfReview && selfReview.safetyFlags) ||
    (closeout && closeout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!closeout) blockers.push("phase379-schema-closeout-report-unavailable");
  if (!selfReview) blockers.push("phase379-schema-self-review-report-unavailable");
  const recommendedNextRendererMilestone =
    blockers.length === 0 ? "inertStateContainerCreationReadiness" : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 379,
      helperName: "getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      nextStepOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      recommendedNextRendererMilestone,
      inertStateContainerCreationReadiness:
        recommendedNextRendererMilestone === "inertStateContainerCreationReadiness",
      pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
      actualStateContainerCreationBlocked: true,
      broadRuntimeBehaviorBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 379,
    helperName: "getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    nextStepOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    recommendedNextRendererMilestone,
    inertStateContainerCreationReadiness:
      recommendedNextRendererMilestone === "inertStateContainerCreationReadiness",
    pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
    actualStateContainerCreationBlocked: true,
    broadRuntimeBehaviorBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationReadinessReport(options = {}) {
  const schemaCloseout =
    typeof getCustom25DVisualManualRendererStateSchemaCloseoutReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaCloseoutReport(options)
      : null;
  const schemaSelfReview =
    typeof getCustom25DVisualManualRendererStateSchemaSelfReviewReport === "function"
      ? getCustom25DVisualManualRendererStateSchemaSelfReviewReport(options)
      : null;
  const schemaNextStep =
    typeof getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport ===
    "function"
      ? getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport(options)
      : null;
  const requiredOptionKeys =
    (schemaNextStep &&
      Array.isArray(schemaNextStep.requiredOptionKeys) &&
      schemaNextStep.requiredOptionKeys) ||
    (schemaSelfReview &&
      Array.isArray(schemaSelfReview.requiredOptionKeys) &&
      schemaSelfReview.requiredOptionKeys) ||
    (schemaCloseout &&
      Array.isArray(schemaCloseout.requiredOptionKeys) &&
      schemaCloseout.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (schemaNextStep && schemaNextStep.blockedBehavior) ||
    (schemaSelfReview && schemaSelfReview.blockedBehavior) ||
    (schemaCloseout && schemaCloseout.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (schemaNextStep && schemaNextStep.preservedSystems) ||
    (schemaSelfReview && schemaSelfReview.preservedSystems) ||
    (schemaCloseout && schemaCloseout.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (schemaNextStep && schemaNextStep.safetyFlags) ||
    (schemaSelfReview && schemaSelfReview.safetyFlags) ||
    (schemaCloseout && schemaCloseout.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const phase379CloseoutAvailable = !!schemaCloseout;
  const phase379SelfReviewAvailable = !!schemaSelfReview;
  const phase379NextStepAvailable = !!schemaNextStep;
  const expectedFieldSchema =
    (schemaCloseout && schemaCloseout.expectedFieldSchema) || {
      created: "boolean",
      initialized: "boolean",
      running: "boolean",
      mapAttached: "boolean",
      drawingEnabled: "boolean",
      layerCount: "number",
      rendererInstance: "object|null",
      mapReference: "object|null",
      layers: "array",
      lastManualAction: "string|null",
      lastError: "string|null"
    };
  const allowedLaterCreationBehavior = {
    createWindowStateContainerOnlyIfAbsent: true,
    useInertDefaultsOnly: true,
    doNotReplaceExistingObject: true,
    doNotMutateExistingObject: true,
    doNotCreateRendererInstances: true,
    doNotAttachMapReferences: true,
    doNotCreateLayers: true,
    doNotDraw: true,
    doNotWireStartup: true
  };
  const blockedRuntimeAreas =
    (schemaCloseout &&
      Array.isArray(schemaCloseout.blockedRuntimeAreas) &&
      schemaCloseout.blockedRuntimeAreas) || [
      "renderer-init-start-run",
      "map-attachment",
      "drawing",
      "dom-mutation",
      "leaflet-mutation",
      "shared-state-mutation",
      "backend-storage-network",
      "gameplay-pins-player-marker-osm"
    ];
  const blockers = [];
  if (!phase379CloseoutAvailable) blockers.push("phase379-schema-closeout-unavailable");
  if (!phase379SelfReviewAvailable) blockers.push("phase379-schema-self-review-unavailable");
  if (!phase379NextStepAvailable) blockers.push("phase379-schema-next-step-unavailable");
  const safestNextMilestone =
    blockers.length === 0 ? "inertStateContainerCreation" : "pauseForManualReview";
  const nextRecommendedStep =
    safestNextMilestone === "inertStateContainerCreation"
      ? "prepare-explicit-inert-state-container-creation-phase"
      : "pause-for-manual-review-before-inert-state-container-creation";

  if (missingKey) {
    return {
      ok: true,
      phase: 380,
      helperName: "getCustom25DVisualInertStateContainerCreationReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase379CloseoutAvailable,
      phase379SelfReviewAvailable,
      phase379NextStepAvailable,
      plannedStateContainerName: "custom25DVisualManualRendererState",
      expectedFieldSchema,
      allowedLaterCreationBehavior,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      safestNextMilestone,
      blockedRuntimeAreas,
      blockers,
      nextRecommendedStep,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 380,
    helperName: "getCustom25DVisualInertStateContainerCreationReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase379CloseoutAvailable,
    phase379SelfReviewAvailable,
    phase379NextStepAvailable,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    expectedFieldSchema,
    allowedLaterCreationBehavior,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    safestNextMilestone,
    blockedRuntimeAreas,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationGateReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualInertStateContainerCreationReadinessReport === "function"
      ? getCustom25DVisualInertStateContainerCreationReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = readiness && Array.isArray(readiness.blockers) ? [...readiness.blockers] : [];
  if (!readiness) blockers.push("phase380-creation-readiness-unavailable");
  const recommendedNextRendererMilestone =
    blockers.length === 0 ? "inertStateContainerCreation" : "pauseForManualReview";

  if (missingKey) {
    return {
      ok: true,
      phase: 380,
      helperName: "getCustom25DVisualInertStateContainerCreationGateReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      gateOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      laterInertCreationMayProceed:
        recommendedNextRendererMilestone === "inertStateContainerCreation",
      inertStateContainerCreation:
        recommendedNextRendererMilestone === "inertStateContainerCreation",
      pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
      laterCreationMeansSafeDefaultsOnly: true,
      laterCreationMeansNoRendererInstance: true,
      laterCreationMeansNoMapReference: true,
      laterCreationMeansNoLayers: true,
      laterCreationMeansNoStartupWiring: true,
      laterCreationMeansNoDrawing: true,
      laterCreationMeansNoGameplayBehavior: true,
      broaderRuntimeBehaviorBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 380,
    helperName: "getCustom25DVisualInertStateContainerCreationGateReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    gateOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    laterInertCreationMayProceed:
      recommendedNextRendererMilestone === "inertStateContainerCreation",
    inertStateContainerCreation:
      recommendedNextRendererMilestone === "inertStateContainerCreation",
    pauseForManualReview: recommendedNextRendererMilestone === "pauseForManualReview",
    laterCreationMeansSafeDefaultsOnly: true,
    laterCreationMeansNoRendererInstance: true,
    laterCreationMeansNoMapReference: true,
    laterCreationMeansNoLayers: true,
    laterCreationMeansNoStartupWiring: true,
    laterCreationMeansNoDrawing: true,
    laterCreationMeansNoGameplayBehavior: true,
    broaderRuntimeBehaviorBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport(options = {}) {
  const readiness =
    typeof getCustom25DVisualInertStateContainerCreationReadinessReport === "function"
      ? getCustom25DVisualInertStateContainerCreationReadinessReport(options)
      : null;
  const gate =
    typeof getCustom25DVisualInertStateContainerCreationGateReport === "function"
      ? getCustom25DVisualInertStateContainerCreationGateReport(options)
      : null;
  const requiredOptionKeys =
    (gate && Array.isArray(gate.requiredOptionKeys) && gate.requiredOptionKeys) ||
    (readiness && Array.isArray(readiness.requiredOptionKeys) && readiness.requiredOptionKeys) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (gate && gate.blockedBehavior) ||
    (readiness && readiness.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (gate && gate.preservedSystems) ||
    (readiness && readiness.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (gate && gate.safetyFlags) ||
    (readiness && readiness.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!readiness) blockers.push("phase380-creation-readiness-unavailable");
  if (!gate) blockers.push("phase380-creation-gate-unavailable");

  if (missingKey) {
    return {
      ok: true,
      phase: 380,
      helperName: "getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      creationReadinessOnly: true,
      stateContainerCreated: false,
      sharedStateCreated: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingEnabled: false,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      runtimeBehaviorChanged: false,
      rollbackNote:
        "remove only the three Phase 380 helper declarations and their namespace/registry entries",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 380,
    helperName: "getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    creationReadinessOnly: true,
    stateContainerCreated: false,
    sharedStateCreated: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    rollbackNote:
      "remove only the three Phase 380 helper declarations and their namespace/registry entries",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function createCustom25DVisualInertManualRendererStateContainer(options = {}) {
  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";
  const requiredSignals = [
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "explicitOptionsOnly",
      passed: options.explicitOptionsOnly === true,
      reason: "explicit-options-only-required"
    },
    {
      key: "allowCustom25DVisualInertStateContainerCreation",
      passed: options.allowCustom25DVisualInertStateContainerCreation === true,
      reason: "inert-state-container-creation-not-allowed"
    },
    {
      key: "confirmManualStateContainerCreationOnly",
      passed: options.confirmManualStateContainerCreationOnly === true,
      reason: "manual-state-container-creation-confirmation-required"
    },
    {
      key: "confirmNoRendererInstanceCreation",
      passed: options.confirmNoRendererInstanceCreation === true,
      reason: "no-renderer-instance-creation-confirmation-required"
    },
    {
      key: "confirmNoMapAttachment",
      passed: options.confirmNoMapAttachment === true,
      reason: "no-map-attachment-confirmation-required"
    },
    {
      key: "confirmNoDrawing",
      passed: options.confirmNoDrawing === true,
      reason: "no-drawing-confirmation-required"
    },
    {
      key: "confirmNoStartupWiring",
      passed: options.confirmNoStartupWiring === true,
      reason: "no-startup-wiring-confirmation-required"
    },
    {
      key: "confirmNoGameplayChange",
      passed: options.confirmNoGameplayChange === true,
      reason: "no-gameplay-change-confirmation-required"
    },
    {
      key: "noStartupWiring",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-acknowledgement-required"
    },
    {
      key: "noBackendChanges",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-acknowledgement-required"
    },
    {
      key: "noPersistence",
      passed: options.noPersistence === true,
      reason: "no-persistence-acknowledgement-required"
    },
    {
      key: "noAutomaticInvocation",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-acknowledgement-required"
    }
  ];
  const requiredOptionKeys = requiredSignals.map((signal) => signal.key);
  const firstFailure = requiredSignals.find((signal) => !signal.passed) || null;
  const missingRequiredOptionKeys = requiredSignals
    .filter((signal) => !signal.passed)
    .map((signal) => signal.key);

  if (!windowExists) {
    return {
      ok: false,
      phase: 381,
      helperName: "createCustom25DVisualInertManualRendererStateContainer",
      manualOnly: true,
      localDevOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      safariSafeFixPhase: 387,
      jsonSafeResult: true,
      allowed: false,
      blocked: true,
      reason: "window-unavailable",
      failedRequirement: "windowAvailable",
      stateExistsBefore: false,
      stateExistsAfter: false,
      stateContainerCreated: false,
      stateCreatedByThisCall: false,
      stateContainerAlreadyExisted: false,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  const stateExistsBefore =
    typeof window.custom25DVisualManualRendererState !== "undefined";

  if (!isLocalDevHost) {
    return {
      ok: false,
      phase: 381,
      helperName: "createCustom25DVisualInertManualRendererStateContainer",
      manualOnly: true,
      localDevOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      safariSafeFixPhase: 387,
      jsonSafeResult: true,
      allowed: false,
      blocked: true,
      reason: "local-dev-host-required",
      failedRequirement: "localDevHost",
      stateExistsBefore,
      stateExistsAfter: stateExistsBefore,
      stateContainerCreated: false,
      stateCreatedByThisCall: false,
      stateContainerAlreadyExisted: stateExistsBefore,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  if (firstFailure) {
    return {
      ok: false,
      phase: 381,
      helperName: "createCustom25DVisualInertManualRendererStateContainer",
      manualOnly: true,
      localDevOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      safariSafeFixPhase: 387,
      jsonSafeResult: true,
      allowed: false,
      blocked: true,
      reason: firstFailure.reason,
      failedRequirement: firstFailure.key,
      stateExistsBefore,
      stateExistsAfter: stateExistsBefore,
      stateContainerCreated: false,
      stateCreatedByThisCall: false,
      stateContainerAlreadyExisted: stateExistsBefore,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  const existingState = window.custom25DVisualManualRendererState;
  if (typeof existingState !== "undefined") {
    return {
      ok: false,
      phase: 381,
      helperName: "createCustom25DVisualInertManualRendererStateContainer",
      manualOnly: true,
      localDevOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      safariSafeFixPhase: 387,
      jsonSafeResult: true,
      allowed: false,
      blocked: true,
      reason: "state-container-already-exists",
      failedRequirement: "stateContainerAbsent",
      stateExistsBefore: true,
      stateExistsAfter: true,
      stateContainerCreated: false,
      stateCreatedByThisCall: false,
      stateContainerAlreadyExisted: true,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount:
        existingState && typeof existingState.layerCount === "number"
          ? existingState.layerCount
          : 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  const inertState = {
    created: true,
    initialized: false,
    running: false,
    mapAttached: false,
    drawingEnabled: false,
    layerCount: 0,
    rendererInstance: null,
    map: null,
    mapReference: null,
    viewport: {
      attached: false,
      width: 0,
      height: 0,
      pixelRatio: 1
    },
    camera: {
      initialized: false,
      center: null,
      zoom: null,
      bearing: 0,
      pitch: 0
    },
    tiles: {
      enabled: false,
      loaded: false,
      requestedCount: 0,
      loadedCount: 0
    },
    layers: [],
    style: {
      activeStyle: null,
      styleLoaded: false,
      palette: null
    },
    metrics: {
      frameCount: 0,
      drawCount: 0,
      lastFrameMs: 0,
      lastDrawMs: 0
    },
    lifecycle: {
      status: "created",
      flags: {
        created: true,
        initialized: false,
        attached: false,
        running: false,
        stopped: false,
        disposed: false
      },
      timestamps: {
        createdAt: null,
        initializedAt: null,
        attachedAt: null,
        startedAt: null,
        stoppedAt: null,
        disposedAt: null
      }
    },
    lastManualCheckAt: null,
    lastManualAction: "inert-state-container-created",
    lastError: null
  };
  window.custom25DVisualManualRendererState = inertState;

  return {
    ok: true,
    phase: 381,
    helperName: "createCustom25DVisualInertManualRendererStateContainer",
    manualOnly: true,
    localDevOnly: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    safariSafeFixPhase: 387,
    jsonSafeResult: true,
    allowed: true,
    blocked: false,
    reason: null,
    failedRequirement: null,
    stateExistsBefore: false,
    stateExistsAfter: true,
    stateContainerCreated: true,
    stateCreatedByThisCall: true,
    stateContainerAlreadyExisted: false,
    sharedStateReplaced: false,
    existingStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    layerCount: 0,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    gameplayChanged: false,
    runtimeBehaviorChanged: false
  };
}

function getCustom25DVisualInertStateContainerCreationReport(options = {}) {
  const gate =
    typeof getCustom25DVisualInertStateContainerCreationGateReport === "function"
      ? getCustom25DVisualInertStateContainerCreationGateReport(options)
      : null;
  const windowExists = typeof window !== "undefined" && window;
  const existingState =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof existingState !== "undefined";
  const isObjectState = !!existingState && typeof existingState === "object";
  const observedStateShape = stateContainerExists
    ? {
        created:
          isObjectState && typeof existingState.created === "boolean"
            ? existingState.created
            : null,
        initialized:
          isObjectState && typeof existingState.initialized === "boolean"
            ? existingState.initialized
            : null,
        running:
          isObjectState && typeof existingState.running === "boolean"
            ? existingState.running
            : null,
        mapAttached:
          isObjectState && typeof existingState.mapAttached === "boolean"
            ? existingState.mapAttached
            : null,
        drawingEnabled:
          isObjectState && typeof existingState.drawingEnabled === "boolean"
            ? existingState.drawingEnabled
            : null,
        layerCount:
          isObjectState && typeof existingState.layerCount === "number"
            ? existingState.layerCount
            : null,
        rendererInstance:
          isObjectState && Object.prototype.hasOwnProperty.call(existingState, "rendererInstance")
            ? existingState.rendererInstance
            : undefined,
        mapReference:
          isObjectState && Object.prototype.hasOwnProperty.call(existingState, "mapReference")
            ? existingState.mapReference
            : undefined,
        layersIsArray: isObjectState ? Array.isArray(existingState.layers) : false,
        lastManualAction:
          isObjectState && typeof existingState.lastManualAction === "string"
            ? existingState.lastManualAction
            : existingState && existingState.lastManualAction === null
              ? null
              : undefined,
        lastError:
          isObjectState && Object.prototype.hasOwnProperty.call(existingState, "lastError")
            ? existingState.lastError
            : undefined
      }
    : null;
  const inertDefaultsMatch = !!(
    isObjectState &&
    existingState.created === true &&
    existingState.initialized === false &&
    existingState.running === false &&
    existingState.mapAttached === false &&
    existingState.drawingEnabled === false &&
    existingState.layerCount === 0 &&
    existingState.rendererInstance === null &&
    existingState.mapReference === null &&
    Array.isArray(existingState.layers) &&
    existingState.layers.length === 0 &&
    existingState.lastManualAction === "inert-state-container-created" &&
    existingState.lastError === null
  );
  const blockers = [];
  if (!gate) blockers.push("phase380-gate-report-unavailable");
  if (!windowExists) blockers.push("window-unavailable");
  const blockedBehavior =
    (gate && gate.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (gate && gate.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (gate && gate.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const nextRecommendedStep =
    inertDefaultsMatch || !stateContainerExists
      ? "review-inert-state-container-creation-closeout"
      : "pause-for-manual-review-before-closeout";

  return {
    ok: true,
    phase: 381,
    helperName: "getCustom25DVisualInertStateContainerCreationReport",
    passive: true,
    reportOnly: true,
    phase380GateAvailable: !!gate,
    stateContainerExists,
    plannedStateContainerName: "custom25DVisualManualRendererState",
    observedStateShape,
    inertDefaultsMatch,
    stateContainerCreatedByReport: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached:
      isObjectState && typeof existingState.mapAttached === "boolean"
        ? existingState.mapAttached
        : false,
    drawingEnabled:
      isObjectState && typeof existingState.drawingEnabled === "boolean"
        ? existingState.drawingEnabled
        : false,
    layerCount:
      isObjectState && typeof existingState.layerCount === "number"
        ? existingState.layerCount
        : 0,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    blockers,
    nextRecommendedStep,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationCloseoutReport(options = {}) {
  const creationReport =
    typeof getCustom25DVisualInertStateContainerCreationReport === "function"
      ? getCustom25DVisualInertStateContainerCreationReport(options)
      : null;
  const blockers = [];
  if (!creationReport) blockers.push("phase381-creation-report-unavailable");
  const blockedBehavior =
    (creationReport && creationReport.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (creationReport && creationReport.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (creationReport && creationReport.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  return {
    ok: true,
    phase: 381,
    helperName: "getCustom25DVisualInertStateContainerCreationCloseoutReport",
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    phase381IntroducedManualCreationHelperOnly: true,
    phase381IntroducedPassiveReportsOnly: true,
    automaticStateCreationOnLoad: false,
    existingStateReplaced: false,
    existingStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    blockers,
    rollbackNote:
      "remove only the three Phase 381 helper declarations and their namespace/registry entries; if a manual test created window.custom25DVisualManualRendererState in the browser session, refresh the page to clear it",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualInertStateContainerCreation",
    "confirmManualStateContainerCreationOnly",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const creationReportAvailable =
    typeof getCustom25DVisualInertStateContainerCreationReport === "function";
  const closeoutAvailable =
    typeof getCustom25DVisualInertStateContainerCreationCloseoutReport === "function";
  const creationReport = creationReportAvailable
    ? getCustom25DVisualInertStateContainerCreationReport(options)
    : null;
  const closeoutReport = closeoutAvailable
    ? getCustom25DVisualInertStateContainerCreationCloseoutReport(options)
    : null;
  const blockers = [];
  if (!creationHelperAvailable) blockers.push("phase381-creation-helper-unavailable");
  if (!creationReportAvailable) blockers.push("phase381-creation-report-unavailable");
  if (!closeoutAvailable) blockers.push("phase381-closeout-report-unavailable");
  const blockedBehavior =
    (closeoutReport && closeoutReport.blockedBehavior) ||
    (creationReport && creationReport.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (closeoutReport && closeoutReport.preservedSystems) ||
    (creationReport && creationReport.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (closeoutReport && closeoutReport.safetyFlags) ||
    (creationReport && creationReport.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };

  return {
    ok: blockers.length === 0,
    phase: 382,
    helperName:
      "getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport",
    passive: true,
    reportOnly: true,
    phase381CreationHelperAvailable: creationHelperAvailable,
    phase381CreationReportAvailable: creationReportAvailable,
    phase381CloseoutAvailable: closeoutAvailable,
    creationHelperInvokedByThisReport: false,
    stateContainerCreatedByThisReport: false,
    automaticStateCreation: false,
    requiredOptionKeys,
    createsOnlyIfAbsent: creationHelperAvailable,
    blocksIfAlreadyExists: creationHelperAvailable,
    existingStateMutated: false,
    sharedStateReplaced: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    blockers,
    nextRecommendedStep:
      blockers.length === 0
        ? "inertStateContainerManualConsoleTest"
        : "pauseForManualReview",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualInertStateContainerCreation",
    "confirmManualStateContainerCreationOnly",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const closeoutAvailable =
    typeof getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport ===
    "function";
  const closeoutReport = closeoutAvailable
    ? getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport(options)
    : null;
  const blockedBehavior =
    (closeoutReport && closeoutReport.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (closeoutReport && closeoutReport.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (closeoutReport && closeoutReport.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };

  return {
    ok: !!closeoutReport,
    phase: 382,
    helperName:
      "getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport",
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    requiredOptionKeys,
    phase382AddsPassiveReportsOnly: true,
    stateCreatedByThisPhase: false,
    stateReplacedByThisPhase: false,
    stateMutatedByThisPhase: false,
    phase381CreationHelperAutoInvoked: false,
    helperDeclarationsMoved: false,
    filesCreated: false,
    indexHtmlTouched: false,
    passiveReportsFileTouched: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    backendChanged: false,
    storageChanged: false,
    networkChanged: false,
    startupWired: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    sharedStateBehaviorTouched: false,
    rollbackNote:
      "remove only the three Phase 382 helper declarations and their namespace/registry entries",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualInertStateContainerManualTestReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualInertStateContainerCreation",
    "confirmManualStateContainerCreationOnly",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const closeoutAvailable =
    typeof getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport ===
    "function";
  const selfReviewAvailable =
    typeof getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport ===
    "function";
  const closeoutReport = closeoutAvailable
    ? getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport(options)
    : null;
  const selfReviewReport = selfReviewAvailable
    ? getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport(options)
    : null;
  const blockers = [];
  if (!closeoutAvailable) blockers.push("phase382-sequence-closeout-unavailable");
  if (!selfReviewAvailable) blockers.push("phase382-sequence-self-review-unavailable");
  if (closeoutReport && closeoutReport.phase381CreationHelperAvailable !== true) {
    blockers.push("phase381-creation-helper-unavailable");
  }
  const requiredManualTestOptions = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    explicitOptionsOnly: true,
    allowCustom25DVisualInertStateContainerCreation: true,
    confirmManualStateContainerCreationOnly: true,
    confirmNoRendererInstanceCreation: true,
    confirmNoMapAttachment: true,
    confirmNoDrawing: true,
    confirmNoStartupWiring: true,
    confirmNoGameplayChange: true,
    noStartupWiring: true,
    noBackendChanges: true,
    noPersistence: true,
    noAutomaticInvocation: true
  };
  const blockedBehavior =
    (selfReviewReport && selfReviewReport.blockedBehavior) ||
    (closeoutReport && closeoutReport.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (selfReviewReport && selfReviewReport.preservedSystems) ||
    (closeoutReport && closeoutReport.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (selfReviewReport && selfReviewReport.safetyFlags) ||
    (closeoutReport && closeoutReport.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };

  return {
    ok: blockers.length === 0,
    phase: 382,
    helperName: "getCustom25DVisualInertStateContainerManualTestReadinessReport",
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    requiredOptionKeys,
    requiredManualTestOptions,
    creationHelperReadyForManualConsoleTest: blockers.length === 0,
    creationHelperInvokedByThisReport: false,
    stateContainerCreatedByThisReport: false,
    automaticStateCreation: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    runtimeBehaviorChanged: false,
    blockers,
    recommendedNextMilestone:
      blockers.length === 0
        ? "inertStateContainerManualConsoleTest"
        : "pauseForManualReview",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function runCustom25DVisualInertStateContainerManualConsoleTest(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualInertStateContainerCreation",
    "confirmManualStateContainerCreationOnly",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation",
    "runManualConsoleTest",
    "confirmManualConsoleTestOnly"
  ];
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const windowExists = typeof window !== "undefined" && window;
  const existingStateBeforeObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExistsBefore = typeof existingStateBeforeObject !== "undefined";
  const requiredSignals = [
    { key: "manual", reason: "manual-flag-required" },
    { key: "developerIntent", reason: "developer-intent-required" },
    { key: "localDevOnly", reason: "local-dev-only-required" },
    { key: "browserConsoleOnly", reason: "browser-console-only-required" },
    { key: "explicitOptionsOnly", reason: "explicit-options-only-required" },
    {
      key: "allowCustom25DVisualInertStateContainerCreation",
      reason: "inert-state-container-creation-not-allowed"
    },
    {
      key: "confirmManualStateContainerCreationOnly",
      reason: "manual-state-container-creation-confirmation-required"
    },
    {
      key: "confirmNoRendererInstanceCreation",
      reason: "no-renderer-instance-creation-confirmation-required"
    },
    {
      key: "confirmNoMapAttachment",
      reason: "no-map-attachment-confirmation-required"
    },
    {
      key: "confirmNoDrawing",
      reason: "no-drawing-confirmation-required"
    },
    {
      key: "confirmNoStartupWiring",
      reason: "no-startup-wiring-confirmation-required"
    },
    {
      key: "confirmNoGameplayChange",
      reason: "no-gameplay-change-confirmation-required"
    },
    {
      key: "noStartupWiring",
      reason: "no-startup-wiring-acknowledgement-required"
    },
    {
      key: "noBackendChanges",
      reason: "no-backend-changes-acknowledgement-required"
    },
    { key: "noPersistence", reason: "no-persistence-acknowledgement-required" },
    {
      key: "noAutomaticInvocation",
      reason: "no-automatic-invocation-acknowledgement-required"
    },
    {
      key: "runManualConsoleTest",
      reason: "manual-console-test-run-acknowledgement-required"
    },
    {
      key: "confirmManualConsoleTestOnly",
      reason: "manual-console-test-only-confirmation-required"
    }
  ];
  const firstFailure =
    requiredSignals.find((signal) => options[signal.key] !== true) || null;
  const missingRequiredOptionKeys = requiredSignals
    .filter((signal) => options[signal.key] !== true)
    .map((signal) => signal.key);

  if (!creationHelperAvailable) {
    return {
      ok: false,
      phase: 383,
      helperName: "runCustom25DVisualInertStateContainerManualConsoleTest",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      safariSafeFixPhase: 386,
      jsonSafeResult: true,
      deepReportEmbeddingAvoided: true,
      allowed: false,
      blocked: true,
      reason: "phase381-creation-helper-unavailable",
      creationHelperAvailable: false,
      creationHelperInvoked: false,
      creationResult: null,
      stateExistsBefore,
      stateExistsAfter: stateExistsBefore,
      inertDefaultsMatchAfterTest: false,
      stateContainerCreated: false,
      stateCreatedByThisRun: false,
      stateContainerAlreadyExisted:
        stateExistsBefore,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  if (firstFailure) {
    return {
      ok: false,
      phase: 383,
      helperName: "runCustom25DVisualInertStateContainerManualConsoleTest",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      safariSafeFixPhase: 386,
      jsonSafeResult: true,
      deepReportEmbeddingAvoided: true,
      allowed: false,
      blocked: true,
      reason: firstFailure.reason,
      creationHelperAvailable: true,
      creationHelperInvoked: false,
      creationResult: null,
      stateExistsBefore,
      stateExistsAfter: stateExistsBefore,
      inertDefaultsMatchAfterTest: false,
      stateContainerCreated: false,
      stateCreatedByThisRun: false,
      stateContainerAlreadyExisted:
        stateExistsBefore,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  if (options.dryRunOnly === true) {
    return {
      ok: true,
      phase: 383,
      helperName: "runCustom25DVisualInertStateContainerManualConsoleTest",
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys: [],
      safariSafeFixPhase: 387,
      jsonSafeResult: true,
      deepReportEmbeddingAvoided: true,
      allowed: true,
      blocked: false,
      dryRunOnly: true,
      wouldCreateStateContainer: !stateExistsBefore,
      reason: "dry-run-only",
      creationHelperAvailable: true,
      creationHelperInvoked: false,
      creationResult: null,
      stateExistsBefore,
      stateExistsAfter: stateExistsBefore,
      stateContainerExistsAfterTest: stateExistsBefore,
      inertDefaultsMatchAfterTest: false,
      stateContainerCreated: false,
      stateCreatedByThisRun: false,
      stateContainerAlreadyExisted: stateExistsBefore,
      sharedStateReplaced: false,
      existingStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }

  const creationResult = createCustom25DVisualInertManualRendererStateContainer(options);
  const stateAfterObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateExistsAfter = typeof stateAfterObject !== "undefined";
  const isObjectState = !!stateAfterObject && typeof stateAfterObject === "object";
  const inertDefaultsMatchAfterTest = !!(
    isObjectState &&
    stateAfterObject.created === true &&
    stateAfterObject.initialized === false &&
    stateAfterObject.running === false &&
    stateAfterObject.mapAttached === false &&
    stateAfterObject.drawingEnabled === false &&
    stateAfterObject.layerCount === 0 &&
    stateAfterObject.rendererInstance === null &&
    stateAfterObject.mapReference === null &&
    Array.isArray(stateAfterObject.layers) &&
    stateAfterObject.layers.length === 0 &&
    stateAfterObject.lastManualAction === "inert-state-container-created" &&
    stateAfterObject.lastError === null
  );
  const creationResultSummary = creationResult
    ? {
        allowed: creationResult.allowed === true,
        blocked: creationResult.blocked === true,
        safariSafeFixPhase:
          typeof creationResult.safariSafeFixPhase === "number"
            ? creationResult.safariSafeFixPhase
            : null,
        reason:
          typeof creationResult.reason === "string" ? creationResult.reason : null,
        failedRequirement:
          typeof creationResult.failedRequirement === "string"
            ? creationResult.failedRequirement
            : null,
        stateContainerCreated: creationResult.stateContainerCreated === true,
        stateContainerAlreadyExisted:
          creationResult.stateContainerAlreadyExisted === true,
        layerCount:
          typeof creationResult.layerCount === "number" ? creationResult.layerCount : 0
      }
    : null;

  return {
    ok: true,
    phase: 383,
    helperName: "runCustom25DVisualInertStateContainerManualConsoleTest",
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    safariSafeFixPhase: 386,
    jsonSafeResult: true,
    deepReportEmbeddingAvoided: true,
    allowed: !!(creationResult && creationResult.allowed === true),
    blocked: !(creationResult && creationResult.allowed === true),
    dryRunOnly: false,
    reason:
      (creationResult && creationResult.reason) ||
      (creationResult && creationResult.allowed === true
      ? "manual-console-test-completed"
        : "manual-console-test-blocked"),
    creationHelperAvailable: true,
    creationHelperInvoked: true,
    creationResult: creationResultSummary,
    stateExistsBefore,
    stateExistsAfter,
    stateContainerExistsAfterTest: stateExistsAfter,
    inertDefaultsMatchAfterTest,
    stateContainerCreated: !!(
      creationResult && creationResult.stateContainerCreated === true
    ),
    stateCreatedByThisRun: !!(
      creationResult && creationResult.stateContainerCreated === true
    ),
    stateContainerAlreadyExisted: !!(
      creationResult && creationResult.stateContainerAlreadyExisted === true
    ),
    sharedStateReplaced: false,
    existingStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    layerCount:
      isObjectState && typeof stateAfterObject.layerCount === "number"
        ? stateAfterObject.layerCount
        : creationResult && typeof creationResult.layerCount === "number"
          ? creationResult.layerCount
          : 0,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    gameplayChanged: false,
    runtimeBehaviorChanged: false
  };
}

function getCustom25DVisualInertStateContainerManualConsoleTestReport(
  options = {}
) {
  const requiredManualOptions = {
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    explicitOptionsOnly: true,
    allowCustom25DVisualInertStateContainerCreation: true,
    confirmManualStateContainerCreationOnly: true,
    confirmNoRendererInstanceCreation: true,
    confirmNoMapAttachment: true,
    confirmNoDrawing: true,
    confirmNoStartupWiring: true,
    confirmNoGameplayChange: true,
    noStartupWiring: true,
    noBackendChanges: true,
    noPersistence: true,
    noAutomaticInvocation: true,
    runManualConsoleTest: true,
    confirmManualConsoleTestOnly: true
  };
  const requiredOptionKeys = Object.keys(requiredManualOptions);
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const existingStateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof existingStateObject !== "undefined";
  const isObjectState = !!existingStateObject && typeof existingStateObject === "object";
  const observedStateShape = stateContainerExists
    ? {
        created:
          isObjectState && typeof existingStateObject.created === "boolean"
            ? existingStateObject.created
            : null,
        initialized:
          isObjectState && typeof existingStateObject.initialized === "boolean"
            ? existingStateObject.initialized
            : null,
        running:
          isObjectState && typeof existingStateObject.running === "boolean"
            ? existingStateObject.running
            : null,
        mapAttached:
          isObjectState && typeof existingStateObject.mapAttached === "boolean"
            ? existingStateObject.mapAttached
            : null,
        drawingEnabled:
          isObjectState && typeof existingStateObject.drawingEnabled === "boolean"
            ? existingStateObject.drawingEnabled
            : null,
        layerCount:
          isObjectState && typeof existingStateObject.layerCount === "number"
            ? existingStateObject.layerCount
            : null,
        layersIsArray: isObjectState ? Array.isArray(existingStateObject.layers) : false,
        rendererInstanceIsNull:
          isObjectState ? existingStateObject.rendererInstance === null : false,
        mapReferenceIsNull:
          isObjectState ? existingStateObject.mapReference === null : false,
        lastManualAction:
          isObjectState && typeof existingStateObject.lastManualAction === "string"
            ? existingStateObject.lastManualAction
            : null,
        lastError:
          isObjectState &&
          Object.prototype.hasOwnProperty.call(existingStateObject, "lastError")
            ? existingStateObject.lastError
            : undefined
      }
    : null;
  const inertDefaultsMatch = !!(
    isObjectState &&
    existingStateObject.created === true &&
    existingStateObject.initialized === false &&
    existingStateObject.running === false &&
    existingStateObject.mapAttached === false &&
    existingStateObject.drawingEnabled === false &&
    existingStateObject.layerCount === 0 &&
    existingStateObject.rendererInstance === null &&
    existingStateObject.mapReference === null &&
    Array.isArray(existingStateObject.layers) &&
    existingStateObject.layers.length === 0 &&
    existingStateObject.lastManualAction === "inert-state-container-created" &&
    existingStateObject.lastError === null
  );
  const blockers = [];
  const readinessHelperAvailable =
    typeof getCustom25DVisualInertStateContainerManualTestReadinessReport ===
    "function";
  if (!readinessHelperAvailable) {
    blockers.push("phase382-manual-test-readiness-helper-unavailable");
  }
  if (
    typeof runCustom25DVisualInertStateContainerManualConsoleTest !== "function"
  ) {
    blockers.push("phase383-manual-console-test-helper-unavailable");
  }
  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 383,
      helperName: "getCustom25DVisualInertStateContainerManualConsoleTestReport",
      passive: true,
      reportOnly: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      safariSafeFixPhase: 386,
      jsonSafeResult: true,
      deepReportEmbeddingAvoided: true,
      blocked: true,
      allowed: false,
      reason: "required-options-missing",
      phase382ManualTestReadinessAvailable: readinessHelperAvailable,
      manualConsoleTestHelperAvailable:
        typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function",
      stateContainerExists,
      inertDefaultsMatch,
      creationHelperInvokedByReport: false,
      manualTestInvokedByReport: false,
      stateContainerCreatedByReport: false,
      sharedStateReplaced: false,
      sharedStateMutated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      layerCount: 0,
      domMutated: false,
      leafletMutated: false,
      startupWired: false,
      gameplayChanged: false,
      runtimeBehaviorChanged: false
    };
  }
  return {
    ok: true,
    phase: 383,
    helperName: "getCustom25DVisualInertStateContainerManualConsoleTestReport",
    passive: true,
    reportOnly: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    safariSafeFixPhase: 386,
    jsonSafeResult: true,
    deepReportEmbeddingAvoided: true,
    phase382ManualTestReadinessAvailable: readinessHelperAvailable,
    manualConsoleTestHelperAvailable:
      typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function",
    blocked: false,
    allowed: true,
    stateContainerExists,
    observedStateShape,
    inertDefaultsMatch,
    creationHelperInvokedByReport: false,
    manualTestInvokedByReport: false,
    stateContainerCreatedByReport: false,
    sharedStateReplaced: false,
    sharedStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached:
      isObjectState && typeof existingStateObject.mapAttached === "boolean"
        ? existingStateObject.mapAttached
        : false,
    drawingEnabled:
      isObjectState && typeof existingStateObject.drawingEnabled === "boolean"
        ? existingStateObject.drawingEnabled
        : false,
    layerCount:
      isObjectState && typeof existingStateObject.layerCount === "number"
        ? existingStateObject.layerCount
        : 0,
    domMutated: false,
    leafletMutated: false,
    startupWired: false,
    requiredManualOptions,
    suggestedConsoleCommand: `window.GrowGoCustom25DVisualManualTests.runCustom25DVisualInertStateContainerManualConsoleTest(${JSON.stringify(requiredManualOptions, null, 2)})`,
    blockers,
    nextRecommendedStep:
      blockers.length === 0
        ? "run-manual-console-test-once"
        : "pauseForManualReview",
    gameplayChanged: false,
    runtimeBehaviorChanged: false
  };
}

function getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport(
  options = {}
) {
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const report =
    typeof getCustom25DVisualInertStateContainerManualConsoleTestReport === "function"
      ? getCustom25DVisualInertStateContainerManualConsoleTestReport(options)
      : null;
  const blockers = [];
  if (!report) blockers.push("phase383-manual-console-test-report-unavailable");
  return {
    ok: !!report,
    phase: 383,
    helperName:
      "getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport",
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    phase383AddedManualConsoleTestHelperOnly: true,
    manualTestHelperAutoInvoked: false,
    passiveReportInvokesManualTest: false,
    passiveReportInvokesCreationHelper: false,
    automaticStateCreationOnLoad: false,
    existingStateReplaced: false,
    existingStateMutated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    rendererRunning: false,
    mapAttached: false,
    drawingEnabled: false,
    domMutated: false,
    leafletMutated: false,
    backendChanged: false,
    storageChanged: false,
    networkChanged: false,
    startupWired: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    blockers,
    rollbackNote:
      "remove only the three Phase 383 helper declarations and their namespace/registry entries; if a manual browser-console test created window.custom25DVisualManualRendererState in the browser session, refresh the page to clear it",
    blockedBehavior: (report && report.blockedBehavior) || blockedBehavior,
    preservedSystems: (report && report.preservedSystems) || preservedSystems,
    safetyFlags: (report && report.safetyFlags) || safetyFlags
  };
}

function getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowCustom25DVisualInertStateContainerCreation",
    "confirmManualStateContainerCreationOnly",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation",
    "runManualConsoleTest",
    "confirmManualConsoleTestOnly"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateExistsBefore =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const runHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";

  return {
    ok: creationHelperAvailable && runHelperAvailable,
    blocked: missingRequiredOptionKeys.length > 0,
    allowed: missingRequiredOptionKeys.length === 0,
    phase: 387,
    helperName:
      "getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport",
    requiredOptionKeys,
    missingRequiredOptionKeys,
    stateExistsBefore,
    creationHelperAvailable,
    runHelperAvailable,
    safariSafeFixPhase: 387,
    jsonSafeResult: true,
    rendererInstanceCreated: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false
  };
}

function getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "closeoutOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const manualReportHelperAvailable =
    typeof getCustom25DVisualInertStateContainerManualConsoleTestReport === "function";
  const preflightDiagnosticHelperAvailable =
    typeof getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport ===
    "function";
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 388,
      helperName:
        "getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport",
      closeoutOnly: true,
      reportOnly: true,
      passive: true,
      safariSafeCloseout: true,
      jsonSafeResult: true,
      deepReportEmbeddingAvoided: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      creationHelperAvailable,
      manualRunHelperAvailable,
      manualReportHelperAvailable,
      preflightDiagnosticHelperAvailable,
      stateContainerExists,
      observedCreated: null,
      observedInitialized: null,
      observedRunning: null,
      observedMapAttached: null,
      observedDrawingEnabled: null,
      observedLayerCount: 0,
      creationHelperInvokedByCloseout: false,
      runHelperInvokedByCloseout: false,
      stateCreatedByCloseout: false,
      stateMutatedByCloseout: false,
      rendererInstanceCreated: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 388,
    helperName:
      "getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport",
    closeoutOnly: true,
    reportOnly: true,
    passive: true,
    safariSafeCloseout: true,
    jsonSafeResult: true,
    deepReportEmbeddingAvoided: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    creationHelperAvailable,
    manualRunHelperAvailable,
    manualReportHelperAvailable,
    preflightDiagnosticHelperAvailable,
    stateContainerExists,
    observedCreated:
      isObjectState && typeof stateObject.created === "boolean"
        ? stateObject.created
        : null,
    observedInitialized:
      isObjectState && typeof stateObject.initialized === "boolean"
        ? stateObject.initialized
        : null,
    observedRunning:
      isObjectState && typeof stateObject.running === "boolean"
        ? stateObject.running
        : null,
    observedMapAttached:
      isObjectState && typeof stateObject.mapAttached === "boolean"
        ? stateObject.mapAttached
        : null,
    observedDrawingEnabled:
      isObjectState && typeof stateObject.drawingEnabled === "boolean"
        ? stateObject.drawingEnabled
        : null,
    observedLayerCount:
      isObjectState && typeof stateObject.layerCount === "number"
        ? stateObject.layerCount
        : 0,
    creationHelperInvokedByCloseout: false,
    runHelperInvokedByCloseout: false,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    rendererInstanceCreated: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererStateFieldsReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const plannedFields = [
    "viewport",
    "camera",
    "tiles",
    "layers",
    "style",
    "metrics",
    "lastManualCheckAt"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const manualReportHelperAvailable =
    typeof getCustom25DVisualInertStateContainerManualConsoleTestReport === "function";
  const safariSafeCloseoutHelperAvailable =
    typeof getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport ===
    "function";
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 389,
      helperName: "getCustom25DVisualManualRendererStateFieldsReadinessReport",
      planningOnly: true,
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      creationHelperAvailable,
      manualRunHelperAvailable,
      manualReportHelperAvailable,
      safariSafeCloseoutHelperAvailable,
      stateContainerExists,
      observedCreated: null,
      observedInitialized: null,
      observedRunning: null,
      observedMapAttached: null,
      observedDrawingEnabled: null,
      observedLayerCount: 0,
      plannedFieldCount: plannedFields.length,
      plannedFields,
      fieldsAddedThisPhase: false,
      fieldsMutatedThisPhase: false,
      futureFieldsArePlanningOnly: true,
      stateCreatedByReadiness: false,
      stateMutatedByReadiness: false,
      creationHelperInvokedByReadiness: false,
      runHelperInvokedByReadiness: false,
      rendererInstanceCreated: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 389,
    helperName: "getCustom25DVisualManualRendererStateFieldsReadinessReport",
    planningOnly: true,
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    creationHelperAvailable,
    manualRunHelperAvailable,
    manualReportHelperAvailable,
    safariSafeCloseoutHelperAvailable,
    stateContainerExists,
    observedCreated:
      isObjectState && typeof stateObject.created === "boolean"
        ? stateObject.created
        : null,
    observedInitialized:
      isObjectState && typeof stateObject.initialized === "boolean"
        ? stateObject.initialized
        : null,
    observedRunning:
      isObjectState && typeof stateObject.running === "boolean"
        ? stateObject.running
        : null,
    observedMapAttached:
      isObjectState && typeof stateObject.mapAttached === "boolean"
        ? stateObject.mapAttached
        : null,
    observedDrawingEnabled:
      isObjectState && typeof stateObject.drawingEnabled === "boolean"
        ? stateObject.drawingEnabled
        : null,
    observedLayerCount:
      isObjectState && typeof stateObject.layerCount === "number"
        ? stateObject.layerCount
        : 0,
    plannedFieldCount: plannedFields.length,
    plannedFields,
    fieldsAddedThisPhase: false,
    fieldsMutatedThisPhase: false,
    futureFieldsArePlanningOnly: true,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    creationHelperInvokedByReadiness: false,
    runHelperInvokedByReadiness: false,
    rendererInstanceCreated: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererInertStateFieldsReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const viewportFieldPresent =
    isObjectState &&
    !!stateObject.viewport &&
    typeof stateObject.viewport === "object";
  const cameraFieldPresent =
    isObjectState && !!stateObject.camera && typeof stateObject.camera === "object";
  const tilesFieldPresent =
    isObjectState && !!stateObject.tiles && typeof stateObject.tiles === "object";
  const layersFieldPresent = isObjectState && Array.isArray(stateObject.layers);
  const styleFieldPresent =
    isObjectState && !!stateObject.style && typeof stateObject.style === "object";
  const metricsFieldPresent =
    isObjectState && !!stateObject.metrics && typeof stateObject.metrics === "object";
  const lastManualCheckAtFieldPresent =
    isObjectState &&
    Object.prototype.hasOwnProperty.call(stateObject, "lastManualCheckAt");
  const inertFieldDefaultsMatch = !!(
    viewportFieldPresent &&
    stateObject.viewport.attached === false &&
    stateObject.viewport.width === 0 &&
    stateObject.viewport.height === 0 &&
    stateObject.viewport.pixelRatio === 1 &&
    cameraFieldPresent &&
    stateObject.camera.initialized === false &&
    stateObject.camera.center === null &&
    stateObject.camera.zoom === null &&
    stateObject.camera.bearing === 0 &&
    stateObject.camera.pitch === 0 &&
    tilesFieldPresent &&
    stateObject.tiles.enabled === false &&
    stateObject.tiles.loaded === false &&
    stateObject.tiles.requestedCount === 0 &&
    stateObject.tiles.loadedCount === 0 &&
    layersFieldPresent &&
    stateObject.layers.length === 0 &&
    styleFieldPresent &&
    stateObject.style.activeStyle === null &&
    stateObject.style.styleLoaded === false &&
    stateObject.style.palette === null &&
    metricsFieldPresent &&
    stateObject.metrics.frameCount === 0 &&
    stateObject.metrics.drawCount === 0 &&
    stateObject.metrics.lastFrameMs === 0 &&
    stateObject.metrics.lastDrawMs === 0 &&
    lastManualCheckAtFieldPresent &&
    stateObject.lastManualCheckAt === null
  );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const manualReportHelperAvailable =
    typeof getCustom25DVisualInertStateContainerManualConsoleTestReport === "function";
  const safariSafeCloseoutHelperAvailable =
    typeof getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 390,
      helperName: "getCustom25DVisualManualRendererInertStateFieldsReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      viewportFieldPresent: false,
      cameraFieldPresent: false,
      tilesFieldPresent: false,
      layersFieldPresent: false,
      styleFieldPresent: false,
      metricsFieldPresent: false,
      lastManualCheckAtFieldPresent: false,
      inertFieldDefaultsMatch: false,
      fieldsAddedToNewCreationPath: true,
      creationHelperAvailable,
      manualRunHelperAvailable,
      manualReportHelperAvailable,
      safariSafeCloseoutHelperAvailable,
      stateCreatedByReport: false,
      stateMutatedByReport: false,
      creationHelperInvokedByReport: false,
      runHelperInvokedByReport: false,
      rendererInstanceCreated: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 390,
    helperName: "getCustom25DVisualManualRendererInertStateFieldsReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    viewportFieldPresent,
    cameraFieldPresent,
    tilesFieldPresent,
    layersFieldPresent,
    styleFieldPresent,
    metricsFieldPresent,
    lastManualCheckAtFieldPresent,
    inertFieldDefaultsMatch,
    fieldsAddedToNewCreationPath: true,
    creationHelperAvailable,
    manualRunHelperAvailable,
    manualReportHelperAvailable,
    safariSafeCloseoutHelperAvailable,
    stateCreatedByReport: false,
    stateMutatedByReport: false,
    creationHelperInvokedByReport: false,
    runHelperInvokedByReport: false,
    rendererInstanceCreated: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererInertStateFieldsCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "closeoutOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const viewportFieldPresent =
    isObjectState &&
    !!stateObject.viewport &&
    typeof stateObject.viewport === "object";
  const cameraFieldPresent =
    isObjectState && !!stateObject.camera && typeof stateObject.camera === "object";
  const tilesFieldPresent =
    isObjectState && !!stateObject.tiles && typeof stateObject.tiles === "object";
  const layersFieldPresent = isObjectState && Array.isArray(stateObject.layers);
  const styleFieldPresent =
    isObjectState && !!stateObject.style && typeof stateObject.style === "object";
  const metricsFieldPresent =
    isObjectState && !!stateObject.metrics && typeof stateObject.metrics === "object";
  const lastManualCheckAtFieldPresent =
    isObjectState &&
    Object.prototype.hasOwnProperty.call(stateObject, "lastManualCheckAt");
  const inertFieldDefaultsMatch = !!(
    viewportFieldPresent &&
    stateObject.viewport.attached === false &&
    stateObject.viewport.width === 0 &&
    stateObject.viewport.height === 0 &&
    stateObject.viewport.pixelRatio === 1 &&
    cameraFieldPresent &&
    stateObject.camera.initialized === false &&
    stateObject.camera.center === null &&
    stateObject.camera.zoom === null &&
    stateObject.camera.bearing === 0 &&
    stateObject.camera.pitch === 0 &&
    tilesFieldPresent &&
    stateObject.tiles.enabled === false &&
    stateObject.tiles.loaded === false &&
    stateObject.tiles.requestedCount === 0 &&
    stateObject.tiles.loadedCount === 0 &&
    layersFieldPresent &&
    stateObject.layers.length === 0 &&
    styleFieldPresent &&
    stateObject.style.activeStyle === null &&
    stateObject.style.styleLoaded === false &&
    stateObject.style.palette === null &&
    metricsFieldPresent &&
    stateObject.metrics.frameCount === 0 &&
    stateObject.metrics.drawCount === 0 &&
    stateObject.metrics.lastFrameMs === 0 &&
    stateObject.metrics.lastDrawMs === 0 &&
    lastManualCheckAtFieldPresent &&
    stateObject.lastManualCheckAt === null
  );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const inertStateFieldsReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInertStateFieldsReport === "function";
  const safariSafeCloseoutHelperAvailable =
    typeof getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 391,
      helperName:
        "getCustom25DVisualManualRendererInertStateFieldsCloseoutReport",
      closeoutOnly: true,
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase390FieldsClosedOut: true,
      stateContainerExists,
      viewportFieldPresent: false,
      cameraFieldPresent: false,
      tilesFieldPresent: false,
      layersFieldPresent: false,
      styleFieldPresent: false,
      metricsFieldPresent: false,
      lastManualCheckAtFieldPresent: false,
      inertFieldDefaultsMatch: false,
      creationHelperAvailable,
      manualRunHelperAvailable,
      inertStateFieldsReportHelperAvailable,
      safariSafeCloseoutHelperAvailable,
      stateCreatedByCloseout: false,
      stateMutatedByCloseout: false,
      creationHelperInvokedByCloseout: false,
      runHelperInvokedByCloseout: false,
      rendererInstanceCreated: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 391,
    helperName: "getCustom25DVisualManualRendererInertStateFieldsCloseoutReport",
    closeoutOnly: true,
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase390FieldsClosedOut: true,
    stateContainerExists,
    viewportFieldPresent,
    cameraFieldPresent,
    tilesFieldPresent,
    layersFieldPresent,
    styleFieldPresent,
    metricsFieldPresent,
    lastManualCheckAtFieldPresent,
    inertFieldDefaultsMatch,
    creationHelperAvailable,
    manualRunHelperAvailable,
    inertStateFieldsReportHelperAvailable,
    safariSafeCloseoutHelperAvailable,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    creationHelperInvokedByCloseout: false,
    runHelperInvokedByCloseout: false,
    rendererInstanceCreated: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererLifecycleReadinessReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const plannedLifecycleStages = [
    "created",
    "initialized",
    "attached",
    "running",
    "stopped",
    "disposed"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const inertStateFieldsReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInertStateFieldsReport === "function";
  const inertStateFieldsCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInertStateFieldsCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 392,
      helperName: "getCustom25DVisualManualRendererLifecycleReadinessReport",
      planningOnly: true,
      reportOnly: true,
      passive: true,
      jsonSafeResult: true,
      lifecycleReadinessOnly: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      observedInitialized: null,
      observedRunning: null,
      observedRendererInstanceExists: false,
      observedMapAttached: null,
      observedDrawingEnabled: null,
      plannedLifecycleStages,
      plannedLifecycleStageCount: plannedLifecycleStages.length,
      lifecycleStagesAddedThisPhase: false,
      lifecycleMutatedThisPhase: false,
      futureLifecycleIsPlanningOnly: true,
      creationHelperAvailable,
      manualRunHelperAvailable,
      inertStateFieldsReportHelperAvailable,
      inertStateFieldsCloseoutHelperAvailable,
      stateCreatedByReadiness: false,
      stateMutatedByReadiness: false,
      creationHelperInvokedByReadiness: false,
      runHelperInvokedByReadiness: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 392,
    helperName: "getCustom25DVisualManualRendererLifecycleReadinessReport",
    planningOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    lifecycleReadinessOnly: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    observedInitialized:
      isObjectState && typeof stateObject.initialized === "boolean"
        ? stateObject.initialized
        : null,
    observedRunning:
      isObjectState && typeof stateObject.running === "boolean"
        ? stateObject.running
        : null,
    observedRendererInstanceExists:
      isObjectState && stateObject.rendererInstance !== null,
    observedMapAttached:
      isObjectState && typeof stateObject.mapAttached === "boolean"
        ? stateObject.mapAttached
        : null,
    observedDrawingEnabled:
      isObjectState && typeof stateObject.drawingEnabled === "boolean"
        ? stateObject.drawingEnabled
        : null,
    plannedLifecycleStages,
    plannedLifecycleStageCount: plannedLifecycleStages.length,
    lifecycleStagesAddedThisPhase: false,
    lifecycleMutatedThisPhase: false,
    futureLifecycleIsPlanningOnly: true,
    creationHelperAvailable,
    manualRunHelperAvailable,
    inertStateFieldsReportHelperAvailable,
    inertStateFieldsCloseoutHelperAvailable,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    creationHelperInvokedByReadiness: false,
    runHelperInvokedByReadiness: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoLifecycleFieldAddition",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const plannedLifecycleStateFields = [
    "lifecycle",
    "lifecycle.status",
    "lifecycle.flags",
    "lifecycle.timestamps"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const lifecycleFieldPresent =
    isObjectState &&
    Object.prototype.hasOwnProperty.call(stateObject, "lifecycle");
  const lifecycleStatusFieldPresent =
    lifecycleFieldPresent &&
    !!stateObject.lifecycle &&
    typeof stateObject.lifecycle === "object" &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "status");
  const lifecycleFlagsFieldPresent =
    lifecycleFieldPresent &&
    !!stateObject.lifecycle &&
    typeof stateObject.lifecycle === "object" &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "flags");
  const lifecycleTimestampsFieldPresent =
    lifecycleFieldPresent &&
    !!stateObject.lifecycle &&
    typeof stateObject.lifecycle === "object" &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "timestamps");
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const lifecycleReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererLifecycleReadinessReport === "function";
  const inertStateFieldsCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInertStateFieldsCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 393,
      helperName:
        "getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport",
      planningOnly: true,
      reportOnly: true,
      passive: true,
      jsonSafeResult: true,
      lifecycleStateFieldsReadinessOnly: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      lifecycleFieldPresent: false,
      lifecycleStatusFieldPresent: false,
      lifecycleFlagsFieldPresent: false,
      lifecycleTimestampsFieldPresent: false,
      plannedLifecycleStateFields,
      plannedLifecycleStateFieldCount: plannedLifecycleStateFields.length,
      fieldsAddedThisPhase: false,
      fieldsMutatedThisPhase: false,
      lifecycleFieldsAddedThisPhase: false,
      futureLifecycleFieldsArePlanningOnly: true,
      creationHelperAvailable,
      manualRunHelperAvailable,
      lifecycleReadinessHelperAvailable,
      inertStateFieldsCloseoutHelperAvailable,
      stateCreatedByReadiness: false,
      stateMutatedByReadiness: false,
      creationHelperInvokedByReadiness: false,
      runHelperInvokedByReadiness: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 393,
    helperName:
      "getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport",
    planningOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    lifecycleStateFieldsReadinessOnly: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    lifecycleFieldPresent,
    lifecycleStatusFieldPresent,
    lifecycleFlagsFieldPresent,
    lifecycleTimestampsFieldPresent,
    plannedLifecycleStateFields,
    plannedLifecycleStateFieldCount: plannedLifecycleStateFields.length,
    fieldsAddedThisPhase: false,
    fieldsMutatedThisPhase: false,
    lifecycleFieldsAddedThisPhase: false,
    futureLifecycleFieldsArePlanningOnly: true,
    creationHelperAvailable,
    manualRunHelperAvailable,
    lifecycleReadinessHelperAvailable,
    inertStateFieldsCloseoutHelperAvailable,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    creationHelperInvokedByReadiness: false,
    runHelperInvokedByReadiness: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererInertLifecycleStateFieldsReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const lifecycleFieldPresent =
    isObjectState &&
    !!stateObject.lifecycle &&
    typeof stateObject.lifecycle === "object";
  const lifecycleStatusFieldPresent =
    lifecycleFieldPresent &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "status");
  const lifecycleFlagsFieldPresent =
    lifecycleFieldPresent &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "flags");
  const lifecycleTimestampsFieldPresent =
    lifecycleFieldPresent &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "timestamps");
  const inertLifecycleDefaultsMatch = !!(
    lifecycleFieldPresent &&
    lifecycleStatusFieldPresent &&
    lifecycleFlagsFieldPresent &&
    lifecycleTimestampsFieldPresent &&
    stateObject.lifecycle.status === "created" &&
    stateObject.lifecycle.flags &&
    stateObject.lifecycle.flags.created === true &&
    stateObject.lifecycle.flags.initialized === false &&
    stateObject.lifecycle.flags.attached === false &&
    stateObject.lifecycle.flags.running === false &&
    stateObject.lifecycle.flags.stopped === false &&
    stateObject.lifecycle.flags.disposed === false &&
    stateObject.lifecycle.timestamps &&
    stateObject.lifecycle.timestamps.createdAt === null &&
    stateObject.lifecycle.timestamps.initializedAt === null &&
    stateObject.lifecycle.timestamps.attachedAt === null &&
    stateObject.lifecycle.timestamps.startedAt === null &&
    stateObject.lifecycle.timestamps.stoppedAt === null &&
    stateObject.lifecycle.timestamps.disposedAt === null
  );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 394,
      helperName:
        "getCustom25DVisualManualRendererInertLifecycleStateFieldsReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      lifecycleFieldPresent: false,
      lifecycleStatusFieldPresent: false,
      lifecycleFlagsFieldPresent: false,
      lifecycleTimestampsFieldPresent: false,
      lifecycleStatus: null,
      lifecycleCreatedFlag: false,
      lifecycleInitializedFlag: false,
      lifecycleAttachedFlag: false,
      lifecycleRunningFlag: false,
      lifecycleStoppedFlag: false,
      lifecycleDisposedFlag: false,
      lifecycleCreatedAt: null,
      lifecycleInitializedAt: null,
      lifecycleAttachedAt: null,
      lifecycleStartedAt: null,
      lifecycleStoppedAt: null,
      lifecycleDisposedAt: null,
      inertLifecycleDefaultsMatch: false,
      lifecycleFieldsAddedToNewCreationPath: true,
      stateCreatedByReport: false,
      stateMutatedByReport: false,
      creationHelperInvokedByReport: false,
      runHelperInvokedByReport: false,
      creationHelperAvailable,
      manualRunHelperAvailable,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 394,
    helperName:
      "getCustom25DVisualManualRendererInertLifecycleStateFieldsReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    lifecycleFieldPresent,
    lifecycleStatusFieldPresent,
    lifecycleFlagsFieldPresent,
    lifecycleTimestampsFieldPresent,
    lifecycleStatus:
      lifecycleStatusFieldPresent && typeof stateObject.lifecycle.status === "string"
        ? stateObject.lifecycle.status
        : null,
    lifecycleCreatedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.created === true : false,
    lifecycleInitializedFlag:
      lifecycleFlagsFieldPresent
        ? stateObject.lifecycle.flags.initialized === true
        : false,
    lifecycleAttachedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.attached === true : false,
    lifecycleRunningFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.running === true : false,
    lifecycleStoppedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.stopped === true : false,
    lifecycleDisposedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.disposed === true : false,
    lifecycleCreatedAt:
      lifecycleTimestampsFieldPresent ? stateObject.lifecycle.timestamps.createdAt : null,
    lifecycleInitializedAt:
      lifecycleTimestampsFieldPresent
        ? stateObject.lifecycle.timestamps.initializedAt
        : null,
    lifecycleAttachedAt:
      lifecycleTimestampsFieldPresent ? stateObject.lifecycle.timestamps.attachedAt : null,
    lifecycleStartedAt:
      lifecycleTimestampsFieldPresent ? stateObject.lifecycle.timestamps.startedAt : null,
    lifecycleStoppedAt:
      lifecycleTimestampsFieldPresent ? stateObject.lifecycle.timestamps.stoppedAt : null,
    lifecycleDisposedAt:
      lifecycleTimestampsFieldPresent ? stateObject.lifecycle.timestamps.disposedAt : null,
    inertLifecycleDefaultsMatch,
    lifecycleFieldsAddedToNewCreationPath: true,
    stateCreatedByReport: false,
    stateMutatedByReport: false,
    creationHelperInvokedByReport: false,
    runHelperInvokedByReport: false,
    creationHelperAvailable,
    manualRunHelperAvailable,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "closeoutOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const lifecycleFieldPresent =
    isObjectState &&
    !!stateObject.lifecycle &&
    typeof stateObject.lifecycle === "object";
  const lifecycleStatusFieldPresent =
    lifecycleFieldPresent &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "status");
  const lifecycleFlagsFieldPresent =
    lifecycleFieldPresent &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "flags");
  const lifecycleTimestampsFieldPresent =
    lifecycleFieldPresent &&
    Object.prototype.hasOwnProperty.call(stateObject.lifecycle, "timestamps");
  const inertLifecycleDefaultsMatch = !!(
    lifecycleFieldPresent &&
    lifecycleStatusFieldPresent &&
    lifecycleFlagsFieldPresent &&
    lifecycleTimestampsFieldPresent &&
    stateObject.lifecycle.status === "created" &&
    stateObject.lifecycle.flags &&
    stateObject.lifecycle.flags.created === true &&
    stateObject.lifecycle.flags.initialized === false &&
    stateObject.lifecycle.flags.attached === false &&
    stateObject.lifecycle.flags.running === false &&
    stateObject.lifecycle.flags.stopped === false &&
    stateObject.lifecycle.flags.disposed === false &&
    stateObject.lifecycle.timestamps &&
    stateObject.lifecycle.timestamps.createdAt === null &&
    stateObject.lifecycle.timestamps.initializedAt === null &&
    stateObject.lifecycle.timestamps.attachedAt === null &&
    stateObject.lifecycle.timestamps.startedAt === null &&
    stateObject.lifecycle.timestamps.stoppedAt === null &&
    stateObject.lifecycle.timestamps.disposedAt === null
  );
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const inertLifecycleStateFieldsReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsReport ===
    "function";
  const lifecycleStateFieldsReadinessHelperAvailable =
    typeof getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 395,
      helperName:
        "getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport",
      closeoutOnly: true,
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      phase394LifecycleFieldsClosedOut: true,
      stateContainerExists,
      lifecycleFieldPresent: false,
      lifecycleStatusFieldPresent: false,
      lifecycleFlagsFieldPresent: false,
      lifecycleTimestampsFieldPresent: false,
      lifecycleStatus: null,
      lifecycleCreatedFlag: false,
      lifecycleInitializedFlag: false,
      lifecycleAttachedFlag: false,
      lifecycleRunningFlag: false,
      lifecycleStoppedFlag: false,
      lifecycleDisposedFlag: false,
      inertLifecycleDefaultsMatch: false,
      creationHelperAvailable,
      manualRunHelperAvailable,
      inertLifecycleStateFieldsReportHelperAvailable,
      lifecycleStateFieldsReadinessHelperAvailable,
      stateCreatedByCloseout: false,
      stateMutatedByCloseout: false,
      creationHelperInvokedByCloseout: false,
      runHelperInvokedByCloseout: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 395,
    helperName:
      "getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport",
    closeoutOnly: true,
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    phase394LifecycleFieldsClosedOut: true,
    stateContainerExists,
    lifecycleFieldPresent,
    lifecycleStatusFieldPresent,
    lifecycleFlagsFieldPresent,
    lifecycleTimestampsFieldPresent,
    lifecycleStatus:
      lifecycleStatusFieldPresent && typeof stateObject.lifecycle.status === "string"
        ? stateObject.lifecycle.status
        : null,
    lifecycleCreatedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.created === true : false,
    lifecycleInitializedFlag:
      lifecycleFlagsFieldPresent
        ? stateObject.lifecycle.flags.initialized === true
        : false,
    lifecycleAttachedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.attached === true : false,
    lifecycleRunningFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.running === true : false,
    lifecycleStoppedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.stopped === true : false,
    lifecycleDisposedFlag:
      lifecycleFlagsFieldPresent ? stateObject.lifecycle.flags.disposed === true : false,
    inertLifecycleDefaultsMatch,
    creationHelperAvailable,
    manualRunHelperAvailable,
    inertLifecycleStateFieldsReportHelperAvailable,
    lifecycleStateFieldsReadinessHelperAvailable,
    stateCreatedByCloseout: false,
    stateMutatedByCloseout: false,
    creationHelperInvokedByCloseout: false,
    runHelperInvokedByCloseout: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererInitializationReadinessReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "planningOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoInitializationShellCreation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const requiredFutureInitializationGuards = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererInitialization",
    "confirmRendererShellOnly",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateObject =
    windowExists && typeof window.custom25DVisualManualRendererState !== "undefined"
      ? window.custom25DVisualManualRendererState
      : undefined;
  const stateContainerExists = typeof stateObject !== "undefined";
  const isObjectState = !!stateObject && typeof stateObject === "object";
  const creationHelperAvailable =
    typeof createCustom25DVisualInertManualRendererStateContainer === "function";
  const manualRunHelperAvailable =
    typeof runCustom25DVisualInertStateContainerManualConsoleTest === "function";
  const inertLifecycleStateFieldsReportHelperAvailable =
    typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsReport ===
    "function";
  const inertLifecycleStateFieldsCloseoutHelperAvailable =
    typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport ===
    "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 396,
      helperName: "getCustom25DVisualManualRendererInitializationReadinessReport",
      planningOnly: true,
      reportOnly: true,
      passive: true,
      jsonSafeResult: true,
      initializationReadinessOnly: true,
      blocked: true,
      allowed: false,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      stateContainerExists,
      observedLifecycleStatus: null,
      observedInitialized: null,
      observedRendererInstanceExists: false,
      observedMapAttached: null,
      observedDrawingEnabled: null,
      plannedInitializationShellName:
        "initializeCustom25DVisualManualRendererShell",
      initializationShellAddedThisPhase: false,
      initializationPerformedThisPhase: false,
      futureInitializationIsPlanningOnly: true,
      requiredFutureInitializationGuards,
      requiredFutureInitializationGuardCount:
        requiredFutureInitializationGuards.length,
      creationHelperAvailable,
      manualRunHelperAvailable,
      inertLifecycleStateFieldsReportHelperAvailable,
      inertLifecycleStateFieldsCloseoutHelperAvailable,
      stateCreatedByReadiness: false,
      stateMutatedByReadiness: false,
      creationHelperInvokedByReadiness: false,
      runHelperInvokedByReadiness: false,
      initializationShellCreated: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 396,
    helperName: "getCustom25DVisualManualRendererInitializationReadinessReport",
    planningOnly: true,
    reportOnly: true,
    passive: true,
    jsonSafeResult: true,
    initializationReadinessOnly: true,
    blocked: false,
    allowed: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    stateContainerExists,
    observedLifecycleStatus:
      isObjectState &&
      stateObject.lifecycle &&
      typeof stateObject.lifecycle.status === "string"
        ? stateObject.lifecycle.status
        : null,
    observedInitialized:
      isObjectState && typeof stateObject.initialized === "boolean"
        ? stateObject.initialized
        : null,
    observedRendererInstanceExists:
      isObjectState && stateObject.rendererInstance !== null,
    observedMapAttached:
      isObjectState && typeof stateObject.mapAttached === "boolean"
        ? stateObject.mapAttached
        : null,
    observedDrawingEnabled:
      isObjectState && typeof stateObject.drawingEnabled === "boolean"
        ? stateObject.drawingEnabled
        : null,
    plannedInitializationShellName:
      "initializeCustom25DVisualManualRendererShell",
    initializationShellAddedThisPhase: false,
    initializationPerformedThisPhase: false,
    futureInitializationIsPlanningOnly: true,
    requiredFutureInitializationGuards,
    requiredFutureInitializationGuardCount:
      requiredFutureInitializationGuards.length,
    creationHelperAvailable,
    manualRunHelperAvailable,
    inertLifecycleStateFieldsReportHelperAvailable,
    inertLifecycleStateFieldsCloseoutHelperAvailable,
    stateCreatedByReadiness: false,
    stateMutatedByReadiness: false,
    creationHelperInvokedByReadiness: false,
    runHelperInvokedByReadiness: false,
    initializationShellCreated: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function initializeCustom25DVisualManualRendererShell(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererInitialization",
    "confirmRendererShellOnly",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );

  if (!windowExists) {
    return {
      ok: false,
      phase: 397,
      helperName: "initializeCustom25DVisualManualRendererShell",
      shellOnly: true,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      reason: "window-unavailable",
      initializationShellCreated: false,
      initializationPerformed: false,
      stateContainerExists: false,
      stateMutatedByShell: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  if (!isLocalDevHost) {
    return {
      ok: false,
      phase: 397,
      helperName: "initializeCustom25DVisualManualRendererShell",
      shellOnly: true,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      reason: "local-dev-host-required",
      initializationShellCreated: false,
      initializationPerformed: false,
      stateContainerExists,
      stateMutatedByShell: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 397,
      helperName: "initializeCustom25DVisualManualRendererShell",
      shellOnly: true,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      initializationShellCreated: false,
      initializationPerformed: false,
      stateContainerExists,
      stateMutatedByShell: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 397,
    helperName: "initializeCustom25DVisualManualRendererShell",
    shellOnly: true,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationShellCreated: true,
    initializationPerformed: false,
    stateContainerExists,
    stateMutatedByShell: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualManualRendererInitializationShellReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "reportOnly",
    "confirmNoStateCreation",
    "confirmNoStateMutation",
    "confirmNoInitializationInvocation",
    "confirmNoRendererInstanceCreation",
    "confirmNoRendererInitialization",
    "confirmNoRendererStart",
    "confirmNoMapAttachment",
    "confirmNoDrawing",
    "confirmNoStartupWiring",
    "confirmNoGameplayChange",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingRequiredOptionKeys = requiredOptionKeys.filter(
    (key) => options[key] !== true
  );
  const windowExists = typeof window !== "undefined" && window;
  const stateContainerExists =
    !!(
      windowExists &&
      typeof window.custom25DVisualManualRendererState !== "undefined"
    );
  const initializationShellHelperAvailable =
    typeof initializeCustom25DVisualManualRendererShell === "function";

  if (missingRequiredOptionKeys.length > 0) {
    return {
      ok: false,
      phase: 397,
      helperName: "getCustom25DVisualManualRendererInitializationShellReport",
      reportOnly: true,
      passive: true,
      blocked: true,
      allowed: false,
      jsonSafeResult: true,
      requiredOptionKeys,
      missingRequiredOptionKeys,
      initializationShellHelperAvailable,
      initializationShellAddedThisPhase: true,
      initializationPerformedByReport: false,
      stateContainerExists,
      stateCreatedByReport: false,
      stateMutatedByReport: false,
      rendererInstanceCreated: false,
      rendererInitialized: false,
      rendererStarted: false,
      mapAttached: false,
      drawingStarted: false,
      startupWired: false,
      gameplayChanged: false,
      backendStorageNetworkChanged: false
    };
  }

  return {
    ok: true,
    phase: 397,
    helperName: "getCustom25DVisualManualRendererInitializationShellReport",
    reportOnly: true,
    passive: true,
    blocked: false,
    allowed: true,
    jsonSafeResult: true,
    requiredOptionKeys,
    missingRequiredOptionKeys: [],
    initializationShellHelperAvailable,
    initializationShellAddedThisPhase: true,
    initializationPerformedByReport: false,
    stateContainerExists,
    stateCreatedByReport: false,
    stateMutatedByReport: false,
    rendererInstanceCreated: false,
    rendererInitialized: false,
    rendererStarted: false,
    mapAttached: false,
    drawingStarted: false,
    startupWired: false,
    gameplayChanged: false,
    backendStorageNetworkChanged: false
  };
}

function getCustom25DVisualExtractedHelperReconciliationReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const expectedByPhase = {
    phase359: [
      "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
      "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport"
    ],
    phase361: [
      "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
      "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport"
    ],
    phase364: [
      "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
      "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport"
    ],
    phase365: [
      "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
      "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
      "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport"
    ]
  };
  const dedupedExpectedExtractedHelpers = Array.from(
    new Set([
      ...expectedByPhase.phase359,
      ...expectedByPhase.phase361,
      ...expectedByPhase.phase364,
      ...expectedByPhase.phase365
    ])
  );
  const availableExtractedHelpers = dedupedExpectedExtractedHelpers.filter(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const missingExtractedHelpers = dedupedExpectedExtractedHelpers.filter(
    (name) => !(typeof globalThis !== "undefined" && typeof globalThis[name] === "function")
  );
  const overlappingPhaseBuckets = [];
  const duplicateRiskHelpers = [];
  const phaseEntries = Object.entries(expectedByPhase);
  for (let index = 0; index < phaseEntries.length; index += 1) {
    const [phaseName, helpers] = phaseEntries[index];
    for (let compareIndex = index + 1; compareIndex < phaseEntries.length; compareIndex += 1) {
      const [comparePhaseName, compareHelpers] = phaseEntries[compareIndex];
      const overlap = helpers.filter((name) => compareHelpers.includes(name));
      if (overlap.length > 0) {
        overlappingPhaseBuckets.push({
          phases: [phaseName, comparePhaseName],
          helpers: overlap
        });
        overlap.forEach((name) => {
          if (!duplicateRiskHelpers.includes(name)) {
            duplicateRiskHelpers.push(name);
          }
        });
      }
    }
  }
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const manualNamespaceExposureExpected =
    !!manualNamespace &&
    dedupedExpectedExtractedHelpers.every(
      (name) => typeof manualNamespace[name] === "function"
    );
  const helperPresenceRegistryExpected = manualNamespaceExposureExpected;
  const currentSourceOfTruthConsistent = !!(
    passiveReportsNamespace &&
    passiveReportsNamespace.phase358LoadPathAvailable === true &&
    missingExtractedHelpers.length === 0
  );
  const blockers = [];
  if (!passiveReportsNamespace) {
    blockers.push("passive-reports-namespace-unavailable");
  }
  if (
    !passiveReportsNamespace ||
    passiveReportsNamespace.phase358LoadPathAvailable !== true
  ) {
    blockers.push("phase358-load-path-marker-unavailable");
  }
  if (missingExtractedHelpers.length > 0) {
    blockers.push("expected-extracted-helpers-missing");
  }
  if (!manualNamespaceExposureExpected) {
    blockers.push("manual-namespace-extracted-helper-exposure-unavailable");
  }
  if (!helperPresenceRegistryExpected) {
    blockers.push("helper-presence-registry-expectations-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 367,
      helperName: "getCustom25DVisualExtractedHelperReconciliationReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveReportsNamespaceAvailable: !!passiveReportsNamespace,
      phase358LoadPathAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      expectedByPhase,
      overlappingPhaseBuckets,
      dedupedExpectedExtractedHelpers,
      availableExtractedHelpers,
      missingExtractedHelpers,
      duplicateRiskHelpers,
      currentSourceOfTruthConsistent,
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      immediateBroadExtractionBlocked: true,
      nextRecommendedStep:
        "resolve-extracted-helper-overlap-and-missing-helper-uncertainty-before-another-bundled-move",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 367,
    helperName: "getCustom25DVisualExtractedHelperReconciliationReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveReportsNamespaceAvailable: !!passiveReportsNamespace,
    phase358LoadPathAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    expectedByPhase,
    overlappingPhaseBuckets,
    dedupedExpectedExtractedHelpers,
    availableExtractedHelpers,
    missingExtractedHelpers,
    duplicateRiskHelpers,
    currentSourceOfTruthConsistent,
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    immediateBroadExtractionBlocked: true,
    nextRecommendedStep:
      "resolve-extracted-helper-overlap-and-missing-helper-uncertainty-before-another-bundled-move",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualExtractedHelperReconciliationReadinessReport(
  options = {}
) {
  const reconciliation =
    typeof getCustom25DVisualExtractedHelperReconciliationReport === "function"
      ? getCustom25DVisualExtractedHelperReconciliationReport(options)
      : null;
  const requiredOptionKeys =
    reconciliation && Array.isArray(reconciliation.requiredOptionKeys)
      ? reconciliation.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    reconciliation && reconciliation.blockedBehavior
      ? reconciliation.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    reconciliation && reconciliation.preservedSystems
      ? reconciliation.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    reconciliation && reconciliation.safetyFlags
      ? reconciliation.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = reconciliation && Array.isArray(reconciliation.blockers)
    ? [...reconciliation.blockers]
    : [];
  if (!reconciliation) {
    blockers.push("phase367-reconciliation-report-unavailable");
  }
  const readyForAnotherBundledMove = !!(
    reconciliation &&
    reconciliation.currentSourceOfTruthConsistent === true &&
    Array.isArray(reconciliation.missingExtractedHelpers) &&
    reconciliation.missingExtractedHelpers.length === 0 &&
    Array.isArray(reconciliation.duplicateRiskHelpers) &&
    reconciliation.duplicateRiskHelpers.length === 0
  );

  if (missingKey) {
    return {
      ok: true,
      phase: 367,
      helperName: "getCustom25DVisualExtractedHelperReconciliationReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForAnotherBundledPassiveMove: readyForAnotherBundledMove,
      readyForBroadExtraction: false,
      recommendAnotherReconciliationPhase:
        !!(
          reconciliation &&
          (reconciliation.missingExtractedHelpers.length > 0 ||
            reconciliation.duplicateRiskHelpers.length > 0)
        ),
      runtimeExtractionBlocked: true,
      startupExtractionBlocked: true,
      mapExtractionBlocked: true,
      gameplayExtractionBlocked: true,
      sharedStateExtractionBlocked: true,
      drawingExtractionBlocked: true,
      domExtractionBlocked: true,
      backendStorageNetworkExtractionBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 367,
    helperName: "getCustom25DVisualExtractedHelperReconciliationReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForAnotherBundledPassiveMove: readyForAnotherBundledMove,
    readyForBroadExtraction: false,
    recommendAnotherReconciliationPhase:
      !!(
        reconciliation &&
        (reconciliation.missingExtractedHelpers.length > 0 ||
          reconciliation.duplicateRiskHelpers.length > 0)
      ),
    runtimeExtractionBlocked: true,
    startupExtractionBlocked: true,
    mapExtractionBlocked: true,
    gameplayExtractionBlocked: true,
    sharedStateExtractionBlocked: true,
    drawingExtractionBlocked: true,
    domExtractionBlocked: true,
    backendStorageNetworkExtractionBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualExtractedHelperReconciliationCloseoutReport(
  options = {}
) {
  const reconciliation =
    typeof getCustom25DVisualExtractedHelperReconciliationReport === "function"
      ? getCustom25DVisualExtractedHelperReconciliationReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualExtractedHelperReconciliationReadinessReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys)
      ? readiness.requiredOptionKeys
      : null) ||
    (reconciliation && Array.isArray(reconciliation.requiredOptionKeys)
      ? reconciliation.requiredOptionKeys
      : null) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) ||
    (reconciliation && reconciliation.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) ||
    (reconciliation && reconciliation.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) ||
    (reconciliation && reconciliation.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!reconciliation) {
    blockers.push("phase367-reconciliation-report-unavailable");
  }
  if (!readiness) {
    blockers.push("phase367-readiness-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 367,
      helperName: "getCustom25DVisualExtractedHelperReconciliationCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      reconciliationReportingOnly: true,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      nextMoveRequiresNoMissingHelpersAndNoUnsafeDuplicateRisk: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 367,
    helperName: "getCustom25DVisualExtractedHelperReconciliationCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    reconciliationReportingOnly: true,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    nextMoveRequiresNoMissingHelpersAndNoUnsafeDuplicateRisk: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualExtractedHelperReconciliationAcceptanceReport(
  options = {}
) {
  const reconciliation =
    typeof getCustom25DVisualExtractedHelperReconciliationReport === "function"
      ? getCustom25DVisualExtractedHelperReconciliationReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualExtractedHelperReconciliationReadinessReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys)
      ? readiness.requiredOptionKeys
      : null) ||
    (reconciliation && Array.isArray(reconciliation.requiredOptionKeys)
      ? reconciliation.requiredOptionKeys
      : null) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) ||
    (reconciliation && reconciliation.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) ||
    (reconciliation && reconciliation.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) ||
    (reconciliation && reconciliation.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const acceptedDedupedExtractedHelpers =
    reconciliation && Array.isArray(reconciliation.dedupedExpectedExtractedHelpers)
      ? reconciliation.dedupedExpectedExtractedHelpers
      : [];
  const historicalOverlapHelpers =
    reconciliation && Array.isArray(reconciliation.duplicateRiskHelpers)
      ? reconciliation.duplicateRiskHelpers
      : [];
  const missingExtractedHelpers =
    reconciliation && Array.isArray(reconciliation.missingExtractedHelpers)
      ? reconciliation.missingExtractedHelpers
      : [];
  const overlapAcceptedAsBookkeepingOnly = !!(
    reconciliation &&
    missingExtractedHelpers.length === 0 &&
    historicalOverlapHelpers.length > 0
  );
  const activeDuplicateCodeBlockers =
    missingExtractedHelpers.length > 0 ? [...historicalOverlapHelpers] : [];
  const sourceOfTruthAccepted = !!(
    reconciliation &&
    missingExtractedHelpers.length === 0 &&
    activeDuplicateCodeBlockers.length === 0
  );
  const readyForNextBundledPassiveMove = !!(
    sourceOfTruthAccepted &&
    readiness &&
    readiness.readyForBroadExtraction === false
  );
  const blockers = [];
  if (!reconciliation) {
    blockers.push("phase367-reconciliation-report-unavailable");
  }
  if (!readiness) {
    blockers.push("phase367-readiness-report-unavailable");
  }
  if (missingExtractedHelpers.length > 0) {
    blockers.push("missing-extracted-helpers-remain");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 368,
      helperName:
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      phase367ReconciliationAvailable: !!reconciliation,
      phase367ReadinessAvailable: !!readiness,
      acceptedDedupedExtractedHelpers,
      historicalOverlapHelpers,
      overlapAcceptedAsBookkeepingOnly,
      missingExtractedHelpers,
      activeDuplicateCodeBlockers,
      sourceOfTruthAccepted,
      readyForNextBundledPassiveMove,
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      immediateBroadExtractionBlocked: true,
      nextRecommendedStep:
        "if-acceptance-is-clean-return-to-one-bundled-passive-move-of-at-most-three-helpers",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 368,
    helperName: "getCustom25DVisualExtractedHelperReconciliationAcceptanceReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    phase367ReconciliationAvailable: !!reconciliation,
    phase367ReadinessAvailable: !!readiness,
    acceptedDedupedExtractedHelpers,
    historicalOverlapHelpers,
    overlapAcceptedAsBookkeepingOnly,
    missingExtractedHelpers,
    activeDuplicateCodeBlockers,
    sourceOfTruthAccepted,
    readyForNextBundledPassiveMove,
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    immediateBroadExtractionBlocked: true,
    nextRecommendedStep:
      "if-acceptance-is-clean-return-to-one-bundled-passive-move-of-at-most-three-helpers",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport(
  options = {}
) {
  const acceptance =
    typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReport(options)
      : null;
  const requiredOptionKeys =
    acceptance && Array.isArray(acceptance.requiredOptionKeys)
      ? acceptance.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    acceptance && acceptance.blockedBehavior
      ? acceptance.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    acceptance && acceptance.preservedSystems
      ? acceptance.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    acceptance && acceptance.safetyFlags
      ? acceptance.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = acceptance && Array.isArray(acceptance.blockers)
    ? [...acceptance.blockers]
    : [];
  if (!acceptance) {
    blockers.push("phase368-acceptance-report-unavailable");
  }
  const readyForOneLaterBundledMove = !!(
    acceptance &&
    acceptance.sourceOfTruthAccepted === true &&
    Array.isArray(acceptance.missingExtractedHelpers) &&
    acceptance.missingExtractedHelpers.length === 0 &&
    Array.isArray(acceptance.activeDuplicateCodeBlockers) &&
    acceptance.activeDuplicateCodeBlockers.length === 0
  );

  if (missingKey) {
    return {
      ok: true,
      phase: 368,
      helperName:
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForOneLaterBundledPassiveMove: readyForOneLaterBundledMove,
      readyForBroadExtraction: false,
      runtimeExtractionBlocked: true,
      startupExtractionBlocked: true,
      mapExtractionBlocked: true,
      gameplayExtractionBlocked: true,
      sharedStateExtractionBlocked: true,
      drawingExtractionBlocked: true,
      domExtractionBlocked: true,
      backendStorageNetworkExtractionBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 368,
    helperName:
      "getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForOneLaterBundledPassiveMove: readyForOneLaterBundledMove,
    readyForBroadExtraction: false,
    runtimeExtractionBlocked: true,
    startupExtractionBlocked: true,
    mapExtractionBlocked: true,
    gameplayExtractionBlocked: true,
    sharedStateExtractionBlocked: true,
    drawingExtractionBlocked: true,
    domExtractionBlocked: true,
    backendStorageNetworkExtractionBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport(
  options = {}
) {
  const acceptance =
    typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport(
          options
        )
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys)
      ? readiness.requiredOptionKeys
      : null) ||
    (acceptance && Array.isArray(acceptance.requiredOptionKeys)
      ? acceptance.requiredOptionKeys
      : null) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) ||
    (acceptance && acceptance.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) ||
    (acceptance && acceptance.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) ||
    (acceptance && acceptance.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!acceptance) {
    blockers.push("phase368-acceptance-report-unavailable");
  }
  if (!readiness) {
    blockers.push("phase368-acceptance-readiness-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 368,
      helperName:
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      acceptanceReportingOnly: true,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      nextMoveAllowedOnlyWhenAcceptanceReadinessClean: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 368,
    helperName:
      "getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    acceptanceReportingOnly: true,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    nextMoveAllowedOnlyWhenAcceptanceReadinessClean: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport(
  options = {}
) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const previousMovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
    "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport",
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
    "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
    "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport"
  ];
  const movedHelpers = [
    "getCustom25DVisualExtractedPassiveReportsFileHealthReport",
    "getCustom25DVisualExtractedPassiveReportsFileReadinessReport",
    "getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport"
  ];
  const previouslyMovedHelpersRemainPresent = Array.isArray(previousMovedHelpers)
    ? previousMovedHelpers.every(
        (name) =>
          typeof globalThis !== "undefined" &&
          typeof globalThis[name] === "function"
      )
    : false;
  const movedHelpersAvailableOnWindow = movedHelpers.every(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const movedHelpersAvailableInManualNamespace =
    !!manualNamespace &&
    movedHelpers.every((name) => typeof manualNamespace[name] === "function");
  const helperPresenceRegistryPreserved =
    !!manualNamespace &&
    [...previousMovedHelpers, ...movedHelpers].every(
      (name) => typeof manualNamespace[name] === "function"
    );
  const blockers = [];
  if (!passiveReportsNamespace || passiveReportsNamespace.phase358LoadPathAvailable !== true) {
    blockers.push("phase358-load-marker-unavailable");
  }
  if (!previouslyMovedHelpersRemainPresent) {
    blockers.push("previously-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableOnWindow) {
    blockers.push("phase369-moved-helpers-unavailable-on-window");
  }
  if (!movedHelpersAvailableInManualNamespace) {
    blockers.push("phase369-moved-helpers-unavailable-in-manual-namespace");
  }
  if (!helperPresenceRegistryPreserved) {
    blockers.push("helper-presence-registry-unavailable-for-moved-helpers");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 369,
      helperName:
        "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers,
      indexHtmlUntouched: true,
      filesCreated: false,
      phase358LoadMarkerAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      previouslyMovedHelpersRemainPresent,
      movedHelpersAvailableOnWindow,
      movedHelpersAvailableInManualNamespace,
      helperPresenceRegistryPreserved,
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayChanged: false,
      mapChanged: false,
      drawingChanged: false,
      domChanged: false,
      leafletChanged: false,
      sharedStateChanged: false,
      backendStorageNetworkChanged: false,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 369,
    helperName:
      "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers,
    indexHtmlUntouched: true,
    filesCreated: false,
    phase358LoadMarkerAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    previouslyMovedHelpersRemainPresent,
    movedHelpersAvailableOnWindow,
    movedHelpersAvailableInManualNamespace,
    helperPresenceRegistryPreserved,
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayChanged: false,
    mapChanged: false,
    drawingChanged: false,
    domChanged: false,
    leafletChanged: false,
    sharedStateChanged: false,
    backendStorageNetworkChanged: false,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport(
  options = {}
) {
  const closeout =
    typeof getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport ===
    "function"
      ? getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport(
          options
        )
      : null;
  const requiredOptionKeys =
    closeout && Array.isArray(closeout.requiredOptionKeys)
      ? closeout.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeout && closeout.blockedBehavior
      ? closeout.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeout && closeout.preservedSystems
      ? closeout.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeout && closeout.safetyFlags
      ? closeout.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (!closeout) {
    blockers.push("phase369-closeout-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 369,
      helperName:
        "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      selfReviewOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      movedHelpers: closeout ? closeout.movedHelpers : [],
      importsExportsAdded: false,
      buildToolingChanged: false,
      runtimeBehaviorChanged: false,
      startupBehaviorChanged: false,
      gameplayBehaviorChanged: false,
      mapBehaviorChanged: false,
      drawingBehaviorChanged: false,
      domBehaviorChanged: false,
      leafletBehaviorChanged: false,
      sharedStateBehaviorChanged: false,
      backendStorageNetworkBehaviorChanged: false,
      safetyFlagsRemainFalse:
        safetyFlags.custom25DMap === true &&
        safetyFlags.landmarkTestMarkers === true &&
        safetyFlags.landmarkSampleData === true &&
        safetyFlags.dinosaurSitesAuData === true,
      rollbackNote:
        "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 369,
    helperName:
      "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    selfReviewOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    movedHelpers: closeout ? closeout.movedHelpers : [],
    importsExportsAdded: false,
    buildToolingChanged: false,
    runtimeBehaviorChanged: false,
    startupBehaviorChanged: false,
    gameplayBehaviorChanged: false,
    mapBehaviorChanged: false,
    drawingBehaviorChanged: false,
    domBehaviorChanged: false,
    leafletBehaviorChanged: false,
    sharedStateBehaviorChanged: false,
    backendStorageNetworkBehaviorChanged: false,
    safetyFlagsRemainFalse:
      safetyFlags.custom25DMap === true &&
      safetyFlags.landmarkTestMarkers === true &&
      safetyFlags.landmarkSampleData === true &&
      safetyFlags.dinosaurSitesAuData === true,
    rollbackNote:
      "move the extracted helper declarations back into script.js and remove their window/globalThis assignments from custom25d-visual-passive-reports.js",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPostReconciliationMoveHealthReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const manualNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualManualTests &&
    typeof window.GrowGoCustom25DVisualManualTests === "object"
      ? window.GrowGoCustom25DVisualManualTests
      : null;
  const acceptance =
    typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
    "function"
      ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReport(options)
      : null;
  const phase359MovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
    "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport"
  ];
  const phase361MovedHelpers = [
    "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
    "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport"
  ];
  const acceptedDedupedSet =
    acceptance && Array.isArray(acceptance.acceptedDedupedExtractedHelpers)
      ? acceptance.acceptedDedupedExtractedHelpers
      : [
          "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
          "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport",
          "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
          "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport",
          "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
          "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
          "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport"
        ];
  const phase369MovedHelpers = [
    "getCustom25DVisualExtractedPassiveReportsFileHealthReport",
    "getCustom25DVisualExtractedPassiveReportsFileReadinessReport",
    "getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport"
  ];
  const expectedMovedHelpers = Array.from(
    new Set([...phase359MovedHelpers, ...phase361MovedHelpers, ...acceptedDedupedSet, ...phase369MovedHelpers])
  );
  const availableMovedHelpers = expectedMovedHelpers.filter(
    (name) => typeof globalThis !== "undefined" && typeof globalThis[name] === "function"
  );
  const missingMovedHelpers = expectedMovedHelpers.filter(
    (name) => !(typeof globalThis !== "undefined" && typeof globalThis[name] === "function")
  );
  const passiveNamespaceMovedHelpers =
    passiveReportsNamespace && Array.isArray(passiveReportsNamespace.movedHelperNames)
      ? passiveReportsNamespace.movedHelperNames
      : [];
  const manualNamespaceExposureExpected =
    !!manualNamespace &&
    expectedMovedHelpers.every((name) => typeof manualNamespace[name] === "function");
  const helperPresenceRegistryExpected = manualNamespaceExposureExpected;
  const movedHelpersExpectedInPassiveReportsFile =
    passiveNamespaceMovedHelpers.length > 0 &&
    expectedMovedHelpers.every((name) => passiveNamespaceMovedHelpers.includes(name));
  const passiveFileAbsenceFailSafeExpected = true;
  const blockers = [];
  if (!passiveReportsNamespace) {
    blockers.push("passive-reports-namespace-unavailable");
  }
  if (
    !passiveReportsNamespace ||
    passiveReportsNamespace.phase358LoadPathAvailable !== true
  ) {
    blockers.push("phase358-load-path-marker-unavailable");
  }
  if (missingMovedHelpers.length > 0) {
    blockers.push("expected-moved-helpers-missing");
  }
  if (!movedHelpersExpectedInPassiveReportsFile) {
    blockers.push("passive-reports-file-moved-helper-list-incomplete");
  }
  if (!manualNamespaceExposureExpected) {
    blockers.push("manual-namespace-moved-helper-exposure-unavailable");
  }
  if (!helperPresenceRegistryExpected) {
    blockers.push("helper-presence-registry-expectations-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 370,
      helperName: "getCustom25DVisualPostReconciliationMoveHealthReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      statusOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      passiveReportsNamespaceAvailable: !!passiveReportsNamespace,
      phase358LoadPathAvailable:
        !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
      expectedMovedHelpers,
      availableMovedHelpers,
      missingMovedHelpers,
      movedHelpersExpectedInPassiveReportsFile,
      manualNamespaceExposureExpected,
      helperPresenceRegistryExpected,
      passiveFileAbsenceFailSafeExpected,
      extractionPerformed: false,
      filesCreated: false,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      immediateBroadExtractionBlocked: true,
      nextRecommendedStep:
        "if-health-is-clean-allow-at-most-one-later-bundled-passive-move-then-pause-to-reassess-density",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 370,
    helperName: "getCustom25DVisualPostReconciliationMoveHealthReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    statusOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    passiveReportsNamespaceAvailable: !!passiveReportsNamespace,
    phase358LoadPathAvailable:
      !!(passiveReportsNamespace && passiveReportsNamespace.phase358LoadPathAvailable === true),
    expectedMovedHelpers,
    availableMovedHelpers,
    missingMovedHelpers,
    movedHelpersExpectedInPassiveReportsFile,
    manualNamespaceExposureExpected,
    helperPresenceRegistryExpected,
    passiveFileAbsenceFailSafeExpected,
    extractionPerformed: false,
    filesCreated: false,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    immediateBroadExtractionBlocked: true,
    nextRecommendedStep:
      "if-health-is-clean-allow-at-most-one-later-bundled-passive-move-then-pause-to-reassess-density",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPostReconciliationMoveReadinessReport(options = {}) {
  const health =
    typeof getCustom25DVisualPostReconciliationMoveHealthReport === "function"
      ? getCustom25DVisualPostReconciliationMoveHealthReport(options)
      : null;
  const requiredOptionKeys =
    health && Array.isArray(health.requiredOptionKeys)
      ? health.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (health && health.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (health && health.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (health && health.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = health && Array.isArray(health.blockers) ? [...health.blockers] : [];
  if (!health) {
    blockers.push("phase370-health-report-unavailable");
  }
  const readyForOneLaterBundledPassiveMove = !!(
    health &&
    health.passiveReportsNamespaceAvailable === true &&
    health.phase358LoadPathAvailable === true &&
    Array.isArray(health.missingMovedHelpers) &&
    health.missingMovedHelpers.length === 0 &&
    health.manualNamespaceExposureExpected === true &&
    health.helperPresenceRegistryExpected === true
  );

  if (missingKey) {
    return {
      ok: true,
      phase: 370,
      helperName: "getCustom25DVisualPostReconciliationMoveReadinessReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForOneLaterBundledPassiveMove,
      readyForBroadExtraction: false,
      pauseAfterOneMoreBundledMoveToReassessDensity: true,
      runtimeExtractionBlocked: true,
      startupExtractionBlocked: true,
      mapExtractionBlocked: true,
      gameplayExtractionBlocked: true,
      sharedStateExtractionBlocked: true,
      drawingExtractionBlocked: true,
      domExtractionBlocked: true,
      backendStorageNetworkExtractionBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 370,
    helperName: "getCustom25DVisualPostReconciliationMoveReadinessReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForOneLaterBundledPassiveMove,
    readyForBroadExtraction: false,
    pauseAfterOneMoreBundledMoveToReassessDensity: true,
    runtimeExtractionBlocked: true,
    startupExtractionBlocked: true,
    mapExtractionBlocked: true,
    gameplayExtractionBlocked: true,
    sharedStateExtractionBlocked: true,
    drawingExtractionBlocked: true,
    domExtractionBlocked: true,
    backendStorageNetworkExtractionBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPostReconciliationMoveHealthCloseoutReport(options = {}) {
  const health =
    typeof getCustom25DVisualPostReconciliationMoveHealthReport === "function"
      ? getCustom25DVisualPostReconciliationMoveHealthReport(options)
      : null;
  const readiness =
    typeof getCustom25DVisualPostReconciliationMoveReadinessReport === "function"
      ? getCustom25DVisualPostReconciliationMoveReadinessReport(options)
      : null;
  const requiredOptionKeys =
    (readiness && Array.isArray(readiness.requiredOptionKeys)
      ? readiness.requiredOptionKeys
      : null) ||
    (health && Array.isArray(health.requiredOptionKeys) ? health.requiredOptionKeys : null) || [
      "manual",
      "developerIntent",
      "localDevOnly",
      "browserConsoleOnly",
      "explicitOptionsOnly",
      "allowManualRendererStateContainerShell",
      "noStartupWiring",
      "noBackendChanges",
      "noPersistence",
      "noAutomaticInvocation"
    ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    (readiness && readiness.blockedBehavior) ||
    (health && health.blockedBehavior) || {
      rendererCreation: true,
      rendererInitialization: true,
      rendererRun: true,
      mapAttachment: true,
      drawing: true,
      domCreation: true,
      startupWiring: true,
      automaticInvocation: true,
      gameplayChanges: true,
      pinChanges: true,
      uiChanges: true,
      backendChanges: true,
      storageWrites: true,
      networkAccess: true
    };
  const preservedSystems =
    (readiness && readiness.preservedSystems) ||
    (health && health.preservedSystems) || {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true,
      storage: true,
      network: true
    };
  const safetyFlags =
    (readiness && readiness.safetyFlags) ||
    (health && health.safetyFlags) || {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    };
  const blockers = [];
  if (!health) {
    blockers.push("phase370-health-report-unavailable");
  }
  if (!readiness) {
    blockers.push("phase370-readiness-report-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 370,
      helperName: "getCustom25DVisualPostReconciliationMoveHealthCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      healthReportingOnly: true,
      helperDeclarationsMovedThisPhase: false,
      filesCreated: false,
      indexHtmlChangedThisPhase: false,
      passiveReportsFileChangedThisPhase: false,
      safeRhythmPreserved: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 370,
    helperName: "getCustom25DVisualPostReconciliationMoveHealthCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    healthReportingOnly: true,
    helperDeclarationsMovedThisPhase: false,
    filesCreated: false,
    indexHtmlChangedThisPhase: false,
    passiveReportsFileChangedThisPhase: false,
    safeRhythmPreserved: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveHelperLoadPathReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const passiveReportsNamespace =
    typeof window !== "undefined" &&
    window &&
    window.GrowGoCustom25DVisualPassiveReports &&
    typeof window.GrowGoCustom25DVisualPassiveReports === "object"
      ? window.GrowGoCustom25DVisualPassiveReports
      : null;
  const passiveReportsNamespaceAvailable = !!passiveReportsNamespace;
  const phase358LoadPathAvailable = !!(
    passiveReportsNamespace &&
    passiveReportsNamespace.phase358LoadPathAvailable === true
  );
  const blockers = [];
  if (!passiveReportsNamespaceAvailable) {
    blockers.push("passive-reports-namespace-unavailable");
  }
  if (!phase358LoadPathAvailable) {
    blockers.push("phase358-load-path-marker-unavailable");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 358,
      helperName: "getCustom25DVisualPassiveHelperLoadPathReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      loadPathPrepared: phase358LoadPathAvailable,
      passiveReportsNamespaceAvailable,
      phase358LoadPathAvailable,
      helpersMoved: [],
      extractionPerformed: false,
      filesCreated: 1,
      indexHtmlTouched: true,
      codeMoved: false,
      importsExportsAdded: false,
      loadingOrderChanged: true,
      runtimeBehaviorChanged: false,
      immediateExtractionBlocked: true,
      nextRecommendedStep: "validate-load-path-before-any-helper-extraction",
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 358,
    helperName: "getCustom25DVisualPassiveHelperLoadPathReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    loadPathPrepared: phase358LoadPathAvailable,
    passiveReportsNamespaceAvailable,
    phase358LoadPathAvailable,
    helpersMoved: [],
    extractionPerformed: false,
    filesCreated: 1,
    indexHtmlTouched: true,
    codeMoved: false,
    importsExportsAdded: false,
    loadingOrderChanged: true,
    runtimeBehaviorChanged: false,
    immediateExtractionBlocked: true,
    nextRecommendedStep: "validate-load-path-before-any-helper-extraction",
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualPassiveHelperLoadPathCloseoutReport(options = {}) {
  const loadPathReport =
    typeof getCustom25DVisualPassiveHelperLoadPathReport === "function"
      ? getCustom25DVisualPassiveHelperLoadPathReport(options)
      : null;
  const requiredOptionKeys =
    loadPathReport && Array.isArray(loadPathReport.requiredOptionKeys)
      ? loadPathReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    loadPathReport && loadPathReport.blockedBehavior
      ? loadPathReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    loadPathReport && loadPathReport.preservedSystems
      ? loadPathReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    loadPathReport && loadPathReport.safetyFlags
      ? loadPathReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [];
  if (typeof getCustom25DVisualPassiveHelperLoadPathReport !== "function") {
    blockers.push("passive-helper-load-path-report-unavailable");
  }
  if (loadPathReport && loadPathReport.phase358LoadPathAvailable !== true) {
    blockers.push("phase358-load-path-has-not-validated-cleanly");
  }

  if (missingKey) {
    return {
      ok: true,
      phase: 358,
      helperName: "getCustom25DVisualPassiveHelperLoadPathCloseoutReport",
      allowed: false,
      blocked: true,
      passive: true,
      reportOnly: true,
      closeoutOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      onlyIntendedBehaviorChangeIsInertPassiveFileLoad: true,
      helperExtractionPerformed: false,
      laterPhase359MayMoveAtMostOneToTwoPassiveHelpersOnly:
        !!(loadPathReport && loadPathReport.phase358LoadPathAvailable === true),
      runtimeExtractionBlocked: true,
      startupExtractionBlocked: true,
      mapExtractionBlocked: true,
      gameplayExtractionBlocked: true,
      sharedStateExtractionBlocked: true,
      drawingExtractionBlocked: true,
      domExtractionBlocked: true,
      backendStorageNetworkExtractionBlocked: true,
      blockers,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    ok: true,
    phase: 358,
    helperName: "getCustom25DVisualPassiveHelperLoadPathCloseoutReport",
    allowed: true,
    blocked: false,
    passive: true,
    reportOnly: true,
    closeoutOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    onlyIntendedBehaviorChangeIsInertPassiveFileLoad: true,
    helperExtractionPerformed: false,
    laterPhase359MayMoveAtMostOneToTwoPassiveHelpersOnly:
      !!(loadPathReport && loadPathReport.phase358LoadPathAvailable === true),
    runtimeExtractionBlocked: true,
    startupExtractionBlocked: true,
    mapExtractionBlocked: true,
    gameplayExtractionBlocked: true,
    sharedStateExtractionBlocked: true,
    drawingExtractionBlocked: true,
    domExtractionBlocked: true,
    backendStorageNetworkExtractionBlocked: true,
    blockers,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualScriptSizeStatusReport(options = {}) {
  const requiredOptionKeys = [
    "manual",
    "developerIntent",
    "localDevOnly",
    "browserConsoleOnly",
    "explicitOptionsOnly",
    "allowManualRendererStateContainerShell",
    "noStartupWiring",
    "noBackendChanges",
    "noPersistence",
    "noAutomaticInvocation"
  ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior = {
    rendererCreation: true,
    rendererInitialization: true,
    rendererRun: true,
    mapAttachment: true,
    drawing: true,
    domCreation: true,
    startupWiring: true,
    automaticInvocation: true,
    gameplayChanges: true,
    pinChanges: true,
    uiChanges: true,
    backendChanges: true,
    storageWrites: true,
    networkAccess: true
  };
  const preservedSystems = {
    existingLeafletMapBehavior: true,
    osmBehavior: true,
    gameplay: true,
    pins: true,
    playerMarker: true,
    captureRadius: true,
    ui: true,
    backend: true,
    storage: true,
    network: true
  };
  const safetyFlags = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };
  const concerns = [
    "script-js-large-single-file-maintenance-risk",
    "helper-density-makes-future-navigation-slower",
    "future-refactor-should-preserve-load-order-and-runtime-behavior",
    "module-splitting-should-happen-in-small-reviewed-phases"
  ];

  if (missingKey) {
    return {
      phase: 321,
      helperName: "getCustom25DVisualScriptSizeStatusReport",
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      statusOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      largeFileConcernAcknowledged: true,
      concerns,
      recommendedNextStep: "future-module-split-plan-only",
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 321,
    helperName: "getCustom25DVisualScriptSizeStatusReport",
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    statusOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    largeFileConcernAcknowledged: true,
    concerns,
    recommendedNextStep: "future-module-split-plan-only",
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFutureModuleSplitPlanReport(options = {}) {
  const sizeStatus =
    typeof getCustom25DVisualScriptSizeStatusReport === "function"
      ? getCustom25DVisualScriptSizeStatusReport(options)
      : null;
  const requiredOptionKeys =
    sizeStatus && Array.isArray(sizeStatus.requiredOptionKeys)
      ? sizeStatus.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    sizeStatus && sizeStatus.blockedBehavior
      ? sizeStatus.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    sizeStatus && sizeStatus.preservedSystems
      ? sizeStatus.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    sizeStatus && sizeStatus.safetyFlags
      ? sizeStatus.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const plannedModules = [
    "core-game-map-startup",
    "custom25dvisual-report-planning-helpers",
    "custom25dvisual-manual-renderer-helpers",
    "ui-menu-helpers",
    "social-qr-helpers",
    "future-firebase-backend-helpers",
    "audio-sound-helpers",
    "poi-collection-helpers"
  ];
  const unsafeToMoveYet = [
    "top-level-startup-sequencing",
    "implicit-load-order-dependencies",
    "shared-global-state-touchpoints"
  ];
  const migrationStrategy = [
    "start-with-report-only-helper-grouping-plan",
    "extract-one-low-risk-helper-family-per-phase",
    "preserve-function-names-and-call-order-during-each-step",
    "verify-runtime-behavior-after-each-tiny-phase"
  ];

  if (missingKey) {
    return {
      phase: 321,
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      planOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      moduleSplitPerformed: false,
      plannedModules,
      firstSafeCandidate: "custom25dvisual-report-planning-helpers",
      unsafeToMoveYet,
      migrationStrategy,
      rollbackPlanIncluded: true,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 321,
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    planOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    moduleSplitPerformed: false,
    plannedModules,
    firstSafeCandidate: "custom25dvisual-report-planning-helpers",
    unsafeToMoveYet,
    migrationStrategy,
    rollbackPlanIncluded: true,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualFutureModuleSplitReadinessReport(options = {}) {
  const sizeStatus =
    typeof getCustom25DVisualScriptSizeStatusReport === "function"
      ? getCustom25DVisualScriptSizeStatusReport(options)
      : null;
  const splitPlan =
    typeof getCustom25DVisualFutureModuleSplitPlanReport === "function"
      ? getCustom25DVisualFutureModuleSplitPlanReport(options)
      : null;
  const requiredOptionKeys =
    splitPlan && Array.isArray(splitPlan.requiredOptionKeys)
      ? splitPlan.requiredOptionKeys
      : sizeStatus && Array.isArray(sizeStatus.requiredOptionKeys)
        ? sizeStatus.requiredOptionKeys
        : [
            "manual",
            "developerIntent",
            "localDevOnly",
            "browserConsoleOnly",
            "explicitOptionsOnly",
            "allowManualRendererStateContainerShell",
            "noStartupWiring",
            "noBackendChanges",
            "noPersistence",
            "noAutomaticInvocation"
          ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    splitPlan && splitPlan.blockedBehavior
      ? splitPlan.blockedBehavior
      : sizeStatus && sizeStatus.blockedBehavior
        ? sizeStatus.blockedBehavior
        : {
            rendererCreation: true,
            rendererInitialization: true,
            rendererRun: true,
            mapAttachment: true,
            drawing: true,
            domCreation: true,
            startupWiring: true,
            automaticInvocation: true,
            gameplayChanges: true,
            pinChanges: true,
            uiChanges: true,
            backendChanges: true,
            storageWrites: true,
            networkAccess: true
          };
  const preservedSystems =
    splitPlan && splitPlan.preservedSystems
      ? splitPlan.preservedSystems
      : sizeStatus && sizeStatus.preservedSystems
        ? sizeStatus.preservedSystems
        : {
            existingLeafletMapBehavior: true,
            osmBehavior: true,
            gameplay: true,
            pins: true,
            playerMarker: true,
            captureRadius: true,
            ui: true,
            backend: true,
            storage: true,
            network: true
          };
  const safetyFlags =
    splitPlan && splitPlan.safetyFlags
      ? splitPlan.safetyFlags
      : sizeStatus && sizeStatus.safetyFlags
        ? sizeStatus.safetyFlags
        : {
            custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
            landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
            landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
            dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
          };
  const blockers = [
    "preserve-script-load-order",
    "avoid-cross-cutting-runtime-dependency-breakage",
    "split-in-tiny-reviewed-phases-only"
  ];
  const safetyNotes = [
    "do-not-move-code-in-phase-321",
    "start-with-report-only-helper-family-when-splitting-later",
    "verify-each-extraction-with-no-runtime-behavior-change"
  ];

  if (missingKey) {
    return {
      phase: 321,
      ok: true,
      allowed: false,
      blocked: true,
      reportOnly: true,
      readinessOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyForImmediateSplit: false,
      readyForPlanningOnly: true,
      suggestedFuturePhaseType: "tiny-passive-module-split-plan-phase",
      suggestedFirstExtractionArea: "custom25dvisual-report-planning-helpers",
      blockers,
      safetyNotes,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 321,
    ok: true,
    allowed: true,
    blocked: false,
    reportOnly: true,
    readinessOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyForImmediateSplit: false,
    readyForPlanningOnly: true,
    suggestedFuturePhaseType: "tiny-passive-module-split-plan-phase",
    suggestedFirstExtractionArea: "custom25dvisual-report-planning-helpers",
    blockers,
    safetyNotes,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport(options = {}) {
  const scriptSizeStatusReport =
    typeof getCustom25DVisualScriptSizeStatusReport === "function"
      ? getCustom25DVisualScriptSizeStatusReport(options)
      : null;
  const moduleSplitPlanReport =
    typeof getCustom25DVisualFutureModuleSplitPlanReport === "function"
      ? getCustom25DVisualFutureModuleSplitPlanReport(options)
      : null;
  const moduleSplitReadinessReport =
    typeof getCustom25DVisualFutureModuleSplitReadinessReport === "function"
      ? getCustom25DVisualFutureModuleSplitReadinessReport(options)
      : null;
  const requiredOptionKeys =
    moduleSplitReadinessReport &&
    Array.isArray(moduleSplitReadinessReport.requiredOptionKeys)
      ? moduleSplitReadinessReport.requiredOptionKeys
      : moduleSplitPlanReport && Array.isArray(moduleSplitPlanReport.requiredOptionKeys)
        ? moduleSplitPlanReport.requiredOptionKeys
        : scriptSizeStatusReport &&
            Array.isArray(scriptSizeStatusReport.requiredOptionKeys)
          ? scriptSizeStatusReport.requiredOptionKeys
          : [
              "manual",
              "developerIntent",
              "localDevOnly",
              "browserConsoleOnly",
              "explicitOptionsOnly",
              "allowManualRendererStateContainerShell",
              "noStartupWiring",
              "noBackendChanges",
              "noPersistence",
              "noAutomaticInvocation"
            ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    moduleSplitReadinessReport && moduleSplitReadinessReport.blockedBehavior
      ? moduleSplitReadinessReport.blockedBehavior
      : moduleSplitPlanReport && moduleSplitPlanReport.blockedBehavior
        ? moduleSplitPlanReport.blockedBehavior
        : scriptSizeStatusReport && scriptSizeStatusReport.blockedBehavior
          ? scriptSizeStatusReport.blockedBehavior
          : {
              rendererCreation: true,
              rendererInitialization: true,
              rendererRun: true,
              mapAttachment: true,
              drawing: true,
              domCreation: true,
              startupWiring: true,
              automaticInvocation: true,
              gameplayChanges: true,
              pinChanges: true,
              uiChanges: true,
              backendChanges: true,
              storageWrites: true,
              networkAccess: true
            };
  const preservedSystems =
    moduleSplitReadinessReport && moduleSplitReadinessReport.preservedSystems
      ? moduleSplitReadinessReport.preservedSystems
      : moduleSplitPlanReport && moduleSplitPlanReport.preservedSystems
        ? moduleSplitPlanReport.preservedSystems
        : scriptSizeStatusReport && scriptSizeStatusReport.preservedSystems
          ? scriptSizeStatusReport.preservedSystems
          : {
              existingLeafletMapBehavior: true,
              osmBehavior: true,
              gameplay: true,
              pins: true,
              playerMarker: true,
              captureRadius: true,
              ui: true,
              backend: true,
              storage: true,
              network: true
            };
  const safetyFlags =
    moduleSplitReadinessReport && moduleSplitReadinessReport.safetyFlags
      ? moduleSplitReadinessReport.safetyFlags
      : moduleSplitPlanReport && moduleSplitPlanReport.safetyFlags
        ? moduleSplitPlanReport.safetyFlags
        : scriptSizeStatusReport && scriptSizeStatusReport.safetyFlags
          ? scriptSizeStatusReport.safetyFlags
          : {
              custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
              landmarkTestMarkers:
                ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
              landmarkSampleData:
                ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
              dinosaurSitesAuData:
                ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
            };
  const concerns =
    scriptSizeStatusReport && Array.isArray(scriptSizeStatusReport.concerns)
      ? scriptSizeStatusReport.concerns
      : [
          "script-js-large-single-file-maintenance-risk",
          "helper-density-makes-future-navigation-slower",
          "future-refactor-should-preserve-load-order-and-runtime-behavior",
          "module-splitting-should-happen-in-small-reviewed-phases"
        ];
  const recommendedFirstExtractionArea =
    moduleSplitReadinessReport &&
    typeof moduleSplitReadinessReport.suggestedFirstExtractionArea === "string"
      ? moduleSplitReadinessReport.suggestedFirstExtractionArea
      : moduleSplitPlanReport &&
          typeof moduleSplitPlanReport.firstSafeCandidate === "string"
        ? moduleSplitPlanReport.firstSafeCandidate
        : "custom25dvisual-report-planning-helpers";

  if (missingKey) {
    return {
      phase: 322,
      phaseClosedOut: 321,
      helperName: "getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport",
      ok: true,
      allowed: false,
      blocked: true,
      closeoutOnly: true,
      reportOnly: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      scriptSizeStatusReportAvailable:
        typeof getCustom25DVisualScriptSizeStatusReport === "function",
      moduleSplitPlanReportAvailable:
        typeof getCustom25DVisualFutureModuleSplitPlanReport === "function",
      moduleSplitReadinessReportAvailable:
        typeof getCustom25DVisualFutureModuleSplitReadinessReport ===
        "function",
      moduleSplitPerformed: false,
      codeMoved: false,
      filesCreated: false,
      importsAdded: false,
      exportsAdded: false,
      loadingOrderChanged: false,
      runtimeBehaviorChanged: false,
      recommendedFirstExtractionArea,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 322,
    phaseClosedOut: 321,
    helperName: "getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport",
    ok: true,
    allowed: true,
    blocked: false,
    closeoutOnly: true,
    reportOnly: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    scriptSizeStatusReportAvailable:
      typeof getCustom25DVisualScriptSizeStatusReport === "function",
    moduleSplitPlanReportAvailable:
      typeof getCustom25DVisualFutureModuleSplitPlanReport === "function",
    moduleSplitReadinessReportAvailable:
      typeof getCustom25DVisualFutureModuleSplitReadinessReport === "function",
    moduleSplitPerformed: false,
    codeMoved: false,
    filesCreated: false,
    importsAdded: false,
    exportsAdded: false,
    loadingOrderChanged: false,
    runtimeBehaviorChanged: false,
    recommendedFirstExtractionArea,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport(options = {}) {
  const closeoutReport =
    typeof getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport === "function"
      ? getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport(options)
      : null;
  const requiredOptionKeys =
    closeoutReport && Array.isArray(closeoutReport.requiredOptionKeys)
      ? closeoutReport.requiredOptionKeys
      : [
          "manual",
          "developerIntent",
          "localDevOnly",
          "browserConsoleOnly",
          "explicitOptionsOnly",
          "allowManualRendererStateContainerShell",
          "noStartupWiring",
          "noBackendChanges",
          "noPersistence",
          "noAutomaticInvocation"
        ];
  const missingKey = requiredOptionKeys.find((key) => options[key] !== true) || null;
  const reasonByKey = {
    manual: "manual-flag-required",
    developerIntent: "developer-intent-required",
    localDevOnly: "local-dev-only-required",
    browserConsoleOnly: "browser-console-only-required",
    explicitOptionsOnly: "explicit-options-only-required",
    allowManualRendererStateContainerShell:
      "manual-renderer-state-container-shell-not-allowed",
    noStartupWiring: "no-startup-wiring-acknowledgement-required",
    noBackendChanges: "no-backend-changes-acknowledgement-required",
    noPersistence: "no-persistence-acknowledgement-required",
    noAutomaticInvocation: "no-automatic-invocation-acknowledgement-required"
  };
  const blockedBehavior =
    closeoutReport && closeoutReport.blockedBehavior
      ? closeoutReport.blockedBehavior
      : {
          rendererCreation: true,
          rendererInitialization: true,
          rendererRun: true,
          mapAttachment: true,
          drawing: true,
          domCreation: true,
          startupWiring: true,
          automaticInvocation: true,
          gameplayChanges: true,
          pinChanges: true,
          uiChanges: true,
          backendChanges: true,
          storageWrites: true,
          networkAccess: true
        };
  const preservedSystems =
    closeoutReport && closeoutReport.preservedSystems
      ? closeoutReport.preservedSystems
      : {
          existingLeafletMapBehavior: true,
          osmBehavior: true,
          gameplay: true,
          pins: true,
          playerMarker: true,
          captureRadius: true,
          ui: true,
          backend: true,
          storage: true,
          network: true
        };
  const safetyFlags =
    closeoutReport && closeoutReport.safetyFlags
      ? closeoutReport.safetyFlags
      : {
          custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
          landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
          landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
          dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
        };
  const blockers = [
    "future-module-split-should-wait-for-tiny-reviewed-phases",
    "load-order-sensitive-areas-still-need-cautious-extraction",
    "renderer-metadata-planning-remains-safer-immediate-work"
  ];
  const safetyNotes = [
    "pause-module-split-work-after-planning-sequence",
    "return-to-renderer-metadata-planning-before-any-real-file-split",
    "keep-any-future-split-to-small-reversible-phases"
  ];
  const concerns =
    closeoutReport && Array.isArray(closeoutReport.concerns)
      ? closeoutReport.concerns
      : [
          "script-js-large-single-file-maintenance-risk",
          "helper-density-makes-future-navigation-slower",
          "future-refactor-should-preserve-load-order-and-runtime-behavior",
          "module-splitting-should-happen-in-small-reviewed-phases"
        ];

  if (missingKey) {
    return {
      phase: 322,
      helperName: "getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport",
      ok: true,
      allowed: false,
      blocked: true,
      selfReviewOnly: true,
      reportOnly: true,
      sequenceReviewed: true,
      reason: reasonByKey[missingKey] || "required-option-missing",
      failedRequirement: missingKey,
      requiredOptionKeys,
      readyToReturnToRendererMetadataPlanning: true,
      readyForImmediateModuleSplit: false,
      futureModuleSplitShouldBeTinyPhases: true,
      blockers,
      safetyNotes,
      concerns,
      blockedBehavior,
      preservedSystems,
      safetyFlags
    };
  }

  return {
    phase: 322,
    helperName: "getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport",
    ok: true,
    allowed: true,
    blocked: false,
    selfReviewOnly: true,
    reportOnly: true,
    sequenceReviewed: true,
    reason: null,
    failedRequirement: null,
    requiredOptionKeys,
    readyToReturnToRendererMetadataPlanning: true,
    readyForImmediateModuleSplit: false,
    futureModuleSplitShouldBeTinyPhases: true,
    blockers,
    safetyNotes,
    concerns,
    blockedBehavior,
    preservedSystems,
    safetyFlags
  };
}

function exposeCustom25DVisualManualTestHelpersForLocalDevConsole(options = {}) {
  const windowExists = typeof window !== "undefined" && window;
  const hostname =
    windowExists && window.location && typeof window.location.hostname === "string"
      ? window.location.hostname
      : "";
  const isLocalDevHost =
    hostname === "localhost" ||
    hostname === "127.0.0.1" ||
    hostname === "0.0.0.0" ||
    hostname === "::1";
  const strictRequirements = [
    {
      key: "manual",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "localDevOnly",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    }
  ];
  const firstFailure = strictRequirements.find((requirement) => !requirement.passed) || null;
  const namespaceKey = "GrowGoCustom25DVisualManualTests";

  if (!windowExists) {
    return {
      phase: 240,
      name: "custom-25d-visual-manual-test-helper-exposure",
      ok: true,
      allowed: false,
      exposed: false,
      reason: "window-unavailable",
      failedRequirement: "windowAvailable",
      namespace: namespaceKey
    };
  }

  if (!isLocalDevHost) {
    return {
      phase: 240,
      name: "custom-25d-visual-manual-test-helper-exposure",
      ok: true,
      allowed: false,
      exposed: false,
      reason: "local-dev-host-required",
      failedRequirement: "localDevHost",
      namespace: namespaceKey
    };
  }

  if (firstFailure) {
    return {
      phase: 240,
      name: "custom-25d-visual-manual-test-helper-exposure",
      ok: true,
      allowed: false,
      exposed: false,
      reason: firstFailure.reason,
      failedRequirement: firstFailure.key,
      namespace: namespaceKey
    };
  }

  const createNamespaceWrapper = (helperName, resolver) => (...args) => {
    const helper = typeof resolver === "function" ? resolver() : null;
    if (typeof helper !== "function") {
      return {
        phase: 240,
        name: "custom-25d-visual-manual-test-helper-exposure-wrapper",
        ok: false,
        allowed: false,
        exposed: true,
        helperName,
        reason: "helper-unavailable-at-call-time",
        failedRequirement: "helperAvailable",
        namespace: namespaceKey
      };
    }

    return helper(...args);
  };

  const namespace = {
    getCustom25DVisualFirstShapeTestContract: createNamespaceWrapper(
      "getCustom25DVisualFirstShapeTestContract",
      () =>
        typeof getCustom25DVisualFirstShapeTestContract === "function"
          ? getCustom25DVisualFirstShapeTestContract
          : null
    ),
    createCustom25DVisualFirstShapeManualTestLayer: createNamespaceWrapper(
      "createCustom25DVisualFirstShapeManualTestLayer",
      () =>
        typeof createCustom25DVisualFirstShapeManualTestLayer === "function"
          ? createCustom25DVisualFirstShapeManualTestLayer
          : null
    ),
    clearCustom25DVisualFirstShapeManualTestLayer: createNamespaceWrapper(
      "clearCustom25DVisualFirstShapeManualTestLayer",
      () =>
        typeof clearCustom25DVisualFirstShapeManualTestLayer === "function"
          ? clearCustom25DVisualFirstShapeManualTestLayer
          : null
    ),
    runCustom25DVisualFirstShapeManualTest: createNamespaceWrapper(
      "runCustom25DVisualFirstShapeManualTest",
      () =>
        typeof runCustom25DVisualFirstShapeManualTest === "function"
          ? runCustom25DVisualFirstShapeManualTest
          : null
    ),
    getCustom25DVisualFirstShapeManualTestVerificationReport: createNamespaceWrapper(
      "getCustom25DVisualFirstShapeManualTestVerificationReport",
      () =>
        typeof getCustom25DVisualFirstShapeManualTestVerificationReport === "function"
          ? getCustom25DVisualFirstShapeManualTestVerificationReport
          : null
    ),
    getCustom25DVisualFirstShapeManualTestCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualFirstShapeManualTestCloseoutReport",
      () =>
        typeof getCustom25DVisualFirstShapeManualTestCloseoutReport === "function"
          ? getCustom25DVisualFirstShapeManualTestCloseoutReport
          : null
    ),
    getCustom25DVisualInertRendererShellCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualInertRendererShellCloseoutReport",
      () =>
        typeof getCustom25DVisualInertRendererShellCloseoutReport === "function"
          ? getCustom25DVisualInertRendererShellCloseoutReport
          : null
    ),
    getCustom25DVisualRendererLifecyclePlanReport: createNamespaceWrapper(
      "getCustom25DVisualRendererLifecyclePlanReport",
      () =>
        typeof getCustom25DVisualRendererLifecyclePlanReport === "function"
          ? getCustom25DVisualRendererLifecyclePlanReport
          : null
    ),
    getCustom25DVisualRendererLifecycleContractReport: createNamespaceWrapper(
      "getCustom25DVisualRendererLifecycleContractReport",
      () =>
        typeof getCustom25DVisualRendererLifecycleContractReport === "function"
          ? getCustom25DVisualRendererLifecycleContractReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardPlanReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardPlanReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardPlanReport === "function"
          ? getCustom25DVisualManualInitializationGuardPlanReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardContractReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardContractReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardContractReport === "function"
          ? getCustom25DVisualManualInitializationGuardContractReport
          : null
    ),
    canInitializeCustom25DVisualRendererManually: createNamespaceWrapper(
      "canInitializeCustom25DVisualRendererManually",
      () =>
        typeof canInitializeCustom25DVisualRendererManually === "function"
          ? canInitializeCustom25DVisualRendererManually
          : null
    ),
    getCustom25DVisualManualInitializationGuardShellCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationGuardShellCloseoutReport",
      () =>
        typeof getCustom25DVisualManualInitializationGuardShellCloseoutReport === "function"
          ? getCustom25DVisualManualInitializationGuardShellCloseoutReport
          : null
    ),
    getCustom25DVisualManualInitializationApprovalSignalPlanReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationApprovalSignalPlanReport",
      () =>
        typeof getCustom25DVisualManualInitializationApprovalSignalPlanReport === "function"
          ? getCustom25DVisualManualInitializationApprovalSignalPlanReport
          : null
    ),
    getCustom25DVisualManualInitializationApprovalSignalContractReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationApprovalSignalContractReport",
      () =>
        typeof getCustom25DVisualManualInitializationApprovalSignalContractReport === "function"
          ? getCustom25DVisualManualInitializationApprovalSignalContractReport
          : null
    ),
    evaluateCustom25DVisualManualInitializationSignals: createNamespaceWrapper(
      "evaluateCustom25DVisualManualInitializationSignals",
      () =>
        typeof evaluateCustom25DVisualManualInitializationSignals === "function"
          ? evaluateCustom25DVisualManualInitializationSignals
          : null
    ),
    getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport: createNamespaceWrapper(
      "getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport",
      () =>
        typeof getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport === "function"
          ? getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport
          : null
    ),
    getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport",
        () =>
          typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport ===
          "function"
            ? getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport
            : null
      ),
    getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport",
        () =>
          typeof getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport ===
          "function"
            ? getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport
            : null
      ),
    canUseCustom25DVisualManualRendererInitializationIntegratedGuard:
      createNamespaceWrapper(
        "canUseCustom25DVisualManualRendererInitializationIntegratedGuard",
        () =>
          typeof canUseCustom25DVisualManualRendererInitializationIntegratedGuard ===
          "function"
            ? canUseCustom25DVisualManualRendererInitializationIntegratedGuard
            : null
      ),
    getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport",
        () =>
          typeof getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport ===
          "function"
            ? getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellContractReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellContractReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellContractReport
            : null
      ),
    initializeCustom25DVisualRendererManually:
      createNamespaceWrapper(
        "initializeCustom25DVisualRendererManually",
        () =>
          typeof initializeCustom25DVisualRendererManually === "function"
            ? initializeCustom25DVisualRendererManually
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerPlanReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerContractReadinessReport
            : null
      ),
    createCustom25DVisualManualRendererStateContainerShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererStateContainerShell",
        () =>
          typeof createCustom25DVisualManualRendererStateContainerShell === "function"
            ? createCustom25DVisualManualRendererStateContainerShell
            : null
      ),
    getCustom25DVisualManualRendererStateContainerShellVerificationReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerShellVerificationReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerShellVerificationReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerShellVerificationReport
            : null
      ),
    getCustom25DVisualManualRendererStateMutationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport
            : null
      ),
    applyCustom25DVisualManualRendererStateMutationShell: createNamespaceWrapper(
      "applyCustom25DVisualManualRendererStateMutationShell",
      () =>
        typeof applyCustom25DVisualManualRendererStateMutationShell === "function"
          ? applyCustom25DVisualManualRendererStateMutationShell
          : null
    ),
    getCustom25DVisualManualRendererStateMutationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateMutationShellSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateMutationShellSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererStateMutationShellSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererStateMutationShellSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererNextStateMutationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNextStateMutationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererNextStateMutationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererNextStateMutationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport
            : null
      ),
    applyCustom25DVisualManualRendererSecondStateMutationShell: createNamespaceWrapper(
      "applyCustom25DVisualManualRendererSecondStateMutationShell",
      () =>
        typeof applyCustom25DVisualManualRendererSecondStateMutationShell === "function"
          ? applyCustom25DVisualManualRendererSecondStateMutationShell
          : null
    ),
    getCustom25DVisualManualRendererSecondStateMutationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSecondStateMutationCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSecondStateMutationCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSecondStateMutationCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport
            : null
      ),
    applyCustom25DVisualManualRendererStateFieldContractShell:
      createNamespaceWrapper(
        "applyCustom25DVisualManualRendererStateFieldContractShell",
        () =>
          typeof applyCustom25DVisualManualRendererStateFieldContractShell ===
          "function"
            ? applyCustom25DVisualManualRendererStateFieldContractShell
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractNextReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractNextReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractNextReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractNextReadinessReport
            : null
      ),
    applyCustom25DVisualManualRendererStateFieldContractValidationShell:
      createNamespaceWrapper(
        "applyCustom25DVisualManualRendererStateFieldContractValidationShell",
        () =>
          typeof applyCustom25DVisualManualRendererStateFieldContractValidationShell ===
          "function"
            ? applyCustom25DVisualManualRendererStateFieldContractValidationShell
            : null
      ),
    getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPostFieldContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostFieldContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPostFieldContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPostFieldContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport
            : null
      ),
    getCustom25DVisualManualRendererInstanceShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInstanceShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInstanceShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInstanceShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport
            : null
      ),
    createCustom25DVisualManualRendererInstanceShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererInstanceShell",
        () =>
          typeof createCustom25DVisualManualRendererInstanceShell === "function"
            ? createCustom25DVisualManualRendererInstanceShell
            : null
      ),
    getCustom25DVisualManualRendererInstanceShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInstanceShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInstanceShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInstanceShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPostInstanceShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostInstanceShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPostInstanceShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPostInstanceShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport
            : null
      ),
    createCustom25DVisualManualRendererLifecycleMethodShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererLifecycleMethodShell",
        () =>
          typeof createCustom25DVisualManualRendererLifecycleMethodShell ===
          "function"
            ? createCustom25DVisualManualRendererLifecycleMethodShell
            : null
      ),
    getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport
            : null
      ),
    createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation",
        () =>
          typeof createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
          "function"
            ? createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateContainerShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateContainerShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateContainerShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateContainerShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateContainerPreflightReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateContainerPreflightReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateContainerPreflightReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateContainerPreflightReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSchemaReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSchemaReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSchemaReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSchemaReviewReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadAccessPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadAccessPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadAccessPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshot:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshot",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshot === "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshot
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport
            : null
      ),
    getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport",
        () =>
          typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport ===
          "function"
            ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport
            : null
      ),
    getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellContractReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellContractReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellContractReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererIntegrationShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererIntegrationShell",
        () =>
          typeof createCustom25DVisualManualRendererIntegrationShell === "function"
            ? createCustom25DVisualManualRendererIntegrationShell
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatus:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatus",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatus === "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatus
            : null
      ),
    getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererLifecyclePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecyclePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecyclePlanReport === "function"
            ? getCustom25DVisualManualRendererLifecyclePlanReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleContractReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleContractReport === "function"
            ? getCustom25DVisualManualRendererLifecycleContractReport
            : null
      ),
    getCustom25DVisualManualRendererLifecyclePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecyclePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecyclePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecyclePlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererLifecycleShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererLifecycleShell",
        () =>
          typeof createCustom25DVisualManualRendererLifecycleShell === "function"
            ? createCustom25DVisualManualRendererLifecycleShell
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectPlanReport === "function"
            ? getCustom25DVisualManualRendererObjectPlanReport
            : null
      ),
    getCustom25DVisualManualRendererObjectContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectContractReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectContractReport === "function"
            ? getCustom25DVisualManualRendererObjectContractReport
            : null
      ),
    getCustom25DVisualManualRendererObjectPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererObjectShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererObjectShell",
        () =>
          typeof createCustom25DVisualManualRendererObjectShell === "function"
            ? createCustom25DVisualManualRendererObjectShell
            : null
      ),
    getCustom25DVisualManualRendererObjectShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShellStatusCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShellStatusCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShellStatusCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShellStatusCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapePlanReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeContractReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeContractReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeContractReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapePlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererObjectShapeShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererObjectShapeShell",
        () =>
          typeof createCustom25DVisualManualRendererObjectShapeShell === "function"
            ? createCustom25DVisualManualRendererObjectShapeShell
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererObjectShapeShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererObjectShapeShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererObjectShapeShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataPlanReport === "function"
            ? getCustom25DVisualManualRendererMetadataPlanReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataShell === "function"
            ? createCustom25DVisualManualRendererMetadataShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataContractPlanReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataContractShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataContractShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataContractShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataContractShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationContractReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationContractReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationContractReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationContractReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataValidationShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataValidationShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataValidationShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataValidationShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataValidationResultContractShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataValidationResultContractShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataValidationResultContractShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport
            : null
      ),
    createCustom25DVisualManualRendererMetadataValidationResultShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererMetadataValidationResultShell",
        () =>
          typeof createCustom25DVisualManualRendererMetadataValidationResultShell ===
          "function"
            ? createCustom25DVisualManualRendererMetadataValidationResultShell
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualSecondPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualSecondPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport ===
            "function"
            ? globalThis.getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualBundledPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualBundledPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualBundledPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualBundledPassiveMoveHealthReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveMoveHealthReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualBundledPassiveMoveHealthReport ===
            "function"
            ? globalThis.getCustom25DVisualBundledPassiveMoveHealthReport
            : null
      ),
    getCustom25DVisualBundledPassiveMoveReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveMoveReadinessReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualBundledPassiveMoveReadinessReport ===
            "function"
            ? globalThis.getCustom25DVisualBundledPassiveMoveReadinessReport
            : null
      ),
    getCustom25DVisualBundledPassiveMoveHealthCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualBundledPassiveMoveHealthCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualBundledPassiveMoveHealthCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualBundledPassiveMoveHealthCloseoutReport
            : null
      ),
    getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionReassessmentReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionReassessmentReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionReassessmentReport === "function"
            ? getCustom25DVisualPassiveExtractionReassessmentReport
            : null
      ),
    getCustom25DVisualPassiveExtractionReassessmentReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionReassessmentReadinessReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionReassessmentReadinessReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionReassessmentReadinessReport
            : null
      ),
    getCustom25DVisualPassiveExtractionReassessmentCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionReassessmentCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionReassessmentCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionReassessmentCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport
            : null
      ),
    getCustom25DVisualRendererPathResumeReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererPathResumeReadinessReport",
        () =>
          typeof getCustom25DVisualRendererPathResumeReadinessReport === "function"
            ? getCustom25DVisualRendererPathResumeReadinessReport
            : null
      ),
    getCustom25DVisualRendererPathResumeNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererPathResumeNextStepReport",
        () =>
          typeof getCustom25DVisualRendererPathResumeNextStepReport === "function"
            ? getCustom25DVisualRendererPathResumeNextStepReport
            : null
      ),
    getCustom25DVisualRendererPathResumeCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererPathResumeCloseoutReport",
        () =>
          typeof getCustom25DVisualRendererPathResumeCloseoutReport === "function"
            ? getCustom25DVisualRendererPathResumeCloseoutReport
            : null
      ),
    getCustom25DVisualRendererStateContainerReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerReadinessReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerReadinessReport === "function"
            ? getCustom25DVisualRendererStateContainerReadinessReport
            : null
      ),
    getCustom25DVisualRendererStateContainerNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerNextStepReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerNextStepReport === "function"
            ? getCustom25DVisualRendererStateContainerNextStepReport
            : null
      ),
    getCustom25DVisualRendererStateContainerReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualRendererStateContainerPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerPlanCloseoutReport
            : null
      ),
    getCustom25DVisualRendererStateContainerPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerPlanSelfReviewReport
            : null
      ),
    getCustom25DVisualRendererStateContainerPlanNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualRendererStateContainerPlanNextStepReport",
        () =>
          typeof getCustom25DVisualRendererStateContainerPlanNextStepReport ===
          "function"
            ? getCustom25DVisualRendererStateContainerPlanNextStepReport
            : null
      ),
    getCustom25DVisualInertStateContainerContractPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerContractPlanReport",
        () =>
          typeof getCustom25DVisualInertStateContainerContractPlanReport ===
          "function"
            ? getCustom25DVisualInertStateContainerContractPlanReport
            : null
      ),
    getCustom25DVisualInertStateContainerContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerContractReadinessReport",
        () =>
          typeof getCustom25DVisualInertStateContainerContractReadinessReport ===
          "function"
            ? getCustom25DVisualInertStateContainerContractReadinessReport
            : null
      ),
    getCustom25DVisualInertStateContainerContractPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerContractPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerContractPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerContractPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaNextStepReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaNextStepReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaNextStepReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport",
        () =>
          typeof getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport ===
          "function"
            ? getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationReadinessReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationReadinessReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationReadinessReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationGateReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationGateReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationGateReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationGateReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport
            : null
      ),
    createCustom25DVisualInertManualRendererStateContainer:
      createNamespaceWrapper(
        "createCustom25DVisualInertManualRendererStateContainer",
        () =>
          typeof createCustom25DVisualInertManualRendererStateContainer === "function"
            ? createCustom25DVisualInertManualRendererStateContainer
            : null
      ),
    getCustom25DVisualInertStateContainerCreationReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationReport === "function"
            ? getCustom25DVisualInertStateContainerCreationReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationCloseoutReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualInertStateContainerManualTestReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerManualTestReadinessReport",
        () =>
          typeof getCustom25DVisualInertStateContainerManualTestReadinessReport ===
          "function"
            ? getCustom25DVisualInertStateContainerManualTestReadinessReport
            : null
      ),
    runCustom25DVisualInertStateContainerManualConsoleTest:
      createNamespaceWrapper(
        "runCustom25DVisualInertStateContainerManualConsoleTest",
        () =>
          typeof runCustom25DVisualInertStateContainerManualConsoleTest ===
          "function"
            ? runCustom25DVisualInertStateContainerManualConsoleTest
            : null
      ),
    getCustom25DVisualInertStateContainerManualConsoleTestReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerManualConsoleTestReport",
        () =>
          typeof getCustom25DVisualInertStateContainerManualConsoleTestReport ===
          "function"
            ? getCustom25DVisualInertStateContainerManualConsoleTestReport
            : null
      ),
    getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport
            : null
      ),
    getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport",
        () =>
          typeof getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport ===
          "function"
            ? getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererStateFieldsReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateFieldsReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateFieldsReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateFieldsReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInertStateFieldsReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertStateFieldsReport",
        () =>
          typeof getCustom25DVisualManualRendererInertStateFieldsReport ===
          "function"
            ? getCustom25DVisualManualRendererInertStateFieldsReport
            : null
      ),
    getCustom25DVisualManualRendererInertStateFieldsCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertStateFieldsCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInertStateFieldsCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInertStateFieldsCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInertLifecycleStateFieldsReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertLifecycleStateFieldsReport",
        () =>
          typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsReport ===
          "function"
            ? getCustom25DVisualManualRendererInertLifecycleStateFieldsReport
            : null
      ),
    getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationReadinessReport
            : null
      ),
    initializeCustom25DVisualManualRendererShell:
      createNamespaceWrapper(
        "initializeCustom25DVisualManualRendererShell",
        () =>
          typeof initializeCustom25DVisualManualRendererShell === "function"
            ? initializeCustom25DVisualManualRendererShell
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractShell:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractShell",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractShell ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractShell
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationContractShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationContractShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationContractShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationGuardReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGuardReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGuardReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGuardReadinessReport
            : null
      ),
    canInitializeCustom25DVisualManualRenderer:
      createNamespaceWrapper(
        "canInitializeCustom25DVisualManualRenderer",
        () =>
          typeof canInitializeCustom25DVisualManualRenderer === "function"
            ? canInitializeCustom25DVisualManualRenderer
            : null
      ),
    getCustom25DVisualManualRendererInitializationGuardShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGuardShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGuardShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGuardShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightShellReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightShellReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightShellReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightShellReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationPreflightCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationPreflightCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationPreflightCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationGateReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationGateReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationGateReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationGateReviewReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationLockReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationLockReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationLockReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationLockReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationSequenceSummaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationSequenceSummaryReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationSequenceSummaryReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationHandoffPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationHandoffPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationHandoffPlanReport
            : null
      ),
    getCustom25DVisualManualRendererInitializationNextStepReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererInitializationNextStepReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererInitializationNextStepReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererInitializationNextStepReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningBoundaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningBoundaryReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningBoundaryReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningBoundaryReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningRiskReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningRiskReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningRiskReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningRiskReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningRiskCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningRiskCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningRiskCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningRiskCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPlanningHandoffSummaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPlanningHandoffSummaryReport",
        () =>
          typeof getCustom25DVisualManualRendererPlanningHandoffSummaryReport ===
          "function"
            ? getCustom25DVisualManualRendererPlanningHandoffSummaryReport
            : null
      ),
    getCustom25DVisualManualRendererStateCreationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateCreationReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererStateCreationReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererStateCreationReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationReadinessReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationReadinessReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationReadinessReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationCloseoutReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationCloseoutReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationCloseoutReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationAcceptanceReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport
            : null
      ),
    getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport",
        () =>
          typeof getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport ===
          "function"
            ? getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceStatusReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceStatusReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceStatusReport
            : null
      ),
    getCustom25DVisualFutureSafeExtractionCandidateReport:
      createNamespaceWrapper(
        "getCustom25DVisualFutureSafeExtractionCandidateReport",
        () =>
          typeof getCustom25DVisualFutureSafeExtractionCandidateReport ===
          "function"
            ? getCustom25DVisualFutureSafeExtractionCandidateReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceReadinessReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceReadinessReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceReadinessReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport
            : null
      ),
    getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport",
        () =>
          typeof getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
          "function"
            ? getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestReadinessReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestReadinessReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestReadinessReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightReadinessReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightReadinessReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport",
        () =>
          typeof getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport ===
          "function"
            ? getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport
            : null
      ),
    getCustom25DVisualFirstPassiveExtractionCandidatePlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveExtractionCandidatePlanReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport
            : null
      ),
    getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport
            : null
      ),
    getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport
            : null
      ),
    getCustom25DVisualExtractedPassiveReportsFileHealthReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedPassiveReportsFileHealthReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthReport ===
            "function"
            ? globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthReport
            : null
      ),
    getCustom25DVisualExtractedPassiveReportsFileReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedPassiveReportsFileReadinessReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualExtractedPassiveReportsFileReadinessReport ===
            "function"
            ? globalThis.getCustom25DVisualExtractedPassiveReportsFileReadinessReport
            : null
      ),
    getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport",
        () =>
          typeof globalThis !== "undefined" &&
          typeof globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport ===
            "function"
            ? globalThis.getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport
            : null
      ),
    getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport",
        () =>
          typeof getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport ===
          "function"
            ? getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport
            : null
      ),
    getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport",
        () =>
          typeof getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport ===
          "function"
            ? getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport
            : null
      ),
    getCustom25DVisualPostReconciliationMoveHealthReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationMoveHealthReport",
        () =>
          typeof getCustom25DVisualPostReconciliationMoveHealthReport === "function"
            ? getCustom25DVisualPostReconciliationMoveHealthReport
            : null
      ),
    getCustom25DVisualPostReconciliationMoveReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationMoveReadinessReport",
        () =>
          typeof getCustom25DVisualPostReconciliationMoveReadinessReport ===
          "function"
            ? getCustom25DVisualPostReconciliationMoveReadinessReport
            : null
      ),
    getCustom25DVisualPostReconciliationMoveHealthCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPostReconciliationMoveHealthCloseoutReport",
        () =>
          typeof getCustom25DVisualPostReconciliationMoveHealthCloseoutReport ===
          "function"
            ? getCustom25DVisualPostReconciliationMoveHealthCloseoutReport
            : null
      ),
    getCustom25DVisualPassiveHelperLoadPathReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveHelperLoadPathReport",
        () =>
          typeof getCustom25DVisualPassiveHelperLoadPathReport === "function"
            ? getCustom25DVisualPassiveHelperLoadPathReport
            : null
      ),
    getCustom25DVisualPassiveHelperLoadPathCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualPassiveHelperLoadPathCloseoutReport",
        () =>
          typeof getCustom25DVisualPassiveHelperLoadPathCloseoutReport ===
          "function"
            ? getCustom25DVisualPassiveHelperLoadPathCloseoutReport
            : null
      ),
    getCustom25DVisualScriptSizeStatusReport:
      createNamespaceWrapper(
        "getCustom25DVisualScriptSizeStatusReport",
        () =>
          typeof getCustom25DVisualScriptSizeStatusReport === "function"
            ? getCustom25DVisualScriptSizeStatusReport
            : null
      ),
    getCustom25DVisualFutureModuleSplitPlanReport:
      createNamespaceWrapper(
        "getCustom25DVisualFutureModuleSplitPlanReport",
        () =>
          typeof getCustom25DVisualFutureModuleSplitPlanReport === "function"
            ? getCustom25DVisualFutureModuleSplitPlanReport
            : null
      ),
    getCustom25DVisualFutureModuleSplitReadinessReport:
      createNamespaceWrapper(
        "getCustom25DVisualFutureModuleSplitReadinessReport",
        () =>
          typeof getCustom25DVisualFutureModuleSplitReadinessReport === "function"
            ? getCustom25DVisualFutureModuleSplitReadinessReport
            : null
      ),
    getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport:
      createNamespaceWrapper(
        "getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport",
        () =>
          typeof getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport ===
          "function"
            ? getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport
            : null
      ),
    getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport:
      createNamespaceWrapper(
        "getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport",
        () =>
          typeof getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport ===
          "function"
            ? getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport
            : null
      ),
    createCustom25DVisualManualRendererSharedStateContainerShell:
      createNamespaceWrapper(
        "createCustom25DVisualManualRendererSharedStateContainerShell",
        () =>
          typeof createCustom25DVisualManualRendererSharedStateContainerShell === "function"
            ? createCustom25DVisualManualRendererSharedStateContainerShell
            : null
      )
  };

  window[namespaceKey] = namespace;
  globalThis[namespaceKey] = namespace;

  return {
    phase: 240,
    name: "custom-25d-visual-manual-test-helper-exposure",
    ok: true,
    allowed: true,
    exposed: true,
    reason: "local-dev-console-helper-namespace-exposed",
    failedRequirement: null,
    namespace: namespaceKey,
    hostname,
    localDevHost: true,
    manualOnly: true,
    browserConsoleOnly: true,
    helperPresence: {
      getCustom25DVisualFirstShapeTestContract:
        typeof namespace.getCustom25DVisualFirstShapeTestContract === "function",
      createCustom25DVisualFirstShapeManualTestLayer:
        typeof namespace.createCustom25DVisualFirstShapeManualTestLayer === "function",
      clearCustom25DVisualFirstShapeManualTestLayer:
        typeof namespace.clearCustom25DVisualFirstShapeManualTestLayer === "function",
      runCustom25DVisualFirstShapeManualTest:
        typeof namespace.runCustom25DVisualFirstShapeManualTest === "function",
      getCustom25DVisualFirstShapeManualTestVerificationReport:
        typeof namespace.getCustom25DVisualFirstShapeManualTestVerificationReport === "function",
      getCustom25DVisualFirstShapeManualTestCloseoutReport:
        typeof namespace.getCustom25DVisualFirstShapeManualTestCloseoutReport === "function",
      getCustom25DVisualInertRendererShellCloseoutReport:
        typeof namespace.getCustom25DVisualInertRendererShellCloseoutReport === "function",
      getCustom25DVisualRendererLifecyclePlanReport:
        typeof namespace.getCustom25DVisualRendererLifecyclePlanReport === "function",
      getCustom25DVisualRendererLifecycleContractReport:
        typeof namespace.getCustom25DVisualRendererLifecycleContractReport === "function",
      getCustom25DVisualManualInitializationGuardPlanReport:
        typeof namespace.getCustom25DVisualManualInitializationGuardPlanReport === "function",
      getCustom25DVisualManualInitializationGuardContractReport:
        typeof namespace.getCustom25DVisualManualInitializationGuardContractReport === "function",
      canInitializeCustom25DVisualRendererManually:
        typeof namespace.canInitializeCustom25DVisualRendererManually === "function",
      getCustom25DVisualManualInitializationGuardShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualInitializationGuardShellCloseoutReport === "function",
      getCustom25DVisualManualInitializationApprovalSignalPlanReport:
        typeof namespace.getCustom25DVisualManualInitializationApprovalSignalPlanReport === "function",
      getCustom25DVisualManualInitializationApprovalSignalContractReport:
        typeof namespace.getCustom25DVisualManualInitializationApprovalSignalContractReport === "function",
      evaluateCustom25DVisualManualInitializationSignals:
        typeof namespace.evaluateCustom25DVisualManualInitializationSignals === "function",
      getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport:
        typeof namespace.getCustom25DVisualManualInitializationSignalEvaluatorCloseoutReport === "function",
      getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport:
        typeof namespace.getCustom25DVisualManualInitializationGuardEvaluatorIntegrationPlanReport === "function",
      getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport:
        typeof namespace.getCustom25DVisualManualInitializationGuardEvaluatorIntegrationContractReport === "function",
      canUseCustom25DVisualManualRendererInitializationIntegratedGuard:
        typeof namespace.canUseCustom25DVisualManualRendererInitializationIntegratedGuard === "function",
      getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport:
        typeof namespace.getCustom25DVisualManualInitializationIntegratedGuardCloseoutReport === "function",
      getCustom25DVisualManualRendererInitializationShellPlanReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationShellPlanReport === "function",
      getCustom25DVisualManualRendererInitializationShellContractReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationShellContractReport === "function",
      initializeCustom25DVisualRendererManually:
        typeof namespace.initializeCustom25DVisualRendererManually === "function",
      getCustom25DVisualManualRendererInitializationShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationShellCloseoutReport === "function",
      getCustom25DVisualManualRendererStateContainerPlanReport:
        typeof namespace.getCustom25DVisualManualRendererStateContainerPlanReport === "function",
      getCustom25DVisualManualRendererStateContainerContractReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateContainerContractReadinessReport === "function",
      createCustom25DVisualManualRendererStateContainerShell:
        typeof namespace.createCustom25DVisualManualRendererStateContainerShell === "function",
      getCustom25DVisualManualRendererStateContainerShellVerificationReport:
        typeof namespace.getCustom25DVisualManualRendererStateContainerShellVerificationReport ===
        "function",
      getCustom25DVisualManualRendererStateMutationReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateMutationReadinessReport ===
        "function",
      getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateMutationReadinessCloseoutReport ===
        "function",
      applyCustom25DVisualManualRendererStateMutationShell:
        typeof namespace.applyCustom25DVisualManualRendererStateMutationShell ===
        "function",
      getCustom25DVisualManualRendererStateMutationShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateMutationShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateMutationShellSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererStateMutationShellSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererNextStateMutationReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererNextStateMutationReadinessReport ===
        "function",
      getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererNextStateMutationReadinessCloseoutReport ===
        "function",
      applyCustom25DVisualManualRendererSecondStateMutationShell:
        typeof namespace.applyCustom25DVisualManualRendererSecondStateMutationShell ===
        "function",
      getCustom25DVisualManualRendererSecondStateMutationCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSecondStateMutationCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererSecondStateMutationSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererStateFieldContractReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateFieldContractReadinessReport ===
        "function",
      getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateFieldContractReadinessCloseoutReport ===
        "function",
      applyCustom25DVisualManualRendererStateFieldContractShell:
        typeof namespace.applyCustom25DVisualManualRendererStateFieldContractShell ===
        "function",
      getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateFieldContractShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateFieldContractNextReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateFieldContractNextReadinessReport ===
        "function",
      applyCustom25DVisualManualRendererStateFieldContractValidationShell:
        typeof namespace.applyCustom25DVisualManualRendererStateFieldContractValidationShell ===
        "function",
      getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateFieldContractValidationCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPostFieldContractReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererPostFieldContractReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport:
        typeof namespace.getCustom25DVisualManualRendererPostFieldContractTransitionPlanReport ===
        "function",
      getCustom25DVisualManualRendererInstanceShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererInstanceShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport:
        typeof namespace.getCustom25DVisualManualRendererInstanceShellRiskBoundaryReport ===
        "function",
      createCustom25DVisualManualRendererInstanceShell:
        typeof namespace.createCustom25DVisualManualRendererInstanceShell ===
        "function",
      getCustom25DVisualManualRendererInstanceShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInstanceShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPostInstanceShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererPostInstanceShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport:
        typeof namespace.getCustom25DVisualManualRendererPostInstanceShellTransitionPlanReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleMethodShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleMethodShellRiskBoundaryReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleMethodShellTransitionPlanReport ===
        "function",
      createCustom25DVisualManualRendererLifecycleMethodShell:
        typeof namespace.createCustom25DVisualManualRendererLifecycleMethodShell ===
        "function",
      getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleMethodShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererPostLifecycleMethodShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport:
        typeof namespace.getCustom25DVisualManualRendererPostLifecycleMethodShellTransitionPlanReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleMethodReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleMethodRiskBoundaryReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleMethodTransitionPlanReport ===
        "function",
      createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation:
        typeof namespace.createCustom25DVisualManualRendererCallableLifecycleMethodPlaceholderFoundation ===
        "function",
      getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecyclePlaceholderCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecyclePlaceholderReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlPlanReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlShellPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlShellCreationCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlShellPostCreationReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualInitializeControlShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualStartControlShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualStopControlShellReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualAttachMapControlShellReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualDrawFrameControlShellReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualControlsCompleteCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgePlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionBridgeShellReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGatePlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateShellReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererCallableLifecycleManualExecutionGateContractShellReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateContainerShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateContainerShellCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateContainerCreationPlanReport === "function",
      getCustom25DVisualManualRendererSharedStateContainerPreflightReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateContainerPreflightReport === "function",
      getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateContainerShellCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateSchemaReviewReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSchemaReviewReport === "function",
      getCustom25DVisualManualRendererSharedStateReadAccessPlanReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateReadAccessPlanReport === "function",
      getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateReadAccessPlanCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateReadResultContractPlanReport === "function",
      getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateReadResultContractCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotReaderPlanCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotReaderReadinessReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshot:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshot === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotConsumerReadinessReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotConsumerContractReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotConsumerPlanCloseoutReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotConsumerReport === "function",
      getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSharedStateSnapshotConsumerCloseoutReport === "function",
      getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport:
        typeof namespace.getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanReport === "function",
      getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport:
        typeof namespace.getCustom25DVisualManualRendererSnapshotConsumerIntegrationContractReport === "function",
      getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererSnapshotConsumerIntegrationPlanCloseoutReport === "function",
      getCustom25DVisualManualRendererIntegrationShellPlanReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellPlanReport ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellContractReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellContractReport ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellPlanCloseoutReport ===
        "function",
      createCustom25DVisualManualRendererIntegrationShell:
        typeof namespace.createCustom25DVisualManualRendererIntegrationShell ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellStatusReaderPlanReport ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellStatusReaderContractReport ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellStatus:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellStatus ===
        "function",
      getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererIntegrationShellStatusCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererLifecyclePlanReport:
        typeof namespace.getCustom25DVisualManualRendererLifecyclePlanReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleContractReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleContractReport ===
        "function",
      getCustom25DVisualManualRendererLifecyclePlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererLifecyclePlanCloseoutReport ===
        "function",
      createCustom25DVisualManualRendererLifecycleShell:
        typeof namespace.createCustom25DVisualManualRendererLifecycleShell ===
        "function",
      getCustom25DVisualManualRendererLifecycleShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleShellStatusCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererObjectPlanReport:
        typeof namespace.getCustom25DVisualManualRendererObjectPlanReport ===
        "function",
      getCustom25DVisualManualRendererObjectContractReport:
        typeof namespace.getCustom25DVisualManualRendererObjectContractReport ===
        "function",
      getCustom25DVisualManualRendererObjectPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererObjectPlanCloseoutReport ===
        "function",
      createCustom25DVisualManualRendererObjectShell:
        typeof namespace.createCustom25DVisualManualRendererObjectShell ===
        "function",
      getCustom25DVisualManualRendererObjectShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererObjectShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererObjectShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererObjectShellStatusCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShellStatusCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererObjectShapePlanReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShapePlanReport ===
        "function",
      getCustom25DVisualManualRendererObjectShapeContractReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShapeContractReport ===
        "function",
      getCustom25DVisualManualRendererObjectShapePlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShapePlanCloseoutReport ===
        "function",
      createCustom25DVisualManualRendererObjectShapeShell:
        typeof namespace.createCustom25DVisualManualRendererObjectShapeShell ===
        "function",
      getCustom25DVisualManualRendererObjectShapeShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShapeShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererObjectShapeShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShapeShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererObjectShapeShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererObjectShapeShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererMetadataPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataPlanReport ===
        "function",
      createCustom25DVisualManualRendererMetadataShell:
        typeof namespace.createCustom25DVisualManualRendererMetadataShell ===
        "function",
      getCustom25DVisualManualRendererMetadataShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererMetadataShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererMetadataContractPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataContractPlanReport ===
        "function",
      createCustom25DVisualManualRendererMetadataContractShell:
        typeof namespace.createCustom25DVisualManualRendererMetadataContractShell ===
        "function",
      getCustom25DVisualManualRendererMetadataContractShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataContractShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationPlanReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationContractReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationContractReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationPlanCloseoutReport ===
        "function",
      createCustom25DVisualManualRendererMetadataValidationShell:
        typeof namespace.createCustom25DVisualManualRendererMetadataValidationShell ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultPlanReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationStatusResultPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationStatusResultPlanSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultContractPlanReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultContractReadinessReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultContractPlanCloseoutReport ===
        "function",
      createCustom25DVisualManualRendererMetadataValidationResultContractShell:
        typeof namespace.createCustom25DVisualManualRendererMetadataValidationResultContractShell ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultContractShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultContractShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultContractShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellPlanReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellPlanSelfReviewReport ===
        "function",
      createCustom25DVisualManualRendererMetadataValidationResultShell:
        typeof namespace.createCustom25DVisualManualRendererMetadataValidationResultShell ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellStatusReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellReadinessReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultReviewPlanReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultReviewPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultReviewPlanSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellReviewReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellReviewCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererMetadataValidationResultShellReviewSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererNonRuntimeValidationPlanReport:
        typeof namespace.getCustom25DVisualManualRendererNonRuntimeValidationPlanReport ===
        "function",
      getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererNonRuntimeValidationReadinessReport ===
        "function",
      getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererNonRuntimeValidationPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererNonRuntimeValidationPlanSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveNonRuntimeValidationReviewSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistPlanReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistPlanSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationChecklistSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryPlanReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryPlanSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummaryCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummarySequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationSummarySequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestonePlanReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestoneReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestonePlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestonePlanSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestoneReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestoneReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestoneCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPassiveValidationMilestoneSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualHelperDensityMaintenanceStatusReport:
        typeof namespace.getCustom25DVisualHelperDensityMaintenanceStatusReport ===
        "function",
      getCustom25DVisualFutureSafeExtractionCandidateReport:
        typeof namespace.getCustom25DVisualFutureSafeExtractionCandidateReport ===
        "function",
      getCustom25DVisualHelperDensityMaintenanceReadinessReport:
        typeof namespace.getCustom25DVisualHelperDensityMaintenanceReadinessReport ===
        "function",
      getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualHelperDensityMaintenanceReadinessCloseoutReport ===
        "function",
      getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport:
        typeof namespace.getCustom25DVisualHelperDensityMaintenanceReadinessSelfReviewReport ===
        "function",
      getCustom25DVisualPassiveExtractionManifestPlanReport:
        typeof namespace.getCustom25DVisualPassiveExtractionManifestPlanReport ===
        "function",
      getCustom25DVisualPassiveExtractionManifestReadinessReport:
        typeof namespace.getCustom25DVisualPassiveExtractionManifestReadinessReport ===
        "function",
      getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionManifestPlanCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionManifestPlanSequenceCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualPassiveExtractionManifestPlanSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightPlanReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightPlanReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightReadinessReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightReadinessReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightPlanCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightPlanSequenceCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightPlanSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightSequenceCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualPassiveExtractionPreflightSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualFirstPassiveExtractionCandidatePlanReport:
        typeof namespace.getCustom25DVisualFirstPassiveExtractionCandidatePlanReport ===
        "function",
      getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport:
        typeof namespace.getCustom25DVisualFirstPassiveExtractionCandidateReadinessReport ===
        "function",
      getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport:
        typeof namespace.getCustom25DVisualFirstPassiveExtractionCandidatePlanCloseoutReport ===
        "function",
      getCustom25DVisualFirstPassiveHelperMoveCloseoutReport:
        typeof namespace.getCustom25DVisualFirstPassiveHelperMoveCloseoutReport ===
        "function",
      getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport:
        typeof namespace.getCustom25DVisualFirstPassiveHelperMoveSelfReviewReport ===
        "function",
      getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualFirstPassiveHelperMoveSequenceCloseoutReport ===
        "function",
      getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualFirstPassiveHelperMoveSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualSecondPassiveHelperMoveCloseoutReport:
        typeof namespace.getCustom25DVisualSecondPassiveHelperMoveCloseoutReport ===
        "function",
      getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport:
        typeof namespace.getCustom25DVisualSecondPassiveHelperMoveSelfReviewReport ===
        "function",
      getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualSecondPassiveHelperMoveSequenceCloseoutReport ===
        "function",
      getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualSecondPassiveHelperMoveSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualBundledPassiveHelperMoveCloseoutReport:
        typeof namespace.getCustom25DVisualBundledPassiveHelperMoveCloseoutReport ===
        "function",
      getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport:
        typeof namespace.getCustom25DVisualBundledPassiveHelperMoveSelfReviewReport ===
        "function",
      getCustom25DVisualBundledPassiveMoveHealthReport:
        typeof namespace.getCustom25DVisualBundledPassiveMoveHealthReport ===
        "function",
      getCustom25DVisualBundledPassiveMoveReadinessReport:
        typeof namespace.getCustom25DVisualBundledPassiveMoveReadinessReport ===
        "function",
      getCustom25DVisualBundledPassiveMoveHealthCloseoutReport:
        typeof namespace.getCustom25DVisualBundledPassiveMoveHealthCloseoutReport ===
        "function",
      getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport:
        typeof namespace.getCustom25DVisualFinalBundledPassiveHelperMoveCloseoutReport ===
        "function",
      getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport:
        typeof namespace.getCustom25DVisualFinalBundledPassiveHelperMoveSelfReviewReport ===
        "function",
      getCustom25DVisualPassiveExtractionReassessmentReport:
        typeof namespace.getCustom25DVisualPassiveExtractionReassessmentReport ===
        "function",
      getCustom25DVisualPassiveExtractionReassessmentReadinessReport:
        typeof namespace.getCustom25DVisualPassiveExtractionReassessmentReadinessReport ===
        "function",
      getCustom25DVisualPassiveExtractionReassessmentCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionReassessmentCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveExtractionSequenceCloseoutReport ===
        "function",
      getCustom25DVisualPassiveExtractionSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualPassiveExtractionSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport:
        typeof namespace.getCustom25DVisualPassiveExtractionSequencePauseRecommendationReport ===
        "function",
      getCustom25DVisualRendererPathResumeReadinessReport:
        typeof namespace.getCustom25DVisualRendererPathResumeReadinessReport ===
        "function",
      getCustom25DVisualRendererPathResumeNextStepReport:
        typeof namespace.getCustom25DVisualRendererPathResumeNextStepReport ===
        "function",
      getCustom25DVisualRendererPathResumeCloseoutReport:
        typeof namespace.getCustom25DVisualRendererPathResumeCloseoutReport ===
        "function",
      getCustom25DVisualRendererStateContainerReadinessReport:
        typeof namespace.getCustom25DVisualRendererStateContainerReadinessReport ===
        "function",
      getCustom25DVisualRendererStateContainerNextStepReport:
        typeof namespace.getCustom25DVisualRendererStateContainerNextStepReport ===
        "function",
      getCustom25DVisualRendererStateContainerReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualRendererStateContainerReadinessCloseoutReport ===
        "function",
      getCustom25DVisualRendererStateContainerPlanCloseoutReport:
        typeof namespace.getCustom25DVisualRendererStateContainerPlanCloseoutReport ===
        "function",
      getCustom25DVisualRendererStateContainerPlanSelfReviewReport:
        typeof namespace.getCustom25DVisualRendererStateContainerPlanSelfReviewReport ===
        "function",
      getCustom25DVisualRendererStateContainerPlanNextStepReport:
        typeof namespace.getCustom25DVisualRendererStateContainerPlanNextStepReport ===
        "function",
      getCustom25DVisualInertStateContainerContractPlanReport:
        typeof namespace.getCustom25DVisualInertStateContainerContractPlanReport ===
        "function",
      getCustom25DVisualInertStateContainerContractReadinessReport:
        typeof namespace.getCustom25DVisualInertStateContainerContractReadinessReport ===
        "function",
      getCustom25DVisualInertStateContainerContractPlanCloseoutReport:
        typeof namespace.getCustom25DVisualInertStateContainerContractPlanCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateSchemaReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateSchemaReadinessReport ===
        "function",
      getCustom25DVisualManualRendererStateSchemaNextStepReport:
        typeof namespace.getCustom25DVisualManualRendererStateSchemaNextStepReport ===
        "function",
      getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateSchemaReadinessCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateSchemaCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateSchemaCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateSchemaSelfReviewReport:
        typeof namespace.getCustom25DVisualManualRendererStateSchemaSelfReviewReport ===
        "function",
      getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport:
        typeof namespace.getCustom25DVisualManualRendererStateSchemaCloseoutNextStepReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationReadinessReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationReadinessReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationGateReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationGateReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationReadinessCloseoutReport ===
        "function",
      createCustom25DVisualInertManualRendererStateContainer:
        typeof namespace.createCustom25DVisualInertManualRendererStateContainer ===
        "function",
      getCustom25DVisualInertStateContainerCreationReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationCloseoutReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationCloseoutReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationSequenceCloseoutReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationSequenceSelfReviewReport ===
        "function",
      getCustom25DVisualInertStateContainerManualTestReadinessReport:
        typeof namespace.getCustom25DVisualInertStateContainerManualTestReadinessReport ===
        "function",
      runCustom25DVisualInertStateContainerManualConsoleTest:
        typeof namespace.runCustom25DVisualInertStateContainerManualConsoleTest ===
        "function",
      getCustom25DVisualInertStateContainerManualConsoleTestReport:
        typeof namespace.getCustom25DVisualInertStateContainerManualConsoleTestReport ===
        "function",
      getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport:
        typeof namespace.getCustom25DVisualInertStateContainerManualConsoleTestCloseoutReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationPreflightDiagnosticReport ===
        "function",
      getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport:
        typeof namespace.getCustom25DVisualInertStateContainerCreationSafariSafeCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererStateFieldsReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateFieldsReadinessReport ===
        "function",
      getCustom25DVisualManualRendererInertStateFieldsReport:
        typeof namespace.getCustom25DVisualManualRendererInertStateFieldsReport ===
        "function",
      getCustom25DVisualManualRendererInertStateFieldsCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInertStateFieldsCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleReadinessReport ===
        "function",
      getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererLifecycleStateFieldsReadinessReport ===
        "function",
      getCustom25DVisualManualRendererInertLifecycleStateFieldsReport:
        typeof namespace.getCustom25DVisualManualRendererInertLifecycleStateFieldsReport ===
        "function",
      getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInertLifecycleStateFieldsCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererInitializationReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationReadinessReport ===
        "function",
      initializeCustom25DVisualManualRendererShell:
        typeof namespace.initializeCustom25DVisualManualRendererShell ===
        "function",
      getCustom25DVisualManualRendererInitializationShellReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationShellReport ===
        "function",
      getCustom25DVisualManualRendererInitializationShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererInitializationContractReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationContractReadinessReport ===
        "function",
      getCustom25DVisualManualRendererInitializationContractShell:
        typeof namespace.getCustom25DVisualManualRendererInitializationContractShell ===
        "function",
      getCustom25DVisualManualRendererInitializationContractShellReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationContractShellReport ===
        "function",
      getCustom25DVisualManualRendererInitializationContractShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationContractShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererInitializationGuardReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationGuardReadinessReport ===
        "function",
      canInitializeCustom25DVisualManualRenderer:
        typeof namespace.canInitializeCustom25DVisualManualRenderer ===
        "function",
      getCustom25DVisualManualRendererInitializationGuardShellReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationGuardShellReport ===
        "function",
      getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationGuardShellCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererInitializationPreflightReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationPreflightReadinessReport ===
        "function",
      getCustom25DVisualManualRendererInitializationPreflightReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationPreflightReport ===
        "function",
      getCustom25DVisualManualRendererInitializationPreflightShellReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationPreflightShellReport ===
        "function",
      getCustom25DVisualManualRendererInitializationPreflightCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationPreflightCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererInitializationGateReviewReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationGateReviewReport ===
        "function",
      getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationAuthorizationChecklistReport ===
        "function",
      getCustom25DVisualManualRendererInitializationLockReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationLockReport ===
        "function",
      getCustom25DVisualManualRendererInitializationSequenceSummaryReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationSequenceSummaryReport ===
        "function",
      getCustom25DVisualManualRendererInitializationSequenceCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationSequenceCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererInitializationHandoffPlanReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationHandoffPlanReport ===
        "function",
      getCustom25DVisualManualRendererInitializationNextStepReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererInitializationNextStepReadinessReport ===
        "function",
      getCustom25DVisualManualRendererPlanningBoundaryReport:
        typeof namespace.getCustom25DVisualManualRendererPlanningBoundaryReport ===
        "function",
      getCustom25DVisualManualRendererPlanningRiskReviewReport:
        typeof namespace.getCustom25DVisualManualRendererPlanningRiskReviewReport ===
        "function",
      getCustom25DVisualManualRendererPlanningRiskCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererPlanningRiskCloseoutReport ===
        "function",
      getCustom25DVisualManualRendererPlanningHandoffSummaryReport:
        typeof namespace.getCustom25DVisualManualRendererPlanningHandoffSummaryReport ===
        "function",
      getCustom25DVisualManualRendererStateCreationReadinessReport:
        typeof namespace.getCustom25DVisualManualRendererStateCreationReadinessReport ===
        "function",
      getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport:
        typeof namespace.getCustom25DVisualManualRendererStateCreationReadinessCloseoutReport ===
        "function",
      getCustom25DVisualExtractedHelperReconciliationReport:
        typeof namespace.getCustom25DVisualExtractedHelperReconciliationReport ===
        "function",
      getCustom25DVisualExtractedHelperReconciliationReadinessReport:
        typeof namespace.getCustom25DVisualExtractedHelperReconciliationReadinessReport ===
        "function",
      getCustom25DVisualExtractedHelperReconciliationCloseoutReport:
        typeof namespace.getCustom25DVisualExtractedHelperReconciliationCloseoutReport ===
        "function",
      getCustom25DVisualExtractedHelperReconciliationAcceptanceReport:
        typeof namespace.getCustom25DVisualExtractedHelperReconciliationAcceptanceReport ===
        "function",
      getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport:
        typeof namespace.getCustom25DVisualExtractedHelperReconciliationAcceptanceReadinessReport ===
        "function",
      getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport:
        typeof namespace.getCustom25DVisualExtractedHelperReconciliationAcceptanceCloseoutReport ===
        "function",
      getCustom25DVisualExtractedPassiveReportsFileHealthReport:
        typeof namespace.getCustom25DVisualExtractedPassiveReportsFileHealthReport ===
        "function",
      getCustom25DVisualExtractedPassiveReportsFileReadinessReport:
        typeof namespace.getCustom25DVisualExtractedPassiveReportsFileReadinessReport ===
        "function",
      getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport:
        typeof namespace.getCustom25DVisualExtractedPassiveReportsFileHealthCloseoutReport ===
        "function",
      getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport:
        typeof namespace.getCustom25DVisualPostReconciliationBundledPassiveHelperMoveCloseoutReport ===
        "function",
      getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport:
        typeof namespace.getCustom25DVisualPostReconciliationBundledPassiveHelperMoveSelfReviewReport ===
        "function",
      getCustom25DVisualPostReconciliationMoveHealthReport:
        typeof namespace.getCustom25DVisualPostReconciliationMoveHealthReport ===
        "function",
      getCustom25DVisualPostReconciliationMoveReadinessReport:
        typeof namespace.getCustom25DVisualPostReconciliationMoveReadinessReport ===
        "function",
      getCustom25DVisualPostReconciliationMoveHealthCloseoutReport:
        typeof namespace.getCustom25DVisualPostReconciliationMoveHealthCloseoutReport ===
        "function",
      getCustom25DVisualPassiveHelperLoadPathReport:
        typeof namespace.getCustom25DVisualPassiveHelperLoadPathReport ===
        "function",
      getCustom25DVisualPassiveHelperLoadPathCloseoutReport:
        typeof namespace.getCustom25DVisualPassiveHelperLoadPathCloseoutReport ===
        "function",
      getCustom25DVisualScriptSizeStatusReport:
        typeof namespace.getCustom25DVisualScriptSizeStatusReport ===
        "function",
      getCustom25DVisualFutureModuleSplitPlanReport:
        typeof namespace.getCustom25DVisualFutureModuleSplitPlanReport ===
        "function",
      getCustom25DVisualFutureModuleSplitReadinessReport:
        typeof namespace.getCustom25DVisualFutureModuleSplitReadinessReport ===
        "function",
      getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport:
        typeof namespace.getCustom25DVisualScriptSizeModuleSplitPlanCloseoutReport ===
        "function",
      getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport:
        typeof namespace.getCustom25DVisualScriptSizeModuleSplitPlanSelfReviewReport ===
        "function",
      createCustom25DVisualManualRendererSharedStateContainerShell:
        typeof namespace.createCustom25DVisualManualRendererSharedStateContainerShell === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    namespacedOnly: true,
    invokedHelpers: false,
    domCreated: false,
    domMutated: false,
    drewAnything: false,
    cleanupExecuted: false,
    storageUsed: false,
    networkUsed: false,
    preservedSystems: {
      existingLeafletMapBehavior: true,
      osmBehavior: true,
      gameplay: true,
      pins: true,
      playerMarker: true,
      captureRadius: true,
      ui: true,
      backend: true
    }
  };
}

if (
  typeof window !== "undefined" &&
  window &&
  window.location &&
  (window.location.hostname === "localhost" ||
    window.location.hostname === "127.0.0.1" ||
    window.location.hostname === "0.0.0.0" ||
    window.location.hostname === "::1")
) {
  exposeCustom25DVisualManualTestHelpersForLocalDevConsole({
    manual: true,
    developerIntent: true,
    localDevOnly: true,
    browserConsoleOnly: true
  });
}

function clearCustom25DVisualFirstManualVisibleTestLayer(options = {}) {
  const container =
    options.mapContainer ||
    options.container ||
    (options.map && typeof options.map.getContainer === "function"
      ? options.map.getContainer()
      : null);
  const doc =
    (container && container.ownerDocument) || (typeof document !== "undefined" ? document : null);

  if (!doc || typeof doc.querySelectorAll !== "function") {
    return {
      phase: 212,
      cleared: false,
      noOp: true,
      safeToRepeat: true,
      touchesMapContainerOnlyForOwnTestElement: true,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      reason: "no-manual-visible-test-layer-document-context"
    };
  }

  const scope =
    container && typeof container.querySelectorAll === "function" ? container : doc;
  const elements = scope.querySelectorAll(
    '[data-custom-25d-visible-test="true"], [data-custom25d-manual-visible-test-layer="true"]'
  );

  if (!elements.length) {
    return {
      phase: 212,
      cleared: false,
      noOp: true,
      safeToRepeat: true,
      touchesMapContainerOnlyForOwnTestElement: true,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      reason: "no-manual-visible-test-layer-to-clear"
    };
  }

  elements.forEach((element) => {
    if (element && element.parentNode) {
      element.parentNode.removeChild(element);
    }
  });

  return {
    phase: 212,
    cleared: true,
    noOp: false,
    safeToRepeat: true,
    touchesMapContainerOnlyForOwnTestElement: true,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    reason: "manual-visible-test-layer-cleared"
  };
}

function getCustom25DVisualFirstManualVisibleTestReadinessReport(options = {}) {
  const visibleTestGuard =
    typeof canCreateCustom25DVisualManualVisibleTestLayer === "function"
      ? canCreateCustom25DVisualManualVisibleTestLayer(options)
      : {
          ok: false,
          allowed: false,
          reason: "manual-visible-test-layer-guard-unavailable",
          failedRequirement: "guardUnavailable"
        };
  const cleanupReport =
    typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
      ? clearCustom25DVisualFirstManualVisibleTestLayer(options)
      : {
          phase: 212,
          cleared: false,
          noOp: true,
          safeToRepeat: false,
          reason: "manual-visible-test-layer-cleanup-unavailable"
        };

  return {
    phase: 213,
    name: "custom-25d-visual-first-manual-visible-test-readiness-report",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason: visibleTestGuard.reason || "custom-25d-map-disabled",
    passive: true,
    reportOnly: true,
    readinessOnly: true,
    manualTestGuidanceOnly: true,
    createsVisibleTestLayer: false,
    callsCreateVisibleTestLayer: false,
    callsCreateLifecycleShell: false,
    callsCreateAttachmentSmokeShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    automaticInvocationAdded: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    visibleHelperPresent:
      typeof createCustom25DVisualFirstManualVisibleTestLayer === "function",
    cleanupHelperPresent:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function",
    guardHelperPresent:
      typeof canCreateCustom25DVisualManualVisibleTestLayer === "function",
    startupWired: false,
    defaultVisible: false,
    manualTestPossibleOnlyWithGuard: true,
    safetyFlagsRemainFalse: true,
    visibleBehaviorApprovedByDefault: false,
    requiredManualOptions: {
      manual: true,
      developerIntent: true,
      allowVisibleTestLayer: true,
      allowManualMapAttachment: true,
      allowTinyTestOnly: true,
      cleanupVerified: true,
      attachmentSmokeShellReady: true,
      preserveExistingMap: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      noStartupWiring: true,
      noBackendChanges: true
    },
    cleanupStatus: {
      noOp: cleanupReport.noOp === true,
      safeToRepeat: cleanupReport.safeToRepeat === true,
      touchesMapContainerOnlyForOwnTestElement:
        cleanupReport.touchesMapContainerOnlyForOwnTestElement === true
    },
    blockedReasons: [
      "custom-25d-map-disabled",
      "readiness-report-passive-only",
      "no-automatic-visible-behavior-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved-by-default",
      "no-drawing-approved-by-default",
      "no-gameplay-changes-approved",
      "no-osm-changes-approved",
      "no-pin-changes-approved",
      "no-player-marker-changes-approved",
      "no-capture-radius-changes-approved",
      "no-backend-changes-approved"
    ],
    reviewedHelpers: {
      visibleTestGuard:
        typeof canCreateCustom25DVisualManualVisibleTestLayer === "function",
      visibleTestCleanup:
        typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    notes: [
      "Passive/report-only/readiness-only/manual-test-guidance-only helper.",
      "Reports whether a manual browser-console test would be possible and which options would be required.",
      "Does not create the visible test layer, wire startup, or approve visible behavior by default."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestOverridePlan(options = {}) {
  const visibleTestReadiness =
    typeof getCustom25DVisualFirstManualVisibleTestReadinessReport === "function"
      ? getCustom25DVisualFirstManualVisibleTestReadinessReport(options)
      : {
          ok: false,
          allowed: false,
          blockedReason: "manual-visible-test-readiness-report-unavailable"
        };
  const visibleTestGuard =
    typeof canCreateCustom25DVisualManualVisibleTestLayer === "function"
      ? canCreateCustom25DVisualManualVisibleTestLayer(options)
      : {
          ok: false,
          allowed: false,
          reason: "manual-visible-test-layer-guard-unavailable"
        };

  return {
    phase: 214,
    name: "custom-25d-visual-local-dev-visible-test-override-plan",
    ok: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    blockedReason:
      visibleTestGuard.reason || visibleTestReadiness.blockedReason || "custom-25d-map-disabled",
    passive: true,
    reportOnly: true,
    planningOnly: true,
    localDevTestOnly: true,
    createsVisibleTestLayer: false,
    callsCreateVisibleTestLayer: false,
    callsCreateLifecycleShell: false,
    callsCreateAttachmentSmokeShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    automaticInvocationAdded: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    localDevOverrideRequirements: {
      localDevOnly: true,
      browserConsoleOnly: true,
      manual: true,
      developerIntent: true,
      allowVisibleTestLayer: true,
      allowManualMapAttachment: true,
      allowTinyTestOnly: true,
      cleanupVerified: true,
      attachmentSmokeShellReady: true,
      preserveExistingMap: true,
      preserveGameplayOverlays: true,
      preserveOSMBehavior: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      noStartupWiring: true,
      noBackendChanges: true,
      noPersistence: true,
      noAutomaticInvocation: true
    },
    localDevOverridePlanned: true,
    localDevOverrideImplemented: false,
    defaultVisible: false,
    startupWired: false,
    productionBehaviorChanged: false,
    normalAppBehaviorChanged: false,
    safetyFlagsRemainFalse: true,
    visibleBehaviorApprovedByDefault: false,
    blockedReasons: [
      "custom-25d-map-disabled",
      "local-dev-override-planning-only",
      "no-automatic-visible-behavior-approved",
      "no-startup-wiring-approved",
      "no-map-attachment-approved-by-default",
      "no-drawing-approved-by-default",
      "no-gameplay-changes-approved",
      "no-osm-changes-approved",
      "no-pin-changes-approved",
      "no-player-marker-changes-approved",
      "no-capture-radius-changes-approved",
      "no-backend-changes-approved"
    ],
    reviewedHelpers: {
      visibleTestReadiness:
        typeof getCustom25DVisualFirstManualVisibleTestReadinessReport === "function",
      visibleTestGuard:
        typeof canCreateCustom25DVisualManualVisibleTestLayer === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    notes: [
      "Passive/report-only/planning-only/local-dev-test-only helper.",
      "Plans the safe local-dev-only override conditions for a future manual browser-console visible test.",
      "Does not create the visible test layer, flip safety flags, wire startup, or approve visible behavior by default."
    ]
  };
}

function canUseCustom25DVisualLocalDevVisibleTestOverride(options = {}) {
  const localDevOverridePlan =
    typeof getCustom25DVisualLocalDevVisibleTestOverridePlan === "function"
      ? getCustom25DVisualLocalDevVisibleTestOverridePlan(options)
      : {
          ok: false,
          allowed: false,
          blockedReason: "local-dev-visible-test-override-plan-unavailable"
        };
  const visibleTestGuard =
    typeof canCreateCustom25DVisualManualVisibleTestLayer === "function"
      ? canCreateCustom25DVisualManualVisibleTestLayer(options)
      : {
          ok: false,
          allowed: false,
          reason: "manual-visible-test-layer-guard-unavailable"
        };

  const requirements = [
    {
      key: "localDevOnly",
      label: "options.localDevOnly === true",
      passed: options.localDevOnly === true,
      reason: "local-dev-only-required"
    },
    {
      key: "browserConsoleOnly",
      label: "options.browserConsoleOnly === true",
      passed: options.browserConsoleOnly === true,
      reason: "browser-console-only-required"
    },
    {
      key: "manual",
      label: "options.manual === true",
      passed: options.manual === true,
      reason: "manual-flag-required"
    },
    {
      key: "developerIntent",
      label: "options.developerIntent === true",
      passed: options.developerIntent === true,
      reason: "developer-intent-required"
    },
    {
      key: "allowLocalDevVisibleTestOverride",
      label: "options.allowLocalDevVisibleTestOverride === true",
      passed: options.allowLocalDevVisibleTestOverride === true,
      reason: "local-dev-visible-test-override-not-allowed"
    },
    {
      key: "allowVisibleTestLayer",
      label: "options.allowVisibleTestLayer === true",
      passed: options.allowVisibleTestLayer === true,
      reason: "visible-test-layer-not-allowed"
    },
    {
      key: "allowManualMapAttachment",
      label: "options.allowManualMapAttachment === true",
      passed: options.allowManualMapAttachment === true,
      reason: "manual-map-attachment-required"
    },
    {
      key: "allowTinyTestOnly",
      label: "options.allowTinyTestOnly === true",
      passed: options.allowTinyTestOnly === true,
      reason: "tiny-test-only-required"
    },
    {
      key: "cleanupVerified",
      label: "options.cleanupVerified === true",
      passed: options.cleanupVerified === true,
      reason: "cleanup-verification-required"
    },
    {
      key: "attachmentSmokeShellReady",
      label: "options.attachmentSmokeShellReady === true",
      passed: options.attachmentSmokeShellReady === true,
      reason: "attachment-smoke-shell-readiness-required"
    },
    {
      key: "preserveExistingMap",
      label: "options.preserveExistingMap === true",
      passed: options.preserveExistingMap === true,
      reason: "existing-map-preservation-required"
    },
    {
      key: "preserveGameplayOverlays",
      label: "options.preserveGameplayOverlays === true",
      passed: options.preserveGameplayOverlays === true,
      reason: "gameplay-overlay-preservation-required"
    },
    {
      key: "preserveOSMBehavior",
      label: "options.preserveOSMBehavior === true",
      passed: options.preserveOSMBehavior === true,
      reason: "osm-behavior-preservation-required"
    },
    {
      key: "preservePins",
      label: "options.preservePins === true",
      passed: options.preservePins === true,
      reason: "pin-preservation-required"
    },
    {
      key: "preservePlayerMarker",
      label: "options.preservePlayerMarker === true",
      passed: options.preservePlayerMarker === true,
      reason: "player-marker-preservation-required"
    },
    {
      key: "preserveCaptureRadius",
      label: "options.preserveCaptureRadius === true",
      passed: options.preserveCaptureRadius === true,
      reason: "capture-radius-preservation-required"
    },
    {
      key: "noStartupWiring",
      label: "options.noStartupWiring === true",
      passed: options.noStartupWiring === true,
      reason: "no-startup-wiring-required"
    },
    {
      key: "noBackendChanges",
      label: "options.noBackendChanges === true",
      passed: options.noBackendChanges === true,
      reason: "no-backend-changes-required"
    },
    {
      key: "noPersistence",
      label: "options.noPersistence === true",
      passed: options.noPersistence === true,
      reason: "no-persistence-required"
    },
    {
      key: "noAutomaticInvocation",
      label: "options.noAutomaticInvocation === true",
      passed: options.noAutomaticInvocation === true,
      reason: "no-automatic-invocation-required"
    },
    {
      key: "acceptsTemporaryLocalDevVisual",
      label: "options.acceptsTemporaryLocalDevVisual === true",
      passed: options.acceptsTemporaryLocalDevVisual === true,
      reason: "temporary-local-dev-visual-acceptance-required"
    }
  ];

  const firstFailure = requirements.find((requirement) => !requirement.passed) || null;
  const allowed = firstFailure === null;

  return {
    phase: 215,
    name: "custom-25d-visual-local-dev-visible-test-override-guard",
    ok: true,
    allowed,
    ready: false,
    reason:
      allowed ? "local-dev-visible-test-override-guard-passed-evaluation-only" : firstFailure.reason,
    failedRequirement: firstFailure ? firstFailure.key : null,
    guardOnly: true,
    evaluatorOnly: true,
    localDevOnly: true,
    browserConsoleTestOnly: true,
    createsVisibleTestLayer: false,
    callsCreateVisibleTestLayer: false,
    callsCreateLifecycleShell: false,
    callsCreateAttachmentSmokeShell: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsInitializationState: false,
    initializesRenderer: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    wiresStartup: false,
    automaticInvocationAdded: false,
    enforcesRuntimeSchema: false,
    addsRuntimeValidators: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    productionFlagsRemainFalse: true,
    defaultVisible: false,
    startupWired: false,
    productionBehaviorChanged: false,
    normalAppBehaviorChanged: false,
    requirements,
    blockedReasons: [
      "custom-25d-map-disabled",
      "local-dev-override-guard-evaluator-only",
      "no-automatic-visible-behavior-approved",
      "no-startup-wiring-approved",
      "no-production-flag-changes-approved",
      "no-map-attachment-approved-by-default",
      "no-drawing-approved-by-default",
      "no-gameplay-changes-approved",
      "no-osm-changes-approved",
      "no-pin-changes-approved",
      "no-player-marker-changes-approved",
      "no-capture-radius-changes-approved",
      "no-backend-changes-approved"
    ],
    reviewedHelpers: {
      localDevOverridePlan:
        typeof getCustom25DVisualLocalDevVisibleTestOverridePlan === "function",
      visibleTestGuard:
        typeof canCreateCustom25DVisualManualVisibleTestLayer === "function"
    },
    safetyFlags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    notes: [
      "Guard-only/evaluator-only/local-dev-only/browser-console-test-only helper.",
      "Evaluates whether a future local-dev manual visible test override would be allowed.",
      "Does not create, attach, draw, approve production behavior, or change default app behavior."
    ]
  };
}

function runCustom25DVisualLocalDevVisibleTest(options = {}) {
  const overrideGuard =
    typeof canUseCustom25DVisualLocalDevVisibleTestOverride === "function"
      ? canUseCustom25DVisualLocalDevVisibleTestOverride(options)
      : {
          ok: false,
          allowed: false,
          reason: "local-dev-visible-test-override-guard-unavailable",
          failedRequirement: "guardUnavailable"
        };

  if (!overrideGuard.allowed) {
    return {
      phase: 216,
      name: "custom-25d-visual-local-dev-visible-test-runner",
      ok: true,
      allowed: false,
      ready: false,
      ran: false,
      created: false,
      visible: false,
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      startupWired: false,
      automaticInvocationAdded: false,
      productionFlagsChanged: false,
      normalAppBehaviorChanged: false,
      gameplayChanged: false,
      osmChanged: false,
      pinsChanged: false,
      playerMarkerChanged: false,
      captureRadiusChanged: false,
      backendChanged: false,
      cleanupAvailable:
        typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function",
      reason: overrideGuard.reason || "local-dev-visible-test-override-not-allowed",
      failedRequirement: overrideGuard.failedRequirement || "localDevOnly",
      notes: [
        "Blocked by local-dev override guard.",
        "Did not call the manual visible test layer creator.",
        "Default no-option behavior remains blocked and invisible."
      ]
    };
  }

  const runnerOptions = {
    localDevOnly: options.localDevOnly === true,
    browserConsoleOnly: options.browserConsoleOnly === true,
    manual: options.manual === true,
    developerIntent: options.developerIntent === true,
    allowLocalDevVisibleTestOverride: options.allowLocalDevVisibleTestOverride === true,
    allowVisibleTestLayer: options.allowVisibleTestLayer === true,
    allowManualMapAttachment: options.allowManualMapAttachment === true,
    allowTinyTestOnly: options.allowTinyTestOnly === true,
    cleanupVerified: options.cleanupVerified === true,
    attachmentSmokeShellReady: options.attachmentSmokeShellReady === true,
    preserveExistingMap: options.preserveExistingMap === true,
    preserveGameplayOverlays: options.preserveGameplayOverlays === true,
    preserveOSMBehavior: options.preserveOSMBehavior === true,
    preservePins: options.preservePins === true,
    preservePlayerMarker: options.preservePlayerMarker === true,
    preserveCaptureRadius: options.preserveCaptureRadius === true,
    noStartupWiring: options.noStartupWiring === true,
    noBackendChanges: options.noBackendChanges === true,
    noPersistence: options.noPersistence === true,
    noAutomaticInvocation: options.noAutomaticInvocation === true,
    acceptsTemporaryLocalDevVisual: options.acceptsTemporaryLocalDevVisual === true,
    allowCustom25DMapDisabledLocalDevVisibleTestBypass: true,
    mapContainer: options.mapContainer || null,
    container: options.container || null,
    map: options.map || null
  };

  const result =
    typeof createCustom25DVisualFirstManualVisibleTestLayer === "function"
      ? createCustom25DVisualFirstManualVisibleTestLayer(runnerOptions)
      : {
          ok: false,
          allowed: true,
          created: false,
          visible: false,
          reason: "manual-visible-test-layer-creator-unavailable"
        };
  const doc = typeof document !== "undefined" ? document : null;
  const elementFound =
    !!(
      doc &&
      typeof doc.querySelector === "function" &&
      doc.querySelector(
        '[data-custom-25d-visible-test="true"], [data-custom25d-manual-visible-test-layer="true"]'
      )
    );

  return {
    phase: 216,
    name: "custom-25d-visual-local-dev-visible-test-runner",
    ok: true,
    allowed: overrideGuard.allowed === true,
    ready: result.ready === true,
    ran: true,
    created: result.created === true,
    visible: result.visible === true || elementFound,
    elementFound,
    manualOnly: true,
    localDevOnly: true,
    browserConsoleOnly: true,
    startupWired: false,
    automaticInvocationAdded: false,
    productionFlagsChanged: false,
    normalAppBehaviorChanged: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    cleanupAvailable:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function",
    reason:
      result.reason || "local-dev-visible-test-runner-completed-with-manual-visible-test-layer-result",
    notes: [
      "Manual local-dev/browser-console-only runner.",
      "Calls the visible test layer creator only after the local-dev override guard allows it.",
      "Does not change startup wiring, production flags, or default app behavior."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestConsoleGuide(options = {}) {
  const helperPresence = {
    runner: typeof runCustom25DVisualLocalDevVisibleTest === "function",
    overrideGuard:
      typeof canUseCustom25DVisualLocalDevVisibleTestOverride === "function",
    visibleLayerCreator:
      typeof createCustom25DVisualFirstManualVisibleTestLayer === "function",
    cleanupHelper:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
  };

  const manualOptions = {
    localDevOnly: true,
    browserConsoleOnly: true,
    manual: true,
    developerIntent: true,
    allowLocalDevVisibleTestOverride: true,
    allowVisibleTestLayer: true,
    allowManualMapAttachment: true,
    allowTinyTestOnly: true,
    cleanupVerified: true,
    attachmentSmokeShellReady: true,
    preserveExistingMap: true,
    preserveGameplayOverlays: true,
    preserveOSMBehavior: true,
    preservePins: true,
    preservePlayerMarker: true,
    preserveCaptureRadius: true,
    noStartupWiring: true,
    noBackendChanges: true,
    noPersistence: true,
    noAutomaticInvocation: true,
    acceptsTemporaryLocalDevVisual: true
  };

  return {
    phase: 217,
    name: "custom-25d-visual-local-dev-visible-test-console-guide",
    ok: true,
    passive: true,
    reportOnly: true,
    helperPresence,
    manualOptions,
    runCommandExample: `runCustom25DVisualLocalDevVisibleTest(${JSON.stringify(manualOptions, null, 2)})`,
    cleanupCommandExample: "clearCustom25DVisualFirstManualVisibleTestLayer({})",
    startupWired: false,
    automaticInvocationAdded: false,
    createsVisibleTestLayer: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsRendererObject: false,
    initializesRenderer: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsLayerState: false,
    attachesToMap: false,
    draws: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    notes: [
      "Passive guide/report-only helper.",
      "Does not call the runner, creator, or cleanup helper.",
      "Normal app reload remains unchanged and invisible."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestPreflightReport(options = {}) {
  const helperPresence = {
    consoleGuide:
      typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function",
    runner: typeof runCustom25DVisualLocalDevVisibleTest === "function",
    overrideGuard:
      typeof canUseCustom25DVisualLocalDevVisibleTestOverride === "function",
    visibleLayerCreator:
      typeof createCustom25DVisualFirstManualVisibleTestLayer === "function",
    cleanupHelper:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
  };

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  const allHelpersPresent = Object.values(helperPresence).every(Boolean);
  const allSafetyFlagsFalse = Object.values(safetyFlagSnapshot).every(Boolean);

  const fallbackManualOptions = {
    localDevOnly: true,
    browserConsoleOnly: true,
    manual: true,
    developerIntent: true,
    allowLocalDevVisibleTestOverride: true,
    allowVisibleTestLayer: true,
    allowManualMapAttachment: true,
    allowTinyTestOnly: true,
    cleanupVerified: true,
    attachmentSmokeShellReady: true,
    preserveExistingMap: true,
    preserveGameplayOverlays: true,
    preserveOSMBehavior: true,
    preservePins: true,
    preservePlayerMarker: true,
    preserveCaptureRadius: true,
    noStartupWiring: true,
    noBackendChanges: true,
    noPersistence: true,
    noAutomaticInvocation: true,
    acceptsTemporaryLocalDevVisual: true
  };

  const guideManualOptions =
    helperPresence.consoleGuide &&
    typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function"
      ? getCustom25DVisualLocalDevVisibleTestConsoleGuide(options).manualOptions || fallbackManualOptions
      : fallbackManualOptions;

  const runCommandExample =
    helperPresence.consoleGuide &&
    typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function"
      ? getCustom25DVisualLocalDevVisibleTestConsoleGuide(options).runCommandExample ||
        `runCustom25DVisualLocalDevVisibleTest(${JSON.stringify(guideManualOptions, null, 2)})`
      : `runCustom25DVisualLocalDevVisibleTest(${JSON.stringify(guideManualOptions, null, 2)})`;

  const cleanupCommandExample =
    helperPresence.consoleGuide &&
    typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function"
      ? getCustom25DVisualLocalDevVisibleTestConsoleGuide(options).cleanupCommandExample ||
        "clearCustom25DVisualFirstManualVisibleTestLayer({})"
      : "clearCustom25DVisualFirstManualVisibleTestLayer({})";

  return {
    phase: 218,
    name: "custom-25d-visual-local-dev-visible-test-preflight-report",
    ok: true,
    passive: true,
    reportOnly: true,
    ready: false,
    allowed: false,
    defaultDecision: "no-go",
    defaultBlockedNoOptionsExpectation: true,
    helperPresence,
    safetyFlagSnapshot,
    manualOnlyLocalDevBrowserConsoleReadiness: {
      manualOnly: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      normalReloadUnchanged: true,
      defaultVisible: false
    },
    runCommandExample,
    cleanupCommandExample,
    readyForManualConsoleTest: allHelpersPresent && allSafetyFlagsFalse,
    startupWired: false,
    automaticInvocationAdded: false,
    createsVisibleTestLayer: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    attachesToMap: false,
    draws: false,
    renders: false,
    createsRendererObject: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    backendChanged: false,
    notes: [
      "Passive/report-only helper for manual browser-console visible test preflight.",
      "Does not call the guide command examples, runner, creator, or cleanup helper.",
      "Normal app reload remains unchanged and invisible."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestObservationGuide(options = {}) {
  const helperPresence = {
    consoleGuide:
      typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function",
    preflightReport:
      typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function",
    runner: typeof runCustom25DVisualLocalDevVisibleTest === "function",
    cleanupHelper:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
  };

  const preflightSnapshot =
    helperPresence.preflightReport &&
    typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function"
      ? getCustom25DVisualLocalDevVisibleTestPreflightReport(options)
      : null;

  const consoleGuide =
    helperPresence.consoleGuide &&
    typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function"
      ? getCustom25DVisualLocalDevVisibleTestConsoleGuide(options)
      : null;

  const runCommandExample =
    (consoleGuide && consoleGuide.runCommandExample) ||
    (preflightSnapshot && preflightSnapshot.runCommandExample) ||
    null;

  const cleanupCommandExample =
    (consoleGuide && consoleGuide.cleanupCommandExample) ||
    (preflightSnapshot && preflightSnapshot.cleanupCommandExample) ||
    null;

  const requiredHelpersPresent = Object.values(helperPresence).every(Boolean);
  const preflightReady =
    !!(
      preflightSnapshot &&
      (preflightSnapshot.readyForManualConsoleTest === true ||
        preflightSnapshot.ready === true)
    );

  return {
    phase: 219,
    name: "custom-25d-visual-local-dev-visible-test-observation-guide",
    ok: true,
    passive: true,
    reportOnly: true,
    helperPresence,
    preflightSnapshot,
    runCommandExample,
    cleanupCommandExample,
    manualObservationChecklist: [
      "normal reload remains unchanged before manual command",
      "visible test appears only after manual command",
      "tiny 2.5D TEST layer appears only once",
      "existing Leaflet map remains usable",
      "base pins remain unchanged",
      "player marker remains unchanged",
      "capture radius remains unchanged",
      "UI/menu remains unchanged",
      "cleanup command removes only the test layer",
      "reload returns to normal invisible state"
    ],
    observationResultTemplate: {
      ranManually: false,
      appearedOnlyAfterManualCommand: false,
      mapStillUsable: false,
      pinsUnchanged: false,
      playerMarkerUnchanged: false,
      captureRadiusUnchanged: false,
      cleanupWorked: false,
      notes: ""
    },
    readyForManualObservation: requiredHelpersPresent && preflightReady,
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsVisibleTestLayer: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive observation/checklist/report-only helper for the future manual browser-console visible test.",
      "Does not call the runner, creator, cleanup helper, or visible layer creation path.",
      "Normal app reload remains unchanged and invisible."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestResultReport(options = {}) {
  const helperPresence = {
    consoleGuide:
      typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function",
    preflightReport:
      typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function",
    observationGuide:
      typeof getCustom25DVisualLocalDevVisibleTestObservationGuide === "function",
    runner: typeof runCustom25DVisualLocalDevVisibleTest === "function",
    cleanupHelper:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
  };

  const observationGuideSnapshot =
    helperPresence.observationGuide &&
    typeof getCustom25DVisualLocalDevVisibleTestObservationGuide === "function"
      ? getCustom25DVisualLocalDevVisibleTestObservationGuide(options)
      : null;

  const manualResults = {
    ranManually: options.ranManually === true,
    appearedOnlyAfterManualCommand:
      options.appearedOnlyAfterManualCommand === true,
    singleTestLayerOnly: options.singleTestLayerOnly === true,
    mapStillUsable: options.mapStillUsable === true,
    pinsUnchanged: options.pinsUnchanged === true,
    playerMarkerUnchanged: options.playerMarkerUnchanged === true,
    captureRadiusUnchanged: options.captureRadiusUnchanged === true,
    uiUnchanged: options.uiUnchanged === true,
    cleanupWorked: options.cleanupWorked === true,
    reloadReturnedToInvisible: options.reloadReturnedToInvisible === true,
    notes: typeof options.notes === "string" ? options.notes : ""
  };

  const checklistCompletionSummary = {
    totalChecks: 10,
    passedChecks: [
      manualResults.ranManually,
      manualResults.appearedOnlyAfterManualCommand,
      manualResults.singleTestLayerOnly,
      manualResults.mapStillUsable,
      manualResults.pinsUnchanged,
      manualResults.playerMarkerUnchanged,
      manualResults.captureRadiusUnchanged,
      manualResults.uiUnchanged,
      manualResults.cleanupWorked,
      manualResults.reloadReturnedToInvisible
    ].filter(Boolean).length
  };

  return {
    phase: 220,
    name: "custom-25d-visual-local-dev-visible-test-result-report",
    ok: true,
    passive: true,
    reportOnly: true,
    helperPresence,
    observationGuideSnapshot,
    manualResults,
    checklistCompletionSummary: {
      ...checklistCompletionSummary,
      completedAllChecks:
        checklistCompletionSummary.passedChecks === checklistCompletionSummary.totalChecks
    },
    passedManualVisibleSmokeTest:
      manualResults.ranManually &&
      manualResults.appearedOnlyAfterManualCommand &&
      manualResults.singleTestLayerOnly &&
      manualResults.mapStillUsable &&
      manualResults.pinsUnchanged &&
      manualResults.playerMarkerUnchanged &&
      manualResults.captureRadiusUnchanged &&
      manualResults.uiUnchanged &&
      manualResults.cleanupWorked &&
      manualResults.reloadReturnedToInvisible,
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsVisibleTestLayer: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive/manual result-report helper only.",
      "Does not perform the manual test itself.",
      "Normal app reload remains unchanged and invisible unless the developer manually uses the browser console command."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestDiagnosticsReport(options = {}) {
  const helperPresence = {
    consoleGuide:
      typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function",
    preflightReport:
      typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function",
    observationGuide:
      typeof getCustom25DVisualLocalDevVisibleTestObservationGuide === "function",
    resultReport:
      typeof getCustom25DVisualLocalDevVisibleTestResultReport === "function",
    runner: typeof runCustom25DVisualLocalDevVisibleTest === "function",
    overrideGuard:
      typeof canUseCustom25DVisualLocalDevVisibleTestOverride === "function",
    creator:
      typeof createCustom25DVisualFirstManualVisibleTestLayer === "function",
    cleanupHelper:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
  };

  const preflightSnapshot =
    helperPresence.preflightReport &&
    typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function"
      ? getCustom25DVisualLocalDevVisibleTestPreflightReport(options)
      : null;

  const observationGuideSnapshot =
    helperPresence.observationGuide &&
    typeof getCustom25DVisualLocalDevVisibleTestObservationGuide === "function"
      ? getCustom25DVisualLocalDevVisibleTestObservationGuide(options)
      : null;

  const manualResultSnapshot =
    options.manualResult && typeof options.manualResult === "object"
      ? options.manualResult
      : null;

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  const documentExists = typeof document !== "undefined";
  const windowExists = typeof window !== "undefined";
  const mapGlobals = {
    map: typeof map !== "undefined",
    leafletMap: typeof leafletMap !== "undefined",
    globalMap: typeof globalMap !== "undefined",
    appMap: typeof appMap !== "undefined",
    windowMap: windowExists && typeof window.map !== "undefined",
    windowLeafletMap: windowExists && typeof window.leafletMap !== "undefined",
    windowGlobalMap: windowExists && typeof window.globalMap !== "undefined",
    windowAppMap: windowExists && typeof window.appMap !== "undefined"
  };
  const mapReferenceAppearsAvailable = Object.values(mapGlobals).some(Boolean);
  const leafletContainerExists = documentExists
    ? !!document.querySelector(".leaflet-container")
    : false;
  const domContains2d5dTestText = documentExists
    ? (document.body && typeof document.body.textContent === "string"
        ? document.body.textContent
        : ""
      ).includes("2.5D TEST")
    : false;

  const diagnosticConclusions = {
    missingGuide: !helperPresence.consoleGuide,
    missingPreflight: !helperPresence.preflightReport,
    missingObservationGuide: !helperPresence.observationGuide,
    missingResultReport: !helperPresence.resultReport,
    missingRunner: !helperPresence.runner,
    missingOverrideGuard: !helperPresence.overrideGuard,
    missingCreator: !helperPresence.creator,
    missingCleanup: !helperPresence.cleanupHelper,
    preflightNotReady: !(
      preflightSnapshot &&
      preflightSnapshot.readyForManualConsoleTest === true
    ),
    mapContainerMissing: !leafletContainerExists,
    testElementNotFound: !domContains2d5dTestText
  };

  const likelyBlockedReason =
    (!helperPresence.consoleGuide && "missingGuide") ||
    (!helperPresence.preflightReport && "missingPreflight") ||
    (!helperPresence.observationGuide && "missingObservationGuide") ||
    (!helperPresence.resultReport && "missingResultReport") ||
    (!helperPresence.runner && "missingRunner") ||
    (!helperPresence.creator && "missingCreator") ||
    (!helperPresence.cleanupHelper && "missingCleanup") ||
    (!(preflightSnapshot && preflightSnapshot.readyForManualConsoleTest === true) &&
      "preflightNotReady") ||
    (!leafletContainerExists && "mapContainerMissing") ||
    (!domContains2d5dTestText && "testElementNotFound") ||
    "no-diagnostic-blocker-detected";

  const diagnosticsComplete =
    helperPresence.consoleGuide &&
    helperPresence.preflightReport &&
    helperPresence.observationGuide &&
    helperPresence.resultReport &&
    helperPresence.runner &&
    helperPresence.overrideGuard &&
    helperPresence.creator &&
    helperPresence.cleanupHelper &&
    Object.values(safetyFlagSnapshot).every(Boolean);

  return {
    phase: 221,
    name: "custom-25d-visual-local-dev-visible-test-diagnostics-report",
    ok: true,
    passive: true,
    reportOnly: true,
    helperPresence,
    preflightSnapshot,
    observationGuideSnapshot,
    manualResultSnapshot,
    safetyFlagSnapshot,
    domMapEnvironmentPresence: {
      documentExists,
      windowExists,
      mapReferenceAppearsAvailable,
      mapGlobals,
      leafletContainerExists,
      domContains2d5dTestText
    },
    diagnosticConclusions,
    likelyBlockedReason,
    readyForNextVisibleFixPhase:
      diagnosticsComplete &&
      Object.values(safetyFlagSnapshot).every(Boolean) &&
      !domContains2d5dTestText,
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsVisibleTestLayer: false,
    mutatesDom: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive diagnostics/report-only helper.",
      "Does not run the manual test and does not create or attach anything.",
      "Does not mutate the DOM, draw, or wire startup."
    ]
  };
}

function getCustom25DVisualLocalDevVisibleTestSuccessCloseoutReport(options = {}) {
  const helperPresence = {
    consoleGuide:
      typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function",
    preflightReport:
      typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function",
    observationGuide:
      typeof getCustom25DVisualLocalDevVisibleTestObservationGuide === "function",
    resultReport:
      typeof getCustom25DVisualLocalDevVisibleTestResultReport === "function",
    diagnosticsReport:
      typeof getCustom25DVisualLocalDevVisibleTestDiagnosticsReport === "function",
    runner: typeof runCustom25DVisualLocalDevVisibleTest === "function",
    creator:
      typeof createCustom25DVisualFirstManualVisibleTestLayer === "function",
    cleanupHelper:
      typeof clearCustom25DVisualFirstManualVisibleTestLayer === "function"
  };

  const helperSnapshots = {
    consoleGuide:
      helperPresence.consoleGuide &&
      typeof getCustom25DVisualLocalDevVisibleTestConsoleGuide === "function"
        ? getCustom25DVisualLocalDevVisibleTestConsoleGuide(options)
        : null,
    preflightReport:
      helperPresence.preflightReport &&
      typeof getCustom25DVisualLocalDevVisibleTestPreflightReport === "function"
        ? getCustom25DVisualLocalDevVisibleTestPreflightReport(options)
        : null,
    observationGuide:
      helperPresence.observationGuide &&
      typeof getCustom25DVisualLocalDevVisibleTestObservationGuide === "function"
        ? getCustom25DVisualLocalDevVisibleTestObservationGuide(options)
        : null,
    resultReport:
      helperPresence.resultReport &&
      typeof getCustom25DVisualLocalDevVisibleTestResultReport === "function"
        ? getCustom25DVisualLocalDevVisibleTestResultReport(options)
        : null,
    diagnosticsReport:
      helperPresence.diagnosticsReport &&
      typeof getCustom25DVisualLocalDevVisibleTestDiagnosticsReport === "function"
        ? getCustom25DVisualLocalDevVisibleTestDiagnosticsReport(options)
        : null
  };

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  return {
    phase: 224,
    name: "custom-25d-visual-local-dev-visible-test-success-closeout-report",
    ok: true,
    passive: true,
    reportOnly: true,
    helperPresence,
    helperSnapshots,
    successfulDomCheckExpectations: {
      bodyContainsTestText: 'document.body.textContent.includes("2.5D TEST") === true',
      singleTaggedElement:
        'document.querySelectorAll(\'[data-custom-25d-visible-test="true"]\').length === 1'
    },
    cleanupExpectation: {
      bodyDoesNotContainTestTextAfterCleanup:
        'document.body.textContent.includes("2.5D TEST") === false'
    },
    normalReloadUnchangedAndInvisible: true,
    safetyFlagSnapshot,
    approvesStartupWiring: false,
    approvesProductionUse: false,
    approvesRealRendererCreation: false,
    approvesRealMapAttachment: false,
    approvesLargerVisibleTests: false,
    nextPhaseReadinessRecommendation:
      "planning-only-follow-up-for-future-guarded-visual-test-expansion",
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsVisibleTestLayer: false,
    mutatesDom: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive closeout/report-only helper for the successful strict manual local-dev browser-console visible test.",
      "Does not run the test, create the test element, or perform cleanup.",
      "Does not approve startup wiring, production use, real renderer creation, real map attachment, or larger visible tests."
    ]
  };
}

function getCustom25DVisualManualVisibleTestNextStepPlan(options = {}) {
  const successCloseoutSnapshot =
    typeof getCustom25DVisualLocalDevVisibleTestSuccessCloseoutReport === "function"
      ? getCustom25DVisualLocalDevVisibleTestSuccessCloseoutReport(options)
      : null;

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  return {
    phase: 225,
    name: "custom-25d-visual-manual-visible-test-next-step-plan",
    ok: true,
    passive: true,
    reportOnly: true,
    planningOnly: true,
    successCloseoutSnapshot,
    nextPlanningStep:
      "plan a future manually triggered, local-dev-only, browser-console-only visual test expansion",
    nextVisibleTestRequirements: {
      tiny: true,
      temporary: true,
      manuallyInvoked: true,
      cleanupSafe: true,
      startupConnected: false
    },
    preservationRequirements: {
      preserveExistingLeafletMapBehavior: true,
      preserveOSMBehavior: true,
      preserveGameplay: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      preserveUI: true,
      preserveBackendBehavior: true
    },
    approvals: {
      startupWiring: false,
      productionUse: false,
      enableCustom25DMap: false,
      rendererCreation: false,
      rendererInitialization: false,
      lifecycleCreation: false,
      registryCreation: false,
      persistentLayerState: false,
      automaticMapAttachment: false,
      automaticDrawing: false,
      largerVisualTests: false
    },
    safetyFlagSnapshot,
    nextPhaseReadinessRecommendation:
      "planning-only-future-manual-visual-test-expansion-phase",
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsDomElements: false,
    mutatesDom: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsPersistentLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive plan/report-only helper.",
      "Summarizes the next planning-only step after the tiny manual visible test proof.",
      "Does not approve startup wiring, production use, ENABLE_CUSTOM_25D_MAP, renderer creation, renderer initialization, lifecycle creation, registry creation, persistent layer state, automatic map attachment, automatic drawing, or larger visual tests."
    ]
  };
}

function getCustom25DVisualExpandedManualVisibleTestContract(options = {}) {
  const nextStepPlanSnapshot =
    typeof getCustom25DVisualManualVisibleTestNextStepPlan === "function"
      ? getCustom25DVisualManualVisibleTestNextStepPlan(options)
      : null;

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  return {
    phase: 226,
    name: "custom-25d-visual-expanded-manual-visible-test-contract",
    ok: true,
    passive: true,
    reportOnly: true,
    contractOnly: true,
    nextStepPlanSnapshot,
    futureExpandedTestScope: {
      localDevOnly: true,
      browserConsoleOnly: true,
      manuallyInvoked: true,
      temporary: true,
      cleanupSafe: true,
      visualProofingOnly: true
    },
    futureManualOverrideRequirement:
      "future expanded test may only be considered after all strict manual override requirements are explicitly true",
    futureExpandedTestBoundaries: {
      remainsVisuallyTiny: true,
      notRealRenderer: true,
      notRealMapLayer: true,
      notProductionFeature: true
    },
    preservationRequirements: {
      preserveExistingLeafletMapBehavior: true,
      preserveOSMBehavior: true,
      preserveGameplay: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      preserveUI: true,
      preserveBackendBehavior: true
    },
    requiredFutureTestConstraints: {
      noStartupWiring: true,
      noAutomaticInvocation: true,
      noPersistence: true,
      noBackendWrites: true,
      noProductionFlagChanges: true,
      cleanupRemovesOnlyTemporaryTestElement: true,
      normalReloadRemainsUnchangedAndInvisible: true
    },
    safetyFlagSnapshot,
    nextPhaseReadinessRecommendation:
      "contract-only-readiness-for-future-manual-expanded-visible-test-implementation-phase",
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsDomElements: false,
    mutatesDom: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsPersistentLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive contract/report-only helper.",
      "Defines the safety contract for a future tiny expanded manual visible test.",
      "Does not call the visible test runner, creator, cleanup, renderer, DOM creation, attachment, drawing, gameplay, OSM, pins, player marker, capture radius, UI, or backend code."
    ]
  };
}

function getCustom25DVisualExpandedManualVisibleTestReadinessChecklist(options = {}) {
  const expandedContractSnapshot =
    typeof getCustom25DVisualExpandedManualVisibleTestContract === "function"
      ? getCustom25DVisualExpandedManualVisibleTestContract(options)
      : null;

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  return {
    phase: 227,
    name: "custom-25d-visual-expanded-manual-visible-test-readiness-checklist",
    ok: true,
    passive: true,
    reportOnly: true,
    checklistOnly: true,
    expandedContractSnapshot,
    futureImplementationReadinessChecklist: {
      localDevOnly: true,
      browserConsoleOnly: true,
      manuallyInvoked: true,
      temporary: true,
      cleanupSafe: true,
      visuallyTiny: true,
      notConnectedToStartup: true,
      notPersistent: true,
      notRealRenderer: true,
      notRealMapLayer: true,
      notProductionBehavior: true,
      preserveExistingLeafletMapBehavior: true,
      preserveOSMBehavior: true,
      preserveGameplay: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      preserveUI: true,
      preserveBackendBehavior: true,
      explicitManualOverrideRequired: true,
      failsClosedByDefault: true,
      cleanupRemovesOnlyTemporaryExpandedTestElement: true,
      normalReloadRemainsUnchangedAndInvisible: true,
      noScopeExpansionIntoStartupWiring: true,
      noScopeExpansionIntoRealRendererCreation: true,
      noScopeExpansionIntoRendererInitialization: true,
      noScopeExpansionIntoLifecycleCreation: true,
      noScopeExpansionIntoRegistryCreation: true,
      noScopeExpansionIntoPersistentLayerState: true,
      noScopeExpansionIntoAutomaticMapAttachment: true,
      noScopeExpansionIntoAutomaticDrawing: true
    },
    safetyFlagSnapshot,
    nextPhaseReadinessRecommendation:
      "checklist-only-readiness-for-future-expanded-manual-visible-test-implementation-planning-phase",
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsDomElements: false,
    mutatesDom: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsPersistentLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive checklist/report-only helper.",
      "Provides an implementation-neutral readiness checklist for a future tiny expanded manual visible test implementation phase.",
      "Does not call the visible test runner, creator, cleanup, renderer, DOM creation, attachment, drawing, gameplay, OSM, pins, player marker, capture radius, UI, or backend code."
    ]
  };
}

function getCustom25DVisualExpandedManualVisibleTestImplementationPlan(options = {}) {
  const readinessChecklistSnapshot =
    typeof getCustom25DVisualExpandedManualVisibleTestReadinessChecklist === "function"
      ? getCustom25DVisualExpandedManualVisibleTestReadinessChecklist(options)
      : null;

  const safetyFlagSnapshot = {
    custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
    landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
    landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
    dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
  };

  return {
    phase: 228,
    name: "custom-25d-visual-expanded-manual-visible-test-implementation-plan",
    ok: true,
    passive: true,
    reportOnly: true,
    implementationPlanOnly: true,
    readinessChecklistSnapshot,
    futureImplementationPlan: {
      noAutomaticBehavior: true,
      localDevOnly: true,
      browserConsoleOnly: true,
      manuallyInvoked: true,
      temporary: true,
      cleanupSafe: true,
      visuallyTiny: true,
      failsClosedByDefault: true,
      notConnectedToStartup: true,
      notPersistent: true,
      notBackendConnected: true,
      notRealRenderer: true,
      notRealMapLayer: true,
      notProductionBehavior: true
    },
    recommendations: {
      reuseExistingStrictManualVisibleTestGuardPattern: true,
      useDistinctExpandedTestDataAttribute: true,
      cleanupRemovesOnlyFutureExpandedTestElement: true,
      cleanupDoesNotTouchExistingMapPinsUiOrEarlierTinyTestUnlessExplicitlyIntended: true
    },
    futureManualChecks: {
      appearsOnlyAfterFullManualOverrideCommand: true,
      expandedTestCountExactlyOne: true,
      cleanupRemovesExpandedTest: true,
      normalReloadRemainsUnchangedAndInvisible: true,
      noOptionCallsRemainBlockedAndInvisible: true,
      incompleteOptionCallsRemainBlockedAndInvisible: true
    },
    preservationRequirements: {
      preserveExistingLeafletMapBehavior: true,
      preserveOSMBehavior: true,
      preserveGameplay: true,
      preservePins: true,
      preservePlayerMarker: true,
      preserveCaptureRadius: true,
      preserveUI: true,
      preserveBackendBehavior: true
    },
    safetyFlagSnapshot,
    nextPhaseReadinessRecommendation:
      "implementation-plan-only-readiness-for-future-expanded-manual-visible-test-phase",
    startupWired: false,
    automaticInvocationAdded: false,
    callsRunner: false,
    callsCreator: false,
    callsCleanupHelper: false,
    createsDomElements: false,
    mutatesDom: false,
    attachesToMap: false,
    draws: false,
    createsRendererObject: false,
    initializesRendererState: false,
    createsLifecycleObject: false,
    createsRegistry: false,
    createsPersistentLayerState: false,
    gameplayChanged: false,
    osmChanged: false,
    pinsChanged: false,
    playerMarkerChanged: false,
    captureRadiusChanged: false,
    uiChanged: false,
    backendChanged: false,
    notes: [
      "Passive implementation-plan/report-only helper.",
      "Provides a narrow implementation plan for a future tiny expanded manual visible test.",
      "Does not call the visible test runner, creator, cleanup, renderer, DOM creation, attachment, drawing, gameplay, OSM, pins, player marker, capture radius, UI, or backend code."
    ]
  };
}

function getCustom25DLandmarkVisibleTestReadinessPlan() {
  return {
    ok: true,
    phase: 32,
    name: "custom-25d-landmark-visible-test-readiness-plan",
    dormant: true,
    purpose: "Readiness plan for a future manual-only visible landmark test without enabling it yet.",
    previousPhase: {
      nextPhasePlanExists: typeof getCustom25DLandmarkNextPhasePlan === "function"
    },
    visibleTestingAllowedNow: false,
    readyForVisibleRendering: false,
    requiresExplicitFutureApproval: true,
    recommendedBeforeVisibleTest: [
      "keep all feature flags false by default",
      "define a manual-only activation path",
      "require console-only invocation",
      "require existing OSM to remain visible underneath",
      "require no gameplay, reward, capture, or backend changes",
      "require test markers to remain disabled until explicitly armed"
    ],
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    },
    visibleOutputCreated: false,
    stateMutated: false,
    flagsChanged: false,
    safetyNotes: [
      "Planning-only helper.",
      "Does not render markers or layers.",
      "Does not create DOM elements.",
      "Does not enable flags.",
      "Does not mutate manual test state."
    ]
  };
}

function getCustom25DLandmarkVisibleTestPathPlan() {
  return {
    ok: true,
    phase: 33,
    name: "custom-25d-landmark-visible-test-path-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose: "Describe the future manual-only path for visible landmark testing without enabling it now.",
    dependsOn: [
      "getCustom25DLandmarkNextPhasePlan",
      "getCustom25DLandmarkVisibleTestReadinessPlan"
    ],
    blockedUntil: [
      "explicit future approval",
      "manual-only activation path is defined",
      "all safety flags remain false by default"
    ],
    requiredBeforeVisibleTesting: [
      "manual-only invocation path documented",
      "OSM remains visible underneath",
      "no gameplay, reward, capture, or backend changes",
      "test markers stay disabled until explicitly armed"
    ],
    manualOnlyFuturePath: [
      "review dormant planning helpers",
      "approve a console-only test path",
      "enable only the minimum temporary flags during a future manual test",
      "restore all flags to false after testing"
    ],
    safetyRules: [
      "do not render anything by default",
      "do not create markers or layers by default",
      "do not mutate gameplay or backend state"
    ],
    forbiddenNow: [
      "visible rendering",
      "marker creation",
      "layer creation",
      "DOM or UI creation",
      "flag activation"
    ],
    expectedCurrentBehavior: "No visible landmark test behavior is active.",
    nextSuggestedPhase: "manual-visible-test-approval-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    }
  };
}

function getCustom25DLandmarkVisibleTestPreflightPlan() {
  return {
    ok: true,
    phase: 34,
    name: "custom-25d-landmark-visible-test-preflight-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose: "Describe the preflight checks required before any future manual-only visible landmark test is considered.",
    dependsOn: [
      "getCustom25DLandmarkNextPhasePlan",
      "getCustom25DLandmarkVisibleTestReadinessPlan",
      "getCustom25DLandmarkVisibleTestPathPlan"
    ],
    requiredCleanState: [
      "working tree is clean before temporary test changes",
      "all dormant helpers remain present",
      "no unrelated gameplay changes are in progress"
    ],
    requiredFlagState: [
      "ENABLE_CUSTOM_25D_MAP = false",
      "ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS = false",
      "ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA = false",
      "ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA = false"
    ],
    requiredHelperChecks: [
      "planning helpers exist exactly once",
      "manual test helpers remain dormant",
      "no helper mutates state unless explicitly intended"
    ],
    requiredBehaviorChecks: [
      "no visible marker rendering by default",
      "no Leaflet layers added by default",
      "no DOM or UI debug tools added by default"
    ],
    requiredManualReview: [
      "confirm OSM stays visible underneath",
      "confirm no gameplay, reward, capture, or backend changes",
      "confirm any future visible test remains manual-only"
    ],
    forbiddenNow: [
      "visible rendering",
      "marker creation",
      "layer creation",
      "DOM or UI creation",
      "flag activation"
    ],
    expectedCurrentBehavior: "No visible landmark test behavior is active.",
    passCriteriaForFutureVisibleTest: [
      "explicit approval is given",
      "manual-only invocation path is defined",
      "temporary test flags can be restored safely after inspection"
    ],
    failCriteria: [
      "any default-visible output appears",
      "any gameplay or backend state changes are required",
      "OSM visibility is reduced in an unsafe way"
    ],
    nextSuggestedPhase: "manual-visible-test-approval-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP === false,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS === false,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA === false,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA === false
    }
  };
}

function getCustom25DLandmarkVisibleTestApprovalGatePlan() {
  return {
    ok: true,
    phase: 35,
    name: "custom-25d-landmark-visible-test-approval-gate-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the approval gates that must be satisfied before any future manual-only visible landmark marker test can begin.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    approvalRequiredFrom: [
      "explicit future product approval",
      "manual-only renderer test approval",
      "custom 2.5D landmark safety review approval",
    ],
    requiredPreflightEvidence: [
      "preflight plan remains documented and dormant",
      "working tree is clean before any future visible test",
      "all landmark-related feature flags remain false by default",
      "a reversible manual-only activation path is documented before use",
    ],
    requiredSafetyEvidence: [
      "OSM remains visible underneath at all times",
      "no gameplay, reward, capture, collection, or backend changes are introduced",
      "no default-visible landmark markers render automatically",
      "no markers, layers, DOM elements, or visible debug tools are created by default",
    ],
    requiredManualDecision: [
      "decide whether a future visible test is still inspection-only",
      "decide which console-only steps are allowed during a future approved test",
      "decide how rollback to all-false flags will be verified after testing",
    ],
    approvedActionsFutureOnly: [
      "console-only manual activation after explicit approval",
      "temporary and reversible local-only flag changes for inspection",
      "post-test restoration of all safety flags to false",
    ],
    rejectedActionsNow: [
      "visible rendering in the current phase",
      "automatic startup activation",
      "persistent feature flag changes",
      "gameplay-connected landmark behavior",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with no visible landmark marker output and no gameplay changes.",
    passCriteriaForFutureApproval: [
      "explicit approval is granted for a manual-only visible test",
      "required preflight evidence is reviewed and accepted",
      "required safety evidence is reviewed and accepted",
      "rollback expectation is documented and feasible before any visible test begins",
    ],
    failCriteria: [
      "any safety flag is enabled by default",
      "any visible landmark rendering occurs without explicit approval",
      "any gameplay, reward, collection, backend, or OSM behavior changes are required",
      "any opaque or blank canvas background is introduced",
    ],
    rollbackExpectation:
      "Any future visible test must remain temporary and restore all landmark-related safety flags to false immediately after inspection.",
    nextSuggestedPhase: "custom-25d-landmark-visible-test-approval-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestRollbackPlan() {
  return {
    ok: true,
    phase: 36,
    name: "custom-25d-landmark-visible-test-rollback-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the rollback expectations for any future manual-only visible landmark marker test before that test is ever allowed to begin.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    rollbackTriggers: [
      "any safety flag remains enabled after a future manual-only test",
      "any visible landmark output appears outside the approved manual test window",
      "any gameplay, reward, collection, backend, or OSM behavior changes are detected",
      "any blank, opaque, or map-obscuring canvas behavior appears",
    ],
    rollbackActionsFutureOnly: [
      "return all landmark-related safety flags to false immediately after testing",
      "remove any temporary manual-only activation steps",
      "restore the last known safe dormant map state",
      "confirm no visible landmark rendering remains active",
    ],
    requiredRollbackEvidence: [
      "post-test flag values are documented as false",
      "working tree is reviewed for only approved reversible changes",
      "default app behavior matches the dormant baseline after rollback",
      "no visible landmark markers, layers, or UI remain after rollback",
    ],
    safeDefaultState: {
      custom25DMap: false,
      landmarkTestMarkers: false,
      landmarkSampleData: false,
      dinosaurSitesAuData: false,
      visibleLandmarkRendering: false,
    },
    requiredPostRollbackChecks: [
      "OSM remains visible underneath",
      "no markers, layers, DOM elements, or manual-test UI remain active",
      "no gameplay, reward, collection, or backend changes remain",
      "normal blue pins, player marker, and capture radius still behave exactly as before",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with rollback planning documented only and no visible landmark marker output.",
    passCriteriaForRollbackReadiness: [
      "a future test can be fully reversed to the all-false safe default state",
      "post-rollback checks are explicit and reviewable",
      "rollback does not depend on gameplay, backend, or map behavior changes",
      "rollback can be verified without creating visible output by default",
    ],
    failCriteria: [
      "rollback requires leaving any safety flag enabled",
      "rollback depends on permanent visible rendering changes",
      "rollback leaves markers, layers, DOM elements, or UI behind",
      "rollback cannot restore the dormant safe baseline immediately after testing",
    ],
    nextSuggestedPhase: "custom-25d-landmark-visible-test-rollback-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestRiskPlan() {
  return {
    ok: true,
    phase: 37,
    name: "custom-25d-landmark-visible-test-risk-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the risks that must be considered before any future manual-only visible landmark marker test can begin.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    primaryRisks: [
      "future visible test could escape dormant/manual-only boundaries",
      "temporary test changes could linger past inspection",
      "manual-only rendering could be mistaken for approved gameplay behavior",
    ],
    mapSafetyRisks: [
      "OSM visibility could be reduced too far during future testing",
      "visible landmark markers could crowd or visually compete with normal blue pins",
      "future test rendering could accidentally obscure player marker or capture radius",
      "future test canvas behavior could reintroduce blank or pale map failures",
    ],
    gameplaySafetyRisks: [
      "future visible test could accidentally connect to capture logic",
      "future visible test could accidentally affect rewards, collections, or progression",
      "future visible test could be confused with active gameplay landmarks before approval",
    ],
    performanceRisks: [
      "future visible markers could add too much draw cost or map clutter",
      "future manual-only rendering could create too many markers or layers at once",
      "future test behavior could reduce mobile responsiveness if not tightly gated",
    ],
    dataSafetyRisks: [
      "future test could surface real POI data earlier than intended",
      "temporary test data could be mistaken for approved production landmark content",
      "future test changes could accidentally blur the line between sample and real data",
    ],
    userExperienceRisks: [
      "players could misread temporary test markers as a live feature",
      "visible test output could make the map feel busier or less readable",
      "future manual-only testing could create confusing visual inconsistency if not fully rolled back",
    ],
    mitigationPlanFutureOnly: [
      "keep all landmark-related flags false by default",
      "allow only explicit console-only temporary activation during approved testing",
      "require rollback to the dormant baseline immediately after inspection",
      "require proof that no gameplay, backend, or OSM behavior changed",
    ],
    requiredEvidenceBeforeTesting: [
      "preflight, approval, rollback, and readiness plans are reviewed together",
      "future visible test path remains manual-only and reversible",
      "post-test rollback steps are documented before any activation",
      "normal pins, player marker, capture radius, and OSM visibility remain protected",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with risk planning documented only and no visible landmark marker output.",
    passCriteriaForRiskReadiness: [
      "major visible-test risks are explicitly listed and reviewable",
      "mitigation remains manual-only and reversible",
      "no risk handling depends on gameplay or backend changes",
      "the dormant default state remains unchanged while planning continues",
    ],
    failCriteria: [
      "any visible landmark behavior is enabled during this planning phase",
      "risk handling requires changing gameplay, rewards, backend, or OSM behavior",
      "future visible testing cannot be clearly separated from normal map behavior",
      "risk mitigation cannot restore the all-false dormant baseline immediately after testing",
    ],
    nextSuggestedPhase: "custom-25d-landmark-visible-test-risk-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestObservationPlan() {
  return {
    ok: true,
    phase: 38,
    name: "custom-25d-landmark-visible-test-observation-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe what should be observed during any future manual-only visible landmark marker test before that test is ever approved to run.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    observationScope: [
      "future manual-only visible landmark marker behavior",
      "interaction between test markers and the protected custom 2.5D map",
      "visual separation between landmark test markers and normal gameplay pins",
      "preservation of dormant default behavior outside explicit test windows",
    ],
    mapObservationChecklist: [
      "confirm the map remains readable with OSM still visible underneath",
      "confirm player marker and capture radius remain unobscured",
      "confirm landmark test output does not crowd the map or dominate the scene",
      "confirm no blank, pale, or opaque canvas failure returns",
    ],
    markerObservationChecklist: [
      "confirm any future test markers are visually distinct from normal blue pins",
      "confirm test markers do not replace gameplay pins",
      "confirm any future marker density remains low and reviewable",
      "confirm markers remain clearly temporary and test-only",
    ],
    osmSafetyObservationChecklist: [
      "confirm OSM remains visible underneath at all times",
      "confirm no future test step hides or fully covers OSM",
      "confirm base map context stays readable during manual-only inspection",
    ],
    gameplaySafetyObservationChecklist: [
      "confirm no capture logic is affected",
      "confirm no rewards, collections, XP, coins, or backend writes occur",
      "confirm normal blue pins, player marker, and capture radius behave exactly as before",
      "confirm visible test output is not mistaken for approved gameplay",
    ],
    performanceObservationChecklist: [
      "confirm map responsiveness remains acceptable on mobile",
      "confirm no excessive marker, layer, or draw buildup occurs",
      "confirm any temporary visible test remains lightweight and reversible",
    ],
    userExperienceObservationChecklist: [
      "confirm the map does not become cluttered or confusing",
      "confirm future visible test output is understandable as temporary",
      "confirm landmark visuals do not overpower navigation or gameplay clarity",
    ],
    requiredEvidenceFutureOnly: [
      "screenshots or observations showing pins remain dominant",
      "evidence that OSM stays visible underneath",
      "evidence that gameplay behavior is unchanged during the manual-only test",
      "evidence that rollback returns the map to the dormant baseline immediately after testing",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with observation planning documented only and no visible landmark marker output.",
    passCriteriaForObservationReadiness: [
      "the future observation checklist is explicit and reviewable",
      "observation remains manual-only and reversible",
      "observation requirements protect OSM visibility and pin dominance",
      "the dormant all-false default state remains unchanged while planning continues",
    ],
    failCriteria: [
      "any visible landmark output appears during this planning phase",
      "observation expectations require gameplay or backend changes",
      "future visible tests cannot be cleanly distinguished from normal map behavior",
      "observation planning does not preserve the dormant safe baseline",
    ],
    nextSuggestedPhase: "custom-25d-landmark-visible-test-observation-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestReportPlan() {
  return {
    ok: true,
    phase: 39,
    name: "custom-25d-landmark-visible-test-report-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the report structure that should be produced after any future manual-only visible landmark marker test.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    reportScope: [
      "future manual-only visible landmark marker test results",
      "map safety observations during temporary approved testing",
      "marker clarity and separation from normal gameplay pins",
      "confirmation that rollback returned the map to the dormant baseline",
    ],
    requiredReportSections: [
      "test context and approval reference",
      "active temporary flag state during testing",
      "map and marker observations",
      "safety findings and rollback outcome",
      "decision outcome and next recommendation",
    ],
    requiredSafetyFindings: [
      "whether all safety gates remained respected during testing",
      "whether all landmark-related flags returned to false after testing",
      "whether any forbidden behavior appeared",
    ],
    requiredMapFindings: [
      "whether the map remained readable",
      "whether player marker and capture radius stayed unobscured",
      "whether no blank, pale, or opaque canvas issue appeared",
    ],
    requiredMarkerFindings: [
      "whether test markers were clearly separate from normal blue pins",
      "whether marker density remained low and reviewable",
      "whether markers appeared temporary and test-only",
    ],
    requiredOsmFindings: [
      "whether OSM stayed visible underneath throughout the test",
      "whether base map context remained readable",
      "whether no future test step hid or fully covered OSM",
    ],
    requiredGameplayFindings: [
      "whether captures, rewards, collections, XP, coins, and backend behavior remained unchanged",
      "whether normal blue pins, player marker, and capture radius behaved exactly as before",
      "whether visible test output stayed disconnected from gameplay",
    ],
    requiredPerformanceFindings: [
      "whether the map stayed responsive",
      "whether marker or layer activity remained lightweight",
      "whether future visible testing stayed reversible without performance regressions",
    ],
    requiredUserExperienceFindings: [
      "whether the map remained understandable and uncluttered",
      "whether future visible test output was clearly temporary",
      "whether landmark visuals avoided overpowering core gameplay readability",
    ],
    requiredScreenshotsFutureOnly: [
      "default dormant baseline before any temporary test activation",
      "approved manual-only visible landmark test state",
      "post-rollback dormant baseline after all flags return to false",
    ],
    requiredDecisionOutcome: [
      "approve further manual-only testing",
      "request revisions before further testing",
      "reject visible testing until safety concerns are resolved",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with report planning documented only and no visible landmark marker output.",
    passCriteriaForReportReadiness: [
      "future report sections are explicit and reviewable",
      "report expectations preserve OSM visibility and gameplay safety",
      "report plan includes rollback confirmation and decision outcome",
      "the dormant all-false default state remains unchanged while planning continues",
    ],
    failCriteria: [
      "any visible landmark behavior appears during this planning phase",
      "report expectations require gameplay or backend changes",
      "report structure cannot distinguish temporary test behavior from normal gameplay",
      "report planning does not preserve the dormant safe baseline",
    ],
    nextSuggestedPhase: "custom-25d-landmark-visible-test-report-check",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestFinalCheckPlan() {
  return {
    ok: true,
    phase: 40,
    name: "custom-25d-landmark-visible-test-final-check-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the final checklist that must pass before any future manual-only visible landmark marker test can be considered.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    finalCleanStateChecks: [
      "working tree is clean before any future visible test",
      "no unreviewed dormant helper edits remain pending",
      "default app state is still the dormant baseline before temporary testing",
    ],
    finalFlagChecks: [
      "ENABLE_CUSTOM_25D_MAP remains false by default",
      "ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS remains false by default",
      "ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA remains false by default",
      "ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA remains false by default",
    ],
    finalHelperChecks: [
      "planning helpers remain present and reviewable",
      "manual-only test helpers remain dormant by default",
      "no helper enables visible behavior automatically",
    ],
    finalSafetyChecks: [
      "OSM remains visible underneath",
      "no opaque or blank canvas background exists",
      "no markers, layers, DOM elements, or visible manual-test UI are created by default",
      "no gameplay, reward, collection, or backend changes are introduced",
    ],
    finalBehaviorChecks: [
      "normal blue pins remain unchanged",
      "player marker remains unchanged",
      "capture radius remains unchanged",
      "current map behavior remains fully dormant with no visible landmark output",
    ],
    finalManualReviewChecks: [
      "preflight, approval, rollback, risk, observation, and report plans are reviewed together",
      "future visible testing remains manual-only and explicitly approved",
      "future rollback path is documented before any visible test begins",
    ],
    finalApprovalRequirement:
      "An explicit future approval decision is required before any manual-only visible landmark marker test can be considered.",
    finalRollbackRequirement:
      "Any future visible test must include an immediate rollback back to the all-false dormant state after inspection.",
    finalReportingRequirement:
      "Any future visible test must produce a structured report covering safety, map behavior, markers, OSM visibility, gameplay isolation, performance, and rollback outcome.",
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with final-check planning documented only and no visible landmark marker output.",
    passCriteriaForFutureVisibleTestConsideration: [
      "all final checks are explicit and reviewable",
      "all safety flags remain false by default",
      "future visible testing remains manual-only, reversible, and approval-gated",
      "the dormant all-false baseline remains unchanged while planning continues",
    ],
    failCriteria: [
      "any visible landmark behavior appears during this planning phase",
      "any final-check expectation requires gameplay or backend changes",
      "future visible testing cannot be clearly separated from normal map behavior",
      "final-check planning does not preserve the dormant safe baseline",
    ],
    nextSuggestedPhase: "custom-25d-landmark-visible-test-final-check-review",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestGoNoGoPlan() {
  return {
    ok: true,
    phase: 41,
    name: "custom-25d-landmark-visible-test-go-no-go-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the go/no-go decision structure for whether any future manual-only visible landmark marker test may proceed.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    goRequirements: [
      "all safety flags remain false by default until an explicitly approved future test window",
      "preflight, approval, rollback, risk, observation, report, and final-check plans are reviewed together",
      "future visible testing remains manual-only, reversible, and temporary",
      "OSM visibility, pin dominance, and gameplay isolation remain protected",
    ],
    noGoConditions: [
      "any visible landmark behavior appears before explicit approval",
      "any future test requires gameplay, reward, collection, or backend changes",
      "any future test cannot guarantee immediate rollback to the dormant baseline",
      "any future test risks obscuring OSM, blue pins, player marker, or capture radius",
    ],
    requiredEvidenceBeforeGo: [
      "clean-state and flag-state checks are documented",
      "risk, observation, and report expectations are explicit and reviewable",
      "rollback steps are documented before temporary activation",
      "future visible test path remains console-only and manually approved",
    ],
    requiredManualApproval: [
      "explicit approval for manual-only visible testing",
      "explicit approval that no gameplay or backend behavior may change",
      "explicit approval that rollback must happen immediately after inspection",
    ],
    allowedFutureGoActions: [
      "temporary console-only activation during an approved future test",
      "temporary, reversible local-only flag changes for inspection",
      "structured reporting and immediate rollback after the future test",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with go/no-go planning documented only and no visible landmark marker output.",
    goDecisionCriteria: [
      "future visible testing is manual-only, explicitly approved, and reversible",
      "all planning and safety checkpoints are satisfied before temporary activation",
      "OSM visibility and gameplay isolation remain protected throughout",
      "the dormant all-false baseline remains unchanged outside future test windows",
    ],
    noGoDecisionCriteria: [
      "any missing approval, missing rollback path, or missing report expectation blocks testing",
      "any required behavior change outside inspection-only rendering blocks testing",
      "any inability to preserve dormant defaults blocks testing",
      "any uncertainty about map safety or player-facing clarity blocks testing",
    ],
    rollbackExpectation:
      "Any future go decision must still require an immediate rollback back to the all-false dormant state after inspection.",
    reportingExpectation:
      "Any future go decision must require a structured report covering safety, map behavior, marker clarity, OSM visibility, gameplay isolation, performance, and rollback outcome.",
    nextSuggestedPhase: "custom-25d-landmark-visible-test-go-no-go-review",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestExecutionBoundaryPlan() {
  return {
    ok: true,
    phase: 42,
    name: "custom-25d-landmark-visible-test-execution-boundary-plan",
    dormant: true,
    planningOnly: true,
    visibleTestingAllowedNow: false,
    purpose:
      "Describe the execution boundaries that must contain any future manual-only visible landmark marker test.",
    dependsOn: [
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    executionBoundary: [
      "future testing remains manual-only",
      "future testing remains temporary and reversible",
      "future testing remains local-only unless explicitly re-approved",
      "future testing remains separate from gameplay and backend behavior",
    ],
    allowedFutureManualOnlyActions: [
      "temporary console-only activation during an approved future test window",
      "temporary reversible local-only flag changes for inspection",
      "temporary observation, screenshot capture, and structured reporting",
      "immediate rollback after inspection completes",
    ],
    disallowedCurrentActions: [
      "default-on visible landmark rendering",
      "automatic startup execution",
      "persistent marker or layer creation",
      "any gameplay-connected landmark behavior",
    ],
    requiredBeforeAnyExecution: [
      "explicit manual approval is granted",
      "preflight, safety, rollback, and report plans are reviewed",
      "all landmark-related flags are false before temporary activation",
      "a rollback path is documented before the future test begins",
    ],
    requiredDuringExecution: [
      "OSM remains visible underneath",
      "normal blue pins remain readable and dominant",
      "player marker and capture radius remain unobscured",
      "no gameplay, reward, collection, or backend changes occur",
    ],
    requiredAfterExecution: [
      "all landmark-related flags are returned to false immediately",
      "no test markers, layers, DOM elements, or UI remain active",
      "a structured report records what happened",
      "the dormant baseline is visually and behaviorally restored",
    ],
    mapBoundaryRules: [
      "do not obscure or replace the existing map context",
      "do not reduce OSM visibility below safe readability",
      "do not introduce opaque or blank canvas behavior",
    ],
    markerBoundaryRules: [
      "do not replace normal blue pins",
      "do not create dense or noisy visible marker output",
      "do not make temporary markers look like approved live gameplay markers",
    ],
    dataBoundaryRules: [
      "do not enable real POI data by default",
      "do not blur the line between sample, dormant, and approved data",
      "do not create backend writes or persistent live data effects",
    ],
    gameplayBoundaryRules: [
      "do not change capture logic",
      "do not change rewards, XP, coins, collections, or progression",
      "do not change player marker or capture radius behavior",
    ],
    rollbackBoundaryRules: [
      "rollback must happen immediately after inspection",
      "rollback must restore all flags to false",
      "rollback must restore no-visible-output behavior",
    ],
    reportingBoundaryRules: [
      "report must document temporary active flags",
      "report must document map, marker, OSM, gameplay, and rollback findings",
      "report must record whether future testing should proceed or stop",
    ],
    forbiddenNow: [
      "enable custom 2.5D map by default",
      "enable landmark test markers",
      "enable sample landmark data",
      "enable AU dinosaur site data",
      "render visible markers",
      "create Leaflet markers or layers",
      "create DOM elements or manual-test UI",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with execution-boundary planning documented only and no visible landmark marker output.",
    passCriteriaForBoundaryReadiness: [
      "execution boundaries are explicit and reviewable",
      "future testing is clearly manual-only, temporary, and reversible",
      "map, marker, data, gameplay, rollback, and reporting boundaries remain separated",
      "the dormant all-false baseline remains unchanged while planning continues",
    ],
    failCriteria: [
      "any visible landmark behavior appears during this planning phase",
      "execution boundaries require gameplay or backend changes",
      "future testing cannot be contained to a manual-only reversible path",
      "boundary planning does not preserve the dormant safe baseline",
    ],
    nextSuggestedPhase: "custom-25d-landmark-visible-test-execution-boundary-review",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestRuntimeGuardPlan() {
  return {
    ok: true,
    phase: 43,
    name: "custom-25d-landmark-visible-test-runtime-guard-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Future runtime guard planning for manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    visibleMarkersDefaultOff: true,
    requiresExplicitRuntimeOptIn: true,
    requiresAllSafetyFlagsFalseByDefault: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotCreateDomElements: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    branchGuard: [
      "future visible landmark testing must not be developed directly on main",
      "manual-only visible testing must remain isolated to an explicit future branch or approved local test window",
    ],
    featureFlagGuard: [
      "ENABLE_CUSTOM_25D_MAP remains false by default",
      "ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS remains false by default",
      "ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA remains false by default",
      "ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA remains false by default",
    ],
    runtimeOptInGuard: [
      "any future visible test must require explicit runtime opt-in",
      "runtime opt-in must remain manual-only and reversible",
      "runtime opt-in must not happen automatically on load or startup",
    ],
    sampleDataGuard: [
      "sample landmark data must remain disabled by default",
      "sample data must not appear without explicit temporary approval",
    ],
    dinosaurSiteDataGuard: [
      "AU dinosaur site data must remain disabled by default",
      "dormant real landmark seed data must not become visible without explicit temporary approval",
    ],
    markerCreationGuard: [
      "no marker creation by default",
      "no automatic marker creation path may be introduced in this phase",
    ],
    layerCreationGuard: [
      "no layer creation by default",
      "no active Leaflet layer path may be introduced in this phase",
    ],
    domGuard: [
      "no DOM elements, UI panels, debug buttons, or visible manual-test tools are created",
      "no event listeners, timers, or intervals are introduced for landmark visible testing",
    ],
    osmVisibilityGuard: [
      "OSM must remain visible underneath",
      "no opaque or blank canvas background may be introduced",
      "no future runtime guard may reduce base map readability by default",
    ],
    rollbackGuard: [
      "any future runtime opt-in must be paired with immediate rollback expectations",
      "all safety flags must return to false after any future visible test window",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with runtime guard planning documented only and no visible landmark marker output.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestDataIsolationPlan() {
  return {
    ok: true,
    phase: 44,
    name: "custom-25d-landmark-visible-test-data-isolation-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Future data isolation planning for manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    dataDefaultOff: true,
    sampleDataDefaultOff: true,
    dinosaurSiteDataDefaultOff: true,
    noRealPoiExpansion: true,
    visibleMarkersDefaultOff: true,
    requiresExplicitRuntimeOptIn: true,
    doesNotLoadData: true,
    doesNotExposeData: true,
    doesNotMutateData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotCreateDomElements: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    dormantSampleDataBoundary: [
      "sample landmark data remains disabled by default",
      "sample data must not load automatically",
      "sample data must not become visible without explicit future manual opt-in",
    ],
    dormantDinosaurSiteDataBoundary: [
      "dinosaur site data remains disabled by default",
      "dinosaur site data must not load automatically",
      "dormant AU dinosaur site data must not become visible without explicit future manual opt-in",
    ],
    noAutomaticDataLoading: [
      "no data loading on startup",
      "no data loading from timers, intervals, or event listeners",
      "no automatic expansion from dormant data definitions into active visible data",
    ],
    noVisibleMarkerDataPath: [
      "no data path may create visible landmark markers by default",
      "no data path may auto-populate marker layers",
      "no data path may bypass manual-only future runtime opt-in",
    ],
    noGameplayDataPath: [
      "no data path may connect landmark data to capture logic",
      "no data path may affect progression, XP, coins, or gameplay state",
      "no data path may affect normal blue pins, player marker, or capture radius",
    ],
    noRewardsDataPath: [
      "no data path may create rewards or reward side effects",
      "no data path may award items, XP, coins, or unlocks",
    ],
    noCollectionsDataPath: [
      "no data path may populate or mutate collections",
      "no data path may expose dormant landmark data through collection systems",
    ],
    noBackendWritePath: [
      "no backend writes are allowed in this phase",
      "no runtime path may persist dormant landmark test data",
    ],
    noBackendReadPath: [
      "no backend reads are allowed in this phase",
      "no external or remote POI fetch path may be introduced in this phase",
    ],
    manualTestDataAccessOnly: [
      "any future data access must remain manual-only and explicitly approved",
      "future temporary data access must remain local, reviewable, and reversible",
    ],
    rollbackSafeDataBoundary: [
      "any future temporary data activation must return to the all-false dormant state after testing",
      "no persistent data exposure may remain after rollback",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with data isolation planning documented only and no visible landmark marker output or automatic data loading.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestUiIsolationPlan() {
  return {
    ok: true,
    phase: 45,
    name: "custom-25d-landmark-visible-test-ui-isolation-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Future UI isolation planning for manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    uiDefaultOff: true,
    visibleMarkersDefaultOff: true,
    debugUiDefaultOff: true,
    noVisibleControlsByDefault: true,
    requiresExplicitRuntimeOptIn: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreatePanels: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    noDefaultVisibleUi: [
      "no visible landmark test UI appears by default",
      "no runtime path may expose landmark test controls automatically",
    ],
    noDebugButtonCreation: [
      "no debug buttons are created in this phase",
      "no future visible test control buttons may appear without explicit manual-only approval",
    ],
    noManualTestPanelCreation: [
      "no manual test panels are created",
      "no future test panel may appear by default or on startup",
    ],
    noDomQueryRequirement: [
      "no DOM queries are required in this phase",
      "no runtime path may depend on querying UI elements for landmark testing by default",
    ],
    noEventListenerPath: [
      "no event listeners are attached in this phase",
      "no automatic click, touch, keyboard, or resize handling is introduced for landmark visible testing",
    ],
    noMarkerInteractionUi: [
      "no marker interaction UI is created",
      "no popups, tooltips, overlays, or test marker controls are introduced",
    ],
    noGameplayUiPath: [
      "no gameplay-facing UI may be altered for landmark visible testing",
      "no menus, HUD elements, or player controls may be repurposed in this phase",
    ],
    noRewardUiPath: [
      "no reward UI may be created or altered",
      "no reward previews, banners, or dialogs may be introduced",
    ],
    noCollectionUiPath: [
      "no collection UI may be created or altered",
      "no dormant landmark data may surface through collection views",
    ],
    noBackendStatusUiPath: [
      "no backend status UI may be created",
      "no data sync indicators or network status panels may be introduced for landmark testing",
    ],
    rollbackSafeUiBoundary: [
      "any future temporary UI for approved visible testing must be removable without residue",
      "post-test rollback must return the app to a no-visible-landmark-test-UI state",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with UI isolation planning documented only and no visible landmark marker output, UI creation, or event listener attachment.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestMarkerLifecyclePlan() {
  return {
    ok: true,
    phase: 46,
    name: "custom-25d-landmark-visible-test-marker-lifecycle-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Future marker lifecycle planning for manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    markersDefaultOff: true,
    visibleMarkersDefaultOff: true,
    markerLifecycleDefaultOff: true,
    noMarkerCreationByDefault: true,
    noLayerCreationByDefault: true,
    requiresExplicitRuntimeOptIn: true,
    doesNotCreateMarkers: true,
    doesNotAttachMarkers: true,
    doesNotRemoveMarkers: true,
    doesNotMutateMarkerState: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestUiIsolationPlan()",
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    noDefaultMarkerCreation: [
      "no visible landmark markers are created by default",
      "no marker lifecycle starts automatically on load or startup",
    ],
    noMarkerLayerCreation: [
      "no marker layers are created in this phase",
      "no future marker test layer may appear without explicit manual-only approval",
    ],
    noMarkerAttachPath: [
      "no marker attach path is introduced",
      "no runtime path may attach visible landmark markers to the map by default",
    ],
    noMarkerCleanupPathByDefault: [
      "no marker cleanup path is needed because no markers are created",
      "no automatic marker teardown lifecycle is introduced in this phase",
    ],
    noMapMutationPath: [
      "no map mutation path is introduced for landmark marker lifecycle behavior",
      "no add/remove/update map operations are introduced for visible landmark markers",
    ],
    noDomMarkerPath: [
      "no DOM-backed marker path is introduced",
      "no overlays, popups, or DOM marker containers are created",
    ],
    noMarkerEventListenerPath: [
      "no marker event listeners are attached",
      "no click, touch, hover, drag, or lifecycle callbacks are introduced for markers",
    ],
    noGameplayMarkerPath: [
      "no marker lifecycle behavior may connect to gameplay systems",
      "no marker lifecycle may affect normal blue pins, player marker, or capture radius",
    ],
    noRewardMarkerPath: [
      "no reward logic may be connected to marker lifecycle behavior",
      "no reward display or payout path may be introduced through markers",
    ],
    noCollectionMarkerPath: [
      "no collection logic may be connected to marker lifecycle behavior",
      "no dormant landmark data may surface through collection marker flows",
    ],
    rollbackSafeMarkerBoundary: [
      "any future temporary marker lifecycle must be removable without residue",
      "post-test rollback must return the app to a no-visible-landmark-marker state",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with marker lifecycle planning documented only and no visible landmark marker output, marker lifecycle behavior, or map mutation.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestCleanupBoundaryPlan() {
  return {
    ok: true,
    phase: 47,
    name: "custom-25d-landmark-visible-test-cleanup-boundary-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Future cleanup boundary planning for manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    cleanupDefaultOff: true,
    visibleMarkersDefaultOff: true,
    noCleanupBehaviorByDefault: true,
    noMarkerRemovalByDefault: true,
    noLayerMutationByDefault: true,
    requiresExplicitRuntimeOptIn: true,
    doesNotCreateMarkers: true,
    doesNotAttachMarkers: true,
    doesNotRemoveMarkers: true,
    doesNotMutateMarkerState: true,
    doesNotCreateLayers: true,
    doesNotMutateLayers: true,
    doesNotMutateMapState: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestMarkerLifecyclePlan()",
      "getCustom25DLandmarkVisibleTestUiIsolationPlan()",
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    noDefaultCleanupExecution: [
      "no cleanup behavior executes by default",
      "no cleanup lifecycle starts automatically on load or startup",
    ],
    noMarkerRemovalPathByDefault: [
      "no marker removal path is introduced",
      "no runtime path may remove visible landmark markers by default",
    ],
    noLayerMutationPathByDefault: [
      "no layer mutation path is introduced",
      "no future cleanup path may alter map layers without explicit manual-only approval",
    ],
    noMapMutationPathByDefault: [
      "no cleanup path may mutate map state in this phase",
      "no add, remove, clear, or reset map operations are introduced for landmark cleanup behavior",
    ],
    noDomCleanupPathByDefault: [
      "no DOM cleanup path is introduced",
      "no overlays, panels, buttons, or DOM containers are created or cleaned up in this phase",
    ],
    noEventListenerCleanupPathByDefault: [
      "no event listener cleanup path is introduced",
      "no click, touch, hover, drag, timer, or interval teardown behavior is added",
    ],
    noGameplayCleanupPath: [
      "no cleanup behavior may connect to gameplay systems",
      "no cleanup behavior may affect normal blue pins, player marker, or capture radius",
    ],
    noRewardCleanupPath: [
      "no reward cleanup behavior is introduced",
      "no reward display or payout path may be altered through cleanup behavior",
    ],
    noCollectionCleanupPath: [
      "no collection cleanup behavior is introduced",
      "no dormant landmark data may surface through cleanup-related collection flows",
    ],
    noBackendCleanupPath: [
      "no backend cleanup behavior is introduced",
      "no read, write, delete, or sync cleanup path may be introduced for landmark testing",
    ],
    rollbackSafeCleanupBoundary: [
      "any future temporary cleanup behavior must be removable without residue",
      "post-test rollback must return the app to a no-visible-landmark-cleanup-behavior state",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with cleanup boundary planning documented only and no visible landmark marker output, cleanup behavior, or layer mutation.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestManualOptInPlan() {
  return {
    ok: true,
    phase: 48,
    name: "custom-25d-landmark-visible-test-manual-opt-in-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Future manual opt-in planning for visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    requiresExplicitRuntimeOptIn: true,
    requiresDeveloperIntent: true,
    requiresLocalManualTestContext: true,
    noAutomaticOptIn: true,
    noPersistentOptIn: true,
    noRemoteOptIn: true,
    noUiOptInByDefault: true,
    visibleMarkersDefaultOff: true,
    dataDefaultOff: true,
    doesNotEnableFlags: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotLoadData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestCleanupBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestMarkerLifecyclePlan()",
      "getCustom25DLandmarkVisibleTestUiIsolationPlan()",
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    noDefaultOptIn: [
      "no visible landmark testing opt-in exists by default",
      "no automatic activation path is introduced on load or startup",
    ],
    noPersistedOptIn: [
      "no persisted opt-in state is introduced",
      "no localStorage, saved preference, or sticky test mode is introduced",
    ],
    noRemoteOptIn: [
      "no remote config or backend-driven opt-in is introduced",
      "no network-based activation path is introduced in this phase",
    ],
    noUiOptInByDefault: [
      "no UI opt-in control is created",
      "no buttons, panels, switches, menus, or debug controls are introduced",
    ],
    noAutomaticTestActivation: [
      "no timers, intervals, listeners, or startup hooks may activate landmark visible testing",
      "no runtime path may auto-enable marker visibility or data loading",
    ],
    localManualDeveloperOnlyActivationBoundary: [
      "any future opt-in must remain local and manual-only",
      "any future opt-in must require direct developer intent during an explicit test context",
    ],
    explicitRuntimeIntentBoundary: [
      "future visible testing must require deliberate runtime action",
      "future opt-in must be explicit, reviewable, and reversible",
    ],
    flagSafetyBoundary: [
      "all landmark-related safety flags remain false by default",
      "no helper in this phase enables or mutates those flags",
    ],
    dataSafetyBoundary: [
      "no sample or dinosaur site data is loaded automatically",
      "no dormant data is exposed through a visible opt-in path in this phase",
    ],
    markerSafetyBoundary: [
      "no visible landmark markers or layers are created",
      "no future opt-in path is implemented in this phase",
    ],
    rollbackSafeOptInBoundary: [
      "any future temporary opt-in must be removable without residue",
      "post-test rollback must restore the all-false dormant state immediately after testing",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with manual opt-in planning documented only and no visible landmark marker output, opt-in behavior, or data loading.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestReadinessSummaryPlan() {
  return {
    ok: true,
    phase: 49,
    name: "custom-25d-landmark-visible-test-readiness-summary-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Readiness summary planning before future manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    requiresExplicitRuntimeOptIn: true,
    requiresDeveloperIntent: true,
    visibleMarkersDefaultOff: true,
    dataDefaultOff: true,
    uiDefaultOff: true,
    cleanupDefaultOff: true,
    markerLifecycleDefaultOff: true,
    doesNotEnableFlags: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotLoadData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestManualOptInPlan()",
      "getCustom25DLandmarkVisibleTestCleanupBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestMarkerLifecyclePlan()",
      "getCustom25DLandmarkVisibleTestUiIsolationPlan()",
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    branchReadiness: [
      "future visible landmark testing must remain off main until explicitly approved",
      "manual-only testing must stay isolated to an explicit future local test context",
    ],
    safetyFlagReadiness: [
      "all landmark-related safety flags remain false by default",
      "no helper in this phase enables or mutates those flags",
    ],
    manualOptInReadiness: [
      "future visible testing requires explicit runtime opt-in",
      "future opt-in requires direct developer intent and remains reversible",
    ],
    dataIsolationReadiness: [
      "sample and dinosaur site data remain off by default",
      "no dormant data is loaded or exposed automatically",
    ],
    uiIsolationReadiness: [
      "no visible controls, debug buttons, panels, or UI are created",
      "no UI path may activate or expose visible landmark testing by default",
    ],
    markerLifecycleReadiness: [
      "no markers, layers, or lifecycle behavior are created by default",
      "no marker attach, cleanup, or removal behavior is introduced",
    ],
    cleanupBoundaryReadiness: [
      "no cleanup behavior executes by default",
      "any future cleanup path must remain removable without residue",
    ],
    rollbackReadiness: [
      "any future approved visible test must roll back to the all-false dormant state immediately after testing",
      "no persistent opt-in, marker, UI, or data side effects may remain after rollback",
    ],
    osmVisibilityReadiness: [
      "OSM must remain visible underneath",
      "no opaque or blank canvas background may be introduced",
    ],
    noGameplayImpactReadiness: [
      "no gameplay, normal blue pins, player marker, or capture radius behavior may change",
      "no rewards or collections behavior may be affected",
    ],
    noBackendImpactReadiness: [
      "no backend reads, writes, remote config, or network-driven activation may be introduced",
      "no backend state may be changed by this planning phase",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with readiness summary planning documented only and no visible landmark marker output, opt-in behavior, UI creation, or data loading.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestFinalReadinessGatePlan() {
  return {
    ok: true,
    phase: 50,
    name: "custom-25d-landmark-visible-test-final-readiness-gate-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Final readiness gate planning before future manual-only visible landmark marker testing.",
    noDefaultExecution: true,
    manualOnly: true,
    requiresExplicitRuntimeOptIn: true,
    requiresDeveloperIntent: true,
    requiresCleanMainBeforeTestBranch: true,
    requiresAllSafetyPlansReviewed: true,
    requiresRollbackPlanReady: true,
    visibleMarkersDefaultOff: true,
    dataDefaultOff: true,
    uiDefaultOff: true,
    cleanupDefaultOff: true,
    markerLifecycleDefaultOff: true,
    doesNotEnableFlags: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotLoadData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestReadinessSummaryPlan()",
      "getCustom25DLandmarkVisibleTestManualOptInPlan()",
      "getCustom25DLandmarkVisibleTestCleanupBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestMarkerLifecyclePlan()",
      "getCustom25DLandmarkVisibleTestUiIsolationPlan()",
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    cleanMainGate: [
      "main must be clean and up to date before any future visible-test branch is considered",
      "future testing must begin from an explicit safe checkpoint",
    ],
    branchSafetyGate: [
      "future visible testing must stay off main",
      "future manual-only testing must remain isolated to a dedicated branch or explicit local test context",
    ],
    safetyFlagGate: [
      "all landmark-related safety flags remain false by default",
      "no helper in this phase enables or mutates those flags",
    ],
    manualOptInGate: [
      "future visible testing requires explicit runtime opt-in",
      "future opt-in requires direct developer intent and remains reversible",
    ],
    dataIsolationGate: [
      "sample and dinosaur site data remain off by default",
      "no dormant data is loaded or exposed automatically",
    ],
    uiIsolationGate: [
      "no visible controls, panels, or debug UI are created by default",
      "no UI path may activate landmark testing automatically",
    ],
    markerLifecycleGate: [
      "no markers, layers, attach paths, or cleanup behavior are created by default",
      "no marker lifecycle may begin without an explicit future manual test path",
    ],
    cleanupBoundaryGate: [
      "no cleanup behavior executes by default",
      "any future cleanup behavior must be removable without residue",
    ],
    rollbackGate: [
      "any future approved visible test must roll back to the all-false dormant state immediately after testing",
      "no persistent opt-in, marker, UI, or data side effects may remain after rollback",
    ],
    osmVisibilityGate: [
      "OSM must remain visible underneath",
      "no opaque or blank canvas background may be introduced",
    ],
    noGameplayImpactGate: [
      "no gameplay, normal blue pins, player marker, or capture radius behavior may change",
      "no rewards or collections behavior may be affected",
    ],
    noBackendImpactGate: [
      "no backend reads, writes, remote config, or network-driven activation may be introduced",
      "no backend state may be changed by this planning phase",
    ],
    finalGoNoGoGate: [
      "future visible testing may not proceed until all prior planning gates are reviewed together",
      "any missing readiness area blocks movement from planning toward live manual testing",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with final readiness gate planning documented only and no visible landmark marker output, opt-in behavior, UI creation, or data loading.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

function getCustom25DLandmarkVisibleTestLaunchSafetyWrapperPlan() {
  return {
    ok: true,
    phase: 51,
    name: "custom-25d-landmark-visible-test-launch-safety-wrapper-plan",
    dormant: true,
    planningOnly: true,
    purpose:
      "Final launch safety wrapper planning before the first actual manual-only visible landmark marker test path.",
    noDefaultExecution: true,
    manualOnly: true,
    requiresExplicitRuntimeOptIn: true,
    requiresDeveloperIntent: true,
    requiresCleanMainBeforeVisibleTestBranch: true,
    requiresAllReadinessGatesPassed: true,
    requiresRollbackPlanReady: true,
    requiresVisibleTestScopeLimited: true,
    requiresNoDefaultUi: true,
    requiresNoDefaultDataLoading: true,
    visibleMarkersDefaultOff: true,
    dataDefaultOff: true,
    uiDefaultOff: true,
    cleanupDefaultOff: true,
    markerLifecycleDefaultOff: true,
    doesNotEnableFlags: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotLoadData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    dependsOn: [
      "getCustom25DLandmarkVisibleTestFinalReadinessGatePlan()",
      "getCustom25DLandmarkVisibleTestReadinessSummaryPlan()",
      "getCustom25DLandmarkVisibleTestManualOptInPlan()",
      "getCustom25DLandmarkVisibleTestCleanupBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestMarkerLifecyclePlan()",
      "getCustom25DLandmarkVisibleTestUiIsolationPlan()",
      "getCustom25DLandmarkVisibleTestDataIsolationPlan()",
      "getCustom25DLandmarkVisibleTestRuntimeGuardPlan()",
      "getCustom25DLandmarkVisibleTestExecutionBoundaryPlan()",
      "getCustom25DLandmarkVisibleTestGoNoGoPlan()",
      "getCustom25DLandmarkVisibleTestFinalCheckPlan()",
      "getCustom25DLandmarkVisibleTestReportPlan()",
      "getCustom25DLandmarkVisibleTestObservationPlan()",
      "getCustom25DLandmarkVisibleTestRiskPlan()",
      "getCustom25DLandmarkVisibleTestRollbackPlan()",
      "getCustom25DLandmarkVisibleTestApprovalGatePlan()",
      "getCustom25DLandmarkVisibleTestPreflightPlan()",
      "getCustom25DLandmarkVisibleTestPathPlan()",
      "getCustom25DLandmarkVisibleTestReadinessPlan()",
      "getCustom25DLandmarkNextPhasePlan()",
    ],
    cleanMainGate: [
      "main must be clean and up to date before any future visible-test branch is used",
      "future visible testing must begin from an explicit safe checkpoint",
    ],
    visibleTestBranchGate: [
      "future visible testing must stay off main",
      "future testing must remain isolated to a dedicated manual-only branch or local test context",
    ],
    explicitManualRuntimeOptInGate: [
      "future visible testing requires explicit runtime opt-in",
      "future opt-in requires direct developer intent and remains reversible",
    ],
    limitedTestScopeGate: [
      "future visible testing must remain narrowly scoped and temporary",
      "future testing may not expand into broader gameplay, UI, data, or backend behavior",
    ],
    noDefaultUiGate: [
      "no visible controls, panels, or debug UI are created by default",
      "no UI path may activate landmark testing automatically",
    ],
    noDefaultDataLoadingGate: [
      "sample and dinosaur site data remain off by default",
      "no dormant data is loaded or exposed automatically",
    ],
    markerLifecycleGate: [
      "no markers, layers, attach paths, or cleanup behavior are created by default",
      "no marker lifecycle may begin without an explicit future manual test path",
    ],
    cleanupRollbackGate: [
      "any future approved visible test must roll back to the all-false dormant state immediately after testing",
      "no persistent opt-in, marker, UI, or data side effects may remain after rollback",
    ],
    osmVisibilityGate: [
      "OSM must remain visible underneath",
      "no opaque or blank canvas background may be introduced",
    ],
    noGameplayImpactGate: [
      "no gameplay, normal blue pins, player marker, or capture radius behavior may change",
      "no rewards or collections behavior may be affected",
    ],
    noBackendImpactGate: [
      "no backend reads, writes, remote config, or network-driven activation may be introduced",
      "no backend state may be changed by this planning phase",
    ],
    finalLaunchHoldGate: [
      "future visible testing may not proceed until all prior planning gates are reviewed together",
      "any missing readiness area blocks movement from planning toward the first live manual test path",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant with launch safety wrapper planning documented only and no visible landmark marker output, opt-in behavior, UI creation, or data loading.",
    flags: {
      custom25DMap: ENABLE_CUSTOM_25D_MAP,
      landmarkTestMarkers: ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      landmarkSampleData: ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      dinosaurSitesAuData: ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA,
    },
  };
}

const CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE = {
  layer: null,
  markersCreated: 0,
  lastRunAt: null,
  lastClearAt: null
};

function canRunCustom25DLandmarkVisibleTinyManualTest(options = {}) {
  const blockedReasons = [];
  const requiredOptions = {
    manual: true,
    developerIntent: true,
    allowVisibleTestMarkers: true,
    limit: "1-3"
  };
  const requestedLimit = Number(options.limit);

  if (options.manual !== true) blockedReasons.push("options.manual must be true");
  if (options.developerIntent !== true) blockedReasons.push("options.developerIntent must be true");
  if (options.allowVisibleTestMarkers !== true) blockedReasons.push("options.allowVisibleTestMarkers must be true");
  if (!Number.isFinite(requestedLimit)) {
    blockedReasons.push("options.limit must be provided");
  } else if (requestedLimit < 1 || requestedLimit > 3) {
    blockedReasons.push("options.limit must be between 1 and 3");
  }

  if (!map) blockedReasons.push("map is unavailable");
  if (typeof L === "undefined") blockedReasons.push("Leaflet is unavailable");
  if (ENABLE_CUSTOM_25D_MAP !== false) blockedReasons.push("ENABLE_CUSTOM_25D_MAP must remain false by default");
  if (ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS !== false) blockedReasons.push("ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS must remain false by default");
  if (ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA !== false) blockedReasons.push("ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA must remain false by default");
  if (ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA !== false) blockedReasons.push("ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA must remain false by default");

  return {
    ok: blockedReasons.length === 0,
    allowed: blockedReasons.length === 0,
    blockedReasons,
    requiredOptions,
    phase: 58,
    manualOnly: true,
    defaultOff: true
  };
}

function runCustom25DLandmarkVisibleTinyManualTest(options = {}) {
  const safety = canRunCustom25DLandmarkVisibleTinyManualTest(options);
  if (!safety.allowed) {
    return {
      ok: false,
      ran: false,
      blocked: true,
      blockedReasons: safety.blockedReasons,
      requiredOptions: safety.requiredOptions,
      markersCreated: 0,
      markerLimit: 3,
      fakeInternalOnly: true,
      cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()",
      phase: 58,
      manualOnly: true,
      defaultOff: true
    };
  }

  const requestedLimit = Math.max(1, Math.min(3, Math.floor(Number(options.limit))));
  const center = map.getCenter();
  const spacingOffsets = [
    { latOffset: 0.00018, lngOffset: 0 },
    { latOffset: -0.00009, lngOffset: -0.00017 },
    { latOffset: -0.00009, lngOffset: 0.00017 }
  ];
  const fakeEntries = [
    {
      id: "tiny-manual-landmark-a",
      label: "Internal Test A"
    },
    {
      id: "tiny-manual-landmark-b",
      label: "Internal Test B"
    },
    {
      id: "tiny-manual-landmark-c",
      label: "Internal Test C"
    }
  ].map((entry, index) => ({
    ...entry,
    lat: center.lat + spacingOffsets[index].latOffset,
    lng: center.lng + spacingOffsets[index].lngOffset
  })).slice(0, requestedLimit);

  if (CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.layer && map.hasLayer(CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.layer)) {
    map.removeLayer(CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.layer);
  }

  const testLayer = L.layerGroup();
  fakeEntries.forEach((entry) => {
    const icon = L.divIcon({
      className: "",
      iconSize: [22, 28],
      iconAnchor: [11, 26],
      html: `
        <div style="position:relative;width:22px;height:28px;filter:drop-shadow(0 2px 4px rgba(73,42,4,0.28));pointer-events:none;">
          <div style="position:absolute;left:2px;top:0;width:18px;height:18px;border-radius:999px;background:linear-gradient(180deg,#f7cf72 0%,#e79b23 58%,#b86b12 100%);box-shadow:0 0 0 2px rgba(255,244,214,0.96),0 0 0 3px rgba(123,71,10,0.72),inset 0 1px 1px rgba(255,255,255,0.58);"></div>
          <div style="position:absolute;left:6px;top:4px;width:10px;height:10px;border-radius:999px;background:radial-gradient(circle at 35% 35%,#fffdf3 0%,#fff0bf 55%,#f6ca67 100%);box-shadow:inset 0 1px 1px rgba(255,255,255,0.68),0 0 0 1px rgba(150,94,22,0.32);"></div>
          <div style="position:absolute;left:8px;top:15px;width:8px;height:8px;background:linear-gradient(180deg,#d98a1c 0%,#9b560f 100%);transform:rotate(45deg);border-radius:1px 1px 3px 1px;box-shadow:0 1px 1px rgba(92,48,8,0.18);"></div>
        </div>
      `,
    });

    const marker = L.marker([entry.lat, entry.lng], {
      icon,
      pane: "markerPane",
      interactive: false,
      keyboard: false,
    });

    marker.bindTooltip(
      `
        <div style="padding:4px 6px;border-radius:10px;background:rgba(34,23,10,0.92);color:#fff6d6;border:1px solid rgba(241,192,88,0.72);box-shadow:0 2px 6px rgba(0,0,0,0.18);font-size:10px;line-height:1.15;font-weight:700;letter-spacing:0;text-align:center;white-space:nowrap;">
          Internal Landmark Test<br>
          <span style="display:block;margin-top:2px;font-size:9px;font-weight:600;color:#ffd87a;">No gameplay</span>
        </div>
      `,
      {
        permanent: true,
        direction: "top",
        offset: [0, -20],
        opacity: 1,
        interactive: false,
        className: "",
      }
    );

    marker.addTo(testLayer);
  });

  testLayer.addTo(map);
  CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.layer = testLayer;
  CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.markersCreated = fakeEntries.length;
  CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.lastRunAt = Date.now();

  return {
    ok: true,
    ran: true,
    blocked: false,
    markersCreated: fakeEntries.length,
    markerLimit: 3,
    fakeInternalOnly: true,
    cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()",
    phase: 58,
    manualOnly: true,
    defaultOff: true,
    safetySummary: {
      noGameplay: true,
      noRewards: true,
      noCollections: true,
      noBackend: true,
      normalBluePinsUnaffected: true,
      playerMarkerUnaffected: true,
      captureRadiusUnaffected: true,
      osmRemainsVisible: true
    }
  };
}

function clearCustom25DLandmarkVisibleTinyManualTest() {
  const currentLayer = CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.layer;
  const markersRemoved = CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.markersCreated || 0;

  if (currentLayer && map && map.hasLayer(currentLayer)) {
    map.removeLayer(currentLayer);
  }

  CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.layer = null;
  CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.markersCreated = 0;
  CUSTOM_25D_VISIBLE_TINY_MANUAL_TEST_STATE.lastClearAt = Date.now();

  return {
    ok: true,
    cleared: Boolean(currentLayer),
    markersRemoved,
    phase: 58,
    manualOnly: true,
    defaultOff: true
  };
}

function getCustom25DLandmarkVisibleTinyManualTestConsoleGuide() {
  return {
    ok: true,
    phase: 53,
    name: "custom-25d-landmark-visible-test-console-guide",
    dormant: true,
    planningOnly: true,
    documentationOnly: true,
    purpose:
      "Browser console guide for the Phase 52 tiny manual-only visible landmark marker test.",
    noDefaultExecution: true,
    manualOnly: true,
    doesNotRunTest: true,
    doesNotEnableFlags: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotLoadData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    requiredBranchCheckpointReminder: [
      "Use this guide only from the dedicated local manual test branch or an explicitly approved local test context.",
      "Keep the default app state unchanged before any manual console testing.",
    ],
    requiredSafetyFlagsReminder: [
      "ENABLE_CUSTOM_25D_MAP = false",
      "ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS = false",
      "ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA = false",
      "ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA = false",
    ],
    defaultBlockedCallExample: "runCustom25DLandmarkVisibleTinyManualTest()",
    expectedDefaultBlockedBehavior: [
      "Calling the manual test with no options should return a blocked result.",
      "No markers should be created.",
      "No UI or data loading should occur.",
    ],
    explicitManualOptInCallExample:
      'runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 1 })',
    clearCleanupCallExample: "clearCustom25DLandmarkVisibleTinyManualTest()",
    warnings: [
      "Local manual testing only.",
      "Never enable this behavior by default.",
      "Verify OSM remains visible underneath.",
      "Verify normal blue pins, player marker, and capture radius remain unchanged.",
      "Verify the manual clear path is used only after an intentional local manual test run.",
    ],
    helperNames: [
      "canRunCustom25DLandmarkVisibleTinyManualTest()",
      "runCustom25DLandmarkVisibleTinyManualTest()",
      "clearCustom25DLandmarkVisibleTinyManualTest()",
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant; this guide documents manual console usage only and does not run the manual test or create visible markers.",
  };
}

function getCustom25DLandmarkVisibleTinyManualTestResultReportPlan() {
  return {
    ok: true,
    phase: 55,
    name: "custom-25d-landmark-visible-test-result-report-plan",
    dormant: true,
    planningOnly: true,
    documentationOnly: true,
    purpose:
      "Document the Phase 54 manual-only visible landmark marker test result.",
    noDefaultExecution: true,
    manualOnly: true,
    doesNotRunTest: true,
    doesNotEnableFlags: true,
    doesNotCreateUi: true,
    doesNotCreateDebugButtons: true,
    doesNotCreateDomElements: true,
    doesNotAttachEventListeners: true,
    doesNotLoadData: true,
    doesNotCreateMarkers: true,
    doesNotCreateLayers: true,
    doesNotMutateMapState: true,
    doesNotChangeMapBehavior: true,
    doesNotChangeGameplay: true,
    doesNotChangeBackend: true,
    doesNotChangeRewards: true,
    doesNotChangeCollections: true,
    doesNotChangeNormalPins: true,
    doesNotChangePlayerMarker: true,
    doesNotChangeCaptureRadius: true,
    doesNotChangeOsmVisibility: true,
    reportSummary: {
      phase54Branch: "feature/custom-25d-landmark-visible-test-console-visual-check",
      phase54CodeChanges: false,
      phase54CommitNeeded: false,
      defaultBlockedCallSucceeded: true,
      manualOptInVisualCallSucceeded: true,
      markersCreated: 3,
      cleanupSucceeded: true,
      osmRemainedVisible: true,
      normalBluePinsUnaffected: true,
      playerMarkerUnaffected: true,
      captureRadiusUnaffected: true,
      uiUnaffected: true,
      gameplayUnaffected: true,
      backendUnaffected: true,
      rewardsUnaffected: true,
      collectionsUnaffected: true
    },
    consoleCommands: [
      "runCustom25DLandmarkVisibleTinyManualTest()",
      "getCustom25DLandmarkVisibleTinyManualTestConsoleGuide()",
      "runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 3 })",
      "clearCustom25DLandmarkVisibleTinyManualTest()"
    ],
    nextStepRecommendations: [
      "continue with a slightly more polished manual marker visual test",
      "still off by default",
      "still manual-only",
      "still no UI switch",
      "still no real POIs",
      "still no sample/dinosaur data loading by default"
    ],
    helperNames: [
      "getCustom25DLandmarkVisibleTinyManualTestConsoleGuide()",
      "canRunCustom25DLandmarkVisibleTinyManualTest()",
      "runCustom25DLandmarkVisibleTinyManualTest()",
      "clearCustom25DLandmarkVisibleTinyManualTest()"
    ],
    expectedCurrentBehavior:
      "Current behavior remains fully dormant; this report documents the successful manual console test result only and does not run the manual test or create visible markers."
  };
}

function getCustom25DLandmarkVisibleTinyManualTestSafetyCheck() {
  return {
    ok: true,
    phase: 60,
    name: "Manual-only landmark visible test safety check",
    dormant: true,
    purpose:
      "Describe the passive safety expectations for the guarded manual-only landmark visible marker test path.",
    requiresManualConsoleCall: true,
    requiredOptions: {
      manual: true,
      developerIntent: true,
      allowVisibleTestMarkers: true,
      limit: 3
    },
    maxMarkers: 3,
    defaultCreatesMarkers: false,
    defaultCreatesUi: false,
    defaultAddsEventListeners: false,
    defaultLoadsSampleData: false,
    defaultLoadsDinosaurSiteData: false,
    cleanupScope: "Removes only the manual fake/internal test markers and their attached labels from the dedicated manual test layer.",
    mapMovement: {
      autoPan: false,
      autoFly: false,
      autoZoom: false
    },
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    manualRunCommand:
      'runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 3 })',
    cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()"
  };
}

function getCustom25DLandmarkVisibleTinyManualTestConsoleGuideAudit() {
  return {
    ok: true,
    phase: 61,
    name: "Manual-only landmark visible test console guide audit",
    dormant: true,
    purpose:
      "Describe what the passive console guide should communicate about the guarded manual-only landmark visible test path.",
    guideShouldExplain: [
      "The visible landmark test is manual-only and off by default.",
      "Nothing appears unless the developer explicitly runs the manual console command.",
      "The test creates up to 3 fake/internal markers only.",
      "The markers use the centered triangle spacing around the current map center from Phase 59.",
      "Cleanup removes only the manual test markers and their attached labels.",
      "No gameplay, rewards, collections, backend calls, sample data, or dinosaur data are involved."
    ],
    requiredManualCommand:
      'runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 3 })',
    cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()",
    requiredOptions: {
      manual: true,
      developerIntent: true,
      allowVisibleTestMarkers: true,
      limit: 3
    },
    expectedLimit: 3,
    expectedMarkerLayout: "Centered triangle spacing around the current map center for up to 3 fake/internal test markers.",
    expectedNoMapMovement: {
      autoPan: false,
      autoFly: false,
      autoZoom: false
    },
    expectedNoDefaultBehavior: {
      createsMarkersOnLoad: false,
      createsUiOnLoad: false,
      addsEventListenersOnLoad: false,
      loadsSampleDataOnLoad: false,
      loadsDinosaurSiteDataOnLoad: false
    },
    expectedCleanupScope:
      "Removes only the manual fake/internal test markers and their attached labels from the dedicated manual test layer.",
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    notes: [
      "Keep marker style unchanged from Phase 56.",
      "Keep labels unchanged from Phase 57.",
      "Keep result objects unchanged from Phase 58.",
      "Keep spacing unchanged from Phase 59.",
      "Keep safety helper unchanged from Phase 60 unless placement consistency is required."
    ]
  };
}

function getCustom25DLandmarkVisibleTinyManualTestObservationAudit() {
  return {
    ok: true,
    phase: 62,
    name: "Manual-only landmark visible test observation audit",
    dormant: true,
    purpose:
      "Describe the passive visual observation checklist for the guarded manual-only landmark visible marker test path.",
    manualRunCommand:
      'runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 3 })',
    cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()",
    expectedVisibleResult:
      "Up to 3 fake/internal landmark visible test markers appear only after the explicit manual console command.",
    expectedMarkerCount: 3,
    expectedMarkerLayout:
      "Centered triangle spacing around the current map center from Phase 59, visually less cramped than the earlier layout.",
    expectedMarkerStyle:
      "Small gold/orange landmark-style manual test markers from Phase 56 remain unchanged.",
    expectedLabels:
      'Phase 57 permanent labels remain attached and communicate "Internal Landmark Test" and "No gameplay".',
    expectedResultObject:
      "Phase 58 result object behavior remains unchanged and reports guarded/manual-only test details without changing app behavior.",
    expectedCleanupResult:
      "Cleanup removes only the manual test markers and their attached labels from the dedicated manual test layer.",
    expectedNoDefaultBehavior: {
      createsMarkersOnLoad: false,
      createsUiOnLoad: false,
      addsEventListenersOnLoad: false,
      loadsSampleDataOnLoad: false,
      loadsDinosaurSiteDataOnLoad: false
    },
    expectedNoMapMovement: {
      autoPan: false,
      autoFly: false,
      autoZoom: false
    },
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    passCriteria: [
      "Nothing appears on normal/default app load.",
      "The manual command creates no more than 3 fake/internal markers.",
      "The marker layout is visibly less cramped than before Phase 59.",
      "Labels remain attached to the test markers.",
      "The result object is returned as expected.",
      "Cleanup removes the manual markers and labels.",
      "Normal gameplay and map systems remain unchanged."
    ],
    failCriteria: [
      "Markers appear on default app load.",
      "More than 3 markers appear.",
      "Marker style changes unexpectedly.",
      "Labels change unexpectedly.",
      "Result object changes unexpectedly.",
      "Cleanup removes anything beyond manual test markers and labels.",
      "The map moves automatically.",
      "Sample or dinosaur data loads.",
      "Normal gameplay or map systems change."
    ],
    notes: [
      "Do not change marker style from Phase 56.",
      "Do not change labels from Phase 57.",
      "Do not change result objects from Phase 58.",
      "Do not change spacing from Phase 59.",
      "Do not change safety helper from Phase 60.",
      "Do not change console guide audit from Phase 61."
    ]
  };
}

function getCustom25DLandmarkVisibleTinyManualTestFinalChecklist() {
  return {
    ok: true,
    phase: 63,
    name: "Manual-only landmark visible test final checklist",
    dormant: true,
    purpose:
      "Summarize the final passive checklist for deciding whether the guarded manual-only landmark visible test path is complete.",
    manualRunCommand:
      'runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 3 })',
    cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()",
    preRunChecks: [
      "Confirm all four safety flags remain false.",
      "Confirm the app is in its normal default state with no visible test markers.",
      "Confirm no sample or dinosaur site data is loaded by default.",
      "Confirm no extra UI or event listeners were added for the manual test path."
    ],
    duringRunChecks: [
      "Manual command creates no more than 3 fake/internal markers.",
      "Markers use the Phase 56 visible marker style.",
      "Labels use the Phase 57 label behavior and text.",
      "Result object behavior from Phase 58 remains unchanged.",
      "Spacing uses the Phase 59 centered triangle layout around the current map center.",
      "No auto-pan, auto-fly, or auto-zoom occurs.",
      "No gameplay, backend, rewards, collections, or map systems are affected."
    ],
    cleanupChecks: [
      "Cleanup removes only the manual test markers and their attached labels.",
      "Cleanup does not affect normal blue pins, player marker, capture radius, or other map content.",
      "Cleanup does not leave test UI or extra listeners behind."
    ],
    defaultLoadChecks: [
      "Default app load creates no visible test markers.",
      "Default app load creates no new UI.",
      "Default app load creates no new event listeners.",
      "Default app load does not load sample data.",
      "Default app load does not load dinosaur site data."
    ],
    expectedMarkerLimit: 3,
    expectedMarkerLayout:
      "Centered triangle spacing around the current map center from Phase 59.",
    expectedMarkerStyle:
      "Phase 56 small gold/orange landmark-style manual test markers remain unchanged.",
    expectedLabels:
      'Phase 57 labels remain attached and communicate "Internal Landmark Test" and "No gameplay".',
    expectedResultObject:
      "Phase 58 result object behavior remains unchanged and guarded/manual-only.",
    expectedNoMapMovement: {
      autoPan: false,
      autoFly: false,
      autoZoom: false
    },
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    completionCriteria: [
      "All helpers exist exactly once.",
      "Syntax check passes.",
      "Flags remain false.",
      "Default app behavior is unchanged.",
      "Manual test remains explicitly console-only.",
      "Manual test remains fake/internal only.",
      "Cleanup is safe and narrow.",
      "No visible debug UI has been added.",
      "No data loads by default."
    ],
    doNotProceedIf: [
      "Any marker appears without the manual console command.",
      "Any flag is enabled by default.",
      "More than 3 markers can appear in this manual test.",
      "Map movement happens automatically.",
      "Cleanup affects anything beyond manual test markers and labels.",
      "Sample or dinosaur data loads by default.",
      "Normal gameplay or map systems change."
    ],
    notes: [
      "Do not change marker style from Phase 56.",
      "Do not change labels from Phase 57.",
      "Do not change result objects from Phase 58.",
      "Do not change spacing from Phase 59.",
      "Do not change safety helper from Phase 60.",
      "Do not change console guide audit from Phase 61.",
      "Do not change observation audit from Phase 62."
    ]
  };
}

function getCustom25DLandmarkVisibleTinyManualTestCompletionDecision() {
  return {
    ok: true,
    phase: 64,
    name: "Manual-only landmark visible test completion decision",
    dormant: true,
    purpose:
      "Summarize whether the guarded manual-only landmark visible test path is complete enough and what the safe future options are.",
    currentStatus: "ready-for-merge-review",
    completedPieces: [
      "guarded manual-only console run path",
      "required developer opt-in options",
      "max marker count of 3",
      "Phase 56 visible marker style",
      "Phase 57 label behavior/text",
      "Phase 58 result object behavior",
      "Phase 59 centered triangle marker spacing",
      "Phase 60 safety check helper",
      "Phase 61 console guide audit helper",
      "Phase 62 observation audit helper",
      "Phase 63 final checklist helper",
      "safe cleanup command",
      "no default marker creation",
      "no default UI creation",
      "no default event listeners",
      "no sample data loading",
      "no dinosaur site data loading",
      "no auto-pan, auto-fly, or auto-zoom"
    ],
    manualRunCommand:
      'runCustom25DLandmarkVisibleTinyManualTest({ manual: true, developerIntent: true, allowVisibleTestMarkers: true, limit: 3 })',
    cleanupCommand: "clearCustom25DLandmarkVisibleTinyManualTest()",
    completionSummary:
      "The manual visible landmark test path is fully guarded, fake/internal only, visually inspectable by explicit console call, and safely removable by the dedicated cleanup command.",
    safeFutureOptions: [
      "keep the manual visible test path dormant for future developer inspection",
      "remove the manual visible test path later if it is no longer needed",
      "use the guarded manual-only pattern later for real landmark POI development",
      "create a separate future branch for real landmark, dinosaur, or film-location data only after this test path is merged and stable"
    ],
    recommendedNextStep:
      "Treat this manual visible marker test path as complete after Phase 64 is merged, unless a bug is found during manual inspection.",
    doNotDoYet: [
      "do not add real landmark data",
      "do not add dinosaur site data",
      "do not add film location data",
      "do not enable visible landmark markers by default",
      "do not connect this to gameplay",
      "do not connect this to rewards",
      "do not connect this to collections",
      "do not add backend storage",
      "do not add UI toggles",
      "do not add map auto-movement"
    ],
    protectedSystems: {
      gameplay: true,
      backend: true,
      rewards: true,
      collections: true,
      normalBluePins: true,
      playerMarker: true,
      captureRadius: true,
      osmBehavior: true
    },
    safetyFlags: {
      ENABLE_CUSTOM_25D_MAP,
      ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS,
      ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA,
      ENABLE_CUSTOM_25D_DINOSAUR_SITES_AU_DATA
    },
    notes: [
      "Do not change marker style from Phase 56.",
      "Do not change labels from Phase 57.",
      "Do not change result objects from Phase 58.",
      "Do not change spacing from Phase 59.",
      "Do not change safety helper from Phase 60.",
      "Do not change console guide audit from Phase 61.",
      "Do not change observation audit from Phase 62.",
      "Do not change final checklist from Phase 63."
    ]
  };
}

function getCustom25DLandmarkTestMarkers(bounds) {
  if (!ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS || !bounds) return [];

  const center = bounds.getCenter();
  return [
    {
      id: "landmark-test-generic",
      lat: center.lat + 0.00018,
      lng: center.lng + 0.00012,
      category: "generic"
    }
  ];
}

function drawLandmarkPreviewGlyph(ctx, x, y, size, category, color) {
  ctx.save();
  ctx.strokeStyle = color;
  ctx.fillStyle = color;
  ctx.lineCap = "round";
  ctx.lineJoin = "round";
  ctx.lineWidth = Math.max(0.75, size * 0.08);

  if (category === "dinosaur") {
    ctx.beginPath();
    ctx.arc(x - size * 0.02, y + size * 0.02, size * 0.16, 0, Math.PI * 2);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x - size * 0.04, y + size * 0.12);
    ctx.lineTo(x + size * 0.18, y - size * 0.08);
    ctx.stroke();
  } else if (category === "film") {
    ctx.beginPath();
    ctx.rect(x - size * 0.18, y - size * 0.12, size * 0.36, size * 0.24);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(x - size * 0.1, y - size * 0.12);
    ctx.lineTo(x - size * 0.1, y + size * 0.12);
    ctx.moveTo(x, y - size * 0.12);
    ctx.lineTo(x, y + size * 0.12);
    ctx.stroke();
  } else if (category === "music") {
    ctx.beginPath();
    ctx.moveTo(x - size * 0.02, y - size * 0.18);
    ctx.lineTo(x - size * 0.02, y + size * 0.08);
    ctx.lineTo(x + size * 0.16, y + size * 0.02);
    ctx.lineTo(x + size * 0.16, y - size * 0.16);
    ctx.stroke();
    ctx.beginPath();
    ctx.arc(x - size * 0.06, y + size * 0.14, size * 0.07, 0, Math.PI * 2);
    ctx.arc(x + size * 0.12, y + size * 0.08, size * 0.07, 0, Math.PI * 2);
    ctx.fill();
  } else if (category === "waterfall") {
    ctx.beginPath();
    ctx.moveTo(x - size * 0.14, y - size * 0.14);
    ctx.quadraticCurveTo(x - size * 0.04, y - size * 0.02, x - size * 0.08, y + size * 0.18);
    ctx.moveTo(x + size * 0.05, y - size * 0.14);
    ctx.quadraticCurveTo(x + size * 0.15, y - size * 0.02, x + size * 0.1, y + size * 0.18);
    ctx.stroke();
  } else if (category === "beach") {
    ctx.beginPath();
    ctx.arc(x - size * 0.02, y + size * 0.02, size * 0.07, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(x - size * 0.02, y + size * 0.02);
    ctx.lineTo(x + size * 0.12, y - size * 0.16);
    ctx.stroke();
  } else if (category === "historic") {
    ctx.beginPath();
    ctx.moveTo(x - size * 0.16, y + size * 0.12);
    ctx.lineTo(x + size * 0.16, y + size * 0.12);
    ctx.moveTo(x - size * 0.12, y + size * 0.12);
    ctx.lineTo(x - size * 0.12, y - size * 0.08);
    ctx.moveTo(x, y + size * 0.12);
    ctx.lineTo(x, y - size * 0.08);
    ctx.moveTo(x + size * 0.12, y + size * 0.12);
    ctx.lineTo(x + size * 0.12, y - size * 0.08);
    ctx.moveTo(x - size * 0.18, y - size * 0.08);
    ctx.lineTo(x, y - size * 0.18);
    ctx.lineTo(x + size * 0.18, y - size * 0.08);
    ctx.stroke();
  } else {
    ctx.beginPath();
    ctx.arc(x, y, size * 0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(x, y - size * 0.18);
    ctx.lineTo(x, y - size * 0.03);
    ctx.stroke();
  }

  ctx.restore();
}

function drawSpecialPoiFoundation(ctx, point, recipe, category = "generic") {
  const outerRadius = 13;
  const innerRadius = 9.2;
  const glowRadius = 16.5;
  const gradient = ctx.createRadialGradient(point.x, point.y - 1.5, 2, point.x, point.y, innerRadius);
  gradient.addColorStop(0, recipe.innerTop);
  gradient.addColorStop(1, recipe.innerBottom);

  ctx.save();
  ctx.shadowColor = recipe.ringShadow;
  ctx.shadowBlur = 6;
  ctx.shadowOffsetY = 1.5;

  ctx.fillStyle = recipe.glow;
  ctx.beginPath();
  ctx.arc(point.x, point.y, glowRadius, 0, Math.PI * 2);
  ctx.fill();

  ctx.fillStyle = recipe.ring;
  ctx.beginPath();
  ctx.arc(point.x, point.y, outerRadius, 0, Math.PI * 2);
  ctx.fill();

  ctx.shadowColor = "transparent";
  ctx.shadowBlur = 0;
  ctx.shadowOffsetY = 0;

  ctx.fillStyle = "rgba(255,255,255,0.96)";
  ctx.beginPath();
  ctx.arc(point.x, point.y, outerRadius - 2.2, 0, Math.PI * 2);
  ctx.fill();

  ctx.fillStyle = gradient;
  ctx.beginPath();
  ctx.arc(point.x, point.y, innerRadius, 0, Math.PI * 2);
  ctx.fill();

  ctx.strokeStyle = "rgba(255,255,255,0.32)";
  ctx.lineWidth = 1;
  ctx.beginPath();
  ctx.arc(point.x, point.y - 0.6, innerRadius - 1.25, Math.PI * 1.08, Math.PI * 1.9);
  ctx.stroke();

  drawLandmarkPreviewGlyph(ctx, point.x, point.y, innerRadius * 1.5, category, recipe.glyph);
  ctx.restore();
}

function renderCustomLandmarkLayer(ctx, bounds) {
  const activeLandmarks = getActiveCustom25DLandmarkData();
  const testMarkers = getCustom25DLandmarkTestMarkers(bounds);
  const allMarkers = [...activeLandmarks, ...testMarkers];
  if (!allMarkers.length) return;

  allMarkers.forEach((marker) => {
    const point = map.latLngToLayerPoint([marker.lat, marker.lng]);
    if (!bounds.contains([marker.lat, marker.lng])) return;
    const rendererCategory = marker.rendererCategory || marker.category || "generic";
    drawSpecialPoiFoundation(ctx, point, getLandmarkVisualRecipe(rendererCategory), rendererCategory);
  });
}

function drawCustom25DLandmarkFoundation(ctx, bounds) {
  if (!ENABLE_CUSTOM_25D_MAP) return;
  if (!ENABLE_CUSTOM_25D_LANDMARK_TEST_MARKERS && !ENABLE_CUSTOM_25D_LANDMARK_SAMPLE_DATA) return;
  renderCustomLandmarkLayer(ctx, bounds);
}
/* CUSTOM 2.5D MAP EXPERIMENT END */
/* CUSTOM 2.5D MAP EXPERIMENT END */

/* ----------------------------- */
/* MAP SETUP */
/* ----------------------------- */

function initMap() {
  map = L.map("map", {
    zoomControl: false,
    doubleClickZoom: false,
    preferCanvas: true
  }).setView(DEFAULT_CENTER, 17);


  L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
    maxZoom: 20,
    attribution: "&copy; OpenStreetMap contributors"
  }).addTo(map);

  initCustom25DMapExperiment();

  pinsLayer = L.layerGroup().addTo(map);

  map.on("moveend zoomend", () => {
    scheduleRedrawPins();
    requestRoadPinsForCurrentView(false);
  });

  map.on("click", closeMenu);
  map.on("dblclick", recenterMapOnPlayer);
}

function recenterMapOnPlayer(event) {
  if (event?.originalEvent) {
    L.DomEvent.preventDefault(event.originalEvent);
  }

  if (!playerLatLng) {
    showToast("Location", "Player location not ready yet.");
    return;
  }

  closeMenu();
  map.setView(playerLatLng, Math.max(map.getZoom(), 18), {
    animate: true
  });
}

/* ----------------------------- */
/* UI */
/* ----------------------------- */

function initBasicUi() {
  if (avatarButton) {
    avatarButton.addEventListener("click", (event) => {
      event.stopPropagation();
      toggleMenu();
    });
  }

  if (menuOverlay) {
    menuOverlay.addEventListener("pointerdown", (event) => {
      event.preventDefault();
      event.stopPropagation();
      closeMenu();
    });
    menuOverlay.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      closeMenu();
    });
  }

  if (sideMenu) {
    document.addEventListener("pointerdown", handleOutsideMenuPointerDown, true);

    sideMenu.addEventListener("click", (event) => {
      const button = event.target.closest(".menu-btn");
      if (!button) return;

      const rawLabel = button.innerText || button.textContent || "";
      const label = rawLabel.trim().toLowerCase();

      if (label.includes("stats")) {
        openStats();
        return;
      }

      if (label.includes("leaders")) {
        openLeaders();
        return;
      }

      if (label.includes("achievements")) {
        openAchievements();
        return;
      }

      if (label.includes("social") || button.id === "socialBtn") {
        openSocial();
        return;
      }

      if (label.includes("crafting")) {
        openCrafting();
        return;
      }

      if (label.includes("market") || button.id === "marketBtn") {
        openMarket();
        return;
      }

      if (label.includes("inventory") || button.id === "inventoryBtn") {
        openInventory();
        return;
      }

      if (label.includes("collections")) {
        openCollections();
        return;
      }

      if (label.includes("settings")) {
        openSettings();
        return;
      }

      showToast("Coming soon", `${rawLabel.trim()} screen is wired for the next build.`);
    });

    setupMenuBackSwipe();
  }
}

function handleOutsideMenuPointerDown(event) {
  if (!sideMenu || !sideMenu.classList.contains("open")) return;
  if (sideMenu.contains(event.target)) return;

  event.preventDefault();
  event.stopPropagation();
  closeMenu();
}

function setupMenuBackSwipe() {
  if (!sideMenu) return;

  const SWIPE_BACK_MIN_X = 36;
  const SWIPE_BACK_TRIGGER_X = 48;
  const SWIPE_BACK_MAX_Y = 82;
  const SWIPE_BACK_RATIO = 0.72;

  function shouldIgnoreMenuBackSwipe(target) {
    if (!target || typeof target.closest !== "function") return false;

    return !!target.closest(
      "#profileSwipeCard, input, textarea, select, .market-sell-overlay, .inventory-buy-overlay, .social-party-overlay, .friends-overlay, .players-met-overlay, .add-friend-overlay"
    );
  }

  function handleSwipeStart(startX, startY, target) {
    if (shouldIgnoreMenuBackSwipe(target)) {
      menuBackSwipeStartX = 0;
      menuBackSwipeStartY = 0;
      menuBackSwipeActive = false;
      return;
    }

    menuBackSwipeStartX = startX;
    menuBackSwipeStartY = startY;
    menuBackSwipeActive = true;
  }

  function resetMenuBackSwipe() {
    menuBackSwipeStartX = 0;
    menuBackSwipeStartY = 0;
    menuBackSwipeActive = false;
  }

  function isMenuBackSwipe(diffX, diffY, minX) {
    if (diffX < minX) return false;
    if (Math.abs(diffY) > SWIPE_BACK_MAX_Y) return false;
    return Math.abs(diffX) >= Math.abs(diffY) * SWIPE_BACK_RATIO;
  }

  function handleSwipeMove(currentX, currentY) {
    if (!menuBackSwipeActive) return;

    const diffX = currentX - menuBackSwipeStartX;
    const diffY = currentY - menuBackSwipeStartY;

    if (!isMenuBackSwipe(diffX, diffY, SWIPE_BACK_TRIGGER_X)) return;

    resetMenuBackSwipe();
    navigateMenuBackOnePage();
  }

  function handleSwipeEnd(endX, endY) {
    if (!menuBackSwipeActive) return;

    const diffX = endX - menuBackSwipeStartX;
    const diffY = endY - menuBackSwipeStartY;

    resetMenuBackSwipe();

    if (isMenuBackSwipe(diffX, diffY, SWIPE_BACK_MIN_X)) {
      navigateMenuBackOnePage();
    }
  }

  sideMenu.addEventListener("touchstart", (event) => {
    if (!event.touches || event.touches.length !== 1) return;
    handleSwipeStart(event.touches[0].clientX, event.touches[0].clientY, event.target);
  }, { passive: true });

  sideMenu.addEventListener("touchmove", (event) => {
    if (!event.touches || event.touches.length !== 1) return;
    handleSwipeMove(event.touches[0].clientX, event.touches[0].clientY);
  }, { passive: true });

  sideMenu.addEventListener("touchend", (event) => {
    if (!event.changedTouches || event.changedTouches.length !== 1) return;
    handleSwipeEnd(event.changedTouches[0].clientX, event.changedTouches[0].clientY);
  }, { passive: true });

  sideMenu.addEventListener("touchcancel", () => {
    resetMenuBackSwipe();
  }, { passive: true });

  sideMenu.addEventListener("mousedown", (event) => {
    handleSwipeStart(event.clientX, event.clientY, event.target);
  });

  sideMenu.addEventListener("mousemove", (event) => {
    handleSwipeMove(event.clientX, event.clientY);
  });

  sideMenu.addEventListener("mouseup", (event) => {
    handleSwipeEnd(event.clientX, event.clientY);
  });

  sideMenu.addEventListener("mouseleave", () => {
    resetMenuBackSwipe();
  });

  sideMenu.addEventListener("pointerdown", (event) => {
    if (menuBackSwipePointerId !== null) return;
    menuBackSwipePointerId = event.pointerId;
    handleSwipeStart(event.clientX, event.clientY, event.target);
  });

  sideMenu.addEventListener("pointermove", (event) => {
    if (menuBackSwipePointerId !== event.pointerId) return;
    handleSwipeMove(event.clientX, event.clientY);
  });

  sideMenu.addEventListener("pointerup", (event) => {
    if (menuBackSwipePointerId !== event.pointerId) return;
    menuBackSwipePointerId = null;
    handleSwipeEnd(event.clientX, event.clientY);
  });

  sideMenu.addEventListener("pointercancel", () => {
    menuBackSwipePointerId = null;
    resetMenuBackSwipe();
  });
}

function navigateMenuBackOnePage() {
  if (!sideMenu || !sideMenu.classList.contains("open")) return false;

  if (marketScreen && !marketScreen.classList.contains("hidden")) {
    if (marketBuyView && !marketBuyView.classList.contains("hidden")) {
      showMarketDetailsView();
      return true;
    }

    if (
      (marketItemDetailsView && !marketItemDetailsView.classList.contains("hidden")) ||
      (marketListingsView && !marketListingsView.classList.contains("hidden"))
    ) {
      showMarketItemsView();
      return true;
    }

    closeMarket();
    return true;
  }

  if (inventoryScreen && !inventoryScreen.classList.contains("hidden")) {
    if (inventoryDetailsView && !inventoryDetailsView.classList.contains("hidden")) {
      selectedInventoryItemId = null;
      showInventoryListView();
      renderInventory();
      return true;
    }

    closeInventory();
    return true;
  }

  if (craftingScreen && !craftingScreen.classList.contains("hidden")) {
    if (recipeDetailsPanel && !recipeDetailsPanel.classList.contains("hidden")) {
      closeRecipeDetails();
      return true;
    }

    closeRecipeDetails();
    craftingScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  if (statsScreen && !statsScreen.classList.contains("hidden")) {
    statsScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  if (leadersScreen && !leadersScreen.classList.contains("hidden")) {
    leadersScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  if (achievementsScreen && !achievementsScreen.classList.contains("hidden")) {
    achievementsScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  if (socialScreen && !socialScreen.classList.contains("hidden")) {
    socialScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  if (settingsScreen && !settingsScreen.classList.contains("hidden")) {
    settingsScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  if (collectionsScreen && !collectionsScreen.classList.contains("hidden")) {
    collectionsScreen.classList.add("hidden");
    showMenuHome();
    return true;
  }

  return false;
}

function toggleMenu() {
  if (!sideMenu || !menuOverlay) return;

  const isOpen = sideMenu.classList.toggle("open");

  menuOverlay.classList.toggle("open", isOpen);
  document.body.classList.toggle("menu-open", isOpen);
}

function closeMenu() {
  if (sideMenu) sideMenu.classList.remove("open");
  if (menuOverlay) menuOverlay.classList.remove("open");

  document.body.classList.remove("menu-open");
}

function hideMenuHome() {
  if (menuHome) menuHome.classList.add("hidden");
}

function showMenuHome() {
  if (menuHome) menuHome.classList.remove("hidden");
}

function hideSubmenus() {
  if (statsScreen) statsScreen.classList.add("hidden");
  if (leadersScreen) leadersScreen.classList.add("hidden");
  if (achievementsScreen) achievementsScreen.classList.add("hidden");
  if (socialScreen) socialScreen.classList.add("hidden");
  if (craftingScreen) craftingScreen.classList.add("hidden");
  if (marketScreen) marketScreen.classList.add("hidden");
  if (inventoryScreen) inventoryScreen.classList.add("hidden");
  if (settingsScreen) settingsScreen.classList.add("hidden");
  if (collectionsScreen) collectionsScreen.classList.add("hidden");
}

function openCollections() {
  if (!collectionsScreen) return;

  hideSubmenus();
  hideMenuHome();
  renderCollections();
  collectionsScreen.classList.remove("hidden");
}

function openAchievements() {
  if (!achievementsScreen) return;

  hideSubmenus();
  hideMenuHome();
  renderAchievements();
  achievementsScreen.classList.remove("hidden");
}

function renderAchievements() {
  if (!achievementsList) return;

  const achievements = getAllAchievementDefinitions();
  const unlockedCount = achievements.filter((achievement) => unlockedAchievements.has(achievement.id)).length;

  achievementsList.innerHTML = `
    <div class="achievements-summary">
      <span>Unlocked</span>
      <strong>${formatNumber(unlockedCount)} / ${formatNumber(achievements.length)}</strong>
    </div>
    ${achievements.map(renderAchievementCard).join("")}
  `;
}

function getAllAchievementDefinitions() {
  const cardAchievements = CARD_SETS.map((set) => ({
    id: set.completionAchievementId,
    title: `${set.setName} Complete`,
    description: `Complete every card in ${set.setName}.`,
    cardSetId: set.setId,
    target: set.totalCards || set.cards.length
  }));

  return [
    ...POI_ACHIEVEMENTS,
    ...cardAchievements
  ];
}

function renderAchievementCard(achievement) {
  const progress = getAchievementProgress(achievement);
  const progressValue = Math.min(progress, achievement.target);
  const progressPercent = Math.min(100, Math.round((progressValue / achievement.target) * 100));
  const unlocked = unlockedAchievements.has(achievement.id);

  return `
    <div class="achievement-card ${unlocked ? "unlocked" : "locked"}">
      <div class="achievement-medal">${unlocked ? "★" : "•"}</div>
      <div class="achievement-info">
        <div class="achievement-topline">
          <h3>${escapeHtml(achievement.title)}</h3>
          <span>${unlocked ? "Unlocked" : "Locked"}</span>
        </div>
        <p>${escapeHtml(achievement.description)}</p>
        <div class="achievement-progress-track">
          <div class="achievement-progress-fill" style="width: ${progressPercent}%;"></div>
        </div>
        <div class="achievement-progress-text">
          ${formatNumber(progressValue)} / ${formatNumber(achievement.target)}
        </div>
      </div>
    </div>
  `;
}

function loadPOIs() {
  capturedPOIs = loadCapturedPOIs();

  pinStore.forEach((pin, id) => {
    if (pin.type === "poi") {
      pinStore.set(id, normalizePOI(pin));
    }
  });

  MOCK_POIS.forEach((poi) => {
    if (!pinStore.has(poi.id)) {
      addPOIToMap(poi);
    }
  });
}

function addPOIToMap(poi) {
  const normalized = normalizePOI({
    ...poi,
    type: "poi",
    discoveredBy: poi.discoveredBy || getActivePlayerId(),
    discoveredAt: poi.discoveredAt || getTrustedNow()
  });

  pinStore.set(normalized.id, normalized);
}

function normalizePOI(poi) {
  const captured = capturedPOIs.get(poi.id) || null;
  const category = poi.category || getPoiCollectionCategory(poi.poiCategory || poi.subcategory || "Places");
  const subcategory = poi.subcategory || poi.poiCategory || "Local POI";
  const name = poi.name || poi.poiName || subcategory;

  return {
    ...poi,
    id: poi.id,
    type: "poi",
    name,
    category,
    subcategory,
    rarity: poi.rarity === "special" ? "special" : "normal",
    icon: poi.icon || getPoiIconKey(subcategory),
    lat: poi.lat,
    lng: poi.lng,
    description: poi.description || `${subcategory} discovered in the world.`,
    captured: Boolean(captured || poi.captured),
    capturedAt: captured?.capturedAt || poi.capturedAt || null,
    poiName: name,
    poiCategory: subcategory
  };
}

function getPoiCollectionCategory(subcategory) {
  if (subcategory === "Church" || subcategory === "Churches") return "Places";
  if (subcategory === "Park" || subcategory === "Parks") return "Places";
  if (subcategory === "Beach" || subcategory === "Beaches") return "Places";
  if (subcategory === "Waterfall" || subcategory === "Waterfalls") return "Places";
  if (subcategory === "Landmark" || subcategory === "Landmarks") return "Places";
  if (subcategory === "Historic") return "Tourist Attractions";
  if (subcategory === "Hospital") return "Places";
  return "Tourist Attractions";
}

function getPoiIconKey(subcategory) {
  const key = String(subcategory || "").toLowerCase();
  if (key.includes("church")) return "church";
  if (key.includes("park")) return "park";
  if (key.includes("beach")) return "beach";
  if (key.includes("waterfall")) return "waterfall";
  if (key.includes("dinosaur") || key.includes("fossil")) return "dinosaur";
  if (key.includes("film")) return "film";
  if (key.includes("music")) return "music";
  if (key.includes("landmark")) return "landmark";
  return "landmark";
}

function renderPOIs() {
  clearPinIconCache();
  scheduleRedrawPins();
  renderPOICollections();
}

function renderPOICollections() {
  renderPoiPinsCollection();
}

function renderCollections() {
  renderOwnedPinsCollection();
  renderPOICollections();
  renderCardCollections();
}

function createDefaultCardCollection() {
  const now = getTrustedNow();

  return {
    ownedCards: {
      "dino-002": {
        cardId: "dino-002",
        ownedAt: now,
        source: "starter"
      },
      "dino-004": {
        cardId: "dino-004",
        ownedAt: now,
        source: "starter"
      }
    },
    completedSets: {},
    duplicateCards: {},
    packHistory: []
  };
}

function loadPlayerCardCollection() {
  try {
    const saved = JSON.parse(localStorage.getItem(CARD_COLLECTION_STORAGE_KEY) || "null");
    const defaults = createDefaultCardCollection();

    if (!saved || typeof saved !== "object") {
      savePlayerCardCollection(defaults);
      return defaults;
    }

    return {
      ...defaults,
      ...saved,
      ownedCards: {
        ...defaults.ownedCards,
        ...(saved.ownedCards || {})
      },
      completedSets: saved.completedSets || {},
      duplicateCards: saved.duplicateCards || {},
      packHistory: saved.packHistory || []
    };
  } catch (error) {
    console.warn("Could not load card collection.", error);
    return createDefaultCardCollection();
  }
}

function savePlayerCardCollection(collectionData = playerCardCollection) {
  try {
    localStorage.setItem(CARD_COLLECTION_STORAGE_KEY, JSON.stringify(collectionData));
  } catch (error) {
    console.warn("Could not save card collection.", error);
  }
}

function getAllCards() {
  return CARD_SETS.flatMap((set) => set.cards);
}

function getCardSet(setId) {
  return CARD_SETS.find((set) => set.setId === setId) || null;
}

function getCardById(cardId) {
  return getAllCards().find((card) => card.cardId === cardId) || null;
}

function getCardForPOI(poiId) {
  return getAllCards().find((card) => card.matchingPoiId === poiId) || null;
}

function isDinosaurPOI(pin) {
  const text = [
    pin?.category,
    pin?.subcategory,
    pin?.poiCategory,
    pin?.name,
    pin?.poiName,
    pin?.icon
  ].filter(Boolean).join(" ").toLowerCase();

  return text.includes("dinosaur") || text.includes("fossil");
}

function getMissingCardsInSet(setId) {
  const set = getCardSet(setId);
  if (!set) return [];

  return set.cards.filter((card) => !isCardOwned(card.cardId));
}

function pickCardRewardForPOI(pin) {
  if (isDinosaurPOI(pin)) {
    const missingCards = getMissingCardsInSet("dinosaur-discoveries");
    if (missingCards.length > 0) {
      return {
        card: missingCards[Math.floor(Math.random() * missingCards.length)],
        mode: "set-missing"
      };
    }

    const set = getCardSet("dinosaur-discoveries");
    return {
      card: set?.cards[Math.floor(Math.random() * set.cards.length)] || null,
      mode: "set-duplicate"
    };
  }

  const exactCard = getCardForPOI(pin?.id);
  return exactCard ? { card: exactCard, mode: "exact" } : null;
}

function renderPoiCardRewardLine(pin) {
  const reward = pickCardRewardForPOI(pin);
  const card = reward?.card || null;

  if (isDinosaurPOI(pin)) {
    const missingCount = getMissingCardsInSet("dinosaur-discoveries").length;

    return `
      <div class="poi-card-reward-line ${missingCount > 0 ? "new" : "owned"}">
        <span>Card Reward</span>
        <strong>Dinosaur Discoveries</strong>
        <em>${missingCount > 0 ? "Awards one missing dinosaur card" : "Set complete · duplicate card reward"}</em>
      </div>
    `;
  }

  if (!card) return "";

  const set = getCardSet(card.setId);
  const owned = isCardOwned(card.cardId);

  return `
    <div class="poi-card-reward-line ${owned ? "owned" : "new"}">
      <span>Card Reward</span>
      <strong>${escapeHtml(card.cardName)}</strong>
      <em>${escapeHtml(set?.setName || "Card Collection")} · ${owned ? "Already collected" : "Guaranteed on capture"}</em>
    </div>
  `;
}

function isCardOwned(cardId) {
  return Boolean(playerCardCollection?.ownedCards?.[cardId]);
}

function getCardSetProgress(set) {
  const ownedCount = set.cards.filter((card) => isCardOwned(card.cardId)).length;

  return {
    ownedCount,
    totalCards: set.totalCards || set.cards.length,
    complete: ownedCount >= (set.totalCards || set.cards.length)
  };
}

function renderCardCollections() {
  if (!cardSetsView || !cardCollectionsCount) return;

  const totals = CARD_SETS.reduce((summary, set) => {
    const progress = getCardSetProgress(set);
    summary.owned += progress.ownedCount;
    summary.total += progress.totalCards;
    return summary;
  }, { owned: 0, total: 0 });

  cardCollectionsCount.textContent = `${formatNumber(totals.owned)} / ${formatNumber(totals.total)}`;

  if (activeCardSetId) {
    openCardSet(activeCardSetId, false);
    return;
  }

  showCardSetsView();
  cardSetsView.innerHTML = CARD_SETS.map(renderCardSetBack).join("");
}

function renderCardSetBack(set) {
  const progress = getCardSetProgress(set);
  const completion = playerCardCollection.completedSets?.[set.setId] || null;
  const completeClass = completion ? "complete" : "";
  const rewardLabel = completion?.titleAwarded || set.completedTitle || "Complete";
  const coverArt = set.coverImage
    ? `<img class="card-set-back-cover-image" src="${escapeAttribute(set.coverImage)}" alt="${escapeAttribute(set.setName)} cover art" loading="lazy" />`
    : `<span>${escapeHtml(set.themeIcon || "★")}</span>`;

  return `
    <button class="card-set-back ${escapeAttribute(set.themeClass || "")} ${completeClass}" data-card-set-id="${escapeAttribute(set.setId)}" type="button">
      <div class="card-set-back-art">
        ${coverArt}
      </div>
      <div class="card-set-back-name">${escapeHtml(set.setName)}</div>
      <div class="card-set-back-progress">${formatNumber(progress.ownedCount)} / ${formatNumber(progress.totalCards)}</div>
      ${completion ? `<div class="card-set-complete-badge">${escapeHtml(rewardLabel)}</div>` : ""}
    </button>
  `;
}

function showCardSetsView() {
  activeCardSetId = null;
  if (cardSetsView) cardSetsView.classList.remove("hidden");
  if (cardSetDetailView) cardSetDetailView.classList.add("hidden");
}

function openCardSet(setId, updateActive = true) {
  const set = getCardSet(setId);
  if (!set || !cardSetDetailView || !cardSetGrid || !cardSetDetailHeader) return;

  if (updateActive) {
    activeCardSetId = setId;
  }

  const progress = getCardSetProgress(set);
  if (cardSetsView) cardSetsView.classList.add("hidden");
  cardSetDetailView.classList.remove("hidden");
  cardSetDetailHeader.innerHTML = `
    <div class="card-set-detail-title">
      <span>${escapeHtml(set.themeIcon || "★")}</span>
      <div>
        <h4>${escapeHtml(set.setName)}</h4>
        <p>${formatNumber(progress.ownedCount)} / ${formatNumber(progress.totalCards)} cards collected</p>
      </div>
    </div>
    ${renderCardSetRewardSummary(set)}
  `;
  cardSetGrid.innerHTML = set.cards.map((card) => renderCardSlot(card, set)).join("");
}

function renderCardSetRewardSummary(set) {
  const completion = playerCardCollection.completedSets?.[set.setId] || null;
  const artifact = set.artifactRewardId ? CARD_ARTIFACTS[set.artifactRewardId] : null;

  if (!completion) {
    return `
      <div class="card-set-reward-summary">
        <span>Completion Reward</span>
        <strong>Title: ${escapeHtml(set.completedTitle || "Collector")}</strong>
        ${artifact ? `<em>Artifact: ${escapeHtml(artifact.name)}</em>` : ""}
      </div>
    `;
  }

  return `
    <div class="card-set-reward-summary complete">
      <span>Completed</span>
      <strong>Title: ${escapeHtml(completion.titleAwarded || set.completedTitle || "Collector")}</strong>
      ${completion.artifactAwarded && artifact ? `<em>Artifact: ${escapeHtml(artifact.name)}</em>` : ""}
    </div>
  `;
}

function renderCardSlot(card, set) {
  const owned = isCardOwned(card.cardId);
  const ownedClass = owned ? "owned" : "unowned";
  const art = getCardArtMarkup(card, owned);

  return `
    <button
      class="collection-card-slot ${ownedClass} rarity-${escapeAttribute(card.rarity)}"
      data-card-id="${escapeAttribute(card.cardId)}"
      type="button"
      aria-label="${escapeAttribute(card.cardName)} ${owned ? "owned" : "unowned"}"
    >
      <div class="collection-card-art">
        ${art}
      </div>
      <div class="collection-card-label">
        <span>${formatNumber(card.cardNumber)}</span>
        <strong>${escapeHtml(card.cardName)}</strong>
      </div>
      ${owned ? `<div class="collection-card-owned-pill">Owned</div>` : `<div class="collection-card-ghost-pill">Missing</div>`}
    </button>
  `;
}

function getCardArtMarkup(card, owned) {
  const set = getCardSet(card.setId);
  const icon = set?.themeIcon || "★";
  const initial = String(card.cardName || "?").charAt(0).toUpperCase();
  const cardImage = owned ? (card.image || null) : (card.ghostImage || card.image || null);

  if (cardImage) {
    return `
      <div class="collection-card-art-inner image-card-art">
        <img class="collection-card-image" src="${escapeAttribute(cardImage)}" alt="${escapeAttribute(card.cardName)} card art" loading="lazy" />
      </div>
    `;
  }

  if (card.setId === "dinosaur-discoveries") {
    return getDinosaurCardArtMarkup(card, owned);
  }

  return `
    <div class="collection-card-art-inner">
      <div class="collection-card-icon">${escapeHtml(owned ? icon : initial)}</div>
    </div>
  `;
}

function getDinosaurCardArtMarkup(card, owned) {
  const profile = DINOSAUR_CARD_ART_PROFILES[card.cardName] || {
    badge: "Fossil",
    habitat: "Discovery Zone",
    frameClass: "theropod",
    silhouetteClass: "theropod"
  };

  const ownedStateClass = owned ? "owned" : "ghost";

  return `
    <div class="collection-card-art-inner dinosaur-card-art ${escapeAttribute(profile.frameClass)} ${ownedStateClass}">
      <div class="dinosaur-card-foil"></div>
      <div class="dinosaur-card-sky"></div>
      <div class="dinosaur-card-sun"></div>
      <div class="dinosaur-card-silhouette ${escapeAttribute(profile.silhouetteClass)}"></div>
      <div class="dinosaur-card-ground"></div>
      <div class="dinosaur-card-topline">
        <span>#${formatNumber(card.cardNumber)}</span>
        <strong>${escapeHtml(profile.badge)}</strong>
      </div>
      <div class="dinosaur-card-bottomline">
        <strong>${escapeHtml(card.cardName)}</strong>
        <span>${escapeHtml(profile.habitat)}</span>
      </div>
    </div>
  `;
}

function openCardPreview(cardId) {
  const card = getCardById(cardId);
  const set = card ? getCardSet(card.setId) : null;
  if (!card || !set || !isCardOwned(card.cardId)) return;

  closeCardPreview();

  const overlay = document.createElement("div");
  overlay.id = "cardPreviewOverlay";
  overlay.className = "card-preview-overlay";
  overlay.innerHTML = `
    <div class="card-preview-card rarity-${escapeAttribute(card.rarity)}">
      <div class="card-preview-art">
        ${getCardArtMarkup(card, true)}
      </div>
      <h3>${escapeHtml(card.cardName)}</h3>
      <p>Card #${formatNumber(card.cardNumber)} / ${formatNumber(set.totalCards)}</p>
      <p>${escapeHtml(set.setName)}</p>
    </div>
  `;

  overlay.addEventListener("click", (event) => {
    if (event.target === overlay) {
      closeCardPreview();
    }
  });

  document.body.appendChild(overlay);
}

function closeCardPreview() {
  const overlay = document.getElementById("cardPreviewOverlay");
  if (overlay) overlay.remove();
}

function handleUnownedCardLongPress(cardId) {
  const card = getCardById(cardId);
  const set = card ? getCardSet(card.setId) : null;
  if (!card || !set || isCardOwned(card.cardId)) return;

  openCardMarketPrompt(card, set);
}

function openCardMarketPrompt(card, set) {
  closeCardMarketPrompt();

  const overlay = document.createElement("div");
  overlay.id = "cardMarketPromptOverlay";
  overlay.className = "card-market-prompt-overlay";
  overlay.innerHTML = `
    <div class="card-market-prompt">
      <button class="card-market-close" data-card-market-close type="button">×</button>
      <h3>You don’t own this card.</h3>
      <p>Would you like to search for it on the Market?</p>
      <div class="card-market-missing">
        <strong>#${formatNumber(card.cardNumber)} ${escapeHtml(card.cardName)}</strong>
        <span>${escapeHtml(set.setName)}</span>
      </div>
      <div class="card-market-actions">
        <button data-card-market-close type="button">Cancel</button>
        <button data-card-market-open="${escapeAttribute(card.cardId)}" type="button">Open Market</button>
      </div>
    </div>
  `;

  overlay.addEventListener("click", (event) => {
    if (event.target === overlay || event.target.closest("[data-card-market-close]")) {
      closeCardMarketPrompt();
      return;
    }

    const marketButton = event.target.closest("[data-card-market-open]");
    if (marketButton) {
      closeCardMarketPrompt();
      openMarket();
      activeMarketCategory = "cards";
      renderMarket();
      showToast("Market", "Card trading search will be wired in the server phase.");
    }
  });

  document.body.appendChild(overlay);
}

function closeCardMarketPrompt() {
  const overlay = document.getElementById("cardMarketPromptOverlay");
  if (overlay) overlay.remove();
}

function clearCardLongPress() {
  if (cardLongPressTimer) {
    clearTimeout(cardLongPressTimer);
    cardLongPressTimer = null;
  }

  setTimeout(() => {
    cardLongPressTriggered = false;
  }, 80);
}

function awardCard(cardId, source = "test") {
  const card = getCardById(cardId);
  const set = card ? getCardSet(card.setId) : null;
  if (!card || !set) return null;

  if (isCardOwned(cardId)) {
    handleDuplicateCard(cardId);
    showToast("Duplicate Card Found!", `${card.cardName} added to Inventory.`);
    return { status: "duplicate", card };
  }

  playerCardCollection.ownedCards[cardId] = {
    cardId,
    ownedAt: getTrustedNow(),
    source
  };

  checkCardSetCompletion(card.setId);
  savePlayerCardCollection();
  renderCardCollections();
  refreshInventoryIfOpen();
  showToast("New Card Found!", `${card.cardName} added to ${set.setName}.`);
  return { status: "new", card };
}

function handleDuplicateCard(cardId) {
  const card = getCardById(cardId);
  if (!card) return;

  playerCardCollection.duplicateCards[cardId] = Number(playerCardCollection.duplicateCards[cardId] || 0) + 1;
  savePlayerCardCollection();
  refreshInventoryIfOpen();
}

function awardCardPack(packSize = 1, source = "test-pack") {
  const allCards = getAllCards();
  const size = Math.max(1, Number(packSize || 1));
  const results = [];

  for (let i = 0; i < size; i += 1) {
    const card = allCards[Math.floor(Math.random() * allCards.length)];
    if (!card) continue;
    results.push(awardCard(card.cardId, source));
  }

  playerCardCollection.packHistory.push({
    source,
    packSize: size,
    openedAt: getTrustedNow(),
    results: results.filter(Boolean).map((result) => ({
      cardId: result.card.cardId,
      status: result.status
    }))
  });

  savePlayerCardCollection();
  showToast("Card Pack Opened", `${formatNumber(results.length)} card${results.length === 1 ? "" : "s"} revealed.`);
  return results;
}

function checkCardSetCompletion(setId) {
  const set = getCardSet(setId);
  if (!set) return false;
  if (playerCardCollection.completedSets?.[setId]) return false;

  const progress = getCardSetProgress(set);
  if (!progress.complete) return false;

  awardCardSetCompletionRewards(set);
  return true;
}

function awardCardSetCompletionRewards(set) {
  const completedAt = getTrustedNow();
  const titleAwarded = set.completedTitle || `${set.setName} Collector`;
  const artifact = set.artifactRewardId ? CARD_ARTIFACTS[set.artifactRewardId] : null;

  playerCardCollection.completedSets[set.setId] = {
    completedAt,
    titleAwarded,
    achievementAwarded: true,
    artifactAwarded: artifact?.artifactId || null
  };

  playerTitles.add(titleAwarded);

  if (artifact) {
    playerArtifacts.add(artifact.artifactId);
  }

  if (set.completionAchievementId) {
    unlockedAchievements.add(set.completionAchievementId);
  }

  savePlayerTitles();
  savePlayerArtifacts();
  saveUnlockedAchievements();
  savePlayerCardCollection();
  renderAchievements();
  showCardSetCompletionPopup(set, titleAwarded, artifact);
  showToast("Collection Complete!", `${set.setName} complete. Title earned: ${titleAwarded}.`);
  showRewardBurst("Complete!", "level-up");
}

function showCardSetCompletionPopup(set, titleAwarded, artifact = null) {
  closeCardSetCompletionPopup();

  const overlay = document.createElement("div");
  overlay.id = "cardCompletionOverlay";
  overlay.className = "card-completion-overlay";
  overlay.innerHTML = `
    <div class="card-completion-popup">
      <button class="card-completion-close" data-card-completion-close type="button">×</button>
      <div class="card-completion-medal">${escapeHtml(set.themeIcon || "★")}</div>
      <h3>Collection Complete!</h3>
      <h4>${escapeHtml(set.setName)}</h4>
      <div class="card-completion-reward">
        <span>Title Earned</span>
        <strong>${escapeHtml(titleAwarded)}</strong>
      </div>
      <div class="card-completion-reward">
        <span>Achievement Unlocked</span>
        <strong>${escapeHtml(set.setName)} Complete</strong>
      </div>
      ${artifact ? `
        <div class="card-completion-reward artifact">
          <span>Artifact Earned</span>
          <strong>${escapeHtml(artifact.icon)} ${escapeHtml(artifact.name)}</strong>
          <em>${escapeHtml(artifact.description)}</em>
        </div>
      ` : ""}
    </div>
  `;

  overlay.addEventListener("click", (event) => {
    if (event.target === overlay || event.target.closest("[data-card-completion-close]")) {
      closeCardSetCompletionPopup();
    }
  });

  document.body.appendChild(overlay);
}

function closeCardSetCompletionPopup() {
  const overlay = document.getElementById("cardCompletionOverlay");
  if (overlay) overlay.remove();
}

function awardFullCardSet(setId = "dinosaur-discoveries", source = "test-complete") {
  const set = getCardSet(setId);
  if (!set) return [];
  return set.cards.map((card) => awardCard(card.cardId, source));
}

window.awardCard = awardCard;
globalThis.awardCard = awardCard;
window.awardCardPack = awardCardPack;
globalThis.awardCardPack = awardCardPack;
window.awardFullCardSet = awardFullCardSet;
globalThis.awardFullCardSet = awardFullCardSet;

function renderOwnedPinsCollection() {
  if (!ownedPinsList || !ownedPinsCount) return;

  const activePlayerId = getActivePlayerId();
  const ownedPins = Array.from(pinStore.values())
    .filter((pin) => pin.ownerId === activePlayerId)
    .sort((a, b) => {
      const levelDiff = getBasePinLevel(b) - getBasePinLevel(a);
      if (levelDiff) return levelDiff;
      return Number(b.ownedAt || 0) - Number(a.ownedAt || 0);
    });

  ownedPinsCount.textContent = `${ownedPins.length} owned`;
  const pendingTotal = getOwnedPinsPendingTotal(ownedPins);

  if (ownedPinsPendingTotal) {
    ownedPinsPendingTotal.textContent = `${formatNumber(pendingTotal)} pts`;
  }

  if (claimOwnedPinRewardsBtn) {
    claimOwnedPinRewardsBtn.disabled = pendingTotal <= 0;
  }

  if (!ownedPins.length) {
    ownedPinsList.innerHTML = `
      <div class="owned-pins-empty">
        Long-press a base pin on the map to purchase your first owned pin.
      </div>
    `;
    return;
  }

  ownedPinsList.innerHTML = ownedPins.map(renderOwnedPinCard).join("");
}

function renderOwnedPinCard(pin) {
  const level = getBasePinLevel(pin);
  const levelInfo = getBasePinLevelInfo(level);
  const plantLabel = pin.plant ? getPlantStageLabel(pin.plant) : "No plant";
  const plantTiming = pin.plant ? getPlantTimingLabel(pin.plant) : "Plant a seed to start growing";
  const pending = Number(pin.ownerPendingPoints || 0);
  const replant = pin.replantEnabled ? "On" : "Off";
  const distance = getDistanceToPinLabel(pin);

  return `
    <div class="owned-pin-card" data-owned-pin-id="${escapeAttribute(pin.id)}">
      <div class="owned-pin-topline">
        <div>
          <h4>Base Pin</h4>
          <div class="owned-pin-coords">${Number(pin.lat).toFixed(5)}, ${Number(pin.lng).toFixed(5)}</div>
        </div>
        <span class="owned-pin-level ${escapeAttribute(levelInfo.className)}">
          Lv ${level} ${escapeHtml(levelInfo.name)}
        </span>
      </div>

      <div class="owned-pin-detail-row">
        <span>Plant</span>
        <strong>${escapeHtml(plantLabel)}</strong>
      </div>
      <div class="owned-pin-detail-row">
        <span>Growth</span>
        <strong>${escapeHtml(plantTiming)}</strong>
      </div>
      <div class="owned-pin-detail-row">
        <span>Replant</span>
        <strong>${escapeHtml(replant)}</strong>
      </div>
      <div class="owned-pin-detail-row">
        <span>Pending rewards</span>
        <strong>${formatNumber(pending)} pts</strong>
      </div>
      <div class="owned-pin-detail-row">
        <span>Distance</span>
        <strong>${escapeHtml(distance)}</strong>
      </div>
      <div class="owned-pin-actions">
        <button data-owned-pin-manage="${escapeAttribute(pin.id)}" type="button">Manage</button>
        <button data-owned-pin-go="${escapeAttribute(pin.id)}" type="button">Go to pin</button>
      </div>
    </div>
  `;
}

function renderPoiPinsCollection() {
  if (!poiPinsList || !poiPinsCount) return;
  closePoiDetails();

  const poiPins = Array.from(pinStore.values())
    .filter((pin) => pin.type === "poi")
    .sort((a, b) => {
      const distanceA = getDistanceToPinValue(a);
      const distanceB = getDistanceToPinValue(b);
      if (distanceA !== distanceB) return distanceA - distanceB;
      return String(a.poiName || "").localeCompare(String(b.poiName || ""));
    });

  const capturedCount = poiPins.filter((pin) => pin.captured).length;
  poiPinsCount.textContent = `${formatNumber(capturedCount)} / ${formatNumber(poiPins.length)} collected`;

  if (!poiPins.length) {
    poiPinsList.innerHTML = `
      <div class="owned-pins-empty">
        Scan nearby POIs in Settings to add churches, hospitals, parks, landmarks and local places.
      </div>
    `;
    return;
  }

  poiPinsList.innerHTML = groupPOIsByCategory(poiPins).map(renderPoiCategoryGroup).join("");
}

function groupPOIsByCategory(poiPins) {
  const groups = new Map(POI_COLLECTION_CATEGORIES.map((category) => [category, []]));

  poiPins.forEach((pin) => {
    const category = pin.category || "Tourist Attractions";
    if (!groups.has(category)) {
      groups.set(category, []);
    }

    groups.get(category).push(pin);
  });

  return Array.from(groups.entries())
    .filter(([, pins]) => pins.length > 0)
    .map(([category, pins]) => ({
      category,
      pins: pins.slice(0, 40)
    }));
}

function renderPoiCategoryGroup(group) {
  const capturedCount = group.pins.filter((pin) => pin.captured).length;
  const subcategorySummary = getPoiSubcategorySummary(group.pins);
  const subcategoryRows = getPoiSubcategoryGroups(group.pins).map(renderPoiSubcategoryRow).join("");

  return `
    <section class="poi-category-group">
      <div class="poi-category-header">
        <div>
          <h4>${escapeHtml(group.category)}</h4>
          <span>${escapeHtml(subcategorySummary)}</span>
        </div>
        <strong>${formatNumber(capturedCount)} / ${formatNumber(group.pins.length)}</strong>
      </div>
      <div class="poi-subcategory-list">
        ${subcategoryRows}
      </div>
      <div class="poi-category-list">
        ${group.pins.map(renderPoiPinCard).join("")}
      </div>
    </section>
  `;
}

function getPoiSubcategorySummary(pins) {
  const subcategories = Array.from(new Set(pins.map((pin) => pin.subcategory || pin.poiCategory || "POI")));
  return subcategories.slice(0, 3).join(" · ");
}

function getPoiSubcategoryGroups(pins) {
  const groups = new Map();

  pins.forEach((pin) => {
    const subcategory = pin.subcategory || pin.poiCategory || "POI";
    if (!groups.has(subcategory)) {
      groups.set(subcategory, []);
    }

    groups.get(subcategory).push(pin);
  });

  return Array.from(groups.entries())
    .sort(([a], [b]) => a.localeCompare(b))
    .map(([subcategory, subcategoryPins]) => ({
      subcategory,
      pins: subcategoryPins
    }));
}

function renderPoiSubcategoryRow(group) {
  const capturedCount = group.pins.filter((pin) => pin.captured).length;
  const total = group.pins.length;
  const percent = total > 0 ? Math.round((capturedCount / total) * 100) : 0;

  return `
    <div class="poi-subcategory-row">
      <span>${escapeHtml(group.subcategory)}</span>
      <div class="poi-subcategory-progress">
        <div style="width: ${percent}%;"></div>
      </div>
      <strong>${formatNumber(capturedCount)} / ${formatNumber(total)}</strong>
    </div>
  `;
}

function renderPoiPinCard(pin) {
  const distance = getDistanceToPinLabel(pin);
  const captureStatus = pin.captured ? "Collected" : "Ready";
  const category = pin.category || "Places";
  const subcategory = pin.subcategory || pin.poiCategory || "POI";
  const name = pin.name || pin.poiName || subcategory;
  const discoveredDate = pin.discoveredAt ? getDisplayDate(pin.discoveredAt) : "Unknown";

  return `
    <div class="poi-pin-card" data-poi-pin-id="${escapeAttribute(pin.id)}" role="button" tabindex="0">
      <div class="poi-pin-badge ${escapeAttribute(pin.rarity || "normal")}">${escapeHtml(getPoiIconText(pin.icon))}</div>
      <div class="poi-pin-info">
        <div class="poi-pin-topline">
          <h4>${escapeHtml(name)}</h4>
          <span class="${pin.captured ? "captured" : "ready"}">${escapeHtml(captureStatus)}</span>
        </div>
        <div class="poi-pin-meta">
          ${escapeHtml(category)} · ${escapeHtml(subcategory)} · ${escapeHtml(distance)} · Found ${escapeHtml(discoveredDate)}
        </div>
        <button data-poi-pin-go="${escapeAttribute(pin.id)}" type="button">Go to POI</button>
      </div>
    </div>
  `;
}

function initCollectionsUi() {
  if (!collectionsScreen) return;

  collectionsScreen.addEventListener("click", (event) => {
    if (event.target.closest("#openTestCardPackBtn")) {
      awardCardPack(3, "test-pack");
      return;
    }

    const setBackButton = event.target.closest("#cardSetBackBtn");
    if (setBackButton) {
      showCardSetsView();
      renderCardCollections();
      return;
    }

    const cardSetButton = event.target.closest("[data-card-set-id]");
    if (cardSetButton) {
      openCardSet(cardSetButton.dataset.cardSetId);
      return;
    }

    const cardSlot = event.target.closest("[data-card-id]");
    if (cardSlot) {
      if (cardLongPressTriggered) {
        cardLongPressTriggered = false;
        return;
      }

      const cardId = cardSlot.dataset.cardId;
      if (isCardOwned(cardId)) {
        openCardPreview(cardId);
      } else {
        showToast("Missing card", "Long-press missing cards to search the Market.");
      }
      return;
    }

    if (event.target.closest("#claimOwnedPinRewardsBtn")) {
      claimOwnedPinRewards();
      return;
    }

    const manageButton = event.target.closest("[data-owned-pin-manage]");
    if (manageButton) {
      openOwnedPinFromCollections(manageButton.dataset.ownedPinManage);
      return;
    }

    const goButton = event.target.closest("[data-owned-pin-go]");
    if (goButton) {
      goToOwnedPin(goButton.dataset.ownedPinGo);
      return;
    }

    const poiGoButton = event.target.closest("[data-poi-pin-go]");
    if (poiGoButton) {
      goToPoiPin(poiGoButton.dataset.poiPinGo);
      return;
    }

    const poiCard = event.target.closest("[data-poi-pin-id]");
    if (poiCard) {
      openPoiDetails(poiCard.dataset.poiPinId);
      return;
    }

    if (event.target.closest("[data-poi-details-close]")) {
      closePoiDetails();
    }
  });

  collectionsScreen.addEventListener("pointerdown", (event) => {
    const cardSlot = event.target.closest("[data-card-id]");
    if (!cardSlot) return;

    clearCardLongPress();
    const cardId = cardSlot.dataset.cardId;

    cardLongPressTimer = setTimeout(() => {
      cardLongPressTriggered = true;
      handleUnownedCardLongPress(cardId);
    }, CARD_LONG_PRESS_MS);
  });

  ["pointerup", "pointercancel", "pointerleave", "lostpointercapture"].forEach((eventName) => {
    collectionsScreen.addEventListener(eventName, clearCardLongPress);
  });

  collectionsScreen.addEventListener("keydown", (event) => {
    if (event.key !== "Enter" && event.key !== " ") return;

    const cardSlot = event.target.closest("[data-card-id]");
    if (cardSlot) {
      event.preventDefault();
      const cardId = cardSlot.dataset.cardId;
      if (isCardOwned(cardId)) {
        openCardPreview(cardId);
      } else {
        handleUnownedCardLongPress(cardId);
      }
      return;
    }

    const poiCard = event.target.closest("[data-poi-pin-id]");
    if (!poiCard) return;

    event.preventDefault();
    openPoiDetails(poiCard.dataset.poiPinId);
  });
}

function getDistanceToPinValue(pin) {
  if (!playerLatLng) return Number.POSITIVE_INFINITY;
  return playerLatLng.distanceTo([pin.lat, pin.lng]);
}

function getDistanceToPinLabel(pin) {
  if (!playerLatLng) return "Location needed";

  const distance = getDistanceToPinValue(pin);
  if (distance >= 1000) return `${(distance / 1000).toFixed(2)} km`;
  return `${Math.round(distance)} m`;
}

function openPoiDetails(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin || pin.type !== "poi" || !poiDetailsPanel || !poiDetailsCard) return;

  const category = pin.category || "Places";
  const subcategory = pin.subcategory || pin.poiCategory || "POI";
  const rarity = pin.rarity === "special" ? "Special" : "Normal";
  const capturedAt = pin.capturedAt ? getDisplayDate(pin.capturedAt) : "Not collected";
  const distance = getDistanceToPinLabel(pin);
  const status = pin.captured ? "Collected" : "Ready to collect";

  poiDetailsCard.innerHTML = `
    <button class="poi-details-close" data-poi-details-close type="button">×</button>
    <div class="poi-details-hero">
      <div class="poi-details-badge ${escapeAttribute(pin.rarity || "normal")}">
        ${escapeHtml(getPoiIconText(pin.icon))}
      </div>
      <div>
        <h3>${escapeHtml(pin.name || pin.poiName || subcategory)}</h3>
        <div class="poi-details-rarity ${escapeAttribute(pin.rarity || "normal")}">
          ${escapeHtml(rarity)} POI
        </div>
      </div>
    </div>

    <div class="poi-details-row">
      <span>Category</span>
      <strong>${escapeHtml(category)}</strong>
    </div>
    <div class="poi-details-row">
      <span>Subcategory</span>
      <strong>${escapeHtml(subcategory)}</strong>
    </div>
    <div class="poi-details-row">
      <span>Status</span>
      <strong>${escapeHtml(status)}</strong>
    </div>
    <div class="poi-details-row">
      <span>Captured</span>
      <strong>${escapeHtml(capturedAt)}</strong>
    </div>
    <div class="poi-details-row">
      <span>Distance</span>
      <strong>${escapeHtml(distance)}</strong>
    </div>
    <div class="poi-details-description">
      ${escapeHtml(pin.description || "A real-world point of interest.")}
    </div>
    <div class="poi-details-reward">
      ${formatNumber(POI_PIN_VALUE)} points · ${formatNumber(POI_PIN_VALUE)} XP · ${formatNumber(POI_COIN_REWARD)} coins
    </div>
    ${renderPoiCardRewardLine(pin)}
    <div class="poi-details-actions">
      <button data-poi-details-close type="button">Close</button>
      <button data-poi-pin-go="${escapeAttribute(pin.id)}" type="button">Go to POI</button>
    </div>
  `;

  poiDetailsPanel.classList.remove("hidden");
  poiDetailsPanel.scrollIntoView({ block: "nearest" });
}

function closePoiDetails() {
  if (poiDetailsPanel) {
    poiDetailsPanel.classList.add("hidden");
  }
}

function getOwnedPinsForActivePlayer() {
  const activePlayerId = getActivePlayerId();
  return Array.from(pinStore.values()).filter((pin) => pin.ownerId === activePlayerId);
}

function getOwnedPinsPendingTotal(ownedPins = getOwnedPinsForActivePlayer()) {
  return ownedPins.reduce((total, pin) => total + Number(pin.ownerPendingPoints || 0), 0);
}

function claimOwnedPinRewards() {
  const ownedPins = getOwnedPinsForActivePlayer();
  const pendingTotal = getOwnedPinsPendingTotal(ownedPins);

  if (pendingTotal <= 0) {
    showToast("Owned Pins", "No rewards ready to claim.");
    return;
  }

  ownedPins.forEach((pin) => {
    pin.ownerPendingPoints = 0;
  });

  addStat("score", pendingTotal);
  addPlayerXp(pendingTotal);
  scheduleSavePinsToLocal();
  renderCollections();
  showToast("Rewards claimed", `+${formatNumber(pendingTotal)} points and XP.`);
}

function openOwnedPinFromCollections(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin || pin.ownerId !== getActivePlayerId()) return;

  openBasePinPlantPopup(pin);
}

function goToOwnedPin(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin) return;

  closeBasePinPopup();
  closeMenu();
  map.setView([pin.lat, pin.lng], Math.max(map.getZoom(), 18), {
    animate: true
  });
}

function goToPoiPin(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin || pin.type !== "poi") return;

  closePoiDetails();
  closeMenu();
  map.setView([pin.lat, pin.lng], Math.max(map.getZoom(), 18), {
    animate: true
  });
}

function initSettingsUi() {
  if (soundEffectsToggle) {
    soundEffectsToggle.addEventListener("click", () => {
      setPlayerPreference("soundEffects", !playerState.settings.soundEffects);
    });
  }

  if (vibrationToggle) {
    vibrationToggle.addEventListener("click", () => {
      setPlayerPreference("vibration", !playerState.settings.vibration);
    });
  }

  if (copyBackupBtn) {
    copyBackupBtn.addEventListener("click", () => {
      copyLocalBackup();
    });
  }

  if (downloadBackupBtn) {
    downloadBackupBtn.addEventListener("click", () => {
      downloadLocalBackup();
    });
  }

  if (restoreBackupBtn && restoreBackupInput) {
    restoreBackupBtn.addEventListener("click", () => {
      restoreBackupInput.click();
    });

    restoreBackupInput.addEventListener("change", () => {
      handleRestoreBackupFile(restoreBackupInput.files && restoreBackupInput.files[0]);
      restoreBackupInput.value = "";
    });
  }

  if (changeAvatarBtn) {
    changeAvatarBtn.addEventListener("click", () => {
      if (menuAvatarInput) {
        menuAvatarInput.click();
      }
    });
  }

  if (clearAvatarBtn) {
    clearAvatarBtn.addEventListener("click", () => {
      playerState.avatarSrc = null;
      savePlayerState();
      clearAvatar();
      renderSocialScreen();
      updatePlayerMarkerIcon();
      showToast("Avatar cleared", "Profile picture removed from this device.");
    });
  }

  if (scanPoiBtn) {
    scanPoiBtn.addEventListener("click", () => {
      scanNearbyPois();
    });
  }

  if (advanceCropsBtn) {
    advanceCropsBtn.addEventListener("click", () => {
      advanceOwnedCropTestStage(false);
    });
  }

  if (readyCropsBtn) {
    readyCropsBtn.addEventListener("click", () => {
      advanceOwnedCropTestStage(true);
    });
  }

  if (resetLocalProgressBtn) {
    resetLocalProgressBtn.addEventListener("click", () => {
      if (!window.confirm("Reset local progress on this device?")) return;
      resetLocalProgress();
    });
  }
}

function openSettings() {
  if (!settingsScreen) return;

  hideSubmenus();
  hideMenuHome();
  renderSettings();
  settingsScreen.classList.remove("hidden");
}

function renderSettings() {
  if (settingsPlayerName) {
    settingsPlayerName.textContent = playerState.name || DEFAULT_PLAYER_NAME;
  }

  if (settingsPlayerId) {
    settingsPlayerId.textContent = getOrCreateGrowGoPlayerId();
  }

  renderSettingsToggles();
}

function setPlayerPreference(key, enabled) {
  playerState.settings = {
    ...createDefaultPlayerState().settings,
    ...(playerState.settings || {}),
    [key]: Boolean(enabled)
  };

  savePlayerState();
  renderSettingsToggles();
}

function renderSettingsToggles() {
  renderSettingsToggle(soundEffectsToggle, playerState.settings.soundEffects);
  renderSettingsToggle(vibrationToggle, playerState.settings.vibration);
}

function renderSettingsToggle(button, enabled) {
  if (!button) return;

  button.classList.toggle("off", !enabled);
  button.setAttribute("aria-pressed", enabled ? "true" : "false");

  const stateLabel = button.querySelector("strong");
  if (stateLabel) {
    stateLabel.textContent = enabled ? "On" : "Off";
  }
}

async function scanNearbyPois() {
  if (!playerLatLng) {
    showToast("POI scan", "Player location not ready yet.");
    return;
  }

  if (scanPoiBtn) {
    scanPoiBtn.disabled = true;
    scanPoiBtn.textContent = "Scanning POIs...";
  }

  try {
    const response = await fetch(OVERPASS_API_URL, {
      method: "POST",
      headers: {
        "Content-Type": "application/x-www-form-urlencoded;charset=UTF-8"
      },
      body: new URLSearchParams({
        data: buildPoiOverpassQuery(playerLatLng.lat, playerLatLng.lng)
      })
    });

    if (!response.ok) {
      throw new Error(`POI scan failed: ${response.status}`);
    }

    const data = await response.json();
    const pois = extractPoisFromOverpass(data);
    let added = 0;

    pois.slice(0, MAX_POIS_PER_SCAN).forEach((poi) => {
      if (pinStore.has(poi.id)) return;
      addPOIToMap(poi);
      added += 1;
    });

    if (added > 0) {
      addStat("newPois", added);
      checkPoiAchievements();
      scheduleSavePinsToLocal();
      clearPinIconCache();
      scheduleRedrawPins();
    }

    showToast(
      "POI scan complete",
      added > 0
        ? `${added} new POI${added === 1 ? "" : "s"} added locally.`
        : "No new POIs found nearby."
    );
  } catch (error) {
    console.warn("POI scan failed:", error);
    showToast("POI scan failed", "OpenStreetMap did not respond. Try again soon.");
  } finally {
    if (scanPoiBtn) {
      scanPoiBtn.disabled = false;
      scanPoiBtn.textContent = "Scan nearby POIs";
    }
  }
}

function buildPoiOverpassQuery(lat, lng) {
  return `
[out:json][timeout:35];
(
  nwr["amenity"="place_of_worship"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
  nwr["amenity"="hospital"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
  nwr["historic"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
  nwr["tourism"~"^(attraction|museum|gallery|viewpoint|artwork|information)$"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
  nwr["leisure"~"^(park|garden|nature_reserve)$"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
  nwr["boundary"="national_park"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
  nwr["man_made"~"^(lighthouse|tower|water_tower|obelisk)$"](around:${POI_SCAN_RADIUS_METERS},${lat},${lng});
);
out tags center ${MAX_POIS_PER_SCAN};
`;
}

function extractPoisFromOverpass(data) {
  const elements = Array.isArray(data?.elements) ? data.elements : [];
  const seen = new Set();
  const pois = [];

  elements.forEach((el) => {
    const tags = el.tags || {};
    const category = getPoiCategory(tags);
    const lat = typeof el.lat === "number" ? el.lat : el.center?.lat;
    const lng = typeof el.lon === "number" ? el.lon : el.center?.lon;

    if (!category || typeof lat !== "number" || typeof lng !== "number") return;

    const id = `poi:osm:${el.type}:${el.id}`;
    if (seen.has(id)) return;

    seen.add(id);
    pois.push({
      id,
      type: "poi",
      name: getPoiName(tags, category),
      category: getPoiCollectionCategory(category),
      subcategory: category,
      rarity: "normal",
      icon: getPoiIconKey(category),
      lat,
      lng,
      description: `${category} from OpenStreetMap.`,
      captured: false,
      capturedAt: null,
      poiCategory: category,
      poiName: getPoiName(tags, category),
      osmType: el.type,
      osmId: el.id,
      discoveredBy: getActivePlayerId(),
      discoveredAt: getTrustedNow()
    });
  });

  return pois;
}

function getPoiCategory(tags) {
  if (tags.amenity === "hospital") return "Hospital";
  if (tags.amenity === "place_of_worship") return "Church";
  if (tags.historic) return "Historic";
  if (tags.leisure === "park" || tags.leisure === "garden" || tags.leisure === "nature_reserve" || tags.boundary === "national_park") {
    return "Park";
  }
  if (tags.man_made === "lighthouse" || tags.man_made === "tower" || tags.man_made === "water_tower" || tags.man_made === "obelisk") {
    return "Landmark";
  }
  if (tags.tourism) return "Local POI";
  return "";
}

function getPoiName(tags, category) {
  return tags.name || tags["name:en"] || category || "Point of Interest";
}

function advanceOwnedCropTestStage(makeReady = false) {
  const plantedPins = getOwnedPinsForActivePlayer().filter((pin) => pin.plant?.plantedAt);

  if (!plantedPins.length) {
    showToast("Farming test", "No planted owned pins to advance.");
    return;
  }

  const now = getTrustedNow();
  const stageMs = PIN_PLANT_STAGE_HOURS * 60 * 60 * 1000;

  plantedPins.forEach((pin) => {
    const currentStage = getPinPlantStage(pin);
    const nextStage = makeReady ? 4 : Math.min(4, currentStage + 1);
    const stageAge = nextStage >= 4 ? stageMs * 3 : stageMs * (nextStage - 1);

    pin.plant.plantedAt = now - stageAge - (60 * 1000);

    if (nextStage >= 4 && pin.plant.harvestedByDay) {
      delete pin.plant.harvestedByDay[getActivePlayerId()];
    }
  });

  scheduleSavePinsToLocal();
  clearPinIconCache();
  scheduleRedrawPins();
  renderCollections();
  showToast(
    "Farming test",
    makeReady
      ? `${plantedPins.length} crop${plantedPins.length === 1 ? "" : "s"} ready to harvest.`
      : `${plantedPins.length} crop${plantedPins.length === 1 ? "" : "s"} advanced.`
  );
}

function triggerMilestoneFeedback(pattern = [35, 45, 55]) {
  if (!playerState.settings?.vibration) return;
  if (!navigator.vibrate) return;

  try {
    navigator.vibrate(pattern);
  } catch (error) {
    console.warn("Vibration feedback failed.", error);
  }
}

function triggerLevelUpFeedback() {
  triggerMilestoneFeedback([35, 45, 55]);
  showRewardBurst("Level Up!", "level-up");
}

function resetLocalProgress() {
  try {
    [
      PIN_STORAGE_KEY,
      CAPTURED_POIS_STORAGE_KEY,
      SERVER_STARTED_AT_KEY,
      AVATAR_STORAGE_KEY,
      STATS_STORAGE_KEY,
      ACHIEVEMENTS_STORAGE_KEY,
      CRAFTING_STORAGE_KEY,
      MARKET_STORAGE_KEY,
      CARD_COLLECTION_STORAGE_KEY,
      PLAYER_TITLES_STORAGE_KEY,
      PLAYER_ARTIFACTS_STORAGE_KEY,
      PLAYER_STORAGE_KEY,
      GROWGO_PLAYER_ID_KEY,
      GROWGO_PROFILE_CARD_MODE_KEY,
      "growgo-players-met",
      "growgo-friends"
    ].forEach((key) => localStorage.removeItem(key));

    localStorage.setItem(PROGRESSION_RESET_KEY, PROGRESSION_RESET_VERSION);
  } catch (error) {
    console.warn("Could not reset local progress.", error);
  }

  window.location.reload();
}
/* ----------------------------- */
/* STATS UI */
/* ----------------------------- */

function initStatsUi() {
  if (statsBack) {
    statsBack.addEventListener("click", () => {
      if (statsScreen) statsScreen.classList.add("hidden");
      showMenuHome();
    });
  }

  if (statsScreen) {
    statsScreen.addEventListener("click", (event) => {
      const tab = event.target.closest(".stats-tabs .tab");
      if (!tab) return;

      statsScreen.querySelectorAll(".stats-tabs .tab").forEach((button) => {
        button.classList.remove("active");
      });

      tab.classList.add("active");
      renderStats(tab.dataset.tab);
    });
  }
}

function openStats() {
  if (!statsScreen) return;

  resetTodayStatsIfNeeded();
  hideSubmenus();
  hideMenuHome();

  statsScreen.classList.remove("hidden");

  statsScreen.querySelectorAll(".stats-tabs .tab").forEach((tab) => {
    tab.classList.toggle("active", tab.dataset.tab === "today");
  });

  renderStats("today");
}

function renderStats(tabName) {
  if (!statsContent) return;

  resetTodayStatsIfNeeded();

  if (tabName === "today") {
    statsContent.innerHTML = `
      ${renderStatRow("Score", playerStats.today.score)}
      ${renderStatRow("Captures", playerStats.today.captures)}
      ${renderStatRow("Gold Earned", playerStats.today.goldEarned)}
      ${renderStatRow("Items Crafted", playerStats.today.itemsCrafted)}
      ${renderStatRow("Resources Gained", playerStats.today.resourcesGained)}
      ${renderStatRow("Quests Done", playerStats.today.questsDone)}
      ${renderStatRow("Birds Captured", playerStats.today.birdsCaptured)}
      ${renderStatRow("Fish Caught", playerStats.today.fishCaught)}
      ${renderStatRow("New POIs", playerStats.today.newPois)}
      ${renderStatRow("POI Captures", playerStats.today.poiCaptures)}
      ${renderStatRow("Players Met", playerStats.today.playersMet)}
    `;
    return;
  }

  if (tabName === "lifetime") {
    statsContent.innerHTML = `
      ${renderStatRow("Score", playerStats.lifetime.score)}
      ${renderStatRow("Captures", playerStats.lifetime.captures)}
      ${renderStatRow("Gold Earned", playerStats.lifetime.goldEarned)}
      ${renderStatRow("Items Crafted", playerStats.lifetime.itemsCrafted)}
      ${renderStatRow("Resources Gained", playerStats.lifetime.resourcesGained)}
      ${renderStatRow("Quests Done", playerStats.lifetime.questsDone)}
      ${renderStatRow("Birds Captured", playerStats.lifetime.birdsCaptured)}
      ${renderStatRow("Fish Caught", playerStats.lifetime.fishCaught)}
      ${renderStatRow("New POIs", playerStats.lifetime.newPois)}
      ${renderStatRow("POI Captures", playerStats.lifetime.poiCaptures)}
      ${renderStatRow("Church POIs", playerStats.lifetime.poiChurchCaptures)}
      ${renderStatRow("Hospital POIs", playerStats.lifetime.poiHospitalCaptures)}
      ${renderStatRow("Historic POIs", playerStats.lifetime.poiHistoricCaptures)}
      ${renderStatRow("Park POIs", playerStats.lifetime.poiParkCaptures)}
      ${renderStatRow("Landmark POIs", playerStats.lifetime.poiLandmarkCaptures)}
      ${renderStatRow("Local POIs", playerStats.lifetime.poiLocalCaptures)}
      ${renderStatRow("Markets Attended", playerStats.lifetime.marketsAttended)}
      ${renderStatRow("Players Met", playerStats.lifetime.playersMet)}
      ${renderStatRow("#1 Local Finishes", playerStats.lifetime.localNumberOnes)}
      ${renderStatRow("#1 Regional Finishes", playerStats.lifetime.regionalNumberOnes)}
      ${renderStatRow("#1 Global Finishes", playerStats.lifetime.globalNumberOnes)}
    `;
    return;
  }

  if (tabName === "best") {
    statsContent.innerHTML = `
      ${renderBestRow("Best Score Day", playerStats.best.score)}
      ${renderBestRow("Most Captures Day", playerStats.best.captures)}
      ${renderBestRow("Most Gold Earned Day", playerStats.best.goldEarned)}
      ${renderBestRow("Most Items Crafted Day", playerStats.best.itemsCrafted)}
      ${renderBestRow("Most Resources Gained Day", playerStats.best.resourcesGained)}
      ${renderBestRow("Most Quests Done Day", playerStats.best.questsDone)}
      ${renderBestRow("Most Birds Captured Day", playerStats.best.birdsCaptured)}
      ${renderBestRow("Most Fish Caught Day", playerStats.best.fishCaught)}
      ${renderBestRow("Most New POIs Day", playerStats.best.newPois)}
      ${renderBestRow("Most POI Captures Day", playerStats.best.poiCaptures)}
      ${renderBestRow("Most Players Met Day", playerStats.best.playersMet)}
      ${renderRankRow("Best Local Rank", playerStats.best.bestLocalRank)}
      ${renderRankRow("Best Regional Rank", playerStats.best.bestRegionalRank)}
      ${renderRankRow("Best Global Rank", playerStats.best.bestGlobalRank)}
    `;
  }
}

function getStatIcon(label) {
  const icons = {
    "Score": "⭐",
    "Captures": "📍",
    "Gold Earned": "🪙",
    "Items Crafted": "🍴",
    "Resources Gained": "🌿",
    "Quests Done": "📜",
    "Birds Captured": "🐦",
    "Fish Caught": "🐟",
    "New POIs": "🗺️",
    "POI Captures": "📌",
    "Church POIs": "⛪",
    "Hospital POIs": "🏥",
    "Historic POIs": "🏛️",
    "Park POIs": "🌳",
    "Landmark POIs": "🗿",
    "Local POIs": "🧭",
    "Players Met": "🤝",
    "Markets Attended": "🏪",
    "#1 Local Finishes": "🏆",
    "#1 Regional Finishes": "🏆",
    "#1 Global Finishes": "🏆"
  };

  return icons[label] || "✨";
}

function renderStatRow(label, value) {
  return `
    <div class="stat-card">
      <div class="stat-icon">${getStatIcon(label)}</div>
      <div class="stat-label">${escapeHtml(label)}</div>
      <div class="stat-value">${formatNumber(value)}</div>
    </div>
  `;
}

function renderBestRow(label, data) {
  const value = data?.value || 0;
  const date = data?.date || "—";

  return `
    <div class="stat-card">
      <div class="stat-icon">🏅</div>
      <div class="stat-label">${escapeHtml(label)}</div>
      <div class="stat-value">${formatNumber(value)}</div>
      <div class="stat-date">${escapeHtml(date)}</div>
    </div>
  `;
}

function renderRankRow(label, data) {
  const rank = data?.rank ? `#${data.rank}` : "—";
  const date = data?.date || "—";

  return `
    <div class="stat-card">
      <div class="stat-icon">👑</div>
      <div class="stat-label">${escapeHtml(label)}</div>
      <div class="stat-value">${escapeHtml(rank)}</div>
      <div class="stat-date">${escapeHtml(date)}</div>
    </div>
  `;
}

function formatNumber(value) {
  return Number(value || 0).toLocaleString();
}

function formatDuration(ms) {
  const totalMinutes = Math.max(0, Math.ceil(Number(ms || 0) / (1000 * 60)));
  const days = Math.floor(totalMinutes / (60 * 24));
  const hours = Math.floor((totalMinutes - (days * 60 * 24)) / 60);
  const minutes = totalMinutes % 60;

  if (days > 0) {
    return `${days}d ${hours}h`;
  }

  if (hours > 0) {
    return `${hours}h ${minutes}m`;
  }

  return `${minutes}m`;
}

function getPlayerProgressInfo() {
  const savedProgress = {
    ...createDefaultPlayerState().progress,
    ...(playerState.progress || {})
  };
  let level = Math.max(1, Number(savedProgress.level || 1));
  let totalXp = Math.max(0, Number(savedProgress.xp || 0));

  while (totalXp >= getTotalXpForLevel(level + 1)) {
    level += 1;
  }

  const currentLevelStart = getTotalXpForLevel(level);
  const nextLevelAt = getTotalXpForLevel(level + 1);
  const currentXpIntoLevel = Math.max(0, totalXp - currentLevelStart);
  const neededThisLevel = Math.max(1, nextLevelAt - currentLevelStart);
  const percent = Math.min(100, Math.max(0, (currentXpIntoLevel / neededThisLevel) * 100));

  return {
    level,
    totalXp,
    currentXpIntoLevel,
    neededThisLevel,
    percent,
    xpToGo: Math.max(0, neededThisLevel - currentXpIntoLevel)
  };
}

function renderPlayerOverview() {
  const progress = getPlayerProgressInfo();
  const currentXp = Math.floor(progress.currentXpIntoLevel);
  const neededXp = Math.max(1, progress.neededThisLevel || 1);
  const percent = Math.max(0, Math.min(100, progress.percent || 0));
  const playerName = playerState.name || DEFAULT_PLAYER_NAME;
  const coins = Number(marketState?.wallet || 0);
  const score = Number(playerStats?.lifetime?.score || 0);

  playerState.name = playerName;
  playerState.progress = {
    ...createDefaultPlayerState().progress,
    ...(playerState.progress || {}),
    level: progress.level,
    xp: progress.totalXp,
    coins,
    score
  };
  savePlayerState();

  document.querySelectorAll(".menu-player-name").forEach((el) => {
    el.textContent = playerName;
  });

  document.querySelectorAll(".menu-level-number").forEach((el) => {
    el.textContent = progress.level;
  });

  document.querySelectorAll(".menu-xp-fill").forEach((el) => {
    el.style.width = `${percent}%`;
  });

  document.querySelectorAll(".menu-xp-text").forEach((el) => {
    el.innerHTML = `
      <span>${Math.round(percent)}%</span>
      <span>${formatNumber(currentXp)} / ${formatNumber(neededXp)} XP</span>
    `;
  });

  document.querySelectorAll(".social-profile-card strong").forEach((el) => {
    el.textContent = playerName;
  });

  renderMarketWallet();
}

/* ----------------------------- */
/* LEADERS UI */
/* ----------------------------- */

const MOCK_LEADERBOARD = [];

for (let i = 1; i <= 120; i++) {
  MOCK_LEADERBOARD.push({
    rank: i,
    name: `Player${i}`,
    score: Math.floor(Math.random() * 50000) + 1000
  });
}

MOCK_LEADERBOARD[0] = {
  rank: 1,
  name: "Godspeed1074",
  score: 128450
};

MOCK_LEADERBOARD[117] = {
  rank: 118,
  name: DEFAULT_PLAYER_NAME,
  score: 0,
  me: true
};

function initLeadersUi() {
  if (leadersBackBtn) {
    leadersBackBtn.addEventListener("click", () => {
      if (leadersScreen) leadersScreen.classList.add("hidden");
      showMenuHome();
    });
  }

  if (!leadersScreen) return;

  leadersScreen.addEventListener("click", (event) => {
    const button = event.target.closest(".seg-btn");
    if (!button) return;

    const parent = button.parentElement;

    if (parent) {
      parent.querySelectorAll(".seg-btn").forEach((btn) => {
        btn.classList.remove("active");
      });
    }

    button.classList.add("active");
    renderLeaderboard();
  });
}

function openLeaders() {
  if (!leadersScreen) return;

  hideSubmenus();
  hideMenuHome();

  leadersScreen.classList.remove("hidden");
  renderLeaderboard();
}

function getLeaderBoardState() {
  const getActiveValue = (filterName, fallback) => {
    const group = leadersScreen?.querySelector(`[data-leader-filter="${filterName}"]`);
    return group?.querySelector(".seg-btn.active")?.dataset.board || fallback;
  };

  return {
    metric: getActiveValue("metric", "points"),
    scope: getActiveValue("scope", "local"),
    period: getActiveValue("period", "daily")
  };
}

function getLeaderBoardTitle(state) {
  return [state.period, state.scope, state.metric]
    .map((part) => part.replaceAll("-", " "))
    .map((part) => part.charAt(0).toUpperCase() + part.slice(1))
    .join(" ");
}

function getLeaderMetricLabel(metric) {
  if (metric === "achievements") return "Unlocked";
  if (metric === "records") return "Record";
  return "Score";
}

function getLeaderMetricValue(player, metric) {
  if (metric === "achievements") {
    return Math.max(0, Math.round((player.score || 0) / 15000));
  }

  if (metric === "records") {
    return `${Math.max(1, Math.round((player.score || 0) / 12000))}x`;
  }

  return Number(player.score || 0).toLocaleString();
}

function getLeaderSubtitle(state) {
  if (state.metric === "achievements") {
    return "Most achievements unlocked for this board.";
  }

  if (state.metric === "records") {
    return "Best streaks and standout records for this board.";
  }

  return "Top player points for this board.";
}

function renderLeaderboard() {
  if (!leaderboardList) return;

  const state = getLeaderBoardState();
  renderLeaderboardSummary(state);

  if (state.metric === "achievements") {
    renderAchievementLeaderboard(state);
    return;
  }

  if (state.metric === "records") {
    renderRecordsLeaderboard(state);
    return;
  }

  renderPointsLeaderboard(state);
}

function renderLeaderboardSummary(state) {
  if (!leaderboardSummary) return;

  leaderboardSummary.innerHTML = `
    <div>
      <span>Active Board</span>
      <strong>${escapeHtml(getLeaderBoardTitle(state))}</strong>
    </div>
    <p>${escapeHtml(getLeaderSubtitle(state))}</p>
  `;
}

function getCurrentLeaderPlayer() {
  return {
    ...(MOCK_LEADERBOARD.find((p) => p.me) || {}),
    rank: 118,
    name: playerState.name || DEFAULT_PLAYER_NAME,
    score: playerStats?.lifetime?.score || 0,
    me: true
  };
}

function renderPointsLeaderboard(state) {
  const top100 = MOCK_LEADERBOARD.slice(0, 100);
  const currentPlayer = getCurrentLeaderPlayer();

  let html = renderYourRankCard(currentPlayer, state);
  html += top100.map((player) => {
    const cardClass = [
      "leader-card",
      player.rank === 1 ? "champion" : "",
      player.me ? "my-rank" : ""
    ].filter(Boolean).join(" ");

    return `
      <div class="${cardClass}">
        <div class="leader-rank">#${player.rank}</div>

        <div class="leader-avatar">
          ${player.rank <= 3 ? "👑" : "🙂"}
        </div>

        <div class="leader-info">
          <strong>${escapeHtml(player.name)}</strong>
          <span>${escapeHtml(getLeaderBoardTitle(state).toUpperCase())}</span>
        </div>

        <div class="leader-score">
          <span>${escapeHtml(getLeaderMetricLabel(state.metric))}</span>
          <strong>${escapeHtml(getLeaderMetricValue(player, state.metric))}</strong>
        </div>
      </div>
    `;
  }).join("");

  leaderboardList.innerHTML = html;
}

function renderYourRankCard(player, state) {
  return `
    <div class="leader-card my-rank leader-your-rank">
      <div class="leader-rank">#${player.rank}</div>

      <div class="leader-avatar">⭐</div>

      <div class="leader-info">
        <strong>${escapeHtml(player.name)}</strong>
        <span>Your Rank</span>
      </div>

      <div class="leader-score">
        <span>${escapeHtml(getLeaderMetricLabel(state.metric))}</span>
        <strong>${escapeHtml(getLeaderMetricValue(player, state.metric))}</strong>
      </div>
    </div>
  `;
}

function renderAchievementLeaderboard(state) {
  const currentPlayer = getCurrentLeaderPlayer();
  const achievementPlayers = MOCK_LEADERBOARD.slice(0, 10).map((player) => ({
    ...player,
    score: getLeaderMetricValue(player, "achievements")
  }));

  leaderboardList.innerHTML = `
    ${renderYourRankCard(currentPlayer, state)}
    ${achievementPlayers.map((player) => renderPlaceholderLeaderRow(player, state, "🏅")).join("")}
  `;
}

function renderRecordsLeaderboard(state) {
  const currentPlayer = getCurrentLeaderPlayer();
  const recordPlayers = MOCK_LEADERBOARD.slice(0, 10).map((player) => ({
    ...player,
    score: getLeaderMetricValue(player, "records")
  }));

  leaderboardList.innerHTML = `
    ${renderYourRankCard(currentPlayer, state)}
    ${recordPlayers.map((player) => renderPlaceholderLeaderRow(player, state, "⚡")).join("")}
  `;
}

function renderPlaceholderLeaderRow(player, state, icon) {
  return `
    <div class="leader-card ${player.rank === 1 ? "champion" : ""}">
      <div class="leader-rank">#${player.rank}</div>
      <div class="leader-avatar">${icon}</div>
      <div class="leader-info">
        <strong>${escapeHtml(player.name)}</strong>
        <span>${escapeHtml(getLeaderBoardTitle(state).toUpperCase())}</span>
      </div>
      <div class="leader-score">
        <span>${escapeHtml(getLeaderMetricLabel(state.metric))}</span>
        <strong>${escapeHtml(String(player.score))}</strong>
      </div>
    </div>
  `;
}
/* ----------------------------- */
/* SOCIAL UI */
/* ----------------------------- */

function initSocialUi() {
  if (socialBackBtn) {
    socialBackBtn.addEventListener("click", () => {
      if (socialScreen) socialScreen.classList.add("hidden");
      showMenuHome();
    });
  }

  if (!socialScreen) return;

  socialScreen.addEventListener("click", (event) => {
    const actionButton = event.target.closest("[data-social-action]");
    if (!actionButton) return;

    const action = actionButton.dataset.socialAction;

    if (action === "meet") {
      openSocialMeetMock();
      return;
    }

    if (action === "friends") {
  openFriendsPopup();
  return;
}

    if (action === "party") {
      openSocialPartyPopup();
      return;
    }
if (action === "players-met") {
  openPlayersMetPopup();
  return;
}

    if (action === "guild") {
      showToast("Guild", "Guilds are coming soon.");
      return;
    }

    if (action === "create-market") {
      showToast("Create Market", "Market event hosting is coming soon.");
      return;
    }

    if (action === "nearby-markets") {
      showToast("Nearby Markets", "Nearby real-world markets are coming soon.");
      return;
    }

    if (action === "verify-attendance") {
      showToast("Verify Attendance", "Market attendance check-in is coming soon.");
      return;
    }
  });
}
function openSocialMeetMock() {
  openMeetScannerPopup();
}
function openMeetScannerPopup() {
  closeMeetScannerPopup();

  const popup = document.createElement("div");
  popup.id = "meet-scanner-popup";
  popup.className = "meet-scanner-popup";

  popup.innerHTML = `
    <h2 class="meet-scanner-title">Meet Player</h2>

    <p class="meet-scanner-text">
      Point your camera at another player's GrowGo QR code. It will scan automatically.
    </p>

    <div id="growgo-qr-reader" class="growgo-qr-reader"></div>

    <div class="meet-scanner-actions single">
  <button type="button" class="meet-scanner-close" onclick="closeMeetScannerPopup()">Close</button>
</div>
  `;

  const target = socialScreen || document.body;
  target.appendChild(popup);

  setTimeout(() => {
    startGrowGoQrScanner();
  }, 250);
}

async function startGrowGoQrScanner() {
  const readerEl = document.getElementById("growgo-qr-reader");

  if (!readerEl) {
    showToast("Meet", "QR scanner box was not found.");
    return;
  }

  if (typeof Html5Qrcode === "undefined") {
    readerEl.innerHTML = `
      <div class="meet-scanner-error">
        QR scanner library did not load. Check the html5-qrcode script in index.html.
      </div>
    `;
    showToast("Meet", "QR scanner library did not load.");
    return;
  }

  if (growGoQrScannerRunning) return;

  try {
    readerEl.innerHTML = "";

    growGoQrScanner = new Html5Qrcode("growgo-qr-reader", {
      formatsToSupport: [Html5QrcodeSupportedFormats.QR_CODE],
      experimentalFeatures: {
        useBarCodeDetectorIfSupported: true
      }
    });

    const qrConfig = {
      fps: 15,
      qrbox: function(viewfinderWidth, viewfinderHeight) {
        const minEdge = Math.min(viewfinderWidth, viewfinderHeight);
        const boxSize = Math.floor(minEdge * 0.78);

        return {
          width: boxSize,
          height: boxSize
        };
      },
      aspectRatio: 1.0,
      disableFlip: false
    };

    await growGoQrScanner.start(
      { facingMode: "environment" },
      qrConfig,
      (decodedText) => {
        handleGrowGoQrScan(decodedText);
      },
      () => {
        // Ignore scan misses.
      }
    );

    growGoQrScannerRunning = true;
  } catch (error) {
    console.warn("Could not start GrowGo QR scanner.", error);

    readerEl.innerHTML = `
      <div class="meet-scanner-error">
        Camera could not start.<br>
        Allow camera permission, then close and reopen Meet.
      </div>
    `;

    showToast("Meet", "Camera could not start.");
  }
}
let lastBadQrToastAt = 0;

async function handleGrowGoQrScan(decodedText) {
  const scannedText = String(decodedText || "").trim();
  const scannedId = scannedText.toUpperCase();

  console.log("GrowGo QR detected:", scannedText);

  if (!/^GG[A-Z0-9]{6}$/.test(scannedId)) {
    const now = Date.now();

    if (now - lastBadQrToastAt > 2500) {
      lastBadQrToastAt = now;
      showToast("QR detected", `Not a GrowGo player code: ${scannedText.slice(0, 20)}`);
    }

    return;
  }

  if (growGoQrScannerRunning === "saving") {
    return;
  }

  growGoQrScannerRunning = "saving";

  saveMetPlayer(scannedId);

  showToast("Meet", `Player met: ${scannedId}`);

  try {
    if (growGoQrScanner) {
      await growGoQrScanner.stop();
      await growGoQrScanner.clear();
    }
  } catch (error) {
    console.warn("Could not stop QR scanner after scan.", error);
  }

  growGoQrScanner = null;
  growGoQrScannerRunning = false;

  closeMeetScannerPopup();

  if (typeof openPlayersMetPopup === "function") {
    setTimeout(() => {
      openPlayersMetPopup();
    }, 250);
  }
}

function mockScanGrowGoPlayer() {
  const fakePlayerId = `GG${Math.random().toString(36).slice(2, 8).toUpperCase()}`;

  saveMetPlayer(fakePlayerId);
  closeMeetScannerPopup();

  showToast("Meet", `Mock scanned player ${fakePlayerId}.`);
}
function saveMetPlayer(playerId) {
  try {
    const playersMet = getPlayersMet();

    if (!playersMet.some((player) => player.id === playerId)) {
      playersMet.push({
        id: playerId,
        name: `Player ${playerId}`,
        metAt: Date.now()
      });

      savePlayersMet(playersMet);
      addStat("playersMet", 1);
    }
  } catch (error) {
    console.warn("Could not save met player.", error);
  }
}

async function closeMeetScannerPopup() {
  try {
    if (growGoQrScanner && growGoQrScannerRunning) {
      await growGoQrScanner.stop();
      await growGoQrScanner.clear();
    }
  } catch (error) {
    console.warn("Could not stop QR scanner.", error);
  }

  growGoQrScanner = null;
  growGoQrScannerRunning = false;

  const popup = document.getElementById("meet-scanner-popup");
  if (popup) popup.remove();
}
function getPlayersMet() {
  try {
    const raw = localStorage.getItem("growgo-players-met");
    const players = raw ? JSON.parse(raw) : [];

    return Array.isArray(players) ? players : [];
  } catch (error) {
    console.warn("Could not load players met.", error);
    return [];
  }
}

function savePlayersMet(players) {
  try {
    localStorage.setItem("growgo-players-met", JSON.stringify(players));
  } catch (error) {
    console.warn("Could not save players met.", error);
  }
}
function getGrowGoFriends() {
  try {
    const raw = localStorage.getItem("growgo-friends");
    const friends = raw ? JSON.parse(raw) : [];

    return Array.isArray(friends) ? friends : [];
  } catch (error) {
    console.warn("Could not load friends.", error);
    return [];
  }
}

function saveGrowGoFriends(friends) {
  try {
    localStorage.setItem("growgo-friends", JSON.stringify(friends));
  } catch (error) {
    console.warn("Could not save friends.", error);
  }
}

function addGrowGoFriend(player) {
  if (!player || !player.id) return;

  const friends = getGrowGoFriends();

  if (friends.some((friend) => friend.id === player.id)) {
    showToast("Friends", `${player.name || player.id} is already in your friend list.`);
    return;
  }

  friends.push({
    id: player.id,
    name: player.name || player.id,
    addedAt: Date.now()
  });

  saveGrowGoFriends(friends);

  showToast("Friend Added", `${player.name || player.id} added to your friends.`);
}

function openPlayersMetPopup() {
  closePlayersMetPopup();

  const playersMet = getPlayersMet();

  const overlay = document.createElement("div");
  overlay.id = "playersMetOverlay";
  overlay.className = "social-party-overlay";

  overlay.innerHTML = `
    <div class="social-party-popup">
      <button class="social-party-close" type="button">×</button>

      <div class="social-party-icon">🤝</div>

      <h3>Players Met</h3>

      <div class="social-party-text">
        Players you have scanned with Meet will appear here.
      </div>

      <div class="social-party-rules">
        ${
          playersMet.length
            ? playersMet.map((player) => `
                <button
                  class="social-party-rule"
                  data-met-player-id="${escapeAttribute(player.id)}"
                  type="button"
                >
                  ${escapeHtml(player.name || player.id)}
                  <br>
                  <small>${escapeHtml(player.id)}</small>
                </button>
              `).join("")
            : `
              <div class="social-party-rule">
                No players met yet. Tap Meet to mock scan someone.
              </div>
            `
        }
      </div>
    </div>
  `;

  document.body.appendChild(overlay);

  overlay.addEventListener("click", (event) => {
    if (
      event.target === overlay ||
      event.target.closest(".social-party-close")
    ) {
      closePlayersMetPopup();
      return;
    }

    const playerButton = event.target.closest("[data-met-player-id]");
    if (!playerButton) return;

    openAddFriendPrompt(playerButton.dataset.metPlayerId);
  });
}
function openFriendsPopup() {
  closeFriendsPopup();

  const friends = getGrowGoFriends();

  const overlay = document.createElement("div");
  overlay.id = "friendsOverlay";
  overlay.className = "social-party-overlay";

  overlay.innerHTML = `
    <div class="social-party-popup">
      <button class="social-party-close" type="button">×</button>

      <div class="social-party-icon">👥</div>

      <h3>Friends</h3>

      <div class="social-party-text">
        Your GrowGo friends will appear here.
      </div>

      <div class="social-party-rules">
        ${
          friends.length
            ? friends.map((friend) => `
                <div class="social-party-rule">
                  ${escapeHtml(friend.name || friend.id)}
                  <br>
                  <small>${escapeHtml(friend.id)}</small>
                </div>
              `).join("")
            : `
              <div class="social-party-rule">
                No friends yet. Tap Meet to scan a player, then add them from Players Met.
              </div>
            `
        }
      </div>

      <div class="social-party-actions">
        <button class="social-party-primary" data-open-players-met type="button">
          Players Met
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);

  overlay.addEventListener("click", (event) => {
    if (
      event.target === overlay ||
      event.target.closest(".social-party-close")
    ) {
      closeFriendsPopup();
      return;
    }

    if (event.target.closest("[data-open-players-met]")) {
      closeFriendsPopup();
      openPlayersMetPopup();
    }
  });
}

function closeFriendsPopup() {
  const overlay = document.getElementById("friendsOverlay");
  if (overlay) overlay.remove();
}

function closePlayersMetPopup() {
  const overlay = document.getElementById("playersMetOverlay");
  if (overlay) overlay.remove();
}

function openAddFriendPrompt(playerId) {
  closeAddFriendPrompt();

  const playersMet = getPlayersMet();
  const player = playersMet.find((entry) => entry.id === playerId);

  const overlay = document.createElement("div");
  overlay.id = "addFriendOverlay";
  overlay.className = "social-party-overlay";

  overlay.innerHTML = `
    <div class="social-party-popup">
      <button class="social-party-close" type="button">×</button>

      <div class="social-party-icon">👥</div>

      <h3>Add Friend?</h3>

      <div class="social-party-text">
        Would you like to add ${escapeHtml(player?.name || playerId)} as a friend?
      </div>

      <div class="social-party-actions">
        <button class="social-party-secondary" data-add-friend-action="cancel" type="button">
          Not Now
        </button>

        <button class="social-party-primary" data-add-friend-action="add" type="button">
          Add Friend
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);

  overlay.addEventListener("click", (event) => {
    if (
      event.target === overlay ||
      event.target.closest(".social-party-close") ||
      event.target.closest("[data-add-friend-action='cancel']")
    ) {
      closeAddFriendPrompt();
      return;
    }

    const addButton = event.target.closest("[data-add-friend-action='add']");
    if (!addButton) return;

    addGrowGoFriend(player || {
  id: playerId,
  name: playerId
});

showToast(
  "Friend Request Mock",
  "Later, this will send an accept / deny request to the other player."
);

closeAddFriendPrompt();
closePlayersMetPopup();
  });
}

function closeAddFriendPrompt() {
  const overlay = document.getElementById("addFriendOverlay");
  if (overlay) overlay.remove();
}
function openSocial() {
  if (!socialScreen) return;

  hideSubmenus();
  hideMenuHome();

  socialScreen.classList.remove("hidden");
  renderSocialScreen();
}

function renderSocialScreen() {
  const savedAvatar = getSavedAvatar();

  if (socialAvatarImg && socialAvatarFallback) {
    if (savedAvatar) {
      socialAvatarImg.src = savedAvatar;
      socialAvatarImg.classList.remove("hidden");
      socialAvatarFallback.classList.add("hidden");
    } else {
      socialAvatarImg.src = "";
      socialAvatarImg.classList.add("hidden");
      socialAvatarFallback.classList.remove("hidden");
      socialAvatarFallback.textContent = "+";
    }
  }

  if (socialPublicId) {
    socialPublicId.textContent = getOrCreateGrowGoPlayerId();
  }
}
function openSocialPartyPopup() {
  closeSocialPartyPopup();

  const overlay = document.createElement("div");
  overlay.id = "socialPartyOverlay";
  overlay.className = "social-party-overlay";

  overlay.innerHTML = `
    <div class="social-party-popup">
      <button class="social-party-close" type="button">×</button>

      <div class="social-party-icon">🎉</div>

      <h3>Party</h3>

      <div class="social-party-text">
        Party up with nearby players and share capture bonuses while staying close together.
      </div>

      <div class="social-party-rules">
        <div class="social-party-rule">
          Requires at least 2 players.
        </div>

        <div class="social-party-rule">
          Party members must stay within 100 meters of the creator.
        </div>

        <div class="social-party-rule">
          Creating a party uses 1 party token and lasts 4 hours.
        </div>
      </div>

      <div class="social-party-actions">
        <button class="social-party-primary" data-party-action="create" type="button">
          Create Party
        </button>

        <button class="social-party-secondary" data-party-action="join" type="button">
          Join Party
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);

  overlay.addEventListener("click", (event) => {
    if (
      event.target === overlay ||
      event.target.closest(".social-party-close")
    ) {
      closeSocialPartyPopup();
      return;
    }

    const partyAction = event.target.closest("[data-party-action]");
    if (!partyAction) return;

    if (partyAction.dataset.partyAction === "create") {
      showToast("Party", "Create Party flow is coming soon.");
      closeSocialPartyPopup();
      return;
    }

    if (partyAction.dataset.partyAction === "join") {
      showToast("Party", "Join Party flow is coming soon.");
      closeSocialPartyPopup();
    }
  });
}

function closeSocialPartyPopup() {
  const overlay = document.getElementById("socialPartyOverlay");
  if (overlay) overlay.remove();
}

/* ----------------------------- */
/* AVATAR UPLOAD */
/* ----------------------------- */

function initAvatarUpload() {
  const savedAvatar = getSavedAvatar();

  if (savedAvatar) {
    applyAvatar(savedAvatar);
  } else {
    clearAvatar();
  }

  setupProfileSwipeCard();
  renderPlayerQrCode();

  if (!menuAvatarInput) return;

  menuAvatarInput.addEventListener("change", (event) => {
    const file = event.target.files && event.target.files[0];
    if (!file) return;

    if (!file.type.startsWith("image/")) {
      showToast("Avatar", "Please choose an image file.");
      return;
    }

    const reader = new FileReader();

    reader.onload = (e) => {
      const src = e.target?.result;
      if (!src) return;

      playerState.avatarSrc = src;
      savePlayerState();

      applyAvatar(src);
      updatePlayerMarkerIcon();
      showToast("Avatar updated", "Profile picture saved.");
    };

    reader.readAsDataURL(file);
  });
}

function setupProfileSwipeCard() {
  if (!profileSwipeCard) return;

  let profileSwipeMoved = false;

  setProfileCardMode(profileSwipeCurrentMode);

  function handleProfileSwipeStart(startX, startY) {
    profileSwipeStartX = startX;
    profileSwipeStartY = startY;
    profileSwipeMoved = false;
  }

  function handleProfileSwipeEnd(endX, endY) {
    const diffX = endX - profileSwipeStartX;
    const diffY = endY - profileSwipeStartY;

    if (Math.abs(diffX) < 35) return;
    if (Math.abs(diffX) < Math.abs(diffY)) return;

    profileSwipeMoved = true;

    if (diffX > 0) {
      setProfileCardMode("qr");
    } else {
      setProfileCardMode("photo");
    }
  }

  profileSwipeCard.addEventListener("touchstart", (event) => {
    if (!event.touches || event.touches.length === 0) return;

    handleProfileSwipeStart(
      event.touches[0].clientX,
      event.touches[0].clientY
    );
  }, { passive: true });

  profileSwipeCard.addEventListener("touchend", (event) => {
    if (!event.changedTouches || event.changedTouches.length === 0) return;

    handleProfileSwipeEnd(
      event.changedTouches[0].clientX,
      event.changedTouches[0].clientY
    );
  }, { passive: true });

  profileSwipeCard.addEventListener("mousedown", (event) => {
    handleProfileSwipeStart(event.clientX, event.clientY);
  });

  profileSwipeCard.addEventListener("mouseup", (event) => {
    handleProfileSwipeEnd(event.clientX, event.clientY);
  });

  profileSwipeCard.addEventListener("click", (event) => {
    if (profileSwipeMoved) {
      event.preventDefault();
      event.stopPropagation();

      setTimeout(() => {
        profileSwipeMoved = false;
      }, 80);

      return;
    }

    if (profileSwipeCurrentMode === "qr") return;

    const savedAvatar = getSavedAvatar();

    if (savedAvatar) {
      showToast("Profile picture", "Change profile picture in Settings.");
      return;
    }

    if (menuAvatarInput) {
      menuAvatarInput.click();
    }
  });
}

function setProfileCardMode(mode) {
  profileSwipeCurrentMode = mode === "qr" ? "qr" : "photo";
  playerState.profileCardMode = profileSwipeCurrentMode;
  savePlayerState();

  if (!profilePhotoFace || !profileQrFace) return;

if (profileSwipeCard) {
  profileSwipeCard.classList.toggle("qr-mode", profileSwipeCurrentMode === "qr");
}

if (profileSwipeCurrentMode === "qr") {
  profilePhotoFace.classList.remove("active");
  profileQrFace.classList.add("active");
  renderPlayerQrCode();
} else {
  profileQrFace.classList.remove("active");
  profilePhotoFace.classList.add("active");
}
}

function getOrCreateGrowGoPlayerId() {
  try {
    let playerId = playerState.publicId || localStorage.getItem(GROWGO_PLAYER_ID_KEY);

    if (playerId) {
      if (playerState.publicId !== playerId) {
        playerState.publicId = playerId;
        savePlayerState();
      }

      return playerId;
    }

    const alphabet = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
    let randomPart = "";

    for (let i = 0; i < 6; i++) {
      randomPart += alphabet[Math.floor(Math.random() * alphabet.length)];
    }

    playerId = `GG${randomPart}`;
    playerState.publicId = playerId;
    savePlayerState();

    return playerId;
  } catch (error) {
    console.warn("Could not create player ID.", error);
    return "GGPLAYER";
  }
}

function renderPlayerQrCode() {
  if (!playerQrCode) return;

  playerQrCode.innerHTML = "";

  const playerId = getOrCreateGrowGoPlayerId();

  if (typeof QRCode === "undefined") {
    playerQrCode.textContent = "";
    console.warn("QRCode library is not loaded.");
    return;
  }

new QRCode(playerQrCode, {
  text: playerId,
  width: 120,
  height: 120,
  colorDark: "#000000",
  colorLight: "#ffffff",
  correctLevel: QRCode.CorrectLevel.H
});
}

function applyAvatar(src) {
  if (menuAvatarImg) {
    menuAvatarImg.src = src;
    menuAvatarImg.classList.remove("hidden");
  }

  if (menuAvatarPlus) {
    menuAvatarPlus.classList.add("hidden");
  }

  if (topAvatarImage) {
    topAvatarImage.src = src;
    topAvatarImage.classList.remove("hidden");
  }

  if (topAvatarFallback) {
    topAvatarFallback.classList.add("hidden");
  }
}

function clearAvatar() {
  if (menuAvatarImg) {
    menuAvatarImg.src = "";
    menuAvatarImg.classList.add("hidden");
  }

  if (menuAvatarPlus) {
    menuAvatarPlus.classList.remove("hidden");
  }

  if (topAvatarImage) {
    topAvatarImage.src = "";
    topAvatarImage.classList.add("hidden");
  }

  if (topAvatarFallback) {
    topAvatarFallback.classList.remove("hidden");
    topAvatarFallback.textContent = "+";
  }
}

function getSavedAvatar() {
  try {
    return playerState.avatarSrc || localStorage.getItem(AVATAR_STORAGE_KEY);
  } catch {
    return playerState.avatarSrc || null;
  }
}

/* ----------------------------- */
/* PLAYER LOCATION */
/* ----------------------------- */

function locatePlayer() {
  if (!navigator.geolocation) {
    setPlayerLocation(DEFAULT_CENTER[0], DEFAULT_CENTER[1], false);
    return;
  }

  navigator.geolocation.getCurrentPosition(
    (position) => {
      setPlayerLocation(position.coords.latitude, position.coords.longitude, true);
    },
    () => {
      setPlayerLocation(DEFAULT_CENTER[0], DEFAULT_CENTER[1], false);
      showToast("Location", "Using default map area.");
    },
    {
      enableHighAccuracy: true,
      timeout: 10000,
      maximumAge: 10000
    }
  );

  navigator.geolocation.watchPosition(
    (position) => {
      setPlayerLocation(position.coords.latitude, position.coords.longitude, false);
    },
    () => {},
    {
      enableHighAccuracy: true,
      timeout: 15000,
      maximumAge: 5000
    }
  );
}

function setPlayerLocation(lat, lng, shouldCenter) {
  playerLatLng = L.latLng(lat, lng);

  if (!playerMarker) {
    playerMarker = L.marker(playerLatLng, {
      icon: buildPlayerIcon(),
      zIndexOffset: 3000
    }).addTo(map);
  } else {
    playerMarker.setLatLng(playerLatLng);
  }

  if (!captureRing) {
    captureRing = L.circle(playerLatLng, {
      radius: CAPTURE_RADIUS_METERS,
      color: "rgba(255,255,255,0.95)",
      weight: 2,
      fillColor: "rgba(255,255,255,0.12)",
      fillOpacity: 0.12,
      interactive: false
    }).addTo(map);
  } else {
    captureRing.setLatLng(playerLatLng);
  }

  if (shouldCenter) {
    map.setView(playerLatLng, 18);
  }

  requestRoadPinsForCurrentView(true);
  scheduleRedrawPins();
}

function buildPlayerIcon() {
  const avatarSrc = getSavedAvatar();

  const html = avatarSrc
    ? `
      <div style="width:54px;height:54px;border-radius:50%;overflow:hidden;border:3px solid white;box-shadow:0 4px 14px rgba(0,0,0,0.35);background:white;">
        <img src="${escapeAttribute(avatarSrc)}" alt="Player" style="width:100%;height:100%;object-fit:cover;display:block;">
      </div>
    `
    : `
      <div style="width:24px;height:24px;border-radius:50%;background:#2d7cff;border:3px solid white;box-shadow:0 3px 10px rgba(0,0,0,0.3);"></div>
    `;

  return L.divIcon({
    className: "player-marker-icon",
    html,
    iconSize: [54, 54],
    iconAnchor: [27, 27]
  });
}

function updatePlayerMarkerIcon() {
  if (!playerMarker) return;
  playerMarker.setIcon(buildPlayerIcon());
}

/* ----------------------------- */
/* ROAD PIN LOADING */
/* ----------------------------- */

function requestRoadPinsForCurrentView(force = false) {
  if (!map) return;

  if (map.getZoom() < MIN_FETCH_ZOOM) {
    /* CUSTOM 2.5D MAP EXPERIMENT START */
    setCustom25DMapZoneFeatures([]);
    setCustom25DMapBuildingFeatures([]);
    /* CUSTOM 2.5D MAP EXPERIMENT END */
    setCustom25DMapRoadFeatures([]);
    scheduleRedrawPins();
    return;
  }

  if (roadFetchTimer) clearTimeout(roadFetchTimer);

  roadFetchTimer = setTimeout(() => {
    fetchRoadPinsForViewport(force).catch((error) => {
      if (error?.name === "AbortError") {
        scheduleRedrawPins();
        return;
      }

      console.warn("Road pin fetch failed:", error);
      scheduleRedrawPins();

      if (!roadErrorToastShown && pinStore.size === 0) {
        roadErrorToastShown = true;
        showToast("Pins loading", "Road pins are taking a moment. Move or zoom the map to try again.");
      }
    });
  }, force ? 0 : PIN_FETCH_DEBOUNCE_MS);
}

async function fetchRoadPinsForViewport(force = false) {
  const bounds = map.getBounds().pad(0.12);
  const paddedBounds = {
    south: bounds.getSouth() - ROAD_FETCH_PADDING,
    west: bounds.getWest() - ROAD_FETCH_PADDING,
    north: bounds.getNorth() + ROAD_FETCH_PADDING,
    east: bounds.getEast() + ROAD_FETCH_PADDING
  };

  const viewportKey = [
    paddedBounds.south.toFixed(3),
    paddedBounds.west.toFixed(3),
    paddedBounds.north.toFixed(3),
    paddedBounds.east.toFixed(3)
  ].join("|");

  if (!force && fetchedViewportKeys.has(viewportKey)) {
    scheduleRedrawPins();
    return;
  }

  if (roadFetchInFlight && roadFetchAbortController) {
    roadFetchAbortController.abort();
  }

  roadFetchInFlight = true;
  roadFetchAbortController = new AbortController();

  try {
    const response = await fetch(OVERPASS_API_URL, {
      method: "POST",
      body: buildOverpassQuery(paddedBounds),
      signal: roadFetchAbortController.signal
    });

    if (!response.ok) throw new Error(`Overpass failed: ${response.status}`);

    const data = await response.json();
    const features = extractMapFeaturesFromOverpass(data);
    /* CUSTOM 2.5D MAP EXPERIMENT START */
    setCustom25DMapZoneFeatures(features.zoneFeatures);
    setCustom25DMapBuildingFeatures(features.buildingFeatures);
    /* CUSTOM 2.5D MAP EXPERIMENT END */
    setCustom25DMapRoadFeatures(features.roadWays);
    const converted = syncPinsNearWater(features.waterFeatures, bounds);
    const newPins = buildPinsFromWays(features.roadWays, bounds, features.waterFeatures);

    let added = converted;

    newPins.forEach((pin) => {
      if (!pinStore.has(pin.id)) {
        pinStore.set(pin.id, pin);
        addPinToSpatialBuckets(pin);
        added = true;
      }
    });

    if (added) scheduleSavePinsToLocal();

    fetchedViewportKeys.add(viewportKey);
    scheduleRedrawPins();
  } finally {
    roadFetchInFlight = false;
    roadFetchAbortController = null;
  }
}

function buildOverpassQuery(boundsObj) {
  const highwayRegex = HIGHWAY_TYPES.join("|");

  return `
[out:json][timeout:25];
(
  way["highway"~"^(${highwayRegex})$"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["natural"="water"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["natural"="coastline"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["natural"="beach"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["natural"="sand"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["natural"="wetland"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["natural"="grassland"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["water"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["waterway"~"^(river|stream|canal)$"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["landuse"~"^(reservoir|basin)$"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["landuse"~"^(grass|meadow|village_green|recreation_ground)$"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["leisure"~"^(park|garden|pitch|sports_centre|nature_reserve|common)$"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
  way["building"](${boundsObj.south},${boundsObj.west},${boundsObj.north},${boundsObj.east});
);
(._;>;);
out body;
`;
}

function extractMapFeaturesFromOverpass(data) {
  const elements = Array.isArray(data?.elements) ? data.elements : [];
  const nodesById = new Map();

  elements.forEach((el) => {
    if (el.type === "node" && typeof el.lat === "number" && typeof el.lon === "number") {
      nodesById.set(el.id, [el.lat, el.lon]);
    }
  });

  const roadWays = [];
  const waterFeatures = [];
  /* CUSTOM 2.5D MAP EXPERIMENT START */
  const zoneFeatures = [];
  const buildingFeatures = [];
  /* CUSTOM 2.5D MAP EXPERIMENT END */

  elements.forEach((el) => {
    if (el.type !== "way" || !Array.isArray(el.nodes)) return;

    const coords = el.nodes.map((nodeId) => nodesById.get(nodeId)).filter(Boolean);
    const tags = el.tags || {};

    if (coords.length < 2) return;

    if (tags.highway) {
      roadWays.push({
        id: el.id,
        coords,
        highway: tags.highway
      });
      return;
    }

    if (isWaterFeature(tags)) {
      waterFeatures.push({
        id: el.id,
        coords,
        closed: coords.length >= 4 && coords[0][0] === coords[coords.length - 1][0] && coords[0][1] === coords[coords.length - 1][1]
      });
    }

    /* CUSTOM 2.5D MAP EXPERIMENT START */
    if (isCustom25DBuildingFeature(tags, coords)) {
      const center = getFeatureCenter(coords);
      buildingFeatures.push({
        id: `building:${el.id}`,
        coords,
        center,
        buildingType: tags.building || "yes",
        shopTag: tags.shop || "",
        amenity: tags.amenity || "",
        office: tags.office || "",
        cuisine: tags.cuisine || "",
        tourism: tags.tourism || "",
        leisure: tags.leisure || "",
        landuse: tags.landuse || "",
        buildingArea: estimateFeatureArea(coords)
      });
    }

    const zoneType = getCustom25DZoneType(tags);
    if (zoneType) {
      zoneFeatures.push({
        id: `zone:${el.id}`,
        zoneType,
        coords,
        closed: coords.length >= 4 && coords[0][0] === coords[coords.length - 1][0] && coords[0][1] === coords[coords.length - 1][1]
      });
    }
    /* CUSTOM 2.5D MAP EXPERIMENT END */
  });

  markCustom25DBuildingContext(buildingFeatures, zoneFeatures, waterFeatures);

  return { roadWays, waterFeatures, zoneFeatures, buildingFeatures };
}

/* CUSTOM 2.5D MAP EXPERIMENT START */
function isCustom25DBuildingFeature(tags, coords) {
  return Boolean(tags?.building) && Array.isArray(coords) && coords.length >= 4;
}

function getFeatureCenter(coords) {
  const total = coords.reduce((acc, [lat, lng]) => ({
    lat: acc.lat + lat,
    lng: acc.lng + lng
  }), { lat: 0, lng: 0 });

  return {
    lat: total.lat / coords.length,
    lng: total.lng / coords.length
  };
}

function estimateFeatureArea(coords) {
  if (!Array.isArray(coords) || coords.length < 3) return 0;

  const averageLat = coords.reduce((sum, [lat]) => sum + lat, 0) / coords.length;
  const metersPerDegLat = 111320;
  const metersPerDegLng = Math.cos((averageLat * Math.PI) / 180) * 111320;
  let area = 0;

  for (let i = 0; i < coords.length; i += 1) {
    const [lat1, lng1] = coords[i];
    const [lat2, lng2] = coords[(i + 1) % coords.length];
    const x1 = lng1 * metersPerDegLng;
    const y1 = lat1 * metersPerDegLat;
    const x2 = lng2 * metersPerDegLng;
    const y2 = lat2 * metersPerDegLat;
    area += x1 * y2 - x2 * y1;
  }

  return Math.abs(area) * 0.5;
}

function getApproxDistanceMeters(a, b) {
  if (!a || !b) return Number.POSITIVE_INFINITY;

  const latScale = 111320;
  const lngScale = Math.cos((((a.lat + b.lat) * 0.5) * Math.PI) / 180) * 111320;
  const dx = (a.lng - b.lng) * lngScale;
  const dy = (a.lat - b.lat) * latScale;
  return Math.hypot(dx, dy);
}

function markCustom25DBuildingContext(buildingFeatures, zoneFeatures, waterFeatures) {
  if (!Array.isArray(buildingFeatures) || !buildingFeatures.length) return;

  const coastalCenters = [
    ...((Array.isArray(zoneFeatures) ? zoneFeatures : [])
      .filter((feature) => feature?.zoneType === "water" || feature?.zoneType === "beach")
      .map((feature) => getFeatureCenter(feature.coords))),
    ...((Array.isArray(waterFeatures) ? waterFeatures : [])
      .filter((feature) => Array.isArray(feature?.coords) && feature.coords.length >= 2)
      .map((feature) => getFeatureCenter(feature.coords)))
  ];

  buildingFeatures.forEach((feature) => {
    feature.nearCoast = coastalCenters.some((center) => getApproxDistanceMeters(feature.center, center) <= 180);
  });
}

function getCustom25DZoneType(tags) {
  if (!tags) return null;

  if (
    tags.natural === "water" ||
    tags.natural === "coastline" ||
    Boolean(tags.water) ||
    ["river", "stream", "canal"].includes(tags.waterway) ||
    ["reservoir", "basin"].includes(tags.landuse)
  ) {
    return "water";
  }

  if (tags.natural === "beach" || tags.natural === "sand") return "beach";
  if (tags.natural === "wetland") return "wetland";
  if (tags.leisure === "pitch" || tags.leisure === "sports_centre") return "sports";
  if (["park", "garden", "nature_reserve", "common"].includes(tags.leisure)) return "park";
  if (
    tags.natural === "grassland" ||
    ["grass", "meadow", "village_green", "recreation_ground"].includes(tags.landuse)
  ) {
    return "grass";
  }

  return null;
}
/* CUSTOM 2.5D MAP EXPERIMENT END */

function isWaterFeature(tags) {
  return (
    tags.natural === "water" ||
    tags.natural === "coastline" ||
    Boolean(tags.water) ||
    ["river", "stream", "canal"].includes(tags.waterway) ||
    ["reservoir", "basin"].includes(tags.landuse)
  );
}

function buildPinsFromWays(ways, visibleBounds, waterFeatures = []) {
  const generated = [];
  const localSeen = [];

  for (const way of ways) {
    const coords = way.coords;
    if (!coords || coords.length < 2) continue;

    for (let i = 0; i < coords.length - 1; i++) {
      const start = L.latLng(coords[i][0], coords[i][1]);
      const end = L.latLng(coords[i + 1][0], coords[i + 1][1]);
      const segmentLength = start.distanceTo(end);

      if (segmentLength < PIN_SPACING_METERS * 0.75) {
        maybeAddRoadPin(interpolateLatLng(start, end, 0.5), visibleBounds, localSeen, generated, waterFeatures);
        continue;
      }

      const steps = Math.floor(segmentLength / PIN_SPACING_METERS);

      for (let step = 0; step <= steps; step++) {
        const t = Math.min(1, (step * PIN_SPACING_METERS) / segmentLength);
        maybeAddRoadPin(interpolateLatLng(start, end, t), visibleBounds, localSeen, generated, waterFeatures);

        if (generated.length >= MAX_PINS_PER_FETCH) return generated;
      }
    }
  }

  return generated;
}

function maybeAddRoadPin(point, visibleBounds, localSeen, generated, waterFeatures = []) {
  if (!visibleBounds.contains(point)) return;

  const candidate = L.latLng(point.lat, point.lng);
  const key = `${point.lat.toFixed(6)},${point.lng.toFixed(6)}`;

  if (pinStore.has(key)) return;
  if (isTooCloseToExistingPinFast(candidate)) return;
  if (isTooCloseToLocalPins(candidate, localSeen)) return;

  localSeen.push(candidate);

  const isWaterPin = isPointNearWaterFeatures(candidate, waterFeatures);
  const pin = {
    id: key,
    type: isWaterPin ? "water" : "base",
    lat: point.lat,
    lng: point.lng,
    basePoints: BASE_PIN_VALUE,
    capturedAt: null
  };

  maybeAssignWaterPinFish(pin);
  generated.push(pin);
}

function syncPinsNearWater(waterFeatures, visibleBounds) {
  if (!Array.isArray(waterFeatures) || !waterFeatures.length) return false;

  let changed = false;

  pinStore.forEach((pin) => {
    if (!pin || (pin.type !== "base" && pin.type !== "water")) return;
    if (pin.ownerId || pin.plant) return;
    if (!visibleBounds.contains([pin.lat, pin.lng])) return;

    const nearWater = isPointNearWaterFeatures(L.latLng(pin.lat, pin.lng), waterFeatures);

    if (nearWater && pin.type !== "water") {
      pin.type = "water";
      maybeAssignWaterPinFish(pin);
      changed = true;
      return;
    }

    if (!nearWater && pin.type === "water") {
      pin.type = "base";
      delete pin.fish;
      changed = true;
    }
  });

  if (changed) {
    clearPinIconCache();
  }

  return changed;
}

function isPointNearWaterFeatures(point, waterFeatures) {
  return waterFeatures.some((feature) => {
    if (!Array.isArray(feature.coords) || feature.coords.length < 2) return false;

    if (feature.closed && isPointInsideLatLngPolygon(point, feature.coords)) {
      return true;
    }

    return getDistanceToLatLngPath(point, feature.coords) <= WATER_PIN_DISTANCE_METERS;
  });
}

function getDistanceToLatLngPath(point, coords) {
  let closest = Infinity;

  for (let i = 0; i < coords.length - 1; i++) {
    const start = L.latLng(coords[i][0], coords[i][1]);
    const end = L.latLng(coords[i + 1][0], coords[i + 1][1]);
    const distance = getDistanceToSegmentMeters(point, start, end);

    if (distance < closest) closest = distance;
    if (closest <= WATER_PIN_DISTANCE_METERS) return closest;
  }

  return closest;
}

function getDistanceToSegmentMeters(point, start, end) {
  const centerLat = (point.lat + start.lat + end.lat) / 3;
  const metersPerLat = 111320;
  const metersPerLng = Math.cos(centerLat * Math.PI / 180) * 111320;
  const px = point.lng * metersPerLng;
  const py = point.lat * metersPerLat;
  const ax = start.lng * metersPerLng;
  const ay = start.lat * metersPerLat;
  const bx = end.lng * metersPerLng;
  const by = end.lat * metersPerLat;
  const dx = bx - ax;
  const dy = by - ay;
  const lengthSquared = (dx * dx) + (dy * dy);

  if (!lengthSquared) {
    return point.distanceTo(start);
  }

  const t = Math.max(0, Math.min(1, (((px - ax) * dx) + ((py - ay) * dy)) / lengthSquared));
  const closestX = ax + (t * dx);
  const closestY = ay + (t * dy);

  return Math.hypot(px - closestX, py - closestY);
}

function isPointInsideLatLngPolygon(point, coords) {
  let inside = false;
  const x = point.lng;
  const y = point.lat;

  for (let i = 0, j = coords.length - 1; i < coords.length; j = i++) {
    const yi = coords[i][0];
    const xi = coords[i][1];
    const yj = coords[j][0];
    const xj = coords[j][1];
    const intersects = ((yi > y) !== (yj > y)) &&
      (x < ((xj - xi) * (y - yi)) / ((yj - yi) || 1e-12) + xi);

    if (intersects) inside = !inside;
  }

  return inside;
}

function maybeAssignWaterPinFish(pin) {
  if (!pin || pin.type !== "water" || pin.fish) return;

  const roll = Math.random();
  let fishType = null;

  if (roll < WATER_PIN_SALMON_CHANCE) {
    fishType = "salmon";
  } else if (roll < WATER_PIN_SALMON_CHANCE + WATER_PIN_BLUE_FISH_CHANCE) {
    fishType = "blue";
  }

  if (!fishType) return;

  pin.fish = {
    type: fishType,
    spawnedAt: getTrustedNow()
  };
}

function getActiveWaterPinFish(pin, mutateExpired = true) {
  if (!pin || pin.type !== "water" || !pin.fish) return null;

  const fish = WATER_PIN_FISH_TYPES[pin.fish.type];
  const spawnedAt = Number(pin.fish.spawnedAt || 0);

  if (!fish || !spawnedAt || getTrustedNow() - spawnedAt >= WATER_PIN_FISH_LIFETIME_MS) {
    if (mutateExpired) {
      delete pin.fish;
      scheduleSavePinsToLocal();
    }

    return null;
  }

  return {
    ...fish,
    type: pin.fish.type,
    spawnedAt
  };
}

/* ----------------------------- */
/* SPATIAL BUCKETS */
/* ----------------------------- */

function rebuildSpatialBuckets() {
  pinSpatialBuckets.clear();
  pinStore.forEach(addPinToSpatialBuckets);
}

function addPinToSpatialBuckets(pin) {
  if (pin?.type === "poi") return;

  const key = getBucketKey(pin.lat, pin.lng);

  if (!pinSpatialBuckets.has(key)) {
    pinSpatialBuckets.set(key, []);
  }

  pinSpatialBuckets.get(key).push(pin);
}

function getBucketKey(lat, lng) {
  return `${Math.floor(lat * 1000)}:${Math.floor(lng * 1000)}`;
}

function getNearbyBucketKeys(lat, lng) {
  const baseLat = Math.floor(lat * 1000);
  const baseLng = Math.floor(lng * 1000);
  const keys = [];

  for (let y = -1; y <= 1; y++) {
    for (let x = -1; x <= 1; x++) {
      keys.push(`${baseLat + y}:${baseLng + x}`);
    }
  }

  return keys;
}

function isTooCloseToExistingPinFast(candidate) {
  const keys = getNearbyBucketKeys(candidate.lat, candidate.lng);

  for (const key of keys) {
    const bucket = pinSpatialBuckets.get(key);
    if (!bucket) continue;

    for (const pin of bucket) {
      if (candidate.distanceTo([pin.lat, pin.lng]) < MIN_PIN_SEPARATION_METERS) {
        return true;
      }
    }
  }

  return false;
}

function isTooCloseToLocalPins(candidate, localSeen) {
  for (const existing of localSeen) {
    if (candidate.distanceTo(existing) < MIN_PIN_SEPARATION_METERS) return true;
  }

  return false;
}

function interpolateLatLng(start, end, t) {
  return L.latLng(
    start.lat + (end.lat - start.lat) * t,
    start.lng + (end.lng - start.lng) * t
  );
}

/* ----------------------------- */
/* PIN RENDERING */
/* ----------------------------- */

function scheduleRedrawPins() {
  if (redrawTimer) cancelAnimationFrame(redrawTimer);
  redrawTimer = requestAnimationFrame(redrawVisiblePins);
}

function redrawVisiblePins() {
  if (!pinsLayer || !map) return;

  const bounds = map.getBounds().pad(0.08);
  const cameraCenter = map.getCenter();
  const visiblePins = [];

  pinStore.forEach((pin) => {
    if (!bounds.contains([pin.lat, pin.lng])) return;

    visiblePins.push({
      pin,
      distance: cameraCenter.distanceTo([pin.lat, pin.lng])
    });
  });

  visiblePins.sort((a, b) => a.distance - b.distance);

  const selectedPins = visiblePins.slice(0, MAX_VISIBLE_PINS).map((entry) => entry.pin);
  const selectedIds = new Set(selectedPins.map((pin) => pin.id));

  renderedPinMarkers.forEach((marker, pinId) => {
    if (!selectedIds.has(pinId)) {
      pinsLayer.removeLayer(marker);
      renderedPinMarkers.delete(pinId);
    }
  });

  selectedPins.forEach((pin) => {
    const iconState = getPinIconState(pin);
    const zIndex = iconState.type === "poi" ? 1300 : iconState.glowing ? 1500 : 1000;
    const existingMarker = renderedPinMarkers.get(pin.id);

    if (existingMarker) {
      if (existingMarker._growgoIconState !== iconState.key) {
        existingMarker.setIcon(buildPinIcon(pin, iconState));
        existingMarker._growgoIconState = iconState.key;
      }

      if (existingMarker._growgoZIndex !== zIndex) {
        existingMarker.setZIndexOffset(zIndex);
        existingMarker._growgoZIndex = zIndex;
      }

      return;
    }

    const marker = L.marker([pin.lat, pin.lng], {
      icon: buildPinIcon(pin, iconState),
      zIndexOffset: zIndex
    });

    marker._growgoIconState = iconState.key;
    marker._growgoZIndex = zIndex;

    marker.on("click", () => {
      if (pinLongPressTriggered) {
        pinLongPressTriggered = false;
        return;
      }

      capturePin(pin);
    });
    marker.on("add", () => bindPinLongPress(marker, pin));
    marker.on("dblclick", (event) => {
      if (event?.originalEvent) {
        L.DomEvent.stopPropagation(event.originalEvent);
      }
    });

    pinsLayer.addLayer(marker);
    renderedPinMarkers.set(pin.id, marker);
  });
}

function getPinIconState(pin) {
  const type = pin.type || "base";
  const points = getPinPoints(pin);
  const capturedToday = type === "poi" ? Boolean(pin.captured) : wasCapturedToday(pin);
  const glowing = shouldPinGlow(pin, capturedToday);
  const ownerId = pin.ownerId || "";
  const plantStage = getPinPlantStage(pin);
  const plantSeedId = pin.plant?.seedId || "";
  const plantHarvestedKey = pin.plant?.harvestedByDay?.[getActivePlayerId()] || "";
  const activeFish = getActiveWaterPinFish(pin);
  const fishType = activeFish?.type || "";

  return {
    type,
    fishType,
    points,
    capturedToday,
    glowing,
    owned: Boolean(ownerId),
    ownedByActivePlayer: ownerId === getActivePlayerId(),
    plantStage,
    plantSeedId,
    key: `${type}|${pin.category || ""}|${pin.subcategory || ""}|${pin.rarity || ""}|${pin.icon || ""}|${pin.poiCategory || ""}|${pin.poiName || ""}|${fishType}|${points}|${capturedToday ? 1 : 0}|${glowing ? 1 : 0}|${ownerId}|${plantStage}|${plantSeedId}|${plantHarvestedKey}`
  };
}

function buildPinIcon(pin, state = null) {
  const iconState = state || getPinIconState(pin);
  const cacheKey = iconState.key;

  if (pinIconCache.has(cacheKey)) {
    return pinIconCache.get(cacheKey);
  }

  if (iconState.type === "poi") {
    return buildPoiIcon(pin, cacheKey);
  }

  const glowClass = iconState.glowing ? "pin-ready-glow" : "";
  const capturedClass = iconState.capturedToday ? "pin-captured-today" : "";
  const ownedClass = iconState.owned ? "pin-owned" : "";
  const typeClass = iconState.type === "water" ? "water-pin-marker" : "";
  const valueClass = iconState.type === "water" ? "" : getBasePinValueClass(iconState.points);
  const pinImage = iconState.type === "water" ? "pin-water-blue.png" : "pin-base-purple.png";
  const showPointNumber = iconState.type !== "water" && !iconState.capturedToday;
  const fish = getActiveWaterPinFish(pin);
  const fishClass = fish ? "water-pin-has-fish" : "";
  const fishBadge = fish
    ? `
      <div
        class="water-pin-fish water-pin-fish-${escapeAttribute(fish.className)}"
        aria-label="${escapeAttribute(fish.label)}"
      >
        ${escapeHtml(fish.icon)}
      </div>
    `
    : "";
  const pinAlt = iconState.type === "water" ? "Water Pin" : "Base Pin";
  const plantVisual = getPinPlantVisual(pin);
  const plantBadge = iconState.plantStage > 0
    ? `
      <div
        class="base-pin-plant-stage ${escapeAttribute(plantVisual.className)}"
        aria-label="${escapeAttribute(plantVisual.label)}"
      >
        ${escapeHtml(plantVisual.icon)}
      </div>
    `
    : "";

  const html = `
    <div class="base-pin-marker ${typeClass} ${valueClass} ${fishClass} ${glowClass} ${capturedClass} ${ownedClass}">
      <img src="${pinImage}" alt="${pinAlt}">
      ${showPointNumber ? `<div class="base-pin-number">${iconState.points}</div>` : ""}
      ${fishBadge}
      ${plantBadge}
    </div>
  `;

  const icon = L.divIcon({
    className: "base-pin-icon",
    html,
    iconSize: [64, 84],
    iconAnchor: [32, 84]
  });

  pinIconCache.set(cacheKey, icon);
  return icon;
}

function getBasePinValueClass(points) {
  const value = Number(points || 0);

  if (value >= 50) return "base-pin-value-50";
  if (value >= 40) return "base-pin-value-40";
  if (value >= 30) return "base-pin-value-30";
  if (value >= 20) return "base-pin-value-20";
  if (value >= 10) return "base-pin-value-10";
  return "base-pin-value-5";
}

function buildPoiIcon(pin, cacheKey) {
  const category = pin.subcategory || pin.poiCategory || "POI";
  const capturedClass = pin.captured ? "poi-captured" : "";
  const rarityClass = pin.rarity === "special" ? "special" : "normal";
  const artClass = `poi-art-${escapeAttribute(pin.icon || getPoiIconKey(category))}`;
  const html = `
    <div class="poi-pin-marker ${rarityClass} ${capturedClass}" aria-label="${escapeAttribute(pin.name || pin.poiName || category)}">
      <span class="poi-art ${artClass}">${escapeHtml(getPoiIconText(pin.icon || category))}</span>
    </div>
  `;

  const icon = L.divIcon({
    className: "poi-pin-icon",
    html,
    iconSize: [38, 50],
    iconAnchor: [19, 50]
  });

  pinIconCache.set(cacheKey, icon);
  return icon;
}

function getPoiIconLabel(category) {
  return getPoiIconText(category);
}

function getPoiIconText(icon) {
  const key = String(icon || "").toLowerCase();
  if (key.includes("church")) return "⛪";
  if (key.includes("park")) return "🌳";
  if (key.includes("beach")) return "◒";
  if (key.includes("waterfall")) return "▥";
  if (key.includes("dinosaur") || key.includes("fossil")) return "◆";
  if (key.includes("film")) return "▶";
  if (key.includes("music")) return "♪";
  if (key.includes("hospital")) return "+";
  return "★";
}

function clearPinIconCache() {
  pinIconCache.clear();

  renderedPinMarkers.forEach((marker) => {
    marker._growgoIconState = null;
  });
}

function shouldPinGlow(pin, capturedToday = null) {
  if (!playerLatLng) return false;

  const alreadyCaptured = capturedToday === null ? wasCapturedToday(pin) : capturedToday;

  if (alreadyCaptured) return false;

  const captureRadius = pin?.type === "poi" ? POI_CAPTURE_RADIUS_METERS : CAPTURE_RADIUS_METERS;
  const isInCaptureRing =
    playerLatLng.distanceTo([pin.lat, pin.lng]) <= captureRadius;

  return isInCaptureRing;
}

function startPinLongPress(pin) {
  clearPinLongPress();
  activeLongPressPin = pin;

  pinLongPressTimer = setTimeout(() => {
    pinLongPressTriggered = true;
    openPinLongPressPopup(activeLongPressPin);

    setTimeout(() => {
      pinLongPressTriggered = false;
    }, 800);
  }, PIN_LONG_PRESS_MS);
}

function clearPinLongPress() {
  if (pinLongPressTimer) {
    clearTimeout(pinLongPressTimer);
    pinLongPressTimer = null;
  }

  activeLongPressPin = null;
}

function bindPinLongPress(marker, pin) {
  const element = marker.getElement();
  if (!element || element._growgoLongPressBound) return;

  element._growgoLongPressBound = true;
  element.style.touchAction = "manipulation";
  element.style.webkitUserSelect = "none";
  element.style.userSelect = "none";

  element.addEventListener("pointerdown", (event) => {
    if (event.pointerType === "mouse" && event.button !== 0) return;

    event.preventDefault();
    startPinLongPress(pin);
  }, { passive: false });

  ["pointerup", "pointercancel", "pointerleave", "lostpointercapture"].forEach((eventName) => {
    element.addEventListener(eventName, clearPinLongPress);
  });

  element.addEventListener("contextmenu", (event) => {
    event.preventDefault();
  });
}

function openPinLongPressPopup(pin) {
  clearPinLongPress();
  if (!pin) return;

  if (pin.type === "poi") {
    openPoiInfoPopup(pin);
    return;
  }

  if (pin.type === "water") {
    showToast("Water pin", "Water pins cannot be purchased yet.");
    return;
  }

  if (!pin.ownerId) {
    openBasePinPurchasePopup(pin);
    return;
  }

  if (pin.ownerId === getActivePlayerId()) {
    openBasePinPlantPopup(pin);
    return;
  }

  showToast("Owned pin", "This base pin already belongs to another player.");
}

function openPoiInfoPopup(pin) {
  closeBasePinPopup();

  const distance = getDistanceToPinLabel(pin);
  const status = pin.captured ? "Collected" : "Ready to capture";
  const category = pin.category || "Places";
  const subcategory = pin.subcategory || pin.poiCategory || "POI";
  const name = pin.name || pin.poiName || subcategory;
  const rarityLabel = pin.rarity === "special" ? "Special POI" : "Normal POI";

  const overlay = document.createElement("div");
  overlay.id = "basePinOverlay";
  overlay.className = "base-pin-overlay";
  overlay.innerHTML = `
    <div class="base-pin-popup poi-info-popup">
      <button class="base-pin-popup-close" type="button">×</button>
      <h3>${escapeHtml(name)}</h3>
      <div class="poi-info-category">${escapeHtml(rarityLabel)} · ${escapeHtml(category)} · ${escapeHtml(subcategory)}</div>
      <div class="base-pin-popup-copy">
        ${escapeHtml(pin.description || "A real-world point of interest.")}
      </div>
      <div class="base-pin-popup-meta">
        Reward: ${formatNumber(POI_PIN_VALUE)} points, ${formatNumber(POI_PIN_VALUE)} XP, ${formatNumber(POI_COIN_REWARD)} coins
      </div>
      ${renderPoiCardRewardLine(pin)}
      <div class="base-pin-popup-meta">
        Distance: ${escapeHtml(distance)}
      </div>
      <div class="base-pin-popup-meta">
        Status: ${escapeHtml(status)}
      </div>
      <div class="base-pin-popup-actions">
        <button class="base-pin-secondary-btn" data-base-pin-cancel type="button">Close</button>
        <button class="base-pin-primary-btn" data-poi-pin-go="${escapeAttribute(pin.id)}" type="button">
          Go to POI
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);
  overlay.addEventListener("click", handleBasePinPopupClick);
}

function openBasePinPurchasePopup(pin) {
  closeBasePinPopup();

  const overlay = document.createElement("div");
  overlay.id = "basePinOverlay";
  overlay.className = "base-pin-overlay";
  overlay.innerHTML = `
    <div class="base-pin-popup">
      <button class="base-pin-popup-close" type="button">×</button>
      <h3>Purchase Base Pin</h3>
      <div class="base-pin-popup-copy">
        Own this base pin for ${formatNumber(BASE_PIN_PURCHASE_COST)} coins.
      </div>
      <div class="base-pin-popup-meta">
        Wallet: ${formatNumber(marketState.wallet)} coins
      </div>
      <div class="base-pin-popup-actions">
        <button class="base-pin-secondary-btn" data-base-pin-cancel type="button">Not Now</button>
        <button class="base-pin-primary-btn" data-base-pin-purchase="${escapeAttribute(pin.id)}" type="button">
          Purchase
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);
  overlay.addEventListener("click", handleBasePinPopupClick);
}

function openBasePinPlantPopup(pin) {
  closeBasePinPopup();

  const seedOptions = getAvailableBasePinSeeds();
  const planted = pin.plant ? getPlantStageLabel(pin.plant) : "No seed planted";
  const plantTiming = pin.plant ? getPlantTimingLabel(pin.plant) : "Plant a seed to start growing";
  const level = getBasePinLevel(pin);
  const levelInfo = getBasePinLevelInfo(level);
  const upgrade = getBasePinUpgradeInfo(pin);

  const overlay = document.createElement("div");
  overlay.id = "basePinOverlay";
  overlay.className = "base-pin-overlay";
  overlay.innerHTML = `
    <div class="base-pin-popup">
      <button class="base-pin-popup-close" type="button">×</button>
      <h3>Owned Base Pin</h3>
      <div class="base-pin-level ${escapeAttribute(levelInfo.className)}">
        Level ${level} · ${escapeHtml(levelInfo.name)}
      </div>
      <div class="base-pin-popup-copy">
        Current plant: ${escapeHtml(planted)}
      </div>
      <div class="base-pin-popup-meta">
        Growth: ${escapeHtml(plantTiming)}
      </div>
      <div class="base-pin-popup-meta">
        Replant: ${pin.replantEnabled ? "On" : "Off"}
      </div>
      <label class="base-pin-select-label" for="basePinSeedSelect">Seed</label>
      <select id="basePinSeedSelect" class="base-pin-select" ${seedOptions.length ? "" : "disabled"}>
        ${seedOptions.length
          ? seedOptions.map((seed) => `
            <option value="${escapeAttribute(seed.id)}">
              ${escapeHtml(seed.label)} x${formatNumber(seed.quantity)}
            </option>
          `).join("")
          : `<option>No seeds available</option>`}
      </select>
      <button class="base-pin-toggle ${pin.replantEnabled ? "active" : ""}" data-base-pin-replant="${escapeAttribute(pin.id)}" type="button">
        Replant ${pin.replantEnabled ? "On" : "Off"}
      </button>
      ${upgrade ? `
        <button class="base-pin-upgrade-btn" data-base-pin-upgrade="${escapeAttribute(pin.id)}" type="button">
          Upgrade to Level ${upgrade.nextLevel} (${escapeHtml(upgrade.nextInfo.name)}) · ${formatNumber(upgrade.cost)}g
        </button>
      ` : `
        <div class="base-pin-max-level">Epic level reached</div>
      `}
      <div class="base-pin-popup-actions">
        <button class="base-pin-secondary-btn" data-base-pin-cancel type="button">Not Now</button>
        <button class="base-pin-primary-btn" data-base-pin-plant="${escapeAttribute(pin.id)}" type="button" ${seedOptions.length ? "" : "disabled"}>
          Plant Seed
        </button>
      </div>
    </div>
  `;

  document.body.appendChild(overlay);
  overlay.addEventListener("click", handleBasePinPopupClick);
}

function handleBasePinPopupClick(event) {
  if (
    event.target.id === "basePinOverlay" ||
    event.target.closest(".base-pin-popup-close") ||
    event.target.closest("[data-base-pin-cancel]")
  ) {
    closeBasePinPopup();
    return;
  }

  const purchaseButton = event.target.closest("[data-base-pin-purchase]");
  if (purchaseButton) {
    purchaseBasePin(purchaseButton.dataset.basePinPurchase);
    return;
  }

  const plantButton = event.target.closest("[data-base-pin-plant]");
  if (plantButton) {
    const select = document.getElementById("basePinSeedSelect");
    plantBasePinSeed(plantButton.dataset.basePinPlant, select?.value);
    return;
  }

  const replantButton = event.target.closest("[data-base-pin-replant]");
  if (replantButton) {
    toggleBasePinReplant(replantButton.dataset.basePinReplant);
    return;
  }

  const upgradeButton = event.target.closest("[data-base-pin-upgrade]");
  if (upgradeButton) {
    upgradeBasePin(upgradeButton.dataset.basePinUpgrade);
    return;
  }

  const poiGoButton = event.target.closest("[data-poi-pin-go]");
  if (poiGoButton) {
    goToPoiPin(poiGoButton.dataset.poiPinGo);
  }
}

function closeBasePinPopup() {
  const overlay = document.getElementById("basePinOverlay");
  if (overlay) overlay.remove();
}

function purchaseBasePin(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin) return;

  if (pin.ownerId) {
    showToast("Base pin", "This pin has already been purchased.");
    closeBasePinPopup();
    return;
  }

  if (marketState.wallet < BASE_PIN_PURCHASE_COST) {
    showToast("Not enough coins", `${formatNumber(BASE_PIN_PURCHASE_COST)} coins needed.`);
    return;
  }

  marketState.wallet -= BASE_PIN_PURCHASE_COST;
  saveMarketState();
  renderMarketWallet();
  renderPlayerOverview();

  pin.type = "base";
  pin.ownerId = getActivePlayerId();
  pin.ownerName = playerState.name || DEFAULT_PLAYER_NAME;
  pin.ownedAt = getTrustedNow();
  pin.level = 1;
  pin.replantEnabled = false;

  scheduleSavePinsToLocal();
  clearPinIconCache();
  scheduleRedrawPins();
  closeBasePinPopup();
  showToast("Base pin owned", "This pin is now yours.");
}

function getAvailableBasePinSeeds() {
  return BASE_PIN_SEED_OPTIONS
    .map((seed) => ({
      ...seed,
      quantity: Number(marketState.inventory[seed.id] || 0)
    }))
    .filter((seed) => seed.quantity > 0);
}

function getBasePinSeedOption(seedId) {
  return BASE_PIN_SEED_OPTIONS.find((seed) => (
    seed.id === seedId ||
    seed.harvestItemId === seedId
  ));
}

function getBasePinLevel(pin) {
  const level = Number(pin?.level || 1);
  return Math.max(1, Math.min(BASE_PIN_MAX_LEVEL, level));
}

function getBasePinLevelInfo(level) {
  return BASE_PIN_LEVELS[getBasePinLevel({ level })] || BASE_PIN_LEVELS[1];
}

function getBasePinUpgradeInfo(pin) {
  const level = getBasePinLevel(pin);
  if (level >= BASE_PIN_MAX_LEVEL) return null;

  const currentInfo = getBasePinLevelInfo(level);
  const nextLevel = level + 1;

  return {
    nextLevel,
    nextInfo: getBasePinLevelInfo(nextLevel),
    cost: currentInfo.upgradeCost
  };
}

function toggleBasePinReplant(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin || pin.ownerId !== getActivePlayerId()) return;

  pin.replantEnabled = !pin.replantEnabled;
  scheduleSavePinsToLocal();
  openBasePinPlantPopup(pin);
}

function upgradeBasePin(pinId) {
  const pin = pinStore.get(pinId);
  if (!pin || pin.ownerId !== getActivePlayerId()) return;

  const upgrade = getBasePinUpgradeInfo(pin);
  if (!upgrade) return;

  if (marketState.wallet < upgrade.cost) {
    showToast("Not enough gold", `${formatNumber(upgrade.cost)}g needed.`);
    return;
  }

  marketState.wallet -= upgrade.cost;
  saveMarketState();
  renderMarketWallet();
  renderPlayerOverview();

  pin.level = upgrade.nextLevel;
  scheduleSavePinsToLocal();
  clearPinIconCache();
  scheduleRedrawPins();
  openBasePinPlantPopup(pin);
  showToast("Pin upgraded", `Base pin is now ${upgrade.nextInfo.name}.`);
}

function plantBasePinSeed(pinId, seedId) {
  const pin = pinStore.get(pinId);
  const seed = getBasePinSeedOption(seedId);
  if (!pin || !seed || pin.ownerId !== getActivePlayerId()) return;

  const owned = Number(marketState.inventory[seed.id] || 0);
  if (owned <= 0) {
    showToast("Seeds", "You do not have that seed available.");
    return;
  }

  marketState.inventory[seed.id] = owned - 1;
  saveMarketState();
  refreshInventoryIfOpen();

  pin.plant = {
    seedId: seed.id,
    plantedAt: getTrustedNow(),
    harvestedByDay: {}
  };

  scheduleSavePinsToLocal();
  clearPinIconCache();
  scheduleRedrawPins();
  closeBasePinPopup();
  showToast("Seed planted", `${seed.label} is growing on this base pin.`);
}

function getActivePlayerId() {
  return getOrCreateGrowGoPlayerId();
}

/* ----------------------------- */
/* CAPTURE */
/* ----------------------------- */

function capturePin(pin) {
  if (pin?.type === "poi") {
    capturePOI(pin);
    return;
  }

  if (!playerLatLng) {
    showToast("Capture", "Player location not ready yet.");
    return;
  }

  const trustedNow = getTrustedNow();
  const distance = playerLatLng.distanceTo([pin.lat, pin.lng]);

  if (distance > CAPTURE_RADIUS_METERS) {
    showToast("Too far away", `${Math.round(distance)}m away.`);
    return;
  }

  if (wasCapturedToday(pin)) {
    showToast("Already captured", "This pin has already been captured today.");
    return;
  }

  const points = getPinPoints(pin);
  pin.capturedAt = trustedNow;

  addStat("captures", 1);
  recordPoiCaptureStats(pin);
  addStat("score", points);
  addStat("goldEarned", 1);
  addPlayerXp(points);
  addMarketCoins(1);
  awardBasePinOwnerCaptureReward(pin);
  harvestReadyBasePinPlant(pin);
  const waterDrop = awardWaterPinResourceDrop(pin);
  const fishDrop = awardWaterPinFishCapture(pin);

  scheduleSavePinsToLocal();
  clearPinIconCache();

  const resourceText = [
    waterDrop ? "+1 water" : "",
    fishDrop ? `+1 ${fishDrop.label}, +${fishDrop.xp} fish XP` : ""
  ].filter(Boolean).join(", ");
  if (resourceText) {
    showToast("Resource found", resourceText);
  }
  showPinCaptureFloat(pin, points, 1, { showXp: false });
  scheduleRedrawPins();
}

function capturePOI(pin) {
  if (!playerLatLng) {
    showToast("POI", "Player location not ready yet.");
    return;
  }

  const trustedNow = getTrustedNow();
  const distance = playerLatLng.distanceTo([pin.lat, pin.lng]);

  if (distance > POI_CAPTURE_RADIUS_METERS) {
    showToast("Too far away", `${Math.round(distance)}m away.`);
    return;
  }

  if (capturedPOIs.has(pin.id) || pin.captured) {
    showToast("POI collected", "This point of interest is already in your collection.");
    return;
  }

  pin.captured = true;
  pin.capturedAt = trustedNow;

  saveCapturedPOI(pin);
  addStat("captures", 1);
  recordPoiCaptureStats(pin);
  addStat("score", POI_PIN_VALUE);
  addStat("goldEarned", POI_COIN_REWARD);
  addPlayerXp(POI_PIN_VALUE);
  addMarketCoins(POI_COIN_REWARD);
  const cardReward = awardCardForPOI(pin);

  scheduleSavePinsToLocal();
  renderPOIs();
  const cardRewardText = cardReward?.card
    ? ` ${cardReward.status === "duplicate" ? "Duplicate card" : "New card"}: ${cardReward.card.cardName}.`
    : "";
  showToast("POI captured", `+${POI_PIN_VALUE} points, +${POI_PIN_VALUE} XP, +${POI_COIN_REWARD} coins.${cardRewardText}`);
  showPinCaptureFloat(pin, POI_PIN_VALUE, POI_COIN_REWARD);
  showRewardBurst(`+${POI_PIN_VALUE} XP`);
}

function awardCardForPOI(pin) {
  const reward = pickCardRewardForPOI(pin);
  const card = reward?.card || null;
  if (!card) return null;

  return awardCard(card.cardId, "poi");
}

function getPinTypeLabel(pin) {
  if (pin?.type === "poi") {
    return pin.poiCategory ? `${pin.poiCategory.toLowerCase()} POI` : "POI";
  }

  if (pin?.type === "water") return "water pin";
  return "base pin";
}

function recordPoiCaptureStats(pin) {
  if (pin?.type !== "poi") return;

  addStat("poiCaptures", 1);

  const categoryStat = POI_CAPTURE_STAT_BY_CATEGORY[pin.subcategory || pin.poiCategory || ""];
  if (categoryStat) {
    addStat(categoryStat, 1);
  }

  checkPoiAchievements();
}

function awardWaterPinResourceDrop(pin) {
  if (pin?.type !== "water") return false;
  if (Math.random() >= WATER_PIN_RESOURCE_DROP_CHANCE) return false;

  marketState.inventory.water = Number(marketState.inventory.water || 0) + 1;
  addStat("resourcesGained", 1);
  saveMarketState();
  refreshInventoryIfOpen();

  return true;
}

function awardWaterPinFishCapture(pin) {
  const fish = getActiveWaterPinFish(pin);
  if (!fish) return null;

  marketState.inventory[fish.itemId] = Number(marketState.inventory[fish.itemId] || 0) + 1;
  delete pin.fish;

  addStat("fishCaught", 1);
  addStat("resourcesGained", 1);
  addPlayerXp(fish.xp);
  saveMarketState();
  refreshInventoryIfOpen();

  return fish;
}

function awardBasePinOwnerCaptureReward(pin) {
  if (!pin.ownerId || pin.ownerId === getActivePlayerId()) return;

  pin.ownerPendingPoints = Number(pin.ownerPendingPoints || 0) + PIN_OWNER_CAPTURE_REWARD;
}

function harvestReadyBasePinPlant(pin) {
  if (!pin?.plant || getPinPlantStage(pin) < 4) return;

  const playerId = getActivePlayerId();
  const todayKey = getUtcDayKey(getTrustedNow());
  const harvestedByDay = pin.plant.harvestedByDay || {};

  if (harvestedByDay[playerId] === todayKey) return;

  const seed = getBasePinSeedOption(pin.plant.seedId);
  if (!seed) return;

  const harvestItemId = seed.harvestItemId || seed.id;
  const harvestLabel = seed.cropLabel || seed.label;
  const canAutoReplant =
    pin.replantEnabled &&
    pin.ownerId === playerId &&
    Number(marketState.inventory[seed.id] || 0) > 0;

  marketState.inventory[harvestItemId] = Number(marketState.inventory[harvestItemId] || 0) + 1;
  addStat("resourcesGained", 1);

  if (canAutoReplant) {
    marketState.inventory[seed.id] = Math.max(0, Number(marketState.inventory[seed.id] || 0) - 1);
    pin.plant = {
      seedId: seed.id,
      plantedAt: getTrustedNow(),
      harvestedByDay: {}
    };
  } else {
    pin.plant.harvestedByDay = {
      ...harvestedByDay,
      [playerId]: todayKey
    };
  }

  saveMarketState();
  refreshInventoryIfOpen();
  showToast("Harvested", canAutoReplant ? `+1 ${harvestLabel}, replanted.` : `+1 ${harvestLabel}`);
}

function getPinPoints(pin) {
  if (pin?.type === "poi") {
    return POI_PIN_VALUE;
  }

  if (pin?.type === "water") {
    return WATER_PIN_VALUE;
  }

  const now = getTrustedNow();
  const growthStartedAt = Number(pin.capturedAt || getServerStartedAt());
  const hoursSinceGrowthStarted = Math.max(0, (now - growthStartedAt) / (1000 * 60 * 60));
  const growthSteps = Math.floor(hoursSinceGrowthStarted / POINTS_GROWTH_HOURS);

  return (pin.basePoints || BASE_PIN_VALUE) + growthSteps;
}

function getServerStartedAt() {
  try {
    const savedStartedAt = Number(localStorage.getItem(SERVER_STARTED_AT_KEY));

    if (Number.isFinite(savedStartedAt) && savedStartedAt > 0) {
      return savedStartedAt;
    }

    const startedAt = getTrustedNow();
    localStorage.setItem(SERVER_STARTED_AT_KEY, String(startedAt));
    return startedAt;
  } catch (error) {
    console.warn("Could not read server start time.", error);
    return Date.now();
  }
}

function getPinPlantStage(pin) {
  if (!pin?.plant?.plantedAt) return 0;

  const hoursSincePlanting = Math.max(
    0,
    (getTrustedNow() - Number(pin.plant.plantedAt)) / (1000 * 60 * 60)
  );

  if (hoursSincePlanting >= PIN_PLANT_STAGE_HOURS * 3) return 4;
  if (hoursSincePlanting >= PIN_PLANT_STAGE_HOURS * 2) return 3;
  if (hoursSincePlanting >= PIN_PLANT_STAGE_HOURS) return 2;
  return 1;
}

function getPlantStageIcon(stage) {
  if (stage >= 4) return "🌽";
  if (stage === 3) return "🌿";
  if (stage === 2) return "🌱";
  return "·";
}

function getPinPlantVisual(pin) {
  const stage = getPinPlantStage(pin);
  const seed = getBasePinSeedOption(pin?.plant?.seedId);
  const cropLabel = seed ? (seed.cropLabel || seed.label) : "Plant";
  const readyIcon = seed?.icon || getPlantStageIcon(stage);
  const stageNames = {
    1: "planted",
    2: "sprouting",
    3: "almost ready",
    4: "ready to harvest"
  };

  if (stage >= 4) {
    return {
      icon: readyIcon,
      className: "plant-stage-4",
      label: `${cropLabel} ready to harvest`
    };
  }

  if (stage === 3) {
    return {
      icon: "🌿",
      className: "plant-stage-3",
      label: `${cropLabel} almost ready`
    };
  }

  if (stage === 2) {
    return {
      icon: "🌱",
      className: "plant-stage-2",
      label: `${cropLabel} sprouting`
    };
  }

  return {
    icon: "●",
    className: "plant-stage-1",
    label: `${cropLabel} ${stageNames[stage] || "planted"}`
  };
}

function getPlantStageLabel(plant) {
  if (!plant) return "No seed planted";

  const seed = getBasePinSeedOption(plant.seedId);
  const label = seed ? (seed.cropLabel || seed.label) : "Plant";
  const stage = getPinPlantStage({ plant });

  if (stage >= 4) return `${label}, ready to harvest`;
  if (stage === 3) return `${label}, almost ready`;
  if (stage === 2) return `${label}, sprouting`;
  return `${label}, planted`;
}

function getPlantTimingLabel(plant) {
  if (!plant?.plantedAt) return "Plant a seed to start growing";

  const stage = getPinPlantStage({ plant });
  const now = getTrustedNow();
  const plantedAt = Number(plant.plantedAt);
  const stageMs = PIN_PLANT_STAGE_HOURS * 60 * 60 * 1000;

  if (stage >= 4) {
    const playerId = getActivePlayerId();
    const harvestedByDay = plant.harvestedByDay || {};

    if (harvestedByDay[playerId] === getUtcDayKey(now)) {
      return "Harvested today";
    }

    return "Ready now";
  }

  const nextStageAt = plantedAt + (stage * stageMs);
  const harvestAt = plantedAt + (stageMs * 3);
  const nextLabel = stage === 3 ? "Ready" : "Next stage";

  if (stage === 3) {
    return `${nextLabel} in ${formatDuration(harvestAt - now)}`;
  }

  return `${nextLabel} in ${formatDuration(nextStageAt - now)}`;
}

function wasCapturedToday(pin) {
  if (!pin.capturedAt) return false;

  const now = getTrustedNow();

  return getUtcDayKey(pin.capturedAt) === getUtcDayKey(now);
}

/* ----------------------------- */
/* LOCAL STORAGE */
/* ----------------------------- */

function scheduleSavePinsToLocal() {
  if (savePinsTimer) clearTimeout(savePinsTimer);

  savePinsTimer = setTimeout(() => {
    savePinsToLocal();
  }, 250);
}

function savePinsToLocal() {
  try {
    localStorage.setItem(PIN_STORAGE_KEY, JSON.stringify(Array.from(pinStore.values())));
  } catch (error) {
    console.warn("Could not save pins.", error);
  }
}

function loadCapturedPOIs() {
  try {
    const raw = localStorage.getItem(CAPTURED_POIS_STORAGE_KEY);
    if (!raw) return new Map();

    const data = JSON.parse(raw);
    if (!Array.isArray(data)) return new Map();

    return new Map(data
      .filter((poi) => poi && typeof poi.id === "string")
      .map((poi) => [poi.id, poi]));
  } catch (error) {
    console.warn("Could not load captured POIs.", error);
    return new Map();
  }
}

function saveCapturedPOI(pin) {
  const capturedPOI = {
    id: pin.id,
    name: pin.name || pin.poiName || "POI",
    category: pin.category || "Places",
    subcategory: pin.subcategory || pin.poiCategory || "POI",
    rarity: pin.rarity === "special" ? "special" : "normal",
    icon: pin.icon || getPoiIconKey(pin.subcategory || pin.poiCategory),
    lat: pin.lat,
    lng: pin.lng,
    description: pin.description || "",
    captured: true,
    capturedAt: pin.capturedAt || getTrustedNow()
  };

  capturedPOIs.set(pin.id, capturedPOI);

  try {
    localStorage.setItem(CAPTURED_POIS_STORAGE_KEY, JSON.stringify(Array.from(capturedPOIs.values())));
  } catch (error) {
    console.warn("Could not save captured POI.", error);
  }
}

function loadPinsFromLocal() {
  try {
    const raw = localStorage.getItem(PIN_STORAGE_KEY);
    if (!raw) return;

    const data = JSON.parse(raw);
    if (!Array.isArray(data)) return;

    data.forEach((pin) => {
      if (
        pin &&
        typeof pin.id === "string" &&
        typeof pin.lat === "number" &&
        typeof pin.lng === "number"
      ) {
        pinStore.set(pin.id, {
          id: pin.id,
          type: pin.type || "base",
          lat: pin.lat,
          lng: pin.lng,
          basePoints: typeof pin.basePoints === "number" ? pin.basePoints : BASE_PIN_VALUE,
          capturedAt: pin.capturedAt || null,
          ownerId: pin.ownerId || null,
          ownerName: pin.ownerName || null,
          ownedAt: pin.ownedAt || null,
          level: getBasePinLevel(pin),
          replantEnabled: Boolean(pin.replantEnabled),
          ownerPendingPoints: Number(pin.ownerPendingPoints || 0),
          plant: pin.plant || null,
          fish: pin.fish || null,
          name: pin.name || pin.poiName || null,
          category: pin.category || null,
          subcategory: pin.subcategory || pin.poiCategory || null,
          rarity: pin.rarity || null,
          icon: pin.icon || null,
          description: pin.description || null,
          captured: Boolean(pin.captured),
          poiCategory: pin.poiCategory || null,
          poiName: pin.poiName || null,
          osmType: pin.osmType || null,
          osmId: pin.osmId || null,
          discoveredBy: pin.discoveredBy || null,
          discoveredAt: pin.discoveredAt || null
        });
      }
    });
  } catch (error) {
    console.warn("Could not load saved pins.", error);
  }
}

/* ----------------------------- */
/* TOASTS + HELPERS */
/* ----------------------------- */

function showToast(title, body) {
  if (!toastStack) return;

  const toast = document.createElement("div");
  const lowerTitle = String(title || "").toLowerCase();
  const rewardClass = lowerTitle.includes("captured") || lowerTitle.includes("reward") || lowerTitle.includes("collected")
    ? " reward"
    : "";
  const levelClass = lowerTitle.includes("level up") ? " level-up" : "";
  toast.className = `toast${rewardClass}${levelClass}`;
  toast.innerHTML = `
    <div class="toast-title">${escapeHtml(title)}</div>
    <div class="toast-body">${escapeHtml(body)}</div>
  `;

  toastStack.appendChild(toast);

  setTimeout(() => {
    toast.style.opacity = "0";
    toast.style.transform = "translateX(20px)";
    toast.style.transition = "opacity 180ms ease, transform 180ms ease";

    setTimeout(() => {
      toast.remove();
    }, 220);
  }, 3000);
}

function showRewardBurst(text, type = "reward") {
  const burst = document.createElement("div");
  burst.className = `growgo-reward-burst ${type === "level-up" ? "level-up" : ""}`;
  burst.textContent = text;

  document.body.appendChild(burst);

  setTimeout(() => {
    burst.remove();
  }, 1100);
}

function showPinCaptureFloat(pin, points, coins = 1, options = {}) {
  if (!map || !pin || typeof pin.lat !== "number" || typeof pin.lng !== "number") return;

  const mapRect = map.getContainer().getBoundingClientRect();
  const point = map.latLngToContainerPoint([pin.lat, pin.lng]);
  const float = document.createElement("div");
  const coinText = `+${formatNumber(coins)}`;
  const showXp = options.showXp !== false;

  float.className = `pin-capture-float ${showXp ? "" : "compact"}`.trim();
  float.style.left = `${mapRect.left + point.x}px`;
  float.style.top = `${mapRect.top + point.y}px`;
  float.innerHTML = `
    <div class="pin-capture-text">+${formatNumber(points)} pts</div>
    ${showXp ? `<div class="pin-capture-xp">+${formatNumber(points)} XP</div>` : ""}
    <div class="pin-capture-coin" aria-label="${escapeAttribute(coinText)} coin reward">
      <span>${escapeHtml(coinText)}</span>
    </div>
  `;

  document.body.appendChild(float);

  setTimeout(() => {
    float.remove();
  }, 1300);
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function escapeAttribute(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll('"', "&quot;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;");
}
