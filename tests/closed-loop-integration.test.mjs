import test from 'node:test'; import assert from 'node:assert/strict';
import {evaluateClosedLoop} from '../asset-factory/golden-reference/closed-loop-integration.mjs';
const base={assetId:'TEST',buildId:'BUILD_001',comparison:{metrics:{silhouetteIoU:.5},components:{results:[{componentId:'BLOCK',issues:[{errorRatio:.3}]},{componentId:'PROTECTED',issues:[]}],protectedComponents:['PROTECTED']}}};
test('closed-loop evaluation marks target improvement and known-good corrected build',()=>{const after={buildId:'BUILD_002',comparison:{metrics:{silhouetteIoU:.8},components:{results:[{componentId:'BLOCK',issues:[{errorRatio:.1}]},{componentId:'PROTECTED',issues:[]}],protectedComponents:['PROTECTED']}}};const e=evaluateClosedLoop({baseline:base,corrected:after,targetComponent:'BLOCK',targetIssue:'COMPONENT_WIDTH_FAIL',protectedComponents:['PROTECTED'],referenceBefore:{referenceId:'R',version:'v1',checksum:'x'},referenceAfter:{referenceId:'R',version:'v1',checksum:'x'},cameraBefore:{id:'CAM'},cameraAfter:{id:'CAM'}});assert.equal(e.targetComponentEvaluation,'TARGET_COMPONENT_IMPROVED');assert.equal(e.knownGoodBuildId,'BUILD_002');});
test('regression keeps baseline known-good and protects reference/camera gates',()=>{const after={buildId:'BUILD_002',comparison:{metrics:{silhouetteIoU:.9},components:{results:[],protectedComponents:[]}}};const e=evaluateClosedLoop({baseline:base,corrected:after,targetComponent:'BLOCK',targetIssue:'X',protectedComponents:['PROTECTED'],referenceBefore:{referenceId:'R',version:'v1',checksum:'x'},referenceAfter:{referenceId:'R',version:'v1',checksum:'changed'},cameraBefore:{id:'CAM'},cameraAfter:{id:'MOVED'}});assert.equal(e.goldenReferenceValidation,'GOLDEN_REFERENCE_MUTATION_FAIL');assert.equal(e.cameraValidation,'CAMERA_ALIGNMENT_FAIL');});
