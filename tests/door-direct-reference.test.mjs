import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),out=path.join(root,'test-output/door-direct-reference');
test('direct reference diagnostic contract',()=>{const m=JSON.parse(fs.readFileSync(path.join(out,'DOOR_DIRECT_REFERENCE_METRICS.json'))),s=JSON.parse(fs.readFileSync(path.join(out,'DOOR_DIRECT_REFERENCE_SETTINGS.json')));assert.deepEqual(m.sourceDimensions,m.renderDimensions);assert.equal(s.cameraType,'ORTHO');assert.equal(s.uvCoverage,'FULL_ONCE');assert.equal(s.material,'EMISSION_UNLIT');for(const f of ['DOOR_DIRECT_REFERENCE_RENDER.png','DOOR_DIRECT_REFERENCE_OVERLAY.png','DOOR_DIRECT_REFERENCE_DIFFERENCE.png','DOOR_DIRECT_REFERENCE_DIAGNOSTIC_BOARD.png','DOOR_DIRECT_REFERENCE.blend'])assert.ok(fs.existsSync(path.join(out,f)),f)});
