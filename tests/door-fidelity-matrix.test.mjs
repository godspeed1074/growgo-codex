import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const out=path.join(process.cwd(),'test-output/door-fidelity-calibration');test('fidelity matrix and contract generated',()=>{const m=JSON.parse(fs.readFileSync(path.join(out,'DOOR_PIXEL_FIDELITY_MATRIX.json'))),c=JSON.parse(fs.readFileSync(path.join(out,'GROWGO_REFERENCE_TEXTURE_RENDER_CONTRACT.json')));assert.equal(m.roundtrip.meanRGBDifference,0);assert.equal(m.results.length,4);assert.ok(c.winningTest);assert.ok(fs.existsSync(path.join(out,'SOURCE_ROUNDTRIP.png')))});
