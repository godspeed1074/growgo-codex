import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),out=path.join(root,'test-output/door-mask-calibration');test('door mask calibration passes 2D context-aware gate',()=>{const e=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/DOOR_MASK_CALIBRATION_EVIDENCE.json'))),m=JSON.parse(fs.readFileSync(path.join(out,'DOOR_TRUE_LAYER_METRICS.json')));assert.equal(e.status,'PASS_2D_CONTEXT_AWARE_GATE');assert.equal(m.fullCrop.exactMismatchPercentage,0);assert.equal(m.scopeCoveragePercent,100);assert.equal(m.noLayerPercent,0);assert.equal(e.blenderRun,false);for(const f of ['DOOR_MASK_DEBUG_BOARD.png','DOOR_MODULE_SCOPE_MASK.png','DOOR_REFERENCE_CONTEXT.png','DOOR_FULL_CROP_REASSEMBLED.png','DOOR_MODULE_REASSEMBLED_TRANSPARENT.png','DOOR_LAYER_MASKS.json','DOOR_MASK_COVERAGE_AUDIT.json'])assert.ok(fs.existsSync(path.join(out,f)),f)});
