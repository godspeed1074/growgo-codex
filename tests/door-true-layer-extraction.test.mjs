import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),out=path.join(root,'test-output/door-true-layer-proof');test('true transparent layer extraction stops on failed 2D gate',()=>{const e=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/DOOR_TRUE_LAYER_CALIBRATION_EVIDENCE.json'))),m=JSON.parse(fs.readFileSync(path.join(out,'DOOR_TRUE_LAYER_METRICS.json')));assert.equal(e.status,'FAIL_2D_REASSEMBLY_GATE');assert.equal(e.fullSourceBackplateUsed,false);assert.equal(m.fullSourceBackplateUsed,false);for(const f of ['DOOR_TRUE_LAYER_REASSEMBLED.png','DOOR_TRUE_LAYER_DIFFERENCE.png','DOOR_LAYER_MASKS.json'])assert.ok(fs.existsSync(path.join(out,f)),f)});
