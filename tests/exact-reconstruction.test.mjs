import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),out=path.join(root,'test-output/shop-exact-reconstruction'),r=JSON.parse(fs.readFileSync(path.join(out,'SHOP_EXACT_RESULT.json'))),ids=['WALL','FOUNDATION','WINDOW','DOOR','AWNING','FASCIA','SIGN','TRIM','PLANTER','SHRUB'];
test('exact reconstruction real-worker outputs',()=>{assert.equal(r.status,'PASS');assert.equal(r.candidateLineage,'SHOP_EXACT_RECONSTRUCTION@1.0.0');assert.ok(r.budget.materials<=32);assert.equal(r.budget.anonymousGeometryCount,0);for(const id of ids)assert.ok(r.componentIds.includes(id),id);for(const f of ['SHOP_EXACT_RECONSTRUCTION.blend','SHOP_EXACT_GAMEPLAY.png','SHOP_EXACT_HERO.png','SHOP_EXACT_CLOSEUP.png','SHOP_EXACT_FRONT.png','SHOP_EXACT_BACK.png','SHOP_EXACT_LEFT.png','SHOP_EXACT_RIGHT.png','SHOP_EXACT_COMPONENT_ID.png','SHOP_EXACT_GOLDEN_COMPARISON_BOARD.png','SHOP_EXACT_FOUR_SIDE_BOARD.png'])assert.ok(fs.existsSync(path.join(out,f)),f)});
test('exact spec and protected recipe',()=>{const spec=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_EXACT_RECONSTRUCTION_SPEC.json')));assert.equal(spec.immutable,true);assert.ok(spec.silhouetteLandmarks.AWNING_LEFT_EDGE);const ev=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_GOLD_STANDARD_EVIDENCE_EXACT_RECONSTRUCTION.json')));assert.equal(ev.goldStandardFrozen,false);const recipe=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_LAYER_B_RECIPE.json')));assert.equal(recipe.recipeId,'GG-REC-BLD-SIMPLE-SHOP-GROWGO-001');assert.equal(recipe.recipeVersion,'1.0.0')});
