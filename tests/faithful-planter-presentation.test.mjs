import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/plant-faithful-presentation'),mod=path.join(root,'asset-factory/modular');
test('faithful planter presentation keeps the locked front authority',()=>{const e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_FAITHFUL_PRESENTATION_EVALUATION.json'),'utf8'));assert.equal(e.frontAuthority.preserved,true);assert.equal(e.sourcePlanterUnchanged,true);assert.equal(e.cameraRollDegrees,0);});
test('faithful planter presentation emits four non-front review views',()=>{for(const k of ['M30','M15','P15','P30'])assert.equal(fs.existsSync(path.join(out,`FAITHFUL_FINAL_${k}.png`)),true);});
test('faithful planter presentation emits a versioned Blender deliverable',()=>assert.equal(fs.existsSync(path.join(out,'GG-VEG-PLANTER-SHRUB-001_FRONT_AUTHORITY_2_5D_1.0.0.blend')),true));
test('delivered Blender asset has measured geometry depth and no anonymous meshes',()=>{const e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_FAITHFUL_PRESENTATION_EVALUATION.json'),'utf8'));assert.equal(e.topologyPass,true);assert.ok(e.topology.observedLeafMeshes>=26);assert.ok(e.topology.minimumLeafDepth>0);assert.ok(e.topology.maximumPlanterDepth>0);assert.equal(e.topology.anonymousGeometryCount,0);});
