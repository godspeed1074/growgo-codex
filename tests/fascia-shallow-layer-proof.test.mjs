import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),cal=path.join(root,'test-output/fascia-layer-calibration'),out=path.join(root,'test-output/fascia-shallow-proof/output'),e=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/FASCIA_SHALLOW_LAYER_PRODUCTION_EVIDENCE.json')));
test('fascia source layers and 2D gates pass',()=>{assert.equal(e.status,'PASS');assert.equal(e.pngRoundtrip,'PASS');assert.equal(e.twoDReassembly,'PASS');assert.equal(e.fullCropReconstruction,'PASS');assert.equal(e.fullSourceBackplateUsed,false);assert.equal(e.scope.coveragePercent,100);});
test('fascia real Blender proof outputs exist',()=>{for(const f of ['FASCIA_FIDELITY_FRONT.png','FASCIA_PAPERCUT_FRONT.png','FASCIA_GAMEPLAY.png','FASCIA_CLOSEUP.png','FASCIA_FRONT.png','FASCIA_BACK.png','FASCIA_LEFT.png','FASCIA_RIGHT.png','FASCIA_COMPONENT_ID.png','FASCIA_BLACK_SILHOUETTE.png','FASCIA_COMPARISON_BOARD.png','FASCIA_SHALLOW_LAYER_PROOF.blend'])assert.equal(fs.existsSync(path.join(out,f)),true,f);});
test('fascia visible pixels, silhouette, four-side, components, and budget pass',()=>{for(const v of Object.values(e.visiblePixelChecks))assert.equal(v,'YES');assert.equal(e.silhouette.status,'PASS');assert.equal(e.fourSide.status,'PASS');assert.deepEqual(e.componentIds,['FASCIA_BODY','FASCIA_TOP_CAP','FASCIA_BOTTOM_TRIM','SIGN_RECESS','SIGN_BORDER','SIGN_FACE','SIGN_LETTERING','SIGN_DECORATION']);assert.equal(e.anonymousGeometry,0);assert.equal(e.budget.mobileBudget,'PASS');assert.equal(e.permanentFasciaVersionPromoted,false);assert.equal(e.layerBAssembled,false);});
