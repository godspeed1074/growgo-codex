import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/plant-final-presentation-cleanup');test('cleanup removes only old planter pixels while retaining locked foliage pixels',()=>{const r=JSON.parse(fs.readFileSync(path.join(out,'FOLIAGE_ALPHA_CLEANUP_RESULT.json'),'utf8'));assert.equal(r.status,'PASS_ALPHA_SEPARATION');assert.equal(r.records.length,5);for(const x of r.records){assert.ok(x.oldPlanterPixelsRemoved>0);assert.equal(x.foliagePixelDrift,0);assert.equal(x.flowerPixelDrift,0);assert.ok(fs.existsSync(path.join(out,`VIEW_${x.view}_FOLIAGE_ONLY.png`)));}});
