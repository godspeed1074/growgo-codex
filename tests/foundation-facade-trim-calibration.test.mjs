import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/foundation-shell-calibration'),r=JSON.parse(fs.readFileSync(path.join(out,'SHOP_CALIBRATED_RESULT.json')));test('foundation and trim shell calibration preserves heroes and outputs',()=>{for(const f of ['SHOP_SHELLCAL_FRONT.png','SHOP_SHELLCAL_GAMEPLAY.png','SHOP_SHELLCAL_HERO.png','SHOP_SHELLCAL_CLOSEUP.png','SHOP_SHELLCAL_BACK.png','SHOP_SHELLCAL_LEFT.png','SHOP_SHELLCAL_RIGHT.png','SHOP_SHELLCAL_COMPONENT_ID.png','SHOP_SHELLCAL_GOLDEN_COMPARISON_BOARD.png','SHOP_SHELLCAL_FOUR_SIDE_BOARD.png','SHOP_SHELLCAL_LAYER_B.blend'])assert.ok(fs.existsSync(path.join(out,f)),f);assert.equal(r.budget.anonymousGeometryCount,0);assert.equal(r.budget.mobileBudget,'PASS');});
