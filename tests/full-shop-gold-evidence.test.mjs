import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),out=path.join(root,'test-output/full-shop-gold-evidence'),r=JSON.parse(fs.readFileSync(path.join(out,'SHOP_FULL_RESULT.json')));const ids=['WALL','FOUNDATION','WINDOW','DOOR','AWNING','FASCIA','SIGN','TRIM','PLANTER','SHRUB'];
test('full shop real-worker evidence and budgets',()=>{assert.equal(r.status,'PASS');assert.equal(r.recipeId,'GG-REC-BLD-SIMPLE-SHOP-GROWGO-001');assert.equal(r.recipeVersion,'1.0.0');assert.ok(r.budget.materials<=32);assert.equal(r.budget.anonymousGeometryCount,0);for(const id of ids)assert.ok(r.componentIds.includes(id),id);for(const f of ['SHOP_FULL_GAMEPLAY.png','SHOP_FULL_HERO.png','SHOP_FULL_CLOSEUP.png','SHOP_FULL_FRONT.png','SHOP_FULL_BACK.png','SHOP_FULL_LEFT.png','SHOP_FULL_RIGHT.png','SHOP_FULL_COMPONENT_ID.png','SHOP_FULL_LAYER_B.blend'])assert.ok(fs.existsSync(path.join(out,f)),f)});
test('gold evidence and recipe protection',()=>{const ev=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_GOLD_STANDARD_EVIDENCE.json')));assert.equal(ev.status,'EVIDENCE_CAPTURED_NOT_FROZEN');assert.equal(ev.knownGoodBuildId,null);assert.ok(fs.existsSync(path.join(out,'SHOP_FULL_GOLDEN_REFERENCE_COMPARISON_BOARD.png')));assert.ok(fs.existsSync(path.join(out,'SHOP_FULL_FOUR_SIDE_BOARD.png')));const recipe=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_LAYER_B_RECIPE.json')));assert.equal(recipe.recipeId,'GG-REC-BLD-SIMPLE-SHOP-GROWGO-001');assert.equal(recipe.recipeVersion,'1.0.0')});
