import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),mod=path.join(root,'asset-factory/modular'),out=path.join(root,'test-output/plant-level-camera');test('four non-front foliage views use one zero-roll level camera and preserve immutable front',()=>{const e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_LEVEL_CAMERA_PRESENTATION_EVALUATION.json'),'utf8'));for(const k of ['M30','M15','P15','P30']){assert.ok(fs.existsSync(path.join(out,`VIEW_${k}_FOLIAGE_ONLY_LEVEL.png`)));assert.ok(fs.existsSync(path.join(out,`PLANT_LEVEL_FINAL_${k}.png`)));}assert.equal(e.view0ChecksumPreserved,true);assert.equal(e.newCameraRollDegrees,0);assert.equal(e.status,'BLOCKED_LEVEL_CAMERA_COMPOSITE_SCALE');});
