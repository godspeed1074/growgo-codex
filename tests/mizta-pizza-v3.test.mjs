import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'../asset-factory/modular');const r=n=>JSON.parse(fs.readFileSync(path.join(root,n)));test('V3 uses only permitted structural and presentation deltas',()=>{const d=r('MIZTA_PIZZA_V3_VARIANT_DELTA.json');assert.equal(d.newStructuralArchitecture,0);assert.equal(d.unexpectedStructuralDelta.length,0)});test('V3 pizza display is contained, while glass ordering remains an explicit block',()=>{const b=r('MIZTA_PIZZA_V3_DISPLAY_BOUNDS_AUDIT.json');assert.equal(b.pixelsOutsideApprovedDisplayBounds,0);assert.equal(b.pizzaBehindGlass,'UNPROVEN');assert.equal(b.pizzaAwningOverlapPixels,0)});test('V3 keeps approved sources protected',()=>assert.ok(Object.values(r('MIZTA_PIZZA_V3_SOURCE_PROTECTION_AUDIT.json')).filter(x=>x==='NO').length>=6));
