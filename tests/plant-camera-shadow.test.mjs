import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),mod=path.join(root,'asset-factory/modular'),out=path.join(root,'test-output/plant-camera-shadow');test('camera-shadow proof preserves negative space but is not promoted when gameplay cone fails',()=>{for(const n of ['PLANT_HIDDEN_VISIBILITY_FRONT_TEST_B.png','PLANT_CAMERA_SHADOW_LEFT_15.png','PLANT_CAMERA_SHADOW_RIGHT_15.png','PLANT_CAMERA_SHADOW_LEFT_30.png','PLANT_CAMERA_SHADOW_RIGHT_30.png'])assert.ok(fs.existsSync(path.join(out,n)),n);let e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_CAMERA_SHADOW_2_5D_EVALUATION.json'),'utf8'));assert.equal(e.negativeSpaceIntrusionPixels,0);assert.equal(e.hiddenMagentaVisiblePixels,0);assert.equal(e.authorityLock,'NOT_LOCKED');assert.equal(e.shopUnchanged,true);});
