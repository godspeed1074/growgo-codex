import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),mod=path.join(root,'asset-factory/modular'),out=path.join(root,'test-output/plant-depth-fins');test('depth fins retain an invisible front diagnostic and are withheld when gameplay cone remains cards',()=>{for(const n of ['PLANT_DEPTH_FIN_MAGENTA_FRONT_B.png','PLANT_DEPTH_FIN_LEFT_20.png','PLANT_DEPTH_FIN_RIGHT_20.png','PLANT_DEPTH_FIN_LEFT_30.png','PLANT_DEPTH_FIN_RIGHT_30.png','LEAF_001_FIN_FRONT.png','LEAF_001_FIN_LEFT_30.png'])assert.ok(fs.existsSync(path.join(out,n)),n);let e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_DEPTH_FIN_GAMEPLAY_EVALUATION.json'),'utf8'));assert.equal(e.frontFinLeakagePixels,0);assert.equal(e.authorityLock,'NOT_LOCKED');assert.equal(e.shopUnchanged,true);});
