import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),mod=path.join(root,'asset-factory/modular'),out=path.join(root,'test-output/plant-five-view-impostor');
test('five-view impostor contains transparent render evidence for every runtime azimuth',()=>{for(const n of ['VIEW_M30_RGBA.png','VIEW_M15_RGBA.png','VIEW_0_RGBA.png','VIEW_P15_RGBA.png','VIEW_P30_RGBA.png','PLANTER_DEPTH_A_P30.png','PLANTER_DEPTH_B_P30.png','PLANTER_DEPTH_C_P30.png'])assert.ok(fs.existsSync(path.join(out,n)),n);});
test('five-view presentation preserves the immutable source and does not authorize Atlas changes',()=>{const e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_FIVE_VIEW_IMPOSTOR_EVALUATION.json'),'utf8')),m=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_FIVE_VIEW_IMPOSTOR_MANIFEST.json'),'utf8'));assert.equal(e.underlyingModularSourcePreserved,true);assert.equal(e.view0IoU,100);assert.equal(e.planter,'FAIL_SHARED_3D_DERIVATIVE_READS_AS_TALL_SIDE_SLAB');assert.equal(e.knownGood,'NO');assert.match(m.atlasCompatibility,/NO/);assert.equal(e.shopUnchanged,true);});
