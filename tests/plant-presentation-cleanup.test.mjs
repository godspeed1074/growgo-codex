import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),mod=path.join(root,'asset-factory/modular');test('presentation cleanup records exact foliage preservation and prohibits unsafe camera-mismatched compositing',()=>{const e=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_FINAL_PRESENTATION_CLEANUP_EVALUATION.json'),'utf8'));assert.equal(e.oldPlanterRemovedFromFiveFoliageImages,true);assert.equal(e.foliagePixelDrift,0);assert.equal(e.flowerPixelDrift,0);assert.equal(e.shared3DPlanterComposited,false);assert.equal(e.status,'BLOCKED_CAMERA_CONTRACT_MISMATCH');assert.equal(e.shopUnchanged,true);});
