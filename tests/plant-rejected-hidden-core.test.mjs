import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),mod=path.join(root,'asset-factory/modular');
test('rejected hidden foliage core cannot replace the locked front authority',()=>{const r=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_REJECTED_HIDDEN_FOLIAGE_CORE_001.json'),'utf8')),s=JSON.parse(fs.readFileSync(path.join(mod,'PLANT_26_LEAF_PRODUCTION_STATUS.json'),'utf8'));assert.equal(r.status,'REJECTED');assert.equal(r.frontAuthority.preserved,true);assert.equal(r.neverPromotedToLayerA,true);assert.equal(s.activePlantAuthority.status,'LOCKED_AND_PRESERVED');assert.equal(s.activePlantAuthority.hiddenGeometryAuthority,'NOT_GRANTED');});
