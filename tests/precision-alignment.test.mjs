import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/precision-alignment'),r=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_PRECISION_ALIGNMENT_RESULT.json')));test('precision alignment outputs and protected gates pass',()=>{for(const f of ['SHOP_PRECISION_FRONT.png','SHOP_PRECISION_GAMEPLAY.png','SHOP_PRECISION_HERO.png','SHOP_PRECISION_CLOSEUP.png','SHOP_PRECISION_BACK.png','SHOP_PRECISION_LEFT.png','SHOP_PRECISION_RIGHT.png','SHOP_PRECISION_COMPONENT_ID.png','SHOP_PRECISION_GOLDEN_COMPARISON_BOARD.png','SHOP_PRECISION_FOUR_SIDE_BOARD.png','SHOP_PRECISION_LAYER_B.blend'])assert.ok(fs.existsSync(path.join(out,f)),f);assert.equal(r.protectedInternals,'PASS');assert.equal(r.steamDeck,'PASS');assert.equal(r.fourSide,'PASS');assert.equal(r.componentIds,'PASS');assert.equal(r.anonymousGeometry,0);assert.equal(r.budget.mobileBudget,'PASS');assert.equal(r.knownGoodBuildId,null);});
