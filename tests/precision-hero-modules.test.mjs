import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),out=path.join(root,'test-output/precision-hero-modules'),ids=['GG-BLD-AWNING-SHOP-FABRIC-001','GG-BLD-WINDOW-SHOP-LARGE-002','GG-BLD-FASCIA-SHOP-NAVY-001','GG-BLD-DOOR-SHOP-002','GG-VEG-PLANTER-SHRUB-001'];
test('precision hero modules have real-worker evidence',()=>{for(const id of ids){const d=path.join(out,`${id}@4.0.0`);const b=JSON.parse(fs.readFileSync(path.join(d,'BUDGET.json')));assert.equal(b.anonymousGeometryCount,0);assert.ok(b.triangles<=1200);for(const f of ['FRONT.png','BACK.png','LEFT.png','RIGHT.png','GAMEPLAY.png','CLOSE_UP.png','SILHOUETTE.png','COMPONENT_ID.png'])assert.ok(fs.existsSync(path.join(d,f)),`${id}/${f}`)}});
test('precision board and protected recipe exist',()=>{assert.ok(fs.existsSync(path.join(out,'SHOP_PRECISION_HERO_MODULE_REVIEW_BOARD.png')));const g=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_GOLD_STANDARD_EVIDENCE_PRECISION_FEATURES.json')));assert.equal(g.layerBAssembled,false);assert.equal(g.goldStandardFrozen,false);const recipe=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_LAYER_B_RECIPE.json')));assert.equal(recipe.recipeId,'GG-REC-BLD-SIMPLE-SHOP-GROWGO-001')});
