import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/real-wall-panel'),v=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_CODEX_VISION_REVIEW.json')));test('real wall panel and vision review artifacts exist',()=>{for(const f of ['SHOP_REALWALL_FRONT.png','SHOP_REALWALL_GAMEPLAY.png','SHOP_REALWALL_HERO.png','SHOP_REALWALL_CLOSEUP.png','SHOP_REALWALL_COMPONENT_ID.png','SHOP_REALWALL_GOLDEN_COMPARISON_BOARD.png','SHOP_REALWALL_FOUR_SIDE_BOARD.png','SHOP_REALWALL_HELPER_VISIBILITY_AUDIT.png'])assert.ok(fs.existsSync(path.join(out,f)),f);assert.equal(v.verdict,'VISION_METRIC_CONFLICT');assert.equal(v.whatCodexActuallySees.palePlaceholderGeometryVisible,'NO');});
