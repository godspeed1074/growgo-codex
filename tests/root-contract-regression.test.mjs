import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/root-regression-shop');test('regression shop rebuild completes with persistent root worker',()=>{for(const f of ['SHOP_CALIBRATED_GAMEPLAY.png','SHOP_CALIBRATED_FRONT.png','SHOP_CALIBRATED_HERO.png','SHOP_CALIBRATED_COMPONENT_ID.png','SHOP_CALIBRATED_LAYER_B.blend','SHOP_CALIBRATED_RESULT.json'])assert.ok(fs.existsSync(path.join(out,f)),f);const r=JSON.parse(fs.readFileSync(path.join(out,'SHOP_CALIBRATED_RESULT.json')));assert.equal(r.status,'PASS');assert.equal(r.budget.anonymousGeometryCount,0);});
