import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/root-contract-proof'),r=JSON.parse(fs.readFileSync(path.join(out,'ROOT_CONTRACT_RESULT.json')));test('persistent roots and responsive opening proof pass',()=>{assert.equal(r.rootTransformInheritance,'PASS');assert.equal(r.visibleArtBounds,'PASS');assert.equal(r.transparentPaddingCompensation,'PASS');assert.equal(r.doorPlus25.rendered,'PASS');assert.equal(r.doorPlus25.openingUpdated,'PASS');assert.equal(r.doorPlus25.wallPanelReflow,'PASS');assert.equal(r.doorPlus25.landingUpdated,'PASS');assert.equal(r.windowPlus20.rendered,'PASS');assert.equal(r.windowPlus20.openingUpdated,'PASS');assert.equal(r.windowPlus20.wallPanelReflow,'PASS');assert.equal(r.temporaryTransformsRestored,true);assert.equal(r.anonymousGeometry,0);for(const f of ['ROOT_CONTRACT_BASELINE.png','ROOT_CONTRACT_DOOR_PLUS25.png','ROOT_CONTRACT_WINDOW_PLUS20.png','ROOT_CONTRACT_PROOF.blend'])assert.ok(fs.existsSync(path.join(out,f)),f);});
