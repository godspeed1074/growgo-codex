import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/shop-final-plant-review'),mod=path.join(root,'asset-factory/modular');
test('plant-only shop review preserves locked shop and camera',()=>{const r=JSON.parse(fs.readFileSync(path.join(out,'SHOP_FINAL_PLANT_INTEGRATION_RESULT.json'),'utf8'));assert.equal(r.status,'PASS');assert.equal(r.sourceShopUnchanged,true);assert.equal(r.cameraUnchanged,true);assert.equal(r.anonymousGeometryCount,0);assert.equal(r.invariants.atlasModified,false);});
test('operator board and every required plant review render exist',()=>{for(const f of ['SHOP_FINAL_PLANT_FRONT.png','SHOP_FINAL_PLANT_GAMEPLAY_1X.png','SHOP_FINAL_PLANT_GAMEPLAY_2X.png','SHOP_FINAL_PLANT_HERO.png','SHOP_PLANT_M15.png','SHOP_PLANT_P15.png','SHOP_PLANT_M30.png','SHOP_PLANT_P30.png','SHOP_WITH_FINAL_PLANT_OPERATOR_REVIEW_BOARD.png'])assert.equal(fs.existsSync(path.join(out,f)),true);});
