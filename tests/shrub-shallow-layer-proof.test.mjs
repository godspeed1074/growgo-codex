import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'),cal=path.join(root,'test-output/shrub-layer-calibration'),out=path.join(root,'test-output/shrub-shallow-proof/output'),e=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHRUB_SHALLOW_LAYER_PRODUCTION_EVIDENCE.json')));
test('shrub organic source layers and 2D gates pass',()=>{assert.equal(e.status,'PASS');assert.equal(e.pngRoundtrip,'PASS');assert.equal(e.twoDReassembly,'PASS');assert.equal(e.fullCropReconstruction,'PASS');assert.equal(e.fullSourceBackplateUsed,false);assert.equal(e.scope.foregroundMismatchPixels,0);});
test('shrub real proof outputs exist',()=>{for(const f of ['SHRUB_FIDELITY_FRONT.png','SHRUB_PAPERCUT_FRONT.png','SHRUB_GAMEPLAY.png','SHRUB_CLOSEUP.png','SHRUB_FRONT.png','SHRUB_BACK.png','SHRUB_LEFT.png','SHRUB_RIGHT.png','SHRUB_COMPONENT_ID.png','SHRUB_BLACK_SILHOUETTE.png','SHRUB_COMPARISON_BOARD.png','SHRUB_SHALLOW_LAYER_PROOF.blend'])assert.equal(fs.existsSync(path.join(out,f)),true,f);});
test('shrub visible-pixel, silhouette, four-side, component, and budget gates pass',()=>{for(const v of Object.values(e.visiblePixelChecks))assert.equal(v,'YES');assert.equal(e.silhouette.status,'PASS');assert.equal(e.papercutDepth.status,'PASS');assert.equal(e.fourSide.status,'PASS');assert.equal(e.anonymousGeometry,0);assert.equal(e.budget.mobileBudget,'PASS');assert.equal(e.permanentShrubVersionPromoted,false);assert.equal(e.layerBAssembled,false);});
