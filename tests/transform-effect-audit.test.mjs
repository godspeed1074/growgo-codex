import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/transform-effect-audit'),e=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_DOOR_TRANSFORM_AUDIT.json')));test('transform effect audit proves isolated scale changes rendered pixels',()=>{for(const f of ['DOOR_ISOLATED_BOUNDS.png','DOOR_TRANSFORM_EXAGGERATION_TEST.png','WINDOW_ISOLATED_BOUNDS.png','WINDOW_TRANSFORM_EXAGGERATION_TEST.png','SHOP_TRANSFORM_EFFECT_AUDIT_BOARD.png'])assert.ok(fs.existsSync(path.join(out,f)),f);assert.ok(e.renderedBounds.doorExaggerated.height>e.renderedBounds.doorIsolated.height);assert.ok(e.renderedBounds.windowExaggerated.width>e.renderedBounds.windowIsolated.width);assert.equal(e.temporaryTransformsRestored,true);assert.ok(e.classifications.includes('TRANSFORM_PIPELINE_PASS'));});
