import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/upper-shell-final');
test('upper shell convergence keeps protected baseline and required evidence',()=>{const d=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_UPPER_SHELL_VISION_DIFFERENCES.json')));assert.equal(d.status,'NEEDS_CORRECTION');assert.deepEqual(d.protectedModules,['DOOR','WINDOW','SHRUB']);for(const n of ['SHOP_EXACTMATCH_FRONT.png','SHOP_EXACTMATCH_GAMEPLAY.png','SHOP_EXACTMATCH_HERO.png','SHOP_EXACTMATCH_CLOSEUP.png','SHOP_EXACTMATCH_BACK.png','SHOP_EXACTMATCH_LEFT.png','SHOP_EXACTMATCH_RIGHT.png','SHOP_EXACTMATCH_COMPONENT_ID.png','SHOP_UPPER_SHELL_CONVERGENCE_BOARD.png','SHOP_EXACTMATCH_FOUR_SIDE_BOARD.png'])assert.equal(fs.existsSync(path.join(out,n)),true,n);});
