import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/visible-facade-sanity'),e=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_VISIBLE_FACADE_SANITY_AUDIT.json')));test('visible facade crops and contradiction audit exist',()=>{for(const f of ['SHOP_REFERENCE_VISIBLE_FACADE.png','SHOP_BLENDER_VISIBLE_FACADE.png','SHOP_VISIBLE_FACADE_SANITY_BOARD.png'])assert.ok(fs.existsSync(path.join(out,f)),f);assert.equal(e.metricsTrustworthy,false);assert.equal(e.productionAssetChanged,false);assert.ok(Math.abs(e.doorVisibleHeightErrorPct)>10);assert.equal(e.wallPanelAudit.status,'WALL_PANEL_VISUAL_PLACEHOLDER_FAIL');});
