import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');
test('vision convergence records bounded review and vision-over-metrics gate',()=>{const inv=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_VISION_DIFFERENCE_INVENTORY.json'))),review=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_CODEX_VISION_REVIEW.json'))),std=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/GROWGO_REFERENCE_LAYERED_MODULE_STANDARD_DRAFT.json')));assert.equal(inv.referenceAuthority,'IMMUTABLE_GOLDEN_REFERENCE');assert.equal(review.finalVerdict,'VISION_NEEDS_CORRECTION');assert.equal(review.internalIterationsUsed,3);assert.equal(review.whatCodexActuallySees.unintendedVisibleGeometry,'NO');assert.equal(std.visualConvergence.maxInternalIterations,3);assert.equal(std.visualConvergence.visionOverridesMetrics,true);assert.equal(std.visualConvergence.knownGoodAutoPromotion,false);});
