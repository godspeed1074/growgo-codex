import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/wall-panel-integration'),a=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_FACADE_CONTACT_AUDIT.json')));test('wall panel integration outputs and contacts pass',()=>{for(const f of ['SHOP_PANELCAL_FRONT.png','SHOP_PANELCAL_GAMEPLAY.png','SHOP_PANELCAL_HERO.png','SHOP_PANELCAL_CLOSEUP.png','SHOP_PANELCAL_BACK.png','SHOP_PANELCAL_LEFT.png','SHOP_PANELCAL_RIGHT.png','SHOP_PANELCAL_COMPONENT_ID.png','SHOP_PANELCAL_GOLDEN_COMPARISON_BOARD.png','SHOP_PANELCAL_FOUR_SIDE_BOARD.png','SHOP_PANELCAL_LAYER_B.blend'])assert.ok(fs.existsSync(path.join(out,f)),f);for(const v of Object.values(a).filter(v=>typeof v==='string'))assert.equal(v,'CONTACT_PASS');assert.deepEqual(a.floatingComponents,[]);});
