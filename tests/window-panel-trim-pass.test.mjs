import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'),out=path.join(root,'test-output/window-panel-pass'),audit=JSON.parse(fs.readFileSync(path.join(root,'asset-factory/modular/SHOP_DOOR_BACKING_AUDIT.json')));
test('window panel and upper trim integration evidence is complete',()=>{for(const n of ['SHOP_WINDOWPANEL_FRONT.png','SHOP_WINDOWPANEL_GAMEPLAY.png','SHOP_WINDOWPANEL_HERO.png','SHOP_WINDOWPANEL_CLOSEUP.png','SHOP_WINDOWPANEL_BACK.png','SHOP_WINDOWPANEL_LEFT.png','SHOP_WINDOWPANEL_RIGHT.png','SHOP_WINDOWPANEL_COMPONENT_ID.png','SHOP_WINDOWPANEL_GOLDEN_COMPARISON_BOARD.png','SHOP_WINDOWPANEL_FOUR_SIDE_BOARD.png'])assert.equal(fs.existsSync(path.join(out,n)),true,n);assert.equal(audit.windowPanelsVisible,true);assert.equal(audit.upperTrimConnected,true);assert.equal(audit.protectedHeroModules,'PASS');assert.equal(audit.visionVerdict,'VISION_METRIC_CONFLICT');});
